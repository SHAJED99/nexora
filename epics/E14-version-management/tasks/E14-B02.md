---
id: E14-B02
epic: E14
type: bug
title: "`EARS-VER-1`/`FR-VER-006`'s \"block application communication\" is not implemented — `AppBinding` is `GetMaterialApp.initialBinding`, so the mesh coordinator, inbound pipeline, link-quality feed, background service and notification dispatcher all start unconditionally, including behind the mandatory-update screen"
layer: backend
size: M
status: done
owner_agent: builder
model: any
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
severity: S2
priority: { moscow: must, p: P1 }
depends_on: [E14-B01]
blocks: []
traces_to: [FR-VER-006, FR-VER-010]
required_context:
  - epics/E14-version-management/epic.md
  - epics/E14-version-management/tasks/E14-T04.md
  - lib/app/main.dart
  - lib/app/bindings.dart
  - lib/core/messaging/messaging_coordinator.dart
found_by: reviewer (claude-opus-5), E14 epic bug sweep 2026-09-06
found_in: "the seam between E14-T04's routing decision and E06-T11/E08-T06/E10-T10's startup wiring in AppBinding. T04's files: fence covers main.dart but not bindings.dart, so the half of FR-VER-006 that is not about UI had no owning task at all."
files:
  create: []
  update:
    - lib/app/bindings.dart
    - lib/app/main.dart
    - test/widget_test.dart
  delete: []
---
# E14-B02 · The mandatory-update screen blocks the UI but not communication

## The defect

`FR-VER-006` (`spec/srs.md:159`) has two clauses:

> When the installed version is UPDATE_REQUIRED, the system shall **block
> application communication** *and* present a non-dismissible mandatory
> update prompt directing the user to Google Play.

The epic's own `EARS-VER-1` repeats both. `E14-T04` implemented the second
clause (verified — see the sweep's own falsification below). **Nothing in
this epic implements the first.**

`main.dart:130-138` passes the version-derived `initialRoute` into
`GetMaterialApp`, but `initialBinding: AppBinding(...)` sits right beside it
and GetX runs `initialBinding.dependencies()` at app construction,
**independent of which route is shown**. That method starts everything:

- `bindings.dart:130` — `messagingStack.coordinator.start()`, which
  (`messaging_coordinator.dart:281`) calls `_inbound.start()`, subscribing
  the inbound pipeline to live transport data and starting the queue's
  `Timer.periodic`.
- `bindings.dart:154-160` — `LinkQualityFeed(...).start()`.
- `bindings.dart:224` — `backgroundObserver.start()`.
- `bindings.dart:342` — `notificationDispatcher.start()`.

So on a device pinned to `/version-update-required`, the app is still
routing, relaying, decrypting inbound frames, holding transport links open
and dispatching notifications. The user cannot *see* a conversation; the
device is nonetheless a fully participating mesh node running an
explicitly-unsupported build.

This matters most for exactly the case `FR-VER-010` exists to serve: an
emergency minimum-version bump published *because* the old build's protocol
or crypto handling is unsafe. Blocking only the UI leaves the unsafe code
paths running.

## Repro

Because `E14-B01` currently makes the update screen unreachable, reproduce
against the composition rather than a live device:

1. Check out `epic_14`.
2. Read `lib/app/main.dart:139-146` — `initialBinding` and `initialRoute` are
   independent arguments to the same `GetMaterialApp`; there is no branch on
   `initialRoute` anywhere in `AppBinding`.
3. `grep -n "\.start()" lib/app/bindings.dart` — four unconditional starts,
   none gated on `VersionState`.
4. Or, after `E14-B01` is fixed: seed the cache with a `minimumSupportedBuild`
   above the installed build, launch, and observe the mandatory-update screen
   while `messagingStack.coordinator` is running.

## Expected

`FR-VER-006` / `EARS-VER-1`: under `UPDATE_REQUIRED`, application
communication is blocked — the messaging coordinator / inbound pipeline /
relay engine / background service are not started (or are started and
immediately stopped), and no frames are sent or processed.

## Actual

All four subsystems start unconditionally at app construction, whatever the
version state. `AppBinding` has no knowledge that `VersionState` exists.

## Severity

**S2** — a specced, security-relevant control is entirely absent, with no
workaround. Not S1: it is currently latent behind `E14-B01` (the screen is
unreachable at all today), and it is a missing mitigation rather than an
active exploit.

## Suggested fix shape (not binding)

Pass the evaluated `VersionState` (or a plain `bool blockCommunication`) from
`main.dart` into `AppBinding`, and gate the four `start()` calls on it.
Construction should stay unconditional — `MessagingStack.create`'s "does NOT
start anything" contract and the single-instance discipline in
`messaging_stack.dart:11-23` both depend on the composition root running
normally; only the *starts* should be suppressed. Note that
`initialRouteFor` in `main.dart` is already the pure, tested decision point,
so the same value can drive both.

## Regression test (required before close)

A widget/composition test that builds `NexoraApp` with
`VersionState.updateRequired`, and asserts the messaging coordinator's inbound
pipeline is **not** started (a call counter on the transport-data
subscription, per `L-feedback` on `fail()` inside injected seams — assert
`calls == 0`, never `fail('must not be called')`, which a broad catch would
swallow). Plus the mirror-image assertion under `upToDate` that it **is**
started, so the test cannot pass by breaking startup generally. Both must
fail on today's `epic_14` HEAD in the right direction.

## What this fix does NOT do

- Does NOT change `VersionUpdateView`, `VersionUpdateController`,
  `EvaluateVersionStateUseCase` or `VersionPolicyService`.
- Does NOT change `MessagingStack.create`'s "does NOT start anything"
  contract, nor construct anything conditionally — construction stays
  unconditional; only the lifecycle `start()` calls are gated.
- Does NOT add a mid-session re-check or a way to un-block without a relaunch.
- Does NOT change what `UPDATE_AVAILABLE` does — that state is explicitly
  non-blocking (`FR-VER-005`) and must keep starting everything.
- Does NOT fix `E14-B01`; this fix is only observable once that one lands.

## Implementation checklist
- [x] `AppBinding` gains a `blockCommunication` field (default `false`),
      threaded through its constructor — construction/registration of
      every subsystem (`messagingStack`, `LinkQualityFeed`,
      `backgroundObserver`, `notificationDispatcher`, every `Get.put`)
      stays unconditional; only the four `.start()` calls are gated `59a2a5f`
- [x] `messagingStack.coordinator.start()` gated `59a2a5f`
- [x] `LinkQualityFeed(...).start()` gated (construction/`Get.put`
      unconditional, `.start()` alone gated) `59a2a5f`
- [x] `backgroundObserver.start()` gated (construction/`Get.put`
      unconditional) `59a2a5f`
- [x] `notificationDispatcher.start()` (the `unawaited(...catchError(...))`
      call) gated (construction/`Get.put` unconditional) `59a2a5f`
- [x] `main.dart`'s `NexoraApp` gains a `blockCommunication` field
      (default `false`), threaded into `AppBinding`; `main()` computes it
      as `versionState == VersionState.updateRequired` from the SAME
      `versionState` E14-B01's fix now actually populates `59a2a5f`
- [x] regression test written
      (`test/widget_test.dart`, group "E14-B02 — AppBinding.blockCommunication
      gates the four starts"): builds a real `MessagingStack` over a
      `_CountingTransportService` (a `TransportService` subclass counting
      accesses to `.discoveredDevices` — the one stream both
      `MessagingCoordinator.start()` and `InboundPipeline.start()` read
      before subscribing to anything else), calls
      `AppBinding(..., blockCommunication: true).dependencies()`, asserts
      the count is `0` — a plain counter (`expect(..., 0)`), never a
      `fail()` inside the seam itself (per this project's own lesson on
      `fail()` in injected seams being swallowed by a broad catch) `59a2a5f`
- [x] mirror-image test in the same group: `blockCommunication: false`
      (the default, matching `upToDate`/`updateAvailable`) still starts
      everything — asserts the count is `greaterThan(0)`, so the fix
      cannot pass by disabling startup unconditionally `59a2a5f`
- [x] confirmed both new tests fail against the pre-fix shape in the
      right direction (see Run log) and pass after `59a2a5f`
- [x] `flutter analyze` clean (1 pre-existing, unrelated info finding —
      same as baseline), `flutter test` green (1182/1182) `59a2a5f`
- [x] **Round 2 (coverage-gap fix, 2026-09-06)**: review found the original
      regression-test group only actually proved ONE of the four gated
      subsystems (the messaging coordinator) — deleting the other three
      gates (`linkQualityFeed.start()`, `backgroundObserver.start()`,
      `notificationDispatcher.start()`) one at a time left the full suite
      green at 1182/1182. Extended the SAME test group, test-only, no
      production code change:
      - `_CountingTransportService` gained a `linkQualityAccessCount`
        counter on its `linkQuality` getter (proxy for
        `LinkQualityFeed.start()`'s one subscription)
      - new `_CountingBackgroundControl extends BackgroundStub` counts
        accesses to `.state`/`.powerStates` (proxy for
        `BackgroundLifecycleObserver.start()`'s two subscriptions), passed
        via `AppBinding`'s existing `backgroundControl:` test seam
      - a mocked `NotificationApi.ensureChannels` Pigeon channel counts
        invocations (proxy for `NotificationDispatcher.start()` ever
        reaching `NotificationService.ensureReady()`)
      All three are plain counters asserted with `expect(..., 0)`/
      `expect(..., greaterThan(0))`, never `fail()` inside an injected
      seam, matching this project's own lesson and the existing
      `discoveredDevicesAccessCount` pattern

## Run log
- **Red confirmed before the fix**: temporarily removed the
  `if (!blockCommunication) { ... }` guard around
  `messagingStack.coordinator.start()` in `bindings.dart` (reverting to an
  unconditional call, the pre-fix shape) and reran
  `test/widget_test.dart` — the blocking-case test failed exactly as
  expected: `Expected: <0> Actual: <2>` (both
  `MessagingCoordinator.start()`'s own `discoveredDevices.listen(...)` and
  `InboundPipeline.start()`'s own `discoveredDevices.listen(...)`
  subscribed). Restored the fix immediately after confirming (no commit
  of the reverted state).
- **Mirror-image**: with the fix in place and `blockCommunication: false`
  (or omitted), the same test group's second test passes — the mesh still
  starts normally (count `2`, `greaterThan(0)`), proving the fix does not
  simply disable startup unconditionally.
- **Why `discoveredDevices` access-count, not a literal
  `incomingData`-subscription count**: the bug's own regression-test note
  says "a call counter on the transport-data subscription". Directly
  counting `incomingData(deviceId)` subscriptions would require driving a
  full discover -> connect -> data event sequence through the mocked
  Pigeon channel (as `inbound_pipeline_test.dart` does) just to prove a
  *negative*. `.discoveredDevices` is the one stream both
  `MessagingCoordinator.start()` (`messaging_coordinator.dart:287`) and
  `InboundPipeline.start()` (`inbound_pipeline.dart:295`) subscribe to
  synchronously and unconditionally the moment either is started — before
  any device, connection, or incoming byte exists — and no other
  production code path reads it before a route's own controller is
  resolved (`devices_controller.dart`/`dashboard_controller.dart` also
  read it, but both are `lazyPut`, never constructed by
  `AppBinding.dependencies()` itself, and this test never navigates to
  either route). So a `0` count is a direct, non-fragile proof that
  neither `.start()` ran, without needing to simulate the rest of the
  chain.
- **Full suite**: `flutter test` → 1182/1182 (baseline 1179 + this bug's
  2 new tests + E14-B01's 1, fixed together).
- **`flutter analyze`**: 1 issue, the same pre-existing, unrelated info
  finding as baseline (`annotate_overrides`,
  `test/core/calls/call_migration_controller_test.dart:177`) — no new
  findings. (Two analyzer findings this fix's own new test file
  temporarily introduced — an unused `flutter/services.dart` import and
  an unnecessary `transport_api.g.dart` import in `test/widget_test.dart`
  — were caught and removed before this final run; not present in the
  committed diff.)

- **Round 2 (coverage-gap fix, 2026-09-06)** — falsified each of the three
  newly-proven gates individually, restoring immediately after each:
  - Reverted `if (!blockCommunication) { linkQualityFeed.start(); }` to an
    unconditional `linkQualityFeed.start();` — the blocking-case test
    failed exactly as expected: `Expected: <0> Actual: <1>` on
    `transport.linkQualityAccessCount`. Restored; reran green.
  - Reverted `if (!blockCommunication) { backgroundObserver.start(); }` to
    an unconditional `backgroundObserver.start();` — failed exactly as
    expected: `Expected: <0> Actual: <1>` on
    `backgroundControl.stateAccessCount`. Restored; reran green.
  - Reverted the `if (!blockCommunication) { unawaited(notificationDispatcher
    .start()...); }` guard to an unconditional call — failed exactly as
    expected: `Expected: <0> Actual: <1>` on `ensureChannelsCallCount`.
    Restored; reran green.
  - `git diff lib/app/bindings.dart` confirmed empty after all three
    restorations — no production code touched by this round.
  - Full suite re-run after all three restorations: `flutter test` →
    1182/1182 passing (same total; the fix only strengthens existing test
    bodies, adds no new top-level tests). `flutter analyze` → 1 issue, the
    same pre-existing `annotate_overrides` info finding, no new findings
    (the decoded-message-handler API,
    `messenger.setMockDecodedMessageHandler`, was used instead of the
    deprecated raw `BasicMessageChannel.setMockMessageHandler` specifically
    to avoid introducing new `deprecated_member_use` info findings).

## Self-review
- [x] Every §Implementation checklist item done, with the commit hash
- [x] `flutter analyze && flutter test` green (1 pre-existing unrelated
      info finding; 1182/1182 tests)
- [x] Diff confined to this task's `files:` — `lib/app/bindings.dart`,
      `lib/app/main.dart`, `test/widget_test.dart` (plus E14-B01's own
      `lib/app/main.dart`/`test/features/version/presentation/version_update_controller_test.dart`,
      both bugs fixed in the same commit per this dispatch's explicit
      coordination instruction, since this task `depends_on: [E14-B01]`
      and both fixes touch `main.dart`'s same call site)
- [x] `## What this fix does NOT do` respected —
      `VersionUpdateView`/`VersionUpdateController`/
      `EvaluateVersionStateUseCase`/`VersionPolicyService` untouched;
      `MessagingStack.create`'s "does NOT start anything" contract intact
      — every construction/registration in `AppBinding.dependencies()`
      stays unconditional, only the four `.start()` calls are gated; no
      mid-session re-check or un-block-without-relaunch path added;
      `UPDATE_AVAILABLE`/`UP_TO_DATE` still start everything (proven by
      the mirror-image test)
- [x] No secrets or PII logged — this fix adds no new logging
- [x] ADR-0002 (GetX MVC) honoured — `blockCommunication` is a plain
      constructor field on the existing `Bindings` subclass, no new
      pattern introduced
- [x] Introducing no counter/cursor — `discoveredDevicesAccessCount` is a
      test-only instrumentation field on a test double, not a production
      invariant, so the §6 counter-atomicity checklist does not apply

**Deviations from spec/design:**
_(none — the fix matches the task file's own "suggested fix shape"
exactly: a plain `blockCommunication` bool threaded from `main.dart`'s
already-evaluated `VersionState`, gating only the four named `.start()`
calls, construction left unconditional.)_

**Files actually touched:** `lib/app/bindings.dart`, `lib/app/main.dart`,
`test/widget_test.dart` (this task's own `files:` list), plus
`test/features/version/presentation/version_update_controller_test.dart`
(E14-B01's own `files:` list — both bugs fixed together in one commit per
this dispatch's explicit instruction).

### Definition of Done
- [x] The exact defect (unconditional `.start()` calls regardless of
      `VersionState`) is fixed — all four gated on `blockCommunication`
- [x] Regression test proven red before, green after, in the right
      direction, plus the required mirror-image assertion (see Run log)
- [x] `flutter analyze` clean (baseline's 1 pre-existing info finding
      only), `flutter test` green (1182/1182)
- [x] Independent review (rule 5) — pending, `status: review-requested`
