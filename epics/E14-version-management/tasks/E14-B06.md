---
id: E14-B06
epic: E14
type: bug
title: "`FR-VER-008`'s reconnect-triggered re-evaluation has no implementation at all — no connectivity hook exists to refresh the version-policy cache and re-route on network restoration"
layer: backend
size: M
status: done
owner_agent: builder
model: any
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
severity: S3
priority: { moscow: should, p: P2 }
depends_on: [E14-T01, E14-B01]
blocks: []
traces_to: [FR-VER-008]
required_context:
  - epics/E14-version-management/tasks/E14-B01.md
  - lib/core/services/version_policy_service.dart
  - lib/app/main.dart
found_by: reviewer (claude-opus-5), E14-B01/B02 fix review 2026-09-06
found_in: "E14-B01's own fix explicitly and correctly fenced this out (launch-time refresh only); this tracks the fenced-out half so it isn't silently claimed satisfied when the epic closes."
files:
  create:
    - lib/features/version/domain/version_reconnect_watcher.dart
    - test/features/version/domain/version_reconnect_watcher_test.dart
  update:
    - lib/app/main.dart
    - lib/core/services/firebase_paths.dart
    - lib/core/services/version_policy_service.dart
    - test/core/services/version_policy_service_test.dart
  delete: []
---
# E14-B06 · `FR-VER-008` reconnect re-evaluation is unimplemented

## The defect

`FR-VER-008` requires: on reconnect, the app shall fetch the current version
policy and re-evaluate. `E14-B01`'s fix (launch-time `refresh()` call in
`lib/app/main.dart`) correctly and explicitly left this out of its own scope
— there is no connectivity-change signal wired anywhere in this codebase that
could trigger a re-fetch mid-session. A user who launches the app while
offline (or before an emergency policy update is published), then later
reconnects without relaunching, never sees the updated policy or gets routed
to `/version-update-required` until their next cold start.

## Expected

`FR-VER-008`: reconnect (real network restoration during a live session)
triggers a fresh `VersionPolicyService.refresh()` and re-evaluation via
`EvaluateVersionStateUseCase`, re-routing to the mandatory-update screen if
the result changes to `updateRequired`.

## Actual

No connectivity-change hook exists in `lib/` that could drive this. Launch-
time evaluation (`E14-B01`) is the only trigger.

## Severity

**S3** — a real gap against a named FR, but narrower than `E14-B01`/`B02`:
`FR-VER-010`'s emergency-enforcement lever still works on next launch (most
messaging apps are relaunched at least daily), and this does not regress any
existing protection. Not blocking `E14`'s own `epic_14 -> development` merge
on its own, but should not be silently dropped — `FR-VER-008` must not be
marked satisfied until this lands.

## What this fix does NOT do

- Does NOT change `E14-B01`'s launch-time refresh or its fence.
- Does NOT invent a new connectivity primitive if one already exists
  elsewhere in the mesh/transport stack for a different purpose — check
  `TransportService`/`LinkQualityFeed`/`BackgroundObserver` first for an
  existing reconnect signal before adding a new one.
- Does NOT require polling; a reconnect *event* is sufficient, not a timer.

## Investigation

Checked `TransportService`/`LinkQualityFeed`/`BackgroundObserver`
(`BackgroundLifecycleObserver`, `lib/app/bindings.dart`) per this task's own
fence, before writing anything new:

- `TransportService.connectionState`/`connect()` (`lib/core/transport/
  transport_service.dart`) report per-peer-device mesh transport link state
  (Bluetooth/WiFi-Direct-style, via Pigeon) — a completely different signal
  from "can this app reach the Firebase project". `VersionPolicyService
  .refresh()` reads `config/version_policy` from Firebase Realtime Database,
  not from any mesh peer, so a mesh reconnect has no bearing on whether a
  fresh policy is fetchable.
- `LinkQualityFeed` is downstream of the same mesh `TransportService`
  signal, same conclusion.
- `BackgroundLifecycleObserver` tracks app lifecycle (foreground/background)
  and device power state (Doze/battery saver) — not connectivity at all.

Read `FR-VER-008` verbatim in `spec/srs.md:161`: *"The system shall cache
the last known valid version policy locally for offline use; on reconnect
it shall fetch the current policy and re-evaluate."* `VersionPolicyService
.refresh()` (the only fetch this FR could mean) reads exclusively from
Firebase Realtime Database (`lib/core/services/version_policy_service.dart`),
so "reconnect" here is reconnection to Firebase/the internet — not a mesh
transport concept, confirming neither existing signal above is the right
one to reuse.

No existing internet-connectivity signal exists anywhere in `lib/`
(confirmed by grep — no `connectivity_plus`, no `.info/connected` usage, no
`ConnectivityResult`). Rather than adding a new dependency (a 🧍
`new_dependency` gate), this fix reuses Firebase Realtime Database's own
built-in `.info/connected` special path — already available via the
`firebase_database` package this app already depends on, and it is
literally the connection state of the exact channel `refresh()` reads from.
See `FirebasePaths.infoConnected()`'s own doc comment.

## Fix

- `FirebasePaths.infoConnected()` — new registry entry for Firebase's
  built-in `.info/connected` path (`lib/core/services/firebase_paths.dart`).
- `VersionReconnectWatcher` (`lib/features/version/domain/
  version_reconnect_watcher.dart`) — new, injectable, event-driven class:
  subscribes to a `Stream<bool>` connectivity signal, tracks the previous
  value, and on a genuine `false -> true` transition calls
  `VersionPolicyService.refresh()` then composes
  **[Round 2 correction, F3]: this section originally also claimed the
  transition test alone could never re-fire `E14-B01`'s own launch-time
  check because a fresh subscription's very first event is exempt. That
  claim was wrong — see §Round 2 (F3) below for what actually happens at
  cold start and the real fix (`_everConnected`).**
  `EvaluateVersionStateUseCase` exactly the way `main.dart`'s own
  `evaluateVersionStateAtLaunch` already does, invoking an injected
  `onUpdateRequired` callback if the result is `VersionState.updateRequired`.
  No `Timer`, no polling — driven entirely by stream events.
- `lib/app/main.dart` — after `runApp`, constructs one
  `VersionReconnectWatcher` reusing the SAME `versionPolicyService`/
  `_readInstalledBuildNumber` `main()` already built for the launch-time
  check (never a second `VersionPolicyService`), wired to the real
  `FirebaseDatabase.instance.ref(FirebasePaths.infoConnected()).onValue`
  stream, and navigates via `Get.offNamed(Routes.versionUpdateRequired)` on
  `updateRequired` — the same forced-navigation shape
  `LoginController._signIn` already uses (`Get.offNamed`,
  `login_controller.dart:58`), adapted for a mid-session re-route rather
  than `initialRouteFor`'s launch-time initial-route return.

## Implementation checklist
- [x] Investigated `TransportService`/`LinkQualityFeed`/`BackgroundObserver`
      for an existing reusable connectivity signal — confirmed none of the
      three answers "can this app reach Firebase" (see §Investigation) `a7f6315`
- [x] Read `FR-VER-008` verbatim in `spec/srs.md` — confirmed "reconnect"
      means internet/Firebase reconnection, not mesh transport `a7f6315`
- [x] `FirebasePaths.infoConnected()` added (`.info/connected`, Firebase's
      own built-in signal — no new dependency) `a7f6315`
- [x] `VersionReconnectWatcher` written — event-driven, no polling, only
      fires on a genuine disconnect-to-connect transition `a7f6315`
- [x] Wired in `lib/app/main.dart` after `runApp`, reusing the existing
      `versionPolicyService`/`_readInstalledBuildNumber`, navigating via
      `Get.offNamed(Routes.versionUpdateRequired)` `a7f6315`
- [x] Regression tests written
      (`test/features/version/domain/version_reconnect_watcher_test.dart`):
      reconnect + updateRequired policy navigates once; reconnect +
      unchanged/up-to-date policy never navigates; duplicate `true` events
      without an intervening `false` do not re-trigger; `stop()` cancels the
      subscription `a7f6315`
- [x] Confirmed the navigation tests fail against a deliberately broken
      "reconnect" condition, then pass again restored (see Run log) `a7f6315`
- [x] `flutter analyze` clean (1 pre-existing, unrelated baseline info
      finding), `flutter test` green (1186/1186, up from baseline 1182 —
      this bug's 4 new tests) `a7f6315`
- [x] `E14-B01`'s launch-time fix (`evaluateVersionStateAtLaunch`,
      `initialRouteFor`) left untouched — no diff in either function `a7f6315`

## Round 2 (2026-09-06) — review findings F1–F5

A cross-model review of the round-1 fix (`a7f6315`) found three blocking
defects and two non-blocking ones. Fixed here:

- [x] **F1** — `VersionReconnectWatcher._pending = _pending.then((_) =>
      _reevaluate())` had no `onError` handler: one thrown re-evaluation
      propagated into `_pending`'s own Future, so every SUBSEQUENT `.then`
      in the chain short-circuited without running `_reevaluate()` again —
      permanently and silently killing the watcher for the rest of the
      session, with no logging anywhere. Fixed with
      `.catchError((e, st) => ObservabilityService.instance.logError(...))`
      so the chain always resolves. Regression test:
      `test_a_thrown_error_during_reevaluation_does_not_permanently_kill_the_watcher`
      (a fake `VersionPolicyService.cached()` throws on the first reconnect
      only; asserts a SECOND, later reconnect still navigates) `186b6e1`
- [x] **F2** — `VersionPolicyService.refresh()`'s `insertOnConflictUpdate`
      Drift write sat OUTSIDE the try/catch ending at (old) line 96 — a
      locked/full/corrupt local database threw straight out of `refresh()`,
      breaking `EARS-VER-4`'s "never throws" contract for real (this file
      is outside this task's original `files:` list; touched here because
      the round-2 review explicitly required the root fix in `refresh()`
      itself, not just defense-in-depth in the watcher). Extended the
      try/catch to cover the write. Regression test (added to
      `test/core/services/version_policy_service_test.dart`, this task's
      own `files:` list now updated to include both this file and its
      test): a `QueryInterceptor` that throws on every `runInsert`, proving
      `refresh()` still `completes` rather than throwing `186b6e1`
- [x] **F3** — the shipped claim ("a fresh subscription can never re-fire
      `E14-B01`'s launch-time check because `_previouslyConnected` starts
      `null`") was WRONG: Firebase's `.info/connected` characteristically
      emits `false` first at cold start (its own "not yet connected" initial
      value), then `true` once the socket actually connects — a textbook
      `false -> true` transition on the very first two events, which the
      old `null`-seeded check read as a genuine reconnect and fired a
      duplicate refresh+evaluate seconds after launch. Fixed with an
      `_everConnected` gate: the first-ever `true` this stream reports never
      counts as a reconnect, no matter how many `false` events preceded it
      — only a `true` that follows a `false` AFTER a connection has already
      been established once counts. Regression test:
      `test_cold_start_false_then_true_sequence_does_not_duplicate_the_launch_time_check`
      (exact cold-start sequence -> 0 navigations; a LATER genuine
      disconnect/reconnect afterward -> 1) `186b6e1`. Existing test
      `test_duplicate_connected_events_without_an_intervening_disconnect_do_not_re_trigger`
      updated to establish its baseline reconnect via `true, false, true`
      (matching the first test in the file) rather than the old
      `false, true` shape, since that shape is now correctly the cold-start
      case the new test above covers `186b6e1`
- [x] **F5** — added a same-route guard
      (`if (Get.currentRoute != Routes.versionUpdateRequired)`) around
      `main.dart`'s `onUpdateRequired` navigation so a flapping connection
      doesn't repeatedly tear down/rebuild the mandatory-update screen
      `186b6e1`
- [x] **F4** — recorded, not fixed, per this bug's own review instructions:
      added to `epics/E14-version-management/tracker.md`'s carried-forward
      observations — mid-session `updateRequired` navigates but does not
      retroactively set `AppBinding.blockCommunication` `186b6e1`
- [x] Confirmed each of F1/F2/F3's new regression tests fails against the
      pre-fix code and passes again restored (see Round 2 Run log below)
- [x] `flutter analyze` clean (1 pre-existing, unrelated baseline info
      finding), `flutter test` green (1189/1189, up from 1186 — this
      round's 3 new tests: the F1 test, the F2 test, and F3's new cold-start
      test) `186b6e1`

## Run log
- **Red confirmed before the fix**: temporarily forced
  `VersionReconnectWatcher._onConnectivityChanged`'s `isReconnect` to always
  `false` and reran `test/features/version/domain/
  version_reconnect_watcher_test.dart` — 2 of 4 tests failed exactly as
  expected (`test_reconnect_with_update_required_policy_navigates_to_
  mandatory_update_screen` and
  `test_duplicate_connected_events_without_an_intervening_disconnect_do_not_
  re_trigger`, both asserting `navigateCalls == 1` and getting `0`).
  Restored the real condition immediately after confirming (no commit of
  the reverted state).
- **Full suite**: `flutter test` → 1186/1186 passing (baseline 1182 + 4 new
  tests from this bug).
- **`flutter analyze`**: 1 issue, the same pre-existing baseline info
  finding already documented in `E14-B01`'s own Run log
  (`annotate_overrides` on `test/core/calls/call_migration_controller_test
  .dart:177`) — no new findings introduced.
- **Manual composition check**: read `lib/app/main.dart`'s new wiring
  end-to-end after editing — `versionPolicyService` and
  `_readInstalledBuildNumber` are the exact same values `main()` already
  built for the launch-time path, never re-constructed; `VersionPolicyService
  .refresh()`'s own never-throwing contract (`EARS-VER-4`) means a reconnect
  event during a real network blip degrades to a harmless no-op re-check.

### Round 2 (2026-09-06) Run log

- **F3 red confirmed before the fix**: reverted `_onConnectivityChanged` to
  the original `connected && _previouslyConnected == false` check (no
  `_everConnected` gate) and reran
  `test_cold_start_false_then_true_sequence_does_not_duplicate_the_launch_time_check`
  — failed exactly as expected (`Expected: <0>, Actual: <1>`, the cold-start
  `false, true` pair fired a spurious reconnect). Restored the real
  `_everConnected` gate immediately after confirming (no commit of the
  reverted state).
- **F1 red confirmed before the fix**: reverted `_onConnectivityChanged` to
  `_pending = _pending.then((_) => _reevaluate())` (no `.catchError`) and
  reran `test_a_thrown_error_during_reevaluation_does_not_permanently_kill_the_watcher`
  — failed exactly as expected (an uncaught `Exception` from the first
  reconnect's `_reevaluate()` propagated, and the assertion that a SECOND,
  later reconnect still navigates got `Expected: <1>, Actual: <0>`).
  Restored the real `.catchError` immediately after confirming (no commit
  of the reverted state).
- **F2 red confirmed before the fix**: reverted `VersionPolicyService
  .refresh()`'s write back outside the try/catch and reran the new
  `QueryInterceptor`-based test — failed exactly as expected (`Exception:
  simulated locked/full/corrupt local database` propagated straight out of
  `refresh()`, `expectLater(..., completes)` failed). Restored the real
  try/catch immediately after confirming (no commit of the reverted state).
- **Note on "logged" verification**: this codebase's `ObservabilityService`
  (genesis structural stub, ADR-0006) only `print()`s in debug mode and has
  no test-observable capture point anywhere in the existing suite (grepped
  `test/` for `logError`/`ObservabilityService` — no hits before this
  round). F1's/F2's "failure is logged" requirement is satisfied by code
  reading (both new catch blocks call `ObservabilityService.instance
  .logError(...)`, matching this file's own existing call shape), not by an
  automated assertion — consistent with every other `logError` call site in
  this codebase, none of which are assertion-tested either.
- **Full suite**: `flutter test` → 1189/1189 passing (1186 baseline + 3 new
  tests this round: F1's, F2's, and F3's new cold-start test — F3 also
  restructured one existing test's setup without adding a new one).
- **`flutter analyze`**: 1 issue, the same pre-existing baseline info
  finding (`annotate_overrides`,
  `test/core/calls/call_migration_controller_test.dart:177`) — no new
  findings introduced.

## Self-review
- [x] Every §Implementation checklist item done, with the commit hash
      (round 1 `a7f6315`, round 2 `186b6e1`)
- [x] `flutter analyze && flutter test` green (1 pre-existing unrelated
      info finding; 1189/1189 tests as of round 2)
- [x] Diff confined to this task's `files:` (round 2 updated the
      frontmatter to add `lib/core/services/version_policy_service.dart`
      and its test, both touched only because the round-2 review
      explicitly required the F2 root fix there) —
      `lib/features/version/domain/version_reconnect_watcher.dart` (new),
      `test/features/version/domain/version_reconnect_watcher_test.dart`
      (new), `lib/app/main.dart`, `lib/core/services/firebase_paths.dart`,
      `lib/core/services/version_policy_service.dart` (round 2),
      `test/core/services/version_policy_service_test.dart` (round 2)
- [x] `## What this fix does NOT do` respected — `E14-B01`'s launch-time
      refresh/fence untouched; no new connectivity primitive invented (the
      mesh transport stack was checked first and correctly ruled out, see
      §Investigation); no polling/`Timer` anywhere in the new code
- [x] No secrets or PII logged — round 2's two new `logError` calls
      (F1, F2) log only a fixed error code and the caught exception object,
      same shape as every existing call site in this file/service
- [x] ADR/EARS: `EARS-VER-4` (`refresh()` never throws) — round 1 relied on
      this without re-verifying it; round 2's F2 finding showed that
      reliance was actually misplaced (the write path broke the contract),
      so round 2 fixes `refresh()` itself to make `EARS-VER-4` true rather
      than assumed
- [x] Loading/error/empty states: n/a — this is a background composition
      root wiring change, no new UI surface

**Deviations from spec/design:**
1. This task file's own frontmatter shipped with an empty `files:` list —
   filled in above with the files actually touched, matching the shape
   every sibling `E14-Bxx` task file uses.
2. Reused Firebase Realtime Database's own `.info/connected` special path
   rather than adding a `connectivity_plus`-style dependency. Judgment call,
   not a spec contradiction: `FR-VER-008`'s "reconnect" is reconnection to
   the exact source this policy is fetched from, and Firebase's own signal
   for that is already available via the `firebase_database` package this
   app already depends on — no new dependency, no 🧍 gate needed.
3. Navigation is a mid-session `Get.offNamed(Routes.versionUpdateRequired)`
   (mirroring `LoginController._signIn`'s established forced-navigation
   pattern), not `initialRouteFor` — that function is specifically the
   launch-time *initial route* decision (`E14-T04`'s own task) and is left
   untouched per this bug's own fence.
4. **(Round 2)** `lib/core/services/version_policy_service.dart` and
   `test/core/services/version_policy_service_test.dart` were outside this
   task's original `files:` list. Touched anyway because the round-2 review
   explicitly required the F2 fix to land in `refresh()` itself (a
   `VersionReconnectWatcher`-only defense-in-depth catch would not have made
   `EARS-VER-4` actually true) — not a scope improvisation, a review
   instruction. Frontmatter's `files:` updated to match.

**Files actually touched:** `lib/features/version/domain/
version_reconnect_watcher.dart` (new),
`test/features/version/domain/version_reconnect_watcher_test.dart` (new),
`lib/app/main.dart`, `lib/core/services/firebase_paths.dart` (round 1);
`lib/core/services/version_policy_service.dart`,
`test/core/services/version_policy_service_test.dart` (round 2) — this
task's own `files:` list (filled in above).

### Definition of Done
- [x] The exact defect (no reconnect-triggered re-evaluation anywhere) is
      fixed — a genuine Firebase reconnect now refreshes and re-evaluates
      the version policy, re-routing to the mandatory-update screen when
      the result is `updateRequired`
- [x] Regression tests proven red before, green after (see Run log; round 2
      Run log for F1/F2/F3's own red-before-green confirmations)
- [x] `flutter analyze` clean (baseline's 1 pre-existing info finding
      only), `flutter test` green (1189/1189 as of round 2)
- [x] Round 2: the three round-2 blocking findings (F1 permanently-killed
      watcher, F2 refresh() throw gap, F3 false cold-start-immunity claim)
      are fixed, with regression tests, and the shipped claim F3
      contradicted is corrected in §Fix above rather than left standing
- [x] Round 2: F5 (same-route guard) implemented; F4 (mid-session
      `blockCommunication` gap) recorded in
      `epics/E14-version-management/tracker.md`, not fixed, per this
      round's own review instructions
- [x] `E14-B01`'s launch-time fix and fence left untouched
- [x] Independent review (rule 5) — pending, `status: review-requested`
