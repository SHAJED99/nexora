---
id: E14-T03
epic: E14
type: feature
title: Verify the version policy's signature before trusting it
layer: backend
size: S
status: done
side: unblocked
owner_agent: builder
model: any
preferred_agent: any
token_estimate: { tier: S, range: "15k-30k" }
priority: { moscow: should, p: P3 }
depends_on: [E14-T01]
blocks: []
traces_to: [FR-VER-011, EARS-VER-17]
required_context:
  - epics/E14-version-management/tasks/E14-T01.md
  - lib/core/services/version_policy_service.dart
  - lib/core/observability/observability_service.dart
  - agent/memory/decisions/
design_contract: n/a
files:
  create:
    - lib/core/services/version_policy_signature_verifier.dart
    - test/core/services/version_policy_signature_verifier_test.dart
  update:
    - pubspec.yaml
    - pubspec.lock
    - lib/core/services/version_policy_service.dart
    - test/core/services/version_policy_service_test.dart
    - test/features/version/domain/version_reconnect_watcher_test.dart
    - test/features/version/presentation/version_update_controller_test.dart
started_at: 2026-09-07
completed_at: 2026-09-07
executed_by: claude-sonnet-5
reviewed_at: 2026-09-07
reviewed_by: opus (claude-opus-5, direct Claude Code reviewer subagent; round 1 changes-requested, round 2 approved)
review_outcome: approved
---
# E14-T03 · Verify the version policy's signature before trusting it

## 1. Goal
`FR-VER-011`: "where appropriate, version policy shall be
cryptographically signed and verified by the application, to protect
against unauthorized modification."

## 2. Why this had `side: blocked` and an empty `files:` fence
**Update 2026-09-06: `OQ-E14-T03-1` is now resolved (see §9 Open
Questions) — asymmetric signing, option (b).** `side` is now
`unblocked`. This section is kept as-written (below) for the historical
record of what the decision actually needed to resolve; it is no longer
a live blocker. A concrete task-sharding pass (files:, functions:, EARS
criteria in §7/§8) still needs to happen before this is dispatchable —
notably, the exact signing LIBRARY (no Ed25519-capable dependency exists
in `pubspec.yaml` today, confirmed by grep) is its own, separate 🧍
`new_dependency` gate, not resolved by this decision.

This task could not be written concretely without a human decision this
session had no basis to make unilaterally (rule 3 — new cryptographic
infrastructure, plus a secrets/key-distribution question):

- **What signs the policy?** A private key held by whoever publishes
  `config/version_policy` (an ops/release process, per `E14-T01` §4's
  "no client ever writes this node") — but WHERE that private key lives,
  who can access it, and how it's rotated if compromised is an
  operational decision with no existing precedent in this codebase.
  `ADR-0003` (Signal Protocol) governs message/session cryptography
  between mesh peers, not app-to-ops policy signing — a different trust
  relationship entirely, and reusing Signal Protocol machinery for it
  would be a category error, not a reuse.
- **What verifies it, and with what bundled key?** The app needs a public
  key baked into the binary at build time to verify against. Which
  algorithm (Ed25519 is the obvious fit if a signing library is already a
  dependency — verify at execution time whether one exists before adding
  a new one, itself a 🧍 `new_dependency` gate either way) and how that
  public key gets into the app (a literal constant? a build-time
  config injection, matching whatever secrets pattern `E14-T01`'s own DSN-
  style config uses?) are both foundational choices.
- **What happens on a verification FAILURE?** Reject the whole policy
  read (fail closed — but then a single bad signature could make
  `EvaluateVersionStateUseCase` fall back to its own "no cached policy →
  UP_TO_DATE" default, which is fail-OPEN in the opposite direction from
  what signing was meant to protect) or something else. This is a real
  security-posture decision, not an implementation detail.

`FR-VER-011`'s own "where appropriate" qualifier is doing real work here:
it explicitly permits deciding this is not appropriate for v1's threat
model (the same class of call `ADR-0006` made for observability's
scope) — that is itself one of the options the human should see, not an
assumption this task makes for them.

## 3. What this task does
**Sharded and implemented 2026-09-07**, once (a) `OQ-E14-T03-1` was
resolved (asymmetric Ed25519 signing, fail-closed) and (b) the human
picked the signing library (`cryptography` ^2.9.0, the `new_dependency`
gate this task was otherwise blocked on — see §Open Questions).

A new `VersionPolicySignatureVerifier`
(`lib/core/services/version_policy_signature_verifier.dart`) verifies
each fetched policy's `signature` field as an Ed25519 signature (base64)
over a canonical pipe-joined message of the policy's four numeric fields
(`minimumSupportedBuild|currentBuild|updateAvailableBuild|updatedAt`),
under a build-time-embedded public key
(`--dart-define=VERSION_POLICY_PUBLIC_KEY=<base64>`, empty by default).
`VersionPolicyService.refresh()` now calls this verifier before ever
writing a fetched payload to the local cache; a failed (or unconfigured-
key) verification is folded into the exact same "no usable policy was
fetched, leave the cache untouched" path malformed payloads already use
(`EARS-VER-4`) — fail-closed, per the resolved `OQ-E14-T03-1`.

## 4. What this task does NOT do
- Does **not** choose or build the ops-side signing process, private-key
  custody, or key-rotation procedure — those remain an operational
  runbook outside this repo, per `OQ-E14-T03-1`'s own answer.
- Does **not** change `E14-T02`'s own state-machine contract — a
  verification failure reaches `EvaluateVersionStateUseCase` exactly the
  way "no cached policy" already did (via `VersionPolicyService.cached()`
  returning `null`/the prior cached row), not a new failure mode T02 has
  to learn about.
- Does **not** add signing/verification to any node other than
  `config/version_policy` — no other Firebase node in this schema carries
  a `signature` field.
- Does **not** implement key rotation, multiple trusted keys, or a
  key-versioning scheme — a single embedded public key, replaced only by
  shipping a new app build, matching this task's own scope as verify-only.

## 5. Contract
- **New:** `VersionPolicySignatureVerifier.verify({minimumSupportedBuild,
  currentBuild, updateAvailableBuild, updatedAt, signatureBase64}) ->
  Future<bool>` — pure, never throws (malformed input is a verification
  failure, not a crash); `canonicalMessage({...}) -> List<int>` exposed
  for tests (and, in principle, an eventual signing tool) to construct
  the exact bytes an ops process must sign.
- **Changed:** `VersionPolicyService`'s constructor gains an optional
  `signatureVerifier` param (test seam, mirrors `firebaseDatabase`'s own
  override pattern); `refresh()` gains the fail-closed verification step
  described in §3, between parsing and the cache write.
- **New dependency:** `cryptography: ^2.9.0` (human-approved 2026-09-07,
  `new_dependency` gate) — Ed25519 verify only, never sign.

## 6. Risks
Leaving `config/version_policy` unverified means anyone who can write to
that node (today: nobody, per `E14-T01`'s `.write: false` rule — the
actual risk surface is a Firebase project-level compromise, not a client-
side one) could push an arbitrary policy. `E14-T01`'s rule already
narrows this to "whoever has ops/Firebase-console access," which may be
judged sufficient without app-level signing at all — exactly the
"where appropriate" call above.

## 7. Implementation checklist
- [x] `cryptography: ^2.9.0` added to `pubspec.yaml` (human-approved
      dependency), `flutter pub get` clean (no other version changes)
- [x] `VersionPolicySignatureVerifier` written first with its own
      falsifying tests (valid signature accepts; wrong key, wrong
      message, tampered signature bytes, non-base64 input, unconfigured
      key all reject) — see Run log
- [x] `VersionPolicyService.refresh()` wired to fail-closed on a failed
      verification, folded into the existing "leave cache untouched"
      path
- [x] `version_policy_service_test.dart`'s existing fixtures updated to
      use a real test keypair + real signatures (the literal string
      `'sig-v1'` never verifies against any key, so every pre-existing
      "successful refresh" test needed a genuine signature to keep
      passing) — no test's *assertion* changed, only its fixture data
- [x] New regression test proving an UNSIGNED/invalid-signature payload
      is rejected exactly like a malformed one (cache left untouched)
- [x] `flutter test && flutter analyze` green (full suite, see Run log)
- [x] Diff confined to `files:` above

## 8. Acceptance criteria (EARS) + test plan
- **EARS-VER-17**: WHEN `config/version_policy` is fetched, THE system
  SHALL verify its `signature` field as a valid Ed25519 signature (over
  the policy's four numeric fields, under the build-embedded public key)
  before caching it; an invalid, missing, or unverifiable signature SHALL
  be treated identically to a malformed payload (existing cache left
  untouched, never thrown). (FR-VER-011)
  - Test: `test_EARS_VER_17_valid_signature_is_cached` — a payload signed
    with the matching test private key IS cached.
  - Test: `test_EARS_VER_17_invalid_signature_leaves_cache_untouched` —
    a payload signed with a DIFFERENT keypair, or with tampered numeric
    fields (signature no longer matches), leaves the pre-existing cache
    untouched and `refresh()` still never throws.
  - Test: `test_EARS_VER_17_unconfigured_public_key_fails_closed` — with
    no public key override/dart-define present, even an otherwise
    well-formed payload is never cached (fail-closed, not skip-
    verification).

## 9. Self-review + Definition of Done
- [x] `flutter test && flutter analyze` green — see Run log for exact counts
- [x] Diff confined to `files:` — verified via `git diff --stat` against
      `origin/development`
- [x] No secrets logged: signature/public-key bytes are never logged in
      cleartext; only the fact of a verification failure is (matches
      every sibling wrapper's `ObservabilityService.logError` pattern)
- [x] Every ADR touching these files honoured: no ADR governs app-to-ops
      policy signing specifically (this task's own §2 already
      established `ADR-0003` does not apply here — a different trust
      relationship); `ADR-0006`'s observability privacy stance
      (`configurePrivacyOptions`) untouched, only its ordinary
      `logError` call sites used, consistent with every sibling wrapper
- [x] Not a durable counter/cursor or an `.isIn(ids)`/`IN (...)` query —
      neither checklist applies to this change

## Open Questions
- **OQ-E14-T03-1 — is policy signing appropriate for v1, and if so,
  with what key infrastructure?** Three options, advisory only:
  (a) skip it for v1 — `FR-VER-011`'s own "where appropriate" qualifier
  permits this; the write-side rule already restricts who can publish
  the node; revisit if a real incident ever demonstrates the gap
  matters. (b) a build-time-embedded Ed25519 public key (or whatever
  algorithm a library already in `pubspec.yaml` supports, checked before
  adding a new dependency), verifying a signature over the policy's own
  fields, fail-CLOSED on mismatch (a verification failure is treated as
  "no valid policy," same as `E14-T01`'s own "no cached policy" case —
  fail open toward `UP_TO_DATE`, consistent with `E14-T02`'s existing
  fail-open default, not a stricter new failure mode). (c) something
  else the human has in mind for how release signing already works in
  this project outside this repo (e.g. reusing whatever key already
  signs the Android release build, if one exists).
  - **Status:** ✅ resolved
  - **Answer:** Option (b) — asymmetric signing. A private key (held
    offline by whoever runs the ops/release process that publishes
    `config/version_policy`; the exact custody/rotation procedure is an
    operational runbook, not something this task builds) signs each
    policy payload. The app ships only the corresponding PUBLIC key,
    embedded at build time — never the private key, never a shared
    secret. Algorithm: Ed25519 if a signing-capable library already
    exists in `pubspec.yaml` (check `cryptography`/`pointycastle`/etc.
    before adding anything new — this is still a 🧍 `new_dependency` gate
    if none does). Verification failure is fail-CLOSED at the signature
    check itself (an invalid/missing signature is never trusted as a
    valid policy), but this composes with `E14-T01`/`E14-T02`'s EXISTING
    fail-open default (no valid cached policy → `UP_TO_DATE`) rather than
    inventing a new, stricter failure mode — the net effect is: a bad
    signature is treated exactly like "no policy fetched yet," not like
    "force everyone to update."
  - **Answered by:** human
  - **Date:** 2026-09-06

## Review (2026-09-07)
- **reviewed_by:** `opus` (`claude-opus-5`, direct Claude Code reviewer
  subagent) — differs from `executed_by` (`claude-sonnet-5`), satisfying
  rule 5. Reviewed in an isolated worktree off `origin/epic_14_task_03`
  (`bb8148b`), reading the actual files, not only the diff.
- **verdict: CHANGES REQUESTED** — one blocking finding (F1). Everything
  else in this PR is genuinely good work; F1 is a one-line fix.

### What reproduced exactly (not taken on trust)
- **scope:** in-contract. `git diff --stat origin/development` touches
  exactly the 8 files in `files:` plus this task file — no incidental
  changes. `## What this task does NOT do` respected: `E14-T02`'s state
  machine is untouched, no key rotation/multi-key scheme, no other node
  signed.
- **suite:** `flutter test` run by the reviewer — **1304/1304 green**,
  matching the Run log's claim exactly. The four named files re-run in
  isolation: 37/37 green.
- **analyze:** `flutter analyze` — 2 issues, both pre-existing and
  unrelated (`call_migration_controller_test.dart:177` `annotate_overrides`,
  `login_controller_enrollment_gate_test.dart:18` `unnecessary_import`).
  Baseline claim confirmed; the new verifier file contributes zero.
- **canonical message:** unambiguous. Pipe-joined decimal integers cannot
  produce a field-splice collision (a decimal integer can never contain
  `|`), confirmed by a reviewer-written probe asserting
  `(1,23,4,5) != (12,3,4,5)`.
- **all four fields are really bound to the signature:** the PR's own test
  only tampers `updateAvailableBuild`. The reviewer wrote an independent
  probe tampering **each** of the four fields alone against a genuine
  signature — all four reject. Also probed empty signature string and
  empty public-key bytes: both reject without throwing.
- **EARS-VER-17:** 3/3 verified. `test_EARS_VER_17_valid_signature_is_cached`
  (valid → cached); `test_EARS_VER_17_invalid_signature_leaves_cache_untouched`
  (seeds a real cache row at `minimumSupportedBuild: 100` first, then
  proves a forged payload leaves it at `100` and `refresh()` `completes`);
  `test_EARS_VER_17_unconfigured_public_key_fails_closed` (no key → never
  cached, i.e. fail-closed, not skip-verification).

### Falsification (the tests are real, not decorative)
Two deliberate mutants applied and reverted verbatim:
- **Mutant A** — `verify()` returns `true` unconditionally:
  **12 tests go red** across the verifier and service suites.
- **Mutant B** — the `if (!signatureValid) return;` guard in `refresh()`
  neutered (`if (false && ...)`): **3 tests go red**, precisely the three
  `EARS-VER-17` integration tests.
Restored to `bb8148b` and re-confirmed green. The tests have real wiring
behind them.

### F1 (BLOCKING, S3) — a malformed build-time public key throws out of `refresh()`, regressing `EARS-VER-4`
`lib/core/services/version_policy_signature_verifier.dart:92-93`:
```dart
final publicKeyBytes = _publicKeyBytesOverride ??
    (_publicKeyBase64.isEmpty ? null : base64.decode(_publicKeyBase64));
```
That `base64.decode` sits **outside** every `try` in the method. The
signature decode below it is correctly guarded (`:103-110`) and the crypto
calls are correctly guarded (`:119-136`) — this one path was missed.

`lib/core/services/version_policy_service.dart:129` then awaits
`_signatureVerifier.verify(...)` **outside** any `try/catch` (the fetch
above it and the Drift write below it are each guarded; this new call is
not). So the throw propagates straight out of `refresh()`.

**Reproduced, not theorised.** A reviewer probe run as
`flutter test --dart-define=VERSION_POLICY_PUBLIC_KEY='not!!valid!!base64'`
fails with `FormatException: Invalid character (at character 4)` rather
than returning `false`.

**Why it matters.** `refresh()` is awaited unguarded on the app's launch
path at `lib/app/main.dart:139`, and again at
`lib/features/version/domain/version_reconnect_watcher.dart:148`. Both call
sites' own comments explicitly state they rely on `refresh()` being
"best-effort, timeout-bounded and never-throwing (`EARS-VER-4`) ... never a
hang or a crash." The verifier's own docstring (`:83-84`) likewise promises
"Never throws." All three claims are false for this one input class. The
consequence is a hard crash at every app launch — and the trigger is a
paste error in the `--dart-define` at exactly the moment this feature is
first switched on for real, which is the worst possible time for it.

**Not attacker-reachable** — the value is a build-time constant, not the
remote payload, and no build sets the define today. That is why this is S3,
not S1. It is still blocking because it is a *recurrence of a
previously-fixed bug class in this very method*: `E14-B06` round 2 (F2)
fixed "this local Drift write used to sit OUTSIDE the try/catch," and the
comment recording that fix sits eleven lines below the new unguarded call
(`version_policy_service.dart:144-150`). The gate exists to catch exactly
this.

**Fix (one line, either end):** move the `base64.decode(_publicKeyBase64)`
inside a `try`/`on FormatException` returning `false` (consistent with how
the signature decode directly below it is already handled), **or** wrap the
`verify()` call in `refresh()` in the same catch-and-log shape as its
neighbours. Prefer the former — it keeps the verifier's own "never throws"
contract true at its own boundary. Add a test that drives the malformed-key
path; `--dart-define` on the test run works, as the probe above shows.

### Non-blocking, carried forward
- **N1 — the ops signing side has no reader.** The canonical message format
  is documented only in this file's header
  (`version_policy_signature_verifier.dart:19-25`). Whoever builds the
  ops/release signer works from a Dart source comment in this repo. Worth a
  short runbook entry, or exporting `canonicalMessage` through a tiny CLI,
  before the feature is switched on. Explicitly out of this task's fence
  (§4) — noting it so it is not rediscovered later.
- **N2 — no key rotation.** Correctly fenced out by §4, but the practical
  consequence is that a compromised signing key can only be replaced by
  shipping a new app build. Fine for v1; should be an explicit line in the
  ops runbook rather than an implicit property.
- **N3 — `signature` is still persisted to the local cache**
  (`version_policy_service.dart:160`) even though it has already served its
  purpose by then. Harmless (a signature is public data, and it is never
  logged), just noted as dead weight.

### Security lens
- No secret material logged: the two `logError` calls
  (`version_policy_signature_verifier.dart:105-108`, `:131-134`) pass only a
  static event name and the exception; neither the public key nor signature
  bytes are ever logged. The service-level log (`:137-140`) passes a fixed
  string, not payload data. **PASS.**
- Attacker-controlled input (the remote payload's `signature` and the four
  numeric fields) is fully guarded: non-base64, wrong-length, and
  wrong-curve-point inputs all return `false` — verified by both the PR's
  tests and the reviewer's own probe. **PASS.**
- `ADR-0006`'s observability privacy stance untouched — no
  `configurePrivacyOptions` change; only ordinary `logError` call sites,
  matching every sibling wrapper. **PASS.**
- Public key only, never a private key or shared secret, embedded via
  `--dart-define` with no "trust anything" fallback. **PASS.**
- `new_dependency` gate for `cryptography: ^2.9.0` recorded as
  human-approved 2026-09-07 (Run log + `pubspec.yaml:106-113`). **PASS.**
- Not an auth/payment path; no `.isIn(ids)`/`IN (...)`; no deletion >50
  lines. No `delete_over_50_lines` gate needed.

### Re-review scope
F1 only. Everything else above is verified and needs no re-checking — the
fix should not touch anything beyond the two files named in F1.

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- 2026-09-06 — Human resolved `OQ-E14-T03-1`: asymmetric signing (option
  b), fail-closed at the signature check, composing with `E14-T02`'s
  existing fail-open default rather than a new stricter failure mode.
  `status`/`side` updated (`todo`/`unblocked`); §2 annotated as
  historical rather than a live blocker. This does NOT itself unblock
  dispatch — the task still needs a real task-sharding pass (concrete
  `files:`, `functions:`, EARS criteria) before a builder can pick it up,
  and the specific signing library remains its own separate
  `new_dependency` gate (confirmed via grep: no Ed25519-capable
  dependency exists in `pubspec.yaml` as of this decision).
- 2026-09-07 — **Human picked `cryptography: ^2.9.0`** among three
  presented options (that one, `pointycastle`, or "something else"),
  clearing the `new_dependency` gate. Task-sharded (§5 Contract above)
  and implemented in one pass on branch `epic_14_task_03`
  (`git worktree add ../wt-E14-T03 -b epic_14_task_03 origin/development`).
  - Added `cryptography: ^2.9.0` to `pubspec.yaml`; `flutter pub get` —
    "Changed 1 dependency!", no other version changes.
  - Wrote `VersionPolicySignatureVerifier` first, with its own dedicated
    test file (9 tests: valid signature, wrong keypair, tampered fields,
    wrong public key, non-base64 signature, wrong-length signature,
    unconfigured key, and 2 tests proving `canonicalMessage` is stable/
    field-order-sensitive) — all 9 pass against the implementation as
    written (no red/green cycle recorded here since the file was
    designed test-first from the start, not retrofitted).
  - Wired `VersionPolicyService.refresh()` to call it between parsing
    and the cache write, folding a failed verification into the exact
    same "leave cache untouched" path a malformed payload already used.
  - **Discovered mid-implementation**: this task's own `files:` fence
    (as first drafted) listed only `version_policy_service_test.dart`
    among the tests needing real signatures — running the full suite
    surfaced 2 MORE test files with their own private
    `_FixedReadVersionPolicyService`/payload-building duplicates,
    unlisted anywhere: `test/features/version/domain/
    version_reconnect_watcher_test.dart` (6 call sites, one shared
    `_payload()` helper) and `test/features/version/presentation/
    version_update_controller_test.dart` (3 call sites). Both used the
    same literal `'sig-v1'` convention, which now fails closed. Widened
    `files:` to include both (rule 6 — a genuine contract correction
    found via testing, not scope creep chosen in advance) and applied
    the identical fix: a fixed-seed test keypair, `signatureVerifier`
    threaded through every service construction, `_payload`/fixture
    helpers made `async` and signing for real.
  - `flutter test`: full suite, all green — 1304/1304 (1291 baseline +
    9 new verifier tests + 3 new `EARS-VER-17` integration tests in
    `version_policy_service_test.dart` + 1 net from the two widened
    files' own helper refactor). Re-ran the two widened files in
    isolation first to confirm 0 failures before the full-suite run.
  - `flutter analyze`: 2 issues, both pre-existing/unrelated (matches
    the documented baseline exactly:
    `call_migration_controller_test.dart:177` `annotate_overrides`,
    `login_controller_enrollment_gate_test.dart:18` `unnecessary_import`).
    One NEW info (`prefer_initializing_formals` on the new verifier
    file's constructor) was fixed at the source (restructured to a
    single-line initializer with the same `// ignore:` convention
    `VersionPolicyService`'s own constructor already uses for the exact
    same private-field-vs-public-param-name reason) rather than left in
    or blanket-ignored.
  - `git diff --stat` against `origin/development` confirmed the diff
    is confined to exactly the 8 files in this task's `files:` fence
    (2 created, 6 updated, `pubspec.lock`'s own resolver-generated diff
    included) — no incidental changes elsewhere.
- 2026-09-07 — **F1 fixed** (the reviewer's only blocking finding):
  `version_policy_signature_verifier.dart`'s public-key `base64.decode`
  call was moved inside a `try`/`on FormatException` returning `false`,
  the identical pattern the signature decode two lines below it already
  used. To make this genuinely testable (not merely re-described), added
  a second test seam — `publicKeyBase64Override` (a raw base64 STRING,
  distinct from the existing `publicKeyBytesOverride`) — so a test can
  drive the exact same decode path production uses without needing an
  actual `--dart-define` flag on the test run.
  - **Falsified before restoring**: reproduced the reviewer's own repro
    first (mutant: guard removed, decode call bare) — the new regression
    test (`test_review_F1_malformed_configured_public_key_fails_closed`)
    failed with the identical `FormatException: Invalid character (at
    character 4)` the reviewer's `--dart-define` probe hit. Restored the
    fix, reran: green.
  - `flutter test`: full suite, 1305/1305 green (1304 + this one new
    regression test).
  - `flutter analyze`: 2 issues, unchanged from the documented baseline.
    One transient NEW info on the new `_publicKeyBase64Override` field
    (same `prefer_initializing_formals` class as before) was fixed at
    the source with the same `// ignore:` convention, not left in.
  - `files:` fence unchanged — the fix stayed inside the two files
    already listed (`version_policy_signature_verifier.dart`,
    `version_policy_signature_verifier_test.dart`), per the reviewer's
    own "re-review scope: F1 only" note.
  - `status` set to `review-requested` for re-review; `review_outcome`
    left as the reviewer's own `changes-requested` until they re-verify.

## Review round 2 — re-verification of F1 (2026-09-07)
- **reviewed_by:** `opus` (`claude-opus-5`, direct Claude Code reviewer
  subagent) — same reviewer as round 1, still ≠ `executed_by`
  (`claude-sonnet-5`), rule 5 holds. Isolated worktree at `3dc5f56`.
- **verdict: ✅ APPROVE.** F1 is genuinely closed. Per the round-1 note,
  scope of this pass was F1 only — nothing else was re-litigated.

### Verified, not trusted
- **scope:** `git diff --stat 810cefe 3dc5f56` = exactly 3 files — the two
  named in F1 plus this task file. No widening of the `files:` fence.
- **the fix** (`version_policy_signature_verifier.dart:103-127`): the
  public-key `base64.decode` now sits inside `try { } on FormatException`,
  logging `version_policy.public_key_not_base64` and returning `false` —
  fail-closed, composing with the existing "no valid policy → UP_TO_DATE"
  default rather than throwing. Every decode path in `verify()` is now
  guarded; no unguarded throw site remains in the method.
- **the original repro, re-run by the reviewer**: a reviewer-written probe
  (not the builder's test) constructing a bare `VersionPolicySignatureVerifier()`
  under `flutter test --dart-define=VERSION_POLICY_PUBLIC_KEY='not!!valid!!base64'`
  now returns `false` and **completes without throwing**. This is the exact
  round-1 repro; it passes.
- **suite:** `flutter test` — **1305/1305 green**, claim confirmed.
- **analyze:** `flutter analyze` — **2 issues**, both the documented
  pre-existing ones (`call_migration_controller_test.dart:177`
  `annotate_overrides`, `login_controller_enrollment_gate_test.dart:18`
  `unnecessary_import`). The new field contributes zero.

### Falsification — the new test is not decorative
Mutant applied: the new `try/on FormatException` stripped, leaving
`publicKeyBytes = base64.decode(base64Key);` bare (the round-1 shape).
- `test_review_F1_malformed_configured_public_key_fails_closed` → **RED**,
  `FormatException: Invalid character (at character 4)` — the right reason.
- the reviewer's own `--dart-define` probe → **RED**, byte-identical error.
Both restored verbatim (`git status` clean, zero diff) and re-confirmed green.
That the seam and the real `--dart-define` path fail with the *identical*
exception is itself the evidence that `publicKeyBase64Override` faithfully
mirrors production rather than testing a parallel code path.

### The new `publicKeyBase64Override` seam — no silent behavior change
- Used in **exactly one** place repo-wide: the new regression test
  (`version_policy_signature_verifier_test.dart:192`). Every other call site
  (8 across 4 test files) still uses `publicKeyBytesOverride`, untouched.
- **Bytes really do win when both are set**, as the doc comment claims:
  `publicKeyBytes = _publicKeyBytesOverride` is assigned first and the
  base64 branch is entered only `if (publicKeyBytes == null)` — the string
  seam is unreachable whenever bytes are supplied. Not merely documented;
  structurally enforced by the short-circuit.
- Default construction is unchanged (both params optional, both default
  `null`), so no existing test's behavior shifts.

**No new findings. Merge-ready.**
