---
id: E14-T06
epic: E14
type: feature
title: Migration-safety regression suite — no upgrade path ever destroys local data
layer: backend
size: M
status: review-requested
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: must, p: P2 }
depends_on: []
blocks: []
traces_to: [FR-VER-003, FR-VER-009]
required_context:
  - lib/core/persistence/database.dart
  - test/core/persistence/database_migration_test.dart
design_contract: n/a
files:
  create:
    - test/core/persistence/migration_safety_regression_test.dart
  update: []
  delete: []
started_at: 2026-09-05
completed_at: 2026-09-05
executed_by: claude-sonnet-5
reviewed_at:
reviewed_by:
review_outcome:
---
# E14-T06 · Migration-safety regression suite

## 1. Goal
`FR-VER-003`/`FR-VER-009`: prove, as one standing regression suite rather
than scattered per-task assertions, that every migration step in
`database.dart`'s `onUpgrade` from schemaVersion 1 to the current version
is additive-only and never destroys a pre-existing row.

## 2. Business logic
`FR-VER-003` ("application updates shall migrate database schema, local
configuration, cryptographic structures, communication protocol, and
synchronization metadata without intentionally destroying existing local
conversations"), `FR-VER-009` ("mandatory application updates shall not
delete local messages, voice messages, call recordings, attachments,
user settings, or conversation history").

`docs/conventions.md`'s "Schema migrations" rule already mandates
additive-only steps, and every task from `E01` through `E13` has
individually followed it (each migration test file --
`database_migration_test.dart` and its per-task siblings -- already
proves ITS OWN step is additive, in isolation). What does not exist yet
is ONE suite that walks the FULL chain end to end -- v1 all the way to
the current version -- seeding realistic data at v1 (messages,
conversations, settings) and asserting every one of those rows survives
verbatim after every subsequent step runs, which is a stronger and more
direct statement of `FR-VER-003`/`FR-VER-009` than "each step is additive
in isolation" already gives.

## 3. What this task does
- One new test file that: builds a raw v1 schema (reuse
  `database_migration_test.dart`'s own earliest fixture-building function
  as the starting point, do not re-derive it), seeds representative rows
  in the tables that exist at v1 (at minimum: `device_identities`,
  `messages` -- the two `FR-VER-009` names explicitly and this codebase
  has from the start), snapshots them, opens the handle at the CURRENT
  `schemaVersion` (triggering the full migration chain), and asserts
  every seeded row is still present with byte-identical field values.
- Runs this same proof for every intermediate `schemaVersion` a real
  install could plausibly resume from (not just v1 -- a device paused
  mid-upgrade-chain at any prior version must also survive), reusing the
  "open at v13/v14/etc." pattern every existing per-task migration test
  file already establishes.

## 4. What this task does NOT do
- Does **not** change any migration step itself -- this is a proof
  suite, not a fix. If it finds a real destructive step, that is a NEW
  bug, filed separately (do not fold a fix into this task's own `files:`
  fence).
- Does **not** duplicate every existing per-task migration test file's
  own narrower assertions -- this suite is additive, a stronger
  end-to-end statement layered on top of what already exists, not a
  replacement for any of them.
- Does **not** cover crypto-structure or protocol-version migration
  (`FR-VER-003`'s other named categories) beyond what `database.dart`'s
  own migration chain touches -- `signal_identity`/`signal_sessions`
  table survival is covered by the same generic "every v1-onward table's
  seeded rows survive" proof, but this task does not separately exercise
  E03's own session-state semantics beyond row presence.

## 5. Contract
No new production code -- test-only task, no `Functions`/`Data`/`UI`
sections apply.

## 6. Risks
- The full migration chain (v1→current) will only grow longer over
  time -- this suite's own runtime cost grows with it. Acceptable; a
  slow-but-thorough regression test is the correct trade here, not a
  reason to sample a subset of versions.
- `schemaVersion` at execution time may have moved past whatever this
  task assumes while writing it -- verify the real current value, same
  standing note as every other task this session that touches
  `database.dart`.

## 7. Implementation checklist
- [x] tests written FIRST, named by EARS id (characterization tests, not
      red-first — see Run log: this proves EXISTING migration behavior is
      correct, per this task's own header note; all 4 tests passed on
      first run against unmodified `database.dart`) `084b1d9`
- [x] reuse (do not re-derive) the earliest existing fixture-building
      function from `database_migration_test.dart` `084b1d9`
- [x] seed representative rows at v1 `084b1d9`
- [x] open at current `schemaVersion`, assert every seeded row survives
      byte-identical `084b1d9`
- [x] repeat for a representative sample of intermediate versions a real
      install could resume from (v11, v13, v16) `084b1d9`

## 8. Acceptance criteria (EARS) + test plan
- **EARS-VER-15**: WHEN a database is migrated from schema v1 to the
  current version, THE system SHALL preserve every pre-existing message
  and device-identity row, byte-identical. (FR-VER-003, FR-VER-009)
- **EARS-VER-16**: WHEN a database is migrated from any intermediate
  schema version to the current version, THE system SHALL preserve every
  pre-existing row from that version's own tables. (FR-VER-003)

### Tests
- `test_EARS_VER_15_v1_to_current_preserves_seeded_rows_byte_identical`
- `test_EARS_VER_16_intermediate_version_to_current_preserves_rows`
  (parameterized/repeated across a representative sample of versions)

### Manual
None — no UI surface, test-only task.

## 9. Self-review + Definition of Done

### Self-review
- [x] Every §7 item done, with commit hashes — all `084b1d9`
- [x] `flutter test && flutter analyze` green — 1165/1165 tests pass
      (full suite, includes every parallel E13/E14 task's own migration
      test file), `flutter analyze` reports only one pre-existing `info`
      in an untouched file (`test/core/calls/call_migration_controller_test.dart:177`,
      missing `@override` — not this task's diff)
- [x] Diff confined to `files:`; §4 respected — no production file
      touched. Diff is exactly one new file,
      `test/core/persistence/migration_safety_regression_test.dart`
- [x] Every existing per-task migration test file still passes unmodified
      (verified via the full-suite run above; none of them were edited)

**Deviations from spec/design**:
- §3 says the v1 fixture should seed "representative rows in the tables
  that exist at v1 (at minimum: `device_identities`, `messages`...)".
  Verified against `lib/core/persistence/database.dart`'s `onUpgrade`:
  `messages` is created by the `from < 11` step (E05-T01), so it does NOT
  exist at schema v1 — only `device_identities` does. The v1 fixture
  therefore seeds only `device_identities` (the one table that actually
  exists at that version); a `messages` row is instead seeded starting at
  v11 (the earliest version it can exist), which is the strictly LONGEST
  possible chain a seeded `messages` row could ever be asked to survive —
  a stronger proof of `FR-VER-009`'s message-row-survival requirement than
  seeding it at any later version would be. This is the simplest reading
  that reconciles the task text with the actual schema history; not a
  spec gap (the actual v1 shape is unambiguous in `database.dart` and in
  `database_migration_test.dart`'s own v1 fixture), so not escalated to
  `## Open Questions`.
- Representative intermediate-version sample picked: v11 (messages
  table's own introduction — longest possible survival chain), v13
  (group data model — reuses `database_migration_test.dart`'s own
  `_createV13Tables`, the exact function §7/§3 point at), v16 (fullest
  available intermediate schema, only two steps short of current —
  reuses `revocation_migration_test.dart`'s `_createV16Tables`). Not
  exhaustive over all 17 versions; task §6 accepts a "representative
  sample", not every version.
- Fixture-building functions are copied verbatim from three existing
  per-task test files (`database_migration_test.dart`,
  `sync_migration_test.dart`, `revocation_migration_test.dart`) rather
  than imported, because Dart's leading-`_` privacy is per-file and this
  task's `files: update: []` forbids adding an export/shared-helper file
  to make them public. Each copy is commented with its exact source. This
  is the literal mechanism behind §7's "reuse, do not re-derive" —
  the DDL text is identical to what already passed review in those
  files, never independently retyped.

**Files actually touched:**
- `test/core/persistence/migration_safety_regression_test.dart` (created)

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id —
      `test_EARS_VER_15_v1_to_current_preserves_seeded_rows_byte_identical`,
      `test_EARS_VER_16_intermediate_version_to_current_preserves_rows_v11`,
      `test_EARS_VER_16_intermediate_version_to_current_preserves_rows_v13`,
      `test_EARS_VER_16_intermediate_version_to_current_preserves_rows_v16`
      — all 4 pass
- [ ] Review APPROVE from a **different model** than `executed_by` (rule 5)
- [ ] Squash-merged to `epic_14`; tracker + metrics stamped
- [ ] 🧍 Human `verified` (epic-level batches are fine)

## Open Questions
None new. No destructive migration step was found — every step in
`database.dart`'s `onUpgrade` (v1 through the current v18) is additive
only; every seeded row (across the v1, v11, v13, v16 fixtures) survived
the remainder of the chain byte-identical.

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- 2026-09-05: read task file, `lib/core/persistence/database.dart` in
  full (live `schemaVersion` = 18, one step higher than any number
  hard-coded anywhere in this task's own text — confirmed via
  `db.schemaVersion` assertions in the new tests rather than trusting a
  hard-coded literal), and `test/core/persistence/database_migration_test.dart`
  in full.
- Found the codebase already has three other fully-built intermediate
  schema fixtures beyond v13/v14 (`_createV11Tables`-equivalent pieces in
  `sync_migration_test.dart`, `_createV15Tables` in
  `notification_migration_test.dart`, `_createV16Tables` in
  `revocation_migration_test.dart`) — reused the v11-composition and v16
  ones (plus `database_migration_test.dart`'s own v13 one) as the three
  representative intermediate versions, per §7's reuse instruction.
- Wrote `test/core/persistence/migration_safety_regression_test.dart`
  with 4 tests: one full-chain v1→current test (EARS-VER-15) and three
  parameterized intermediate-version→current tests for v11/v13/v16
  (EARS-VER-16). Ran the new file alone first (all 4 pass on the first
  run — characterization tests proving existing, already-reviewed
  migration behavior, not a red-first fix), then the full suite
  (`flutter test`, 1165/1165 pass) and `flutter analyze` (clean except
  one pre-existing, out-of-scope `info`). Commit `084b1d9`.
- No genuine destructive migration step was found — `status` stays
  `review-requested`, not `blocked`.
