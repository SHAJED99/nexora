---
id: E14-B03
epic: E14
type: bug
title: "`_readInstalledBuildNumber`'s catch-all fail-open returns `1 << 62`, so any `PackageInfo` failure silently satisfies every version threshold and bypasses `UPDATE_REQUIRED` — including `FR-VER-010`'s emergency enforcement"
layer: backend
size: S
status: review-requested
owner_agent: builder
model: any
preferred_agent: any
token_estimate: { tier: S, range: "20k-50k" }
severity: S3
priority: { moscow: should, p: P2 }
depends_on: [E14-T04]
blocks: []
traces_to: [FR-VER-005, FR-VER-006, FR-VER-010]
required_context:
  - epics/E14-version-management/tasks/E14-T04.md
  - lib/app/main.dart
  - lib/features/version/domain/evaluate_version_state_use_case.dart
found_by: reviewer (claude-opus-5), E14 epic bug sweep 2026-09-06
found_in: "the T02 -> T04 seam: T02 specced a fail-open for a MISSING POLICY (EARS-VER-9, traced to FR-VER-008). T04 extended the same fail-open to a FAILED BUILD READ, which no spec line covers."
files:
  create: []
  update:
    - lib/app/main.dart
    - test/features/version/presentation/version_update_controller_test.dart
  delete: []
---
# E14-B03 · An unreadable build number silently disables version enforcement

## The defect

`lib/app/main.dart:113-127`:

```dart
Future<int> _readInstalledBuildNumber() async {
  try {
    final info = await PackageInfo.fromPlatform();
    return int.parse(info.buildNumber);
  } catch (e) {
    ObservabilityService.instance.logError(
      'version.installed_build_read_failed', cause: e);
    return 1 << 62;
  }
}
```

`1 << 62` is above any plausible `minimumSupportedBuild`, so
`EvaluateVersionStateUseCase.call()` takes neither of its guard branches and
returns `upToDate`. Any failure inside that `try` — a platform-channel error,
a plugin-registration failure, a `PackageInfo` cache miss, or an
`int.parse` `FormatException` on a non-numeric `buildNumber` — therefore
**disables version enforcement entirely** for that launch, silently apart
from one observability log line.

The `int.parse` path is the concrete one: `PackageInfo.buildNumber` is a
`String`. On Android it is `versionCode` and is numeric, so the happy path
holds — but the value is not guaranteed numeric by the plugin's contract, and
a non-numeric build number would fail open rather than being handled.

**The fail-open is justified in the comment by an analogy that does not
transfer.** `EvaluateVersionStateUseCase`'s no-cached-policy fail-open is
specced: `EARS-VER-9`, traced to `FR-VER-008`'s offline-use framing — "we have
never heard a policy, so we cannot claim this build is unsupported" is a
sound inference. "We could not read our own build number" is a different
statement, and no FR covers it. Under `FR-VER-010` (an emergency bump
published because the running build is unsafe), a device that cannot read its
own build number is precisely the device that should not be given the benefit
of the doubt.

## Repro

1. In a test, inject an `InstalledBuildProvider` that throws — or point
   `_readInstalledBuildNumber` at a `PackageInfo` whose `buildNumber` is
   `"1.0.3"` (a valid `CFBundleVersion` shape).
2. Seed a policy with `minimumSupportedBuild: 100`.
3. Evaluate.

## Expected

The spec is silent, which is itself the finding — `FR-VER-010`'s intent
(retroactively marking builds unsupported cannot be sidestepped) argues for
`updateRequired`, or at minimum a distinct, non-silent outcome. This needs a
one-line answer, not a builder's guess: **raise it as an Open Question
(`OQ-E14-B03-1`) before writing the fix.**

## Actual

`VersionState.upToDate`, `initialRoute: /welcome`, one log line. Enforcement
off.

## Severity

**S3** — broken, but conditional on a failure mode that should be rare on the
target platform, and a workaround exists (the next launch that reads
successfully re-enforces). Not S2: it does not fire on the normal path.

## Suggested fix shape (not binding)

Once `OQ-E14-B03-1` is answered: narrow the `catch` to the cases actually
intended to fail open, and make the "I do not know my own build" case its own
explicit branch rather than a sentinel value smuggled through an `int`. A
sentinel of `1 << 62` inside a normal-looking `int` return is what let this
land unnoticed — an `int?` or a small result type says what happened.

## Regression test (required before close)

A test asserting the chosen behaviour for a throwing `InstalledBuildProvider`
against a seeded `minimumSupportedBuild`, and a second asserting the
non-numeric `buildNumber` string case. Both must fail on today's HEAD.

## What this fix does NOT do

- Does NOT change `EvaluateVersionStateUseCase`'s own `EARS-VER-9`
  no-cached-policy fail-open — that one IS specced and stays exactly as it is.
- Does NOT change the `package_info_plus` dependency, its pin, or add any new
  dependency.
- Does NOT add retry logic or a second read attempt.
- Does NOT touch `E14-B01`'s missing `refresh()` call site or `E14-B02`'s
  communication blocking.

## Open Questions
- **OQ-E14-B03-1 — what should happen when the installed build number
  cannot be read?** The bug file's own analysis above already argues one
  way clearly: `EvaluateVersionStateUseCase`'s no-cached-policy fail-open
  is specced (`EARS-VER-9`/`FR-VER-008`, "we have never heard a policy, so
  we cannot claim this build is unsupported" — a sound inference); "we
  could not read our own build number" is a different claim with no FR
  covering it, and under `FR-VER-010` (an emergency bump because the
  running build is unsafe) a device that cannot even read its own build
  number is exactly the device that should NOT get the benefit of the
  doubt.
  - **Status:** ✅ resolved
  - **Answer:** Fail CLOSED — treat an unreadable/non-numeric build
    number as `updateRequired` when a policy IS cached (never silently
    fall through to `upToDate`). This is consistent with this project's
    established posture elsewhere on this exact question (`E14-T03`'s own
    asymmetric-signing decision made the identical call: a verification
    failure should not default to the permissive state). Replace the
    `1 << 62` sentinel with an explicit result type (or `int?` plus a
    distinguishable branch) per the bug's own "Suggested fix shape" — a
    sentinel smuggled through a normal-looking `int` is what let this
    land unnoticed in the first place, and the fix should not repeat that
    pattern. If NO policy is cached at all, `EARS-VER-9`'s existing
    fail-open still applies unchanged (this only concerns a build-number
    read failure with a policy already present to evaluate against).
  - **Answered by:** orchestrator, under the 2026-09-06 extended standing
    autonomy grant (`AGENTS.md`) — using the bug file's own pre-existing
    analysis, not a new judgment call
  - **Date:** 2026-09-06

## Implementation checklist
- [x] `_readInstalledBuildNumber` no longer returns a `1 << 62` sentinel on
      failure; renamed `readInstalledBuildNumber` (non-private, same reason
      `evaluateVersionStateAtLaunch`/`initialRouteFor` already are) and
      returns `int?` — `null` on either a `PackageInfo.fromPlatform()`
      failure or a non-numeric `buildNumber` (`int.tryParse`, never
      `int.parse` + catch)  `191607b`
- [x] `EvaluateVersionStateUseCase.call()` treats a `null`/throwing
      `InstalledBuildProvider` as `VersionState.updateRequired` when a
      policy IS cached, per `OQ-E14-B03-1`'s resolution; the no-cached
      -policy `EARS-VER-9` fail-open (line 96, `if (policy == null) return
      VersionState.upToDate;`) is untouched  `191607b`
- [x] Two regression tests added to
      `test/features/version/presentation/version_update_controller_test.dart`
      (a throwing provider, and a non-numeric `buildNumber` via
      `PackageInfo.setMockInitialValues`), both confirmed red against the
      pre-fix logic and green after (see Run log)  `191607b`

## Run log
- **Red confirmed before the fix**: temporarily restored the pre-fix
  semantics inline in `EvaluateVersionStateUseCase.call()` (no `try`/`catch`
  around `_installedBuildProvider()`, and `null` treated as a huge
  fail-open sentinel via `installedBuildRaw ?? (1 << 62)`) and reran the
  two new tests:
  - `test_E14_B03_throwing_installed_build_provider_is_update_required`
    failed by throwing uncaught (`Bad state: platform channel error`) —
    the provider's exception propagated straight out of
    `evaluateVersionStateAtLaunch`, exactly the crash this bug's own fix
    prevents.
  - `test_E14_B03_non_numeric_build_number_is_update_required` failed on
    an assertion mismatch: `Expected: VersionState.updateRequired, Actual:
    VersionState.upToDate`.
  Restored the real fix immediately after confirming both, no commit of
  the reverted state.
- **Full suite**: `flutter test` → 1283/1283 passing.
- **`flutter analyze`**: 2 issues, both unrelated pre-existing `info`
  findings already present on `origin/development` before this branch
  (`annotate_overrides` on
  `test/core/calls/call_migration_controller_test.dart:177`,
  `unnecessary_import` on
  `test/features/login/presentation/login_controller_enrollment_gate_test.dart:18`)
  — confirmed via `git diff --stat` that neither file is touched by this
  fix.
- **Targeted suite**: `flutter test test/features/version/` → 21/21
  passing, including the two new tests and every pre-existing
  `evaluate_version_state_use_case_test.dart`/`version_reconnect_watcher_test.dart`
  test (both files are outside this task's own `files:` fence but consume
  the changed `InstalledBuildProvider` typedef, so re-running them
  confirms nothing there broke).

## Self-review
- [x] Every §Implementation checklist item done, with the commit hash
- [x] `flutter analyze && flutter test` green (2 pre-existing unrelated
      info findings; 1283/1283 tests)
- [x] Diff confined to this task's own `files:` —
      `lib/app/main.dart`,
      `test/features/version/presentation/version_update_controller_test.dart`
      — PLUS a disclosed, in-spirit widening into
      `lib/features/version/domain/evaluate_version_state_use_case.dart`
      (see Deviations below; the dispatch instruction itself anticipated
      this and pre-authorized disclosing it rather than treating it as
      scope creep)
- [x] `## What this fix does NOT do` respected — `EvaluateVersionStateUseCase`'s
      own `EARS-VER-9` no-cached-policy fail-open is byte-for-byte
      unchanged (`if (policy == null) return VersionState.upToDate;` still
      the first line after reading the policy); `package_info_plus`'s
      pin/dependency untouched; no retry/second read attempt added;
      `E14-B01`'s `refresh()` call site and `E14-B02`'s
      `blockCommunication` wiring untouched
- [x] No secrets or PII logged — the observability call sites already
      existed; this fix only changes what gets returned, not what gets
      logged or its cause payload shape
- [x] ADR/EARS: `EARS-VER-9` honoured (see above); `FR-VER-010`'s emergency
      -enforcement intent is what motivated the fail-closed answer to
      `OQ-E14-B03-1` and is now actually satisfied for the unreadable-build
      case

**Deviations from spec/design:**
1. **Disclosed widening into `evaluate_version_state_use_case.dart`**
   (not in this bug's own `files:` list, but flagged by the dispatch
   instruction as something to check for and disclose if needed). The
   `InstalledBuildProvider` typedef (`Future<int> Function()`) has no room
   to express "unreadable" without another sentinel through a non-nullable
   `int` — exactly the pattern this bug exists to remove. Changed the
   typedef to `Future<int?> Function()` and made
   `EvaluateVersionStateUseCase.call()` the single place that decides what
   `null` (or a thrown provider Future) means: `updateRequired` when a
   policy is cached, per `OQ-E14-B03-1`. This also benefits
   `VersionReconnectWatcher` (E14-B06) for free — it consumes the same
   typedef and passes `readInstalledBuildNumber` straight through, with no
   changes needed to `version_reconnect_watcher.dart` itself. Existing
   tests in `test/features/version/domain/evaluate_version_state_use_case_test.dart`
   and `version_reconnect_watcher_test.dart` all still pass unmodified —
   every existing fake provider returns a literal `int`, which is a valid
   `int?`.
2. Renamed `_readInstalledBuildNumber` → `readInstalledBuildNumber` (no
   underscore) in `lib/app/main.dart` so the non-numeric-`buildNumber`
   regression test can call the exact production reader against a mocked
   `PackageInfo`, rather than re-implementing its parse logic — the same
   precedent `E14-B01`'s own fix already set for
   `evaluateVersionStateAtLaunch`/`initialRouteFor`.

**Files actually touched:** `lib/app/main.dart`,
`test/features/version/presentation/version_update_controller_test.dart`
(this task's own `files:` list), plus
`lib/features/version/domain/evaluate_version_state_use_case.dart`
(disclosed widening, see Deviations #1 above).

### Definition of Done
- [x] The exact defect (the `1 << 62` sentinel silently failing version
      enforcement open) is fixed — replaced with an explicit `int?`
      result and a fail-CLOSED branch per `OQ-E14-B03-1`
- [x] Both required regression tests (throwing provider, non-numeric
      `buildNumber`) added, proven red before the fix and green after (see
      Run log)
- [x] `flutter analyze` clean (2 pre-existing unrelated info findings
      only, both confirmed present on `origin/development` before this
      branch), `flutter test` green (1283/1283)
- [x] Independent review (rule 5) — pending, `status: review-requested`
