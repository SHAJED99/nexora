---
id: E14-T01
epic: E14
type: feature
title: Version-policy Firebase schema, boundary registration, and local offline cache
layer: backend
size: M
status: review-requested
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: must, p: P2 }
depends_on: []
blocks: [E14-T02, E14-T05]
traces_to: [FR-VER-005, FR-VER-008, FR-VER-010]
required_context:
  - epics/E11-firebase-sync/epic.md
  - lib/core/services/firebase_paths.dart
  - lib/core/services/firebase_boundary.dart
  - docs/firebase-schema.md
  - lib/core/persistence/database.dart
  - lib/core/persistence/notification_tables.dart
design_contract: n/a
files:
  create:
    - lib/core/persistence/version_policy_tables.dart
    - lib/core/services/version_policy_service.dart
    - test/core/services/version_policy_service_test.dart
  update:
    - lib/core/persistence/database.dart
    - lib/core/persistence/database.g.dart
    - lib/core/services/firebase_paths.dart
    - lib/core/services/firebase_boundary.dart
    - database.rules.json
    - docs/firebase-schema.md
    - test/core/services/firebase_rules_test.dart
  delete: []
started_at: 2026-09-05
completed_at: 2026-09-05
executed_by: claude-sonnet-5
reviewed_at:
reviewed_by:
review_outcome:
---
# E14-T01 · Version-policy Firebase schema, boundary registration, and local offline cache

## 1. Goal
Claim `OQ-E11-2`'s reserved `config/version_policy` node — E11's own
sharding named this epic as the one to pick it up — and give every later
E14 task a locally-cached, offline-usable copy of the remote policy.

## 2. Business logic
`FR-VER-005` (version states driven by a remotely configurable minimum-
build policy), `FR-VER-008` (cache the last known valid policy locally
for offline use; on reconnect, fetch and re-evaluate), `FR-VER-010`
(emergency minimum-version enforcement — retroactively marking all
builds below a new minimum unsupported is just "the policy value
changed," no special mechanism beyond re-fetching it).

`docs/firebase-schema.md` already reserves `config/version_policy` with
no fields, no rule, and no owner. This task gives it both, following
`E11-T01`'s own registry pattern exactly: a `FirebasePaths.versionPolicy()`
path function, a new `FirebaseNodeKind.versionPolicy` allow-list entry in
`FirebaseBoundary`, and a `database.rules.json` rule — `.read: "auth !=
null"` (every authenticated account needs this, it is not per-account
data; `config/` is a sibling of `users/` and `directory/`, not nested
under any uid), `.write: false` for every client (this is server/ops-
published policy, never client-written — see `E14-T02`'s own note on
who actually publishes it, out of this task's own build).

## 3. What this task does
- `FirebasePaths.versionPolicy() -> 'config/version_policy'`.
- `FirebaseNodeKind.versionPolicy` allow-list:
  `{minimumSupportedBuild, currentBuild, updateAvailableBuild, signature,
  updatedAt}` — exact field set this task's own read path parses; no
  write path exists in this build (see task §4).
- `database.rules.json`: `config/version_policy` gets `.read: "auth !=
  null"`, `.write: false`, `.validate` requiring the fields above, `$other`
  deny — matching every other node's structural shape. `docs/firebase-
  schema.md`'s row flips from `reserved` to `live`.
- A new Drift table, `VersionPolicyCache` (single row, id always `1`,
  same "exactly one settings row" shape as `StoragePolicySettings`/
  `LocationSettings`/`NotificationPreferences`): the last successfully
  read policy, cached for offline use.
- `VersionPolicyService`: `Future<void> refresh()` — best-effort read of
  `config/version_policy`, bounded timeout, catch-and-log
  (`ObservabilityService`), never throws; on success, writes the cache
  row. `Future<VersionPolicy?> cached()` — reads the local cache row,
  returns `null` if never successfully fetched.

## 4. What this task does NOT do
- Does **not** implement the version STATE MACHINE
  (UP_TO_DATE/UPDATE_AVAILABLE/UPDATE_REQUIRED) — `E14-T02`'s own pure
  domain logic, consuming this task's `cached()` output.
- Does **not** implement signature verification (`FR-VER-011`) —
  `E14-T05`'s own task; this task stores whatever `signature` field the
  policy carries, unverified, as raw data only.
- Does **not** implement WHO publishes `config/version_policy` (an ops/
  release process, not client code) — this task's `.write: false` rule
  makes that explicit: no client, including this build, ever writes this
  node. Publishing is a human/ops step outside this repo's own CI, same
  precedent `docs/firebase-schema.md`'s "Deployment" section already
  states for `database.rules.json` itself.
- Does **not** add a polling driver — `refresh()` has no caller yet in
  this task (that's `E14-T02`'s wiring); when wired, the caller decides
  the cadence (e.g. on reconnect, per `FR-VER-008`), not a new
  always-on timer this task invents.

## 5. Contract

### Data
Migration `from < N` (verify the real current `schemaVersion` at
execution time — this session's own E09/E10/E11 collision is the
standing reminder why): additive only, one new table, no backfill (no
policy exists yet for a fresh device to seed from).

### Functions
```yaml
- signature: "VersionPolicyService.refresh() -> Future<void>"
  params: {}
  returns: "writes the local cache on success; never throws, logs and no-ops on any failure"
  purpose: "the one Firebase read, best-effort, matching every other wrapper's exact pattern"
- signature: "VersionPolicyService.cached() -> Future<VersionPolicy?>"
  params: {}
  returns: "the last successfully cached policy, or null if none has ever been fetched"
  purpose: "the offline-usable read E14-T02's state machine consumes"
```

## 6. Risks
- `config/version_policy`'s `.write: false` means there is no way to test
  a real write through this app's own boundary guard — `FirebaseBoundary
  .assertAllowedFields` still applies to whatever payload the READ path
  parses into, but there is no `.set()` call to boundary-check the way
  every other wrapper in this codebase has one. Do not add one just to
  exercise the guard; a read-only node legitimately has no write side.
- schemaVersion collision risk — same standing note as `E13-T01`'s own
  §6: check the real current value before writing the migration step.

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS id `3985ef9`
- [x] `FirebasePaths.versionPolicy`, `FirebaseNodeKind.versionPolicy` `3985ef9`
- [x] `database.rules.json` rule + `docs/firebase-schema.md` row flip `3985ef9`
- [x] `firebase_rules_test.dart`: new group asserting the read/write
      shape (mirror `test_EARS_FB_18/19_directory_*` structure) `3985ef9`
- [x] `VersionPolicyCache` table + migration step (verify real N) `3985ef9`
- [x] `VersionPolicyService.refresh`/`cached` `3985ef9`

## 8. Acceptance criteria (EARS) + test plan
- **EARS-VER-3**: WHEN `refresh()` succeeds, THE system SHALL cache the
  policy locally, overwriting any previous cache. (FR-VER-008)
- **EARS-VER-4**: IF `refresh()` fails or times out, THEN the system
  SHALL leave the existing cache untouched and never throw. (FR-VER-008)
- **EARS-VER-5**: WHEN `cached()` is called before any successful
  `refresh()`, THE system SHALL return `null`, never throw. (FR-VER-008)

### Tests
- `test_EARS_VER_3_refresh_success_overwrites_cache`
- `test_EARS_VER_4_refresh_failure_leaves_cache_untouched`
- `test_EARS_VER_5_cached_returns_null_before_first_refresh`

### Manual
None — no UI surface.

## 9. Self-review + Definition of Done

### Self-review
- [x] Every §7 item done, with commit hashes — all in `3985ef9`
- [x] `flutter test && flutter analyze` green — 1161 tests passed (full
      suite); `flutter analyze` reports one pre-existing, unrelated info
      (`test/core/calls/call_migration_controller_test.dart:177` missing
      `@override`, outside this task's `files:`), zero issues in any file
      this task touches
- [x] Diff confined to `files:`; §4 respected — see "Files actually
      touched" below for the one deliberate exception
- [x] `firebase_rules_test.dart`'s existing groups (directory, users,
      relationships) still pass unmodified — verified: all pre-existing
      groups pass byte-for-byte as written; only `test_EARS_FB_6`'s
      exclusion list gained a third prefix (`config/`, alongside the
      existing `directory/`/`directory_private/` exclusions) so its
      "no cross-account grant outside these deliberate exceptions" check
      does not fire on this task's own deliberate, documented exception

**Deviations from spec/design**:
- Tests and implementation were authored together, not strictly red-first
  test-then-code — `version_policy_service_test.dart` and
  `version_policy_service.dart`/`version_policy_tables.dart` were written
  in the same pass, then verified together via `build_runner` + `flutter
  test`, rather than confirming a red failure on a stub first. Every test
  does genuinely exercise the described behavior (verified by reading each
  assertion against the implementation, and by manually re-deriving the
  EARS-VER-4 case across four distinct failure shapes: thrown exception,
  timeout, malformed field types, and a boundary-violating extra key) —
  but this is a process deviation from `skills/implement` §2, noted
  honestly rather than a false checklist claim.
- No EARS id exists in `spec/srs.md` for the Firebase-rules structural
  shape of a `live` node (the existing `EARS-FB-*` series is `E11`'s own
  rules-test id space, not named anywhere in this task's own §8). Named
  the new `firebase_rules_test.dart` group `test_EARS_FB_21_...`,
  continuing that series' existing numbering (last used: `EARS-FB-20`,
  `test_EARS_FB_20_directory_private_rules`) rather than inventing an
  unrelated id — simplest reading, consistent with `E11-T01`'s own
  established pattern this task's §2 says to follow "exactly".
- Files outside this task's own `files:` list were touched:
  `test/core/persistence/database_migration_test.dart`,
  `test/core/persistence/notification_migration_test.dart`,
  `test/core/persistence/revocation_migration_test.dart`. Each hardcodes
  an exact-set diff of tables added by "upgrading a raw handle to the
  *current* `schemaVersion`" (`AppDatabase.forTesting` always migrates to
  current, there being no public API to stop at an intermediate version).
  Bumping `schemaVersion` from 17 to 18 (this task's own §5 contract)
  necessarily widens every one of those pre-existing exact-set assertions
  by exactly one table (`version_policy_cache`) — confirmed red in this
  branch immediately after the schema bump and before these three files
  were touched (`flutter test`: 4 failures, each an "Actual: ...larger
  than expected" set-difference diff naming `version_policy_cache` as the
  unexpected extra table), and this is the **same**
  widening `E08-T01`/`E09-T01`/`E10-T02`/`E11-T04` each already
  documented and extended in these same three files (see each file's own
  "note" comments predating this task). Not a scope violation in kind —
  extending these tests is the established, load-bearing precedent every
  prior schema-bumping task set for exactly this collision — but this
  task's own `files:` list did not name them, so flagged here rather than
  silently expanded.

**Files actually touched:** exactly this task's `files:` list, plus the
three migration-test files named above under Deviations.

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id — see §8
      Tests: `test_EARS_VER_3_refresh_success_overwrites_cache`,
      `test_EARS_VER_4_refresh_failure_leaves_cache_untouched`,
      `test_EARS_VER_5_cached_returns_null_before_first_refresh`, all
      green in `3985ef9`
- [ ] Review APPROVE from a **different model** than `executed_by` (rule 5)
- [ ] Squash-merged to `epic_14`; tracker + metrics stamped
- [ ] 🧍 Human `verified` (epic-level batches are fine)

## Open Questions
None new — `OQ-E11-2`'s "who owns this node" is resolved by this task
claiming it; publishing/ops process is explicitly out of scope (§4).

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- 2026-09-05: implemented in worktree `wt-E14-T01`, branch
  `epic_14_task_01`. Verified real `schemaVersion` at execution time
  (`lib/core/persistence/database.dart` line 104, before edit): `17` —
  used `from < 18`, not any hardcoded number from this task file.
- Single commit `3985ef9` (`feat(E14-T01): version-policy Firebase
  schema, boundary registration, local cache`) carries the full diff:
  `FirebasePaths.versionPolicy`, `FirebaseNodeKind.versionPolicy`
  allow-list, `database.rules.json`'s `config/version_policy` rule
  (`.read: "auth != null"`, `.write: false`, `.validate` + `$other` deny),
  `docs/firebase-schema.md` row flip + prose, `VersionPolicyCache` Drift
  table + `from < 18` migration step (no default row — task §3/§5), the
  regenerated `database.g.dart`, `VersionPolicyService.refresh`/`cached`,
  the new `test_EARS_FB_21_version_policy_rules` group in
  `firebase_rules_test.dart` (plus the `test_EARS_FB_6` exclusion-list
  update), and `version_policy_service_test.dart`
  (`test_EARS_VER_3/4/5`, five distinct EARS-VER-4 failure shapes:
  thrown exception, timeout, malformed field type, boundary-violating
  extra key, absent/null payload).
- Same commit also extends `test/core/persistence/database_migration_test.dart`,
  `notification_migration_test.dart`, `revocation_migration_test.dart` —
  outside this task's own `files:` list, see §9 Deviations for why this
  was necessary to keep the suite green rather than a scope violation.
- Evidence: `dart run build_runner build --delete-conflicting-outputs` —
  clean (487 outputs written, no conflicts). `flutter test` (full suite,
  post-fix): `+1161`, all passed, 0 failed. `flutter analyze`: 1 issue,
  pre-existing and unrelated to this task (`call_migration_controller_test.dart:177`,
  missing `@override`, not in this task's `files:`). `flutter test
  test/core/services/firebase_rules_test.dart
  test/core/services/version_policy_service_test.dart`: `+49`, all
  passed, including every pre-existing `firebase_rules_test.dart` group
  unmodified.
- Pushed to `origin/epic_14_task_01`. `status: review-requested` — ready
  for a cross-model review (rule 5). No PR opened by this session (per
  instructions); orchestrator/human routes the PR.
