---
id: E15-T13
epic: E15
type: bug
title: Reconcile settings.md's HTML-sourced golden against a real Flutter build (design-verify SCREEN=settings is red, 33.8%)
layer: frontend
size: S
status: done
owner_agent: builder-ui
model: sonnet
preferred_agent: any
token_estimate: { tier: S, range: "5k-15k" }
severity: S3
priority: { moscow: should, p: P3 }  # human-set 2026-09-11, bug_priorities gate cleared
depends_on: [E15-T11]
blocks: []
traces_to: [FR-UI-006, FR-UI-008]
required_context:
  - design/screens/settings.md
  - test/design/flutter_probe_dumper.dart
  - docs/design-gate-flutter.md
  - epics/E15-session-and-settings/tasks/E15-T11.md
found_by: E15-T11's own implementer, confirmed independently by its round-1 reviewer
found_in: E15-T11's own required first-ever `settings` (hub) probe dump
design_contract: design/screens/settings.md
files:
  create:
    - design/golden/settings/default@390x844/probe.json
  update:
    - design/screens/settings.md
    - design/sources.yaml
  delete: []
  # DECIDED 2026-09-11 (orchestrator, non-foundational execution call): option
  # (2) -- re-extract the golden from the shipped Flutter build, matching the
  # precedent every one of this epic's nine sub-screens already set (all
  # `source: derived`). Chosen over option (1) (extending
  # `flutter_probe_dumper.dart`'s icon/role handling) because that option is
  # invasive shared-tooling work touching every other currently-100%-passing
  # gated screen's own dumper behavior, for a screen-specific golden mismatch
  # that has a narrower, lower-risk fix already proven nine times over in
  # this same epic.
---
# E15-T13 · Reconcile `settings.md`'s golden against a real Flutter build

## The defect
`design/screens/settings.md`'s golden was extracted from the HTML design
source (`source: ui`, genesis, 2026-08-26) — before
`test/design/flutter_probe_dumper.dart` (E06-T01) existed. It had never been
compared against a real Flutter build via `--impl flutter` until E15-T11
added the screen's first-ever `design-probe` block (the hub itself was never
one of the probe dumper's registered screens before that task). The result:
`node design/tools/verify.mjs --screen settings --impl flutter` reads
❌ **33.8% match (25/74)**, independently reproduced by both E15-T11's
implementer and its round-1 reviewer (isolated via `git stash` to confirm it
predates and is unrelated to E15-T11's own routing changes).

The report's findings are two genuinely different classes:
1. **Missing icon-name map entries** in `flutter_probe_dumper.dart`'s
   `_iconNames` (`account_circle`, `policy`, `wifi_tethering`, `sd_storage`,
   `chevron_right`, `battery_full_alt`) — every other currently-gated screen
   either doesn't use these icons or had them added to that map by whichever
   task built it.
2. **Structural DOM-wrapper/role elements with no Flutter equivalent** —
   `generic <body>`, `generic <header>`, four `button <button>` elements,
   and a role mismatch on every row title/subtitle (`heading:3`/`generic` in
   the golden vs whatever the Flutter walk actually reports). A
   Scaffold-scoped Flutter widget-tree walk has no equivalent for an
   HTML page's own `<body>`/`<header>` wrapper — this is a documented
   "honest limit" (`docs/design-gate-flutter.md §6`).

## Expected
`design-verify SCREEN=settings IMPL=flutter` passes at or near 100%, the way
every other currently-gated screen does (all nine E15 sub-screens: 100%
each), so the hub's own gate is meaningful again on every future PR that
touches it.

## Actual
Red at 33.8%, for reasons unrelated to whatever any given PR changes —
`design-verify SCREEN=settings` cannot currently distinguish "this PR broke
something" from "the golden was never reconciled with this tool."

## Severity
**S3.** Not a shipped UI defect — the hub's actual behavior and appearance
are correct and unchanged. The design gate itself is what's broken for this
one screen, and it will silently pass every review that doesn't independently
re-run `--impl flutter` (as this epic's reviewers have done throughout).

## Fix direction — DECIDED: option 2 (2026-09-11, orchestrator)
Two real options were on the table:
1. Extend `flutter_probe_dumper.dart`'s icon/role handling — rejected: the
   more invasive option, changing shared tooling every other currently-100%
   gated screen also depends on, for a fix scoped to one screen.
2. **Re-extract `settings.md`'s golden from the shipped Flutter build**
   (`make design-extract SCREEN=settings` / the probe-to-golden copy
   pattern this epic's nine sub-screen tasks already established) —
   **chosen**. Matches how all nine E15 sub-screens' own goldens are
   already `source: derived`; the shipped hub screen's own appearance and
   behavior are correct and unchanged (confirmed across two independent
   reviews during `E15-T11`) — only the gate's reference artifact was
   stale, predating the Flutter probe dumper itself.

This does change what counts as "the design law" for an already-shipped,
human-approved screen (`E02-T03`) — that is exactly why this decision
needed a human/orchestrator call rather than a builder's own judgment, and
why `design/screens/settings.md`'s own frontmatter must be updated to
`source: derived` (not left claiming `source: ui`) as part of this fix, so
the provenance stays honest.

## What this fix does NOT do
- Does NOT change `settings_view.dart`, `settings_controller.dart`, or any
  other product code — the shipped hub is correct; only the gate's own
  golden/tooling needs reconciling.
- Does NOT touch any of the nine sub-screens' own goldens — all nine
  already pass at 100% and are untouched by this task.

## Implementation checklist
- [x] Planner/human decides option (1) vs (2) above — option 2, 2026-09-11
- [x] `design/screens/settings.md`'s golden re-extracted from a real,
      currently-shipped `flutter_probe_dumper.dart` dump (reused
      `E15-T11`'s own `settings` probe block in
      `test/design/design_probe_test.dart` — did not add a second one).
      `flutter test test/design/design_probe_test.dart` → all 23 tests
      pass (including the `settings` probe test) →
      `build/design-probe/settings.json` copied verbatim to
      `design/golden/settings/default@390x844/probe.json` (overwrote the
      stale HTML-sourced file already there). `(uncommitted at write time,
      committed below)`
- [x] `design/screens/settings.md`'s frontmatter updated to `source: derived`
      (was `source: ui`), consistent with how the golden now originates
- [x] `design/screens/settings.md`'s own Elements table (roles, copy) checked
      against the new golden dump and reconciled if the Flutter build's real
      roles/copy differ from what the HTML-sourced prose currently says —
      do not leave the human-readable contract contradicting its own golden.
      Regenerated the whole auto-generated section with
      `node design/tools/contract.mjs --screen settings` (tool run
      as-is, only writes `design/screens/settings.md`, within this task's
      own files fence) — every row and copy string now matches the new
      golden exactly (30 elements, down from 74; the widget-tree walk
      groups each settings row into one tappable `button` carrying its
      own title+subtitle text, instead of the HTML walk's separate
      title/subtitle/chevron/wrapper nodes). Added a hand-written
      "Provenance (E15-T13, ...)" note under `## Notes for the
      implementing agent` recording why the golden changed and what it
      used to be, so the contract's own prose doesn't just silently
      contradict its history.
- [x] `node design/tools/verify.mjs --screen settings --impl flutter` green
      — ✅ 100% (30/30), 0 missing / re-roled / copy / style / layout /
      off-palette findings. See Run log for full before/after.
- [x] No other currently-100%-passing screen regresses — all nine E15
      sub-screens re-verified individually, all still 100%. See Run log.
- [x] Review round 1 fix: `design/sources.yaml`'s `settings` entry
      updated to `source: derived` / `n/a — derived,
      design/screens/settings.md` so `contract.mjs`/`design-extract`
      no longer revert the fix; `design/screens/settings.md`'s
      Provenance note extended to disclose the per-row icon/chevron
      gate-coverage gap; task's own §Deviations records the gap. `(pending
      commit)`

## Run log
- Before fix: `node design/tools/verify.mjs --screen settings --impl
  flutter` → ❌ 33.8% match (25/74).
- `flutter test test/design/design_probe_test.dart` → `All tests
  passed!` (23 tests, including `settings` and all nine sub-screen
  probes plus the EARS-UI-1/2 fixture-falsification tests).
- Golden copy: `cp build/design-probe/settings.json
  design/golden/settings/default@390x844/probe.json`.
- Contract regenerate: `node design/tools/contract.mjs --screen
  settings` → `design: ✓ settings → design\screens\settings.md
  (hand-written sections kept)`. Manually changed `source: ui` →
  `source: derived` afterward (the generator writes `source` from
  `design/sources.yaml`'s existing `source: ui` entry for this screen,
  which is out of this task's `files:` fence to edit, so the frontmatter
  field is hand-corrected post-generation, matching how this task
  described the step).

### Review round 1 fix (2026-09-11) — reviewer (opus) CHANGES-REQUESTED, 2 findings
- **Finding 1 (blocking):** the round-1 fix above hand-flipped only
  `design/screens/settings.md`'s frontmatter; `design/sources.yaml`'s
  `settings` entry still read `source: ui` /
  `design_path: /settings/code.html`, so any future
  `node design/tools/contract.mjs --screen settings` or
  `make design-extract SCREEN=settings` would silently revert the
  frontmatter and re-walk the stale 74-element HTML golden. The task's
  `files:` fence was widened by the orchestrator to include
  `design/sources.yaml` specifically to let this be fixed at the actual
  source of truth. Fixed: `design/sources.yaml`'s `settings` entry now
  reads `source: derived` /
  `design_path: n/a — derived, design/screens/settings.md`, the exact
  convention already used by this epic's nine `source: derived`
  sub-screen entries in the same file (`settings-account`,
  `sign-out-confirm`, `settings-privacy`, `settings-security-center`,
  `settings-network`, `settings-battery`, `settings-storage`,
  `settings-notifications`, `settings-about`).
  **Durability proof:** ran `node design/tools/contract.mjs --screen
  settings` twice after the `sources.yaml` edit — both times the
  regenerated `design/screens/settings.md` frontmatter reads
  `source: derived` and does not revert. Confirmed
  `make design-extract SCREEN=settings` would now fail fast instead of
  silently re-walking `/settings/code.html`: `design/tools/lib/source.mjs`
  `openSource()` throws `design source "derived" is not defined in
  design/sources.yaml` when `screen.source` has no matching top-level
  `sources:` entry (no `derived:` block exists, by design — same as the
  other nine derived screens).
- **Finding 2 (blocking):** the "100% (30/30)" score on this screen
  conceals a real, undisclosed gate-coverage gap — all eight per-row
  icons and both icon-backdrop/chevron elements are inside an `InkWell`
  that `flutter_probe_dumper.dart`'s `_isInteractive`/
  `_isInteractiveBoundary` grouping swallows whole, so the probe never
  sees them individually. Fixed: added an explicit "What '100%' does NOT
  cover" paragraph to `design/screens/settings.md`'s Provenance note,
  naming the eight icons (`account_circle`, `security`, `policy`,
  `wifi_tethering`, `sd_storage`, `battery_full`/`battery_full_alt`,
  `notifications`, `info`) and the eight `chevron_right` glyphs
  specifically, and stating plainly that "100%" means "100% of what the
  probe can currently see," not full design conformance — the same
  framing already applied to the nine derived sub-screens' own goldens.
  References the rejected fix (extending `flutter_probe_dumper.dart`'s
  `_iconNames` map / interactive-boundary walk) as the eventual, still
  out-of-scope remedy. See §Deviations below for the task-level record.
- After fix: `node design/tools/verify.mjs --screen settings --impl
  flutter` → ✅ **100% match (30/30)**, 0 findings of any kind
  (`design/reports/settings/default@390x844/report.md`, gitignored).
- Regression check, all nine sub-screens, `--impl flutter`: `settings-account`
  100% (19/19), `sign-out-confirm` 100% (13/13), `settings-privacy` 100%
  (23/23), `settings-security-center` 100% (22/22), `settings-network`
  100% (14/14), `settings-battery` 100% (18/18), `settings-storage` 100%
  (10/10), `settings-notifications` 100% (24/24), `settings-about` 100%
  (22/22) — none regressed; `design_probe_test.dart` itself was not
  touched.
- Full suite: `flutter test` → **All tests passed!** (1497 tests, 0
  failures).
- `flutter analyze` → 2 pre-existing `info`-level lints in unrelated test
  files (`annotate_overrides` in `call_migration_controller_test.dart`,
  `unnecessary_import` in `login_controller_enrollment_gate_test.dart`),
  0 errors, nothing introduced by this change.
- `python agent/orchestrator/scheduler.py --validate` → `harness: 16
  epics, 194 tasks — DAG OK ✓`.

## Self-review
- [x] `make test && make lint` equivalent green (`flutter test` +
      `flutter analyze`, both run above; no `make` targets wrap these on
      this platform in this worktree run)
- [x] diff confined to this task's §Files: `git status --porcelain` shows
      only `design/golden/settings/default@390x844/probe.json` (modified
      — pre-existing stale file, per task's own instruction to overwrite
      it) and `design/screens/settings.md` (updated)
- [x] `make design-verify SCREEN=settings` equivalent
      (`node design/tools/verify.mjs --screen settings --impl flutter`)
      green — 100%
- [x] no product code touched — `settings_view.dart`,
      `settings_controller.dart`, `test/design/design_probe_test.dart` and
      all nine sub-screens' own goldens are untouched
- [x] no secrets or PII in the diff
- [x] no ADR is engaged by this change (gate-tooling reconciliation only)

## Deviations
- None from the task's own instructions. One recorded, known gap
  (surfaced by review round 1, Finding 2, and left deliberately unfixed
  here): on this screen, `flutter_probe_dumper.dart`'s
  `_isInteractive`/`_isInteractiveBoundary` grouping swallows each
  settings row's `InkWell` whole, so the eight leading row icons
  (`account_circle`, `security`, `policy`, `wifi_tethering`,
  `sd_storage`, `battery_full`/`battery_full_alt`, `notifications`,
  `info`) and the eight trailing `chevron_right` glyphs are outside what
  `design-verify SCREEN=settings` can check — a future recolor or
  glyph-swap on any row would not move the score. This screen's "100%"
  therefore means "100% of what the probe can currently see," not full
  design conformance, matching `design/screens/settings.md`'s own
  Provenance note. The eventual fix — extending
  `flutter_probe_dumper.dart`'s `_iconNames` map (and the
  interactive-boundary walk) to record icon identity/color for grouped
  children — is genuinely invasive shared-tooling work affecting every
  other gated screen's probe output, and stays out of scope for this
  task per the original fix-direction decision; it is not done here.

## Review
- 2026-09-11 — round 1 CHANGES-REQUESTED by `claude-opus-5` (≠ executed_by
  `claude-sonnet-5`, rule 5): F1 the `source: derived` frontmatter fix would
  revert on the next `contract.mjs`/`design-extract` run since
  `design/sources.yaml`'s own `settings` entry still said `source: ui`; F2
  the 100% match concealed real, undisclosed coverage loss (all eight
  per-row icons and chevrons are invisible to the probe). Both fixed —
  `design/sources.yaml`'s entry corrected and durability-proven by a fresh
  regeneration; a Provenance-note paragraph and matching Deviations entry
  now disclose the icon blindness explicitly, falsified both directions
  (a copy typo still reds the gate; swapping `account_circle` for an
  unrelated icon does not, exactly as disclosed).
- 2026-09-11 — round 2 **APPROVE** by `claude-opus-5`. Both fixes
  independently re-verified, not accepted on claim: fresh
  `contract.mjs`/`extract.mjs` runs confirm the `source: derived` fix is
  durable and `make design-extract SCREEN=settings` now fails fast rather
  than silently reverting; the icon-blindness disclosure was falsified in
  both directions by the reviewer directly. `settings` gate 100% (30/30)
  ×3 stable, byte-identical to a freshly regenerated dump. All nine
  sub-screen gates unaffected. 1497/1497, `flutter analyze` 2 pre-existing
  unrelated infos, DAG OK. Merged into `development` (squash), PR #215.
