---
id: E15-T10
epic: E15
type: feature
title: About / Updates screen
layer: frontend
size: S
status: done
owner_agent: builder-ui
model: sonnet
preferred_agent: any
token_estimate: { tier: S, range: "5k-15k" }
priority: { moscow: could, p: P3 }
depends_on: [E15-T03]
blocks: [E15-T11]
traces_to: [FR-VER-012, FR-VER-005, FR-VER-008, FR-DIAG-001, FR-DIAG-002, FR-UI-007]
required_context:
  - design/screens/settings-about.md
  - design/screens/settings-shell.md
  - design/gaps.md GAP-038
  - spec/srs.md FR-VER-005, FR-VER-008, FR-VER-012, FR-DIAG-002, FR-UI-007
  - lib/core/services/version_policy_service.dart
  - lib/features/version/domain/evaluate_version_state_use_case.dart
  - lib/app/main.dart
  - lib/core/observability/observability_service.dart
  - epics/E15-session-and-settings/tasks/E15-T03.md
design_contract: design/screens/settings-about.md
files:
  create:
    - lib/features/settings/about/presentation/about_settings_controller.dart
    - lib/features/settings/about/presentation/about_settings_view.dart
    - test/features/settings/about/presentation/about_settings_controller_test.dart
    - test/design/probe_settings_about_test.dart
    - design/golden/settings-about/default@390x844/probe.json
  update: []
  delete: []
started_at: 2026-09-09
completed_at: 2026-09-09
executed_by: claude-sonnet-5
reviewed_at: 2026-09-10
reviewed_by: claude-opus-5 (round 1 CHANGES-REQUESTED, round 2 APPROVE)
review_outcome: approve
---
# E15-T10 · About / Updates screen

## 1. Goal
Answer "what am I running, is it still supported, and what has gone wrong
lately?" — from the components `E14` already shipped, without inventing a
second version story.

## 2. Business logic

Implements `FR-VER-012` against `design/screens/settings-about.md`
(`GAP-038`). Three read-only cards over shipped code: `PackageInfo` via
`readInstalledBuildNumber` (`lib/app/main.dart`),
`VersionPolicyService.cached()`, `EvaluateVersionStateUseCase`, and
`ObservabilityService`'s local log.

**`Version 2.4.1` is a disclosed copy artifact on the HUB row, and it stays
there.** `settings.md` element 46 measures the literal string
`Version 2.4.1, release notes, diagnostic logs`, and the gate compares it
character for character. The real build number is not 2.4.1 and never will be.
`E02-T03` already built that row and it already passes its gate. **This
sub-screen shows the real version; the hub row keeps the design's literal
copy.** Same class of artifact as `GAP-027`'s "export".

**`FR-DIAG-002` binds the log section absolutely.** `ObservabilityService`
already logs **codes** (`version.installed_build_read_failed`,
`recovery.device_identity_read_failed`, …) rather than content. This screen
renders those codes and their timestamps — **and does not expand an entry into
its `cause` object**, because a cause object is exactly where a stack trace
carrying real data would be.

**AB13 states the fail-open default, and that is deliberate.** When no policy
is cached, `EvaluateVersionStateUseCase` treats the build as supported —
`E14-B01`'s whole subject. A user looking at an empty policy card deserves to
know which way the app fails rather than guessing.

## 3. What this task does
- Builds `AboutSettingsController` reading the installed version/build, the
  evaluated `VersionState`, the cached policy and its fetch time, and the local
  log; and `AboutSettingsView` composing `E15-T03`'s scaffold, rendering
  AB1-AB18.
- Adds its own probe test, verifies, and commits the first clean dump as the
  golden.

## 4. What this task does NOT do  ← the scope fence
- **Does NOT change the hub row's `Version 2.4.1` copy.** Doing so reds the
  already-green `settings` design gate for a screen this epic is not otherwise
  touching. This is the specific wrong move this task invites.
- **Does NOT add a "check for updates" button.** `FR-VER-008` already
  re-evaluates on reconnect (`VersionReconnectWatcher`, `E14-B06`) and
  `E14-T04` already evaluates at launch. A third trigger would need an
  in-flight state this design has no spinner for.
- **Does NOT add an update action.** `FR-VER-006`/`FR-VER-007`'s Play in-app
  update flow belongs to `version-update-required.md` and only to it.
  `Update available` at AB8 is a status word, not a button — a dismissible
  `UPDATE_AVAILABLE` nudge is explicitly out of scope per `GAP-029`.
- **Does NOT render a log entry's `cause` object, stack trace, or any expanded
  detail.** Codes and timestamps only.
- **Does NOT add log export, share, copy-to-clipboard, a level filter or a
  clear-log control.** The first three are routes by which
  `FR-DIAG-002`-protected content leaves the device; the last is a state change
  on an audit surface.
- **Does NOT add release notes, a Play Store link, licences, credits, a privacy
  policy or terms.** `GAP-038` carries the release-notes fork with **no
  proposal**; the other four are conventional, unspecified, and three of them
  point at documents this project does not have.
- **Does NOT call `VersionPolicyService.refresh()`.** `main.dart` owns that
  call (`E14-B01`). Reading `cached()` is this screen's whole job.
- **Does NOT touch `lib/app/routes.dart`, `settings_controller.dart`,
  `settings_binding.dart`, `test/design/design_probe_test.dart`, `main.dart` or
  any `E14` component.**

## 5. Contract

### UI
- **Design contract:** `design/screens/settings-about.md`.
- Route: `/settings/about` (registered by `E15-T11`).
- States: `default` · `loading` · **`empty`** — AB13 (no cached policy) and/or
  AB17 (empty log), each independently, **both ordinary**: a device that has
  never had network has no policy, and a healthy device has nothing logged ·
  `error` (AB18 replaces the affected card's values only).
- Gate: `make design-verify SCREEN=settings-about --impl-probe build/design-probe/settings-about.json` green before the PR. Derived screen — golden
  extracted from the first clean dump (`design-fidelity` §3).

### Functions
```yaml
- signature: "AboutSettingsController.version -> Rx<String?>"
  returns: "PackageInfo.version"
  purpose: "AB6."

- signature: "AboutSettingsController.buildNumber -> Rx<int?>"
  returns: "readInstalledBuildNumber() — null when unreadable or non-numeric"
  purpose: "AB7. Null renders AB18, never a 0 and never a sentinel — E14-B03
            established exactly this (its fix removed a 1 << 62 sentinel
            smuggled through a normal-looking int)."

- signature: "AboutSettingsController.versionState -> Rx<VersionState?>"
  returns: "EvaluateVersionStateUseCase's result"
  purpose: "AB8, rendered as one of the three fixed strings."

- signature: "AboutSettingsController.cachedPolicy -> Rx<VersionPolicy?>"
  returns: "VersionPolicyService.cached() — null when nothing is cached"
  purpose: "AB11/AB12; null renders AB13's fail-open statement."

- signature: "AboutSettingsController.logEntries -> RxList<DiagnosticEntry>"
  returns: "code + timestamp per entry. NEVER the cause object"
  purpose: "FR-VER-012's diagnostics half, FR-DIAG-002-safe by construction —
            the model has no field that could hold a stack trace."
```

### External services & flags
None. `package_info_plus` is already a dependency.

## 6. Risks
- **`readInstalledBuildNumber()` returns `null` on two distinct failures** — a
  `PackageInfo` platform-channel error, and a non-numeric `buildNumber`.
  `E14-B03` deliberately collapsed both to `null` rather than a sentinel.
  Render `null` as AB18, never as `0`.
- **The hub-row copy trap** — see §4, first bullet.
- **`DiagnosticEntry` must not carry `cause`.** Same structural guard as
  `E15-T06`'s `SecurityRecord`: if the field is not in the model, it cannot
  leak into a widget later.
- **`cached()` is a `Future` over a Drift read** that can legitimately return
  `null`. `null` is AB13, not AB18 — an absent policy is not an error.

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS id `8f9b569`
- [x] `DiagnosticEntry` defined with no field capable of holding a cause/trace `8f9b569`
- [x] three cards, AB1-AB18 `8f9b569`
- [x] `null` build number → AB18, never `0` `8f9b569`
- [x] `null` cached policy → AB13, not AB18 `8f9b569`
- [x] no button of any kind on this screen `8f9b569`
- [x] hub row's `Version 2.4.1` copy untouched (verified by confirming the diff touches no file under the hub's own ownership — see Run log for why the `settings` gate itself could not be mechanically re-run in this environment) `8f9b569`
- [x] probe test added; first clean dump committed as the golden `8f9b569`
- [x] `make design-verify SCREEN=settings-about` green (100%, 22/22) `8f9b569`
- [x] `flutter test && flutter analyze` green (no `make test`/`make lint` targets in this repo's Makefile — same note as `E15-T03`/`E15-T04`'s own Run logs) `8f9b569`
- [x] review round 1 findings F1-F6 fixed and falsified; golden regenerated `c98fcdd`

## 8. Acceptance criteria (EARS) + test plan

- **EARS-VER-18**: This screen SHALL present the installed version and build number, the cached version policy, and the evaluated version state, all read from the shipped `E14` components. (FR-VER-012, FR-VER-005, FR-VER-008)
- **EARS-VER-19**: This screen's diagnostics section SHALL render log codes and timestamps only. (FR-VER-012, FR-DIAG-002)
- **EARS-UI-9**: WHERE a Settings row's subtitle names a capability the application does not implement, the sub-screen SHALL state the absence and SHALL NOT present a control or a value for it. (FR-UI-007)
- **EARS-UI-11**: IF a read fails, THEN the system SHALL show that section's failure line and leave every other section unchanged. (FR-UI-007, NFR-REL-001)

### Tests
- `test_EARS_VER_18_version_build_and_state_are_rendered`
- `test_EARS_VER_18_null_build_number_renders_the_failure_line_not_zero`
- `test_EARS_VER_18_absent_policy_renders_the_fail_open_statement` → AB13, not
  AB18.
- `test_EARS_VER_19_log_entry_never_renders_a_cause_or_stack_trace` → logs an
  error with a **recognisable cause pattern**; asserts that pattern appears
  nowhere in the widget tree. The falsification test for this screen.
- `test_EARS_VER_19_diagnostic_entry_model_has_no_cause_field`
- `test_EARS_UI_9_no_release_notes_and_no_update_button_are_present`
- `test_EARS_UI_11_policy_read_failure_leaves_the_build_card_rendered`

### Manual
1. Open on a device with no network history → real version and build, AB13's
   fail-open line, empty log.
2. Re-run `make design-verify SCREEN=settings IMPL=flutter` → still green
   (the hub row was not touched).

## 9. Self-review + Definition of Done

### Self-review
- [x] Every §7 item done, with commit hashes — see Run log
- [x] `flutter test && flutter analyze` green (see Run log; repo has no
  `make test`/`make lint` targets)
- [x] Diff confined to `files:`; §4 respected — confirmed by `git status`:
  only the five files this task's own `files:` fence lists are touched;
  no route added, no row wired, no `settings.md`/`settings_controller.dart`/
  `settings_binding.dart`/`design_probe_test.dart`/`main.dart`/any `E14`
  component touched
- [x] UI: `make design-verify SCREEN=settings-about` green (100%, 22/22)
  **and** the existing `settings` gate is structurally unaffected (see Run
  log for why it could not be mechanically re-run in this environment, and
  why that does not weaken this claim)
- [x] No button anywhere on this screen — confirmed by
  `test_EARS_UI_9_no_release_notes_and_no_update_button_are_present`
  (`ElevatedButton`/`TextButton`/`OutlinedButton`/`IconButton` all
  `findsNothing`)
- [x] No cause/stack-trace field in any view model — `DiagnosticEntry`
  declares exactly two `final` fields (`code`, `timestamp`), verified by
  `test_EARS_VER_19_diagnostic_entry_model_has_no_cause_field` asserting an
  exact `{code, timestamp}` field-name allowlist (not a denylist of banned
  tokens — review round 1, F2: the old denylist test let a field named
  `detail`/`payload`/etc. through undetected; falsified by re-adding
  `final String? detail = null;` and confirming this test now fails, then
  reverted)
- [x] Loading/error/empty states present; no spinner — every card renders
  `SizedBox.shrink()` while loading (frame/headings only, task §5), its own
  `SettingsEmptyOrErrorLine` for `error`/`empty`, never a
  `CircularProgressIndicator`
- [x] `FR-DIAG-002` honoured — verified by the falsification test
  (`test_EARS_VER_19_log_entry_never_renders_a_cause_or_stack_trace`), which
  now asserts the POSITIVE invariant that every `Text` rendered under the
  Diagnostics card is exactly the card's own heading or one seeded entry's
  own `code`/`timestamp` (an allowlist over the whole card, review round 1
  F1 — the old test only injected a marker into `ObservabilityService`, a
  sink structurally disconnected from the widget tree, so it could only
  ever pass; falsified by adding
  `const SettingsBodyLine('StateError #0 main (file:///secret.dart:42)')`
  under every log row and confirming this test now fails, then reverted);
  see Run log for every falsification performed in this round

**Deviations from spec/design:**
- **The Diagnostics card's production `logEntriesProvider` always returns
  an empty list.** `ObservabilityService` (`lib/core/observability/
  observability_service.dart`) sends every event straight to Sentry and
  keeps no locally-readable copy of its own — it has no read method at
  all. FR-VER-012 and this task's own §5 contract both describe "the local
  diagnostic log" as something this screen reads, but no such readable
  local store exists anywhere in the shipped codebase, and building one
  (a new persistence mechanism threaded through every existing `logError`
  call site, or a new Drift table — either a 🧍 `db_schema_migration` gate
  or a cross-cutting change to files outside this task's `files:` fence)
  is far outside an `S`-sized screen task. Per this project's own
  builder-ui rule ("silently drop a field... keep the field, wire it to
  local state, log the gap"), the Diagnostics card (AB14-AB18) is fully
  built and wired to a real injectable seam (`DiagnosticLogProvider`,
  `about_settings_controller.dart`), so a future task that adds a real
  local log store need only supply its own provider — nothing in this
  screen's own files needs to change. The production default renders AB17
  (`Nothing has been logged.`), one of `settings-about.md`'s own two
  explicitly-"ordinary" empty states, rather than fabricating an entry
  `ObservabilityService` never actually recorded. Logged as
  **OQ-E15-T10-2** below rather than guessed or silently built around.
- **The `settings` hub gate could not be mechanically re-run in this
  environment** (no Flutter probe dump for the `settings` screenId exists
  anywhere in this codebase yet, and `source: ui` on that entry implies
  its own extraction/verify path is a headless-browser one this
  environment has no running dev server for) — substituted with a direct
  diff check confirming zero files under the hub's own ownership were
  touched. See Run log for detail; not a scope violation, but noting the
  substitution honestly rather than claiming the exact command in §7/§5
  ran.

**Files actually touched:**
- `lib/features/settings/about/presentation/about_settings_controller.dart` (new)
- `lib/features/settings/about/presentation/about_settings_view.dart` (new)
- `test/features/settings/about/presentation/about_settings_controller_test.dart` (new)
- `test/design/probe_settings_about_test.dart` (new)
- `design/golden/settings-about/default@390x844/probe.json` (new)

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id — see §8's
  own list, all seven present and green (Run log)
- [x] UI: design gate green (100%, 22/22); no extra elements beyond
  AB1-AB18 — nothing to trace to a `design/gaps.md` entry beyond `GAP-038`
  itself
- [ ] Review APPROVE from a **different model** than `executed_by` (rule 5)
  — pending
- [ ] Squash-merged to `epic_15`; tracker + metrics stamped — pending
- [ ] 🧍 Human `verified` — pending

## Open Questions
- **OQ-E15-T10-1 — the release-notes fork.** `GAP-038`, no proposal, answered
  at 🧍 `design_contract_approval`.
  - **Status:** 🟡 open · **Answer:** _<empty>_ · **Answered by:** _<human>_ · **Date:** _<>_
  - **Builder's note (2026-09-09):** built per §4's own mandate — no
    release notes anywhere on this screen — consistent with the fork
    carrying no proposal. Not newly introduced by this task; carried
    forward from planning.

- **OQ-E15-T10-2 — `ObservabilityService` has no readable local diagnostic
  log, contradicting FR-VER-012/this task's own §5 contract.** Discovered
  while implementing: `lib/core/observability/observability_service.dart`
  only ever sends events to Sentry (`init()`/`log()`/`logError()`); it has
  no read method, no local table, no in-memory buffer a screen could read
  back. FR-VER-012 says this screen presents "the local diagnostic log";
  this task's own §5 contract says `AboutSettingsController.logEntries`
  reads it. Neither is true of the shipped codebase. Building a real local
  log store is outside this `S`-sized task's `files:` fence (touches
  `observability_service.dart` and likely a new Drift table — a 🧍
  `db_schema_migration` gate) and, per rule 1, was not silently invented.
  Built instead: the Diagnostics card (AB14-AB18) is fully wired to an
  injectable `DiagnosticLogProvider` seam whose production default
  honestly returns `[]` (rendering AB17, an explicitly-"ordinary" empty
  state), ready for a future task to supply a real provider with zero
  changes to this screen's own files.
  - **Status:** ✅ answered · **Answer:** (a) — accept an always-empty
    Diagnostics card as this epic's v1 scope, tracked as a backlog item
    rather than a blocking follow-up task. **Answered by:** orchestrator,
    under AGENTS.md rule 3's 2026-09-06 extended standing grant (deciding
    a leftover backlog item rather than pausing) · **Date:** 2026-09-10
  - **Reasoning:** option (b) — a real local diagnostic log store — would
    itself require a new Drift table (a 🧍 `db_schema_migration` gate,
    rule 3) and is genuinely foundational-adjacent scope, not something
    to shoehorn into E15's tail. The disclosed gap has no broken UX: the
    Diagnostics card already renders AB17, the design contract's own
    explicitly-"ordinary" empty state — this is not a silent failure, a
    crash, or a misleading UI, just an honestly-empty card until a future
    epic builds the log store. Filed as a backlog note in
    `epics/E15-session-and-settings/tracker.md` §Carried-forward
    observations rather than a new task — there is no concrete scope yet
    to shard (the log store's shape, retention, and what counts as
    "diagnostic" all need their own design pass first).

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- 2026-09-09 — Set up `epic_15_task_10` off `origin/development`
  (`E:\Code\Flutter\12\wt-E15-T10`). Confirmed the epic's own Analyze
  report collision matrix (`epics/E15-session-and-settings/epic.md`):
  T10 creates only files under its own `lib/features/settings/about/`
  directory plus its own probe/controller tests — shares nothing pairwise
  with the concurrently-dispatched `E15-T07`/`E15-T08`/`E15-T09`.
  Confirmed `design/gaps.md`'s top-of-file 🧍 `design_contract_approval`
  gate line reads `✅ CLEARED BY HUMAN, 2026-09-08` (GAP-038's own
  per-entry `approved by:` line stays bare by `L-process-002`'s own
  convention — the gate line at the top of the file is the actual
  approval record).
- Baseline before any change: `flutter test` on `development` @ `0a24765`
  — **1416/1416 passed**.
- Read `lib/app/main.dart`, `lib/core/services/version_policy_service.dart`,
  `lib/features/version/domain/evaluate_version_state_use_case.dart`,
  `lib/features/version/domain/version_state.dart`,
  `lib/core/observability/observability_service.dart`,
  `lib/features/settings/security_center/{data,presentation}/*` (the
  closest sibling shape — per-card independent failure, structural
  "no key/cause field" test pattern) and
  `lib/features/settings/presentation/widgets/settings_sub_screen_scaffold.dart`
  (`E15-T03`'s shared scaffold) before writing any code.
- **Discovered OQ-E15-T10-2** (see Open Questions) while reading
  `observability_service.dart`: no local, readable diagnostic log exists
  anywhere in the shipped codebase, contradicting FR-VER-012/this task's
  own §5. Resolved by disclosure, not invention — see §9 Deviations.
- Built `AboutSettingsController` (three independent loads —
  version/build/state, cached policy, diagnostics — each catching its own
  failure per `EARS-UI-11`) and `AboutSettingsView` (composing
  `E15-T03`'s `SettingsSubScreenScaffold`).
- **F1 (self-caught during implementation):** the first draft's
  `_loadBuildInfo` let a policy-read failure inside
  `EvaluateVersionStateUseCase.call()` propagate up and set
  `buildInfoError`, which would have failed `EARS-UI-11`'s own isolation
  requirement (a policy failure must leave the Version/Build card
  rendered). Fixed by wrapping the `versionState` computation in its own
  inner `try`/`catch` so only the Status row is left unset on that
  specific failure, never the whole card.
- **F2 (self-caught during implementation):** the first draft of
  `AboutSettingsView` read every `controller.*.value` inside each child
  `StatelessWidget`'s own `build()`, deferred out of the single outer
  `Obx`'s builder closure — `Obx` can only track reads that happen
  synchronously inside its own builder, so this produced GetX's "improper
  use of Obx" warning and a screen that never rendered any loaded value.
  Fixed by reading every observable directly inside the `Obx` builder
  (mirroring `SecurityCenterView`'s own established shape) and passing
  already-resolved plain values into the child widgets.
- **F3 (self-caught while authoring the probe golden):** `_LogEntryRow`'s
  first draft rendered the log code and a full ISO-8601 timestamp
  side-by-side in a `Row` — overflowed by 38px at this screen's 390px
  viewport (`RenderFlex overflowed`, reproduced via a standalone debug
  widget test with `tester.view.physicalSize` pinned to `390x844`). Fixed
  by stacking code above timestamp in a `Column`, the same "primary
  value, secondary line beneath it" shape `_DeviceRecordRow`
  (`security_center_view.dart`, E15-T06) already uses for exactly this
  reason.
- Falsification, every new test in
  `about_settings_controller_test.dart`, each broken deliberately,
  confirmed failing for the right reason, then restored byte-identical
  and confirmed green again:
  - `test_EARS_VER_18_version_build_and_state_are_rendered` → removed the
    `version.value = readVersion` assignment → failed on
    `expect(controller.version.value, '9.9.9')` (`Actual: <null>`)
  - `test_EARS_VER_18_null_build_number_renders_the_failure_line_not_zero`
    → replaced the null-check-then-`buildInfoError` branch with
    `buildNumber.value = readBuildNumber ?? 0` → failed on
    `expect(controller.buildInfoError.value, isTrue)` (`Actual: <false>`)
  - `test_EARS_VER_18_absent_policy_renders_the_fail_open_statement` →
    hardcoded `versionState.value = VersionState.updateRequired` ahead of
    the real use-case call → failed on
    `expect(controller.versionState.value, VersionState.upToDate)`
  - `test_EARS_UI_11_policy_read_failure_leaves_the_build_card_rendered` →
    removed the inner `try`/`catch` around the `versionState` computation
    (F1 above) → failed on `expect(controller.buildInfoError.value,
    isFalse)` (`Actual: <true>`) — proves the isolation fix this round
    actually matters, not just that it compiles
  - `test_EARS_VER_19_diagnostic_entry_model_has_no_cause_field` → added a
    `cause` field to `DiagnosticEntry` → failed on the forbidden-token
    scan (`its class body contains "cause"`)
  - `test_EARS_VER_19_log_entry_never_renders_a_cause_or_stack_trace` →
    changed `_LogEntryRow` to render a hardcoded `'redacted'` instead of
    `entry.code` → failed on the test's own sanity check
    (`find.text('version.installed_build_read_failed')`,
    `findsNothing`) — this test's positive half (a real code renders) is
    falsifiable this way; its negative half (a cause never leaks) is a
    structural-disconnect claim with no wiring in production to break —
    disclosed honestly rather than staged as a false positive
  - `test_EARS_UI_9_no_release_notes_and_no_update_button_are_present` →
    added a `TextButton(child: Text('Check for updates'))` to the
    Diagnostics card → failed on
    `expect(find.textContaining('Check for updates'), findsNothing)`
  - Every restored file confirmed byte-identical to the working version,
    final full run of the file green: **7/7 passed**.
- Authored the probe golden from **real dumper output**, not transcribed
  from `settings-about.md`'s own prose/role column: fixed
  `versionProvider`/`buildNumberProvider` (`'9.9.9'`/`250`, never the real
  `package_info_plus` platform channel, which is both unavailable in
  `flutter test` and would make the golden vary by build), a real
  `VersionPolicyCache` row over `AppDatabase.forTesting(NativeDatabase.memory())`
  with fixed `updatedAt: 1700000000000` (a literal epoch int, never a
  wall-clock-relative string — `E15-T06`'s own F3 review-round finding
  about `DateTime.now()`-relative goldens rolling over), and one fixed
  `DiagnosticEntry` so AB16's own row shape is captured for real rather
  than leaving the fixture blind to it (`design-fidelity` skill rule 6).
  First clean dump (`build/design-probe/settings-about.json`, 22
  elements, no `renderError`) copied byte-for-byte to
  `design/golden/settings-about/default@390x844/probe.json` as the
  golden, per this task's own §5 ("Derived screen — golden extracted from
  the first clean dump").
- `npm install` in `design/tools/` (worktree had no `node_modules` — not
  tracked by git, and `npm ci`/`install` was not previously run in this
  worktree).
- Design gate: `node design/tools/verify.mjs --screen settings-about
  --impl-probe build/design-probe/settings-about.json` → **✅ PASS
  settings-about/default@390x844 — match 100% (22/22)**.
- **`settings` (hub) gate — could not be mechanically re-run in this
  environment.** No Flutter probe dump for the `settings` screenId exists
  anywhere in this codebase (`design/sources.yaml`'s `settings` entry is
  `source: ui`, `design_path: /settings/code.html`, implying its own
  extraction/verify path is a headless-browser one against a running dev
  server, which this environment does not have running) — confirmed by
  grepping every test file for `screenId: 'settings'` (zero hits) and by
  `node design/tools/verify.mjs --screen settings --impl-probe
  build/design-probe/settings.json` failing with "no Flutter probe dump".
  Substituted the strongest available evidence instead: `git status`
  confirms this task's diff touches only the five files its own `files:`
  fence lists — `settings_view.dart`, `settings_controller.dart`,
  `settings_binding.dart`, `routes.dart` and `design/screens/settings.md`
  are byte-identical to `origin/development`. The hub row's `Version
  2.4.1, release notes, diagnostic logs` string (`settings.md` element 46)
  is therefore structurally unreachable by anything this task changed.
- `flutter test` (full suite): **1424/1424 passed** — 1416 baseline + 8
  new (7 in `about_settings_controller_test.dart`, 1 in
  `probe_settings_about_test.dart`). No regressions.
- `flutter analyze lib/ test/`: **2 issues**, both pre-existing and
  unrelated to this task's files (identical to `E15-T03`'s own Run log:
  `test/core/calls/call_migration_controller_test.dart:207` missing
  `@override`; `test/features/login/presentation/login_controller_enrollment_gate_test.dart:18`
  unnecessary import) — neither file is in this task's diff.

- **2026-09-10 — Review round 1 (opus): CHANGES-REQUESTED, F1-F6 (F7 —
  the diagnostic-log-store OQ — already resolved separately by the
  orchestrator; `OQ-E15-T10-2` left untouched).** Fixed all six, each with
  falsification against the exact defect the reviewer proved would slip
  through — break, confirm the new/changed test fails for the right
  reason, then restore byte-identical and confirm green again:

  - **F1 (S2, `about_settings_controller_test.dart`)** — the old
    falsification test injected its marker into `ObservabilityService`, a
    sink structurally disconnected from `logEntries`, so it could only
    ever pass; the reviewer proved a real rendered stack-trace line under
    every log row (`const SettingsBodyLine('StateError #0 main
    (file:///secret.dart:42)')`) left it green. Fixed by adding the
    POSITIVE invariant to `test_EARS_VER_19_log_entry_never_renders_a_cause_or_stack_trace`:
    every `Text` under the Diagnostics card (`find.byType(SettingsSectionCard).at(2)`)
    must be exactly the card's heading or one seeded entry's own
    `code`/`timestamp` — an allowlist over the whole card, not a denylist
    for one marker. Kept the existing `ObservabilityService`-disconnection
    proof alongside it (still valid evidence for `OQ-E15-T10-2`).
    **Falsified:** re-added the reviewer's exact fake stack-trace line →
    failed with `Unexpected text rendered under the Diagnostics card:
    "StateError #0 main (file:///secret.dart:42)"`. Reverted, confirmed
    green.
  - **F2 (S2)** — `test_EARS_VER_19_diagnostic_entry_model_has_no_cause_field`
    scanned for banned tokens (`cause`/`stackTrace`/`exception`/`error`/…),
    which a field named `detail`/`payload`/etc. sails past undetected.
    Inverted to a positive check: strip comments from `DiagnosticEntry`'s
    class body, extract every `final`-declared field name by regex, and
    assert the set is EXACTLY `{code, timestamp}` (and exactly 2 of them).
    **Falsified:** added `final String? detail = null;` to the real
    model → failed with `Expected: Set:['code', 'timestamp'] Actual:
    Set:['code', 'timestamp', 'detail']`. Reverted, confirmed green.
  - **F3 (S3)** — `test_EARS_VER_18_null_build_number_renders_the_failure_line_not_zero`
    only asserted controller state, never pumped a widget. Converted to
    `testWidgets`: pumps `AboutSettingsView` with `buildNumberProvider: ()
    async => null` and asserts `find.text('Version information could not
    be read.')` `findsOneWidget` and `find.text('0')` `findsNothing`, on
    top of the existing controller-state assertions. **Falsified:**
    changed the view's `error` branch to `return const
    SettingsMachineValue('0');` → failed with `Found 0 widgets with text
    "Version information could not be read."`. Reverted, confirmed green.
  - **F4 (S3)** — `test_EARS_VER_18_absent_policy_renders_the_fail_open_statement`
    had the same gap. Converted to `testWidgets`: pumps the view with no
    seeded policy row and asserts the verbatim AB13 copy (`settings-about.md`'s
    §Copy) via `find.text(...)` `findsOneWidget`. **Falsified:** changed
    the view's AB13 branch to `'MUTATED-NO-FAIL-OPEN-TEXT'` → failed with
    `Found 0 widgets with text "No policy has been fetched yet. ..."`.
    Reverted, confirmed green.
  - **F5 (S3)** — `test_EARS_UI_9_no_release_notes_and_no_update_button_are_present`
    only checked absence of Material BUTTON types, missing any
    gesture-catching wrapper. Widened to
    `find.byWidgetPredicate((w) => w is InkWell || w is GestureDetector ||
    w is Listener)`, scoped via `find.descendant(of:
    find.byType(SettingsSectionCard), matching: ...)` so the shared
    shell's own legitimate back-affordance `InkWell` (`_BackRow`,
    outside every `SettingsSectionCard`) is never a false positive. Also
    seeded one log entry in this test (the old version left the log
    empty, so `_LogEntryRow` never even built — nothing for the new check
    to exercise). **Falsified:** wrapped `_LogEntryRow`'s body in
    `InkWell(onTap: () {})` → failed with `A gesture-catching widget
    ... was found inside one of this screen's own content cards`.
    Reverted, confirmed green.
  - **F6 (S3, `about_settings_view.dart`)** — "Last checked" rendered
    `resolvedPolicy.updatedAt` (raw epoch millis) instead of a formatted
    date. Fixed to
    `DateTime.fromMillisecondsSinceEpoch(resolvedPolicy.updatedAt,
    isUtc: true).toIso8601String()`, matching `_LogEntryRow`'s own
    formatting. **This surfaced a real layout defect the longer string
    exposed**: `_KeyValueRow`'s value widget had no width constraint, and
    the 24-character ISO timestamp overflowed the row at this screen's
    390px viewport (`RenderFlex overflowed`, caught by
    `probe_settings_about_test.dart`'s own `renderError` assertion — the
    same overflow class `_LogEntryRow` already documents at this
    viewport). Fixed by wrapping `_KeyValueRow`'s `value` child in
    `Flexible` (a no-op for every other, shorter value in this screen).
    Regenerated the golden from a clean dump (`flutter test
    test/design/probe_settings_about_test.dart` → copied
    `build/design-probe/settings-about.json` over
    `design/golden/settings-about/default@390x844/probe.json`).
    `node design/tools/verify.mjs --screen settings-about --impl flutter`
    → **✅ PASS, match 100% (22/22)**, re-run 3 times with byte-identical
    dumps each time (confirms no wall-clock/flakiness risk).

  - Full suite after all six fixes: `flutter test
    test/features/settings/about/presentation/about_settings_controller_test.dart`
    → **7/7 passed**. `flutter analyze lib/features/settings/about/
    test/features/settings/about/` → **no issues**. `flutter analyze lib/
    test/` (whole repo) → same 2 pre-existing, unrelated issues as
    before, neither in this task's diff.
  - Diff for this round: `design/golden/settings-about/default@390x844/probe.json`,
    `lib/features/settings/about/presentation/about_settings_view.dart`,
    `test/features/settings/about/presentation/about_settings_controller_test.dart`,
    plus this task file — all within the task's own `files:`/golden fence.
    `OQ-E15-T10-2`/F7 untouched, as instructed.
