---
id: E15-T04
epic: E15
type: feature
title: Notifications settings screen
layer: frontend
size: M
status: done
owner_agent: builder-ui
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: must, p: P2 }
depends_on: [E15-T03]
blocks: [E15-T11]
traces_to: [FR-NOTIFY-003, FR-NOTIFY-001, FR-NOTIFY-002, FR-UI-007]
required_context:
  - design/screens/settings-notifications.md
  - design/screens/settings-shell.md
  - design/gaps.md GAP-032
  - spec/srs.md FR-NOTIFY-001, FR-NOTIFY-002, FR-NOTIFY-003, FR-UI-007
  - lib/core/notifications/notification_settings_repository.dart
  - lib/core/persistence/notification_tables.dart
  - epics/E15-session-and-settings/tasks/E15-T03.md
  - docs/design-gate-flutter.md
design_contract: design/screens/settings-notifications.md
files:
  create:
    - lib/features/settings/notifications/presentation/notification_settings_controller.dart
    - lib/features/settings/notifications/presentation/notification_settings_view.dart
    - test/features/settings/notifications/presentation/notification_settings_controller_test.dart
    - test/design/probe_settings_notifications_test.dart
    - design/golden/settings-notifications/default@390x844/probe.json
  update: []
  delete: []
started_at: 2026-09-08
completed_at: 2026-09-09
executed_by: claude-sonnet-5
reviewed_at: 2026-09-09
reviewed_by: claude-opus-5 (round 1 CHANGES-REQUESTED, round 2 APPROVE)
review_outcome: approve
---
# E15-T04 · Notifications settings screen

## 1. Goal
The human's own first priority: make the Notifications row lead somewhere, so a
user can decide which alerts this device shows and how much they reveal.

## 2. Business logic

Implements `FR-NOTIFY-003` — the user-facing surface for `FR-NOTIFY-001`'s
categories and `FR-NOTIFY-002`'s privacy level — against
`design/screens/settings-notifications.md` (`GAP-032`).

**The data layer already ships.** `NotificationSettingsRepository`
(`lib/core/notifications/`, `E10-T02`) is the sole source of truth:
`isEnabled` / `setEnabled` / `watchEnabled` per category, `privacyLevel` /
`setPrivacyLevel`. This screen holds **no** second copy of a preference and
**no** defaulting rule of its own — the repository's asymmetric defaults
(a missing category row is **enabled**, a missing/unknown privacy level is
**hidden**) are deliberate and documented in `notification_tables.dart`'s
header. Do not re-derive, mirror or "correct" them here.

Two rules from the contract that are the actual work:

1. **Nine categories, not ten.** `NotificationCategory.backgroundService` is
   **absent by contract**: Android requires that notification whenever the
   foreground service runs, so it is not a user preference and `E10-T02` seeds
   no row for it. A tenth row would be a control that cannot work.
2. **`full` privacy is offered as a value, not as a working feature.** It has
   no supported mechanism today — plaintext is decrypted only in the screen
   layer, and a background dispatcher cannot reach it. `FR-UI-007` and
   `EARS-NOTIFY-17` require NT21 to say so.

## 3. What this task does
- Builds `NotificationSettingsController` bound to
  `NotificationSettingsRepository`'s **streams**, and
  `NotificationSettingsView` composing `E15-T03`'s scaffold.
- Renders NT1-NT22 exactly as `settings-notifications.md` specifies, with the
  nine category rows in the contract's stated order and copy.
- Adds its own probe test (`test/design/probe_settings_notifications_test.dart`)
  producing `build/design-probe/settings-notifications.json`, runs
  `make design-verify SCREEN=settings-notifications --impl-probe …`, and
  commits the first clean dump as this screen's golden.

## 4. What this task does NOT do  ← the scope fence
- **Does NOT use a `Switch`, `Checkbox`, `Radio` or any Material toggle.** This
  design measures none, anywhere. On/off is SH12 —
  `radio_button_checked` / `radio_button_unchecked` — the treatment
  `settings-storage.md` SS14-16 already uses and the human already approved.
  "It's Material 3, `FR-UI-001` allows it" is the specific wrong argument this
  fence exists to pre-empt: `FR-UI-001` picks a design *system*, it does not
  license a primitive the design never draws.
- **Does NOT add a tenth category row** for `backgroundService`.
- **Does NOT hide, disable-and-grey, or remove the `full` privacy option.** It
  is shown with NT21's disclosure copy.
- **Does NOT add an LED control or a silent-mode control.** `GAP-032` carries
  that fork with **no proposal** and the default disposition is `GAP-027`'s
  approved (c) — no control, disclosed absence. Building one from the hub row's
  subtitle would be inventing scope from copy.
- **Does NOT add per-conversation muting, quiet hours, a sound picker, a
  vibration setting, or a "send a test notification" button.** None is in
  `FR-NOTIFY-*`; the first belongs on `chat.md` if it is ever specified, and
  Android's own notification channels already own the rest.
- **Does NOT post, build, suppress or decrypt any notification.** This screen
  writes preferences. `E10-T03` is the consumer that acts on them.
- **Does NOT touch `lib/app/routes.dart`, `settings_controller.dart`,
  `settings_binding.dart` or `test/design/design_probe_test.dart`.** All four
  are **`E15-T11`'s alone**. This screen is unreachable in the app until T11
  lands, and that is intended — the epic branch integrates as a whole.
- **Does NOT change `NotificationSettingsRepository` or any Drift table.**

## 5. Contract

### UI
- **Design contract:** `design/screens/settings-notifications.md` — build from
  this, not from `settings.md`'s row.
- Route: `/settings/notifications` (registered by `E15-T11`).
- States required: `default` · `loading` (frame + cards render, row states
  unpopulated — **no spinner**) · `error` (NT22 replaces the affected card's
  rows only, the other card unchanged). **No `empty` state** — both lists are
  fixed sets.
- Gate: `make design-verify SCREEN=settings-notifications --impl-probe build/design-probe/settings-notifications.json` **green before the PR** (rule 2).
  **This screen has no design source**, so its golden is extracted from the
  first clean dump of the build itself, per `design-fidelity` §3 and exactly as
  `E12-B06` records for `device-enrollment`. The gate is a **regression** gate
  from that commit onward; first-build fidelity is proven by the reviewer
  reading the contract's element and copy tables.

### Functions
```yaml
- signature: "NotificationSettingsController.categories -> List<NotificationCategory>"
  returns: "the nine user-facing values, in settings-notifications.md's order"
  purpose: "One place the nine-not-ten rule is expressed."

- signature: "NotificationSettingsController.toggle(NotificationCategory category) -> Future<void>"
  params: { category: "the row that was tapped" }
  returns: "void — delegates to NotificationSettingsRepository.setEnabled"
  purpose: "FR-NOTIFY-003's write path. No local state is mutated; the stream
            is what re-renders."

- signature: "NotificationSettingsController.selectPrivacy(NotificationPrivacyLevel level) -> Future<void>"
  params: { level: "hidden | senderOnly | full" }
  returns: "void — delegates to setPrivacyLevel"
  purpose: "FR-NOTIFY-002's write path. `full` is storable; storing it is not
            the same as honouring it (E10-T02's own header)."
```

### External services & flags
None.

## 6. Risks
- **Read-once instead of watch.** `watchEnabled` is a stream. A `Future`-based
  render goes stale the moment anything else writes, and the bug is invisible
  in a single-screen test.
- **Nine `watchEnabled` subscriptions.** One per category is correct and cheap;
  make sure they are disposed with the controller.
- **The copy is compared character for character.** Twenty-plus strings in
  §Copy, including the em dash in NT19's `Neither who nor what — "New message".`
  and the straight double quotes inside it. Copy them, do not retype them.
- **`full`'s row must still be tappable-looking enough to read but must not
  write.** Decide once, in the controller, and test it (`EARS-NOTIFY-17`).

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS id `98f70d8`
- [x] controller bound to `watchEnabled` streams, disposed cleanly `98f70d8`
- [x] nine category rows, contract order and copy `98f70d8`
- [x] privacy card with three levels; NT21 disclosure copy `98f70d8`
- [x] `loading` / `error` per §5; no spinner anywhere `98f70d8`
- [x] view composes `E15-T03`'s scaffold; no local frame `98f70d8`
- [x] probe test added; first clean dump committed as the golden `98f70d8`
- [x] `make design-verify SCREEN=settings-notifications` green `98f70d8`
- [x] `flutter test && flutter analyze` green (no `make test`/`make lint`
  targets in this repo's Makefile — same note as `E15-T03`'s Run log) `98f70d8`

## 8. Acceptance criteria (EARS) + test plan

- **EARS-NOTIFY-16**: WHEN a notification category is switched on this screen, the system SHALL persist that choice through `NotificationSettingsRepository` and SHALL reflect the persisted value on the next read. (FR-NOTIFY-003, FR-NOTIFY-001)
- **EARS-NOTIFY-17**: WHERE the `full` privacy level has no supported mechanism, this screen SHALL state that it is unavailable rather than presenting it as a working option. (FR-NOTIFY-003, FR-NOTIFY-002, FR-UI-007)
- **EARS-UI-9**: WHERE a Settings row's subtitle names a capability the application does not implement, the sub-screen SHALL state the absence and SHALL NOT present a control or a value for it. (FR-UI-007)
- **EARS-UI-11**: IF a read fails, THEN the system SHALL show that section's failure line and leave every other section unchanged. (FR-UI-007, NFR-REL-001)

### Tests
- `test_EARS_NOTIFY_16_toggling_a_category_persists_through_the_repository` →
  real in-memory `AppDatabase`, toggles, re-reads through the repository.
- `test_EARS_NOTIFY_16_screen_reflects_an_external_write` → writes via the
  repository directly, asserts the view updates (proves the stream binding).
- `test_EARS_NOTIFY_16_background_service_category_is_absent` → asserts exactly
  nine rows and that no row corresponds to `backgroundService`.
- `test_EARS_NOTIFY_17_full_privacy_row_shows_the_unavailable_copy`
- `test_EARS_NOTIFY_17_selecting_full_does_not_write` → **call counter** on the
  repository seam, `expect(calls, 0)`. Not `fail()` inside the seam — a broad
  catch swallows it (`L-testing`).
- `test_EARS_UI_9_no_led_control_is_present`
- `test_EARS_UI_11_category_read_failure_leaves_the_privacy_card_intact`

### Manual
1. Toggle three categories, force-quit, relaunch → all three persist.
2. Tap `Full` → nothing changes, the disclosure copy is visible.

## 9. Self-review + Definition of Done

### Self-review
- [x] Every §7 item done, with commit hashes — all `98f70d8`
- [x] `flutter test && flutter analyze` green (see Run log; repo has no
  `make test`/`make lint` targets)
- [x] Diff confined to `files:`; §4 respected — confirmed no route added,
  no row wiring, `settings_controller.dart`/`settings_binding.dart`/
  `test/design/design_probe_test.dart` untouched, no Drift table/repository
  change
- [x] UI: `make design-verify SCREEN=settings-notifications` green (100%,
  24/24 — see Run log)
- [x] No `Switch`/`Checkbox`/`Radio` anywhere in the diff — grepped the
  diff, confirmed absent; on/off uses `SettingsSelectionGlyph`
  (`radio_button_checked`/`radio_button_unchecked`, SH12) throughout
- [x] Loading/error states present; no spinner — `enabled == null` /
  `privacy.value == null` render an unpopulated selection slot, never a
  `CircularProgressIndicator`; `categoriesError`/`privacyError` each
  replace only their own card's rows with `SettingsEmptyOrErrorLine`
- [x] No routes, no row wiring, no shared probe-fixture edit — this
  screen's own fenced `test/design/probe_settings_notifications_test.dart`
  is separate from `test/design/design_probe_test.dart` (E15-T11's alone)
- [x] No secrets or PII logged (`FR-DIAG-002`) — no logging in this diff at all

**Deviations from spec/design:**
- **Review round 1 finding (F1, required, `claude-opus-5`, PR #195):**
  `NotificationSettingsController.toggle` read
  `_categoryEnabled[category]!.value ?? true`, re-implementing the
  repository's own fail-open default (a missing category row is enabled)
  inside the screen layer — exactly what task §2 prohibits ("no defaulting
  rule of its own ... do not re-derive, mirror or 'correct' them here").
  Consequence: `_CategoryRow.onTap` was wired and non-nullable even during
  the `loading` state (before the first `watchEnabled` emission, when
  `isEnabled()` is still `null` and the row shows no glyph), so a tap in
  that window wrote `false` from a *guessed* `true` rather than the real
  stored value. Fixed in review round 2:
  `toggle` now returns without writing when the value is still `null`
  (`lib/features/settings/notifications/presentation/notification_settings_controller.dart`),
  and `_CategoryRow.onTap`/the view's call site are now nullable and pass
  `null` while loading (`notification_settings_view.dart`) so the tap
  target itself is inert too, not just the write. Added
  `test_EARS_NOTIFY_16_toggle_before_first_emission_performs_no_write`
  (`test/features/settings/notifications/presentation/notification_settings_controller_test.dart`)
  with a `_CountingEnabledRepository` call-counter seam; falsified against
  the pre-fix `?? true` shape (failed `Expected: <0> Actual: <1>`, the
  right reason), restored, confirmed green. This was not disclosed as a
  deviation before the review round — it should have been; recorded here
  per rule 6, even though it is fixed rather than left standing. See Run
  log for the full evidence (test counts, analyze, design-verify).

**Files actually touched:**
- `lib/features/settings/notifications/presentation/notification_settings_controller.dart` (new)
- `lib/features/settings/notifications/presentation/notification_settings_view.dart` (new)
- `test/features/settings/notifications/presentation/notification_settings_controller_test.dart` (new)
- `test/design/probe_settings_notifications_test.dart` (new)
- `design/golden/settings-notifications/default@390x844/probe.json` (new)

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id — see Run log
- [x] UI: design gate green (100%, 24/24); no extra elements beyond the
  contract's own NT1-NT22 (no `design/gaps.md` trace needed — nothing
  added beyond the approved-pending `GAP-032` contract itself)
- [ ] Review APPROVE from a **different model** than `executed_by` (rule 5)
  — pending
- [ ] Squash-merged to `epic_15`; tracker + metrics stamped — pending
- [ ] 🧍 Human `verified` — pending

## Open Questions
- **OQ-E15-T04-1 — the LED fork.** `GAP-032` carries it with no proposal. Until
  the human answers at 🧍 `design_contract_approval`, this screen has no LED
  element and §4 forbids adding one.
  - **Status:** 🟡 open · **Answer:** _<empty>_ · **Answered by:** _<human>_ · **Date:** _<>_

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- 2026-09-08/09 — Branch `epic_15_task_04`, worktree `../wt-E15-T04` off
  `origin/development` (commit `4a97853`, per rule 4). Read
  `settings-notifications.md`, `notification_settings_repository.dart`,
  `notification_tables.dart`, `settings_sub_screen_scaffold.dart` (T03) and
  `GAP-032` in full before writing any code.
- Wrote `notification_settings_controller_test.dart` FIRST (7 tests, all
  named by EARS id). Falsified each of the 3 tests that are the *only*
  evidence for a claim, per the loop's own instruction:
  - `test_EARS_NOTIFY_16_toggling_a_category_persists_through_the_repository`
    → broke `toggle()` to mutate local state instead of calling
    `repository.setEnabled` → failed (`Expected: false, Actual: <true>`)
    → restored, byte-identical, confirmed green.
  - `test_EARS_NOTIFY_17_selecting_full_does_not_write` → removed the
    `if (level == full) return;` guard in `selectPrivacy` → failed
    (`Expected: <0>, Actual: <1>` on the counting repository's call count)
    → restored, confirmed green.
  - `test_EARS_UI_11_category_read_failure_leaves_the_privacy_card_intact`
    → removed the `categoriesError.value = true` line from the stream's
    `onError` handler → failed (`Expected: true, Actual: <false>`) →
    restored, confirmed green.
  - Full 7/7 file green throughout, both before and after each restore.
- **Real bug found and fixed in the harness's own probe test, not in
  product code:** the first cut of `probe_settings_notifications_test.dart`
  called `await db.close()` directly inside the `testWidgets` body. That
  hangs forever under `AutomatedTestWidgetsFlutterBinding` — confirmed
  directly (two separate runs each ran ~4-5 minutes before the test
  runner gave up with "did not complete", and a minimal pump-only repro
  with no probe/dump code at all reproduced the identical hang). Root
  cause: a real dart:io/native async completion awaited straight inside a
  pumped test's own body never gets its OS-level completion callback
  delivered outside `tester.runAsync` — `flutter_probe_dumper.dart`'s own
  header comment documents exactly this gotcha for `File`/`Directory` ops,
  and it turns out to apply to `AppDatabase.close()` too. Fixed by moving
  `db.close()`/`Get.reset()`/`controller.onClose()` into a group-level
  `tearDown()`, matching the exact pattern `design_probe_test.dart`'s own
  `devices`/`device-enrollment-approval` groups already use. Confirmed
  fixed: the corrected file completes in ~1s.
- `make design-probe`/`design-verify` are Node-based
  (`design/tools/*.mjs`); this fresh worktree had no `node_modules` yet —
  ran `npm install` once (`package-lock.json` untouched, nothing in this
  task's `files:` fence changed) to make `make design-verify` runnable at
  all in this checkout.
- Golden authored from **real dumper output**, not transcribed from the
  contract's role column, specifically because of the F1 heading-level
  warning carried into this task: `NT1` ("Notifications") dumps as
  `heading:1` here (not `heading:2` as the contract's own column reads),
  because this screen renders no brand title above it and the dumper's
  `_headingRole` assigns levels by order-of-first-appearance per screen
  (`flutter_probe_dumper.dart`'s own documented heuristic). Confirmed this
  is exactly the self-resolving case the task's warning describes, not a
  defect — the golden is this build's own regression baseline from here
  on, and `make design-verify SCREEN=settings-notifications
  --impl-probe build/design-probe/settings-notifications.json` passes
  100% (24/24 elements, 0 findings in every category — missing, copy,
  style, off-palette, layout, extra). Report:
  `design/reports/settings-notifications/default@390x844/report.md`.
- **Full suite reliability finding (environment, not this task's code):**
  a bare `flutter test` (default concurrency) on this machine silently
  dropped ~55 of ~140 test files with no error reported — each dropped
  file passes cleanly when run standalone (confirmed directly for
  `notification_settings_repository_test.dart`,
  `background_service_test.dart`, several `core/crypto/*` files). Re-running
  with `-j 2` reliably picks up every file. Recorded here rather than
  silently worked around, per this repo's own "disclose a probe/tool
  limitation, don't route around it" convention — this is a `flutter
  test`/worker-isolate resource issue on this dev machine, not a defect in
  any test file.
- `flutter test -j 2` (full suite, from a clean worktree with all 5 new
  files present): **1375/1375 passed**, 0 failures, 0 regressions.
  Includes this task's own 8 new tests (7 controller + 1 probe) and every
  pre-existing file (confirmed by diffing the set of files reporting at
  least one test against a `find test -name '*_test.dart'` listing).
- `flutter analyze lib/ test/`: found 3 issues on the first pass — 2
  pre-existing and unrelated (`test/core/calls/call_migration_controller_test.dart:207`
  missing `@override`; `test/features/login/presentation/login_controller_enrollment_gate_test.dart:18`
  unnecessary import — same two the epic's stated baseline names) plus
  **1 new, real one in this task's own diff**:
  `notification_settings_controller.dart:77:46` — `unnecessary_underscores`
  on an `onError: (Object _, StackTrace __)` callback. Fixed (`__` → `_`)
  and re-ran: **0 issues in this task's own files**, same 2 pre-existing
  ones remain (baseline unchanged). Re-ran the controller + probe test
  files after the fix: 8/8 green; re-diffed the regenerated probe JSON
  against the committed golden — byte-identical (the fix is a parameter
  name only, no rendered-output change) — design-verify re-run: still
  100% (24/24).
- Committed `98f70d8` — `feat(E15-T04): Notifications settings screen`.
- **Review round 2 (fix for F1, `claude-opus-5`'s changes-requested on PR
  #195):** Fixed the loading-window write bug at
  `notification_settings_controller.dart:104` (see §Deviations above for
  the full description).
  - Falsification: reverted `toggle` to the pre-fix `?? true` shape, the
    new test (`test_EARS_NOTIFY_16_toggle_before_first_emission_performs_no_write`)
    failed for the right reason (`Expected: <0> Actual: <1>`), restored
    the fix, confirmed green again. All 8 controller tests green
    afterward.
  - `flutter test` (full suite, default concurrency, this run): **1388/1388
    passing** (the prior 1387 + this task's 1 new regression test), 0
    failures. This corrects the `1375/1375` figure recorded above: the
    reviewer identified that number as an artifact of the `-j 2`
    concurrency flag dropping test files on this machine, not a true
    count — the default-concurrency run above is the accurate one, and it
    completed cleanly with no dropped files this round.
  - `flutter analyze lib/ test/`: exactly 2 pre-existing unrelated
    info-lints (`test/core/calls/call_migration_controller_test.dart:207`
    missing `@override`; `test/features/login/presentation/login_controller_enrollment_gate_test.dart:18`
    unnecessary import) — same two named above, neither touched by this
    change, no new issues.
  - Regenerated the probe
    (`flutter test test/design/probe_settings_notifications_test.dart`)
    and re-ran `node design/tools/verify.mjs --screen
    settings-notifications --impl flutter` — still 100% match, 24/24
    elements (report:
    `design/reports/settings-notifications/default@390x844/report.md`).
    This fix is controller/view loading-state logic only and did not move
    the UI tree, confirmed.
