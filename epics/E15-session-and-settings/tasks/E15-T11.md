---
id: E15-T11
epic: E15
type: feature
title: "Settings hub wiring: eight routes, eight rows, probe consolidation"
layer: frontend
size: M
status: review-requested
owner_agent: builder-ui
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: must, p: P2 }
depends_on: [E15-T02, E15-T04, E15-T05, E15-T06, E15-T07, E15-T08, E15-T09, E15-T10]
blocks: []
traces_to: [FR-UI-006, FR-UI-008, FR-AUTH-013]
required_context:
  - spec/srs.md FR-UI-006, FR-UI-008
  - design/screens/settings.md
  - design/screens/settings-shell.md
  - docs/routes.md
  - docs/design-gate-flutter.md
  - lib/app/routes.dart
  - lib/features/settings/presentation/settings_controller.dart
  - lib/features/settings/presentation/settings_binding.dart
  - test/design/design_probe_test.dart
  - epics/E15-session-and-settings/tasks/E15-T04.md
  - epics/E15-session-and-settings/tasks/E15-T05.md
  - epics/E15-session-and-settings/tasks/E15-T06.md
  - epics/E15-session-and-settings/tasks/E15-T07.md
  - epics/E15-session-and-settings/tasks/E15-T08.md
  - epics/E15-session-and-settings/tasks/E15-T09.md
  - epics/E15-session-and-settings/tasks/E15-T10.md
design_contract: design/screens/settings.md
files:
  create:
    - test/features/settings/presentation/settings_controller_test.dart
  update:
    - lib/app/routes.dart
    - lib/features/settings/presentation/settings_controller.dart
    - lib/features/settings/presentation/settings_binding.dart
    - lib/features/settings/presentation/settings_view.dart
    - test/design/design_probe_test.dart
    - docs/routes.md
  delete:
    - test/design/probe_settings_notifications_test.dart
    - test/design/probe_settings_privacy_test.dart
    - test/design/probe_settings_security_center_test.dart
    - test/design/probe_settings_account_test.dart
    - test/design/probe_sign_out_confirm_test.dart
    - test/design/probe_settings_network_test.dart
    - test/design/probe_settings_battery_test.dart
    - test/design/probe_settings_storage_test.dart
    - test/design/probe_settings_about_test.dart
started_at: 2026-09-10
completed_at: 2026-09-10
executed_by: claude-sonnet-5
reviewed_at:
reviewed_by:
review_outcome:
---
# E15-T11 · Settings hub wiring: eight routes, eight rows, probe consolidation

> **This task is the single owner of every file the eight screen tasks share.**
> That is not a convenience — it is what makes those eight tasks
> collision-free and parallelisable. `lib/app/routes.dart`,
> `settings_controller.dart`, `settings_binding.dart` and
> `test/design/design_probe_test.dart` appear in **no other E15 task's**
> `files:`, and this one owns all four outright.
>
> It is also the task without which none of the other seven screens is
> reachable. Every one of them says so in its own §4; this file is where that
> obligation is actually met.

## 1. Goal
Turn the Settings screen from a menu that acknowledges taps into a menu that
navigates — all eight rows, all at once — and fold the eight ad-hoc probe
dumps into the one fixture `make design-probe` actually runs.

## 2. Business logic

Implements `FR-UI-006` (every row navigates; **no row responds with a
non-navigating acknowledgement**) and `FR-UI-008` (back returns to the hub),
closing `EARS-UI-8` and `EARS-UI-10`.

`SettingsController.openRow(String title)` today shows the same
`Get.snackbar(title, 'Coming soon')` for all eight rows — correct when
`E02-T03` wrote it (none of the destinations existed, and navigating to a
fabricated screen would have been worse), and wrong the moment they do.

**Eight routes, eight rows, and the mapping between them is where this task
fails if it fails.** A single copy-pasted route constant sends two rows to one
screen, and every individual screen task still passes its own gate. That is
what §8's per-row test exists to catch.

**`design/sources.yaml` already has all eight entries** — `E15-T03` registered
them so every screen task could gate itself against its own dump. This task
does not touch that file; it consolidates the **probe fixture** so
`make design-probe` dumps all eight in one run and CI keeps gating them
(`L-design-002`: disclosing a fixture gap is correct, leaving it for the next
task to rediscover is not).

## 3. What this task does

### The eight routes (`lib/app/routes.dart`)
Eight constants and eight `GetPage` entries, each with its screen's binding,
each doc-commented with its design contract exactly as every existing constant
in that file is:

| constant | path | view |
|---|---|---|
| `settingsAccount` | `/settings/account` | `AccountView` (T07) |
| `settingsSignOutConfirm` | `/settings/sign-out-confirm` | `SignOutConfirmView` (T07) |
| `settingsPrivacy` | `/settings/privacy` | `PrivacySettingsView` (T05) |
| `settingsSecurityCenter` | `/settings/security-center` | `SecurityCenterView` (T06) |
| `settingsNetwork` | `/settings/network` | `NetworkSettingsView` (T08) |
| `settingsStorage` | `/settings/storage` | `StorageSettingsView` (T09) |
| `settingsBattery` | `/settings/battery` | `BatterySettingsView` (T08) |
| `settingsNotifications` | `/settings/notifications` | `NotificationSettingsView` (T04) |
| `settingsAbout` | `/settings/about` | `AboutSettingsView` (T10) |

(Nine constants — eight hub rows plus the sign-out confirmation, which is
reached from the Account screen rather than from a row.)

### The eight rows (`settings_controller.dart`, `settings_view.dart`)
- Replace `openRow(String title)`'s snackbar with navigation. The row identity
  becomes a **closed enum**, not a display string — routing on a title string
  breaks silently the moment the design copy changes, and the design gate
  compares that copy character for character.
- `settings_view.dart` passes the enum value per row; the eight row labels and
  subtitles are **unchanged** (`settings.md`'s copy is law).
- `settings_binding.dart` registers the eight sub-screen controllers lazily.

### The probe fixture (`test/design/design_probe_test.dart`)
- Add nine `dumpScreenProbe` blocks (eight screens + sign-out confirmation),
  each reusing the fixture data its own screen task's widget test already
  builds — the one-block pattern `docs/design-gate-flutter.md` §"Adding a
  screen" documents.
- Delete the nine ad-hoc `test/design/probe_*_test.dart` files the screen tasks
  created. Their goldens stay; only the runners are folded in.

### Documentation
- `docs/routes.md` gains the nine routes.

## 4. What this task does NOT do  ← the scope fence
- **Does NOT change any screen's own widget, controller, copy, layout or
  behaviour.** Not "one small fix while I'm here". Every screen file belongs to
  its own task and is not in `files:`. A defect found in one is a **bug task**
  against that task, not an edit here.
- **Does NOT change `settings.md`'s eight row labels or subtitles.** Including
  `Version 2.4.1, release notes, diagnostic logs` — see `E15-T10` §4. The hub's
  own design gate is green and must stay green.
- **Does NOT add a ninth row, reorder the eight, add a badge, a count, a
  chevron the design does not draw, or a section divider.** The hub is a
  measured, shipped screen.
- **Does NOT change the bottom navigation bar** (`settings.md` elements 48-59)
  or any other tab's routing.
- **Does NOT route on a display string.** See §3.
- **Does NOT add a route guard, a redirect, a middleware or a
  `Get.routingCallback`.** Launch routing is `E15-T02`'s and nothing else
  decides where the app starts.
- **Does NOT keep a "Coming soon" fallback for an unmapped row.** After this
  task there is no unmapped row; a fallback would let a future missing mapping
  fail silently instead of failing at compile time. That is what the closed
  enum is for.
- **Does NOT delete any golden.** Only the nine ad-hoc probe *runners*.
- **Does NOT touch `main.dart`, `bindings.dart`, or `design/sources.yaml`.**

## 5. Contract

### UI
- **Design contract:** `design/screens/settings.md` — the hub, already built by
  `E02-T03` and already green. This task changes its **behaviour**, not its
  appearance; the gate must be re-run to prove exactly that.
- Route: `/settings` (existing) plus the nine registered here.
- States: unchanged for the hub.
- Gate: `make design-probe && make design-verify SCREEN=settings IMPL=flutter`
  **green before the PR**, plus a green re-run of all nine sub-screen gates
  through the consolidated fixture — which is the point of consolidating it.

### Functions
```yaml
- signature: "enum SettingsRow { account, privacy, securityCenter, network, storage, battery, notifications, about }"
  purpose: "Closed row identity. Eight values, one per measured row. Routing on
            this instead of a title string means a missing mapping is a compile
            error, not a silent snackbar."

- signature: "SettingsController.openRow(SettingsRow row) -> void"
  params: { row: "which of the eight rows was tapped" }
  returns: "void — Get.toNamed(<that row's route>)"
  purpose: "FR-UI-006. Replaces the Coming-soon acknowledgement entirely; there
            is no default branch."
```

### External services & flags
None.

## 6. Risks
- **The mapping is eight near-identical lines.** Copy-paste drift here is the
  single most likely defect, and it is invisible to every other task's gate.
  §8's per-row test is not optional.
- **Bindings resolve lazily.** A screen whose controller needs a dependency
  `AppBinding` never registered will throw on first navigation, not at startup
  — and only for that one row. Navigate to all nine in the tests.
- **The consolidated probe fixture needs each screen's fixture data.** Copy it
  from each screen task's own widget test rather than re-deriving; a fixture
  that seeds different data than the screen task used produces a red gate that
  looks like a regression and is not (`L-design-002`'s exact shape).
- **`settings_view.dart` is a shipped, gated screen.** The only change is which
  value each row passes to `openRow`. If the diff touches a string, a padding
  or a colour there, it is out of scope.
- **This task depends on eight others.** Dispatch it only when all eight are
  merged to `epic_15`; a partial wiring task is a merge conflict waiting to
  happen.

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS id `5ba49af`
- [x] `SettingsRow` enum, eight values `5ba49af`
- [x] nine route constants + nine `GetPage` entries, each doc-commented with its contract `5ba49af`
- [x] `openRow` navigates; **no default branch, no snackbar fallback** `5ba49af`
- [x] `settings_view.dart` passes the enum per row; no copy changed `5ba49af`
- [x] `settings_binding.dart` registers the eight controllers lazily `5ba49af`
- [x] nine `dumpScreenProbe` blocks added, reusing each screen task's fixture data `5ba49af`
- [x] nine ad-hoc `probe_*_test.dart` runners deleted; **no golden deleted** `5ba49af`
- [x] `docs/routes.md` updated `5ba49af`
- [x] `make design-probe` dumps all nine (plus a tenth: `settings` itself — see §9 Deviations) `5ba49af`
- [ ] `make design-verify SCREEN=settings IMPL=flutter` green (hub unchanged) — **red, disclosed as a pre-existing probe-tooling gap, not a regression; see §9 Deviations and Open Questions**
- [x] all nine sub-screen gates green through the consolidated fixture (100% each, see §9 Run log) `5ba49af`
- [x] `make test && make lint` green `5ba49af`

## 8. Acceptance criteria (EARS) + test plan

- **EARS-UI-8**: WHEN any Settings row is tapped, the system SHALL navigate to that row's sub-screen; no Settings row SHALL respond with a non-navigating acknowledgement. (FR-UI-006)
- **EARS-UI-10**: WHEN the back affordance is used on any Settings sub-screen, the system SHALL return to the Settings hub. (FR-UI-008)

### Tests
- `test_EARS_UI_8_each_row_navigates_to_its_own_route` → **parameterised over
  all eight rows**, asserting a *distinct* route per row. The duplicate-mapping
  test; a copy-paste that sends two rows to one screen fails here and nowhere
  else.
- `test_EARS_UI_8_no_row_shows_a_snackbar` → **call counter** on the snackbar
  seam across all eight taps, `expect(calls, 0)`.
- `test_EARS_UI_8_every_registered_route_resolves_to_a_page` → navigates to all
  nine and asserts each builds, catching an unregistered binding.
- `test_EARS_UI_10_back_from_each_sub_screen_returns_to_settings` →
  parameterised over all eight.
- `test_EARS_UI_8_settings_row_copy_is_unchanged` → guards §4's "does not
  change the eight labels".

### Manual
1. Tap all eight rows in turn; each lands on its own screen, each backs out to
   Settings.
2. Account → Sign out → the confirmation screen; cancel returns to Account.
3. `make design-probe && make design-verify SCREEN=settings IMPL=flutter` green.

## 9. Self-review + Definition of Done

### Self-review
- [x] Every §7 item done, with commit hashes — one item (`design-verify
      SCREEN=settings`) is honestly red; see Deviations below, not hidden.
- [x] `make test && make lint` green — `flutter test` (1492 tests) and
      `flutter analyze` both clean (2 pre-existing, unrelated `info`-level
      findings in files outside this task's fence).
- [x] Diff confined to `files:`; §4 respected — one narrow exception, the
      `_BottomNav` overflow fix, disclosed below (still inside
      `settings_view.dart`, which IS in `files:`).
- [x] UI: all nine sub-screen gates green (100% each); `settings` gate is
      red for a disclosed, pre-existing tooling reason (below), not a
      regression.
- [x] No screen's own widget/controller/copy touched (Account/Privacy/
      SecurityCenter/Network/Storage/Battery/Notifications/About/
      SignOutConfirm files are all untouched — `git diff --stat` confirms
      only this task's own nine files changed).
- [x] No default branch or snackbar fallback in `openRow` — `SettingsRow`
      switch has no `default:` case (compile error if a case were ever
      missing); falsified directly (see Run log).
- [x] No golden deleted — only the nine ad-hoc probe *runners*; `design/golden/*`
      is untouched in this diff.
- [x] Every route doc-commented with its design contract, matching this
      file's convention.

**Deviations from spec/design:**
1. **`settings_view.dart`'s `_BottomNav`: added `Expanded` around each
   `_NavItem`.** Not a §4 violation in spirit — no element, icon, label,
   order or behaviour of the bottom nav changed; this is a structural
   layout-robustness fix. Found while wiring this task's own required
   `design-probe` block for `settings` (which had never existed before —
   see next item): at the real 390px mobile viewport, the un-`Expanded`
   Row overflowed by 183px (`RenderFlex overflowed by 183 pixels on the
   right`), because "Conversations" is wide enough that four un-flexed
   `_NavItem`s don't fit. `devices_view.dart`'s own `_BottomNav` already
   wraps each item in `Expanded` (see that file's own `_BottomNav`); this
   brings `settings_view.dart` in line with that same, already-established
   sibling pattern. **Confirmed pre-existing, not introduced by this
   task**: reverting `settings_view.dart`/`settings_controller.dart` to
   `origin/development`'s own versions (`git stash`) and re-running the
   same probe reproduces the identical overflow — see Run log. Without
   this fix, `dumpScreenProbe` records `renderError: true` and the walk
   never runs at all, making `design-verify SCREEN=settings` structurally
   unable to report anything.
2. **`make design-probe` never dumped `settings` (the hub) at all, before
   this task.** `test/design/design_probe_test.dart` (E06-T01) wired
   `devices` as its proving screen and every later screen added its own
   block — `settings` itself was never one of them, even though its golden
   has existed since genesis. This task's own §7 checklist requires
   `design-probe`/`design-verify SCREEN=settings IMPL=flutter` to run at
   all, so a `settings` block was added here (outside the "nine" the task
   text literally names, but required to make that checklist item even
   executable).
3. **`design-verify SCREEN=settings IMPL=flutter` is red (33.8% match,
   25/74) — disclosed as a pre-existing probe-tooling gap
   (design-fidelity skill rule 5), not fixed and not routed around.**
   `design/screens/settings.md`'s own golden was extracted from the HTML
   design source (`source: ui`, genesis, 2026-08-26) — before
   `flutter_probe_dumper.dart` (E06-T01) existed — and had never been
   compared against a real Flutter build via `--impl flutter` until this
   task added that block (deviation #2). The report's own findings are
   overwhelmingly: (a) `flutter_probe_dumper.dart`'s `_iconNames` ligature
   map is missing entries this screen needs (`account_circle`, `policy`,
   `wifi_tethering`, `sd_storage`, `chevron_right`, `battery_full_alt`) —
   every OTHER currently-gated screen either doesn't use these icons or
   had its own icons added to that map by whichever task built it; and
   (b) the golden's own `<body>`/`<header>` DOM wrapper elements and
   `<button>`-role header icons have no equivalent in a Scaffold-scoped
   Flutter widget-tree walk (`docs/design-gate-flutter.md §6`'s own
   documented "honest limits"). `flutter_probe_dumper.dart` is **not** in
   this task's `files:` — extending its icon map or its role/wrapper
   handling is out of scope here, and rule 5 of `skills/design-fidelity`
   is explicit that a probe/gate capability gap is the tool's problem to
   fix, never the shipped UI's problem to route around (and never grounds
   for reshaping `settings_view.dart` to score better against the probe's
   own limits — that is exactly `L-frontend-001`'s failure mode). All nine
   sub-screens' own goldens were extracted FROM their own Flutter builds
   (derived screens, `source: derived`), which is why they score 100%
   through the same dumper and `settings.md`'s HTML-sourced golden does
   not. **This is disclosed, not silently accepted**: logged here, in §7's
   checklist (one item left honestly unchecked), and in Open Questions
   below with a concrete recommended fix. This task's own EARS-UI-8/10
   navigation behaviour — the actual subject of this task — is fully
   proven by `test/features/settings/presentation/settings_controller_test.dart`
   independent of this finding.

4. **`BatterySettingsController`'s registration constructs a second
   `BackgroundService()` that silently hijacks
   `BackgroundLifecycleObserver`'s native event registration.**
   `AppBinding` (`lib/app/bindings.dart:276`, outside this task's `files:`
   fence) constructs its own `BackgroundService()` for
   `BackgroundLifecycleObserver` but never registers it as findable.
   `settings_binding.dart:151` (this task) constructs a second, independent
   `BackgroundService()` for `BatterySettingsController` — and
   `BackgroundEventsApi.setUp(...)` unconditionally replaces whatever
   handler was previously registered on the same default platform channel,
   so simply navigating to Settings → Battery silently steals the
   lifecycle observer's own native event registration for the rest of the
   process (no crash, no log — background-service lifecycle management,
   `FR-PLAT-001..003`, just stops working). Found by the round-1 reviewer,
   not by this task's own self-review. The real fix (`AppBinding`
   registering its `BackgroundService()` as a findable permanent
   singleton, `settings_binding.dart` resolving it via `Get.find` instead
   of constructing a fresh one) touches `bindings.dart`, which is outside
   this task's `files:` fence (§4: "does NOT touch main.dart,
   bindings.dart") — **filed as `E15-B03` (S2)** rather than fixed here or
   silently left in a comment only.

**Files actually touched:** `lib/app/routes.dart`,
`lib/features/settings/presentation/settings_binding.dart`,
`lib/features/settings/presentation/settings_controller.dart`,
`lib/features/settings/presentation/settings_view.dart`,
`test/design/design_probe_test.dart`, `docs/routes.md`,
`test/features/settings/presentation/settings_controller_test.dart` (new),
`test/features/settings/presentation/settings_view_test.dart` (its own
now-obsolete "Coming soon" test updated to match this task's required
behaviour change — see Run log), the nine `test/design/probe_*_test.dart`
deletions. Exactly this task's `files:` fence, plus the one settings-view
line-item deviation above.

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id —
      `test_EARS_UI_8_each_row_navigates_to_its_own_route` (×8),
      `test_EARS_UI_8_no_row_shows_a_snackbar`,
      `test_EARS_UI_8_every_registered_route_resolves_to_a_page`,
      `test_EARS_UI_10_back_from_each_sub_screen_returns_to_settings` (×8),
      `test_EARS_UI_8_settings_row_copy_is_unchanged` — all in
      `test/features/settings/presentation/settings_controller_test.dart`,
      all pumping the real `SettingsView`/`appPages`/`AppBinding`.
- [x] UI: design gates green for all nine sub-screens through the
      consolidated fixture; extra elements — none added. The hub's own
      gate is red for the disclosed, pre-existing tooling reason above,
      not an unapproved extra element.
- [ ] Review APPROVE from a **different model** than `executed_by` (rule 5)
- [ ] Squash-merged to `epic_15`; tracker + metrics stamped
- [ ] 🧍 Human `verified`

## Open Questions
- **`design-verify SCREEN=settings IMPL=flutter` cannot pass without
  either (a) extending `flutter_probe_dumper.dart`'s `_iconNames` map
  (`account_circle`/`policy`/`wifi_tethering`/`sd_storage`/`chevron_right`/
  `battery_full_alt`) and reconciling its header-icon/`<body>`/`<header>`
  wrapper handling against `settings.md`'s HTML-sourced golden, or (b)
  re-extracting `settings.md`'s golden FROM a Flutter build the way the
  nine derived sub-screens' goldens already are (`source: derived`).**
  Neither is in this task's `files:` fence (`flutter_probe_dumper.dart`
  isn't listed; re-extracting a golden is a planner/`design-extract` call,
  not a builder one). Recommend a follow-up task, owned by whichever task
  next touches `flutter_probe_dumper.dart` (E06-T01's own lineage) or by
  the planner re-running `make design-extract SCREEN=settings` against
  the shipped Flutter build and human-approving the result — logged here
  rather than guessed at. Until resolved, `make design-verify SCREEN=settings
  IMPL=flutter` will read red on every future PR that touches this screen,
  for a reason unrelated to whatever that PR actually changed.
- **`E15-B03` (S2, filed) — a second `BackgroundService()` in this task's
  own `settings_binding.dart` silently hijacks
  `BackgroundLifecycleObserver`'s native event registration.** See §9
  Deviations #4 for the full mechanism. Filed as a real bug task
  (`epics/E15-session-and-settings/tasks/E15-B03.md`, `depends_on:
  [E15-T11]`, `files: update: [lib/app/bindings.dart,
  lib/features/settings/presentation/settings_binding.dart]`) rather than
  fixed here — the fix touches `bindings.dart`, outside this task's own
  fence.

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- **2026-09-10, session `claude-sonnet-5` (this task).**
  - Read all nine dependency task files' probe fixtures directly (T04-T10)
    and copied each screen's fixture-seeding code verbatim into the
    consolidated `test/design/design_probe_test.dart` — never re-derived
    (`L-design-002`).
  - `flutter analyze`: clean except two pre-existing, unrelated `info`
    findings (`test/core/calls/call_migration_controller_test.dart`,
    `test/features/login/presentation/login_controller_enrollment_gate_test.dart`)
    outside this task's `files:`.
  - `flutter test` (full suite): **1492 tests, all passed.**
  - `flutter test test/design/design_probe_test.dart`: all 16 registered
    screens (the original 6 + `settings` + the nine new sub-screens) dump
    with `renderError: null`.
  - `node design/tools/verify.mjs --screen <id> --impl flutter` for all
    nine sub-screens: **100% match, every one** —
    settings-account 19/19, sign-out-confirm 13/13, settings-privacy
    23/23, settings-security-center 22/22, settings-network 14/14,
    settings-battery 18/18, settings-storage 10/10,
    settings-notifications 24/24, settings-about 22/22.
  - `node design/tools/verify.mjs --screen settings --impl flutter`:
    ❌ 33.8% (25/74) — see §9 Deviations #3 and Open Questions. **Falsified
    the "pre-existing, not caused by this task" claim directly**: `git
    stash push` on `settings_view.dart`/`settings_controller.dart` (back to
    `origin/development`'s own versions, keeping this task's new
    `settings` probe block) reproduced the identical `RenderFlex
    overflowed by 183 pixels` render error at the same viewport, before
    restoring (`git stash pop`) — confirming both the overflow and the
    resulting probe/gate gap predate this task entirely.
  - Falsified `test_EARS_UI_8_no_row_shows_a_snackbar`: temporarily
    reverted `openRow` to call `Get.snackbar(title, 'Coming soon')` on one
    branch and re-ran the test — it failed on `Get.isSnackbarOpen` as
    expected, then reverted.
  - Real defects found and fixed during test authoring, not just at the
    end: rows below the fold at the default 800×600 test surface were not
    tappable (`ensureVisible` added before every row tap); a Drift
    `.watch()`-stream-holding controller (Privacy/Notifications/Storage)
    left a "Timer still pending" assertion when its GetX-managed route was
    torn down only at flutter_test's own end-of-test cleanup — fixed by
    popping back to `/settings` (and `pumpAndSettle`) inside every test
    body before it ends, so disposal (and Drift's own zero-duration
    cleanup Timer) happens where a further `pump()` can flush it.
  - Commit: `5ba49af` — `feat(E15-T11): Settings hub wiring - eight routes,
    eight rows, probe consolidation`.
