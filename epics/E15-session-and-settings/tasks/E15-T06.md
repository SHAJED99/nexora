---
id: E15-T06
epic: E15
type: feature
title: Security Center screen
layer: frontend
size: M
status: review-requested
owner_agent: builder-ui
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: should, p: P2 }
depends_on: [E15-T03]
blocks: [E15-T11]
traces_to: [FR-DIAG-003, FR-DIAG-001, FR-DIAG-002, FR-SEC-003, FR-ABUSE-001, FR-UI-007]
required_context:
  - design/screens/settings-security-center.md
  - design/screens/settings-shell.md
  - design/gaps.md GAP-034
  - spec/srs.md FR-DIAG-001, FR-DIAG-002, FR-DIAG-003, FR-SEC-003, FR-ABUSE-001
  - lib/core/persistence/revocation_table.dart
  - lib/core/persistence/crypto_tables.dart
  - lib/core/persistence/relationships_table.dart
  - lib/core/persistence/abuse_tables.dart
  - epics/E15-session-and-settings/tasks/E15-T03.md
design_contract: design/screens/settings-security-center.md
files:
  create:
    - lib/features/settings/security_center/data/security_records_repository.dart
    - lib/features/settings/security_center/presentation/security_center_controller.dart
    - lib/features/settings/security_center/presentation/security_center_view.dart
    - test/features/settings/security_center/data/security_records_repository_test.dart
    - test/features/settings/security_center/presentation/security_center_controller_test.dart
    - test/design/probe_settings_security_center_test.dart
    - design/golden/settings-security-center/default@390x844/probe.json
  update: []
  delete: []
started_at: 2026-09-09
completed_at: 2026-09-09
executed_by: claude-sonnet-5
reviewed_at:
reviewed_by:
review_outcome:
---
# E15-T06 · Security Center screen

## 1. Goal
Show the user what this device has actually recorded about its own security —
revocations, trusted identities, blocks, rate-limit denials — without exposing
a single byte that `FR-DIAG-002` forbids.

## 2. Business logic

Implements `FR-DIAG-003` against `design/screens/settings-security-center.md`
(`GAP-034`). Four read-only sections over four tables that already ship:
`DeviceRevocations` (`E12`), `SignalTrustedIdentities` (`E03`), `Relationships`
filtered to blocked (`E02`), `RateLimitCounters` (`E13-T02`).

**`FR-DIAG-002` binds absolutely, and this is the most likely screen in the
product to violate it.** A record renders as *what happened, to which device
id, and when* — never *what was in it*. No key material, no session material,
no message content, no location. Not truncated, not "just the first eight
bytes", not behind an expand affordance, not in a debug build. The one
identifier that may appear is a **device id**, which is already public:
`E11-B06` made it the hex of the device's own identity **public** key, and
`devices.md` already displays device identifiers on a shipped screen.

**This screen takes no action.** No unblock, no re-trust, no revoke, no
clear-log. `EARS-DIAG-5` makes that a criterion, not a preference. Blocking and
trust decisions have exactly one home (`devices.md`); an audit surface that can
also change security state has quietly become an unreviewed control panel.

**A `SecurityRecordsRepository` exists so the SQL lives in one reviewable
place** and the controller cannot reach a raw table. It is a read-only
repository: no write method, at all.

## 3. What this task does
- Builds `SecurityRecordsRepository` — four read methods, each returning a
  narrow view model carrying **only** the fields the contract renders.
- Builds `SecurityCenterController` + `SecurityCenterView` composing
  `E15-T03`'s scaffold, rendering SC1-SC21.
- Adds its own probe test, verifies, and commits the first clean dump as the
  golden.

## 4. What this task does NOT do  ← the scope fence
- **Does NOT render key material, session material, message content or
  location data.** Not partially, not hashed, not truncated, not behind a tap.
  The view models must not even *carry* those fields — if they are not in the
  model, they cannot leak into a widget later.
- **Does NOT offer any action.** No unblock button, no "trust again", no
  revoke, no clear-log, no swipe action, no long-press menu. A security log
  with a button is the specific wrong move this task invites.
- **Does NOT add export, share, or copy-to-clipboard.** Each is an obvious
  addition to a log screen and each is a route by which `FR-DIAG-002`-protected
  content leaves the device.
- **Does NOT build a certificates section.** The hub row's subtitle promises
  one; **this app has no certificate concept** — `ADR-0003` is a
  Signal-protocol design with identity keys, not X.509. `GAP-034` carries the
  fork with **no proposal**, and its option (b) — relabelling identity-key
  fingerprints as "certificates" — was already considered and rejected there:
  it makes the copy true by making the concept false.
- **Does NOT build a "network audits" section.** Same subtitle, same fork;
  `settings-network.md` already owns everything this app knows about its
  network.
- **Does NOT add a severity colour, a badge, a count on the hub row, or an
  alert.** `settings.md` measures no red and no badge, and `devices.md`'s
  `rgb(186, 26, 26)` carries destructive/blocked semantics a *record* does not.
- **Does NOT add filtering, sorting or search.**
- **Does NOT touch `lib/app/routes.dart`, `settings_controller.dart`,
  `settings_binding.dart` or `test/design/design_probe_test.dart`** — all four
  are `E15-T11`'s alone.
- **Does NOT change any Drift table, or `RateLimiter`, or
  `DeviceRevocationService`.**

## 5. Contract

### UI
- **Design contract:** `design/screens/settings-security-center.md`.
- Route: `/settings/security-center` (registered by `E15-T11`).
- States: `default` · `loading` · **`empty` — the expected state**, not an edge
  case: on a healthy install all four lists are empty and that is the good
  outcome; each section shows its own SC7/SC11/SC15/SC20 line independently and
  an all-empty screen must not read as broken · `error` (SC21 replaces the
  affected section's list only).
- Gate: `make design-verify SCREEN=settings-security-center --impl-probe build/design-probe/settings-security-center.json` green before the PR. Derived
  screen — golden extracted from the first clean dump (`design-fidelity` §3).

### Functions
```yaml
- signature: "SecurityRecordsRepository.revocations() -> Future<List<SecurityRecord>>"
  returns: "device id, record type, timestamp — and nothing else"
  purpose: "FR-DIAG-003 over DeviceRevocations, FR-DIAG-002-safe by construction."

- signature: "SecurityRecordsRepository.trustedIdentities() -> Future<List<SecurityRecord>>"
  returns: "device id, record type, first-seen timestamp. NEVER the identity key bytes"
  purpose: "The section most likely to be implemented wrongly; the return type
            is the guard."

- signature: "SecurityRecordsRepository.blockedPeers() -> Future<List<SecurityRecord>>"
  returns: "device id, record type"
  purpose: "Relationships filtered to blocked. Read only — blocking stays on devices.md."

- signature: "SecurityRecordsRepository.rateLimitDenials() -> Future<List<SecurityRecord>>"
  returns: "limit name, count in the current window"
  purpose: "FR-ABUSE-001's user-visible half."
```

`SecurityRecord` carries exactly: a display identifier, a record type, an
optional timestamp, an optional count. **It has no field that could hold key
material.** That is the design, not an implementation detail.

### External services & flags
None.

## 6. Risks
- **`SignalTrustedIdentities` holds the identity key.** Reading the row and
  passing it into a view model "for later" is how this screen leaks. Project
  the columns in the query; do not select the row and drop the field in the
  widget.
- **Empty looks broken.** Four empty sections on a healthy install is correct.
  The copy must make that obvious (`No device has been revoked.` reads
  differently from a bare blank).
- **Rate-limit counters are per-window.** Displaying a lifetime total would be
  wrong and is not what the table holds (`E13-B01` is about the eviction
  window). Render what the row says.
- **`fail()` in a seam is swallowed** — use call counters for the
  "no action is possible" tests.

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS id `PENDING_HASH`
- [x] `SecurityRecord` defined with no field capable of holding key material `PENDING_HASH`
- [x] four read methods, columns projected narrowly in the query `PENDING_HASH`
- [x] four sections, independent empty lines `PENDING_HASH`
- [x] no action affordance of any kind `PENDING_HASH`
- [x] `loading` / `empty` / `error` per §5 `PENDING_HASH`
- [x] probe test added; first clean dump committed as the golden `PENDING_HASH`
- [x] `make design-verify SCREEN=settings-security-center` green `PENDING_HASH`
- [x] `flutter test && flutter analyze` green (no `make test`/`make lint`
  targets in this repo's Makefile — same note as `E15-T03`/`E15-T04`'s own
  Run logs) `PENDING_HASH`

## 8. Acceptance criteria (EARS) + test plan

- **EARS-DIAG-4**: This screen SHALL render only device identifiers, record types and timestamps, and SHALL NOT render key material, session material, message content or location data. (FR-DIAG-003, FR-DIAG-002)
- **EARS-DIAG-5**: This screen SHALL offer no action that changes trust, block or revocation state. (FR-DIAG-003)
- **EARS-UI-9**: WHERE a Settings row's subtitle names a capability the application does not implement, the sub-screen SHALL state the absence and SHALL NOT present a control or a value for it. (FR-UI-007)
- **EARS-UI-11**: IF a read fails, THEN the system SHALL show that section's failure line and leave every other section unchanged. (FR-UI-007, NFR-REL-001)

### Tests
- `test_EARS_DIAG_4_trusted_identity_row_never_renders_key_bytes` → seeds a
  trusted identity with a **recognisable key pattern**, renders, and asserts
  that pattern appears nowhere in the widget tree. This is the falsification
  test for this screen's whole reason to exist.
- `test_EARS_DIAG_4_record_model_has_no_key_field` → a structural assertion on
  `SecurityRecord`.
- `test_EARS_DIAG_5_no_action_affordance_is_rendered` → asserts no button,
  no dismissible, no long-press menu in any of the four sections.
- `test_EARS_DIAG_5_screen_performs_no_write` → **call counters** on the
  repository seams, `expect(writes, 0)` after interacting with every row.
- `test_EARS_UI_9_no_certificates_section_is_present`
- `test_EARS_UI_11_one_section_failing_leaves_the_other_three_rendered`
- `test_EARS_DIAG_4_all_four_sections_empty_renders_their_own_empty_lines`

### Manual
1. Fresh install → four empty sections, four distinct empty lines, nothing red.
2. Block a peer on `/devices` → it appears here, with no way to unblock it here.

## 9. Self-review + Definition of Done

### Self-review
- [x] Every §7 item done, with commit hashes — see Run log
- [x] `flutter test && flutter analyze` green (see Run log; repo has no
  `make test`/`make lint` targets)
- [x] Diff confined to `files:`; §4 respected — confirmed no route added,
  no row wiring, `settings_controller.dart`/`settings_binding.dart`/
  `test/design/design_probe_test.dart` untouched, no Drift table change,
  `RateLimiter`/`DeviceRevocationService` untouched
- [x] UI: `make design-verify SCREEN=settings-security-center` green
  (100%, 19/19 — see Run log)
- [x] No key/session/plaintext/location field anywhere in a view model —
  `SecurityRecord` carries only `displayIdentifier`/`recordType`/
  `timestamp`/`count`; `trustedIdentities()` projects only `addressName`
  via `selectOnly`, never `identityKey` — falsified directly (see Run log)
- [x] No action affordance, no export, no copy — grepped the diff:
  `InkWell`/`Material` appears exactly once, on `_ManageInDevicesLink`
  (SC16, a navigation link to the pre-existing `/devices` screen, not a
  state-changing action); no button/`Dismissible`/long-press menu
  anywhere else, confirmed by
  `test_EARS_DIAG_5_no_action_affordance_is_rendered`
- [x] Loading/error/empty states present; no spinner — `null` list =
  headings-only (no `CircularProgressIndicator` anywhere in the diff);
  each section's own `*Error` flag replaces only that section's own list
  with `SettingsEmptyOrErrorLine('Records could not be read.')`
- [x] `FR-DIAG-002` honoured — verified by the falsification test, not
  asserted: `test_EARS_DIAG_4_trusted_identity_row_never_renders_key_bytes`
  seeds a recognisable key-byte pattern and asserts it never appears in
  the repository's own output; separately falsified by temporarily
  re-introducing the leak in `trustedIdentities()` and confirming the
  test fails for the right reason (see Run log)

**Deviations from spec/design:**
- **`signal_trusted_identities` (`crypto_tables.dart`, `E03-T01b`) has no
  first-seen timestamp column at all.** The task's own §5 function
  contract for `trustedIdentities()` says "device id, record type,
  first-seen timestamp" and the design contract's SC10 says "device id +
  `Trusted` state label + when first seen" — but the underlying table
  stores only `addressName`/`addressDeviceId`/`identityKey`, with no
  timestamp of any kind. Fabricating one would be inventing data the
  store never recorded (rule 1). `SecurityRecord.timestamp` is
  deliberately optional in the task's own §5 contract ("an optional
  timestamp"), so this is implemented as `timestamp: null` for every
  trusted-identity row — the view renders no "when" line for that
  section as a result. This does not touch any Drift table (§4 forbids
  that) and does not affect `FR-DIAG-002`/`EARS-DIAG-4`'s own binding
  requirement (never render key material) — it only means this one
  section shows one fewer piece of already-nonexistent data than the
  contract's row description implies. Logged as `OQ-E15-T06-2` below
  rather than guessed.
- **`rateLimitDenials()`'s "limit name" is derived from `bucketKey`'s
  prefix, not stored as its own field anywhere.** Neither the task's §5
  contract nor the design contract specifies the exact mapping (both
  just say "limit name" — a value that does not exist as a fixed string
  anywhere in the codebase, since `bucketKey` is `"<limit>:<subject>"`,
  `rate_limiter.dart`'s own callers). Implemented as a fixed lookup table
  over every limit prefix this codebase currently defines (`relay`,
  `storage_volume`, `group_invite`, `device_registration`,
  `connection_request` — read directly from
  `inbound_pipeline.dart`/`group_membership_service.dart`/
  `device_identity_repository.dart`/
  `evaluate_connection_request_use_case.dart`), with an unrecognised
  prefix falling back to the raw prefix string rather than throwing.
  Simplest reading of an unspecified derivation (§ "small ambiguity" per
  `skills/implement`), not a spec gap — no Open Question needed. Never
  surfaces the subject after `:` (a device id or, for
  `device_registration`, an account uid — not a device id at all),
  keeping this FR-DIAG-002-safe by construction; covered by
  `test_EARS_DIAG_4_rate_limit_denials_render_limit_name_and_count_not_the_subject`.

**Files actually touched:**
- `lib/features/settings/security_center/data/security_records_repository.dart` (new)
- `lib/features/settings/security_center/presentation/security_center_controller.dart` (new)
- `lib/features/settings/security_center/presentation/security_center_view.dart` (new)
- `test/features/settings/security_center/data/security_records_repository_test.dart` (new)
- `test/features/settings/security_center/presentation/security_center_controller_test.dart` (new)
- `test/design/probe_settings_security_center_test.dart` (new)
- `design/golden/settings-security-center/default@390x844/probe.json` (new)

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id — see Run log
- [x] UI: design gate green (100%, 19/19); no extra elements beyond the
  contract's own SC1-SC21 (no `design/gaps.md` trace needed — nothing
  added beyond the approved-pending `GAP-034` contract itself)
- [ ] Review APPROVE from a **different model** than `executed_by` (rule 5)
  — pending
- [ ] Squash-merged to `epic_15`; tracker + metrics stamped — pending
- [ ] 🧍 Human `verified` — pending

## Open Questions
- **OQ-E15-T06-1 — the certificates / network-audits fork.** `GAP-034`, no
  proposal, answered at 🧍 `design_contract_approval`.
  - **Status:** 🟡 open · **Answer:** _<empty>_ · **Answered by:** _<human>_ · **Date:** _<>_
- **OQ-E15-T06-2 — `signal_trusted_identities` carries no first-seen
  timestamp column**, so SC10/the task's own §5 contract's "first-seen
  timestamp" cannot be rendered for the Trusted identities section;
  implemented as `timestamp: null` (see §9 Deviations above) rather than
  fabricated. If a first-seen timestamp is wanted here, it needs a new
  column on `signal_trusted_identities` (a 🧍 `db_schema_migration` gate,
  out of this task's scope — §4 forbids any Drift table change) added by
  a future task, not derived here.
  - **Status:** 🟡 open · **Answer:** _<empty>_ · **Answered by:** _<human>_ · **Date:** _<>_

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- 2026-09-09 — Branch `epic_15_task_06`, worktree `../wt-E15-T06` off
  `origin/development` (commit `40217b8`, the same commit T04 merged at,
  per rule 4). Confirmed via `epic.md` §Analyze's collision matrix that
  T05 (Privacy & Security, concurrently in flight in its own worktree
  `wt-E15-T05`) and this task share no files: T05 creates only
  `lib/features/settings/privacy/**` + its own probe/golden; this task
  creates only `lib/features/settings/security_center/**` + its own
  probe/golden. No overlap.
- Read `settings-security-center.md`, `settings-shell.md`,
  `settings_sub_screen_scaffold.dart` (T03), `revocation_table.dart`,
  `crypto_tables.dart`, `relationships_table.dart`, `abuse_tables.dart`
  (rate limiter table lives here as `RateLimitCounters`), `relationship.dart`
  (`RelationshipState`), and `GAP-034` in full before writing any code.
  Also read the sibling `E15-T04` implementation + its Run log as the
  worked example this task's brief pointed at.
- **Real finding during data-model research, before any code was
  written:** `SignalTrustedIdentities` (`crypto_tables.dart`) has no
  first-seen timestamp column at all — confirmed by reading the table
  definition and `drift_signal_store.dart`'s own `saveIdentity`, and by
  grepping the whole `lib/` tree for any other place such a timestamp
  might be tracked (none). Logged as `OQ-E15-T06-2` and §9 Deviations
  rather than fabricated (rule 1) — this is exactly the kind of gap the
  brief's F1/F2 carried-forward findings warn against inventing a local
  answer to.
- Wrote `security_records_repository_test.dart` FIRST (7 tests, all named
  by EARS id). The falsification test for this screen's whole reason to
  exist — `test_EARS_DIAG_4_trusted_identity_row_never_renders_key_bytes`
  — seeds a recognisable 32-byte key pattern and asserts it never appears
  in the repository's output. Falsified directly: temporarily changed
  `trustedIdentities()` to select the whole row and read `identityKey`
  into `displayIdentifier` → failed for the right reason
  (`Expected: 'device-b' Actual: '[171, 171, ...]'`) → restored to the
  `selectOnly`-projected version → confirmed green again, 7/7.
- Wrote `security_records_repository.dart` (green against the tests
  above). Every read method projects only display-safe columns;
  `trustedIdentities()` uses `db.selectOnly(...)..addColumns([addressName])`
  specifically so `identityKey` cannot be selected at all, not merely
  dropped later in a widget (task §6 risk).
- Wrote `security_center_controller_test.dart` (7 tests: 4 non-widget +
  3 `testWidgets`). Falsified
  `test_EARS_UI_11_one_section_failing_leaves_the_other_three_rendered`
  by removing the `revocationsError.value = true;` line from
  `_loadRevocations`'s catch block → failed for the right reason
  (`Expected: true Actual: <false>`) → restored → confirmed green.
- **Real bug found and fixed during the widget-level `EARS-DIAG-5` test,
  same shape as T04's own probe-test finding:** a real `dart:io`/native
  async completion (the four one-shot `Future`-based
  `SecurityRecordsRepository` reads over `NativeDatabase`, triggered by
  `Get.put`'s own `onInit` call) awaited straight inside a pumped test's
  body never gets its completion delivered outside `tester.runAsync` —
  confirmed directly: `flutter test` on the first cut of
  `test_EARS_DIAG_5_no_action_affordance_is_rendered` hung indefinitely
  (had to be moved to a backgrounded shell and killed by its own
  timeout, output frozen mid-test with no further progress). Root cause
  isolated with a throwaway static-widget probe test (no DB at all, same
  row shape) that dumped correctly, proving the walker itself was not at
  fault, then a print-instrumented run showing `controller.revocations.value`
  was still `null` immediately after `dumpScreenProbe` returned in one
  run but populated moments later in another — the classic signature of
  a real isolate/native round-trip that only resolves inside
  `tester.runAsync`. Fixed by draining all four repository reads through
  `tester.runAsync` (a polling loop on the four Rx values) BEFORE any
  `pumpWidget` call, in every `testWidgets` in this file and in
  `probe_settings_security_center_test.dart`. This is also why the
  probe's own golden dump initially came back with only 13 elements
  (headings/cards/glyph/nav-link only, every row and every empty-line
  text silently missing) before this fix — re-confirmed the fix by
  diffing the regenerated probe JSON before/after: 13 → 19 elements,
  the missing 6 being exactly the revoked-device row's three text runs
  plus the three other sections' own empty-line text.
- **Real render-layout bug found and fixed, unrelated to the above:** the
  Revoked-devices card's header `Row` (heading + SC4's `policy` glyph)
  overflowed by 50px under the flutter_test harness's font metrics —
  confirmed directly via a throwaway debug test with
  `FlutterError.onError` instrumented to print the full assertion
  (`RenderFlex overflowed by 50 pixels on the right`,
  `security_center_view.dart:32`). Fixed by wrapping the heading `Text`
  in `Expanded` so it can wrap instead of overflow (matches the same
  fixed-trailing-icon shape `settings_view.dart`'s own hub rows already
  use). Re-confirmed no overflow with the same debug harness after the
  fix, then deleted the throwaway debug test (never part of this task's
  `files:` fence).
- Falsified `test_EARS_DIAG_5_no_action_affordance_is_rendered` directly:
  temporarily wrapped `_DeviceRecordRow` in a `Material > InkWell` (the
  exact regression shape the test exists to catch) → failed for the
  right reason (`Unexpected tappable widget found with labels
  [device-a, 8d ago, Revoked]`) → restored → confirmed green, and
  reran the probe test to confirm the golden JSON is byte-identical
  before/after (the revert is provably a no-op on the rendered tree).
- `npm install` once in this fresh worktree (no `node_modules` yet;
  `package-lock.json` untouched, nothing in this task's `files:` fence
  changed) to make `make design-verify`/`node design/tools/verify.mjs`
  runnable at all, same as `E15-T04`'s own Run log note.
- Golden authored from **real dumper output**, not transcribed from the
  contract's role column, per the brief's carried-forward F1 warning:
  `SC1` ("Security Center") dumps as `heading:1` (not `heading:2` as the
  contract's role column reads) and the four section headings dump as
  `heading:2` (not `heading:3`) — the same self-resolving
  order-of-first-appearance heuristic T04's Run log already documents
  for this exact shape (no brand title above this sub-screen). Confirmed
  this is the expected, self-resolving case, not a defect.
  `node design/tools/verify.mjs --screen settings-security-center
  --impl-probe build/design-probe/settings-security-center.json` passes
  100% (19/19 elements, 0 findings in every category — missing, copy,
  style, off-palette, layout, extra). Report:
  `design/reports/settings-security-center/default@390x844/report.md`.
- `flutter test` (full suite, default concurrency, from a clean worktree
  with all 7 new files present): **1403/1403 passed**, 0 failures. Baseline
  was 1388/1388 on `origin/development` at `40217b8` (confirmed via
  `git log`); this task adds exactly 15 new tests (7 repository + 7
  controller + 1 probe), 1388 + 15 = 1403 — no regressions, nothing
  dropped.
- `flutter analyze lib/ test/`: exactly 2 pre-existing, unrelated
  info-lints (`test/core/calls/call_migration_controller_test.dart:207`
  missing `@override`; `test/features/login/presentation/login_controller_enrollment_gate_test.dart:18`
  unnecessary import) — the same two the epic's stated baseline and
  `E15-T04`'s own Run log name. 0 new issues in this task's own files
  after removing a handful of now-redundant table-import lines the
  analyzer flagged as unused (the `Companion.insert` types they were
  imported for are already re-exported via `database.dart`).
- Committed `PENDING_HASH` — `feat(E15-T06): Security Center screen`.
