---
id: E15-T05
epic: E15
type: feature
title: Privacy & Security settings screen
layer: frontend
size: M
status: review-requested
owner_agent: builder-ui
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: should, p: P2 }
depends_on: [E15-T03]
blocks: [E15-T11]
traces_to: [FR-SEC-005, FR-LOC-001, FR-LOC-002, FR-LOC-003, FR-NOTIFY-002, FR-UI-007]
required_context:
  - design/screens/settings-privacy.md
  - design/screens/settings-shell.md
  - design/gaps.md GAP-033
  - spec/srs.md FR-SEC-005, FR-SEC-001, FR-LOC-001, FR-LOC-002, FR-LOC-003, FR-UI-007
  - agent/memory/decisions/ADR-0003-crypto-protocol.md
  - lib/features/location/data/location_settings_repository.dart
  - lib/core/notifications/notification_settings_repository.dart
  - epics/E15-session-and-settings/tasks/E15-T03.md
design_contract: design/screens/settings-privacy.md
files:
  create:
    - lib/features/settings/privacy/presentation/privacy_settings_controller.dart
    - lib/features/settings/privacy/presentation/privacy_settings_view.dart
    - test/features/settings/privacy/presentation/privacy_settings_controller_test.dart
    - test/design/probe_settings_privacy_test.dart
    - design/golden/settings-privacy/default@390x844/probe.json
  update:
    - design/screens/settings-privacy.md
    - design/gaps.md
  delete: []
started_at: 2026-09-09
completed_at: 2026-09-09
executed_by: claude-sonnet-5
reviewed_at:
reviewed_by:
review_outcome:
---
# E15-T05 · Privacy & Security settings screen

## 1. Goal
One screen that answers "what does this app protect, and what does it share?"
— and lets the user turn the sharing off.

## 2. Business logic

Implements `FR-SEC-005` against `design/screens/settings-privacy.md`
(`GAP-033`). Three cards, one writable.

- **Encryption (read-only).** PV7/PV8 state the protocol actually in force per
  `ADR-0003` — `X3DH + Double Ratchet` for direct messages, `Sender Keys` for
  groups. This is a **statement of fact about an accepted architectural
  decision**, not a preference. A control here would imply the user could
  choose, and they cannot (`FR-SEC-001` is unconditional).
- **Notification privacy (read-only, links out).** PV11 shows the current
  level; PV12 navigates to `/settings/notifications`. **One setting, one
  writer** — `E15-T04` owns it. Two screens writing one preference is the
  "state defined in two documents" trap that cost this project `E05-B03`.
- **Location (writable).** `LocationSettingsRepository` (`E09-T01`) is the sole
  source of truth: `watchGlobalEnabled` / `writeGlobalEnabled`,
  `readAllPeerEnabled` / `writePeerEnabled`.

**PV17 is load-bearing copy, not decoration.** `FR-LOC-003` makes location
availability an **AND** over four conditions — connected, authorized, global
on, per-peer on. A user who does not know that reads a per-person "on" row as
a promise the global switch silently overrides.

## 3. What this task does
- Builds `PrivacySettingsController` bound to
  `LocationSettingsRepository.watchGlobalEnabled()` and a per-peer read, plus a
  read-only `privacyLevel()` from `NotificationSettingsRepository`.
- Builds `PrivacySettingsView` composing `E15-T03`'s scaffold, rendering
  PV1-PV21 exactly as the contract specifies.
- Adds its own probe test, verifies, and commits the first clean dump as the
  golden.

## 4. What this task does NOT do  ← the scope fence
- **Does NOT write the notification privacy level.** PV11 reads; PV12
  navigates. If the diff contains `setPrivacyLevel`, it is wrong.
- **Does NOT build an app lock, a PIN, a biometric gate, or a placeholder for
  one.** The hub row's subtitle promises "app lock" and `ADR-0005`'s
  consequences mention a local lock mechanism — **no FR requires one and none
  exists in `lib/`.** `GAP-033` carries it as a fork with **no proposal**.
  Building one from a subtitle plus an ADR consequence is inventing scope; a
  greyed-out control for it is worse.
- **Does NOT build a permissions screen or list.** Same fork, same reason;
  Android already owns that UI.
- **Does NOT add a block/unblock control.** Blocking lives on `devices.md` and
  keeps exactly one home.
- **Does NOT show location history, a map, a coordinate or a timestamp of a
  fix.** `FR-LOC-004` forbids a permanent location store; displaying a history
  would imply one exists.
- **Does NOT offer a protocol choice, a "re-key" button, or a key-rotation
  control.**
- **Does NOT touch `lib/app/routes.dart`, `settings_controller.dart`,
  `settings_binding.dart` or `test/design/design_probe_test.dart`** — all four
  are `E15-T11`'s alone.
- **Does NOT change `LocationSettingsRepository` or any Drift table.**

## 5. Contract

### UI
- **Design contract:** `design/screens/settings-privacy.md`.
- Route: `/settings/privacy` (registered by `E15-T11`).
- States: `default` · `loading` (values unpopulated, no spinner) · `empty` (the
  per-person list only — PV20; **the ordinary state on a new install**, not an
  error) · `error` (PV21 replaces the affected card's values; **the global
  location switch is never cleared by a failed read of something else**).
- Gate: `make design-verify SCREEN=settings-privacy --impl-probe build/design-probe/settings-privacy.json` green before the PR. Derived screen — golden
  extracted from the first clean dump of the build (`design-fidelity` §3).

### Functions
```yaml
- signature: "PrivacySettingsController.globalLocationEnabled -> RxBool"
  returns: "bound to LocationSettingsRepository.watchGlobalEnabled()"
  purpose: "FR-LOC-001's switch state, streamed not polled."

- signature: "PrivacySettingsController.setGlobalLocation(bool enabled) -> Future<void>"
  returns: "void — delegates to writeGlobalEnabled"
  purpose: "FR-LOC-001's write path. The only write on this screen besides per-peer."

- signature: "PrivacySettingsController.setPeerLocation(String peerDeviceId, bool enabled) -> Future<void>"
  returns: "void — delegates to writePeerEnabled"
  purpose: "FR-LOC-002's write path."

- signature: "PrivacySettingsController.notificationPrivacyLabel -> Rx<String>"
  returns: "`Hidden` | `Sender only` | `Full` — READ ONLY"
  purpose: "PV11. There is deliberately no setter here (see §4)."
```

### External services & flags
None.

## 6. Risks
- **The obvious wrong convenience.** Adding a privacy-level selector here
  "since the user is already in Privacy" is exactly the duplication §2 forbids.
- **`readAllPeerEnabled()` returns a map, not a stream.** Re-read after any
  per-peer write; do not maintain a parallel local map.
- **Peer identity display.** PV19 shows a device id (public, already shown on
  `devices.md`). It must not show anything else about the peer.
- **`location_on` is flagged `[glyph — pending human confirmation]`** in the
  contract — it is the one glyph on this screen not measured anywhere. Do not
  quietly substitute a different one; if the human has not confirmed it by
  build time, raise it.

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS id `eb20b48`
- [x] encryption card, read-only, `ADR-0003`'s two protocol names `eb20b48`
- [x] notification-privacy row: reads, links, never writes `eb20b48`
- [x] global location switch bound to `watchGlobalEnabled` `eb20b48`
- [x] per-peer list from `readAllPeerEnabled`, re-read after each write `eb20b48`
- [x] PV17 copy present, verbatim `eb20b48`
- [x] `loading` / `empty` / `error` per §5 `eb20b48`
- [x] probe test added; first clean dump committed as the golden `eb20b48`
- [x] `make design-verify SCREEN=settings-privacy` green `eb20b48`
- [x] `flutter test && flutter analyze` green (no `make test`/`make lint` targets in this repo's Makefile — same note as `E15-T03`/`E15-T04`'s Run logs) `eb20b48`

## 8. Acceptance criteria (EARS) + test plan

- **EARS-SEC-4**: WHEN the global location switch is turned off, the system SHALL persist it through `LocationSettingsRepository` and location SHALL be unavailable to every peer regardless of per-peer state. (FR-SEC-005, FR-LOC-001, FR-LOC-003)
- **EARS-SEC-5**: This screen SHALL present the notification privacy level as a read-only value linking to the Notifications screen, and SHALL NOT write it. (FR-SEC-005, FR-NOTIFY-002)
- **EARS-UI-9**: WHERE a Settings row's subtitle names a capability the application does not implement, the sub-screen SHALL state the absence and SHALL NOT present a control or a value for it. (FR-UI-007)
- **EARS-UI-11**: IF a read fails, THEN the system SHALL show that section's failure line and leave every other section, and every user-chosen setting, unchanged. (FR-UI-007, NFR-REL-001)

### Tests
- `test_EARS_SEC_4_global_switch_persists_through_the_repository`
- `test_EARS_SEC_4_global_off_overrides_peer_on` → a peer with location on,
  global off; asserts the screen states unavailability (`FR-LOC-003`'s AND).
- `test_EARS_SEC_5_privacy_level_is_read_only` → **call counter** on
  `setPrivacyLevel`, `expect(calls, 0)` after interacting with PV11/PV12.
- `test_EARS_UI_9_no_app_lock_control_is_present`
- `test_EARS_UI_9_no_permissions_control_is_present`
- `test_EARS_UI_11_peer_read_failure_leaves_the_global_switch_intact`

### Manual
1. Turn global location off, force-quit, relaunch → still off.
2. Tap the notification-privacy row → lands on Notifications, level unchanged.

## 9. Self-review + Definition of Done

### Self-review
- [x] Every §7 item done, with commit hashes — all `eb20b48`
- [x] `flutter test && flutter analyze` green (see Run log; repo has no
  `make test`/`make lint` targets — same note as `E15-T03`/`E15-T04`)
- [x] Diff confined to `files:`; §4 respected — confirmed no route added,
  no row wired, `settings_controller.dart`/`settings_binding.dart`/
  `test/design/design_probe_test.dart`/`design/sources.yaml` untouched (the
  `settings-privacy` entry was already registered by `E15-T03`), no
  `LocationSettingsRepository`/Drift table change
- [x] UI: `make design-verify SCREEN=settings-privacy` green (100% — 22/22
  through round 2, 23/23 after round 3's PV22/`GAP-040` — see Run log)
- [x] No `setPrivacyLevel` call anywhere in the diff — grepped the diff,
  confirmed absent; `PrivacySettingsController` has no method of that name
  and no other write path to `NotificationSettingsRepository`. Updated round
  2 (F2): `notificationRepository` is now private
  (`_notificationRepository`), and `test_EARS_SEC_5_privacy_level_is_read_only`
  is a real `testWidgets` test driving a call-counting repository through
  taps on the pumped view's PV11/PV12 rows, `expect(calls, 0)` — not just a
  label-equality check.
- [x] No app-lock, PIN, biometric or permissions element — confirmed by
  reading the diff. Updated round 2 (F1): the two tests are now a
  `testWidgets` scan of the rendered `Text`/`Semantics` tree for
  lock/PIN/biometric/permission-shaped copy
  (`test_EARS_UI_9_no_app_lock_or_permissions_text_renders_anywhere`) plus a
  source-level scan of the controller's own code for the same identifiers
  (`test_EARS_UI_9_no_app_lock_or_permissions_identifier_in_controller_source`)
  — both proven to actually catch the forbidden surface (see Run log
  falsification).
- [x] Loading/error/empty states present; no spinner — `globalLocationEnabled`/
  `peerLocationEnabled`/`notificationPrivacyLabel` all start `null`/`''` and
  the view renders an unpopulated slot (never a `CircularProgressIndicator`)
  until each loads; `peerLocationEnabled!.isEmpty` renders PV20; each of the
  three error flags (`globalLocationError`/`peerLocationError`/
  `notificationPrivacyError`) independently swaps in
  `SettingsEmptyOrErrorLine('Settings could not be read.')` for only its own
  section, per `test_EARS_UI_11_*`
- [x] No secrets or PII logged (`FR-DIAG-002`) — no logging anywhere in this
  diff; PV19's peer id is the same public device id already shown on
  `devices.md`, nothing else about the peer is rendered

**Deviations from spec/design** (none, or list):
- **PV7/PV8 layout (small ambiguity, resolved, disclosed):** the contract's
  own element table cites `PV7`/`PV8` as `label + machine value` styled
  `SH7 + SH11` without specifying a side-by-side vs. stacked arrangement.
  A first cut used a `Row` (label `Expanded`, value at its natural width) —
  against the REAL dumper output (not eyeballed), this squeezed the label
  to `w:5,h:280` (vertically wrapped one-character-per-line) for
  `Direct messages` beside `X3DH + Double Ratchet`, a genuine layout defect,
  not a probe artifact. Fixed by stacking label above value in a `Column`
  instead (`_MachineValueRow` in `privacy_settings_view.dart`) — both
  elements keep their own `SH7`/`SH11` styling and colour; only the
  horizontal-vs-vertical arrangement changed. Simplest reading that avoids
  the defect; noted rather than left silent per rule 6.
- **Card-level error granularity (small ambiguity, resolved, disclosed):**
  task §5/the contract's own §States describe `error` as "PV21 replaces the
  affected card's values only" without naming which of PV11/PV12 (the
  notification-privacy row/link) counts as an affected "value" if the
  read fails. Read `PV12` ("Change in Notifications") as a static
  navigation affordance that makes no read of its own, so it stays rendered
  and tappable even when `notificationPrivacyError` is true (only `PV11`'s
  label is replaced by the error line) — this is the least destructive
  reading and keeps a working path to the one screen that CAN fix the
  underlying read. Same reasoning applied symmetrically to PV16/PV17 on a
  hypothetical global-switch read failure (not currently exercised by a
  live scenario in this app, but guarded the same way): PV17's disclosure
  copy is tied to the switch's own read and is replaced along with it,
  never left standing next to an error line implying a switch state that
  isn't known.
- **`_PeerLocationRow`'s leading glyph (small ambiguity, resolved,
  disclosed):** the contract cites PV19 as "devices.md row shape via
  SH8/SH7/SH12" but names no specific glyph identity for the row (unlike
  PV4/PV14, which do). Read `SH8` as authorizing the row's leading-glyph
  *slot* generically, not a specific icon, and used `Icons.devices` at the
  shell's own muted default (`SettingsRowGlyph`'s `devicesMuted` default) —
  the same generic per-row glyph `devices_view.dart` itself already uses
  for an unidentified peer, so no new visual language was introduced. This
  is chrome, not information about the peer (task §6's own boundary), so it
  does not violate "must not show anything else about the peer."

- **Allowlist check is subset, not full set equality (round 3, small
  ambiguity, resolved, disclosed):** the review-fix instruction asked for
  rendered text to "equal... as a set" the contract's copy strings.
  Implemented as `renderedTexts.difference(allowedTexts).isEmpty` — every
  rendered string must be IN the closed set (this is the property that
  actually catches vocabulary evasion) — rather than two-way equality,
  because the closed set spans every state this screen can render
  (`default`/`loading`/`empty`/`error`, plus PV11's three label values),
  while one pumped render only ever shows one state's subset of it. Full
  equality would fail correct code (e.g. PV21's error copy never appearing
  outside a read failure) for reasons unrelated to EARS-UI-9. Documented in
  the test's own comment.

**Files actually touched:**
- `lib/features/settings/privacy/presentation/privacy_settings_controller.dart` (new)
- `lib/features/settings/privacy/presentation/privacy_settings_view.dart` (new; round 3 adds PV22)
- `test/features/settings/privacy/presentation/privacy_settings_controller_test.dart` (new; round 3 rewrites EARS-UI-9's tests)
- `test/design/probe_settings_privacy_test.dart` (new)
- `design/golden/settings-privacy/default@390x844/probe.json` (new; round 3 regenerates for PV22)
- `design/screens/settings-privacy.md` (round 3: adds PV22)
- `design/gaps.md` (round 3: adds `GAP-040`)

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id — see Run log
- [x] UI: design gate green (100%, 23/23 after round 3's PV22); the one extra
  element beyond the original PV1-PV21 (PV22) traces to `design/gaps.md`
  `GAP-040`, human-approved 2026-09-09 — see Run log round 3
- [ ] Review APPROVE from a **different model** than `executed_by` (rule 5)
  — pending
- [ ] Squash-merged to `epic_15`; tracker + metrics stamped — pending
- [ ] 🧍 Human `verified` — pending

## Open Questions
- **OQ-E15-T05-1 — the app-lock and permissions forks.** `GAP-033`, no
  proposal.
  - **Status:** ✅ resolved · **Answer:** No control for either; add a small
    text element (PV22) stating both are not available in this version —
    `GAP-027`'s "no control" treatment plus the on-screen statement
    `EARS-UI-9`'s second clause requires. Logged as `GAP-040`. ·
    **Answered by:** human · **Date:** 2026-09-09
- **OQ-E15-T05-2 — the `location_on` glyph** is the one value on this screen
  measured in no contract. Flagged in `settings-privacy.md` §Derivation
  boundary 4.
  - **Status:** 🟡 open · **Answer:** _<empty>_ · **Answered by:** _<human>_ · **Date:** _<>_

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- 2026-09-09 — Branch `epic_15_task_05`, worktree `../wt-E15-T05` off
  `origin/development` (commit `40217b8`, `E15-T04` merged, per rule 4).
  Read `settings-privacy.md`, `settings-shell.md`,
  `settings_sub_screen_scaffold.dart` (T03), `location_settings_repository.dart`,
  `notification_settings_repository.dart`, `notification_tables.dart`,
  `ADR-0003-crypto-protocol.md` and `E15-T04`'s controller/view/task file
  (as the worked-example pattern named in the dispatch) in full before
  writing any code.
- Wrote `privacy_settings_controller_test.dart` FIRST (10 tests, all named
  by EARS id), against a real in-memory `AppDatabase` +
  `LocationSettingsRepository` + `NotificationSettingsRepository` — no
  mocks. Falsified every test that is the *only* evidence for a claim:
  - `test_EARS_SEC_4_global_set_before_first_emission_performs_no_write` →
    removed the `if (globalLocationEnabled.value == null) return;` guard in
    `setGlobalLocation` → failed (`Expected: <0> Actual: <1>` on the
    counting repository) → restored, confirmed green.
  - `test_EARS_SEC_4_peer_set_before_first_load_performs_no_write` → same
    removal on `setPeerLocation`'s guard → failed (`Expected: <0>
    Actual: <1>`) → restored, confirmed green.
  - `test_EARS_SEC_4_peer_write_re_reads_the_whole_map_not_a_local_copy` →
    replaced the post-write re-read with a local-map mutation
    (`{...peerLocationEnabled.value!, peerDeviceId: enabled}`) → failed
    (missing `peer-2`, written directly through the repository between two
    controller-driven writes) → restored, confirmed green.
  - `test_EARS_UI_11_peer_read_failure_leaves_the_global_switch_intact` →
    made `_loadPeerLocations`'s catch block also set
    `globalLocationError.value = true` (the cross-contamination bug this
    test exists to catch) → failed (`Expected: false Actual: <true>`) →
    restored, confirmed green.
  - Full 10/10 file green throughout, both before and after each restore.
- **Real layout bug found and fixed via the real dumper, not eyeballed**
  (this task's own carried-forward F1 warning, honoured): the first cut of
  `probe_settings_privacy_test.dart` produced a dump with `"Direct
  messages"` at `box: {w:5, h:280}` — squeezed to almost nothing and
  wrapped vertically. Root cause: `_MachineValueRow` used a `Row` with the
  label `Expanded` and the machine value at its natural (unbounded) width;
  `X3DH + Double Ratchet` at 14px `JetBrains Mono` took most of the row's
  316px budget, leaving the label almost none. Fixed by stacking label
  above value in a `Column` instead (logged in §Deviations). Also found
  (separately) a genuine `RenderFlex overflow by 72 pixels` from
  `_SectionHeader`'s `Row([SettingsSectionHeading(title), icon])` with no
  `Expanded` on the heading — `notification_settings_view.dart`'s own
  identical pattern never hit this because both its titles ("Alerts",
  "Privacy") are short; `Location sharing` (17 chars) is not. Fixed by
  wrapping the heading in `Expanded`. Both fixes confirmed via the real
  `flutter_probe_dumper.dart` output (bisected with throwaway debug
  `testWidgets` files, deleted before commit — never left in the diff),
  not by eyeballing the golden's own numbers.
- **Real async-loading race found and fixed in the probe test itself, not
  in product code:** with both layout bugs fixed, the dump still showed
  no notification-privacy label and no per-person rows at all (the tree
  stopped at the bare `Per person` heading). Confirmed via a throwaway
  debug test (also deleted) that the controller's in-memory state WAS
  fully loaded (`peers={peer-nexora-1: true}`,
  `notifLabel="Sender only"`) shortly after an *additional* manual
  `pumpAndSettle` call made from the test itself — meaning
  `dumpScreenProbe`'s own bounded `pumpAndSettle` (100ms interval, 5s cap)
  was returning as soon as no FRAME was pending, which is not the same
  guarantee as "every outstanding `Future` has resolved": `Get.put`'s
  synchronous `onInit()` starts `readAllPeerEnabled()`/`privacyLevel()` as
  real one-shot Drift/native `Future`s (not streams, unlike
  `watchGlobalEnabled()`, which loaded in time because it is one), and
  nothing was scheduling a new frame at the exact instant those two
  Futures were still in flight. Fixed by draining the event queue
  (`await pumpEventQueue();`, the same idiom the controller test file
  already uses) right after `Get.put`, before the widget is ever pumped —
  so both Futures land before the first frame, sidestepping the race
  entirely rather than trying to out-wait it. Re-ran: all data now present
  on the first clean dump.
- Golden authored from **real dumper output**, not transcribed from the
  contract's role column (this task's own carried-forward F1 heading-level
  warning, honoured): `PV1` ("Privacy & Security") dumps as `heading:1`
  here (not `heading:2` as the contract's own role column reads), for the
  same order-of-first-appearance reason `E15-T04`'s Run log already
  recorded for its own screen — no brand title renders above it. Confirmed
  self-resolving, not a defect.
- `make design-verify` requires Node deps this fresh worktree didn't have;
  ran `npm install` once (`package-lock.json` untouched — confirmed via
  `git status`, nothing in this task's `files:` fence changed) to make the
  gate runnable at all in this checkout, same as `E15-T04`'s Run log notes.
- `node design/tools/verify.mjs --screen settings-privacy --impl flutter
  --impl-probe build/design-probe/settings-privacy.json`: **✅ PASS, 100%
  (22/22)**, 0 findings in every category (missing, copy, style,
  off-palette, layout, extra). Report:
  `design/reports/settings-privacy/default@390x844/report.md`.
- Committed `eb20b48` — `feat(E15-T05): Privacy & Security settings
  screen` (implementation + tests + golden; the task-file update recording
  this Run log is a separate, later commit on the same branch).
- `flutter test` (full suite, default concurrency, clean worktree with all
  5 new files present): **1399/1399 passed**, 0 failures. `origin/development`
  baseline at branch time was **1388/1388** (`E15-T04`'s own final figure,
  confirmed via `git log origin/development`); this task adds 11 new tests
  (10 controller + 1 probe) — `1388 + 11 = 1399`, exact match, 0
  regressions.
- `flutter analyze lib/ test/`: **exactly 2 issues**, both pre-existing and
  unrelated to this task's diff — `test/core/calls/call_migration_controller_test.dart:207`
  missing `@override`; `test/features/login/presentation/login_controller_enrollment_gate_test.dart:18`
  unnecessary import — the same two names `E15-T03`/`E15-T04` both recorded
  as the epic's stated baseline. No new issues in this task's own files.
- No 🧍 gate triggered (no migration, no dependency/lockfile change, no
  secrets/env touched, no deletion >50 lines — this task only adds files).

- **2026-09-09 — Review round 2 (`claude-opus-5`, CHANGES-REQUESTED), F1/F2
  fixes.** The independent reviewer confirmed the design/layout/loading-guard
  work correct via their own falsifications, but found two vacuous tests:
  - **F1 (BLOCKING) — `test_EARS_UI_9_no_app_lock_control_is_present` /
    `test_EARS_UI_9_no_permissions_control_is_present` asserted
    `expect(controller.globalLocationEnabled, isNotNull)` /
    `expect(controller.peerLocationEnabled, isNotNull)` — always-initialized
    `Rx` field objects, unrelated to app-lock/permissions, that can never be
    `null` regardless of what else the controller/view carry. The reviewer
    proved this by adding real `appLockEnabled`/`appLockPin`/
    `biometricGateEnabled`/`grantedPermissions` fields plus a rendered
    app-lock row and watching the full suite stay green.
    **Fix:** replaced both with two real tests: a `testWidgets` test
    (`test_EARS_UI_9_no_app_lock_or_permissions_text_renders_anywhere`) that
    pumps the real `PrivacySettingsView` (real `LocationSettingsRepository`/
    `NotificationSettingsRepository` over an in-memory `AppDatabase`, same
    setup as every other test in this file) and asserts no `Text`/`Semantics`
    node anywhere in the rendered tree matches
    `lock|pin|biometric|permission|fingerprint|face id|touch id`
    (case-insensitive) — realistic candidate copy for such a control per
    `settings-privacy.md`'s own §Derivation boundary items 1-2, not a bare
    placeholder string; and a source-level test
    (`test_EARS_UI_9_no_app_lock_or_permissions_identifier_in_controller_source`)
    that reads `privacy_settings_controller.dart`'s own source (comments
    stripped) and asserts none of those same identifiers appear in its code —
    the same "no CODE line names a forbidden identifier" pattern
    `group_key_rotation_service_test.dart` already uses for `OQ-E07-1`.
    **Falsified:** temporarily added the exact fields the reviewer added
    (`appLockEnabled`/`appLockPin`/`biometricGateEnabled`/
    `grantedPermissions` on the controller) plus a rendered `App lock` /
    `"Require a PIN or biometric unlock."` card in the view → both new tests
    failed for the right reason (`forbidden Text found: "App lock"`; the
    source-scan `Expected: false Actual: <true>`) → removed both, confirmed
    10/10 green again.
  - **F2 (BLOCKING) — `test_EARS_SEC_5_privacy_level_is_read_only` only
    asserted the displayed label string; no call counter, no write path
    exercised, contrary to the task's own §8 test plan ("a CALL COUNTER on
    `setPrivacyLevel`, `expect(calls, 0)`"). The reviewer found the real gap
    underneath: `notificationRepository` was a PUBLIC field, so
    `controller.notificationRepository.setPrivacyLevel(...)` reached the
    write path in one hop with no controller-side gate — proved by wiring
    PV12's `onTap` to call it directly before navigating; suite stayed green.
    **Fix, both parts:** (1) rewrote the test as a `testWidgets` test that
    pumps `PrivacySettingsView` behind a `_CountingNotificationRepository`
    (same call-counter shape as `notification_settings_controller_test.dart`'s
    own `_CountingRepository` for `EARS-NOTIFY-17`), taps PV11's label row
    and PV12's link row (via a `GetPage`-based `GetMaterialApp` with
    `Transition.noTransition` so the assertion isn't gated on a real page
    transition settling), and asserts `setPrivacyLevelCalls == 0`. (2) made
    `notificationRepository` private (`_notificationRepository`) in
    `privacy_settings_controller.dart` — external constructor parameter name
    unchanged, so no other call site (tests, probe) needed a change; the
    `prefer_initializing_formals` info-lint this created is suppressed
    in-line with a one-line reason (the field and the parameter now have
    different names by design). **Falsified:** temporarily added a
    `debugForceWrite()` method on the controller calling
    `_notificationRepository.setPrivacyLevel` and wired PV12's `onTap` to
    call it before navigating → the counter test failed for the right reason
    (`Expected: <0> Actual: <1>`) → removed both, confirmed 10/10 green again.
  - **Widget-test infrastructure gotcha found and fixed along the way (not
    part of F1/F2 themselves, but required to make either test runnable):**
    `pumpEventQueue()` (used successfully by every plain `test()` in this
    file) does not reliably resolve inside a `testWidgets` body's fake-async
    zone — it hung the whole suite for 10+ minutes the first two attempts.
    Removed it from both new `testWidgets` bodies; `pumpWidget` +
    a **bounded** `pumpAndSettle` (`100ms` interval, `EnginePhase
    .sendSemanticsUpdate`, 5s/2s cap — same bound
    `flutter_probe_dumper.dart` already documents for this exact screen,
    design-fidelity rule 5's own precedent) give the controller's `onInit`
    futures the pump cycles they need instead. Separately, cancelling the
    global-location stream subscription in `onClose()` triggers drift's own
    `QueryStream._onCancelOrPause` → `StreamQueryStore.markAsClosed`, which
    schedules a zero-duration cleanup `Timer` — a single `tester.pump()`
    after `onClose()` was not always enough to let it fire before test end
    (intermittent "A Timer is still pending" failures); a bounded
    `pumpAndSettle` after `onClose()` in both tests resolved it for good.
    Neither finding changes any product-code claim; both are recorded here
    per `skills/implement` §2's brownfield/testing-infrastructure honesty
    rule, since a future agent hitting the same hang should not have to
    re-diagnose it.
  - **`notificationRepository` privacy did not require a golden re-extract**:
    controller/test-only change, view diff is nil (confirmed via
    `git diff` — `privacy_settings_view.dart` shows no changes after the
    falsification hooks were reverted). Re-ran `make design-verify` anyway:
    **✅ PASS, 100% (22/22)**, identical to the pre-fix figure.
  - `flutter test` (full suite): **1399/1399 passed**, 0 regressions (same
    total as the pre-fix baseline — F1/F2 replaced 2 tests with 2 tests
    each net +2 in this file, 10 → 10 unchanged count since the two vacuous
    tests were 1-for-1 replaced, not added alongside).
  - `flutter analyze lib/ test/`: **exactly 2 issues**, both the same
    pre-existing/unrelated names as before this round
    (`test/core/calls/call_migration_controller_test.dart:207`,
    `test/features/login/presentation/login_controller_enrollment_gate_test.dart:18`).
    No new issues from this round's diff.
  - **EARS-UI-9's "state the absence" clause — checked, deliberately left
    alone.** The review's own non-blocking note asked whether the screen
    should also *say* app-lock/permissions are unimplemented, not just omit
    a control for them. Re-read `settings-privacy.md` §Derivation boundary
    item 1: "**No app lock, and no placeholder for one**" — a stated-absence
    line ("App lock is not available on this device") is itself a
    placeholder for the very control item 1 forbids drawing, so adding one
    would violate the contract's own boundary, not satisfy it. Left as-is;
    no new UI copy added.
  - Commits: `e3e1140` — controller privacy fix + test rewrite, this
    Run-log update, same branch `epic_15_task_05`, PR #197 (not merged).

- 2026-09-09 — **Round 3.** Rebased `epic_15_task_05` onto `origin/development`
  (fast-forward, no conflicts — `E15-T06` (`cdbe253`, Security Center) had
  merged since round 2 but touches none of this task's files).

  **F3/F4 (BLOCKING, review round 2) — the F1 fix was itself vacuous.** The
  round-2 reviewer proved `test_EARS_UI_9_no_app_lock_or_permissions_text_renders_anywhere`'s
  own denylist (`lock|pin|biometric|permission|fingerprint|face id|touch id`)
  is falsifiable by different, equally natural vocabulary — they built a real
  passcode gate ("App passcode") and a real permissions list ("Camera
  access") and the whole 10/10 suite, including that test, stayed green.
  **Fix, both parts (task's F3/F4):**
  1. Replaced the denylist with a **closed-set ALLOWLIST**
     (`test_EARS_UI_9_rendered_text_is_exactly_the_contract_allowlist`,
     `privacy_settings_controller_test.dart`): reads
     `design/screens/settings-privacy.md`'s own `## Copy — verbatim` section
     at test run time (not retyped by hand — a future contract edit and this
     test cannot drift apart), adds PV11's three enumerated label values
     (`Hidden`/`Sender only`/`Full`, from the Elements table since they are
     not static Copy-section strings), pumps the real view, collects every
     non-empty rendered `Text`/`Semantics` string, and asserts
     `renderedTexts.difference(allowedTexts)` is empty — checked as
     `renderedTexts ⊆ allowedTexts` (an EXTRA string fails), not full set
     equality, because the closed set spans every state (`default`/
     `loading`/`empty`/`error`/PV11's three labels) and one pumped render
     only ever shows one state's subset — recorded as a deliberate reading
     of the task's "equals as a set" instruction, since literal two-way
     equality would fail correct code for state-dependent copy that simply
     isn't on screen in the render under test, which has nothing to do with
     EARS-UI-9. Also added the positive-render assertion the task's item 3
     asked for (`find.text('Privacy & Security')`, same shape
     `test_EARS_SEC_5_privacy_level_is_read_only` already uses for
     `'Sender only'`), inside the same test, so a build failure fails loudly
     instead of the allowlist passing vacuously over an empty tree.
  2. Extended the source-inspection test
     (`test_EARS_UI_9_no_app_lock_or_permissions_identifier_in_source`,
     renamed from `..._in_controller_source`) to scan
     **`privacy_settings_view.dart` as well as the controller** — the
     reviewer's finding that a control living entirely in the view file was
     invisible to the old, controller-only scan. Kept as a denylist
     (identifier-shaped: `[Ll]ock|[Bb]iometric|[Pp]ermission|[Pp]in`) — this
     is a reasonable secondary check per the task's own framing; the primary
     defence against vocabulary evasion is the rendered-text allowlist in
     (1), which does not care about wording at all. One exclusion line was
     needed: PV22's own disclosure string (added in this same round, see
     below) necessarily NAMES "app lock" and "permissions manager" in prose
     — excluded the same way a comment line already is, since this scan's
     job is to catch a field/method, not a sentence disclosing that neither
     exists.
  - **Falsified (task's explicit instruction):** temporarily re-added two
    `SettingsBodyLine`s to the view — `'App passcode'` and `'Camera
    access'` — the reviewer's exact evasion vocabulary. Ran the allowlist
    test alone: **failed for the right reason** —
    `Expected: empty / Actual: Set:['App passcode', 'Camera access']`.
    Removed both lines, re-ran: 11/11 green in this file again. The old
    denylist would have passed this exact input; the new allowlist does not
    — the property the task asked for is proven, not asserted.

  **New design element (human-approved, separate from F3/F4 above) —
  `GAP-040`.** The round-2 reviewer separately flagged that `EARS-UI-9` has
  a second clause — "SHALL state the absence" — that GAP-033's contract
  never implemented (only the "SHALL NOT present a control" half existed).
  **The human, in this round-3 session, explicitly decided:** add a small
  text element stating app lock and permissions management aren't
  implemented, closing GAP-033's two forked, no-proposal items ("app lock",
  "permissions") in favour of `GAP-027`'s "no control, disclosed absence"
  treatment, PLUS the on-screen statement `EARS-UI-9` itself requires (a
  step further than GAP-027's own silent-omission resolution, because
  GAP-027's own criterion never required stating anything).
  - Logged as **`GAP-040`** in `design/gaps.md`, cross-referencing GAP-033
    and GAP-027, `approved by: human, 2026-09-09`, citing this as a direct
    human decision in this session (not an agent assumption). Not routed
    through `skills/change-impact`: no control, no write path and no new
    capability was added — this narrows an already-forked absence into a
    disclosed statement of that same absence.
  - Added **PV22** to `design/screens/settings-privacy.md`: Elements table
    row, `## Copy — verbatim` bullet, updated §States (PV22 is constant
    across every state — it names a capability never implemented, not a
    read, so no failure mode touches it) and §Derivation boundary items 1-2
    (both now cite `GAP-040`'s resolution instead of standing as open
    forks).
  - Added the element to `privacy_settings_view.dart`: one
    `SettingsBodyLine('App lock and a permissions manager are not available
    in this version.')`, SH7 styling (no new token — reuses the same
    body-line widget PV6/PV17 already use), placed after the Location
    sharing card, outside any card (same "generic, outside a card"
    placement PV2's own subtitle uses).
  - `design/sources.yaml`: **no change needed** — checked
    `E15-T03`/`E15-T04`'s own registrations; this file tracks screens (id,
    route, viewports, states), not individual elements within an
    already-registered screen, and `settings-privacy` was already
    registered by `E15-T03`.
  - Regenerated the golden: `flutter test test/design/probe_settings_privacy_test.dart`
    → 1/1 passed, fresh dump confirmed PV22's text present in
    `build/design-probe/settings-privacy.json`. Copied it over
    `design/golden/settings-privacy/default@390x844/probe.json`.
    `node design/tools/verify.mjs --screen settings-privacy --impl flutter
    --impl-probe build/design-probe/settings-privacy.json` → **✅ PASS,
    100% (23/23)** — 22 contract elements (PV1-PV22) + the shell's own back
    affordance (SH1/SH2, counted separately by the dumper).
  - Updated the F1 (round-3) allowlist test to include PV22's copy string —
    automatic, since it reads the contract file directly rather than a
    hardcoded list; confirmed PV22's string is present in the copy section
    it parses.
  - Added `test_EARS_UI_9_the_absence_statement_actually_renders`
    (`testWidgets`, task item 7): `expect(find.text('App lock and a
    permissions manager are not available in this version.'),
    findsOneWidget)` — proves EARS-UI-9's clause (a) actually renders, not
    merely that the task/contract claims it does.
  - **Resolved `OQ-E15-T05-1`** (the app-lock and permissions forks): human,
    2026-09-09, per the `GAP-040` decision above — both forks close to
    "no control, plus a disclosed-absence statement," recorded in full in
    `design/gaps.md` GAP-040.
  - **`OQ-E15-T05-2`** (the `location_on` glyph) — untouched this round, not
    in scope for F3/F4 or GAP-040; remains open.

  **Full verification after both pieces of work:**
  - `flutter test test/features/settings/privacy/presentation/privacy_settings_controller_test.dart`:
    **11/11 passed** (was 10; net +1 — F3 replaced 1 test with 2
    (`..._rendered_text_is_exactly_the_contract_allowlist` +
    `..._the_absence_statement_actually_renders`), F4 renamed the source
    scan in place).
  - `flutter test` (full suite): **1416/1416 passed**, 0 regressions
    (baseline on `origin/development` after `E15-T06`'s merge, plus this
    round's +2 net new tests in this file vs. the round-2 baseline of 1399
    minus the 1 replaced denylist test).
  - `flutter analyze lib/ test/`: **exactly 2 issues**, identical to every
    prior round — `test/core/calls/call_migration_controller_test.dart:207`
    and `test/features/login/presentation/login_controller_enrollment_gate_test.dart:18`.
    No new issues from this round's diff.
  - `make design-verify SCREEN=settings-privacy IMPL=flutter`-equivalent
    (`node design/tools/verify.mjs --screen settings-privacy --impl flutter
    --impl-probe build/design-probe/settings-privacy.json`): **✅ PASS,
    100% (23/23)**.
  - Commits: `(pending)` — F3/F4 test rewrite + GAP-040/PV22, this Run-log
    update, same branch `epic_15_task_05`, PR #197 (not merged — leaving for
    round 4 review per the task instructions).
