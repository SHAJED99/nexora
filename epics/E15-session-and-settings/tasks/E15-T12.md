---
id: E15-T12
epic: E15
type: feature
title: Wire SignOutUseCase's production teardown and remote-revoke closures
layer: backend
size: XS
status: review-requested
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: S, range: "5k-15k" }
priority: { moscow: must, p: P2 }
depends_on: [E15-T01, E15-T07, E15-T11]
blocks: []
traces_to: [FR-AUTH-006, FR-AUTH-008, Q-SEC-009, Q-FUNC-010]
required_context:
  - spec/questions.md Q-SEC-009, Q-FUNC-010
  - lib/features/settings/account/domain/sign_out_use_case.dart
  - lib/core/session/device_revocation_service.dart
  - epics/E15-session-and-settings/tracker.md §Carried-forward observations (F1, F2)
  - epics/E15-session-and-settings/tasks/E15-T01.md
  - epics/E15-session-and-settings/tasks/E15-T07.md
design_contract: n/a
files:
  create: []
  update:
    - lib/features/settings/account/domain/sign_out_use_case.dart
    - lib/features/settings/presentation/settings_binding.dart
    - test/features/settings/account/domain/sign_out_use_case_test.dart
  delete: []
started_at: 2026-09-10
completed_at: 2026-09-10
executed_by: claude-sonnet-5
reviewed_at:
reviewed_by:
review_outcome:
---
# E15-T12 · Wire SignOutUseCase's production teardown and remote-revoke closures

> **This task exists to close two carried-forward findings the epic tracker
> named and left ownerless twice.** `tracker.md` §Carried-forward observations,
> F1 and F2 (both filed against `E15-T01`'s review, both re-flagged before
> `E15-T07` dispatched): the human's answer to `Q-SEC-009`(b) — revoke the
> remote device-registry row on sign-out, best-effort, non-blocking — has no
> mechanism anywhere in the codebase, and `SignOutUseCase`'s `_teardown` closure
> still defaults to a no-op in production (no code closes/unregisters the live
> `AppDatabase`/`MessagingStack` singletons before the wipe deletes the database
> file). Neither `E15-T01` nor `E15-T07` claimed either obligation — both
> correctly, per rule 6: `E15-T01`'s own `files:` fence excludes
> `bindings.dart`/`settings_binding.dart`, and `E15-T07`'s own §5 contract names
> only `SignOutUseCase.call()` + `Get.offAllNamed`. `E15-T07`'s review (round 1,
> 2026-09-10) confirmed that punt was correct and routed the gap here per the
> tracker's own resolution note: "amend its `files:`/§5 … or shard a small
> follow-up." This is that follow-up.
>
> **Depends on `E15-T11`, not just `E15-T01`/`E15-T07`.** No code anywhere
> constructs a production `SignOutUseCase` yet — `SignOutConfirmController`
> takes one via constructor injection, and the only place that will ever
> construct the real one is wherever `SignOutConfirmController` itself gets
> registered for real, which is `E15-T11`'s `settings_binding.dart` (`E15-T11`
> §3 "registers the eight sub-screen controllers lazily" — nine, counting
> sign-out-confirm). Dispatching this before `E15-T11` lands would have nothing
> to wire into.

## 1. Goal
Give `SignOutUseCase` a real, non-blocking best-effort remote device-revoke
call and a real GetX-aware teardown closure in production — closing the gap
between what `Q-SEC-009`(b)/`Q-FUNC-010` answered and what the code actually
does on sign-out.

## 2. Business logic
Implements the human's answer to `Q-SEC-009`(b) (`spec/questions.md`): on
sign-out, call `DeviceRevocationService.revoke(uid, deviceId)` — best-effort,
non-blocking, never allowed to leave the local wipe half-done or fail the
sign-out itself. Left undone, `Q-SEC-009`(b) silently degrades to option (a)
(the outcome the human explicitly rejected), and `Q-FUNC-010`'s answer ("(a)
falls out of (b) for free") stops being true, since it depends on the remote
row actually being gone.

Also implements `FR-AUTH-006`/`FR-AUTH-008`'s teardown ordering
(`sign_out_use_case.dart`'s own doc comment: "teardown -> wipe -> auth clear,
in that binding order") for real: the `AppDatabase`/`MessagingStack` singletons
and their registered repositories must be closed/unregistered from GetX before
`LocalDataWipeService.wipe()` deletes the database file out from under them.

## 3. What this task does
- Add an optional `revoke: Future<void> Function()?` constructor seam to
  `SignOutUseCase`, defaulting to a no-op (same shape as the existing
  `teardown` seam) — called after the wipe, wrapped in the same best-effort
  `try`/`catch`/log pattern the auth clear already uses (never propagates,
  never blocks, never delays navigation).
- In `settings_binding.dart`, when registering the production
  `SignOutConfirmController`, construct its `SignOutUseCase` with:
  - `teardown`: a real closure that closes/unregisters `AppDatabase` and
    `MessagingStack` (and any repository singleton holding a database handle)
    from GetX, in an order that does not throw if a dependency was never
    resolved (nothing has necessarily been visited yet in this session).
  - `revoke`: a closure that calls
    `DeviceRevocationService.revoke(uid, deviceId)` for the current session's
    uid/device id.
- Extend `sign_out_use_case_test.dart` to prove: (a) `revoke` is called
  exactly once, after the wipe and before/alongside the auth clear; (b) a
  throwing `revoke` closure does not propagate and does not prevent
  `call()` from completing; (c) the default (no closure given) behaves exactly
  as today — a no-op, `call()` succeeds identically.

## 4. What this task does NOT do  ← the scope fence
- **Does NOT touch any screen's widget, controller, view, or copy.** Not
  `account_controller.dart`, not `sign_out_confirm_controller.dart`, not any
  `_view.dart`. This is domain + binding wiring only.
- **Does NOT change `SignOutUseCase.call()`'s existing ordering or its
  existing auth-clear error handling.** Only adds the new `revoke` seam and
  calls it — the teardown -> wipe -> auth-clear sequence is unchanged.
- **Does NOT add a new route, a new UI affordance, or any user-visible
  change.** This closes a silent gap; nothing about the screen changes.
- **Does NOT touch `bindings.dart`** (the app-level global binding) — the
  production `SignOutUseCase` construction lives in `settings_binding.dart`,
  scoped to the Settings feature, matching where `SignOutConfirmController`
  itself is registered.
- **Does NOT retry a failed revoke, queue it, or persist it for a later
  attempt.** Best-effort means best-effort — a fire-and-log failure, exactly
  like the existing auth-clear catch. A retry/queue mechanism is out of scope
  and would need its own task if ever wanted.
- **Does NOT change `DeviceRevocationService`'s own API.** `revoke(uid,
  deviceId)` already exists (built for `E09`'s device-enrollment work) —
  this task only calls it from a new call site.

## 5. Contract

### Functions
```yaml
- signature: "SignOutUseCase({LocalDataWipeService? wipeService, GoogleAuthService? authService, Future<void> Function()? teardown, Future<void> Function()? revoke})"
  purpose: "Adds the `revoke` seam alongside the existing `teardown` seam.
            Both default to a no-op; the constructor shape for existing
            callers (E15-T07's SignOutConfirmController, this task's own
            production wiring) is otherwise unchanged."

- signature: "SignOutUseCase.call() -> Future<void>"
  purpose: "Unchanged ordering: teardown -> wipe -> best-effort revoke ->
            best-effort auth clear. `revoke`'s failure is caught and logged
            exactly like the existing auth-clear catch — never propagates,
            never blocks."
```

### External services & flags
- `DeviceRevocationService.revoke(uid, deviceId)` — already built, no API
  change. Called from `settings_binding.dart`'s production closure only.

## 6. Risks
- **A `revoke` call that throws must not become a visible sign-out failure.**
  The whole point of `Q-SEC-009`(b)'s "best-effort, non-blocking" answer is
  that a flaky network must never turn a successful local wipe into an error
  screen. Test this by making the double throw and asserting `call()` still
  completes normally.
- **Teardown ordering matters more here than it looks.** If the real teardown
  closure closes `AppDatabase` in a way that the wipe's own file-delete then
  races against (a lingering open handle on some platforms), the wipe could
  fail. Confirm the close is awaited and fully synchronous-complete before
  `wipeService.wipe()` runs (this is already the ordering `call()` enforces —
  just confirm the real closure respects it and doesn't fire-and-forget).
- **`uid`/`deviceId` must be read before teardown, not after.** Once
  `AppDatabase` is closed, nothing that reads local identity from it will
  resolve. Capture both before constructing/invoking the `revoke` closure.

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS/question id  `331daea`
- [x] `SignOutUseCase`'s `revoke` seam added, defaults to no-op  `331daea`
- [x] `call()` invokes `revoke` best-effort, after the wipe, never propagating  `331daea`
- [x] `settings_binding.dart` constructs the production `SignOutUseCase` with
      real `teardown` and `revoke` closures  `331daea`
- [x] `uid`/`deviceId` captured before teardown runs  `331daea`
- [x] `make test && make lint` green  `331daea`

## 8. Acceptance criteria (EARS) + test plan
- **`Q-SEC-009`(b)**: WHEN sign-out completes its local wipe, the system SHALL
  attempt to revoke the current device's remote registry row, best-effort and
  non-blocking.
- **`FR-AUTH-006`/`FR-AUTH-008` (teardown ordering)**: WHEN sign-out runs in
  production, the system SHALL close/unregister the live database and
  messaging singletons before the wipe deletes the database file.

### Tests
- `test_Q_SEC_009_revoke_is_called_exactly_once_after_the_wipe`
- `test_Q_SEC_009_a_throwing_revoke_does_not_propagate_or_block_call`
- `test_Q_SEC_009_default_no_revoke_closure_behaves_as_a_noop`
- `test_teardown_closure_runs_and_completes_before_wipe_is_invoked`

### Manual
1. Sign out on a real device with a flaky/offline network — confirm the wipe
   still completes and the app lands on `/welcome` (no visible failure from a
   failed revoke).

## 9. Self-review + Definition of Done

### Self-review
- [x] Every §7 item done, with commit hashes — all `331daea`
- [x] `make test && make lint` green — `flutter analyze` clean (2 pre-existing
      infos in unrelated test files, not touched by this diff);
      `flutter test test/features/settings` → 132/132 pass; the sign-out test
      file alone → 7/7 pass
- [x] Diff confined to `files:`; §4 respected — `git diff --stat` against
      `origin/development` touches exactly the three files in §`files:`
      (`sign_out_use_case.dart`, `settings_binding.dart`,
      `sign_out_use_case_test.dart`), nothing else
- [x] No screen's own widget/controller/copy touched — confirmed by the same
      diff scope

**Deviations from spec/design:**
1. **§8's fourth named test
   (`test_teardown_closure_runs_and_completes_before_wipe_is_invoked`) was not
   added as a new, separately-named test.** The pre-existing
   `test_EARS_AUTH_5_teardown_runs_before_wipe_and_wipe_before_auth_clear`
   (E15-T01) already asserts exactly this — `calls == ['teardown', 'wipe',
   'authClear']` via a recording closure — and remains green, unchanged,
   after this task's edits. Adding a byte-for-byte duplicate under a new name
   seemed like noise rather than coverage; flagging the substitution here
   rather than silently treating the §8 list as satisfied by a test that
   doesn't exist under that name.
2. **A real, disclosed limitation in the production `revoke` wiring** (full
   reasoning inline as a comment in `settings_binding.dart`, above the
   `SignOutConfirmController` registration): `revoke`'s own contract (§5)
   fixes it to run AFTER `wipeService.wipe()`, and this task's own `teardown`
   closure closes the shared `AppDatabase` (via `MessagingStack.dispose()`,
   which itself calls `db.close()`) BEFORE the wipe runs. `uid`/`deviceId`
   values are captured before `teardown` (as instructed by §6 Risks), so
   those are safe — but `DeviceRevocationService.revoke()` (outside this
   task's `files:` fence, §4: "does NOT change `DeviceRevocationService`'s
   own API") writes a LOCAL `device_revocations` row FIRST, before its remote
   Firebase push, against a database that by the time `revoke` actually runs
   has already been closed. That local write throws; the throw is caught by
   `SignOutUseCase.call()`'s own best-effort wrapper (never propagates, never
   blocks sign-out — exactly as §5/§6 require), but the practical effect is
   that the remote Firebase push `Q-SEC-009`(b) exists to add will not
   reliably complete in production today. I could not find a fix inside this
   task's fence: it needs either `DeviceRevocationService`'s local write to
   become independently best-effort (that file is out of scope) or the
   `revoke`-after-`wipe` ordering itself to change (§4/§5 fix that ordering).
   Implemented exactly to the literal contract (§5's ordering, §4's "does NOT
   change `DeviceRevocationService`'s own API") since the EARS wording itself
   only requires an "attempt" (§8), and flagging this for a human/reviewer
   decision on whether a follow-up task should change the ordering or give
   `DeviceRevocationService` a revoke-local-write-independently-best-effort
   variant, rather than silently redesigning the call ordering myself.

**Files actually touched:**
- `lib/features/settings/account/domain/sign_out_use_case.dart`
- `lib/features/settings/presentation/settings_binding.dart`
- `test/features/settings/account/domain/sign_out_use_case_test.dart`

### Definition of Done
- [x] Every §8 criterion passes via a test named by its id — `Q-SEC-009`(b)
      via `test_Q_SEC_009_revoke_is_called_exactly_once_after_the_wipe` +
      `test_Q_SEC_009_a_throwing_revoke_does_not_propagate_or_block_call` +
      `test_Q_SEC_009_default_no_revoke_closure_behaves_as_a_noop`;
      `FR-AUTH-006`/`FR-AUTH-008` ordering via the pre-existing
      `test_EARS_AUTH_5_teardown_runs_before_wipe_and_wipe_before_auth_clear`
      (Deviations item 1 above)
- [ ] Review APPROVE from a **different model** than `executed_by` (rule 5)
- [ ] Squash-merged to `development`; tracker + metrics stamped
- [ ] 🧍 Human `verified`

## Open Questions
- (none — `DeviceRevocationService.revoke` already exists; this task only
  wires an existing call site)

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- 2026-09-10, builder (claude-sonnet-5): implemented per §3/§5. Read the
  actual merged code first (per the dispatch brief) — confirmed
  `settings_binding.dart:72` was the live no-op construction site, and that
  `device_revocation_service.dart` actually lives at
  `lib/core/services/device_revocation_service.dart` (the task's own
  `required_context` names `lib/core/session/...`, a stale path — noted here
  rather than silently corrected in the task file, since `required_context`
  isn't in this task's `files:` fence).
- Traced `MessagingStack.dispose()` (`messaging_stack.dart:724`) as the
  existing, already-correct close path: `coordinator.stop()` ->
  `transport.dispose()` -> `db.close()`, fully awaited, already documented
  there as "test-only — the app process never calls this." This task is the
  first production call site. `messagingStack`/`db` share the same
  `AppDatabase` instance (`bindings.dart` header), so `teardown` closes only
  that one connection — no other registered singleton holds a second
  database handle.
- Falsification evidence (all three new invariant-bearing tests, each
  reverted to the correct code immediately after the confirming run):
  1. Removed the `revoke()` call from `call()` entirely →
     `test_Q_SEC_009_revoke_is_called_exactly_once_after_the_wipe` failed
     (`Expected: <1> Actual: <0>`).
  2. Removed the try/catch around `_revoke()` (let it propagate) →
     `test_Q_SEC_009_a_throwing_revoke_does_not_propagate_or_block_call`
     failed (`Bad state: revoke boom` thrown out of `call()`).
  3. Made the default `_revoke` fallback actually call
     `_authService.signOut()` (a non-no-op default) →
     `test_Q_SEC_009_default_no_revoke_closure_behaves_as_a_noop` failed
     (`Actual: ['wipe', 'authClear', 'authClear']`), plus 3 pre-existing
     tests broke as a side effect (expected, given the shared
     `_RecordingAuthService` call counter).
  All three reverted; full suite green again after each revert, and after
  the final, real implementation (7/7 in the sign-out test file, 132/132 in
  `test/features/settings`).
- `flutter analyze` (whole project): 2 pre-existing infos, both in files
  this task never touches (`test/core/calls/call_migration_controller_test.dart`,
  `test/features/login/presentation/login_controller_enrollment_gate_test.dart`).
- `python agent/orchestrator/scheduler.py --validate` → `harness: 16 epics,
  194 tasks — DAG OK` (unaffected, as expected — no task files changed).
- See Deviations item 2 above for a disclosed, unresolved ordering tension in
  the production `revoke` wiring worth a human/reviewer call on whether it
  needs its own follow-up task.
- Commit: `331daea` — `feat(E15-T12): wire production sign-out teardown and
  remote-revoke closures`.
