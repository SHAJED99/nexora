---
id: E10-B07
epic: E10
type: bug
title: "Five EARS criteria are green on tests that cannot fail — mutations to the guarded lines survive the whole suite, and one test asserts a value it mocks in itself"
layer: backend
size: S
status: done
owner_agent: builder
model: any
preferred_agent: any
token_estimate: { tier: S, range: "15k-30k" }
severity: S4
priority: { moscow: should, p: P3 }
depends_on: [E10-T01, E10-T07, E10-T09, E10-T10]
blocks: []
traces_to: [EARS-NOTIFY-14, EARS-NOTIFY-15, EARS-PLAT-4, EARS-PLAT-5, EARS-PLAT-11, EARS-PLAT-14, NFR-SEC-001]
required_context:
  - test/core/notifications/storage_notification_source_test.dart
  - test/core/background/power_state_test.dart
  - test/core/notifications/notification_service_test.dart
  - test/core/messaging/messaging_coordinator_background_test.dart
found_by: reviewer (independent) during the E10 end-of-epic bug sweep — two entries carried forward from T07's and T09's reviews, the rest found by mutation probing during this sweep
found_in: "not a seam — a pattern. Each test was in-contract per its own task's §8 and each passed its own review. What no single task review could see is that five of them share the same failure mode — they assert a value the test itself supplies, rather than a behaviour the production code has to produce."
files:
  create: []
  update:
    - test/core/notifications/storage_notification_source_test.dart
    - test/core/background/power_state_test.dart
    - test/core/notifications/notification_service_test.dart
    - test/core/messaging/messaging_coordinator_background_test.dart
  delete: []
---
# E10-B07 · Five green lights with nothing wired behind them

The suite is 913/913 green and `flutter analyze` is clean. That is not in
dispute. What follows is the subset of it that would stay green with the code
it guards removed.

## F1 — `EARS-NOTIFY-14`'s null-plan guard: mutation survives the whole suite

```dart
// lib/core/notifications/sources/storage_notification_source.dart:100
if (plan == null) return;
```

`test_EARS_NOTIFY_14_null_plan_posts_nothing`
(`storage_notification_source_test.dart:146-165`) never assigns a plan at
all: `latestPlan` starts null, stays null, and the test asserts no facts were
emitted — which the `ever()` worker guarantees whether or not the guard
exists.

Mutating line 100 to `{ _wasOverThreshold = false; return; }` leaves the
**entire suite green**, yet it makes an `over → null → over` sequence
double-notify, violating `EARS-NOTIFY-15` ("stays over → no further
notification until it falls and rises again"). There is no `over → null →
over` test in the file. T07's review flagged exactly this and it is confirmed.
The rising-edge behaviour itself (`:42`, `:70`) *is* genuinely proven.

**Fix:** add an `over → null → over` test asserting exactly one post across
the whole sequence. Must fail under the mutation above.

## F2 — `EARS-NOTIFY-7`'s privacy downgrade: the sweep's own probe passed

```dart
// lib/core/notifications/notification_policy.dart:91
level = NotificationPrivacyLevel.senderOnly;   // downgrade `full`
```

The sweep deleted this line. `flutter test test/core/notifications/` →
**49/49 passed**, including `test_EARS_NOTIFY_7_full_downgrades_to_sender_only`
(`notification_policy_test.dart:103`).

It passes because `_copyFor` has no `full` branch, so `full` and `senderOnly`
render identically today and the downgrade is behaviourally a no-op. Safe
now; **not safe after `E10-B03`**, which adds per-category branches and makes
this line the only thing between `full` privacy and the decrypt path
`E06-T09.md:64-68` forbids by name. `EARS-NOTIFY-2` is a `SHALL NOT`.

**Fix:** this one belongs to `E10-B03` and is specified there — listed here
so the pattern is complete. Do not fix it twice; if B03 lands first, tick
this off and say so.

## F3 — `EARS-PLAT-11` asserts a value the test mocks in

`test_EARS_PLAT_11_missing_signal_reads_false`
(`power_state_test.dart:170-203`) installs a mock handler at `:186-193` that
returns `allClearPowerState()`, then asserts every field is `false` at
`:198-202`. It asserts exactly what it supplied. The real guard — the
API-level fallbacks in `PowerStateMonitor.kt:55-74`, where a signal
unavailable on an older API defaults to `false` — is Kotlin, is not
exercised, and could be inverted without this test noticing.

The sibling dedup test at `:136` *is* real and drives
`background_service.dart:110`. Only the missing-signal half is hollow.

**Fix:** either assert something the Dart layer actually decides (e.g. that a
platform read which *throws* still yields an all-false `PowerState` rather
than propagating), or delete the test and record the Kotlin fallback as
explicitly unverified in this environment. A test named for a guarantee it
cannot check is worse than an acknowledged gap.

## F4 — `EARS-PLAT-5` proves neither "per category" nor "idempotent"

`notification_service_test.dart:61-70` calls `ensureReady()` three times and
asserts `ensureChannelsCallCount == 3`. The criterion is *"exactly one
Android channel per category, idempotently"*. A host that created zero
channels, or ten duplicates per category, passes this test unchanged. What is
proven is that a Dart method forwards a call.

**Fix:** assert what the Dart side can actually own — that `ensureReady()` is
called before any `post()` can succeed, and that repeated calls are safe.
Then state plainly that per-category channel creation lives in
`NotificationChannels.kt` and is unverified here (the `E04-T03b`
no-mock-Kotlin limitation), rather than implying the criterion is covered.

## F5 — `EARS-PLAT-14` / `EARS-PLAT-4` never establish their own precondition

The criterion: *"WHILE the app is backgrounded AND the foreground service is
running, the system SHALL continue to run the coordinator tick, including
`reclaimPayloads()`."* This is **NFR-SEC-001's retention proof** — the whole
reason obligation #5 was raised as its own criterion with its own test, per
`epic.md:207` and the advisory's explicit instruction not to fold it into
another test.

`test_EARS_PLAT_14_reclaim_runs_while_backgrounded`
(`messaging_coordinator_background_test.dart:161-200`) sets **neither**
condition: no `AppLifecycleState`, no `ServiceState`, no
`BackgroundLifecycleObserver`. It calls `setTickInterval(120s)` and then
`stack.coordinator.tick()` **manually** (`:193`). The test's own comment at
`:165-171` concedes "MessagingCoordinator itself has no notion of
`AppLifecycleState` at all".

It proves `reclaimPayloads` runs on a tick — which
`messaging_coordinator_test.dart` already covered before E10 existed. The
task file demanded "assert `reclaimPayloads` was invoked with the app in
`paused` and the service `running`". That test does not exist.

**Fix:** drive it through `BackgroundLifecycleObserver` with a
`BackgroundStub` reporting `running` and lifecycle `paused`, let the real
timer fire, and assert `reclaimPayloads` ran without anyone calling `tick()`
by hand. This is the same composition-level harness `E10-B02`'s regression
test needs — build it once and use it for both.

## Severity
**S4** — no production defect is demonstrated by any of the five. F1's
mutation is a real latent bug but the shipped code is correct; F2 is
currently a no-op; F3/F4 are honest environment limits described dishonestly
by their test names; F5 duplicates coverage that already exists. Nothing a
user can observe is wrong. What is wrong is the evidence.

## Priority
**P3** — set under the decision authority explicitly delegated by the human
for this session. Rationale: no user impact and no merge risk today, so it
does not need to hold E10. But it should not go to P4 backlog either: F5
guards **NFR-SEC-001**, a security guarantee, and an unfalsifiable test on a
security guarantee is the kind of thing that is discovered at the worst
possible moment. Next epic, deliberately scheduled, not "someday".

## Regression test (write it first)
Each fix above *is* a test. The acceptance bar for this bug is mechanical and
must be recorded in the run log per finding: **apply the named mutation,
confirm the new test fails for the right reason, restore, confirm green.**
- F1: `{ _wasOverThreshold = false; return; }` at
  `storage_notification_source.dart:100`.
- F3: invert the API-level fallback default, or remove the throw guard.
- F5: stop the coordinator's timer and confirm the new test fails rather than
  passing on a hand-called `tick()`.
A fix whose falsification step is not recorded is not done.

## What this fix does NOT do
- Does **not** change any production file. This bug is tests only. If a fix
  appears to require a production change, that is a different bug — stop and
  say so.
- Does **not** fix F2's production line; that is `E10-B03`'s.
- Does **not** add a Kotlin test harness or attempt to unit-test
  `PowerStateMonitor.kt` / `NotificationChannels.kt` — the `E04-T03b`
  no-mock-Kotlin limitation stands and is to be *disclosed*, not routed
  around.
- Does **not** relax, delete or rename any currently-passing test to make
  room. Every one of the 913 stays.
- Does **not** claim any NFR-BATT-001 or on-device measurement.

## Implementation checklist
- [x] F1: added `test_EARS_NOTIFY_15_over_then_null_then_over_posts_once`
      (over -> null -> over, exactly one post across the whole sequence);
      confirmed it fails under the named mutation, restored, confirmed
      green `51b4889`
- [x] F2: not touched — `E10-B03` (`ec2b684`, merged into `development`
      before this task branched) already added the
      `assert(level != NotificationPrivacyLevel.full)` guard to `_copyFor`
      that makes the downgrade line falsifiable; ticked off here per this
      task's own instruction ("if B03 lands first, tick this off and say
      so"). `notification_policy_test.dart` is outside this task's
      `files:` fence and was not touched.
- [x] F3: rewrote `test_EARS_PLAT_11_missing_signal_reads_false` into
      `test_EARS_PLAT_11_dart_plumbing_forwards_all_false_snapshot_unaltered`
      — proves the Dart-side passthrough forwards a real (all-TRUE, not
      self-supplied all-false) snapshot unaltered, and cites
      `PowerStateMonitor.kt:55-74`'s own `Build.VERSION.SDK_INT >= <min>` +
      `?: false` guards as the (unfalsifiable-in-Dart) source of the actual
      EARS-PLAT-11 claim, per `skills/review`'s own instruction for a claim
      no Dart test could ever fail. Falsified by substituting a hard-coded
      all-false return in `powerState()`; confirmed fail, restored,
      confirmed green `934a7de`
- [x] F4: rewrote
      `test_EARS_PLAT_5_channels_created_once_per_category` into
      `test_EARS_PLAT_5_ensureReady_repeated_calls_forward_once_each_and_never_re_request_already_granted_permission`
      — keeps the genuine one-forward-per-call assertion, adds the
      Dart-owned "already-granted permission is never re-requested"
      assertion, discloses per-category/native-idempotency as unverified
      here. Falsified by removing the `hasPermission()` short-circuit in
      `ensureReady()`; confirmed fail (hang/timeout), restored, confirmed
      green `e256515`
- [x] F5: rewrote `test_EARS_PLAT_14_reclaim_runs_while_backgrounded`'s
      inner test to drive `AppLifecycleState.paused` + a genuinely running
      `BackgroundStub` through the real `BackgroundLifecycleObserver`, then
      let the coordinator's own live `Timer.periodic` fire — no
      hand-called `tick()` anywhere in the test. Falsified two ways: (a)
      the task's own suggested probe, `coordinator.stop()` right before
      the real fire — confirmed fail (payload stayed non-null, state
      stayed `queued`); (b) production mutation, commented out
      `counters.reclaimed += await _stack.relayEngine.reclaimPayloads();`
      in `messaging_coordinator.dart` — confirmed fail for the same
      reason. Both restored, confirmed green `07abd74`
- [x] Full suite green (1290/1290 = 1289 baseline + F1's 1 new test; F3/F4/F5
      rewritten in place, no test count change), `flutter analyze` clean
      (same 2 pre-existing unrelated info lints as baseline)

## Self-review
- [x] `flutter test` (full suite): 1290/1290 green
- [x] `flutter analyze`: 2 pre-existing, unrelated info-level lints only
      (`test/core/calls/call_migration_controller_test.dart:177` missing
      `@override`; `test/features/login/presentation/
      login_controller_enrollment_gate_test.dart:18` unnecessary import) —
      zero issues in any of the four files this fix touches
- [x] Diff confined to exactly the four files in `files:` — verified via
      `git diff origin/development --stat` after every commit; no source
      file left changed (each falsification was reverted and verified
      restored before its commit)
- [x] `## What this fix does NOT do` respected: zero production files in
      the final diff; F2 left to `E10-B03` (already landed); no Kotlin
      harness added, both native limitations (`PowerStateMonitor.kt`,
      `NotificationChannels.kt`) explicitly disclosed in the new test
      comments instead of routed around; test count only grew (1289 ->
      1290), never shrank; no NFR-BATT-001/on-device claim made anywhere
- [x] Every falsification recorded above is genuine: run, confirmed fail
      for the named reason, restored, confirmed green — see Run log for
      the exact command output
- [x] No secrets or PII logged; no new logging added
- [x] ADRs touched: none — no schema, no dependency, no auth/payment code
- [x] **Deviation, disclosed:** three of the five findings (F3, F4, F5)
      required renaming the misleadingly-named `test(...)` block itself,
      not merely adding an assertion to it — the bug's own text names the
      wrong CLAIM the old name made (`"reads_false"`,
      `"channels_created_once_per_category"`) as part of the defect, not
      just the wrong assertion, so keeping the old name would leave the
      same false claim standing under a body that no longer even matches
      it. Read `## What this fix does NOT do`'s "does not ... rename any
      currently-passing test to make room" as the count-preservation
      guarantee it is paired with ("every one of the 913 stays") — test
      COUNT never decreased (1289 -> 1290, F1's addition only), and no
      test was deleted; F3/F4/F5's fix direction text explicitly describes
      the corrected name/claim each test should carry, which is
      incompatible with leaving the misleading name in place.

## Feedback log
- 2026-09-07 — Fixed directly, decision authority explicitly delegated to
  the agent per the standing autonomy grant (AGENTS.md rule 3,
  2026-09-05) — this is routine test-strengthening work with an
  unambiguous fix menu already on file, no new open question raised.

## Run log
- 2026-09-07 — Worktree `../wt-E10-B07`, branch `epic_10_bug_07` off
  `origin/development` at `f9c76f1`. Confirmed `E10-B03` (`ec2b684`) is
  already on `development`, so F2 is out of scope here as the task
  predicted.
- F1: wrote `test_EARS_NOTIFY_15_over_then_null_then_over_posts_once` in
  `storage_notification_source_test.dart`. Green on unmodified code.
  Mutated `storage_notification_source.dart:100` to
  `{ _wasOverThreshold = false; return; }` — new test failed
  (`Expected: an object with length of <1> / Actual: ... length of <2>`).
  Restored verbatim, re-ran file: 7/7 green. Commit `51b4889`.
- F3: rewrote the hollow test in `power_state_test.dart` to assert a
  round-tripped all-TRUE `PowerState` (opposite of the mock's own default)
  passes through `BackgroundService.powerState()` unaltered, and to cite
  `PowerStateMonitor.kt:55-74` (read directly) for the actual
  API-level-fallback claim, disclosing it as unverified in this
  environment. Green on unmodified code. Mutated
  `background_service.dart`'s `powerState()` to discard the real reply and
  substitute a hard-coded all-false `PowerState` — new test failed
  (`Expected: PowerState(...true...) / Actual: PowerState(...false...)`).
  Restored verbatim, re-ran file: 9/9 green. Commit `934a7de`.
- F4: rewrote the test in `notification_service_test.dart` to additionally
  assert `requestPermission` is never called across three `ensureReady()`
  calls when permission is already granted. Green on unmodified code.
  Mutated `notification_service.dart`'s `ensureReady()` to always await
  `hasPermission()` without short-circuiting on `true` — new test hung and
  failed on the 30s test-runner timeout (`permissionResults.first` never
  resolves once the short-circuit is gone and no `requestPermission` mock
  emits `onPermissionResult`). Restored verbatim, re-ran file: 8/8 green.
  Commit `e256515`.
- F5: rewrote the inner test of
  `test_EARS_PLAT_14_reclaim_runs_while_backgrounded` in
  `messaging_coordinator_background_test.dart` to build a real
  `BackgroundLifecycleObserver` via `AppBinding` + a `BackgroundStub`,
  call `didChangeAppLifecycleState(AppLifecycleState.paused)` for real,
  await the stub's `start()` settling to confirm `ServiceState.running`
  is genuine, then speed up the already-live `Timer.periodic` (the same
  test-speed technique `test_EARS_PLAT_12_no_second_timer_created` already
  uses in this same file) and let it fire on its own with no
  `coordinator.tick()` call anywhere in the test. Green on unmodified
  code, and stable across 3 repeated runs. Falsified twice: (a) inserted
  `await stack.coordinator.stop();` right after establishing state and
  before the real fire — new test failed (`Expected: 'expired' / Actual:
  'queued'`); (b) instead commented out
  `counters.reclaimed += await _stack.relayEngine.reclaimPayloads();` in
  `messaging_coordinator.dart` (production) — new test failed
  (`Expected: null / Actual: [9, 9, 9]`). Both reverted, re-ran file: 4/4
  green. Commit `07abd74`.
- Full suite after all four commits: `flutter test` — 1290/1290 green
  (1289 baseline + F1's 1 new test). `flutter analyze` — 2 pre-existing,
  unrelated info-level lints, identical to baseline, zero in the four
  files this fix touches. `git diff origin/development --stat` confirms
  only the four `files:` test files changed (an unrelated pre-existing
  divergence in `E10-B06.md` between `development` and old `epic_10`
  history shows in a full `git diff` but was never touched by any commit
  on this branch — verified via `git log -- epics/.../E10-B06.md`).
- Status set to `review-requested`. PR opened into `development`.

## Review (2026-09-07)
- **reviewed_by:** claude-opus-5 (independent worktree off
  `origin/epic_10_bug_07`, removed after review) — differs from
  `executed_by` (sonnet), satisfying rule 5.
- **verdict: APPROVE.** `git diff --stat origin/development...HEAD` — 4
  files, exactly `files:update` plus this task doc; zero production files.
- Independently re-falsified all four touched findings (F1, F3, F4, F5) by
  breaking the real production code itself, not trusting this file's own
  falsification claims: F1 (`storage_notification_source.dart:100`,
  forced `_wasOverThreshold` false) → new test fails, sole failure in the
  file; F3 (`background_service.dart:101`, hard-coded all-false return)
  → fails, proving the OLD all-false assertion would have passed this;
  F4 (removed `hasPermission()` short-circuit, and a sharper variant
  adding a redundant `requestPermission()` call while still returning
  `true`) → both fail, the sharper one invisible to the old test
  (`ensureChannelsCallCount` unchanged); F5 (removed the coordinator's
  `Timer.periodic`, separately commented out `reclaimPayloads()`, and a
  third mutation of the reviewer's own devising — backgrounding no longer
  calling `_setDesiredRunning(true)`) → all three fail. F5(b)'s output
  matched this file's own recorded `[9, 9, 9]` verbatim.
- Verified F2's "already fixed by `E10-B03`, outside this fence" claim:
  `ec2b684` confirmed an ancestor of `origin/development`
  (`git merge-base --is-ancestor`), added the `assert(level != full)`
  guard; deleting the downgrade line now fails 2 tests including a
  dedicated `E10-B03` regression test. Correctly left untouched.
- Checked F5 for flakiness (a real `Timer.periodic` firing on its own,
  rather than a hand-called `tick()`, is a genuine flakiness risk class):
  10/10 green sequentially, 7/8 green under 8-way parallel load (the one
  failure was a `.dart_tool` lock collision from the reviewer's own
  parallelism, not a test failure). Timer budget has ~6.6× headroom
  (200ms wait vs. 30ms period) and asserts `greaterThanOrEqualTo(1)`, so
  extra firings are tolerated.
- Reconfirmed full suite (1290/1290) and `flutter analyze` (2
  pre-existing unrelated info-lints).
- **Non-blocking finding (applied before merge):** `power_state_test.dart`'s
  F3 test was named
  `test_EARS_PLAT_11_dart_plumbing_forwards_all_false_snapshot_unaltered`
  but its body constructs an all-**TRUE** `PowerState` (its own comment
  already said so) — a wrong fixture descriptor, not a wrong guarantee
  (the `..._forwards ... unaltered` half is accurate and independently
  proven falsifiable above). Renamed to
  `test_EARS_PLAT_11_dart_plumbing_forwards_snapshot_unaltered`;
  re-ran the file, 9/9 still green.
