---
id: E10-T07
epic: E10
type: feature
title: Storage-warning notifications — StorageManager.latestPlan observer
layer: backend
size: S
status: done
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: S, range: "5k-15k" }
priority: { moscow: should, p: P3 }
depends_on: [E10-T06]
blocks: [E10-T10]
traces_to: [FR-NOTIFY-001]
required_context:
  - epics/E10-notifications-background/tasks/E10-T03.md
  - epics/E08-local-storage/tasks/E08-T06.md
  - epics/E08-local-storage/tasks/E08-T08.md
  - lib/core/storage/storage_manager.dart
  - lib/core/storage/storage_inventory.dart
design_contract: n/a
files:
  create:
    - lib/core/notifications/sources/storage_notification_source.dart
    - test/core/notifications/storage_notification_source_test.dart
  update:
    - lib/app/bindings.dart
  delete: []
started_at: 2026-09-04
completed_at: 2026-09-04
executed_by: claude-sonnet-5
reviewed_at: 2026-09-04
reviewed_by: claude-opus-5 (genuine cross-model)
review_outcome: APPROVE (PR #56, merged). Merge conflict with T06's bindings.dart registration resolved by orchestrating session before review (both registrations kept, order verified). Dashboard threshold-equivalence reasoning (plan.groups.isNotEmpty vs decisions.isNotEmpty) independently verified correct. One S4 finding: null-plan test doesn't actually prove the over->null->over no-double-post case; code is right, test is thin -- fold in next touch. 5th unwired dispose() instance confirmed inert, folded into existing tracker observation.
---
# E10-T07 · Storage-warning notifications

## 1. Goal
Warn the user once when local storage crosses the threshold the dashboard
already warns at — and not again on every six-hourly pass while it stays
there.

## 2. Business logic
- **FR-NOTIFY-001** lists "storage warnings" as its own notification class.
- **The producer already exists and needs no seam.**
  `StorageManager.latestPlan` is an observable `Rx<RetentionPlan?>`
  (`storage_manager.dart:82`), updated by every non-throttled `runPass`,
  which `MessagingCoordinator`'s tick already drives (`E08-T06`). This task
  observes it. That is why this task's `files:` touches **no E08 file at
  all** — the rarest and best case for a notification source.
- **Reuse the dashboard's threshold; do not pick a new one.** `E08-T08`
  built the dashboard storage warning card against a specific threshold.
  Two different "storage is full" thresholds in one app is a defect, and
  choosing a second number here is exactly the invention rule 1 forbids.
  Locate the existing constant and import it. **If it turns out to be an
  inline literal with no shared symbol, STOP and record it in §Open
  Questions rather than duplicating the number** — extracting it is an edit
  to an E08 file this task's fence does not permit.
- **Once per crossing, not once per pass.** The pass runs every six hours
  and the condition persists; re-notifying each time is how a user turns the
  channel off.

## 3. What this task does
- `storage_notification_source.dart`: observes `StorageManager.latestPlan`,
  computes the usage condition using the existing threshold, and emits
  `NotificationFacts(category: storageWarning, ...)` on a **rising edge
  only** — the first plan that is over threshold after one that was not (or
  after process start with no prior state). Falling below and rising again
  re-arms it.
- Edge state is in-memory and re-arms on process start. That is a deliberate,
  documented simplification: at most one extra warning per app launch, and no
  new table.
- `bindings.dart`: register on `E10-T03`'s dispatcher.

## 4. What this task does NOT do
- Does **not** touch `storage_manager.dart`, `storage_inventory.dart`,
  `retention_plan.dart`, `smart_mode_policy.dart`, `retention_executor.dart`
  or any other E08 file. Observation only.
- Does **not** add a second periodic timer, isolate or background service to
  drive the storage pass — `E08-T06.md:119-120` forbade itself that and named
  E10 as the owner of background operation; **`E10-T10` is where that is
  done, through the one existing tick**, not here.
- Does **not** change the storage pass's throttle, ordering, or retention
  decisions.
- Does **not** define a new threshold, percentage or denominator. `GAP-026`
  already settled that the dashboard's percentage has a specified
  denominator; this task inherits it and adds nothing.
- Does **not** offer a "free up space" action, deep link or storage screen —
  `design/screens/settings-storage.md` is E08's contract and E08's task
  (`OQ-E10-1` covers notification-side affordances generally).
- Does **not** persist the edge state.

## 5. Contract

### Functions
```yaml
- signature: "StorageNotificationSource(Rx<RetentionPlan?> latestPlan, {required bool Function(RetentionPlan) isOverThreshold}) -> NotificationSource"
  params: { isOverThreshold: "injected predicate built from E08's existing threshold symbol — never a literal written in this file" }
  returns: "Stream<NotificationFacts>, rising-edge only"
  purpose: "One warning per crossing, using the app's single storage threshold"

- signature: "StorageNotificationSource.dispose() -> void"
  purpose: "Drops the Rx worker; safe twice (bindings teardown calls it)"
```
Copy: title `Storage almost full`, body `NEXORA is running low on local
storage`. Under `hidden`, unchanged — this notification contains no personal
content at any privacy level, which is worth stating so nobody adds a figure
to it later.

## 6. Risks
- `Rx` workers (`ever`/`everAll`) leak if not disposed; `bindings.dart`
  teardown must call `dispose()`.
- The first plan after process start is not a "crossing". Treat an initial
  over-threshold reading as a crossing **once**, then latch — otherwise a
  user whose device is persistently full is never warned at all, which is
  the opposite failure and the worse one.
- `latestPlan` is null before the first pass. Null must not be read as
  "under threshold" and trigger a spurious edge when the first real value
  arrives.
- The throttle means the pass may not run for six hours after storage fills;
  this notification is therefore *late by design*. Do not "fix" that by
  running a pass — that is a second driver.

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS id `eca13a7`
- [x] existing threshold symbol located and imported (or `OQ` raised — §2) `eca13a7`
- [x] `StorageNotificationSource` with rising-edge latch + null handling `eca13a7`
- [x] `dispose()` implemented; no `bindings.dart` teardown call site exists to
      call it from (§Deviations) `eca13a7`
- [x] `bindings.dart` registration `eca13a7`
- [x] `flutter analyze` clean, `flutter test` green (866/866) `eca13a7`

## 8. Acceptance criteria (EARS) + test plan
- **EARS-NOTIFY-14**: WHEN a storage pass produces a plan that is over the
  storage threshold and the previous observed state was not, the system
  SHALL post one `storageWarning` notification. (FR-NOTIFY-001)
- **EARS-NOTIFY-15**: WHILE storage remains over threshold across subsequent
  passes, the system SHALL NOT post a further notification until it has
  fallen below and risen again. (FR-NOTIFY-001)

### Tests
- `test_EARS_NOTIFY_14_rising_edge_posts_once`
- `test_EARS_NOTIFY_14_first_reading_over_threshold_posts_once`
- `test_EARS_NOTIFY_15_repeated_over_threshold_passes_post_nothing`
- `test_EARS_NOTIFY_15_falling_then_rising_rearms`
- `test_EARS_NOTIFY_14_null_plan_posts_nothing`

### Manual
1. Fill local storage past the threshold, wait for a pass → one notification.
2. Next pass → nothing new.

## 9. Self-review + Definition of Done
### Self-review
- [x] Every §7 item done, with commit hashes
- [x] `flutter analyze` + `flutter test` green (866/866, 0 issues in touched files)
- [x] Diff confined to `files:`; §4 respected — **zero E08 files touched**
- [x] Threshold is imported, not re-typed — see §Deviations: there is no
      numeric symbol to import; the reused condition is structural
- [x] `Rx` worker disposed — `dispose()` implemented and unit-tested
      (`dispose() is safe to call twice`); no caller wired from
      `bindings.dart` (§Deviations)

**Deviations from spec/design:**
1. **§2/§5 "existing threshold symbol" resolved as structural, not
   numeric.** Read `dashboard_controller.dart:398-436` (`E08-T08`) in full:
   the warning glyph (`StorageUsageVm.warningActive`) is gated by
   `warningActive: decisions.isNotEmpty` (line 418) — whether Smart Mode's
   last pass produced any candidate group, read from the durable
   `storage_decisions` log. There is no numeric percentage/byte threshold
   anywhere in `core/storage` that gates that glyph;
   `SmartModeThresholds.storagePressureRatio` (0.9) is an internal Smart
   Mode *factor* input scored inside `smart_mode_policy.dart` and never
   referenced by the dashboard's warning condition. So OQ-E10-T07-1's
   literal-vs-symbol fork does not apply — there is neither a shared
   numeric symbol to import nor an inline numeric literal to avoid
   duplicating. The honest translation of "reuse the dashboard's
   threshold" onto this task's required seam (`latestPlan`, not the
   decision log) is `(RetentionPlan plan) => plan.groups.isNotEmpty` —
   same condition, same meaning ("would Smart Mode remove anything"),
   applied to the live plan instead of its durable log record. Wired in
   `bindings.dart`, not in `storage_notification_source.dart` (no literal
   of any kind appears in the source file, satisfying §5's constructor
   contract). Closes `OQ-E10-T07-1` — see below.
2. **`dispose()` has no call site in `bindings.dart` today.** The task's
   §6 risk and §7 checklist both name "`bindings.dart` teardown must call
   `dispose()`", but `AppBinding.dependencies()` has no teardown call site
   at all — the file's own standing comment (present before this task,
   `bindings.dart:197-201` pre-change) documents the identical gap for
   `notificationDispatcher.stop()` and `messagingStack.dispose()`, neither
   of which has a caller either, and states plainly that inventing
   app-lifecycle teardown is `E10-T10`'s scope, not a task registering one
   more source. Implementing `dispose()` and leaving it uncalled would
   silently reproduce the exact "dispose()-wiring gap" this epic's tracker
   already carries forward from T04 (`CallSignaling.dispose()` has no
   caller) — instead: `dispose()` is implemented per the §5 contract,
   unit-tested directly (not through the composition root), and the
   `StorageNotificationSource` instance is registered with `Get.put(...,
   permanent: true)` in `bindings.dart` so a future teardown hook
   (`E10-T10` or later) can resolve and call it without another
   `bindings.dart` edit. Documented explicitly in `bindings.dart` at the
   registration site rather than left implicit. This is the fourth
   instance of the same pattern noted for the tracker's retro.

**Files actually touched:**
- `lib/core/notifications/sources/storage_notification_source.dart` (new)
- `test/core/notifications/storage_notification_source_test.dart` (new)
- `lib/app/bindings.dart` (registration + import widen)

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id
- [ ] Review APPROVE from a different model than `executed_by` (rule 5)
- [ ] Squash-merged to `epic_10`; tracker stamped
- [ ] 🧍 Human `verified`

## Open Questions
- **OQ-E10-T07-1 — the storage threshold may not exist as a shared symbol.**
  If `E08-T08`'s dashboard card compares against an inline literal, this task
  cannot import it and must not duplicate it. The fix is a one-line
  extraction in an E08 file, which is outside this fence.
  - **Status:** 🟢 closed — checked first, per §2's own instruction. The
    dashboard's warning condition (`dashboard_controller.dart:418`,
    `warningActive: decisions.isNotEmpty`) is neither a shared numeric
    symbol nor an inline numeric literal — it is a structural condition
    ("did the pass find anything to remove"). Reused as
    `plan.groups.isNotEmpty`, wired in `bindings.dart`, with no number
    introduced anywhere. No E08 file touched; no scope amendment needed.
  - **Owner:** builder (resolved in this task, `eca13a7`)

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- 2026-09-04 Branched off `origin/epic_10`'s actual tip at dispatch time,
  `45d6d10` ("T05 merged and cross-model reviewed") — `E10-T06`'s own PR
  (#55) was still `OPEN`, not merged, when this task started (per the
  dispatch instructions: branch off the real tip and note it). This task's
  `files:` do not overlap `E10-T06`'s (`group_notification_source.dart`,
  `group_membership_service.dart`) except both edit `lib/app/bindings.dart`
  — the same shared-file case the epic's own anti-collision matrix already
  serializes via `depends_on`. Whoever merges next (T06's PR or this one)
  will hand the other a one-line rebase on `bindings.dart`, same as every
  other file in that table.
- 2026-09-04 Read `dashboard_controller.dart:398-436` in full before
  writing any code, per §2's explicit "check first" instruction, to locate
  the "existing threshold". Found it is not numeric (see §Deviations #1) —
  closes `OQ-E10-T07-1` without a scope amendment.
- 2026-09-04 Tests written first (`storage_notification_source_test.dart`,
  5 named EARS tests + 1 dispose-idempotence test), then
  `storage_notification_source.dart`, then `bindings.dart` wiring.
  `flutter analyze`: clean (one unrelated pre-existing info-level lint in
  `test/core/calls/call_migration_controller_test.dart`, outside `files:`).
  `flutter test`: 866/866 green, commit `eca13a7`.
