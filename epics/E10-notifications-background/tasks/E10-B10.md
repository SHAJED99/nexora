---
id: E10-B10
epic: E10
type: bug
title: "NotificationService.ensureReady() can throw or hang forever instead of returning bool, silently killing every E10 notification source for the rest of the process"
layer: backend
size: S
status: done
owner_agent: builder
model: any
preferred_agent: any
token_estimate: { tier: S, range: "15k-30k" }
severity: S2
priority: { moscow: should, p: TBD }
depends_on: [E10-T01]
blocks: []
traces_to: [FR-NOTIFY-001, FR-PLAT-003]
required_context:
  - epics/E10-notifications-background/tasks/E10-T01.md
  - lib/core/notifications/notification_service.dart
  - lib/core/notifications/notification_dispatcher.dart
found_by: reviewer (independent, cross-model, claude-opus-5) during a retroactive rule-5 re-review of E10-T01
found_in: "ensureReady()'s own doc comment promises Future<bool>, 'true when channels exist and permission is granted; false when denied' -- but had no try/catch and no timeout on the permission-result wait, so a failure to determine readiness surfaced as an uncaught throw or an unresolved Future instead."
files:
  create: []
  update:
    - lib/core/notifications/notification_service.dart
    - test/core/notifications/notification_service_test.dart
  delete: []
---
# E10-B10 · `ensureReady()` doesn't honor its own `Future<bool>` contract

## The defect

`NotificationService.ensureReady()`'s doc comment states the contract
plainly: "Returns true when channels exist and permission is granted;
false when denied." Two real ways it could instead throw or hang forever,
both confirmed by probe:

1. **`_api.ensureChannels()` throwing.** With no host attached yet (the
   window before `NotificationApiHost.attach` runs), the real generated
   Pigeon call throws a `PlatformException`/`MissingPluginException`
   rather than returning. `ensureReady()` had no `try`/`catch` at all, so
   this propagated straight to the caller.
2. **`permissionResults.first` never resolving.** If the OS never
   delivers `onPermissionResult` -- reachable in production, since
   `NotificationApiHost.requestPermission()` can be called against a
   destroyed `Activity` while `ForegroundMeshService` keeps the engine
   alive (the exact "no Activity, revived engine" shape `E10-B05` already
   found on the neighboring foreground-service-notification path) -- the
   `await` on that stream's first event had no bound and hung forever.

`notification_dispatcher.dart`'s `start()` sets `_started = true` before
awaiting `ensureReady()` and only subscribes its notification sources
after it returns, so either failure mode silently disabled every E10
notification source (message, call, connection request, group event,
storage warning) for the rest of the process, with nothing surfaced to
the user and no way to recover short of restarting the app.

## Severity

**S2** -- a real failure mode reachable via a documented, adjacent
lifecycle interaction (`E10-B05`'s own finding), with a total,
process-lifetime loss of every notification source and no user-visible
signal that anything went wrong. Not S1: the app itself keeps running
normally; only notifications stop. Not S3: the blast radius is total
(every category, not one) and the recovery (restart) is not obvious to a
user who has no idea notifications silently stopped.

## Fix direction

Wrap the whole method body in `try`/`catch`, returning `false` on any
exception -- matching the already-documented "false when denied"
contract rather than distinguishing "denied" from "couldn't determine."
Bound every awaited step (`ensureChannels`, `hasPermission`,
`permissionResults.first`, `requestPermission`) with a `.timeout(...)`,
matching this codebase's existing best-effort-platform-call convention
(`FirebaseMetadataService`/`SyncCursorService`, both a 10s default).

## What this fix does NOT do

- Does **not** change `post()`'s or `cancel()`'s own implementation. **Correction (round-2 review):** both actually share this exact defect class today (they forward the raw Pigeon call uncaught) -- `post()`'s own "never throws" doc comment is scoped only to a permission refusal, not a host-not-attached error. Not fixed here; tracked as `CF-21` in the tracker for a follow-up in this same file.
- Does **not** distinguish "permission denied" from "couldn't determine
  readiness" in the return value -- both collapse to `false`, matching
  the method's own pre-existing, unchanged doc comment.
- Does **not** touch the tap-delivery gap -- that is `E10-B09`, a
  separate finding on the same task, filed and fixed independently.

## Regression test (write it first)

Two new tests in `notification_service_test.dart`:
- `test_EARS_PLAT_5_ensureReady_returns_false_not_throws_when_the_host_channel_is_unavailable`
  -- no mock handler registered for `ensureChannels`, confirming
  `ensureReady()` returns `false` rather than propagating the resulting
  `PlatformException`.
- `test_EARS_PLAT_5_ensureReady_returns_false_rather_than_hanging_when_the_permission_result_never_arrives`
  -- `hasPermission` reports `false`, `requestPermission` completes
  normally, but no `onPermissionResult` event is ever sent; confirms
  `ensureReady()` returns `false` after the (test-injected, short) timeout
  rather than hanging. `NotificationService`'s constructor gained an
  optional `readyTimeout` parameter so this test doesn't need to wait the
  real 10s default.

Both confirmed failing on pre-fix code (the first with the exact
predicted `PlatformException`; the second by genuinely hanging, caught
only by the test runner's own 30s cap) and passing post-fix.

## Run log
- 2026-09-04 — Fixed on branch `docs_retro_e10` (retro pass) off
  `origin/epic_10`. Wrapped `ensureReady()` in `try`/`catch` returning
  `false`; added a `.timeout()` (default 10s, overridable via a new
  `readyTimeout` constructor parameter) on every awaited step. Added the
  two regression tests above. Falsified: reverted `ensureReady()`'s body
  to its pre-fix shape (keeping the constructor's new parameter, so the
  file still compiled), confirmed both new tests fail for the predicted
  reasons, restored, reran green. Full suite: 938/938. `flutter analyze`:
  1 pre-existing, unrelated issue (confirmed present before this fix too,
  see `E10-B09`'s Run log for the same note).
- 2026-09-04 — **Round 2 (Opus review, real finding):** round 1 applied
  `.timeout()` to `permissionResults.first` at the moment it was
  captured, before `requestPermission()` was awaited. If
  `requestPermission()` itself threw or timed out, that captured `Future`
  was abandoned mid-flight with its own live timeout `Timer` still
  running -- it later completed with a `TimeoutException` no listener was
  left to catch, an unhandled asynchronous error escaping to the zone
  (exactly the class of failure this bug exists to eliminate, on a path
  round 1 didn't cover). Fixed two ways: (1) moved `.timeout()` to the
  actual `await` site rather than applying it at capture time, and (2)
  attached an immediate no-op `.catchError` "drain" to the captured
  `Future` the instant it's created -- necessary because even with (1)
  alone, closing the stream controller with no event ever emitted (e.g.
  `dispose()` in a test's `tearDown`) independently completes `.first`
  with `Bad state: No element`, an unrelated second way to abandon the
  same `Future` unhandled. A `Future` fans out to every listener
  independently, so the drain and the real `await` do not interfere.
  Added `test_EARS_PLAT_5_ensureReady_returns_false_and_leaves_no_
  unhandled_error_when_requestPermission_itself_fails` for this exact
  path. Falsified against round 1's exact code (stashed round 2, reran):
  fails with `TimeoutException after ...: Future not completed` -- the
  precise round-1 defect, reproduced then fixed. Also corrected: 8
  in-code comments across four files mis-cited this fix as `E10-B08`
  (an already-closed, unrelated bug) instead of `E10-B10` -- fixed to the
  correct id everywhere. Full suite: 939/939. `flutter build apk
  --debug`: compiles clean. **Correction (round-2 review):** `touch`ing
  the Kotlin files does not actually force a Gradle recompile (Kotlin's
  input snapshotting is content-hash based, not mtime-based) -- this
  session's own "forced recompile" claims were up-to-date cached greens.
  The round-2 reviewer confirmed a genuine recompile only after
  `rm -rf android/app/build`, which also came back clean.
