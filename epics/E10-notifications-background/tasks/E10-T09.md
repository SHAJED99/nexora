---
id: E10-T09
epic: E10
type: feature
title: Power-state signals over Pigeon — Doze, Battery Saver, screen lock, background restriction
layer: cross-cutting
size: S
status: done
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: S, range: "5k-15k" }
priority: { moscow: should, p: P3 }
depends_on: [E10-T08]
blocks: [E10-T10]
traces_to: [FR-PLAT-002, FR-PLAT-003]
required_context:
  - agent/memory/decisions/ADR-0007-background-execution.md
  - epics/E10-notifications-background/tasks/E10-T08.md
  - pigeons/background.dart
design_contract: n/a
files:
  create:
    - android/app/src/main/kotlin/com/nexora/nexora/background/PowerStateMonitor.kt
    - lib/core/background/power_state.dart
    - test/core/background/power_state_test.dart
  update:
    - pigeons/background.dart
    - lib/core/background/generated/background_api.g.dart
    - lib/core/background/background_service.dart
    - lib/core/background/background_stub.dart
    - android/app/src/main/kotlin/com/nexora/nexora/background/BackgroundApi.g.kt
    - android/app/src/main/kotlin/com/nexora/nexora/background/BackgroundApiHost.kt
  delete: []
started_at: 2026-09-04
completed_at:
executed_by: sonnet
reviewed_at: 2026-09-04
reviewed_by: claude-opus-5 (genuine cross-model)
review_outcome: APPROVE (PR #60, merged). API-level guards, receiver registration/lifecycle, dedup logic (last-value not set-membership), and CoroutineScope teardown all independently verified. 3 carried-forward observations recorded in tracker (S3 receiver leak on engine recreation, no broadcast exists for isBackgroundRestricted, one tautological test). On-device manual verification remains a standing limitation, same as E09-T05/E10-T08.
---
# E10-T09 · Power-state signals

## 1. Goal
Let Dart *see* the power environment it is running in — Doze, Battery Saver,
background restriction, screen lock — so `E10-T10` can adapt to it instead of
guessing.

## 2. Business logic
- **EARS-PLAT-1 / FR-PLAT-002** requires the system to "account for Android
  Doze, Battery Saver, background execution restrictions, app process
  termination and screen lock". Accounting for something starts with being
  able to observe it. Today nothing in the codebase can.
- **FR-PLAT-003 + ADR-0004**: these are system queries and system broadcasts —
  native, behind the Pigeon interface `E10-T08` created.
- **This task observes and reports. It changes no behaviour.** Nothing in the
  app reacts to these signals until `E10-T10`. Split this way deliberately:
  the reporting half is testable and low-risk, the reacting half is a policy
  decision with battery consequences.
- **`E04-T03a.md:97-98` deferred Doze handling to "T03b/T03c's concern" and
  neither built it** — a grep of the whole E04 epic for `Doze` returns only
  those two lines. This task is where Doze first becomes observable in this
  codebase at all; `E10-T10` §4 records what it does and does not do about
  the Bluetooth read loops.

## 3. What this task does
- Extends `pigeons/background.dart` with a `PowerState` class, a
  `powerState()` host method and an `onPowerStateChanged` event; regenerates
  both files.
- `PowerStateMonitor.kt` — reads `PowerManager.isDeviceIdleMode`,
  `isPowerSaveMode`, `ActivityManager.isBackgroundRestricted`,
  `PowerManager.isIgnoringBatteryOptimizations`, and `KeyguardManager`/screen
  state; registers receivers for `ACTION_DEVICE_IDLE_MODE_CHANGED`,
  `ACTION_POWER_SAVE_MODE_CHANGED`, `ACTION_SCREEN_ON/OFF` and
  `ACTION_USER_PRESENT`, emitting `onPowerStateChanged` on each. Registered
  with `RECEIVER_NOT_EXPORTED` (these are protected system broadcasts —
  the same posture `BluetoothTransport` uses).
- `BackgroundApiHost.kt` — delegates `powerState()` to the monitor;
  registers/unregisters the monitor in the existing attach/detach path.
- `power_state.dart` — the Dart model + `BackgroundService.powerState` /
  `powerStates` stream; `background_stub.dart` gains a settable power state
  so `E10-T10` is testable without a device.

## 4. What this task does NOT do
- Does **not** change any app behaviour based on these signals — no tick
  interval change, no discovery suppression, no notification. `E10-T10`.
- Does **not** request `REQUEST_IGNORE_BATTERY_OPTIMIZATIONS`. It **reads**
  `isIgnoringBatteryOptimizations` (a query, no permission) but never prompts
  — ADR-0007 §S2 / `OQ-E10-4`.
- Does **not** acquire a wake lock, `WAKE_LOCK` permission, or an alarm.
- Does **not** add any permission to the manifest at all. Every value here is
  readable without one; if an implementer finds one that is not, that is an
  Open Question, not a manifest edit.
- Does **not** touch `.../transport/`, `BluetoothTransport.kt`, or the
  Bluetooth read loops.
- Does **not** persist power state or log it with a timestamp trail — a
  device-idle history is a behavioural fingerprint.

## 5. Contract

### Pigeon schema additions (`pigeons/background.dart`)
```dart
class PowerState {
  bool deviceIdle;             // Doze
  bool powerSaveMode;          // Battery Saver
  bool backgroundRestricted;   // per-app background restriction
  bool ignoringBatteryOptimizations;
  bool screenLocked;
}

// added to BackgroundApi
PowerState powerState();

// added to BackgroundEventsApi
void onPowerStateChanged(PowerState state);
```

### Functions
```yaml
- signature: "BackgroundService.powerState() -> Future<PowerState>"
  returns: "a snapshot; on an API level lacking a signal the field is false, never null"
  purpose: "One-shot read for startup"

- signature: "BackgroundService.powerStates -> Stream<PowerState>"
  returns: "broadcast; emits on every system transition, de-duplicated on equal states"
  purpose: "What E10-T10 subscribes to"

- signature: "PowerStateMonitor.register(Context) -> Unit / unregister() -> Unit"
  purpose: "Receiver lifetime tied to the existing host attach/detach — no leak"

- signature: "BackgroundStub.emitPowerState(PowerState state) -> void"
  purpose: "Test lever; without it E10-T10 has no hardware-free test"
```

## 6. Risks
- **API-level gaps.** `isDeviceIdleMode` is API 23+, `isBackgroundRestricted`
  API 28+. Missing signals must default to `false` (the permissive reading),
  never crash, and never be reported as `true` by accident — a spurious
  "Doze" makes `E10-T10` throttle a device that is wide awake.
- Receivers must be unregistered on detach or every hot restart leaks one.
- Broadcast storms: `ACTION_SCREEN_ON/OFF` fire often. De-duplicate equal
  states before emitting, or `E10-T10` recomputes policy for nothing.
- These signals are **not** a substitute for observing that the service was
  killed — an OEM killer produces no broadcast at all. `E10-T08`'s
  `ServiceState.stoppedBySystem` is the signal for that; don't conflate them.

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS id `028ddb1`
- [x] schema extended; both generated files regenerated (not hand-edited) `028ddb1`
- [x] `PowerStateMonitor.kt` — five reads, four receivers, NOT_EXPORTED `028ddb1`
- [x] host wiring + unregister on detach `028ddb1`
- [x] `power_state.dart` + stream de-duplication `028ddb1`
- [x] `background_stub.dart` emit lever `028ddb1`
- [x] `flutter analyze` clean, `flutter test` green, `flutter build apk --debug` succeeds `028ddb1`

## 8. Acceptance criteria (EARS) + test plan
- **EARS-PLAT-17**: WHEN the device enters or leaves Doze, Battery Saver,
  background restriction or screen lock, the system SHALL emit a
  `PowerState` reflecting the new value. (FR-PLAT-002, FR-PLAT-003)
- **EARS-PLAT-11**: IF a signal is unavailable on the running API level,
  THEN the system SHALL report it as `false` and SHALL NOT throw; and equal
  consecutive states SHALL NOT be re-emitted. (FR-PLAT-002)

### Tests
- `test_EARS_PLAT_17_state_change_emits` (× each of the four flags)
- `test_EARS_PLAT_11_duplicate_state_not_reemitted`
- `test_EARS_PLAT_11_missing_signal_reads_false`
- Kotlin proven by compile + manual; no mock-only Kotlin test.

### Manual
1. `adb shell dumpsys deviceidle force-idle` → a `deviceIdle: true` event.
2. `adb shell settings put global low_power 1` → `powerSaveMode: true`.
3. Lock the screen → `screenLocked: true`; unlock → `false`.

## 9. Self-review + Definition of Done
### Self-review
- [x] Every §7 item done, with commit hashes — all `028ddb1`
- [x] `flutter analyze` + `flutter test` green — 896/896, 0 issues (1
  pre-existing `annotate_overrides` info in an unrelated E-of-scope test
  file, untouched by this diff)
- [x] Diff confined to `files:`; §4 respected — exactly the 9 files the
  task lists (3 created, 6 updated); nothing in `.../transport/` or
  `BluetoothTransport.kt` touched
- [x] **No manifest change in the diff** — confirmed via
  `git diff -- android/app/src/main/AndroidManifest.xml` (empty)
- [x] No behaviour anywhere reacts to power state yet — `PowerStateMonitor`
  only reads and reports; `BackgroundApiHost.powerState()`/the event
  forwarder do not call `start()`/`stop()` or change cadence
- [x] Receivers unregistered on detach; no power-state history logged —
  `PowerStateMonitor.unregister()` called from `BackgroundApiHost.detach`;
  no timestamped log/persistence of any `PowerState` value anywhere

**Deviations from spec/design**:
- `BackgroundApiHost` gained its own `CoroutineScope` (`eventsScope`,
  created in `attach`, cancelled in `detach`) to launch the suspend
  `eventsApi.onPowerStateChanged` call — the class had no scope of its own
  before this task (it borrowed `ForegroundMeshService`'s indirectly via
  `eventsApi`, which is only ever *called*, never scheduled, from this
  class). This mirrors the established pattern this task's own brief
  pointed at (disclosed deviation, not silently wired into
  `messaging_stack.dart`'s dispose path — this scope's lifecycle is
  self-contained to `attach`/`detach`, which already existed).
- De-duplication is implemented at **two** layers, not one: once in
  `PowerStateMonitor.emitIfChanged` (Kotlin, so a `SCREEN_ON`/`SCREEN_OFF`
  storm with no real state change never reaches the channel at all) and
  again in `BackgroundService._handlePowerStateChanged` (Dart, so
  EARS-PLAT-11 holds as a Dart-observable contract regardless of what the
  native layer already filtered, and so `BackgroundStub.emitPowerState`
  can offer the same guarantee without touching a platform channel). The
  task's contract only names the Dart-side stream's de-dup explicitly;
  the Kotlin-side dedup is additive belt-and-suspenders, not a
  substitute, and does not change what the Dart contract promises.
- **On-device verification not performed** (§8 Manual steps 1-3 —
  `dumpsys deviceidle force-idle`, `settings put global low_power 1`,
  screen lock/unlock) — this environment has no physical device or
  emulator attached, same standing limitation disclosed in E09-T05 and
  E10-T08. `flutter build apk --debug` compiles the native code cleanly
  (task's own "Kotlin proven by compile + manual" standard), and every
  Dart-observable contract is proven by `power_state_test.dart` against
  the real generated Pigeon codec. The four-receiver registration/
  unregistration path, the five live `PowerManager`/`ActivityManager`/
  `KeyguardManager` reads, and the actual broadcast delivery remain
  unverified on real hardware — a reviewer or the human `verified` gate
  should treat the manual steps as outstanding, not satisfied.

**Files actually touched:**
- `pigeons/background.dart` (update)
- `lib/core/background/generated/background_api.g.dart` (update, regenerated)
- `android/app/src/main/kotlin/com/nexora/nexora/background/BackgroundApi.g.kt` (update, regenerated)
- `android/app/src/main/kotlin/com/nexora/nexora/background/BackgroundApiHost.kt` (update)
- `android/app/src/main/kotlin/com/nexora/nexora/background/PowerStateMonitor.kt` (create)
- `lib/core/background/power_state.dart` (create)
- `lib/core/background/background_service.dart` (update)
- `lib/core/background/background_stub.dart` (update)
- `test/core/background/power_state_test.dart` (create)

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id — see
  §Run log for the mapping
- [ ] Review APPROVE from a different model than `executed_by` (rule 5)
- [ ] Squash-merged to `epic_10`; tracker stamped
- [ ] 🧍 Human `verified` — manual on-device steps (§8 Manual) remain
  outstanding per the disclosed deviation above

## Open Questions
_(none — this task reports facts the OS already publishes; the decisions all
live in `E10-T10` and ADR-0007.)_

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- Commit `028ddb1` — full implementation in one commit (worktree
  `E:\Code\Flutter\12\wt-E10-T09`, branch `epic_10_task_09`).
- EARS -> test mapping (`test/core/background/power_state_test.dart`):
  - EARS-PLAT-17: `test_EARS_PLAT_17_state_change_emits` group — one case
    per flag (`deviceIdle`, `powerSaveMode`, `backgroundRestricted`,
    `screenLocked`), each sending a real encoded `onPowerStateChanged`
    event through the generated codec and asserting it reaches
    `BackgroundService.powerStates`.
  - EARS-PLAT-11 (dedup): `test_EARS_PLAT_11_duplicate_state_not_reemitted`
    — two identical encoded events, exactly one item observed on the
    stream.
  - EARS-PLAT-11 (missing signal -> false, no throw):
    `test_EARS_PLAT_11_missing_signal_reads_false` — an all-false
    `PowerState` round-trips through `BackgroundService.powerState()`
    without throwing (the API-level branching itself is native and is
    covered by `flutter build apk --debug` compiling, per the task's own
    "Kotlin proven by compile + manual" standard).
  - `test_background_stub_emit_power_state_is_observable` and
    `test_background_stub_duplicate_power_state_not_reemitted` prove
    `BackgroundStub.emitPowerState`'s contract independent of any
    platform channel — the lever `E10-T10` depends on.
- Verified red-first honestly: reverted `background_service.dart`,
  `background_stub.dart`, `generated/background_api.g.dart` and deleted
  `power_state.dart` back to the pre-task state, re-ran
  `power_state_test.dart`, confirmed it fails (undefined
  `powerState`/`powerStates`/`emitPowerState` members) — then restored the
  implementation and re-ran the full suite green (896/896).
- `flutter analyze` (whole project): 0 issues from this diff (1
  pre-existing, unrelated `annotate_overrides` info in
  `test/core/calls/call_migration_controller_test.dart`).
- `flutter build apk --debug`: succeeds — `build\app\outputs\flutter-apk\app-debug.apk`.
- On-device manual verification (§8 Manual) **not performed** — no
  physical device/emulator in this environment; see §9 Deviations.
