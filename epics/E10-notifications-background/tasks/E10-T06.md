---
id: E10-T06
epic: E10
type: feature
title: Group-event notifications — GroupMembershipService observation seam
layer: backend
size: S
status: review-requested
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: S, range: "5k-15k" }
priority: { moscow: should, p: P3 }
depends_on: [E10-T05]
blocks: [E10-T07]
traces_to: [FR-NOTIFY-001]
required_context:
  - epics/E10-notifications-background/tasks/E10-T03.md
  - lib/features/groups/domain/group_membership_service.dart
  - lib/core/messaging/group_control.dart
design_contract: n/a
files:
  create:
    - lib/core/notifications/sources/group_notification_source.dart
    - test/core/notifications/group_notification_source_test.dart
  update:
    - lib/features/groups/domain/group_membership_service.dart
    - lib/app/bindings.dart
    - test/features/groups/domain/group_membership_service_test.dart
  delete: []
started_at: 2026-09-04
completed_at: 2026-09-04
executed_by: claude-sonnet-5
reviewed_at:
reviewed_by:
review_outcome:
---
# E10-T06 · Group-event notifications

## 1. Goal
Tell the user when something happened to a group they are in — added,
removed, renamed, ownership moved, group deleted — without telling them
about the changes they made themselves.

## 2. Business logic
- **FR-NOTIFY-001** lists "group events" as its own notification class.
- The producer exists: `GroupMembershipService.handleControlFrame`
  (`group_membership_service.dart:522`) is where an **inbound** membership
  change is applied. Locally-initiated changes go through `_perform`
  (`:287`) and its callers (`addMember`, `removeMember`, `rename`,
  `grantAdmin`, `revokeAdmin`, `transferOwnership`, `deleteGroup`, `leave`).
- **Only inbound changes notify.** A user who just renamed a group does not
  need to be told they renamed it; a notification for one's own action is
  noise that trains people to ignore the channel. The distinction is
  structural — the emission point is the inbound handler, not `_perform` —
  so it cannot drift.
- Group key rotation is **not** a group event for this purpose. It is a
  cryptographic consequence of a membership change and notifying on it would
  double every notification.

## 3. What this task does
- `group_membership_service.dart`: add `Stream<GroupEventNotice> get
  groupEvents` (broadcast), emitted from the **inbound** control-frame path
  only, after the change is applied and persisted, carrying
  `{ GroupEventKind kind, String groupId, String? groupName, String
  actorDeviceId }`. Controller closed in the existing teardown.
- `group_notification_source.dart`: maps each notice to
  `NotificationFacts(category: groupEvent, ...)`, using a stable notification
  id per `groupId` so a burst of changes to one group collapses into one
  notification rather than five.
- `bindings.dart`: register on the dispatcher.

## 4. What this task does NOT do
- Does **not** change membership logic, permission checks, key rotation, or
  any control-frame parsing. `group_permissions.dart`,
  `group_key_rotation_service.dart` and `group_control.dart` are not in
  `files:` and must not be edited.
- Does **not** emit from `_perform` or any locally-initiated path (§2).
- Does **not** notify for group *messages* — those arrive through
  `InboundPipeline.delivered` and are already `E10-T03`'s `message` class.
  Notifying twice for one group message is the obvious bug this fence exists
  to prevent.
- Does **not** notify on key rotation.
- Does **not** add group-event UI, a group activity feed, or notification
  actions (`OQ-E10-1`).
- Does **not** decrypt group message content.

## 5. Contract

### Functions
```yaml
- signature: "GroupMembershipService.groupEvents -> Stream<GroupEventNotice>"
  returns: "broadcast; inbound-applied membership/metadata changes only"
  purpose: "Observation seam on E07's service — emits after the change is durable"

- signature: "GroupEventKind"
  returns: "enum { addedToGroup, removedFromGroup, memberJoined, memberLeft, renamed, adminChanged, ownershipTransferred, groupDeleted }"
  purpose: "Closed set matched to the operations the service already performs"

- signature: "GroupNotificationSource(Stream<GroupEventNotice> events, {required String selfDeviceId}) -> NotificationSource"
  returns: "Stream<NotificationFacts>, one stable notification id per groupId"
  purpose: "Collapses a burst of changes to one group into one notification"
```
Copy: title `<groupName>` or `Group` when the name is unknown; body per kind
(`You were added to this group`, `You were removed from this group`,
`A member joined`, `A member left`, `The group was renamed`, `Admins
changed`, `Ownership was transferred`, `The group was deleted`). Under
`hidden`, title is `NEXORA` and body is `Group activity`.

## 6. Risks
- Emitting **before** the change is persisted means a notification for a
  change a later failure rolls back. Emit after the write, inside the same
  success path.
- `removedFromGroup` / `groupDeleted` may arrive after the local group row is
  gone — capture `groupName` before the deletion, or the notification reads
  `Group` for the case where the name matters most.
- A membership change that fans out to a key rotation must still produce
  exactly one notice. Count the emissions in the test, don't eyeball them.
- The self-vs-other distinction depends on `actorDeviceId` being correct;
  an inbound frame that a peer forwarded on someone else's behalf must
  attribute the originator, not the sender, or "a member joined" names the
  wrong device.

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS id `7bad1ea`
- [x] `GroupEventNotice`/`GroupEventKind` + broadcast controller `7bad1ea`
- [x] emissions from the inbound path only, after persistence `7bad1ea`
- [x] controller closed in existing teardown `7bad1ea`
- [x] `GroupNotificationSource` with per-group id collapsing `7bad1ea`
- [x] `bindings.dart` registration `7bad1ea`
- [x] existing group membership tests still green, unmodified `7bad1ea`
- [x] `flutter analyze` clean, `flutter test` green `7bad1ea`

## 8. Acceptance criteria (EARS) + test plan
- **EARS-NOTIFY-12**: WHEN an inbound group control frame applies a
  membership or metadata change, the system SHALL post one `groupEvent`
  notification for that group. (FR-NOTIFY-001)
- **EARS-NOTIFY-13**: IF the change was initiated locally, THEN the system
  SHALL post nothing; and a change that triggers a key rotation SHALL still
  produce exactly one notification. (FR-NOTIFY-001)

### Tests
- `test_EARS_NOTIFY_12_inbound_membership_change_posts_notification` (× kinds)
- `test_EARS_NOTIFY_13_local_change_posts_nothing` (× each `_perform` caller)
- `test_EARS_NOTIFY_13_rotation_does_not_double_notify`
- `test_EARS_NOTIFY_12_burst_collapses_to_one_id`

### Manual
1. Second device adds this device to a group → one notification.
2. This device renames a group → no notification on this device.

## 9. Self-review + Definition of Done
### Self-review
- [x] Every §7 item done, with commit hashes
- [x] `flutter analyze` + `flutter test` green (876 tests, 0 failures; the
      one `flutter analyze` info-level finding is pre-existing, in
      `test/core/calls/call_migration_controller_test.dart`, not a file this
      task touches)
- [x] Diff confined to `files:`; §4 respected (`git diff --stat` against
      `origin/epic_10` shows exactly the 5 files in this task's `files:`
      list, 0 elsewhere)
- [x] Membership/permission/rotation logic unchanged (no edit inside
      `_perform`, `_checkPermission`, `_mutateMembers`,
      `GroupPermissions.allows`, or `GroupKeyRotationService`; the one new
      call site, `_emitGroupEventNotice`, is additive and read-only against
      `_repository.groupRow`)
- [x] No group content decrypted; no device id logged (the source and the
      new notice class carry only `groupId`/`groupName`/`actorDeviceId`,
      all already-computed, non-secret fields; no `ObservabilityService`
      call was added by this task)

**Deviations from spec/design:**
1. **`GroupEventKind` renamed to `GroupNotificationEventKind`.** §5's
   contract names the new notification-domain enum `GroupEventKind`, but
   that name is already taken by `group_tables.dart`'s wire-level enum
   (`created`/`renamed`/`memberAdded`/...), imported unqualified into
   `group_membership_service.dart` via `group_control.dart` and used
   throughout that file (`frame.kind`, `_kindFor`, the switch in
   `handleControlFrame`). Reusing the name would be a same-file ambiguous
   import, not a style choice — `group_membership_service_test.dart`
   already imports both `group_control.dart` and this file together, so it
   would fail to compile. Every other detail of the contract (the
   eight-value closed set, field names on `GroupEventNotice`) matches §5
   exactly. See `group_membership_service.dart`'s own file header for the
   full reasoning.
2. **`dispose()` added to `GroupMembershipService`, not wired into any
   composition-root teardown.** Task file §3 requires "controller closed in
   the existing teardown"; `GroupMembershipService` had no `dispose()` of
   its own before this task, and `messaging_stack.dart` is not in this
   task's `files:` fence. Follows the exact disclosed-deviation pattern
   `E10-T04`/`E10-T05` already used for `CallSignaling.dispose()`/
   `PrekeyExchange.dispose()` (both confirmed still uncalled anywhere in
   `lib/` per the epic tracker's "Carried-forward observations") — added,
   not silently skipped, so a future composition-root teardown pass can
   wire all three dispose methods at once, per that tracker entry's own
   "Owner" note.
3. **`GroupNotificationSource.selfDeviceId` filters as defense-in-depth,
   not as the primary self/other distinction.** §5's constructor signature
   requires `selfDeviceId`; the self-vs-other split (`addedToGroup` vs
   `memberJoined`, etc.) is already fully resolved upstream, in
   `GroupMembershipService._notificationKindFor`, before a `GroupEventNotice`
   is ever published. This source still drops any notice whose
   `actorDeviceId == selfDeviceId` (which should never occur, since
   `groupEvents` only emits from the inbound path), matching this
   codebase's own established "check a security/privacy-relevant invariant
   twice" pattern (this file's own header; `group_membership_service.dart`'s
   "permission is checked twice, both times locally";
   `connection_request_notification_source.dart`'s blocked-peer filter).
4. **`NotificationFacts` does not carry `GroupEventNotice.kind`.** §5's copy
   table needs the kind (eight distinct body strings) to pick copy, but
   `notification_policy.dart` is not in this task's `files:` fence and
   `NotificationFacts`'s shape (`category`/`conversationId`/`peerDeviceId`/
   `peerDisplayName`/`stableId`) has no field for it. This mirrors
   `CallNotificationSource`/`ConnectionRequestNotificationSource` (E10-T04/
   T05), neither of which extended the copy table either — `groupEvent`
   notifications ship with the same generic "NEXORA"/"Notification" copy
   the tracker's "Carried-forward observations" already flags as still
   live for `incomingCall`/`connectionRequest`. Not a regression this task
   introduces; `OQ-E10-T06-1`/`OQ-E10-1` already covers the copy gap.

**Files actually touched:**
- `lib/features/groups/domain/group_membership_service.dart` (update)
- `lib/core/notifications/sources/group_notification_source.dart` (create)
- `lib/app/bindings.dart` (update)
- `test/features/groups/domain/group_membership_service_test.dart` (update)
- `test/core/notifications/group_notification_source_test.dart` (create)

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id — see
      `test/features/groups/domain/group_membership_service_test.dart`
      (`test_EARS_NOTIFY_12_*` × 8 kinds, `test_EARS_NOTIFY_13_local_change
      _posts_nothing` × every `_perform`/`createGroup` caller,
      `test_EARS_NOTIFY_13_rotation_does_not_double_notify`) and
      `test/core/notifications/group_notification_source_test.dart`
      (`test_EARS_NOTIFY_12_inbound_notice_posts_one_groupEvent
      _notification`, `test_EARS_NOTIFY_12_burst_collapses_to_one_id`,
      `test_EARS_NOTIFY_13_a_notice_naming_self_as_actor_posts_nothing`)
- [ ] Review APPROVE from a different model than `executed_by` (rule 5)
- [ ] Squash-merged to `epic_10`; tracker stamped
- [ ] 🧍 Human `verified`

## Open Questions
- **OQ-E10-T06-1 — group-event copy has no design source.** Eight strings are
  proposed in §5 with no contract behind them. Rolled up as `OQ-E10-1`.
  - **Status:** 🟡 open — not blocking
  - **Owner:** human

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- 2026-09-04 Implemented on `epic_10_task_06` off `origin/epic_10`
  (`45d6d10`). `flutter analyze`: 1 pre-existing info-level finding,
  unrelated file. `flutter test`: 876 tests, all green (27 in the two
  touched test files: 15 pre-existing + 12 new in
  `group_membership_service_test.dart`, 4 new in
  `group_notification_source_test.dart`). Commit `7bad1ea`. PR opened
  against `epic_10`.
