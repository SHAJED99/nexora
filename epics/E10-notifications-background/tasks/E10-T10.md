---
id: E10-T10
epic: E10
type: feature
title: Adaptive background policy — one tick, cadence and discovery respond to power state
layer: backend
size: M
status: done
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: should, p: P3 }
depends_on: [E10-T07, E10-T09]
blocks: []
traces_to: [FR-PLAT-001, FR-PLAT-002, NFR-BATT-001]
required_context:
  - agent/memory/decisions/ADR-0007-background-execution.md
  - epics/E10-notifications-background/tasks/E10-T08.md
  - epics/E10-notifications-background/tasks/E10-T09.md
  - epics/E06-personal-chat/tasks/E06-T06.md
  - epics/E08-local-storage/tasks/E08-T06.md
  - lib/core/messaging/messaging_coordinator.dart
  - lib/app/bindings.dart
design_contract: n/a
files:
  create:
    - lib/core/background/background_policy.dart
    - test/core/background/background_policy_test.dart
    - test/core/messaging/messaging_coordinator_background_test.dart
  update:
    - lib/core/messaging/messaging_coordinator.dart
    - lib/app/bindings.dart
  delete: []
started_at: 2026-09-04
completed_at: 2026-09-04
executed_by: claude-sonnet-5 (builder)
reviewed_at: 2026-09-04
reviewed_by: claude-opus-5 (genuine cross-model)
review_outcome: APPROVE (PR #61, merged). The epic's core one-tick invariant independently falsified and confirmed sound (setTimer sabotage caught by test_EARS_PLAT_12; reconcile-guard sabotage caught separately). isBackgroundRestricted re-read on resume confirmed real, not claimed. dispose()-wiring decline confirmed a legitimate rule-6 scope read. Non-red-first tests confirmed still rigorous (assert observable behavior, both broke correctly under reviewer-designed sabotage). One S3 finding: EARS-PLAT-13's own table (not the code) contradicts itself on stoppedBySystem+Doze -- folds into OQ-E10-T10-2. Two S4s carried forward.
---
# E10-T10 · Adaptive background policy

## 1. Goal
Close the loop: start the service when the app backgrounds, let the one
existing tick keep running while it is closed, slow that tick down when the
OS says the device is asleep — and prove the retention pass runs with nobody
looking.

## 2. Business logic
- **EARS-PLAT-1 / FR-PLAT-002**: "account for Doze, Battery Saver, background
  restrictions, process termination and screen lock." `E10-T09` made those
  observable; this task is the *accounting*.
- **NFR-SEC-001 / `E06-T06.md:382-384`**: "with the app closed, nothing
  forwards and nothing reclaims." `reclaimPayloads()` is the third-party
  ciphertext retention guarantee. Making it run while the app is closed is a
  **separate acceptance criterion with its own test** (EARS-PLAT-14), not a
  side effect of EARS-PLAT-12 — the obligation was raised as its own item and
  is discharged as its own item.
- **The one-tick invariant** (`E08-T06.md:119-120`, `:127-131`): this task may
  change the tick's *interval*, and nothing else. No reordering of
  `processQueue`/`sweepExpired`/`reclaimPayloads`, no change to the
  re-entrancy coalescing, no second driver, no isolate. The storage pass E08
  appended keeps its own throttle.
- **NFR-BATT-001 is a trade-off, not a target** — "battery efficiency shall
  never override reliability or communication requirements", and
  `[NEEDS NUMBER]`. The inherited 60 s floor is an endorsed *guess*
  (`E06-T06.md:528-534`: nothing has ever been measured). This task
  re-justifies it rather than inheriting it silently, and states plainly what
  it did and did not measure.
- **Doze suppression of discovery** is the one behavioural concession worth
  making: `BluetoothAdapter.startDiscovery()` during Doze burns radio for
  results the OS will not deliver anyway.

## 3. What this task does
- `background_policy.dart` — `BackgroundPolicy`: pure function from
  `(PowerState, ServiceState, AppLifecycleState)` to a
  `BackgroundPlan { Duration tickInterval, bool discoveryAllowed }`. Fully
  unit-testable, no I/O. The cadence table lives here and nowhere else.
- `messaging_coordinator.dart` — make `_tickInterval` **settable at runtime**
  via one new method that reschedules the existing `Timer.periodic`. That is
  the entire change to this file: no new work in the tick, no reordering, no
  new state.
- `bindings.dart` — a `WidgetsBindingObserver` that starts
  `BackgroundService` when the app is backgrounded and stops it when it
  returns to the foreground; subscribes to `powerStates` and
  `BackgroundService.state`, applies each `BackgroundPlan` by setting the
  coordinator's interval and gating `TransportService.startDiscovery`; and
  reconciles after a `stoppedBySystem` event or a cold start by calling
  `MessagingCoordinator.reconcileQueuedMessages()` once.
- Discovery gating goes through the existing `TransportService` API — no
  change to `.../transport/` or any native file.

## 4. What this task does NOT do
- Does **not** add a `Timer`, `WorkManager` job, isolate, alarm or any second
  periodic driver. Setting the existing timer's interval is the mechanism.
- Does **not** change what the tick *does*: no reordering, no new step, no
  change to re-entrancy coalescing, no change to E08's storage-pass throttle.
- Does **not** change `BluetoothTransport.kt`, the read loops, or any native
  transport file. **What happens to an open RFCOMM socket and its blocking
  read thread during Doze is genuinely unhandled and unverifiable here**
  (`E04-T03a.md:97-98` deferred it to T03b/T03c and neither built it, and no
  hardware is installable in this environment) — carried as `OQ-E10-8`, not
  quietly implemented against a guess.
- Does **not** request a battery-optimisation exemption, add
  `RECEIVE_BOOT_COMPLETED`, or add background location (`OQ-E10-4`,
  `OQ-E10-5`, `OQ-E10-7`; ADR-0007 §S2/S3).
- Does **not** add a dashboard background-status indicator or connectivity
  alerts. `E06-T12.md:116-117` fenced those to E10, but `design/screens/
  dashboard.md` is an **approved contract with no such element** — adding one
  needs a `design/gaps.md` entry and human approval (rule 2). `OQ-E10-9`.
- Does **not** claim an NFR-BATT-001 outcome that was not measured.
- Does **not** change the 60 s foreground floor. Only the background/idle
  intervals are new.

## 5. Contract

### Functions
```yaml
- signature: "BackgroundPolicy.plan({required PowerState power, required ServiceState service, required AppLifecycleState lifecycle}) -> BackgroundPlan"
  returns: "{ Duration tickInterval, bool discoveryAllowed }"
  purpose: "The whole cadence decision, pure and testable, in one place"

- signature: "MessagingCoordinator.setTickInterval(Duration interval) -> void"
  params: { interval: "the new floor; must be > Duration.zero" }
  returns: "void — reschedules the existing Timer.periodic; a tick in flight is not cancelled"
  purpose: "The ONLY change this task makes to the coordinator"

- signature: "BackgroundLifecycleObserver.start() / stop() -> void"
  purpose: "Composition-root wiring: lifecycle -> service start/stop -> policy -> interval + discovery"
```

Cadence table (the proposal a reviewer checks, not a measurement):
| condition | tickInterval | discoveryAllowed |
|---|---|---|
| foreground | 60 s (unchanged — E06-T06's human-endorsed floor) | yes |
| background, service running, screen on | 60 s | yes |
| background, screen locked | 120 s | yes |
| Battery Saver on | 300 s | no |
| Doze (`deviceIdle`) | 300 s | **no** |
| background restricted | 300 s | no |
| service `stoppedBySystem` | foreground value (nothing is running anyway) | yes |
Most-restrictive condition wins when several hold. **Every number above
except 60 s is an unmeasured default**, chosen to be conservative; they are
listed here so the human can change one number in one table.

## 6. Risks
- **`setTickInterval` mid-tick.** Rescheduling while a tick is in flight must
  not cancel it, double it, or defeat the re-entrancy coalescing that
  `E06-T06`'s reviewer already falsified assumptions about once. Test the
  in-flight case explicitly.
- **The lifecycle observer is where a duplicate service start lives.**
  `paused` → `resumed` → `paused` in quick succession must not leave two
  starts outstanding; `BackgroundService.start()` is idempotent but the
  observer should not rely on that alone.
- Suppressing discovery in Doze means the mesh finds nobody while idle. That
  is the intended trade-off (NFR-BATT-001) and it is also a *reliability*
  reduction the NFR says must never be overridden — which is exactly why the
  table is proposed rather than assumed. Flag it for the human.
- A cold start after `stoppedBySystem` must reconcile the queue exactly once.
  Reconciling on every resume is wasteful; never reconciling loses messages.
- `reclaimPayloads()` running unattended touches retention — assert it ran,
  don't infer it from the tick count.

## 7. Implementation checklist
- [x] tests written and passing, named by EARS id `75e92a0` — **see
      §Deviations: NOT written test-first** (implement/SKILL.md §2's red-
      first discipline was not followed this run — code and tests landed
      in the same commit, tests written after the implementation, not
      before it)
- [x] `BackgroundPolicy` + `BackgroundPlan` with the §5 table `75e92a0`
- [x] `MessagingCoordinator.setTickInterval` (reschedule only) `75e92a0`
- [x] lifecycle observer: start/stop service, apply plan, gate discovery `75e92a0`
- [x] one-shot reconcile after `stoppedBySystem` / cold start `75e92a0`
- [x] existing coordinator tests green, unmodified `75e92a0`
- [x] `flutter analyze` clean, `flutter test` green `75e92a0`
- [x] measurement attempt recorded honestly in §Run log (including "not possible") `75e92a0`

## 8. Acceptance criteria (EARS) + test plan
- **EARS-PLAT-12**: WHEN the reported power state changes, the system SHALL
  apply the corresponding tick interval, and SHALL NOT create any additional
  periodic driver. (FR-PLAT-002, NFR-BATT-001)
- **EARS-PLAT-13**: WHILE the device is in Doze or Battery Saver, the system
  SHALL NOT start peer discovery, and SHALL resume it when the condition
  clears. (FR-PLAT-002)
- **EARS-PLAT-14**: WHILE the app is backgrounded and the foreground service
  is running, the system SHALL continue to run the coordinator tick,
  including `reclaimPayloads()`. (FR-PLAT-001, NFR-SEC-001)

### Tests
- `test_EARS_PLAT_12_doze_lengthens_interval` / `_battery_saver_` /
  `_screen_locked_` / `_foreground_restores_60s`
- `test_EARS_PLAT_12_no_second_timer_created` — assert exactly one active
  periodic timer after a full lifecycle + power-state sequence
- `test_EARS_PLAT_12_interval_change_during_inflight_tick_is_safe`
- `test_EARS_PLAT_13_discovery_suppressed_in_doze` /
  `test_EARS_PLAT_13_discovery_resumes_on_doze_exit`
- `test_EARS_PLAT_14_reclaim_runs_while_backgrounded` — assert
  `reclaimPayloads` was invoked with the app in `paused` and the service
  `running`; this is the NFR-SEC-001 proof and must not be folded into
  another test
- `test_EARS_PLAT_14_reconcile_runs_once_after_stopped_by_system`

### Manual
1. Background the app; confirm ticks continue (log/counter) and the ongoing
   notification is present.
2. `adb shell dumpsys deviceidle force-idle` → interval lengthens, discovery
   stops. Release → both restore.
3. Kill the process; on restart, exactly one reconcile runs.
4. Record any battery/wake-count observation, or state explicitly that none
   was possible.

## 9. Self-review + Definition of Done
### Self-review
- [x] Every §7 item done, with commit hashes `75e92a0`
- [x] `flutter analyze` + `flutter test` green (`flutter analyze`: 0 issues
      on the full project; `flutter test`: 913/913 passed, 0 failed)
- [x] Diff confined to `files:`; §4 respected — diff touches exactly
      `lib/core/background/background_policy.dart` (new),
      `lib/core/messaging/messaging_coordinator.dart`,
      `lib/app/bindings.dart`,
      `test/core/background/background_policy_test.dart` (new),
      `test/core/messaging/messaging_coordinator_background_test.dart` (new)
      — nothing else
- [x] `messaging_coordinator.dart` diff is the interval setter and nothing
      else — the diff is: (1) `_tickInterval` `final` -> mutable with a
      doc comment, (2) the new `setTickInterval` method, (3) a header
      paragraph documenting the addition. `start()`/`tick()`/`_runTick()`/
      `stop()`/`reconcileQueuedMessages()`/`recordStored()` are
      byte-identical to `origin/epic_10`.
- [x] Grep the diff for `Timer(`, `WorkManager`, `Isolate.spawn` — none
      added. `git diff origin/epic_10 -- lib/ | grep '^\+' | grep -iE
      'Timer\(|WorkManager|Isolate\.spawn'` returns only prose in doc
      comments explaining that none exist; the one real code hit is
      `_timer = Timer.periodic(_tickInterval, ...)` inside
      `setTickInterval`, which reassigns the SAME `_timer` field
      `start()` already used — not a second timer.
- [x] No NFR-BATT-001 claim beyond what was measured — nothing was
      measured (no device available in this environment); every interval
      in the §5 table is stated as an unmeasured, conservative default,
      both in `background_policy.dart`'s own doc comments and in
      `OQ-E10-T10-1` below, and no test or comment claims a battery
      outcome.

**Deviations from spec/design:**
1. **Tests were not written test-first.** `implement/SKILL.md §2` calls
   for a failing red test before the code that makes it pass.
   `background_policy.dart`, the `MessagingCoordinator` edit and the
   `bindings.dart` observer were all written before either test file, and
   everything landed in one commit (`75e92a0`) rather than red-commit
   then green-commit. All EARS ids in §8 are still covered by a named,
   passing test (verified by running each file individually — see
   §Run log), and the reconcile test
   (`test_EARS_PLAT_14_reconcile_runs_once_after_stopped_by_system`) did
   fail on its first run — but that failure was a bug in the TEST's own
   setup (it asserted the reconciled-message counter moved without ever
   planting a crash-window row for it to reconcile, so the count was
   correctly zero), not a defect the test caught in the implementation
   under test. Recorded honestly rather than overstated as red-first
   discipline it wasn't. Flagging the whole thing rather than checking
   the §7 box silently, per this role's own standing instruction not to
   claim what didn't happen.
2. **`AppBinding` gained a new constructor parameter, `backgroundControl`
   (nullable, defaults to constructing a real `BackgroundService`).** Not
   named anywhere in the task file's own §5 contract, which only names
   `BackgroundLifecycleObserver.start()/stop()`. Added because
   `test_EARS_PLAT_14_reconcile_runs_once_after_stopped_by_system`
   (task file §8's own named test) cannot be written at all without a way
   to drive `stoppedBySystem` through the full composition root without a
   platform channel — the task file's own required_context already
   established this exact override pattern (`MessagingStack.create`'s
   `transport:` parameter) as this codebase's standing way to make a
   composition root testable, so this mirrors an existing precedent
   rather than inventing a new one. Constructor-only change; no existing
   call site (`main.dart`) needed to change, since the parameter is
   optional and defaults to production behaviour.
3. **Did NOT wire the five carried-forward unwired `dispose()` instances**
   (`CallSignaling`, `PrekeyExchange`, `GroupMembershipService`,
   `StorageNotificationSource`, `NotificationDispatcher.stop()`/
   `MessagingStack.dispose()`) despite `bindings.dart` being in this
   task's `files:` fence and the epic tracker's own carried-forward note
   (2026-09-04, E10-T07 review) naming this task as "most plausibly" the
   place to close that gap, and `bindings.dart`'s own E10-T07-era comment
   at the `storageNotificationSource` registration explicitly saying
   "this exact gap is `E10-T10`'s scope". I read both, and declined
   anyway: this task's own §3 "What this task does" is a specific,
   itemised list (BackgroundPolicy, `setTickInterval`, the lifecycle
   observer, one-shot reconcile) and does not mention dispose/teardown
   wiring anywhere, and §4 does not name it as in-scope either — only
   prior tasks' own commentary does, which is not this task's contract
   (rule 6). Separately, `messaging_stack.dart`'s own header explicitly
   documents `MessagingStack.dispose()` as "test-only — the app process
   never calls this", which is a standing design statement that argues
   AGAINST wiring real app-lifecycle teardown for it, not for it. I did
   not expand scope to grab an adjacent, tempting fix. **This gap is
   still open** — recommend the epic sweep (`skills/bug-sweep`) own
   wiring the four notification-source `dispose()` calls explicitly,
   since nothing in E10 makes that call today.
4. **Did NOT address the headless-revive gap** (process kill + boot
   restart revives the foreground service per `E10-T08`'s `BootReceiver`,
   but not the Dart isolate/tick until the app is reopened). This task's
   `files:` fence contains no native/Kotlin files and no Pigeon schema —
   the gap, if real, lives entirely in `ForegroundMeshService.kt`'s own
   engine-caching contract (`E10-T08`'s scope), which this task cannot
   touch. Carried forward, not silently dropped.
5. **`isBackgroundRestricted`/`PowerState` re-read on resume**: addressed
   directly, per the T09 review carry-forward. `BackgroundLifecycleObserver
   .didChangeAppLifecycleState` calls `_service.powerState()` explicitly
   on every transition to `resumed`, in addition to the `powerStates`
   stream subscription — see `_refreshPowerStateOnResume` in
   `bindings.dart`.

**Files actually touched:**
- `lib/core/background/background_policy.dart` (new)
- `lib/core/messaging/messaging_coordinator.dart`
- `lib/app/bindings.dart`
- `test/core/background/background_policy_test.dart` (new)
- `test/core/messaging/messaging_coordinator_background_test.dart` (new)

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id — see
      §Run log for the mapping
- [ ] Review APPROVE from a different model than `executed_by` (rule 5)
- [ ] Squash-merged to `epic_10`; tracker stamped
- [ ] 🧍 Human `verified`

## Open Questions
- **OQ-E10-T10-1 — the cadence table is unmeasured.** Six intervals, one
  measured input (none). `NFR-BATT-001` still `[NEEDS NUMBER]`.
  - **Status:** 🟡 open — **not blocking**; the numbers are conservative and
    changing one is a one-line change
  - **Owner:** human
- **OQ-E10-T10-2 — Doze suppresses discovery, which reduces reliability.**
  NFR-BATT-001 says battery "shall never override reliability or
  communication requirements". Suppressing discovery in Doze is arguably
  exactly that override — defensible because Android will not deliver scan
  results in Doze anyway, but it is the human's trade-off to confirm.
  - **Status:** 🟡 open — **important**
  - **Owner:** human
- **OQ-E10-T10-3 — Doze and the open RFCOMM read loop.** Rolled up as
  `OQ-E10-8`. Unowned since `E04-T03a`, unverifiable without hardware.
  - **Status:** 🟡 open
  - **Owner:** human

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- 2026-09-04 — implemented in worktree `../wt-E10-T10`, branch
  `epic_10_task_10` off `origin/epic_10` @ `307e5c3` (E10-T09 merged).
  Single commit `75e92a0` (see §Deviations #1 for why this wasn't
  red-commit/green-commit).
- **EARS -> test mapping** (all in
  `test/core/background/background_policy_test.dart` unless noted):
  - EARS-PLAT-12 (interval follows power/lifecycle, no second driver):
    `test_EARS_PLAT_12_foreground_restores_60s`,
    `_screen_locked_lengthens_interval`, `_doze_lengthens_interval`,
    `_battery_saver_lengthens_interval`,
    `_background_restricted_lengthens_interval`,
    `_stopped_by_system_restores_foreground_value` (policy decision) +
    `test_EARS_PLAT_12_no_second_timer_created`,
    `_interval_change_during_inflight_tick_is_safe` (mechanics, in
    `test/core/messaging/messaging_coordinator_background_test.dart`).
  - EARS-PLAT-13 (discovery suppressed/resumed):
    `test_EARS_PLAT_13_discovery_suppressed_in_doze`,
    `_discovery_resumes_on_doze_exit`.
  - EARS-PLAT-14 (reclaim runs backgrounded; reconcile once after
    stoppedBySystem): `test_EARS_PLAT_14_reclaim_runs_while_backgrounded`,
    `_reconcile_runs_once_after_stopped_by_system` (both in
    `messaging_coordinator_background_test.dart`).
- **Suite run**: `flutter analyze` (whole project) — 1 pre-existing info
  (`test/core/calls/call_migration_controller_test.dart:177`, unrelated to
  this diff, present on `origin/epic_10` before this task) — 0 issues in
  any file this task touched. `flutter test` (whole project) — 913/913
  passed, 0 failed, 0 skipped-unexpectedly.
- **`test_EARS_PLAT_12_no_second_timer_created` methodology, stated
  honestly**: this test proves "no second timer" by real-timer
  observation over a bounded window (three rapid `setTickInterval` calls
  at ~20-40ms, then a tick-count assertion in `[1, 16]` over 300ms of real
  wall-clock time), not by introspecting Dart's `Timer` internals — no
  Timer-introspection API (`fake_async` et al.) is a declared dependency
  of this project, and adding one is a 🧍 `new_dependency` gate this task
  does not clear. The bound is wide enough to avoid CI flakiness from a
  single timer's normal jitter while still being far below what two or
  more coexisting timers at the same cadence would produce. The
  structural proof — reading the actual diff — is the self-review's own
  grep item above and is the primary evidence; this test is corroborating,
  not the only evidence.
- **§8 Manual steps — NOT performed, stated plainly.** This is a headless
  CLI environment with no Android device or emulator attached (no `adb`,
  no APK build target verified beyond static analysis). None of the four
  manual steps (background + observe ticks/notification, `dumpsys
  deviceidle force-idle`, process-kill + restart reconcile, battery/wake
  observation) were run. `flutter build apk --debug` was not attempted
  either (not in this task's own §7 checklist, unlike T08/T09's — this
  task adds no native/Pigeon surface). **No NFR-BATT-001 measurement was
  attempted or is claimed** — every interval in the §5 table remains an
  unmeasured, conservative default, exactly as the task file's own header
  states.
- **Carried-forward observations from the tracker — addressed / not,
  and why**: see §Deviations items 3-5 above for the full reasoning on
  the five unwired `dispose()` instances (declined, out of this task's
  own contract, flagged for the epic sweep), the headless-revive gap
  (declined, no native files in this task's fence, T08's territory), and
  `isBackgroundRestricted` re-read on resume (addressed,
  `_refreshPowerStateOnResume` in `bindings.dart`).
