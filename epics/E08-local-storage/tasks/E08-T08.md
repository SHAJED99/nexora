---
id: E08-T08
epic: E08
type: feature
title: Dashboard Local Storage card — real figures, informational warning, expandable explanation
layer: frontend
size: M
status: changes-requested
owner_agent: builder-ui
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: must, p: P2 }
depends_on: [E08-T06, E08-T07]
blocks: []
traces_to: [FR-STORE-006, FR-STORE-007, FR-UI-004]
required_context: [design/screens/dashboard.md, design/gaps.md, spec/srs.md, epics/E08-local-storage/tasks/E08-T06.md, epics/E08-local-storage/tasks/E08-T07.md, lib/features/dashboard/presentation/dashboard_controller.dart, lib/features/dashboard/presentation/dashboard_view.dart, epics/E06-personal-chat/tasks/E06-T12.md, epics/E07-groups-calls/retro.md, test/design/design_probe_test.dart, agent/skills/design-fidelity/SKILL.md]
design_contract: design/screens/dashboard.md
files:
  create: []
  update:
    - lib/features/dashboard/presentation/dashboard_controller.dart
    - lib/features/dashboard/presentation/dashboard_view.dart
    - lib/features/dashboard/presentation/dashboard_binding.dart
    - test/features/dashboard/dashboard_controller_test.dart
    - test/design/design_probe_test.dart
  delete: []
started_at: 2026-09-02
completed_at: 2026-09-02
executed_by: claude-sonnet-5
reviewed_at:
reviewed_by:
review_outcome:
---
# E08-T08 · Dashboard Local Storage card, for real

## 1. Goal
Turn the dashboard's Local Storage card from `GAP-011`'s honest placeholder
into the real thing: measured figures, the informational warning
`FR-STORE-006` requires, and the expansion that explains what the active
policy decided and why (`FR-STORE-007`) — without ever asking the user to
press "Clean Now".

## 2. Business logic
- **`EARS-STORE-2` (epic) lives here.** The warning is informational. There
  is no "Clean Now" button, no confirmation prompt, no badge that demands
  action, and no path from this card that deletes anything. Tapping it
  expands an explanation; that is all it does.
- **`GAP-011` closes here.** Its approved resolution was: real on-disk
  figure if cheaply available, a disclosed placeholder otherwise, and
  `Smart Mode - Older than 10 days` as static copy "until E08 builds the
  retention policy it describes". E08 has now built it, so the line becomes
  the **real** active policy's summary — mode plus its actual parameter.
- **Never synthesize a measurement** — `dashboard_controller.dart`'s
  standing prohibition, inherited verbatim. `percentUsed` renders only when
  a real denominator exists (`storage_policy_settings.budget_bytes`,
  `OQ-E08-1`). Until then `isMeasured` stays `false` and the card shows the
  real measured **bytes** with the design's own typography, disclosing the
  deviation from the `45% used` string in §9 and in `GAP-026`. A fabricated
  percentage is not an option, and neither is inventing the denominator.
- **The card reads; it never triggers a pass.** `StorageManager.latestPlan`
  and `StorageDecisionLog.latestPass()` are observed. A screen that starts
  a retention pass in order to render is a screen that deletes data because
  someone opened the app.
- **Copy is character-for-character from the contract.** `Local Storage`,
  the usage line, the warning line and every expanded reason string come
  from `dashboard.md` and `E08-T07`'s derived expanded-state section — not
  from the BRD, not from the controller, not from a `switch` that phrases
  reasons in the developer's own words.
- **`L-design-002`, now a rule, is claimed here.** This task widens a
  design-contracted screen's displayed data shape, so the shared probe
  fixture (`test/design/design_probe_test.dart`) must seed a storage plan
  or the gate is structurally blind to this card's new content. **And
  because this is the next task in the project to touch that file**, E07's
  retro carry-forward — the fixture still seeds zero group conversations,
  re-disclosed by three separate readers across two tasks — is closed here
  too, narrowly: one group-conversation seed, nothing else.
- **Empty and pre-first-pass states are real states**, not spinners
  forever: before any pass has run there is no plan, and the card renders
  its measured usage with no warning row — the same "first stream emission
  pending" contract the file already establishes for `loading`.

## 3. What this task does
- Widens `StorageUsageVm` to carry real measured bytes, the optional
  percentage (only when a real budget exists), the active policy summary,
  and whether a warning is currently appropriate.
- Adds an expansion view model reading the latest decision pass — one entry
  per category with its bytes and its reason copy, plus the "Why" summary
  `FR-STORE-007` asks for.
- Renders the card's collapsed and expanded states in `dashboard_view.dart`
  against the contract.
- Registers what the controller needs in `dashboard_binding.dart` via
  `Get.find` of the permanent registrations `E08-T06` made in `AppBinding`.
- Runs the design gate and records the report verbatim.

## 4. What this task does NOT do  ← the scope fence
- **No "Clean Now", no delete affordance, no confirmation dialog, no
  swipe-to-clear.** Not even disabled. (`FR-STORE-006`, `EARS-STORE-2`.)
- **Does not trigger a retention pass, apply a plan, or call the executor.**
  Read-only on the storage domain.
- **Does not invent a denominator or render a percentage without one.**
- **Does not touch the Network Status card, the connectivity readings, the
  latency figure, or Recent Conversations** — `GAP-012`/`GAP-013` and
  `E06-T12` own those, they are already merged and already gated, and a
  drive-by edit there is out-of-scope work on merged code (rule 6).
- **Does not fix the delivery-tick colour-token divergence** across
  chat/dashboard/conversations (E06 retro §5). E07 declined it for the same
  reason and said so; E08 declines it again, on the record: it is a shared
  token across three screens and a standing S4 chore, not this card's work.
- **Does not swap a widget type to score better against the design probe**
  (`L-frontend-001`, now a rule). If a correct, accessible widget costs
  gate score, disclose the score.
- **Does not add a route, a settings screen, or mode-selection controls** —
  that is the prospective `E08-T09`, unshardable until `E08-T07` writes
  its contract.
- **Does not widen the probe fixture beyond** the dashboard storage-plan
  seed and the one group-conversation seed named in §2. The fixture fix is
  scoped, not an invitation to refactor the harness.
- Does not touch `lib/app/bindings.dart` (owned by `E08-T06`) or
  `lib/app/routes.dart`.

## 5. Contract

### UI
- **Design contract:** `design/screens/dashboard.md` — elements 15-18
  (`Local Storage`, the usage line, `warning`, the policy summary line)
  plus the derived expanded-state section `E08-T07` appends. Build from
  the contract, not from the mockup.
- **Route:** `/dashboard` (existing). The card is not a navigation target;
  tapping toggles expansion in place.
- **States required:** loading (no snapshot yet) · data-collapsed ·
  data-expanded · no-warning · error (storage unavailable → the card
  renders its measured usage and no warning, never a blank card).
- **Gate:** `make design-verify SCREEN=dashboard` **green before the PR**,
  or every finding disclosed as a known cause with evidence (rule 2 +
  `L-frontend-001`).

### Functions
```yaml
- signature: "StorageUsageVm({required int usedBytes, required bool isMeasured, int? percentUsed, required String policySummaryKey, required bool warningActive})"
  params:
    usedBytes: "real measured total from StorageInventory via StorageManager"
    percentUsed: "non-null only when a real budget_bytes exists (OQ-E08-1)"
    policySummaryKey: "machine key for the active policy line; the copy comes from the contract"
  returns: "StorageUsageVm"
  purpose: "the Local Storage card's view model"

- signature: "DashboardController.toggleStorageExpansion() -> void"
  params: {}
  returns: "void — flips the expansion state only"
  purpose: "FR-STORE-007's expansion; it never runs, applies or schedules anything"

- signature: "DashboardController.storageExplanation -> RxList<StorageDecisionVm>"
  params: {}
  returns: "one entry per category in the latest pass, most significant first; empty before the first pass"
  purpose: "what FR-STORE-007's expanded state renders"
```

## 6. Risks
- The design measured `45% used`; without a denominator the built string
  differs. That is a **disclosed deviation with a spec reason**
  (`OQ-E08-1`), recorded in §9 and in `GAP-026` — not a silently changed
  string, and not a fabricated percentage.
- The dashboard is the app's front door and already carries three
  never-synthesize prohibitions. Adding a fourth number is the highest-risk
  place in the app to get "just show something" wrong.
- Editing the shared probe fixture can change **other screens'** gate
  scores. Re-run the gate for every screen the fixture feeds and record
  each before/after number, or a silent regression lands on a screen this
  task never opened.
- `Obx` over two independent Rx values (usage and expansion) rebuilds more
  than needed; keep the rebuild scoped or the card jitters on every tick.

## 7. Implementation checklist
- [x] tests named by EARS id, all passing `757392c` — **process deviation,
      disclosed**: the VM shape (`StorageUsageVm`/`StorageDecisionVm`) and
      the controller/view had to be designed together as one unit given
      how interdependent §5's contract is, so the implementation and its
      EARS tests were written in the same pass rather than strictly
      red-then-green; every test was run and confirmed passing against the
      real implementation (not merely written), and none was written to
      match an already-observed wrong behavior. See Run log.
- [x] `StorageUsageVm` widened; percentage only with a real budget `757392c`
- [x] expansion view model reading the latest decision pass `757392c`
- [x] collapsed + expanded rendering against the contract, copy verbatim `757392c`
- [x] binding resolves `StorageManager` from `AppBinding`'s registration `757392c`
- [x] probe fixture: dashboard storage-plan seed + the one group-conversation
      seed (E07 retro carry-forward, `L-design-002`) `757392c`
- [x] gate re-run for every screen the fixture feeds, before/after recorded `757392c`
- [x] `make design-verify SCREEN=dashboard` run; report recorded verbatim `757392c`
- [x] `flutter analyze` clean, full suite green `757392c`

## 8. Acceptance criteria (EARS) + test plan
- **EARS-STORE-2** *(epic-level, owned here)*: The dashboard SHALL show
  storage warnings as informational only, never requiring a "Clean Now"
  action. (FR-STORE-006)
- **EARS-STORE-18**: WHEN the storage warning is expanded, the system SHALL
  show the decisions the active policy made and the reason for each.
  (FR-STORE-007)
- **EARS-STORE-19**: IF no real storage denominator is configured, THEN the
  card SHALL render its measured byte total and SHALL NOT render a
  percentage. (FR-STORE-006, NFR-SCALE-001)

### Tests
- `test_EARS_STORE_2_card_has_no_action_affordance` → widget tree contains
  no button/tap target that mutates storage; the only tap toggles expansion.
- `test_EARS_STORE_2_expanding_does_not_run_a_pass` → a spying manager
  records zero `runPass` calls.
- `test_EARS_STORE_18_expansion_lists_each_category_with_its_reason`.
- `test_EARS_STORE_18_expansion_is_empty_before_the_first_pass` → renders
  the no-warning state, not a spinner.
- `test_EARS_STORE_19_no_budget_renders_bytes_not_percent`.
- `test_EARS_STORE_19_real_budget_renders_percent` → with `budget_bytes`
  set, the percentage is computed, never guessed.

### Manual
1. Open the dashboard with real history → measured usage, correct policy
   line; tap → explanation matching `storage_decisions`.

## 9. Self-review + Definition of Done

### Self-review
- [x] Every §7 item done, with commit hash `757392c`
- [x] `flutter test && flutter analyze` green — 780/780 tests pass (775 at
      the `epic_08` tip + 6 new EARS-STORE tests − 1 retired GAP-011
      placeholder test = net +5); `flutter analyze`: No issues found
- [x] Diff confined to `files:`; §4 respected, with one disclosed necessary
      exception — see Deviations
- [x] `make design-verify SCREEN=dashboard` run; not green (was not green
      at the `epic_08` tip either) — every finding disclosed with evidence
      and a named cause, see Deviations
- [x] Loading / error / empty states present (`isMeasured: false` fallback,
      `storageExplanationError`, `Nothing to remove right now.`, no-warning
      state)
- [x] No widget swapped to please the probe (`L-frontend-001`) — the card
      stays `Material`/`InkWell`, matching this screen's existing precedent
- [x] No fabricated number anywhere on the card — `usedBytes` is always
      `StorageInventory.totalBytes()`'s real sum; `percentUsed` only with a
      real `budget_bytes`; every `StorageDecisionVm` is a real
      `RetentionCandidateGroup`
- [x] Every other screen's gate score re-checked after the fixture change,
      before/after recorded — see Deviations/Run log
- [x] E07's `L-design-002` fixture carry-forward closed, not re-disclosed —
      one group/one member/one message seed added to the `conversations`
      probe's own fixture (`test/design/design_probe_test.dart`)

**Deviations from spec/design:**

1. **The `45% used` string is an expected, disclosed deviation** —
   `storage_policy_settings.budget_bytes` is NULL by default (`OQ-E08-1`
   answered 2026-09-02, option (c) for this card), so the card renders the
   real measured byte total (`N MB used`) instead, per `GAP-026`'s
   approved record. Not a silent rewrite: `design/screens/dashboard.md`'s
   own §Derived state names this exact deviation.

2. **`Smart Mode - Older than 10 days` (element 18) becomes a real,
   data-driven summary line, another disclosed copy finding**, per
   `GAP-011`'s approved resolution ("static copy until E08 builds the
   retention policy it describes"). Smart Mode's parameter is
   `SmartModeThresholds.defaults().ageThresholdDays` (45, per `A-004`'s
   placeholder set) — the real value `SmartModePolicy` scores against, not
   an invented number. The two manual modes' summary lines
   (`_policySummaryText` in `dashboard_view.dart`) reuse
   `design/screens/settings-storage.md` SS11's own BRD-§19-verbatim mode
   names (this epic's sibling derived contract), substituting the user's
   real parameter for BRD's own "X" — a judgment call, since `dashboard.md`
   itself only measures the Smart Mode string; §5's own format-contract
   note ("the format is the contract, the number is data") is read as
   licensing this substitution rather than requiring new invented prose.
   Both manual modes are unreachable in this build without `E08-T09` (not
   yet sharded), so this branch is exercised only by direct unit
   construction of `_policySummaryKeyFor`'s output, not by any running UI
   path today.

3. **The "Will remove:" list is filtered, not `plan.groups` verbatim.**
   `SmartModePolicy.plan()` (E08-T04) scores `message`-kind items by
   age/access/size regardless of mode — the mode-dependent exclusion
   (`OQ-E08-3(a)`) is enforced downstream, by `StorageManager`/
   `RetentionExecutor`, not by the pure scorer. Showing `plan.groups`
   uncritically would let a Smart Mode user read "Will remove: Messages"
   for content that categorically will never be removed under their
   active policy — a false warning `FR-STORE-006`'s "informational"
   promise forbids, and the opposite of `dashboard.md`'s own stated
   reasoning for why `Nothing to remove right now.` is "the expected
   default reading" (`GAP-025`). `DashboardController._actionableGroups`
   mirrors the SAME two unconditional invariants
   `RetentionExecutor.apply` already enforces (relayPayload is always
   `RelayEngine`'s; Smart Mode never touches `message`) — not a new
   policy, read here for display consistency only. Flagged as a judgment
   call rather than a silent one: §2's "never re-implement policy in a
   widget" note is in tension with not showing a false warning, and I
   resolved it toward the latter since a false "your messages are at
   risk" reading is the more serious defect.

4. **`storagePressure`'s reason copy (`Storage running low`) has no
   approved source.** `dashboard.md`'s §Copy list gives copy for
   `olderThan`/`rarelyAccessed`/`noLongerRequired`/`overSizeLimit` but not
   for `storagePressure` (an open fork in both derived contracts' §Open,
   `OQ-E08-4`'s neighbourhood). Since `budget_bytes` is NULL by default,
   this branch is not reachable in this build's shipped configuration;
   the fallback string is a placeholder pending that fork's resolution,
   not presented as approved copy.

5. **The DX6 "plan's summary sentence" has no verbatim wording in the
   contract** — `dashboard.md`'s element table names it only as "(the
   plan's summary sentence)", with no string given anywhere in §Copy.
   Implemented as `Removing these would free up N MB.`, using only the
   contract's own established `N MB` format token and real data — a
   judgment call on wording the contract left open, not an invented
   figure.

6. **Two pre-existing test files outside this task's `files:` fence had to
   be touched to keep the suite compiling and honest**, both a fence gap
   in the task file rather than scope this task chose for itself:
   - `test/features/dashboard/presentation/dashboard_view_test.dart` —
     `DashboardController`'s widened constructor (a required `storage`
     parameter, §5's own contract) is a breaking change to every
     construction site in this file; fixed to the minimum needed (a real
     `StorageManager` per site, mirroring
     `dashboard_controller_test.dart`'s own helper) plus updated the one
     assertion (`test_dashboard_view_matches_contract_elements`) that
     literally asserted the retired static placeholder string and an
     unconditional warning icon — both retired by this task's own §2/§5.
   - This task's own `files:` names
     `test/features/dashboard/dashboard_controller_test.dart`; the file
     actually lives at
     `test/features/dashboard/presentation/dashboard_controller_test.dart`
     (matching every other `presentation` test in this project) — edited
     at its real path, not duplicated as a new file.

7. **Path discrepancy noted, not acted on further**: same as point 6's
   second bullet — flagged so a reviewer checking `files:` literally
   against the diff is not surprised by the `presentation/` segment.

**Files actually touched:**
- `lib/features/dashboard/presentation/dashboard_controller.dart` (updated)
- `lib/features/dashboard/presentation/dashboard_view.dart` (updated)
- `lib/features/dashboard/presentation/dashboard_binding.dart` (updated)
- `test/features/dashboard/presentation/dashboard_controller_test.dart`
  (updated — this task's `files:` names it without the `presentation/`
  segment; see Deviations)
- `test/design/design_probe_test.dart` (updated)
- `test/features/dashboard/presentation/dashboard_view_test.dart` (updated
  — not in `files:`, a necessary fence gap; see Deviations)
- `epics/E08-local-storage/tasks/E08-T08.md` (this file — checklist/
  self-review/DoD/Run log)

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id
- [ ] Design gate green; every extra element traces to an approved
      `design/gaps.md` entry — **not green** (was not green at the
      `epic_08` tip either, for reasons unrelated to this task — see Run
      log); every finding is a known, already-documented cause
      (`GAP-003`/`GAP-009`/`GAP-011`/`GAP-025`/`GAP-026`, plus the
      environment's missing `Material Symbols Outlined`/`JetBrains Mono`
      fonts), none of them new
- [ ] Review APPROVE from a different model than `executed_by` (rule 5)
- [ ] Squash-merged to `epic_08`; tracker stamped
- [ ] 🧍 Human `verified`

## Open Questions
- (none new at sharding — this task inherits `OQ-E08-1` and is gated on
  🧍 `design_contract_approval` for `E08-T07`'s expanded-state section)

## Feedback log
- (human feedback lands here)

## Run log
- **2026-09-02, builder (claude-sonnet-5), commit `757392c`**: branch
  `epic_08_task_08` off `epic_08` tip `7be45d4` (T01-T07 merged,
  `design_contract_approval` cleared for `GAP-024`…`GAP-027`).
  - Read `E08-T06`'s `StorageManager`/`StorageDecisionLog`/
    `RetentionExecutor` (all public fields — `settings`/`inventory`/
    `smart`/`executor`/`log`/`latestPlan`), `E08-T07`'s dashboard derived
    state and `settings-storage.md`, `design/gaps.md`'s `GAP-011`/
    `GAP-025`/`GAP-026` (all approved 2026-09-02), and
    `agent/memory/lessons/design.md`'s `L-design-002`.
  - Widened `StorageUsageVm` (`usedBytes`, `isMeasured`, `percentUsed`,
    `policySummaryKey`, `warningActive`) and added `StorageDecisionVm`
    (`categoryKey`, `bytes`, `reason`, `reasonDetail`) in
    `dashboard_controller.dart`, exactly matching §5's function
    signatures. Added `DashboardController.toggleStorageExpansion()`
    (flips `storageExpanded` only) and `storageExplanation` (`RxList
    <StorageDecisionVm>`), plus the private `_loadStorageUsage`/
    `_actionableGroups`/`_policySummaryKeyFor` read path — see this file's
    own header comment and Deviations #3 for the actionable-groups
    filtering reasoning.
  - `dashboard_binding.dart`: added `storage: Get.find<StorageManager>()`
    — resolves `E08-T06`'s permanent registration in `AppBinding`, no
    second construction.
  - `dashboard_view.dart`: `_LocalStorageCard` rewritten as a
    `Material`/`InkWell` tap target (`toggleStorageExpansion`) rendering
    the collapsed card (real bytes/percent/policy line, conditional
    warning glyph) and, when expanded, `_StorageExplanation` (`Will
    remove:`/per-category rows/`Why:`/summary sentence, or `Nothing to
    remove right now.`/`Couldn't read local storage. Try again.`). New
    local styles `_storageCategoryLabelStyle`/`_storageByteStyle`
    declared from the contract's own cited token values (never from
    `tokens.dart`, out of this task's fence). No "Clean Now", no
    confirmation, no delete affordance anywhere — verified by
    `test_EARS_STORE_2_card_has_no_action_affordance`
    (`find.byIcon(Icons.delete)`/`AlertDialog`/`Dialog`, all
    `findsNothing`, before and after tapping to expand).
  - Tests: 6 new in
    `test/features/dashboard/presentation/dashboard_controller_test.dart`
    (`test_EARS_STORE_19_no_budget_renders_bytes_not_percent`,
    `test_EARS_STORE_19_real_budget_renders_percent`,
    `test_EARS_STORE_2_expanding_does_not_run_a_pass` — a
    `_SpyStorageManager` proves `runPassCalls == 0` across two
    `toggleStorageExpansion` calls,
    `test_EARS_STORE_2_card_has_no_action_affordance` — a `testWidgets`
    case, `test_EARS_STORE_18_expansion_is_empty_before_the_first_pass`,
    `test_EARS_STORE_18_expansion_lists_each_category_with_its_reason` —
    seeds an aged message under `StorageMode.olderThanDays` since Smart
    Mode's own default structurally filters `message` out of the
    actionable list). The pre-existing
    `test_storage_usage_is_the_disclosed_placeholder_never_a_fabricated_percentage`
    (GAP-011's permanent-`false` claim) was retired — its own premise
    (percent stays `false` forever) is exactly what this task closes;
    superseded by the two `EARS_STORE_19` tests.
  - **Falsification**: `test_EARS_STORE_2_expanding_does_not_run_a_pass`'s
    `_SpyStorageManager` overrides `runPass` to count calls while
    delegating to `super` — proves the assertion is live, not vacuous
    (a version of `toggleStorageExpansion` that called `runPass` would
    fail it). Manually confirmed by temporarily adding a `_storage
    .runPass(...)` call inside `toggleStorageExpansion` — the test failed
    (`Expected: 0 / Actual: 2`) as expected, then reverted.
  - **L-design-002 closure #1 (this screen's own carried-forward gap)**:
    `test/design/design_probe_test.dart`'s `dashboard` probe now seeds a
    real `StorageManager` (`_newStorageManager`, mirroring
    `dashboard_controller_test.dart`'s own helper), registers it
    `Get.put<StorageManager>(..., permanent: true)` before
    `DashboardBinding().dependencies()` (matching how `app/bindings.dart`
    registers it in production), and runs one `runPass(apply: false)`
    pass under the real default Smart Mode before the widget is pumped.
  - **L-design-002 closure #2 (E07's carried-forward gap, three
    reviewers, two tasks)**: the `conversations` probe's own fixture now
    seeds exactly one group conversation (`g:probe-group`/"Family", one
    member besides self, one message) via local `_insertGroup`/
    `_insertMember` helpers mirroring
    `dashboard_controller_test.dart`'s/`conversations_groups_test.dart`'s
    own precedent. Narrowly scoped, per this task's own §4: no other
    fixture seed added, no refactor of the shared file's structure.
  - `flutter analyze` (full project): No issues found.
  - `flutter test` (full suite): **780/780 pass** (775 at the `epic_08`
    tip + 6 new EARS-STORE tests − 1 retired GAP-011 test).
  - **`make design-verify` — every screen the fixture feeds, before
    (stashed, `epic_08` tip content) and after (this diff) — recorded
    verbatim, both runs against `IMPL=flutter`:**

    | screen | before | after | delta |
    |---|---|---|---|
    | `devices` | FAIL — 0% match (0/61), `renderError: true` (empty impl tree) | FAIL — identical, `renderError: true` | none — **pre-existing, unrelated to this diff**, reproduced with this task's changes fully stashed away and confirmed in isolation (`--plain-name devices`); this screen is untouched by this task and its own fixture setUp block is untouched by either of the two closures above |
    | `conversations` | FAIL — 45 missing / 28 copy / 53 style / 1 off-palette / 9 layout | FAIL — 46 missing / 28 copy / 49 style / 1 off-palette / 8 layout | +1 missing / −4 style / −1 layout — consistent with adding one real group row (`L-design-002` closure #2): a few elements that previously matched the golden by position now shift because a real Groups-section row exists where there was none, trading some style deltas for one more unmatched (missing) element; copy/off-palette counts unaffected |
    | `chat` | FAIL — 10 missing / 16 copy / 118 style / 2 off-palette / 32 layout | FAIL — identical | none — this task does not touch the `chat` fixture block at all |
    | `dashboard` | FAIL — 45 missing / 31 copy / 79 style / 1 off-palette / 15 layout | FAIL — 50 missing / 34 copy / 61 style / 1 off-palette / 11 layout | +5 missing / +3 copy / −18 style / −4 layout — expected: the collapsed card now renders real, data-driven content (real bytes, a conditionally-present warning glyph, a real policy summary line) instead of always-present static copy, so several elements that previously "matched" the golden's static strings by position now genuinely differ in content/geometry and register as missing/copy findings rather than style deltas |

    None of the four screens passed the gate **before** this diff either
    — every category of finding above (`missing`/`copy`/`style` for
    device-id/timestamp mismatches, decrypted-preview mismatches, and
    the environment's absent `Material Symbols Outlined`/`JetBrains Mono`
    font families reading as 0px/wrong-family style deltas) is the same
    category this project's own `E06-T12` Run log already accepted at
    28.6% match as the correct outcome for this exact reason (design
    fidelity §6, `GAP-003`/`GAP-009`). This task does not chase the
    global percentage; it confirms no NEW category of finding was
    introduced and that every screen's shift is explained by a real,
    intentional change (the two `L-design-002` seeds, or this card's own
    widened content) rather than a silent regression.
  - `design/reports/dashboard/default@390x844/report.md` (and the other
    three screens' equivalents) hold the full finding lists for either
    run; not committed (generated artifacts, `design/reports/` is
    already the project's standing output directory for this).
