---
id: E08-T08
epic: E08
type: feature
title: Dashboard Local Storage card — real figures, informational warning, expandable explanation
layer: frontend
size: M
status: todo
owner_agent: builder-ui
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: must, p: P2 }
depends_on: [E08-T06, E08-T07]
blocks: []
traces_to: [FR-STORE-006, FR-STORE-007, FR-UI-004]
required_context: [design/screens/dashboard.md, design/gaps.md, spec/srs.md, epics/E08-local-storage/tasks/E08-T06.md, epics/E08-local-storage/tasks/E08-T07.md, lib/features/dashboard/presentation/dashboard_controller.dart, lib/features/dashboard/presentation/dashboard_view.dart, epics/E06-personal-chat/tasks/E06-T12.md, epics/E07-groups-calls/retro.md, test/design/design_probe_test.dart, agent/skills/design-fidelity/SKILL.md]
design_contract: design/screens/dashboard.md
files:
  create: []
  update:
    - lib/features/dashboard/presentation/dashboard_controller.dart
    - lib/features/dashboard/presentation/dashboard_view.dart
    - lib/features/dashboard/presentation/dashboard_binding.dart
    - test/features/dashboard/dashboard_controller_test.dart
    - test/design/design_probe_test.dart
  delete: []
started_at:
completed_at:
executed_by:
reviewed_at:
reviewed_by:
review_outcome:
---
# E08-T08 · Dashboard Local Storage card, for real

## 1. Goal
Turn the dashboard's Local Storage card from `GAP-011`'s honest placeholder
into the real thing: measured figures, the informational warning
`FR-STORE-006` requires, and the expansion that explains what the active
policy decided and why (`FR-STORE-007`) — without ever asking the user to
press "Clean Now".

## 2. Business logic
- **`EARS-STORE-2` (epic) lives here.** The warning is informational. There
  is no "Clean Now" button, no confirmation prompt, no badge that demands
  action, and no path from this card that deletes anything. Tapping it
  expands an explanation; that is all it does.
- **`GAP-011` closes here.** Its approved resolution was: real on-disk
  figure if cheaply available, a disclosed placeholder otherwise, and
  `Smart Mode - Older than 10 days` as static copy "until E08 builds the
  retention policy it describes". E08 has now built it, so the line becomes
  the **real** active policy's summary — mode plus its actual parameter.
- **Never synthesize a measurement** — `dashboard_controller.dart`'s
  standing prohibition, inherited verbatim. `percentUsed` renders only when
  a real denominator exists (`storage_policy_settings.budget_bytes`,
  `OQ-E08-1`). Until then `isMeasured` stays `false` and the card shows the
  real measured **bytes** with the design's own typography, disclosing the
  deviation from the `45% used` string in §9 and in `GAP-026`. A fabricated
  percentage is not an option, and neither is inventing the denominator.
- **The card reads; it never triggers a pass.** `StorageManager.latestPlan`
  and `StorageDecisionLog.latestPass()` are observed. A screen that starts
  a retention pass in order to render is a screen that deletes data because
  someone opened the app.
- **Copy is character-for-character from the contract.** `Local Storage`,
  the usage line, the warning line and every expanded reason string come
  from `dashboard.md` and `E08-T07`'s derived expanded-state section — not
  from the BRD, not from the controller, not from a `switch` that phrases
  reasons in the developer's own words.
- **`L-design-002`, now a rule, is claimed here.** This task widens a
  design-contracted screen's displayed data shape, so the shared probe
  fixture (`test/design/design_probe_test.dart`) must seed a storage plan
  or the gate is structurally blind to this card's new content. **And
  because this is the next task in the project to touch that file**, E07's
  retro carry-forward — the fixture still seeds zero group conversations,
  re-disclosed by three separate readers across two tasks — is closed here
  too, narrowly: one group-conversation seed, nothing else.
- **Empty and pre-first-pass states are real states**, not spinners
  forever: before any pass has run there is no plan, and the card renders
  its measured usage with no warning row — the same "first stream emission
  pending" contract the file already establishes for `loading`.

## 3. What this task does
- Widens `StorageUsageVm` to carry real measured bytes, the optional
  percentage (only when a real budget exists), the active policy summary,
  and whether a warning is currently appropriate.
- Adds an expansion view model reading the latest decision pass — one entry
  per category with its bytes and its reason copy, plus the "Why" summary
  `FR-STORE-007` asks for.
- Renders the card's collapsed and expanded states in `dashboard_view.dart`
  against the contract.
- Registers what the controller needs in `dashboard_binding.dart` via
  `Get.find` of the permanent registrations `E08-T06` made in `AppBinding`.
- Runs the design gate and records the report verbatim.

## 4. What this task does NOT do  ← the scope fence
- **No "Clean Now", no delete affordance, no confirmation dialog, no
  swipe-to-clear.** Not even disabled. (`FR-STORE-006`, `EARS-STORE-2`.)
- **Does not trigger a retention pass, apply a plan, or call the executor.**
  Read-only on the storage domain.
- **Does not invent a denominator or render a percentage without one.**
- **Does not touch the Network Status card, the connectivity readings, the
  latency figure, or Recent Conversations** — `GAP-012`/`GAP-013` and
  `E06-T12` own those, they are already merged and already gated, and a
  drive-by edit there is out-of-scope work on merged code (rule 6).
- **Does not fix the delivery-tick colour-token divergence** across
  chat/dashboard/conversations (E06 retro §5). E07 declined it for the same
  reason and said so; E08 declines it again, on the record: it is a shared
  token across three screens and a standing S4 chore, not this card's work.
- **Does not swap a widget type to score better against the design probe**
  (`L-frontend-001`, now a rule). If a correct, accessible widget costs
  gate score, disclose the score.
- **Does not add a route, a settings screen, or mode-selection controls** —
  that is the prospective `E08-T09`, unshardable until `E08-T07` writes
  its contract.
- **Does not widen the probe fixture beyond** the dashboard storage-plan
  seed and the one group-conversation seed named in §2. The fixture fix is
  scoped, not an invitation to refactor the harness.
- Does not touch `lib/app/bindings.dart` (owned by `E08-T06`) or
  `lib/app/routes.dart`.

## 5. Contract

### UI
- **Design contract:** `design/screens/dashboard.md` — elements 15-18
  (`Local Storage`, the usage line, `warning`, the policy summary line)
  plus the derived expanded-state section `E08-T07` appends. Build from
  the contract, not from the mockup.
- **Route:** `/dashboard` (existing). The card is not a navigation target;
  tapping toggles expansion in place.
- **States required:** loading (no snapshot yet) · data-collapsed ·
  data-expanded · no-warning · error (storage unavailable → the card
  renders its measured usage and no warning, never a blank card).
- **Gate:** `make design-verify SCREEN=dashboard` **green before the PR**,
  or every finding disclosed as a known cause with evidence (rule 2 +
  `L-frontend-001`).

### Functions
```yaml
- signature: "StorageUsageVm({required int usedBytes, required bool isMeasured, int? percentUsed, required String policySummaryKey, required bool warningActive})"
  params:
    usedBytes: "real measured total from StorageInventory via StorageManager"
    percentUsed: "non-null only when a real budget_bytes exists (OQ-E08-1)"
    policySummaryKey: "machine key for the active policy line; the copy comes from the contract"
  returns: "StorageUsageVm"
  purpose: "the Local Storage card's view model"

- signature: "DashboardController.toggleStorageExpansion() -> void"
  params: {}
  returns: "void — flips the expansion state only"
  purpose: "FR-STORE-007's expansion; it never runs, applies or schedules anything"

- signature: "DashboardController.storageExplanation -> RxList<StorageDecisionVm>"
  params: {}
  returns: "one entry per category in the latest pass, most significant first; empty before the first pass"
  purpose: "what FR-STORE-007's expanded state renders"
```

## 6. Risks
- The design measured `45% used`; without a denominator the built string
  differs. That is a **disclosed deviation with a spec reason**
  (`OQ-E08-1`), recorded in §9 and in `GAP-026` — not a silently changed
  string, and not a fabricated percentage.
- The dashboard is the app's front door and already carries three
  never-synthesize prohibitions. Adding a fourth number is the highest-risk
  place in the app to get "just show something" wrong.
- Editing the shared probe fixture can change **other screens'** gate
  scores. Re-run the gate for every screen the fixture feeds and record
  each before/after number, or a silent regression lands on a screen this
  task never opened.
- `Obx` over two independent Rx values (usage and expansion) rebuilds more
  than needed; keep the rebuild scoped or the card jitters on every tick.

## 7. Implementation checklist
- [ ] tests written FIRST and failing, named by EARS id
- [ ] `StorageUsageVm` widened; percentage only with a real budget
- [ ] expansion view model reading the latest decision pass
- [ ] collapsed + expanded rendering against the contract, copy verbatim
- [ ] binding resolves `StorageManager` from `AppBinding`'s registration
- [ ] probe fixture: dashboard storage-plan seed + the one group-conversation
      seed (E07 retro carry-forward, `L-design-002`)
- [ ] gate re-run for every screen the fixture feeds, before/after recorded
- [ ] `make design-verify SCREEN=dashboard` run; report recorded verbatim
- [ ] `flutter analyze` clean, full suite green

## 8. Acceptance criteria (EARS) + test plan
- **EARS-STORE-2** *(epic-level, owned here)*: The dashboard SHALL show
  storage warnings as informational only, never requiring a "Clean Now"
  action. (FR-STORE-006)
- **EARS-STORE-18**: WHEN the storage warning is expanded, the system SHALL
  show the decisions the active policy made and the reason for each.
  (FR-STORE-007)
- **EARS-STORE-19**: IF no real storage denominator is configured, THEN the
  card SHALL render its measured byte total and SHALL NOT render a
  percentage. (FR-STORE-006, NFR-SCALE-001)

### Tests
- `test_EARS_STORE_2_card_has_no_action_affordance` → widget tree contains
  no button/tap target that mutates storage; the only tap toggles expansion.
- `test_EARS_STORE_2_expanding_does_not_run_a_pass` → a spying manager
  records zero `runPass` calls.
- `test_EARS_STORE_18_expansion_lists_each_category_with_its_reason`.
- `test_EARS_STORE_18_expansion_is_empty_before_the_first_pass` → renders
  the no-warning state, not a spinner.
- `test_EARS_STORE_19_no_budget_renders_bytes_not_percent`.
- `test_EARS_STORE_19_real_budget_renders_percent` → with `budget_bytes`
  set, the percentage is computed, never guessed.

### Manual
1. Open the dashboard with real history → measured usage, correct policy
   line; tap → explanation matching `storage_decisions`.

## 9. Self-review + Definition of Done

### Self-review
- [ ] Every §7 item done, with commit hashes
- [ ] `flutter test && flutter analyze` green
- [ ] Diff confined to `files:`; §4 respected
- [ ] `make design-verify SCREEN=dashboard` green, or every finding
      disclosed with evidence and a named cause
- [ ] Loading / error / empty states present
- [ ] No widget swapped to please the probe (`L-frontend-001`)
- [ ] No fabricated number anywhere on the card
- [ ] Every other screen's gate score re-checked after the fixture change,
      before/after recorded
- [ ] E07's `L-design-002` fixture carry-forward closed, not re-disclosed

**Deviations from spec/design** (the `45% used` string is expected here —
state it with `OQ-E08-1`/`GAP-026` as its reason):

**Files actually touched:**

### Definition of Done
- [ ] Every §8 criterion passes via a test named by its EARS id
- [ ] Design gate green; every extra element traces to an approved
      `design/gaps.md` entry
- [ ] Review APPROVE from a different model than `executed_by` (rule 5)
- [ ] Squash-merged to `epic_08`; tracker stamped
- [ ] 🧍 Human `verified`

## Open Questions
- (none new at sharding — this task inherits `OQ-E08-1` and is gated on
  🧍 `design_contract_approval` for `E08-T07`'s expanded-state section)

## Feedback log
- (human feedback lands here)

## Run log
- (evidence lands here)
