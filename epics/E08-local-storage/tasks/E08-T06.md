---
id: E08-T06
epic: E08
type: feature
title: Retention execution, the decision log, and wiring the storage manager into the app
layer: backend
size: M
status: review-requested
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: must, p: P2 }
depends_on: [E08-T04, E08-T05]
blocks: [E08-T08, E08-T09]
traces_to: [FR-STORE-005, FR-STORE-007, NFR-PRIV-001]
required_context: [spec/srs.md, epics/E08-local-storage/epic.md, epics/E08-local-storage/tasks/E08-T04.md, epics/E08-local-storage/tasks/E08-T05.md, lib/core/messaging/messaging_coordinator.dart, lib/app/bindings.dart, epics/E06-personal-chat/tasks/E06-T06.md, epics/E07-groups-calls/retro.md, docs/conventions.md]
design_contract: n/a
files:
  create:
    - lib/core/storage/storage_decision_log.dart
    - lib/core/storage/retention_executor.dart
    - lib/core/storage/storage_manager.dart
    - test/core/storage/retention_executor_test.dart
    - test/core/storage/storage_manager_test.dart
  update:
    - lib/core/messaging/messaging_coordinator.dart
    - lib/app/bindings.dart
    - test/core/messaging/messaging_coordinator_test.dart
  delete: []
started_at: 2026-09-02
completed_at: 2026-09-02
executed_by: claude-sonnet-5
reviewed_at:
reviewed_by:
review_outcome:
---
# E08-T06 · Retention execution + decision log + composition

> 🟢 **`OQ-E08-3` answered 2026-09-02 (option (a)):** Smart Mode's
> allow-list excludes conversation content entirely; message deletion
> happens only under a manual policy the user explicitly turns on. This
> makes the allow-list **mode-dependent**, which `RetentionExecutor`'s
> original §5 signature could not express — see `OQ-E08-T06-1` below for
> the resulting, orchestrator-amended contract (allow-list moved from the
> constructor to a per-`apply()`-call parameter, supplied by
> `StorageManager` based on which policy produced the plan). Build against
> the amended §5, not the original static-constructor shape.

## 1. Goal
Make the plan real and make it *run*: apply an approved plan, record every
decision with its reason so `FR-STORE-007` has something honest to explain,
and — the part this project has now missed twice — actually compose the
thing into the running app.

## 2. Business logic
- **Plan and apply are separate calls, always.** `planNow()` computes and
  records `outcome: planned` rows; `apply(plan)` deletes and records
  `outcome: applied`. The dashboard's informational warning
  (`EARS-STORE-2`) reads *planned* rows and never triggers `apply` — a
  warning that deletes when you look at it is not informational.
- **Only the classes `OQ-E08-3` authorises may be deleted.** The executor
  takes an explicit allow-list of `StorageItemKind`s, and a candidate group
  outside it is recorded as `outcome: skipped` with its reason preserved —
  visible in the explanation, never silently dropped.
- **Never delete an item that is not yet delivered.** A message whose
  delivery state is `Queued`, `Sent` or `Failed` is in flight; deleting it
  destroys data the user believes they sent. This is an invariant of the
  executor, independent of any policy, and it is asserted before every
  delete.
- **Relay payload retention is not ours.** `RelayEngine.reclaimPayloads()`
  (E04-B02) already owns relay TTL and `MessagingCoordinator` already
  drives it (`E06-T06`). The executor never deletes a `relay_packets` row
  or nulls a payload — if relay bytes appear in a plan they are reported,
  and skipped with the owning mechanism named.
- **Every delete is logged before it happens, in the same transaction.**
  A delete whose decision row failed to write is an unexplainable deletion,
  which is worse than no deletion (`FR-STORE-007`).
- **Composition is this task's own deliverable, not a follow-up.** This
  project has now shipped an unwired controller twice — `OQ-E06-T06-4`
  (`MessagingCoordinator.start()` had no caller) caught it proactively,
  `E07-B03` (`CallMigrationController` never constructed in `lib/`) did
  not, and E07's retro named the pattern explicitly. So `bindings.dart`
  and `messaging_coordinator.dart` are inside **this** task's fence: the
  manager is registered, the tick calls it, and a test proves the wiring
  from the composition root rather than from a hand-built object.
- **The storage pass runs on the existing coordinator tick, not a new
  scheduler.** `MessagingCoordinator` already owns the app's one periodic
  tick; a second timer would be a second answer to "when does background
  work happen".
- **The storage pass is throttled.** A full inventory scan on every tick is
  wasteful; the manager runs at most once per `storagePassInterval` and
  skips (recording nothing) otherwise.

## 3. What this task does
- `storage_decision_log.dart`: `StorageDecisionLog` — append planned/applied/
  skipped rows to `storage_decisions`, and read the most recent pass's rows
  for the explanation surfaces.
- `retention_executor.dart`: `RetentionExecutor.apply(plan, allowedKinds: ...)`
  — transactional, delivery-state guarded, log-before-delete. `allowedKinds`
  is supplied per call by `StorageManager`, mode-dependent (see
  `OQ-E08-T06-1`'s amendment).
- `storage_manager.dart`: `StorageManager` — the facade the app composes:
  reads settings, picks Smart or Manual policy, plans, logs, optionally
  applies, exposes the latest plan for the UI.
- Wires `StorageManager` into `AppBinding` (`Get.put(..., permanent: true)`)
  and calls it from `MessagingCoordinator`'s tick behind the throttle.

## 4. What this task does NOT do  ← the scope fence
- **Does not choose which classes are deletable beyond what `OQ-E08-3`'s
  answer and `OQ-E08-T06-1`'s amendment already state.** The mode-split
  (Smart Mode always empty; Manual Mode may include `message`) is fixed by
  those resolutions — the builder implements exactly that split and does
  not widen it, narrow it, or add a third mode-dependent variant.
- **Does not delete a `relay_packets` row or null a payload** — E04-B02 and
  `E06-T06` own that, and duplicating it would give relay retention two
  owners with different rules.
- **Does not delete a message that is not in a terminal delivered state.**
- **Does not add a second periodic timer, isolate, or background service.**
  `E10` owns background operation; this rides the existing tick.
- **Does not touch `messages` schema, delivery-state semantics, sync
  cursors or dedup state.** If a delete would break E05's dedup or sequence
  guarantees, that is a finding for `OQ-E08-3`, not something to work around
  here.
- **Does not render anything.** No copy, no widget, no controller. `E08-T08`
  and `E08-T09` read from the manager.
- **Does not change `MessagingCoordinator`'s existing tick semantics** —
  no reordering of `processQueue`/`sweepExpired`/`reclaimPayloads`, no
  change to its re-entrancy coalescing (the property `E06-T06`'s reviewer
  falsified). The storage pass is appended, after the existing work, and
  its failure must not abort the tick.

## 5. Contract

### Functions
```yaml
- signature: "StorageDecisionLog({required AppDatabase db})"
  params: { db: "the app's AppDatabase" }
  returns: "StorageDecisionLog"
  purpose: "the only writer of storage_decisions"

- signature: "StorageDecisionLog.recordPass(RetentionPlan plan, {required DecisionOutcome outcome, required int nowEpochMs}) -> Future<String>"
  params: { plan: "from E08-T04/T05", outcome: "planned | applied | skipped", nowEpochMs: "injected clock" }
  returns: "the pass id"
  purpose: "one row per candidate group, so FR-STORE-007 can explain each"

- signature: "StorageDecisionLog.latestPass() -> Future<List<StorageDecisionRow>>"
  params: {}
  returns: "the most recent pass's rows, most recent first; empty when no pass has run"
  purpose: "the read model behind both explanation surfaces"

- signature: "RetentionExecutor({required AppDatabase db, required StorageDecisionLog log})"
  params: {}
  returns: "RetentionExecutor"
  purpose: "the only code path in the app that deletes stored user data. AMENDED by OQ-E08-T06-1's resolution: no constructor-level allowedKinds — the allow-list is mode-dependent (Smart vs. Manual) and must be supplied per apply() call, since a single static set cannot express OQ-E08-3(a)'s answer. See Open Questions."

- signature: "RetentionExecutor.apply(RetentionPlan plan, {required Set<StorageItemKind> allowedKinds, required int nowEpochMs}) -> Future<RetentionOutcome>"
  params:
    plan: "the plan to execute"
    allowedKinds: "the classes this specific call may delete — supplied by StorageManager per pass, based on which policy produced the plan (empty for a Smart Mode plan; {message} for a Manual Mode plan under olderThanDays/overSizeMb, per OQ-E08-3(a)/OQ-E08-T06-1). A candidate group outside this set is always outcome: skipped, regardless of mode."
  returns: "RetentionOutcome — appliedGroups, skippedGroups (each with why), bytesReclaimed"
  purpose: "execute a plan transactionally, logging before deleting"

- signature: "StorageManager({required StorageSettingsRepository settings, required StorageInventory inventory, required SmartModePolicy smart, required RetentionExecutor executor, required StorageDecisionLog log, Duration storagePassInterval = const Duration(hours: 6)})"
  params: {}
  returns: "StorageManager"
  purpose: "the composed facade the app registers and the UI reads"

- signature: "StorageManager.runPass({required int nowEpochMs, bool apply = true}) -> Future<RetentionPlan?>"
  params: { apply: "false computes and logs a plan without executing it" }
  returns: "the plan, or null when the throttle interval has not elapsed"
  purpose: "the one entry point MessagingCoordinator's tick calls"

- signature: "StorageManager.latestPlan -> Rx<RetentionPlan?>"
  params: {}
  returns: "the most recent plan, for the UI to observe; null before the first pass"
  purpose: "so no screen has to trigger a pass in order to render"
```

### Data
Writes `storage_decisions`. Deletes rows only from tables named in the
per-call `allowedKinds` set (empty for Smart Mode; `{message}` for Manual
Mode, per `OQ-E08-T06-1`). Reads `storage_policy_settings` through
`E08-T05`'s repository and inventory through `E08-T02`'s.

## 6. Risks
- **A deletion bug here is unrecoverable user data loss** — there is no
  server copy (ADR-0005). Every test that asserts a delete must also assert
  what was *not* deleted; falsification (revert the guard, watch the right
  test fail) is the expected review style.
- `Get.put` ordering in `AppBinding`: the manager depends on `AppDatabase`
  and the messaging stack, both already registered above it. Registering it
  too early yields a `Get.find` failure at startup — exactly the class of
  composition-root defect `E06-B02` was.
- An exception in the storage pass must not abort the coordinator tick, or
  a storage bug silently stops message delivery. Wrap it, count it, keep
  going.
- The throttle must survive process restart or every cold start runs a full
  scan; persist the last pass time (via `storage_decisions.decided_at`,
  which already records it) rather than an in-memory field alone.
- `bindings.dart` is touched by **no other E08 task** — keep it that way; a
  second task editing it would be this epic's merge-pain source.

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS id `7bc094c`
- [x] `StorageDecisionLog` — recordPass / latestPass `7bc094c`
- [x] `RetentionExecutor` — allow-list, delivery-state guard, log-before-delete, transactional `7bc094c`
- [x] `StorageManager` — settings → policy → plan → log → optional apply, throttled `7bc094c`
- [x] `AppBinding` registers the manager (permanent) `7bc094c`
- [x] `MessagingCoordinator.tick()` calls `runPass`, failure-isolated `7bc094c`
- [x] a test proving the wiring **from `AppBinding`**, not from a hand-built object `7bc094c`
- [x] `flutter analyze` clean, full suite green `7bc094c`

## 8. Acceptance criteria (EARS) + test plan
- **EARS-STORE-13**: WHEN a retention plan is applied, the system SHALL
  record every candidate group's decision and reason before deleting
  anything, and SHALL delete only items of the classes it was explicitly
  authorised to delete. (FR-STORE-005, FR-STORE-007)
- **EARS-STORE-14**: The system SHALL NOT delete a message that is not in a
  terminal delivered state, and SHALL NOT delete or reclaim a relay packet
  payload. (FR-STORE-005, NFR-PRIV-001)
- **EARS-STORE-15**: WHILE the app is running, the storage manager
  registered by the composition root SHALL be driven by the existing
  coordinator tick, and a failure in the storage pass SHALL NOT prevent the
  tick's messaging work. (FR-STORE-005)

### Tests
- `test_EARS_STORE_13_decisions_are_logged_before_deletes` → a db that
  fails the delete still leaves the decision rows; falsifiable.
- `test_EARS_STORE_13_group_outside_allow_list_is_skipped_with_reason`.
- `test_EARS_STORE_13_empty_allow_list_deletes_nothing` → Smart Mode's
  allow-list (always empty per `OQ-E08-T06-1`).
- `test_EARS_STORE_13_smart_mode_never_deletes_message_kind_items` →
  the same message-kind candidate group is `outcome: skipped` when the
  originating plan is Smart Mode, regardless of what allow-list a caller
  might otherwise pass — this must be true even if a future change
  widens Smart Mode's allow-list for a non-message kind. (`OQ-E08-T06-1`)
- `test_EARS_STORE_13_manual_mode_may_delete_message_kind_items` → the
  identical candidate shape IS applied when the originating plan is a
  Manual Mode (`olderThanDays`/`overSizeMb`) plan — proves the
  mode-dependent split is real, not just documented.
- `test_EARS_STORE_14_undelivered_message_is_never_deleted` → seeded
  Queued/Sent/Failed rows survive a plan that names them, **even under
  Manual Mode's message-kind authorisation** — the delivery-state guard
  is unconditional, independent of any allow-list.
- `test_EARS_STORE_14_relay_payloads_are_never_touched`.
- `test_EARS_STORE_15_app_binding_registers_storage_manager` → resolve it
  through `Get.find` after `AppBinding.dependencies()`.
- `test_EARS_STORE_15_coordinator_tick_runs_a_storage_pass`.
- `test_EARS_STORE_15_storage_pass_failure_does_not_abort_tick` → throwing
  manager, messaging work still completes.
- `test_EARS_STORE_15_pass_is_throttled_across_ticks`.

### Manual
1. Run the app, wait a tick interval → a planned pass appears in
   `storage_decisions`; nothing is deleted while Smart Mode is active
   (the default). Switch to a manual policy and confirm its own explicit
   rule can plan and apply message-kind removals.

## 9. Self-review + Definition of Done

### Self-review
- [x] Every §7 item done, with commit hashes (`7bc094c`)
- [x] `flutter test && flutter analyze` green (773/773 tests, up from 757/757
      at the `epic_08` tip; `flutter analyze`: No issues found)
- [x] Diff confined to `files:`; §4 respected — only the declared
      `create:`/`update:` files touched, plus this task file's own doc
      sections
- [x] `grep -rn "StorageManager(" lib/` shows a real composition-root call
      site (`lib/app/bindings.dart`), not only the declaration
      (`lib/core/storage/storage_manager.dart`) — E07-B03's lesson, applied
- [x] No relay payload path (a `relayPayload` group is always `outcome:
      skipped`, verified by `test_EARS_STORE_14_relay_payloads_are_never_
      touched`), no delivery-state change beyond the pre-existing
      `DeliveryStateMachine`-mediated column, no second timer (the storage
      pass rides `MessagingCoordinator`'s existing tick only — no
      `Timer`/isolate/background service added in this diff)
- [x] The mode-dependent split is real and tested: Smart Mode always empty
      (`test_EARS_STORE_13_empty_allow_list_deletes_nothing`,
      `test_EARS_STORE_13_smart_mode_never_deletes_message_kind_items`),
      Manual Mode may include `message`
      (`test_EARS_STORE_13_manual_mode_may_delete_message_kind_items`) — not
      a single static set (`OQ-E08-T06-1`)

**Deviations from spec/design:**
- **`RetentionExecutor` enforces the Smart-Mode-never-`message` rule a
  second time, independently of the caller-supplied `allowedKinds`.** §5's
  amended contract puts the mode/allow-list decision entirely on
  `StorageManager` (which, as built, only ever passes an empty set for a
  Smart Mode plan and `{message}` for a Manual Mode plan) — the executor
  itself does not strictly need to know `plan.mode` to satisfy that
  contract literally. I added a second, defense-in-depth check inside
  `RetentionExecutor.apply` that skips any `message`-kind group whenever
  `plan.mode == StorageMode.smart.name`, **regardless of what `allowedKinds`
  it is handed** — belt-and-braces against a future caller bug or a
  Smart-Mode allow-list widened later for some *other* kind that forgets to
  keep `message` excluded. This directly satisfies §8's own
  `test_EARS_STORE_13_smart_mode_never_deletes_message_kind_items`, whose
  own wording ("regardless of what allow-list a caller might otherwise
  pass") reads as requiring exactly this — a plan-mode-level guard, not
  only a caller-supplied-allow-list-level one. Additive, not a narrowing of
  what Manual Mode may do; falsified (see Run log) to confirm the named
  test fails without it.
- **`StorageDecisionLog.recordPass` always writes at least one row per
  call, even for a zero-candidate plan (a "sentinel" row,
  `categoryKey: 'none'`).** Not named in §5's literal contract, but
  required by §6's own risk note — "persist the last pass time (via
  `storage_decisions.decided_at` ...) rather than an in-memory field
  alone." Without a row on a zero-candidate pass, `decided_at` would never
  advance and the throttle would never actually engage on a healthy device
  with nothing to remove. Falsified (see Run log) to confirm
  `test_storage_manager_zero_candidate_pass_still_advances_the_throttle`
  fails without it.
- **`MessagingCoordinator.storageManager` is a public, mutable, nullable
  field, not a constructor parameter.** `MessagingStack.create()`
  (`messaging_stack.dart`, explicitly outside this task's `files:` fence)
  already fully constructs the coordinator before `AppBinding
  .dependencies()` ever runs (E06-T03's ordering) — its own
  `MessagingCoordinator(...)` call site cannot be changed by this task.
  `AppBinding` sets the field once, after building `StorageManager`, before
  `messagingStack.coordinator.start()`'s effects can matter. Documented in
  both files' headers.
- **`bindings.dart` measures the sqlite file's own on-disk size with a
  small private helper (`_measureDatabaseFileBytes`) that duplicates
  `database.dart`'s own private `_openConnection` path construction**
  (`getApplicationDocumentsDirectory()` + `nexora.sqlite`), rather than
  exposing a new public path getter on `AppDatabase` — `database.dart` is
  not in this task's `files:` fence, so a new public API on it is out of
  scope. The helper also catches any failure (including a missing platform
  channel, e.g. a plain `flutter_test` unit test with no `path_provider`
  mock) and returns `0` rather than letting it propagate — this is a
  peripheral display measurement, not allowed to take the whole storage
  pass down with it, matching §6's "must not abort" posture generally, not
  only for `RelayEngine` calls specifically. Discovered via the first real
  run of `test_EARS_STORE_15_coordinator_tick_runs_a_storage_pass`, which
  failed with a `MissingPluginException` before this fix (see Run log).
- **`StorageManager.runPass(apply: true)` does not additionally write a
  `planned`-outcome row before handing the plan to `RetentionExecutor`.**
  §2's prose describes `planNow()`/`apply(plan)` as if they were two
  separate calls each with their own logging; §5's actual contract
  consolidates them into one `runPass(apply: bool)` method. Read literally,
  `apply: false` is the `planNow()` case (logs `outcome: planned`, no
  executor call) and `apply: true` is the `apply(plan)` case (hands off to
  `RetentionExecutor`, which does its own `applied`/`skipped` logging) —
  never both in the same call, since that would produce two rows per
  candidate group in the very same pass with different, overlapping
  outcomes for no analytical benefit.

**Files actually touched:**
- `lib/core/storage/storage_decision_log.dart` (new)
- `lib/core/storage/retention_executor.dart` (new)
- `lib/core/storage/storage_manager.dart` (new)
- `test/core/storage/retention_executor_test.dart` (new)
- `test/core/storage/storage_manager_test.dart` (new)
- `lib/core/messaging/messaging_coordinator.dart` (updated — `storageManager`
  field, storage pass appended to `_runTick`, `storagePassFailures` counter)
- `lib/app/bindings.dart` (updated — `StorageManager` composition root)
- `test/core/messaging/messaging_coordinator_test.dart` (updated —
  EARS-STORE-15 test group + `_ThrowingStorageManager` helper)
- `epics/E08-local-storage/tasks/E08-T06.md` (this file — checklist/
  self-review/DoD/Run log)

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id
- [ ] Review APPROVE from a different model than `executed_by` (rule 5)
- [ ] Squash-merged to `epic_08`; tracker stamped
- [ ] 🧍 Human `verified`

## Open Questions
- **OQ-E08-T06-1 — the deletable-class allow-list.** Inherits epic-level
  `OQ-E08-3`. The executor ships with an empty allow-list until answered;
  populating it is a spec decision, not an implementation choice.
  - **Status:** 🟢 answered, with a contract amendment — inherits the
    epic-level `OQ-E08-3` resolution (option (a), 2026-09-02): **"Smart
    Mode's allow-list excludes conversation content entirely; message
    deletion happens only under a manual policy the user explicitly turns
    on."** Read literally, this makes the allowed classes **mode-dependent**:
    a Smart Mode pass must never delete `message`-kind items; a Manual Mode
    pass (`olderThanDays`/`overSizeMb`, `E08-T05`) is explicitly authorised
    to, since the user chose that specific rule themselves.
    **§5's `RetentionExecutor` constructor signature as originally written
    (one static `Set<StorageItemKind> allowedKinds>` for the whole
    executor's lifetime) cannot express this — it has no way to know which
    policy produced the plan it is asked to apply.** This is a real
    contract gap, not something to paper over by picking one static set.
    **Amendment: `StorageManager.runPass` must select the allow-list per
    pass, based on which policy produced the plan** — e.g. either (i)
    `StorageManager` constructs/reconfigures the kinds it passes to
    `RetentionExecutor.apply` per call (pass `allowedKinds` as a per-call
    parameter to `apply`, not only a constructor field), or (ii)
    `StorageManager` holds two named allow-lists (`smartModeAllowedKinds`
    empty, `manualModeAllowedKinds` = `{message}`) and selects by
    `settings.mode` before calling `apply`. Either shape is acceptable;
    the builder decides the cleaner implementation, but the resulting
    behavior must be: Smart Mode passes never delete `message`-kind items
    regardless of any allow-list value, and a Manual Mode pass under
    `olderThanDays`/`overSizeMb` may delete `message`-kind items the
    user's own chosen rule selected. `relayPayload`/`databaseFile` stay
    excluded from both, per §4's existing, unambiguous fence (owned
    elsewhere). Write a test proving the mode-dependent split explicitly —
    the same plan/candidate shape must be blocked under Smart Mode and
    permitted under Manual Mode.
  - **Answered by:** orchestrator, amending §5's contract to match the
    human's actual `OQ-E08-3` answer (a mechanical fix required to build
    what was approved, not a new product decision).
  - **Date:** 2026-09-02

## Feedback log
- (human feedback lands here)

## Run log
- 2026-09-02, builder (claude-sonnet-5), commit `7bc094c`: branch
  `epic_08_task_06` off `epic_08` tip `8b73186` (T01-T05 merged, this task's
  own `OQ-E08-T06-1` amendment already in the task file at dispatch — built
  against the amended §5, not the original static-constructor shape).
  - Tests written first: `test/core/storage/retention_executor_test.dart`
    (7 tests, all named `test_EARS_STORE_13_*`/`test_EARS_STORE_14_*`),
    `test/core/storage/storage_manager_test.dart` (5 tests), and 4 new
    tests appended to `test/core/messaging/messaging_coordinator_test.dart`
    (`test_EARS_STORE_15_*`). `RetentionExecutor`/`StorageDecisionLog`/
    `StorageManager` did not exist yet, so the first run failed on
    missing-symbol compile errors — the correct "red for the right reason"
    for these three from-scratch files.
  - `storage_decision_log.dart`: `StorageDecisionLog` — `recordPass`
    (writes one row per candidate group, or one `categoryKey: 'none'`
    sentinel row for a zero-candidate plan so `decided_at` always advances
    — see Deviations), `latestPass` (rows sharing the max `decided_at`).
  - `retention_executor.dart`: `RetentionExecutor.apply(plan, {required
    allowedKinds, required nowEpochMs})` — the amended §5 signature. Order
    of checks per candidate group: (1) `relayPayload` always skipped
    (invariant, names `RelayEngine.reclaimPayloads`); (2) `message` +
    `plan.mode == 'smart'` always skipped regardless of `allowedKinds`
    (defense-in-depth, see Deviations); (3) outside the per-call
    `allowedKinds` -> skipped, reason preserved; (4) `message` ->
    delivery-state guard, querying `messages` directly for each item's
    *current* state and splitting the candidate group into a deletable
    slice and an undelivered slice (real per-item bytes via
    `LENGTH(ciphertext)`, matching `StorageInventory`'s own convention);
    (5) any other kind -> skipped, no delete path exists in this build.
    Decisions for the whole pass are logged via two `log.recordPass` calls
    (`applied`, `skipped`) BEFORE the delete loop runs — proven, not
    assumed, by `test_EARS_STORE_13_decisions_are_logged_before_deletes`,
    which uses a `_ThrowingDeleteExecutor` test subclass overriding the new
    `deleteMessageItems` method to force a failure after the log has
    already committed.
  - `storage_manager.dart`: `StorageManager.runPass({required nowEpochMs,
    bool apply = true})` — throttle check against
    `StorageDecisionLog.latestPass()`'s own `decided_at` (restart-safe, no
    in-memory timer), settings-driven mode selection (Smart Mode: gathers
    `message`+`relayPayload` items via `StorageInventory.itemsOfKind`,
    builds the `stats` map from `storage_item_stats` directly, calls
    `SmartModePolicy.plan`, `allowedKinds` = `{}`; Manual Mode: calls
    `ManualPolicy.plan`, `allowedKinds` = `{message}`), `apply: false` logs
    an `outcome: planned` row itself and returns without touching the
    executor, `apply: true` (default) hands the plan straight to
    `RetentionExecutor.apply`. `latestPlan` is a `Rx<RetentionPlan?>`,
    updated on every non-throttled pass.
  - `messaging_coordinator.dart`: added a public, mutable, nullable
    `storageManager` field (not a constructor parameter — see Deviations)
    and a `storagePassFailures` counter; `_runTick` now calls
    `storageManager?.runPass(...)` in its own try/catch, strictly after the
    existing `processQueue`/`sweepExpired`/`reclaimPayloads` block and its
    own catch — no reordering of the existing steps, no change to the
    `tick()`/`_inFlightTick` re-entrancy coalescing (untouched).
  - `bindings.dart`: builds one shared `StorageDecisionLog` instance,
    constructs `StorageManager` from it plus `StorageSettingsRepository`,
    `StorageInventory` (with a `_measureDatabaseFileBytes` helper — see
    Deviations for why it duplicates `database.dart`'s path construction
    and swallows its own failures), `SmartModePolicy`, and
    `RetentionExecutor`; registers it `Get.put(..., permanent: true)`
    after `AppDatabase`/`MessagingStack` are already registered above it,
    and sets `messagingStack.coordinator.storageManager` before returning.
  - **Falsification performed (§6's explicit instruction for this task) —
    four probes, each confirmed to fail for the right reason then reverted
    verbatim:**
    1. Swapped the delete loop before the two `log.recordPass` calls in
       `RetentionExecutor.apply` ->
       `test_EARS_STORE_13_decisions_are_logged_before_deletes` failed
       (`Expected: non-empty / Actual: []`).
    2. Disabled the delivery-state guard (`if (true)` instead of
       `if (!_undeliveredStates.contains(state))`) ->
       `test_EARS_STORE_14_undelivered_message_is_never_deleted` failed
       (all four seeded messages, including `Queued`/`Sent`/`Failed`, came
       back as deleted).
    3. Disabled the Smart-Mode-never-`message` defense-in-depth check
       (`if (false && ...)`) ->
       `test_EARS_STORE_13_smart_mode_never_deletes_message_kind_items`
       failed (`Expected: empty / Actual: [Instance of
       'RetentionCandidateGroup']`).
    4. Moved the storage pass before the messaging steps in
       `MessagingCoordinator._runTick` and removed its own try/catch ->
       `test_EARS_STORE_15_storage_pass_failure_does_not_abort_tick` failed
       with the simulated exception propagating out of `tick()` entirely
       (messaging work never ran).
    5. Removed the zero-candidate sentinel row from
       `StorageDecisionLog.recordPass` ->
       `test_storage_manager_zero_candidate_pass_still_advances_the_throttle`
       failed (`Expected: null / Actual: <Instance of 'RetentionPlan'>`).
    All five reverted immediately, full suite re-run green after each
    revert.
  - **One real bug caught by the tests, not assumed away**: the first run
    of `test_EARS_STORE_15_coordinator_tick_runs_a_storage_pass` failed
    with a `MissingPluginException` on `getApplicationDocumentsDirectory`
    — `StorageInventory.snapshot()` awaits the injected
    `databaseFileBytes` callback unconditionally, and a plain
    `flutter_test` unit test has no `path_provider` platform channel
    mocked. Fixed by wrapping `_measureDatabaseFileBytes`'s body in its
    own try/catch, returning `0` on any failure (see Deviations) — a
    peripheral display measurement must never be able to take the whole
    storage pass down, in test or in a production edge case.
  - `flutter analyze` (full project): No issues found. `flutter test`
    (full suite): 773/773 pass (757 at the `epic_08` tip + 16 new: 7 in
    `retention_executor_test.dart`, 5 in `storage_manager_test.dart`, 4
    appended to `messaging_coordinator_test.dart`).
  - Pre-existing, unrelated to this change: the same drift `WARNING: ...
    created the database class AppDatabase multiple times ...` noise
    T01/T02/T04/T05's own run logs already documented as present on the
    `epic_08` tip, in test files (including this task's own new
    `newStack`-based tests) that build more than one `AppDatabase` per
    test file by design.
