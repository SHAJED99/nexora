---
id: E08-B07
epic: E08
type: bug
title: "`olderThanDays` selects the newest of the aged set, not the oldest — same root cause as B01/B02, a third unfixed caller"
layer: backend
size: S
status: review-requested
owner_agent: builder
model: any
preferred_agent: any
token_estimate: { tier: S, range: "10k-20k" }
severity: S3
priority: { moscow: should, p: P2 }
depends_on: [E08-B01, E08-B02, E08-B03]
blocks: []
traces_to: [FR-STORE-004, FR-STORE-007]
required_context:
  - epics/E08-local-storage/tasks/E08-B01.md
  - epics/E08-local-storage/tasks/E08-B02.md
  - lib/core/storage/manual_policy.dart
  - lib/core/storage/storage_inventory.dart
found_by: reviewer (independent) during E08-B01/B02's PR review, as a carried-forward observation — not part of either bug's own scope
found_in: the same seam as E08-B01/B02 (`StorageInventory.itemsOfKind`'s bounded window with no paging), a third caller — `ManualPolicy._planOlderThan`. B01/B02's fix added paging/oldestFirst support to `itemsOfKind` and to `_planOverSize`/Smart Mode's item-gathering, but `_planOlderThan` itself was not touched — correctly out of scope for either bug's own repro/regression test, per the epic's one-bug-per-caller pattern.
files:
  create:
    - test/core/storage/manual_policy_older_than_paging_test.dart
  update:
    - lib/core/storage/manual_policy.dart
    - test/core/storage/manual_policy_test.dart
  delete: []
---
# E08-B07 · `olderThanDays` selects the newest of the aged set, not the oldest

## The defect

`ManualPolicy._planOlderThan` (`manual_policy.dart:121-124`) calls:

```dart
items(kind, olderThanEpochMs: cutoff)   // default limit: 500, ORDER BY created_at DESC
```

with no paging. `itemsOfKind`'s `oldestFirst`/paging support (added by
`E08-B01`/`E08-B02`'s fix) exists on the enumeration itself, but this call
site was never updated to use it — `_planOlderThan` still takes whatever
500 rows come back from a `DESC`-ordered, cutoff-filtered query, which are
the **newest** 500 items that happen to be older than the cutoff, not the
**oldest** 500.

## Repro (reviewer's own probe, from E08-B01/B02's PR review)

1. Seed 1500 messages, all older than a 30-day cutoff, with a spread of
   ages beyond that.
2. `StorageSettingsRepository.setMode(StorageMode.olderThanDays, olderThanDays: 30)`.
3. `StorageManager.runPass(...)` under `olderThanDays`.

```
selected=500 of 1500 aged; min=m-01000 max=m-01499
```

The 500 selected are the newest of the 1500 aged messages — i.e. the ones
*closest* to no longer qualifying — not the oldest, which is what a user
asking to delete "data older than 30 days" would expect to go first.

## Expected

FR-STORE-004 — "delete data older than X days" removes the oldest
qualifying data first, same as `overSizeMb`'s and Smart Mode's corrected
behavior.

## Actual

As above: the newest 500 of the aged set are selected each pass, so the
genuinely-oldest data survives every pass until enough newer aged items
have been removed to bring it into the window.

## Severity

**S3** — under-deletion, not wrong-deletion. Unlike `E08-B01`, this does
not delete the *wrong* items; it deletes *a* qualifying set of items,
just not the chronologically-correct-first ones. It self-corrects over
repeated passes (each pass eats 500 from the newest-aged end, gradually
working backward), and reaches full correctness once the aged set drops
under the page size. Not reachable today by any shipped UI (`E08-T09` is
prospective, same reachability note as `E08-B01`/`E08-B04`).

## Fix direction

Apply the same `oldestFirst` paging `E08-B01`'s fix added to
`itemsOfKind` — call `_planOlderThan`'s `items(...)` with
`oldestFirst: true` (or equivalent) and page until the running byte
total/item count captures what the policy should remove, or the kind
is exhausted. This should be a small, mechanical change reusing
`E08-B01`'s own paging helper/pattern rather than a new algorithm —
`_planOlderThan`'s selection rule ("everything older than X days") is
simpler than `_planOverSize`'s ("oldest-first until under a byte cap"),
since it has no early-termination condition other than exhausting the
aged set.

## Regression test (write it first)

Seed > the default page-size worth of items older than the cutoff and
assert the selected ids are the genuinely-oldest ones among the aged set
(not the newest-of-aged). Must fail on today's code with the
`min=m-01000 max=m-01499`-shaped signature above, and pass after.

## What this fix does NOT do

- Does **not** touch `_planOverSize` (`E08-B01`) or Smart Mode's item
  gathering (`E08-B02`) — both already fixed.
- Does **not** change `itemsOfKind`'s signature or paging mechanics —
  `E08-B01`'s fix already added what this needs; this task only changes
  the call site.
- Does **not** change the `olderThanDays` validation, the 1-day floor, or
  any `StorageSettingsRepository` behavior.
- Does **not** touch `RetentionExecutor`'s invariants, the delivery-state
  guard, or relay payload retention.
- Does **not** build `E08-T09` or make manual modes reachable from any UI.

## Feedback log
- 2026-09-03 — Priority set by the human at the `bug_priorities` gate:
  **P2 — fix now, same batch as B03/B05.** Real bug, cheap fix (reuses
  B01's own pagination code), worth closing now rather than leaving a
  fourth rediscovery of the same root cause for later.
- 2026-09-03 — **Round-1 review (CHANGES): this fix introduces the exact
  SQL-variable-limit regression `E08-B03`'s round-1 review just caught,
  and it's worse than the bug it fixes.** Removing the 500-item cap on
  `_planOlderThan` without adding chunking anywhere downstream means a
  device with >32,766 aged messages of one kind gets a hard
  `SqliteException` on every pass, forever — a permanent, silent total
  failure of the `olderThanDays` path, replacing the original bug's
  self-correcting under-deletion. Reviewer's own end-to-end probe
  (40,000 seeded messages) reproduced the throw directly; confirmed as
  a genuine regression (pre-fix code doesn't hit this limit). Escalated
  severity: **S2** (from S3).
  - **Routing decision (orchestrator, following the reviewer's own
    recommended option 1):** sequence this fix **behind `E08-B03`'s
    chunking fix**, rather than duplicating chunking logic here or
    reintroducing an early-termination cap that would defeat this
    task's own purpose. `depends_on` updated to include `E08-B03`.
    `status: blocked` until `E08-B03` merges. Once it does, re-verify
    this fix's regression test against the now-chunked
    `RetentionExecutor`/`_splitByDeliveryState`, and extend the test
    past a single page count to an id count above the SQLite bind
    limit (~32,766) — the current 1500-item test cannot catch this
    class of defect at all.

## Run log
- 2026-09-03 — Fixed `707d12e`. `_planOlderThan` (`lib/core/storage/manual_policy.dart`)
  now pages oldest-first through the whole aged set per kind
  (`oldestFirst: true`, `_pageSize`/offset — reusing `E08-B01`'s own paging
  fields on the same `StorageItemsFetcher`), looping until a page shorter
  than `_pageSize` is returned (kind exhausted). No early-termination cap,
  per the task's own Fix direction — `olderThanDays` has no byte ceiling to
  stop early on, unlike `_planOverSize`.
  - Regression test: `test/core/storage/manual_policy_older_than_paging_test.dart`
    (`test_E08_B07_older_than_selects_genuinely_oldest_past_500_aged_items`)
    — seeds 1500 aged messages (`m-00000`..`m-01499`, oldest to newest) plus
    a 50-item fresh (not-aged) block, asserts all 1500 aged ids are
    selected sorted, and that no fresh id ever appears.
  - Falsified: reverted the fix (`git stash`), re-ran the test — failed with
    `Actual` starting at `m-01000` (Which: at location [0] is 'm-01000'
    instead of 'm-00000') — matching the task's documented
    `min=m-01000 max=m-01499` signature exactly. Restored (`git stash pop`),
    re-ran — green.
  - `flutter test test/core/storage/` — 59 tests, all pass (no regressions
    in `manual_policy_test.dart`'s existing `olderThanDays`/`overSizeMb`
    coverage; its fake fetcher already modeled paging/`oldestFirst`, so no
    edit to that file was needed despite being listed under `files: update:`).
  - `flutter test` (full suite) — 787 tests, all pass.
  - `flutter analyze` — no issues found.
- 2026-09-03 — **Unblocked and finished, post-`E08-B03` merge (`af02704`,
  bookkeeping through `ceb6c31`).** Rebased `epic_08_bug_07` onto `epic_08`
  tip `ceb6c31` (clean rebase, no conflicts). Re-ran the existing
  1500-item regression test first — still green against the now-chunked
  `RetentionExecutor`, as expected (this fix only ever changed the
  `_planOlderThan` call site, never `RetentionExecutor` itself).
  - **Extended the regression test past the round-1 finding**, in the same
    file (`test/core/storage/manual_policy_older_than_paging_test.dart`,
    no new file needed):
    - `test_E08_B07_end_to_end_apply_selects_and_deletes_all_aged_items_past_the_sqlite_variable_limit`
      — real `AppDatabase.forTesting(NativeDatabase.memory())`, real
      `StorageInventory`, real `RetentionExecutor`; seeds 40,000 aged
      messages (`m-000000`..`m-039999`, spread of ages, all `stored`/
      terminal-delivery) plus 100 fresh ones; runs `ManualPolicy.plan`
      through the REAL `itemsOfKind` (not a fake) to get the plan, then
      `RetentionExecutor.apply` end-to-end. Asserts: the plan selects all
      40,000 aged items (not just 500); `apply()` completes without
      throwing; `outcome.appliedGroups`/`bytesReclaimed` match; exactly
      the 100 fresh messages remain in `messages` afterward. 40,000 is
      comfortably past SQLite's `SQLITE_MAX_VARIABLE_NUMBER` (32766 in
      this build) — the id count `RetentionExecutor`'s `IN (...)` queries
      would have choked on pre-`E08-B03`.
    - `test_E08_B07_falsification_an_unchunked_delete_at_this_id_count_throws`
      — the falsification for the large-scale test, run directly rather
      than by reverting/checking out old `RetentionExecutor` code: seeds
      the same 40,000-id set and issues ONE unchunked
      `db.delete(db.messages)..where((t) => t.id.isIn(ids))` — the exact
      shape `RetentionExecutor.deleteMessageItems` had before `E08-B03`'s
      chunking fix. Confirmed it throws an `SqliteException` mentioning
      "too many SQL variables" — the documented mechanism proven
      empirically, not merely asserted. This is what makes the
      end-to-end test above meaningful: it demonstrates the regression
      round-1 review found really would occur without `E08-B03`'s
      chunking, and that this fix is safe now that the chunking exists.
  - `flutter test test/core/storage/manual_policy_older_than_paging_test.dart`
    — 3 tests, all pass (the original 1500-item test plus the two new
    large-scale ones above).
  - `flutter test` (full suite) — 797 tests, all pass (787 + B03's own
    10 net-new tests merged in ahead of this rebase).
  - `flutter analyze` — no issues found (one `unused_import` warning
    caught and fixed during this round: an unused `Value` import left
    over from an earlier draft of the large-scale test).
