---
id: E08-T05
epic: E08
type: feature
title: Manual policies and mode selection — delete-older-than-X and delete-over-X-MB
layer: backend
size: S
status: todo
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: S, range: "5k-15k" }
priority: { moscow: should, p: P3 }
depends_on: [E08-T01, E08-T02]
blocks: [E08-T06, E08-T09]
traces_to: [FR-STORE-004]
required_context: [spec/srs.md, epics/E08-local-storage/tasks/E08-T01.md, epics/E08-local-storage/tasks/E08-T02.md, epics/E08-local-storage/tasks/E08-T04.md, docs/conventions.md]
design_contract: n/a
files:
  create:
    - lib/core/storage/storage_settings_repository.dart
    - lib/core/storage/manual_policy.dart
    - test/core/storage/storage_settings_repository_test.dart
    - test/core/storage/manual_policy_test.dart
  update: []
  delete: []
started_at:
completed_at:
executed_by:
reviewed_at:
reviewed_by:
review_outcome:
---
# E08-T05 · Manual policies + mode selection

## 1. Goal
Let the user choose a storage management mode and have that choice actually
mean something — the two non-default modes `FR-STORE-004` names, each
producing the same shape of plan Smart Mode produces, so every surface
downstream is mode-agnostic.

## 2. Business logic
- **`FR-STORE-004` names exactly three modes**: Smart Mode (default),
  delete-older-than-X-days, delete-when-over-X-MB. No fourth mode, no
  "off". (If the human wants an explicit "never delete anything" mode, that
  is a spec amendment, not an addition made here — see §4.)
- **Smart Mode is the default and is what a fresh install has**, per
  `E08-T01`'s migration-inserted settings row. This repository never has to
  handle a missing row.
- **A manual policy produces the same `RetentionPlan` type Smart Mode
  produces**, with the corresponding `RetentionReason` (`olderThan`,
  `overSizeLimit`). One plan shape means one explanation surface
  (`FR-STORE-007`) rather than three that drift apart.
- **A manual policy is literal, not clever.** "Older than X days" means
  exactly items whose `createdAt` is older than X days — it does not
  additionally consider access frequency, importance or pressure. The user
  chose a simple rule over Smart Mode precisely to get a simple rule.
- **Changing the mode never deletes anything by itself.** It changes what
  the next plan says. Execution stays `E08-T06`'s single responsibility.
- **Validation is concrete**: `olderThanDays` ≥ 1, `maxBytes` ≥ 1 MiB, and
  the mode's own parameter must be non-null while the other two stay NULL.
  An invalid write is rejected loudly, never coerced to a default.

## 3. What this task does
- `storage_settings_repository.dart`: `StorageSettingsRepository` reading
  and writing the single `storage_policy_settings` row — `watch()` (a
  broadcast stream for the UI), `read()`, `setMode(...)`, `setBudgetBytes(...)`.
- `manual_policy.dart`: `ManualPolicy.plan(...)` producing a `RetentionPlan`
  for `olderThanDays` and `overSizeMb`, using `E08-T04`'s types.
- Tests: persistence round-trip, validation rejections, watch emission on
  change, and both manual policies' candidate selection.

## 4. What this task does NOT do  ← the scope fence
- **Deletes nothing.** Same fence as `E08-T04`: plan only.
- **Does not add a mode the spec does not name** — no "off", no "ask me
  first", no per-conversation override. Each would be a real product
  decision with no id behind it.
- **Does not set `budget_bytes` to a default.** `setBudgetBytes` exists so
  `OQ-E08-1`'s answer has a home; this task leaves it NULL.
- **Does not build the settings UI.** The Storage screen is `E08-T09`,
  against `E08-T07`'s contract. No widget, no route, no copy here.
- **Does not re-implement Smart Mode's factors** for the manual modes. A
  manual mode that quietly scores access frequency is Smart Mode with a
  different label.
- Does not touch `settings_controller.dart` or `settings_view.dart`.

## 5. Contract

### Functions
```yaml
- signature: "StorageSettingsRepository({required AppDatabase db})"
  params: { db: "the app's AppDatabase (injected)" }
  returns: "StorageSettingsRepository"
  purpose: "the only reader/writer of storage_policy_settings"

- signature: "StorageSettingsRepository.read() -> Future<StoragePolicySettings>"
  params: {}
  returns: "the single settings row; never null — E08-T01 guarantees it exists"
  purpose: "current policy"

- signature: "StorageSettingsRepository.watch() -> Stream<StoragePolicySettings>"
  params: {}
  returns: "broadcast stream, emitting the current value on listen"
  purpose: "the UI binds to this rather than polling"

- signature: "StorageSettingsRepository.setMode(StorageMode mode, {int? olderThanDays, int? maxBytes}) -> Future<void>"
  params:
    mode: "smart | olderThanDays | overSizeMb"
    olderThanDays: "required and >= 1 iff mode == olderThanDays, else must be null"
    maxBytes: "required and >= 1 MiB iff mode == overSizeMb, else must be null"
  returns: "Future — throws ArgumentError on any violation of the rules above; never coerces"
  purpose: "FR-STORE-004's user choice, persisted"

- signature: "StorageSettingsRepository.setBudgetBytes(int? bytes) -> Future<void>"
  params: { bytes: "the OQ-E08-1 denominator, or null to clear" }
  returns: "Future"
  purpose: "a home for OQ-E08-1's answer; unused until it is answered"

- signature: "ManualPolicy.plan({required StorageMode mode, required StorageInventorySnapshot snapshot, required Future<List<StorageItem>> Function(StorageItemKind, {int? olderThanEpochMs}) items, required StoragePolicySettings settings, required int nowEpochMs}) -> Future<RetentionPlan>"
  params:
    mode: "olderThanDays or overSizeMb — passing smart is a StateError, Smart Mode has its own policy"
    settings: "supplies the mode's parameter"
    nowEpochMs: "injected clock"
  returns: "RetentionPlan with the corresponding RetentionReason per group"
  purpose: "the two non-default modes of FR-STORE-004"
```

### Data
Reads and writes `storage_policy_settings` (single row, `id = 1`). Reads
inventory through the injected callbacks. Writes no other table.

## 6. Risks
- `overSizeMb` needs a rule for *which* items go when the total exceeds the
  cap. Oldest-first, deterministic, tie-broken by id — write it down and
  test it, or two runs disagree and the explanation surface lies.
- A broadcast stream that does not emit its current value on listen makes
  the settings screen render empty on first frame; `E08-T09` will look
  broken and the bug will be blamed on the UI.
- MiB vs MB: `FR-STORE-004` says "MB". The column is bytes. Pick one
  conversion, state it in the doc comment, and never convert twice.
- Rejecting an invalid write loudly is correct, but the UI must not be able
  to *produce* one — `E08-T09` validates before calling; this layer is the
  backstop, not the only guard.

## 7. Implementation checklist
- [ ] tests written FIRST and failing, named by EARS id
- [ ] `StorageSettingsRepository` with read/watch/setMode/setBudgetBytes
- [ ] validation exactly as §5 states, with no coercion path
- [ ] `ManualPolicy.plan` for both non-default modes
- [ ] deterministic oldest-first selection for `overSizeMb`
- [ ] `flutter analyze` clean, full suite green

## 8. Acceptance criteria (EARS) + test plan
- **EARS-STORE-11**: WHEN the user selects a storage management mode, the
  system SHALL persist that choice and SHALL default to Smart Mode when no
  choice has been made. (FR-STORE-004)
- **EARS-STORE-12**: WHILE a manual policy is active, the system SHALL
  determine removal candidates from that policy's single stated rule only,
  and SHALL reject an invalid policy parameter rather than substituting a
  default. (FR-STORE-004)

### Tests
- `test_EARS_STORE_11_default_mode_is_smart` → fresh db.
- `test_EARS_STORE_11_mode_choice_round_trips` → set, reopen, read.
- `test_EARS_STORE_11_watch_emits_current_value_on_listen`.
- `test_EARS_STORE_12_older_than_selects_only_by_age` → an old but
  frequently-accessed item IS a candidate under this mode.
- `test_EARS_STORE_12_over_size_selects_oldest_first_until_under_cap`.
- `test_EARS_STORE_12_invalid_parameters_throw` → zero days, negative
  bytes, parameter set for the wrong mode.
- `test_EARS_STORE_12_manual_plan_is_deterministic`.

### Manual
1. Change mode, restart the app → the choice persists.

## 9. Self-review + Definition of Done

### Self-review
- [ ] Every §7 item done, with commit hashes
- [ ] `flutter test && flutter analyze` green
- [ ] Diff confined to `files:`; §4 respected
- [ ] No fourth mode, no default budget, no UI in the diff

**Deviations from spec/design:**

**Files actually touched:**

### Definition of Done
- [ ] Every §8 criterion passes via a test named by its EARS id
- [ ] Review APPROVE from a different model than `executed_by` (rule 5)
- [ ] Squash-merged to `epic_08`; tracker stamped
- [ ] 🧍 Human `verified`

## Open Questions
- (none open at sharding)

## Feedback log
- (human feedback lands here)

## Run log
- (evidence lands here)
