---
id: E08-B04
epic: E08
type: bug
title: "A retention-deleted message is not remembered, so a re-delivered copy passes E05's id dedup and is re-inserted with a falsified `created_at`"
layer: backend
size: M
status: blocked
owner_agent: planner-then-builder
model: any
preferred_agent: any
token_estimate: { tier: M, range: "30k-60k" }
severity: S3
priority: { moscow: should, p: P2 }
depends_on: [E08-T06]
blocks: []
traces_to: [FR-MSG-003, FR-STORE-004, FR-STORE-007]
required_context:
  - epics/E08-local-storage/tasks/E08-T06.md
  - lib/features/messaging/domain/receive_message_use_case.dart
  - lib/core/storage/retention_executor.dart
found_by: reviewer (independent) during the E08 end-of-epic bug sweep
found_in: the seam between E05-T02's `ReceiveMessageUseCase` (whose dedup key is "does a row with this id exist in `messages`" — sound when nothing ever deletes a message) and E08-T06's `RetentionExecutor` (this project's first code that does). E05 predates E08 by three epics and could not have known; E08's fence stops at `core/storage`. No task owned the join.
files:
  create: []
  update:
    - lib/features/messaging/domain/receive_message_use_case.dart
    - lib/core/storage/retention_executor.dart
    - test/features/messaging/domain/receive_message_use_case_test.dart
  delete: []
---
# E08-B04 · A deleted message can come back, dated today

## The defect

E05's duplicate check is existence in `messages`:

```dart
// lib/features/messaging/domain/receive_message_use_case.dart:110-116
final existing = await (_db.select(_db.messages)
      ..where((t) => t.id.equals(envelope.id)))
    .getSingleOrNull();
if (existing != null) {
  // Duplicate packet (task file §3.3/EARS-MSG-2)
  return null;
}
```

`RetentionExecutor.deleteMessageItems` removes that row and records nothing
that this check consults. So after a retention pass, a still-circulating
copy of the same message is no longer a duplicate — it is a new message.

Worse than a plain re-insert: the re-inserted row does not carry the
original timestamp.

```dart
// receive_message_use_case.dart:119
final createdAt = DateTime.now().millisecondsSinceEpoch;
```

A message the policy deleted for being 200 days old returns dated **today**,
and sorts to the top of the conversation
(`idx_messages_conversation_created_at`, keyset pagination by `created_at`).
It is then immune to the age policy that removed it, for another 45 days.

## Repro
1. Fresh in-memory `AppDatabase`. Seed 2 `messages`.
2. `RetentionExecutor.deleteMessageItems(['m-00000'])`.
3. Run E05's dedup query for `m-00000` — i.e. what
   `ReceiveMessageUseCase.call` runs before inserting.

## Expected
FR-MSG-003 / EARS-MSG-2 — a duplicate packet is recognized and dropped.
FR-STORE-004 — data the user's policy removed stays removed; a retention
delete is not silently undone by the mesh. If a deleted message *may* legally
return, that is a product decision, and FR-STORE-007's explanation surface
must be able to say so.

## Actual
Reviewer's own probe (`test/core/storage/sweep_probe_test.dart`, scratch,
not committed):

```
dedup lookup for the deleted id returns: null -> treated as a NEW message
```

The packet is accepted, a fresh `messages` row is inserted with
`delivery_state: accepted` and `created_at: now`, and `recordStored` writes
a new `delivery_states` row on top of the orphan one `E08-B03` describes
(same `(message_id, state)` PK, so it merges via the existing `DoUpdate`
rather than failing — the collision is silent).

## Severity
**S3** — genuine cross-epic contract violation and a falsified timeline for
the affected message, but narrow: it needs a manual retention mode active
*and* a peer or relay still holding and re-delivering a message old enough
to have been deleted. Relay payloads are TTL-reclaimed
(`RelayEngine.reclaimPayloads`, E04-B02), which limits how long a copy
survives in the mesh. The blast radius is one message per occurrence, not a
broken feature.

**Reachability note for the 🧍 `bug_priorities` gate:** as with `E08-B01`,
no shipped UI can enable a manual mode today (`E08-T09` is prospective), so
this cannot fire in the current build. Smart Mode never deletes a message
(`OQ-E08-3(a)`), so it cannot trigger this either.

## Fix direction — a product call first, not a code call
Two honest shapes; the human/planner picks:
- **(a) Tombstones.** Record deleted message ids (a small table, or a
  `deleted` flag on a retained stub row) and have `ReceiveMessageUseCase`
  consult it. Correct and durable, but it is a **schema migration** (🧍
  `db_schema_migration`, rule 3) and it trades storage back for the storage
  the deletion reclaimed — which needs to be sized honestly before it is
  chosen, since this is a storage epic.
- **(b) Accept it, and say so.** Declare that a re-delivered message may
  legally return, and record it as a known, disclosed limitation of manual
  retention in the explanation surface — with, at minimum, the `created_at`
  falsification fixed by preserving the envelope's own creation time instead
  of stamping `DateTime.now()`, so a returning message at least lands in the
  right place in the conversation.

Note that `OQ-E08-3`'s original option **(b)** already contemplated
tombstones ("keeping tombstones so E05's dedup, sequence and sync-cursor
guarantees survive") and the human chose **(a)** — Smart Mode excludes
conversation content — which left the tombstone question unanswered for the
manual path rather than settled. This bug is that unanswered half surfacing.

## Regression test (write it first)
Delete a message via `RetentionExecutor`, then drive
`ReceiveMessageUseCase.call` with the same envelope and assert the agreed
behaviour (dropped as duplicate under (a); or re-inserted with the
*original* `created_at` under (b)). Must fail on today's code.

## What this fix does NOT do
- Does **not** change Smart Mode's allow-list, the delivery-state guard, or
  the transactional per-group apply path.
- Does **not** add a schema migration without the 🧍 `db_schema_migration`
  gate cleared first.
- Does **not** touch `SyncCursorService`, sequence-number assignment, or
  `sync_cursors` — a retention delete does not rewind a cursor, and the
  reviewer confirmed that is correct: the backfill path re-requests by
  sequence number and is not affected by this defect.
- Does **not** touch the group receive path or `GroupCryptoService`.
- Does **not** fix the orphan rows — that is `E08-B03`.

## Feedback log
- 2026-09-02 — Fix direction and priority set by the human at the
  `bug_priorities` gate: **option (b) — accept and disclose, do not build
  tombstones.** No schema migration. Fix `receive_message_use_case.dart`
  to preserve the envelope's own creation timestamp instead of stamping
  `DateTime.now()` on re-insert, and record the disclosed limitation
  (a re-delivered message may legally return) in the explanation
  surface's copy or documentation as appropriate. **Priority: P2** — real
  and worth fixing before the epic closes, but narrow (needs an active
  manual mode plus a still-circulating mesh copy, neither reachable
  today).
- 2026-09-03 — Builder (`epic_08_bug_04`) hit a hard blocker implementing
  option (b) exactly as specified below the fold in
  `## Open Questions` / `## Handoff`. Not implemented; `status: blocked`.

## Open Questions

- **Q-E08-B04-1 (blocking): the chosen fix (b) requires data
  `MessageEnvelope`'s wire format does not carry.** The Feedback log's
  instruction is "preserve the envelope's own original creation timestamp
  on re-insert, instead of stamping `DateTime.now()`". I read
  `lib/features/messaging/domain/message_envelope.dart` in full (its
  `serialize`/`deserialize` pair is the one, shared, hand-rolled binary wire
  format both `SendMessageUseCase` and `ReceiveMessageUseCase` use — E05-B01's
  own fix made a point of there being exactly one definition). Its fields are
  `id`, `conversationId`, `sequenceNumber`, `payload` — **there is no
  timestamp field of any kind.** I also checked
  `lib/features/messaging/domain/send_message_use_case.dart`: the sending
  side computes `now.millisecondsSinceEpoch` for its own local `Message
  .createdAt` (line ~229) but never puts that value into the `MessageEnvelope`
  it builds two lines later (line ~237-242) before encrypting. So the
  ciphertext a re-delivering peer holds — the thing `ReceiveMessageUseCase
  .call` actually receives — carries no original wall-clock time at all,
  recoverable or otherwise. "The envelope's own creation timestamp" does not
  exist today; there is nothing in `receive_message_use_case.dart` alone to
  preserve.

  This is not a small ambiguity with a simplest-reading answer (`skills
  /implement`'s "small ambiguity" lane) — it is a contradiction between the
  fix direction's premise and the actual wire contract, and every way I can
  see to close it requires touching a file outside this task's `files:`
  fence and outside the P2/no-migration scope the human already set:
  1. **Add a `createdAt`/`originalSentAtMs` field to `MessageEnvelope`.**
     The only way to make "the envelope's own timestamp" literally true.
     Requires editing `message_envelope.dart` (not in `files:`) *and*
     `send_message_use_case.dart` (not in `files:`) to populate it, changes
     what crosses the wire for every message this app ever sends henceforth
     (a real protocol change, not a bug fix inside two files), and raises a
     compatibility question `envelopeFormatVersion` exists precisely to
     flag: old-format envelopes already in flight/relayed have no such field
     to read. None of this is a DB schema migration, but it is the same
     *kind* of decision — a wire/API contract change — that this task's own
     "What this fix does NOT do" fences out by fencing the files it may
     touch, and I don't think it's mine to just make.
  2. **Derive an approximate original time some other way** (e.g. from
     `sequenceNumber`, or from `RetentionExecutor`'s own knowledge of when it
     deleted the row). Both are worse than the current bug in a different
     way: `sequenceNumber` is monotonic but has no wall-clock meaning
     (`message_envelope.dart`'s own doc comment: "wall-clock time is NOT a
     substitute" — that's *why* it exists), and by the time
     `ReceiveMessageUseCase` runs, the deletion event itself is gone too
     (option (b) was explicitly chosen *not* to keep any record of a
     deletion — that's what distinguishes it from tombstones). I did not
     build either of these; both would be inventing behaviour the task
     never asked for and the human didn't approve.
  3. **Keep `DateTime.now()` and only add the disclosure.** Satisfies the
     "no tombstones, no migration" half of the human's decision but not the
     "concrete, required code fix" half — the Feedback log is explicit that
     the timestamp fix is required, not optional, so I did not silently
     narrow the task to just the documentation half either.

  I did not pick any of these unilaterally — routing to `spec/questions.md`
  as `Q-<AREA>-nnn` is the planner's job per `agent/skills/question
  -resolution/SKILL.md`, not something within a builder's file scope; this
  section is that question, ready for the planner to lift.

## Handoff

- **What I did before stopping:** read this task file in full, `receive_message
  _use_case.dart` (all of it, including the E05-B01 header note already
  flagging a *different* known gap in the same file), `retention_executor.dart`
  in full, `message_envelope.dart` in full, `message.dart`, and the relevant
  slice of `send_message_use_case.dart` (the envelope-construction call site).
  Branched `epic_08_bug_04` off `epic_08` at `49d34c3` as instructed. Did not
  write the regression test or touch any of the three `files:` yet, because
  the regression test the task itself specifies — "assert ... re-inserted
  with the *original* `created_at` from the envelope" — cannot be written
  honestly against production code: the envelope the production dedup/insert
  path actually receives has no `created_at` to read, only my own test's
  omniscient knowledge of what the original row's timestamp was (test setup
  data unavailable to `ReceiveMessageUseCase` in reality). A test that reads
  "from the envelope" a field the envelope doesn't have would either not
  compile against the real `MessageEnvelope`, or require exactly the
  out-of-scope envelope change in Q-E08-B04-1 option 1.
- **Why blocked:** `Q-E08-B04-1` above — the human's chosen option (b) names
  a data source (the envelope's own creation timestamp) that does not exist
  in this codebase's wire format, and every way to make it exist touches
  files this task does not authorize and changes the wire contract in a way
  I read as needing the same kind of explicit human sign-off the schema-
  migration gate already gets for the DB side, not a call I should make by
  picking one of the three options above on my own.
- **What would unblock:** the human/planner picks one of the three options
  in `Q-E08-B04-1` (or a fourth I haven't seen) and either (a) widens this
  task's `files:` to include `message_envelope.dart` +
  `send_message_use_case.dart` with an explicit answer on the
  `envelopeFormatVersion` compatibility question, or (b) narrows the
  Feedback log's instruction to option 3 above (disclosure only, keep
  `DateTime.now()`, drop "concrete, required code fix" wording) and confirms
  that in writing here, or (c) supplies some other original-timestamp source
  I have not considered that stays inside the current `files:` fence.
- **Suggested next:** planner, via `agent/skills/question-resolution`, to
  turn `Q-E08-B04-1` into a routed `Q-STORE-nnn` (or similar) entry in
  `spec/questions.md` and get the human's answer at the same
  `bug_priorities`-style gate that set option (b) in the first place; then
  hand back to a builder (any) to finish under the clarified contract.
- **Last test status:** no tests added or run yet for this task — nothing to
  report red/green on. `flutter analyze`/`flutter test` were not run for this
  change since no production or test code was touched.
