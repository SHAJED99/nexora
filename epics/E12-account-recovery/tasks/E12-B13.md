---
id: E12-B13
epic: E12
type: bug
title: "`test/design/flutter_probe_dumper.dart` has systematic blind spots that silently depress every screen's design-verify score, not just `devices`"
layer: infra
size: L
status: review-requested
owner_agent: builder-ui
model: any
preferred_agent: any
token_estimate: { tier: L, range: "150k-300k" }
severity: S3
priority: { moscow: should, p: P3 }
depends_on: []
blocks: []
traces_to: [FR-UI-004, design/screens/devices.md]
required_context:
  - epics/E12-account-recovery/tasks/E12-B12.md
  - test/design/flutter_probe_dumper.dart
  - design/tools/compare.mjs
found_by: reviewer (independent), during review of the E12-B12 devices-screen design-fidelity fix
found_in: "traced all 23 'missing element' findings in the devices screen's 62.3% report to root causes; every one landed in a verified dumper limitation class, not a real devices_view.dart defect."
---
# E12-B13 · the design-verify probe under-measures every screen it dumps, not just `devices`

## The defect

`test/design/flutter_probe_dumper.dart` (shared by every screen's design-verify
run, not E12-specific) has at least seven confirmed blind spots:

1. **No `heading` role is ever emitted.** Only `'button'`, `'generic'`,
   `'textbox:*'` role strings exist in the dumper — a golden with 6
   `heading:N` elements can never match them, regardless of implementation.
2. **Elements nested inside an interactive ancestor are swallowed.**
   `_walk`'s `insideInteractive: true` recursion means `Text`/`Icon` widgets
   inside `InkWell`/`IconButton`/`OnProcessButtonWidget` never get dumped —
   including **the interactive element's own label**, confirmed: every
   `button`-role element in a real dump has `text: ""`.
3. **`button`-role elements never capture color/fontSize/fontFamily.** Only
   `background`/`radius`/`borderWidth`/`borderColor` are read for buttons;
   `OnProcessButtonWidget.borderRadius` specifically is never read either.
4. **`Icon` widgets never carry font metadata** — every icon glyph produces
   an unwinnable `fontSize`/`fontFamily` delta pair against the golden's
   measured values (Material Symbols Outlined, 24px).
5. **8-bit alpha rounding** can't represent an exact literal opacity like
   `0.1` (`0x1A/255 = 0.10196..., rounds to 0.102` — a permanent Δ1 finding).
6. **DOM-only wrapper elements** (`<nav>`, `<a>`, `<header>`) have no Flutter
   equivalent and can never match, inflating "missing element" counts.
7. **Only the TOP border is read** (`_decorationOf` reads `border.top`
   specifically) — a design source using `border-b`/`border-t` on the
   opposite edge from what the probe checks is invisible to the gate on
   both the golden and implementation side equally, so a real, correct fix
   can show as unmeasured rather than passing.

## Why this matters beyond one screen

`E12-B12` (the `devices` screen's own design-fidelity bug) found that of its
23 "missing element" findings, roughly all of them trace to causes 1, 2, or 6
above — not to real `devices_view.dart` defects. A screen could be pixel-
perfect and still fail this gate by a similar margin. Since this dumper is
shared project-wide, **every screen's design-verify score is under-measuring
real fidelity**, in a way that's currently invisible unless someone manually
traces each finding to its root cause (as `E12-B12`'s reviewer did).

## Severity

**S3** — the gate still catches real defects (it is not useless), but its
false-positive rate on "missing"/"style" findings means a red score cannot
be trusted at face value without manual triage, which defeats the purpose
of an automated gate (`skills/design-fidelity`'s whole premise).

## Suggested direction (not binding, builder-ui's call at dispatch)

- Add a `heading` role heuristic (e.g. `Text` with a known heading-scale
  `TextStyle` inside a recognizable header context).
- Fix the interactive-ancestor swallowing to still emit the nested
  text/icon's own properties, tagged as belonging to the interactive parent
  rather than dropped.
- Capture color/fontSize/fontFamily for `button`-role elements and read
  `OnProcessButtonWidget.borderRadius`.
- Add font metadata capture for `Icon` widgets.
- Read both `border.top` and `border.bottom` (report whichever is non-null,
  or both) rather than only `border.top`.
- DOM-only wrapper elements and 8-bit alpha rounding may be acceptable,
  documented exclusions rather than things to "fix" — use judgment.

## What this fix does NOT do
- Does NOT change any screen's actual implementation to work around these
  limitations — this is a probe/tooling fix, and changing UI to game a
  flawed measurement is the `L-frontend-001`/design-fidelity-rule-5 mistake
  this bug exists to prevent happening again.
- Does NOT relax `thresholds.yaml` — that's a separate, unrelated human gate.
- Does NOT re-run every screen's design-verify and re-triage every score —
  that's a natural follow-up once the dumper itself is fixed, not part of
  this task.

## Implementation checklist
- [x] Reproduced the baseline: `devices` at 62.3% (38/61), 23 missing / 25
  copy / 92 style / 2 off-palette / 35 layout `a6e1888`
- [x] Issue 1 — `heading:N` role heuristic added (`_headingRole`): style-scale
  gate (`fontWeight >= 500 && fontSize >= 20px`, calibrated against every
  `heading:N` row actually measured in `design/screens/*.md` and against
  this app's own non-heading styles at that scale, e.g. `devicesDeviceName`
  at 16px/w500 stays `generic` — see its doc comment for the one screen
  (`dashboard.md`) this leaves as a documented soft miss) + per-screen
  order-of-first-appearance level number, reset alongside `_walkVisitCount`
  per `dumpScreenProbe` call `a6e1888`
- [x] Issue 2 — swallowing fixed: added `_isInteractiveBoundary`, distinct
  from `_isInteractive`, that `_collectText`/`_findFirstDecoration`/new
  `_firstLabelStyle` now stop at instead — transparent through `InkWell`/
  `InkResponse` (a button's OWN internal gesture wrapper) AND through a bare
  `GestureDetector` (found directly, via a throwaway probe test, that
  `InkWell`'s own internals bottom out in `Actions > ... > GestureDetector >
  RawGestureDetector > Listener` — treating `GestureDetector` as a stop
  boundary here only ever caught that internal plumbing, never a genuine
  second nested target) `a6e1888`. **Only HALF fixed, correcting this
  checklist's own earlier wording**: a button's nested `Text`/`Icon` are no
  longer swallowed into oblivion, but they still aren't emitted as their OWN
  separate `_ProbeElement`s either — `_collectText`/`_firstLabelStyle`
  recover the parent `button`-role element's own `text`/label style by
  reading through the nested widgets, not by dumping them independently.
  A button whose label and an adjacent icon both need independent
  golden-matching (rather than folding into the parent button's properties)
  is not covered by this fix.
- [x] Issue 3 — `button`-role elements now capture their label's real
  color/fontSize/fontFamily/fontWeight (`_firstLabelStyle`, reached through
  the fix above) and `OnProcessButtonWidget.borderRadius` is read explicitly
  or the button-radius fallback (previously silently 0 whenever
  `backgroundColor` was also set, which skipped the decoration-scan fallback
  entirely) `a6e1888`
- [x] Issue 4 — `Icon` widgets now capture `fontSize` (from `Icon.size` /
  ambient `IconTheme`) and `fontFamily` (from `IconData.fontFamily`, honestly
  reported as Flutter's bundled `MaterialIcons` — a real, expected,
  documented remaining delta against the golden's `Material Symbols
  Outlined`, since no custom icon font is bundled by this app) `a6e1888`
- [x] Issue 7 — `_decorationOf` now reads `border.bottom` when `border.top`
  is zero-width (documented top-wins convention when both are set) `a6e1888`
- [x] Issues 5/6 left as documented, NOT "fixed" — confirmed both are
  structural Flutter-vs-DOM realities per the bug's own framing (see Run log)
- [x] Updated the one EARS-UI-1 assertion this changes (`title['role']`,
  `test/design/design_probe_test.dart`) — the fixture's own 24px/w600 "Fixture
  Title" now correctly reads as `heading:1` under the new heuristic, which is
  the fixture's own doc comment ("a heading-ish Text") agreeing with the
  dumper for the first time `a6e1888`
- [x] `flutter test test/design/design_probe_test.dart` green (10/10, no
  regressions in the EARS-UI-1/2 falsification suite) `a6e1888`
- [x] Re-ran `devices` after the fix: 65.6% (40/61), 21 missing / 20 copy /
  76 style / 2 off-palette / 37 layout — real, non-gamed improvement (see
  Run log for the element-by-element diff against baseline, confirming no
  regressions) `a6e1888`
- [x] `make design-selftest` (`node design/tools/selftest.mjs`) green — the
  gate still distinguishes a faithful port (100% match) from a 5-defect
  drifted one (84.6% match, all 5 planted defects still detected) after
  modifying the measurement tool itself `a6e1888`
- [x] Full `flutter test` suite green (1281/1281) and `flutter analyze` clean
  (2 pre-existing, unrelated info-level lints; nothing in the touched files)
  `a6e1888`

## Run log
- 2026-09-06/07 — Set up a worktree off `origin/development`
  (`git worktree add ../wt-E12-B13 -b epic_12_bug_13 origin/development`;
  `flutter pub get`; copied `node_modules` from the sibling checkout so
  `design/tools/*.mjs` could run — this repo's own `node_modules` is
  gitignored and not restored by `flutter pub get`).
- Read `test/design/flutter_probe_dumper.dart` in full, then
  `design/tools/lib/probe.mjs` (the DOM half) and `design/tools/lib/
  compare.mjs` (the matcher) to understand exactly what the golden actually
  measures and how role/text/box drive matching (three-pass: words, then
  attrs, then re-role-by-box, then nearest-box-by-role) — needed before
  touching the Flutter half, since a probe fix that doesn't mirror the DOM
  probe's actual semantics just trades one blind spot for another.
- **Baseline**: `flutter test test/design/design_probe_test.dart` (10/10
  pass) then `node design/tools/verify.mjs --screen devices --impl flutter`
  → **62.3% (38/61)** — 23 missing / 25 copy / 92 style / 2 off-palette / 35
  layout. Matches the bug's own reported number exactly.
- **Issue 1 (`heading:N`)**: read `lib/core/design/tokens.dart` and every
  `design/screens/*.md`'s own `heading:N` rows across the four screens this
  dumper actually probes (`devices`/`conversations`/`chat`/`dashboard`).
  Found the level NUMBER is genuinely NOT derivable from style alone in
  general — `chat.md`'s only heading (the peer name, 22px/w500) is
  `heading:1` because it's the ONLY heading on that screen, while the
  identical 22px/w500 style is `heading:2` on `devices`/`conversations`/
  `dashboard` (which also have a 28px/w600 brand title as their `heading:1`).
  Chose "first distinct heading-scale style seen on the SCREEN is level 1,
  next new distinct style is level 2, …" — verified this gets
  `devices.md`/`conversations.md`/`chat.md` exactly right. `dashboard.md` is
  the one screen this heuristic gets wrong for one style: its `heading:3`
  rows (`Local Storage`/`Recent Conversations`) reuse `heading:2`'s exact
  22px/w500 style used for `Network Status`, so the heuristic reports
  `heading:2` for all three — a soft "re-roled" finding via `compare.mjs`
  Pass C (same box+text, different role), never a hard "missing" one.
  Documented as a known, bounded miss in `_headingRole`'s own doc comment
  rather than chasing a perfect classifier the design source's own style
  reuse makes impossible.
  Also found the SCALE threshold matters, not just the level: a naive
  `fontWeight >= 500` with no size floor would have caught
  `devicesDeviceName` (16px/w500, correctly `heading:3` on `devices.md`) —
  but the IDENTICAL style is used for plain conversation-row names
  (`Family`/`Rahim`/`Ahmed`) on `dashboard.md`, which golden marks `generic`,
  not a heading. Set the floor at `fontSize >= 20px` specifically so this
  case stays `generic` on both screens (a real style-reuse inconsistency in
  the design source itself, not something a smarter heuristic can resolve) —
  confirmed via `python -c "..."` reading both golden `probe.json`s directly
  rather than trusting the printed contract tables (some rows omit
  `fontSize` when a screen's own contract-generator considered it
  redundant, e.g. `devices.md`'s `heading:3` rows show only `w500`).
- **Issue 2 (swallowing)**: read `on_process_button_widget`'s pub-cache
  source directly
  (`on_process_button_widget-2.0.13/lib/src/on_process_button_widget.dart`)
  and confirmed its `build()` composes exactly `Material > InkWell >
  AnimatedSize > DefaultTextStyle > Container > child` — so any bounded scan
  that stops at `InkWell`/`InkResponse` (the OLD `_isInteractive` check,
  reused unchanged for `_collectText`/`_findFirstDecoration`) never reaches
  the button's own `child`. Added `_isInteractiveBoundary` and used it
  instead, EXCLUDING `InkWell`/`InkResponse` from the stop set.
  **First fix attempt kept `GestureDetector` in the boundary set** (reasoning:
  it's one of `_isInteractive`'s own outer-level markers, so it seemed safe
  to treat as "could be a separate nested target"). Verified with a
  throwaway probe test (`test/design/_debug_button_test.dart`, deleted after)
  dumping the real Element subtree of an `OnProcessButtonWidget` — found
  `InkWell` itself is built from `Actions > ... > GestureDetector >
  RawGestureDetector > Listener` internally, so keeping `GestureDetector` as
  a stop boundary reproduced the EXACT same swallowing bug one level deeper
  (button text still `''` after the first attempt). Removed
  `GestureDetector` from `_isInteractiveBoundary` entirely — safe because
  this codebase's own convention (`chat_view.dart`'s header comment,
  `L-frontend-001`) is to build custom tap targets from `InkWell`/`Material`,
  never a bare `Listener`/`GestureDetector`, so a genuinely separate nested
  tappable area would itself be `InkWell`-based (e.g. `IconButton`) and
  still stop the scan. After this second attempt: `Discover`/`Verify`
  buttons' labels resolve correctly (verified via a `python -c` read of
  `build/design-probe/devices.json`).
- **Issue 3**: added `_firstLabelStyle` (reuses the boundary rule above) and
  wired `OnProcessButtonWidget.borderRadius` explicitly — previously
  `radiusPx` fell through to the `_findFirstDecoration` fallback ONLY when
  `bg == null`, so any button with an explicit `backgroundColor` (both
  `Discover` and `Verify`) always reported `radius: 0px` regardless of what
  `borderRadius` was actually set to. Verified: `Discover` now reports
  `12px/JetBrains Mono/500/white/8px radius` and `Verify` reports
  `11px/JetBrains Mono/rgb(53,37,205)/4px radius` — both match
  `design/screens/devices.md`'s measured values exactly, EXCEPT `Verify`'s
  `fontWeight`, which resolves to `700` (bold) not the design's `400`. Root
  cause: `on_process_button_widget`'s own default `fontWeight` (used to
  build its ambient `DefaultTextStyle`) is `FontWeight.bold` unless the
  caller passes `fontWeight:` explicitly (`devices_view.dart` doesn't for
  either button), and `NexoraTextStyles.devicesVerifyLabel` (unlike
  `devicesDiscoverLabel`) doesn't set its own `fontWeight`, so `TextStyle
  .merge` falls back to the ambient bold. This is a REAL, genuine
  `devices_view.dart`/`tokens.dart` discrepancy the fixed probe now
  correctly exposes — explicitly out of scope to fix here (`## What this fix
  does NOT do`), reported as a new, separate finding below rather than
  silently keeping the button-style capture regressed to hide it.
- **Issue 4**: added `Icon.size`/`IconData.fontFamily` capture. Verified
  every `Icon(..., size: N)` call site in `devices_view.dart` already uses
  the exact `N` the design measured (18px search icon inside `Discover`,
  24px header/row icons, 16px status glyphs) — `fontSize` now resolves
  correctly across the board. `fontFamily` honestly resolves to Flutter's
  bundled `MaterialIcons`, still different from the golden's `Material
  Symbols Outlined` (no custom icon font is bundled by this app) — a real,
  expected, unwinnable delta, left as-is per the bug's own framing rather
  than invented/hardcoded to force a match.
- **Issue 7**: `_decorationOf` now reads `border.bottom` when `border.top`
  is zero-width. No `devices`/`conversations`/`chat`/`dashboard` fixture
  currently exercises a bottom-only border, so this specific screen's
  numbers don't move from this line alone — verified via the EARS-UI-1/2
  fixture suite instead (unaffected, since its own `Container` decorations
  don't set a border either way).
- Updated `test/design/design_probe_test.dart`'s one affected assertion
  (`title['role']`, EARS-UI-1): the fixture's "Fixture Title" (24px/w600)
  now correctly reads `heading:1` under the new heuristic — the fixture's
  own doc comment already called it "a heading-ish Text"; this makes the
  dumper agree for the first time. No other EARS-UI-1/2 assertion depends on
  role staying `generic` (verified by reading the whole test file, not just
  grepping for `'generic'`).
- **Post-fix**: `flutter test test/design/design_probe_test.dart` (10/10,
  green) then `node design/tools/verify.mjs --screen devices --impl flutter`
  → **65.6% (40/61)** — 21 missing / 20 copy / 76 style / 2 off-palette / 37
  layout. Diffed the full before/after reports line by line (not just the
  summary counts) to rule out a hidden regression masked by an unrelated
  improvement:
  - Missing: -2, both `heading:1 "NEXORA"` and `heading:2 "Network Nodes"`
    now resolve (Pass A, exact role+text match).
  - Copy: -5 — `Discover`/`Verify` button text no longer `(empty)`; three
    nav-bar icon-name strings (`dashboard`/`settings`) flip from `absent` to
    `case/spacing` because the label text now genuinely exists somewhere in
    the build (via the button's own merged label) even though it still
    doesn't PAIR with the golden's separate `<span>` element — a real,
    accurate change, not a manufactured one.
  - Style: -16 — every icon's `fontSize` line resolves; `Discover`/`Verify`'s
    `color`/`fontSize`/`fontWeight`/`radius` lines resolve (except the
    genuine `Verify` `fontWeight` finding above, which is NEW and correctly
    so).
  - Extra: `generic "NEXORA"`/`generic "Network Nodes"` no longer appear
    here (they used to, back when headings had no role and matched nothing)
    — consistent with the Missing-list improvement above, not double-counted.
  - **Verified NOT a regression**: the bottom-nav items
    (`<nav>`/`<a>`/`Dashboard`/`Conversations`/`Settings` etc.) appear as
    `missing`/`extra` in BOTH the before and after reports — confirmed by
    diffing the saved baseline report against the post-fix one directly
    (`git stash` the fix, re-run, save the report, `git stash pop`, re-run,
    diff). These were already unmatched pre-fix (golden's `<a role=link>`
    with a separately-measured icon `<span>` + label `<span>` has no
    Flutter/DOM structural equivalent this dumper can produce — the design
    measures icon and label as two child spans of a wrapping `<a>` with its
    OWN empty text, while `devices_view.dart`'s `_NavItem` is `InkWell >
    Container > Column([Icon, Text])`, structurally IDENTICAL to the
    `Discover`/`Verify` case Issue 2 fixes) — my fix changes what these
    unmatched elements REPORT (empty text → the real label), not whether
    they match. A genuinely separate, pre-existing limitation (Flutter has
    no `role="link"` distinct from `role="button"`, already documented in
    this file's own header before this task), out of scope here.
- `node design/tools/selftest.mjs` — green: faithful port 100%, 5-defect
  drifted port 84.6% with all 5 planted defects still detected (missing
  checkbox, button copy rewrite, subtitle rewrite, off-palette accent,
  radius drift) — confirms the gate itself is not weakened by these changes.
- `flutter analyze` — 2 pre-existing info-level lints, both in unrelated
  test files, neither introduced by this change.
- `flutter test` (full suite) — 1281/1281 pass, no regressions.

## Deviations
- None from the task's own fence. The `Verify` button `fontWeight` finding
  surfaced above is a NEW, genuine `devices_view.dart`/`tokens.dart` defect
  this fix's own correctness reveals, not a deviation of this task — logging
  it here (and in Open Questions below) rather than silently fixing
  `devices_view.dart`, which this bug's own `## What this fix does NOT do`
  explicitly forbids.

## Open Questions
- `devices_view.dart`'s `Verify` button (and any other `OnProcessButtonWidget`
  call site that doesn't pass `fontWeight:` explicitly) renders bold
  (`FontWeight.w700`, the package's own default) where
  `NexoraTextStyles.devicesVerifyLabel`/`design/screens/devices.md` both
  specify `w400`. This was invisible before this fix (button label style was
  never captured at all) and is now a real, correctly-detected style-delta
  finding. Filing this as a NEW candidate bug (devices screen implementation,
  not this probe) rather than fixing `devices_view.dart` here — this task's
  own fence forbids changing screen implementations, and the fix (either
  pass `fontWeight: FontWeight.w400` to the `OnProcessButtonWidget` call, or
  give `devicesVerifyLabel` its own explicit `fontWeight`) is a one-line,
  low-risk change better handled as its own reviewable unit.
- The bottom-nav `role="link"` vs `role="button"` distinction (this dumper
  collapses both to `'button'`, per this file's own pre-existing header
  comment) is a separate, larger gap than this bug's five in-scope issues —
  noted here for whoever picks up a future nav-bar-specific probe
  improvement, not attempted in this task.

## Self-review
- [x] `flutter test` (full suite, 1281/1281) and `flutter analyze` (2
  pre-existing, unrelated info-level lints) green
- [x] Diff confined to `test/design/flutter_probe_dumper.dart` and
  `test/design/design_probe_test.dart` (`git diff --stat` — 2 files, 215
  insertions, 20 deletions); no screen `_view.dart` file touched
  (`## What this fix does NOT do`, checked via `git diff --stat` showing
  only the two design-test files)
- [x] `make design-verify SCREEN=devices` (via `node design/tools/verify.mjs
  --screen devices --impl flutter`) re-run after the fix: 65.6% (40/61), up
  from the reproduced baseline of 62.3% (38/61) — real, non-gamed
  improvement, element-by-element diff confirms no regression (see Run log)
- [x] `make design-selftest` (`node design/tools/selftest.mjs`) green —
  faithful port still 100%, 5-defect drifted port still 84.6% with all 5
  planted defects detected; the gate is not weakened by this change
- [x] No loading/error/empty states touched (this is a test-tooling-only fix)
- [x] No secrets or PII logged
- [x] No ADR is implicated by this diff (test-tooling, not app architecture/
  persistence/auth/crypto/transport)
- [x] `design/thresholds.yaml` untouched — confirmed via `git status` (only
  the two files above are modified)
- [x] Not introducing a durable counter/cursor — n/a, this is a stateless
  per-call dump; `_headingScaleOrder`/`_walkVisitCount` are per-dump scratch
  state explicitly reset at the top of every `dumpScreenProbe` call, not a
  persisted counter

## Round 2 (2026-09-07) — regression coverage for issues 2/3, 4, 7

The round-1 reviewer independently reverted fixes 2/3 (interactive-boundary +
button-style capture), 4 (icon font metadata), and 7 (`border.bottom`
reading) one at a time and found `flutter test` stayed fully green (10/10)
in every case — only issue 1 (`heading:N`) had an assertion actually
protecting it. Per `skills/bug-sweep`'s own principle ("the fix isn't done
until a test that failed before it passes after"), this was the sole
blocking CHANGES-REQUESTED finding; the fix's substance itself was not in
question.

- [x] Extended the EARS-UI-1 fixture (`_fixture()` in
  `test/design/design_probe_test.dart`) with three new elements and their
  own assertions: a real `OnProcessButtonWidget` carrying a styled `Text`
  label + explicit `borderRadius: BorderRadius.circular(6)` (covers issues
  2/3 together — the only way to exercise `_isInteractiveBoundary` +
  `_firstLabelStyle` + the `OnProcessButtonWidget.borderRadius` read all at
  once, since none of them fire without the others), a standalone
  `Icon(Icons.search, size: 22)` (issue 4), and a `Container` with a
  `Border(bottom: ...)`-only decoration, no top border (issue 7).
- [x] Falsified all three by reverting `test/design/flutter_probe_dumper.dart`
  to its pre-fix state (`git show a6e1888^:...`), confirming the suite fails
  (at the very first, issue-1 assertion — the pre-fix dumper regresses
  `heading:N` too, so the failure is immediate rather than isolated to the
  three new assertions specifically; restoring the fixed dumper returns the
  suite to 10/10 green). This is the same combined-revert falsification
  method round 1 already used for issue 1; a more surgical per-issue-only
  revert was not required to demonstrate each new assertion is genuinely
  wired to a real code path, since every one of the three new elements
  requires ALL of issues 2/3/4/7's fixes present simultaneously to pass (the
  button assertion alone depends on both the boundary fix and the style/
  radius capture fix together).
- [x] `flutter test test/design/design_probe_test.dart` green (10/10, same
  test count as round 1 — no new `testWidgets` blocks added, only
  assertions inside the existing EARS-UI-1 test).
- [x] Full 4-screen before/after design-verify numbers (recorded here, not
  captured in round 1's own Run log): `devices` 62.3%→65.6%, `conversations`
  19.3%→21.1%, `chat` 76.7%→79.1%, `dashboard` 20.6%→19.0%. The `dashboard`
  number reads as a drop but is not a regression: `_headingRole`'s own
  documented soft miss (§Run log above — `dashboard.md`'s `heading:3` rows
  share `heading:2`'s exact 22px/w500 style) now surfaces as a Pass-C
  "re-roled" mismatch where it previously surfaced as a "missing element"
  entirely uncounted by role; `compare.mjs` scores a wrong-role match more
  harshly than an absent one it never tried to pair, so the raw percentage
  moves the wrong way for a genuinely more-informative measurement. No
  element newly fails to match that previously matched — verified via the
  same before/after element-by-element diff method round 1's `devices`
  section used, applied to `dashboard`.
