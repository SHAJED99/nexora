---
id: E12-B06
epic: E12
type: bug
title: "No `source: derived` screen is registered in `design/sources.yaml` — 13 built screens across E06-E12, including both E12 contracts, have never been gated"
layer: frontend
size: M
status: review-requested
owner_agent: planner
model: any
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
severity: S3
priority: { moscow: should, p: P2 }
depends_on: []
blocks: []
traces_to: [design/gaps.md GAP-028, FR-RECOVER-001]
required_context:
  - design/sources.yaml
  - design/gaps.md
  - design/screens/device-enrollment.md
  - design/screens/device-enrollment-approval.md
  - test/design/design_probe_test.dart
  - agent/skills/design-fidelity/SKILL.md
found_by: T03's cross-model reviewer (carried forward as tracker C1'); independently re-confirmed during the E12 end-of-epic bug sweep
found_in: project-wide planner-owned tooling (`design-fidelity` §1 Ingest), outside every builder task's `files:` fence. Not an E12 defect; surfaced by E12 because E12 is the first epic whose *only* screens are derived.
files:
  create: []
  update:
    - design/sources.yaml
    - test/design/design_probe_test.dart
  delete: []
---
# E12-B06 · derived screens are contracted but never gated

## The defect

`design/sources.yaml` registers **7 screens, all `source: ui`**. None of the
`source: derived` contracts written and built across E06-E11 are registered —
including E12's own two, from the approved `GAP-028`:

- `design/screens/device-enrollment.md`
- `design/screens/device-enrollment-approval.md`

So `make design-verify SCREEN=device-enrollment` fails with "no screen
matched" rather than gating anything. `skills/design-fidelity` §3 is explicit
that this should not be true:

> Derived screens get a contract too — hand-written, marked `source: derived`.
> Once built, extract the built screen as its own golden so it is regression-
> gated from then on like everything else.

The second half of that sentence has never been executed for any derived
screen.

## Consequence for E12 specifically

E12 shipped **two** UI tasks, both carrying `design_contract:` frontmatter,
and **neither could ever run its own gate**. Both were hand-verified instead
(codepoint-exact copy match, token-by-token style match, confirmed by an
independent reviewer pass) — real verification, but ungated and
non-regression-protected. Any future change to these two screens is
unprotected.

This also blocks resolving tracker item **C3** (`DeviceEnrollmentView`'s
primary button rendering 342px against the contract's measured 326px): that
discrepancy cannot be adjudicated until `device-enrollment` has a golden of
its own to measure against.

## Repro

1. `make design-verify SCREEN=device-enrollment` → "no screen matched".
2. `grep 'source: derived' design/sources.yaml` → no matches.

## Expected

Every built screen with a contract is registered and gated, derived or not.

## Severity

**S3** — the screens are believed correct and were manually verified; what is
missing is the machine gate and its regression protection. No user-visible
defect follows directly from this today.

## Note — this is also a sharding defect worth a retro lesson

`E12-T03`'s own contract implied that extraction happens as part of the build
task. It does not: registering a derived screen in `design/sources.yaml` plus
adding a `test/design/design_probe_test.dart` probe block is planner-owned
(`design-fidelity` §1 Ingest). A builder task that carries a
`design_contract:` it structurally cannot verify is a task that cannot meet
its own DoD. Route to `skills/retro`.

## What this fix does NOT do

- Does **not** change any built screen's widget code to improve its score.
  If registering a screen reveals a red gate, that is a **new finding to file**,
  not a licence to reshape the widget tree (design-fidelity rule 5,
  `L-frontend-001`).
- Does **not** relax `thresholds.yaml` (rule 2 — human gate).
- Does **not** hand-edit any generated contract table (rule 1).
- Does **not** fix the `devices` probe fixture (`E12-B05`, separate).

## Implementation checklist
- [x] Added a `device-enrollment` probe block to
  `test/design/design_probe_test.dart` — constructs `DeviceEnrollmentController`
  with a stub `FirebaseMetadataService` (`_NeverGrantsFirebaseMetadataService`,
  same seam-stubbing pattern `device_enrollment_controller_test.dart`'s own
  `_StubFirebaseMetadataService` uses) and a 30s `pollInterval`/1000
  `maxPolls` so no poll tick or timeout fires during the probe's bounded
  pumps, dumps `GetMaterialApp(home: const DeviceEnrollmentView())`,
  capturing the `waiting` state (the route's own first frame) `(pending commit)`
- [x] Added a `device-enrollment-approval` probe block — a SEPARATE
  `setUp`/db from the existing `devices` group (never touches E12-B05's own
  fixture), empty relationships, one `TransportDevice` seeded directly into
  `DevicesController.pendingEnrollments` so the dump captures the
  pending-enrollment row's own elements (DEA1-DEA9) alongside `devices.md`'s
  unchanged chrome, dumps `GetMaterialApp(home: const DevicesView())`
  `(pending commit)`
- [x] Explicit `controller.onClose()` at the end of the `device-enrollment`
  test body (not `addTearDown`/the outer `tearDown()`) — required to cancel
  `DeviceEnrollmentController`'s `Timer.periodic` before flutter_test's own
  end-of-test pending-timer assertion, which fires before either of those
  run `(pending commit)`
- [x] `flutter test test/design/design_probe_test.dart` green (12/12, up
  from 10/10 — the two new probes, no regressions in the existing 10)
  `(pending commit)`
- [x] Registered both screens in `design/sources.yaml` as `source: derived`,
  `viewports: [390x844]`, `states: [{ name: default }]`, matching the
  existing 7 `source: ui` entries' shape; `design_path`/`impl_path` marked
  `n/a` with a doc comment explaining `verify.mjs`'s Flutter-probe path
  never reads `design_path` `(pending commit)`
- [x] Extracted each screen's own golden from the FIRST successful
  `make design-probe` dump (`skills/design-fidelity` §3's instruction) —
  copied `build/design-probe/device-enrollment.json` →
  `design/golden/device-enrollment/default@390x844/probe.json` and
  `build/design-probe/device-enrollment-approval.json` →
  `design/golden/device-enrollment-approval/default@390x844/probe.json`.
  **Not listed in this task's own `files:` frontmatter — see §Deviations.**
  `(pending commit)`
- [x] `node design/tools/verify.mjs --screen device-enrollment --impl
  flutter` → PASS, 100% (4/4) `(pending commit)`
- [x] `node design/tools/verify.mjs --screen device-enrollment-approval
  --impl flutter` → PASS, 100% (22/22) `(pending commit)`
- [x] Full `flutter test` suite green (1283/1283, up from 1281/1281 — the
  two new probe tests, no regressions) and `flutter analyze` clean (2
  pre-existing, unrelated info-level lints; nothing in the touched files)
  `(pending commit)`

## Run log
- Fetched `origin/epic_12` (tip `a767a8d`, E12-B13 merged — its dumper
  blind-spot fixes are in place before this task's screens are ever dumped)
  and cut `epic_12_bug_06` from it in its own worktree
  (`wt-E12-B06`), per this task's own instructions.
- Read both derived contracts in full
  (`design/screens/device-enrollment.md`, `design/screens/
  device-enrollment-approval.md`) plus `DeviceEnrollmentView`/
  `DeviceEnrollmentController` and `DevicesView`'s `_PendingEnrollmentRow`/
  `DevicesController.pendingEnrollments` to find the exact, already-built
  surface each contract describes — confirmed `device-enrollment-approval`
  is not a route/view at all but a conditional row `DevicesView` renders
  when `pendingEnrollments` is non-empty.
- Mirrored `device_enrollment_controller_test.dart`'s own
  `pumpEnrollmentView` construction (stub Firebase service + long
  `pollInterval`/high `maxPolls`, freezing the controller on its first
  `waiting` frame) for the new probe block, since `DeviceEnrollmentController
  .onInit()` starts a real `Timer.periodic` immediately on `Get.put`.
- First attempt at the `device-enrollment` probe failed flutter_test's own
  `!timersPending` end-of-test invariant (`A Timer is still pending even
  after the widget tree was disposed`) — the outer group's `tearDown(() =>
  Get.reset())` and an `addTearDown(controller.onClose)` registered
  alongside `Get.put` both run too late for this specific check (it fires
  inside `_runTestBody` before either callback queue drains). Fixed by
  calling `controller.onClose()` explicitly as the last line of the
  `testWidgets` body itself, immediately after `dumpScreenProbe` returns.
- `flutter test test/design/design_probe_test.dart` → 12/12 green (see
  checklist).
- `flutter test/design-probe/*.json` dumps inspected directly
  (`renderError: null` for both; `device-enrollment` 4 elements matching
  DE1-DE4, `device-enrollment-approval` 22 elements including the seeded
  pending row's DEA1-DEA9 plus `devices.md`'s own unchanged header/title/
  bottom-nav chrome).
- Copied both dumps into `design/golden/<id>/default@390x844/probe.json` —
  no `page.png` written (not required: `verify.mjs`'s flutter-probe branch
  never calls `pixelDiff`, guarded by the same `useFlutterProbe` ternary
  that selects the probe source).
- `node design/tools/verify.mjs --screen device-enrollment --impl flutter`
  → **PASS, 100% (4/4)**.
- `node design/tools/verify.mjs --screen device-enrollment-approval --impl
  flutter` → **PASS, 100% (22/22)**.
  These 100% scores are an expected, honest consequence of this task's own
  method (the golden IS the first dump of the already-built, previously
  hand-verified screen) — not a chased/gamed number. No screen widget code
  was touched to reach it (§What this fix does NOT do). From this point
  forward both screens are regression-gated: a FUTURE change to either
  view that drifts from this golden will show up as a real finding.
- Full `flutter test` → 1283/1283 green. `flutter analyze` → 2 pre-existing,
  unrelated info-level lints only (same two `L-frontend`-unrelated findings
  the E12-B13 Run log already recorded), nothing in the touched files.

## Deviations
- **Golden files created outside the task's own `files:` frontmatter**
  (`design/golden/device-enrollment/default@390x844/probe.json`,
  `design/golden/device-enrollment-approval/default@390x844/probe.json`).
  This task's frontmatter lists only `design/sources.yaml` and
  `test/design/design_probe_test.dart` under `files.update`, with
  `files.create: []` — but registering a screen with no corresponding
  golden makes `make design-verify SCREEN=<id>` fail outright ("no golden —
  run make design-extract"), which would leave this bug's own stated defect
  (both screens ungated) unfixed. The task's own body text and required
  reading both explicitly direct this exact step ("Once built, extract the
  built screen as its own golden", `design-fidelity` §3, quoted verbatim in
  this task's own description) — the `files:` fence is itself the gap here,
  the same category of sharding miss this task's own "Note — this is also
  a sharding defect" section already flags for `E12-T03`'s missing
  extraction step. Proceeding with the two golden files as the simplest,
  necessary completion of this task's stated goal, logged here per
  `skills/implement`'s "small ambiguity" guidance rather than stalling.
- No other deviation. No screen widget code changed (`DeviceEnrollmentView`,
  `DevicesView`/`DevicesController` all untouched — `git diff --stat`
  confirms). `design/thresholds.yaml` untouched. No generated contract
  table hand-edited.

## Open Questions
- Both derived screens now pass at 100% because their golden IS their own
  first build — this closes the gate-registration gap this bug describes,
  but it does **not** independently re-verify the two screens against the
  ORIGINAL design intent the way a `source: ui` screen's golden (extracted
  from the design source itself) does. Tracker item **C3**
  (`DeviceEnrollmentView`'s primary button rendering 342px against the
  contract's measured 326px) is **not** resolved by this task — a 342px
  button is exactly as present in the new golden as it is in the build,
  since both are the same artifact. Adjudicating C3 needs a human/reviewer
  comparison of the golden's own measured box against
  `device-enrollment.md`'s own `DE4` row (`326×58`), not another
  design-verify run — flagging this explicitly rather than letting the
  100% score be read as "C3 is resolved."
- `device-enrollment`'s registered `default` state captures only `waiting`;
  the contract's other two states (`denied`, `no-recovery-notice`) have no
  golden of their own, because `verify.mjs`'s Flutter-probe path resolves
  one dump file per screen id, not per state. A future task that wants
  those two states gated needs either a per-state dump filename convention
  in the probe/verify tooling, or a separate registered screen id per
  state — flagging as a follow-up, not attempted here (outside this bug's
  own fence, which is about registration, not extending the gate's own
  state model).

## Self-review
- [x] `flutter test` (full suite, 1283/1283) and `flutter analyze` (2
  pre-existing, unrelated info-level lints) green
- [x] Diff confined to `design/sources.yaml`, `test/design/
  design_probe_test.dart`, plus the two new golden `probe.json` files
  (§Deviations explains the last two); no screen `_view.dart`/
  `_controller.dart` file touched
- [x] `make design-verify SCREEN=device-enrollment IMPL=flutter` (via
  `node design/tools/verify.mjs --screen device-enrollment --impl flutter`)
  → PASS 100% (4/4)
- [x] `make design-verify SCREEN=device-enrollment-approval IMPL=flutter`
  → PASS 100% (22/22)
- [x] No loading/error/empty states touched — this task only registers
  already-built, already-hand-verified screens; no new UI state introduced
- [x] No secrets or PII logged
- [x] No ADR implicated by this diff (design-tooling registration, not app
  architecture/persistence/auth/crypto/transport)
- [x] `design/thresholds.yaml` untouched — confirmed via `git status`
- [x] No durable counter/cursor introduced — n/a
- [x] No `.isIn(ids)`/raw SQL `IN (...)` introduced — n/a, no database code
  touched
