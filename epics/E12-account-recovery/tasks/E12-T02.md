---
id: E12-T02
epic: E12
type: feature
title: Detect and surface a pending new-device enrollment request on the Devices screen
layer: frontend
size: M
status: review-requested
owner_agent: builder-ui
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: must, p: P2 }
depends_on: [E12-T01]
blocks: []
traces_to: [FR-RECOVER-001]
required_context:
  - epics/E12-account-recovery/tasks/E12-T01.md
  - design/screens/device-enrollment-approval.md
  - design/screens/devices.md
  - lib/features/devices/presentation/devices_controller.dart
  - lib/features/trust/data/relationship_repository.dart
design_contract: design/screens/device-enrollment-approval.md
files:
  create:
    - test/features/devices/presentation/devices_controller_enrollment_test.dart
  update:
    - lib/features/devices/presentation/devices_controller.dart
    - lib/features/devices/presentation/devices_view.dart
  delete: []
started_at: 2026-09-05
completed_at: 2026-09-05
executed_by: sonnet (claude-sonnet-5)
reviewed_at:
reviewed_by:
review_outcome:
---
# E12-T02 · Detect and surface a pending new-device enrollment request

## 1. Goal
Close `FR-RECOVER-001`'s "existing trusted device shall authorize the new
device's enrollment" half — the existing device's side of the journey.

## 2. Business logic
`FR-RECOVER-001`. `DevicesController._onDeviceDiscovered`
(`devices_controller.dart:~150`) already evaluates every newly-discovered
peer via `EvaluateConnectionRequestUseCase`, which defaults an
never-seen device to `RelationshipState.unknown` — the SAME treatment
whether the peer is a stranger or this account's own new device
enrolling. This task adds one more check before that default applies:
if the discovered device id is in `E12-T01`'s `readOwnDeviceIds(uid)`
result AND has no local relationship entry yet
(`RelationshipRepository.get(deviceId) == null`), it is this account's
own device enrolling, not an unknown stranger — render it as a pending
enrollment request (`design/screens/device-enrollment-approval.md`),
not the generic unknown-peer treatment.

Approving or denying reuses `DevicesController`'s OWN EXISTING methods —
`verify(deviceId)` (promotes to trusted/allowed, already wired to
`RelationshipRepository.upsert` via `RelationshipSyncService.push`) and
`block(deviceId)` — no new trust-recording logic. This task is UI
classification + a design contract's row, not a new backend mechanism.

## 3. What this task does
- `DevicesController` gains a way to know "is this discovered device id
  one of my own account's registered devices with no relationship yet" —
  calling `E12-T01`'s `readOwnDeviceIds` once per discovery cycle (cache
  the result for the session; it is a best-effort hint, not something to
  re-fetch on every single discovery event) and cross-referencing against
  `RelationshipRepository.get`.
- `device-enrollment-approval.md`'s row (device platform icon + name +
  short code, `Awaiting your approval` status label, `Approve`/`Deny`
  buttons) renders at the TOP of the existing device list for any device
  classified as a pending enrollment request — `devices_view.dart` gains
  this row rendering, `devices.md`'s own generated element table and
  every other row are unchanged (design-fidelity rule 1: never hand-edit
  a generated contract to accommodate a new row; this contract's row is
  read from `device-enrollment-approval.md` instead).
- `Approve` calls the controller's existing `verify(deviceId)`; `Deny`
  calls the existing `block(deviceId)`. Both already exist; this task
  only adds the classification that decides WHEN to show this row instead
  of the ordinary unknown-peer treatment, and wires the two existing
  buttons to it.

## 4. What this task does NOT do
- Does **not** add any new trust-recording mechanism — `verify`/`block`
  and everything under them (`RelationshipRepository`,
  `RelationshipSyncService`) are completely unchanged.
- Does **not** send any notification (push or otherwise) about a pending
  request — `design/gaps.md` GAP-028 explicitly deferred this; a request
  is only visible while the Devices screen is open and discovery is
  active.
- Does **not** change `EvaluateConnectionRequestUseCase` or
  `evaluate_connection_request_use_case_test.dart` — this task's
  classification happens in `DevicesController`, before that use case's
  own unrelated evaluation is even reached for a device classified as a
  pending enrollment.
- Does **not** touch `E12-T03`'s side (the new device's own waiting
  screen) — this task is the existing/trusted device's screen only.
- Does **not** change what a normal `Unknown`/`Verify` row looks like or
  does for an actual stranger peer — only devices classified as "my own
  account's device, no relationship yet" get the new treatment.

## 5. Contract

### Functions
```yaml
- signature: "DevicesController._classifyDiscoveredDevice(deviceId: String) -> _DeviceClassification"
  params: { deviceId: "a newly-discovered peer's device id" }
  returns: "a new internal enum/class distinguishing pendingEnrollment from the existing unknown/known handling -- exact shape is this task's own implementation choice, verify DevicesController's existing structure before naming it"
  purpose: "the one new decision point this task adds before the existing evaluation logic runs"
```

### UI
- **Design contract:** `design/screens/device-enrollment-approval.md`
- Route: none — a row on the existing `/devices` route.
- States required: the contract's own single `pending` state, rendered
  per discovered device classified as a pending enrollment; absent
  otherwise (no empty/loading/error state of its own — this row simply
  doesn't render when there is no pending request).
- Gate: `make design-verify SCREEN=devices` must stay green — this row is
  NOT part of `devices.md`'s own measured contract (design-fidelity's own
  documented shape for a derived addition to a measured screen); expect
  it to be invisible to that gate, exactly as `dashboard.md`'s
  `warning-expanded` derived state documents for itself. Read the
  collapsed/base screen against `devices.md`'s generated table as usual.

## 6. Risks
- `readOwnDeviceIds` is best-effort and can return an empty/incomplete
  set on a flaky connection — a real enrolling device might briefly NOT
  be classified as one and fall through to the ordinary unknown-peer
  treatment. This is an acceptable degraded case (task §2's own framing:
  `FR-RECOVER-001` says "where possible"), not a bug to engineer around
  with retries/polling loops in this task.
- Caching `readOwnDeviceIds`'s result per session risks staleness if a
  device is added to the account mid-session — acceptable for the same
  "where possible" reason; do not add a refresh timer (`E10`'s one-tick
  discipline — no new polling driver for a best-effort hint).
- Read `E06-B01` before laying out the new row — a trailing
  button-pair-after-a-name is exactly the `RenderFlex` overflow shape
  that bug fixed elsewhere in this codebase; the name must stay the
  flexible/ellipsis child, the button pair fixed-width.

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS id `cb0e0fc`
- [x] read `devices_controller.dart`/`devices_view.dart` in full; confirm
      exact current structure before adding the classification step `cb0e0fc`
- [x] classification step + caching of `readOwnDeviceIds`'s result `cb0e0fc`
- [x] `device-enrollment-approval.md`'s row, wired to existing
      `verify`/`block` `cb0e0fc`
- [x] `make design-verify SCREEN=devices` still green -- see §Run log:
      the Node tool ran, but a pre-existing, environment-only limitation
      (confirmed present on the unmodified `epic_12` baseline, byte-for-byte
      identical failure set) means the gate does not report a clean PASS
      for this screen in this sandbox at all, independent of this task's
      diff `cb0e0fc`

## 8. Acceptance criteria (EARS) + test plan
- **EARS-RECOVER-5**: WHEN a discovered device id is present in this
  account's own device list AND has no local relationship entry, THE
  system SHALL classify it as a pending enrollment request rather than an
  unknown peer. (FR-RECOVER-001)
- **EARS-RECOVER-6**: WHEN a discovered device id is NOT in this
  account's own device list, THE system SHALL classify it exactly as
  before this task (unchanged unknown-peer treatment). (FR-RECOVER-001)
- **EARS-RECOVER-7**: WHEN the pending-enrollment row's `Approve` is
  tapped, THE system SHALL call the existing `verify(deviceId)` path;
  `Deny` SHALL call the existing `block(deviceId)` path. (FR-RECOVER-001)

### Tests
- `test_EARS_RECOVER_5_own_account_device_with_no_relationship_is_pending_enrollment`
- `test_EARS_RECOVER_6_foreign_device_id_unaffected`
- `test_EARS_RECOVER_7_approve_calls_verify_deny_calls_block`

### Manual
1. Sign in on a second physical/emulated device with the same test
   account, keep it discoverable → confirm the first device's Devices
   screen shows the pending-enrollment row, not an `Unknown` row.
2. Tap `Approve` → confirm the row disappears and the device appears as a
   normal trusted row.

## 9. Self-review + Definition of Done

### Self-review
- [x] Every §7 item done, with commit hashes
- [x] `flutter test && flutter analyze` green (1157/1157 tests pass;
      analyze reports only one pre-existing, unrelated info-level note in
      `test/core/calls/call_migration_controller_test.dart`, a file this
      task never touches)
- [x] Diff confined to `files:`; §4 respected
- [ ] `make design-verify SCREEN=devices` green -- NOT achieved; see
      §Deviations and §Run log for the full investigation and evidence
      that this task's diff is not the cause
- [x] Existing `devices_controller_test.dart` suite still green unmodified

**Deviations from spec/design** (none, or list):
- `make design-verify SCREEN=devices` does not report a clean PASS in
  this sandbox, for reasons that predate and are independent of this
  task's diff:
  1. `node design/tools/verify.mjs --screen devices` (the browser/DOM
     path) times out waiting for `localhost:3000` -- no dev server runs
     in this sandbox, so the DOM impl path is unusable here regardless of
     any task's diff.
  2. `--impl flutter` reads `build/design-probe/devices.json`, produced by
     `flutter test test/design/design_probe_test.dart`. That specific
     test file's own `devices` group never registers `TransportService`
     before calling `DevicesBinding().dependencies()` (unlike
     `devices_view_test.dart`, which does) -- `DevicesController`'s
     lazy construction then throws resolving `Get.find<TransportService>()`
     during the first build, which the probe dumper (by design, per its
     own doc comment on `RenderFlex`-style failures) records honestly as
     `"renderError": true` and an empty `elements: []`, rather than
     walking a broken tree. **Confirmed on the unmodified `epic_12`
     baseline via `git stash`, before writing a single line of this
     task's code**: the identical empty/`renderError:true` dump is
     produced with none of this task's changes present -- a pre-existing
     gap in `test/design/design_probe_test.dart`'s own `devices` fixture
     (not in this task's `files:` list, so not something this task may
     fix), unrelated to `DevicesController`/`devices_view.dart`.
  3. To still get a real signal on this task's own diff, a temporary,
     never-committed diagnostic test (mirroring `devices_view_test.dart`'s
     own correct `TransportService` registration) was used to produce a
     working probe dump, fed to `verify.mjs --impl-probe <path>`. Result:
     **62.3% match (38/61), with 23 missing elements, 26 copy mismatches,
     103 style deltas, 4 off-palette tokens, 34 layout deltas** -- and
     then, with the SAME diagnostic harness, `git stash` of only this
     task's two changed files and a re-run produced the **exact same
     23/26/103/4/34 numbers, byte-for-byte**, against `devices.md`'s own
     base contract. This proves this task's diff introduces zero new
     `devices.md`-base-contract regressions; the entire failure set
     (icon-glyph/font mapping in the headless Flutter probe, GAP-003's
     already-documented device-name/subtitle placeholders, and generic
     Flutter-test font substitution) is a pre-existing Flutter-probe-path
     limitation this sandbox has for the `devices` screen's OWN base
     contract, independent of E12-T02.
  4. This task's own NEW row (`device-enrollment-approval.md`) was never
     exercised by either probe run above, since no discovered device was
     ever classified as pending in a `pending: []` `RelationshipRepository`
     seed -- consistent with task §5's own note that this row is invisible
     to `devices.md`'s measured gate by design (the `dashboard.md`
     `warning-expanded` precedent); its own correctness is covered by
     `devices_controller_enrollment_test.dart`'s EARS-RECOVER-5/6/7 tests
     instead, per that same §5 instruction.
  - No design/gaps.md entry needed: this is a tooling-environment finding
    about an existing test fixture and sandbox networking, not a design
    or spec deviation.

**Files actually touched:**
- `lib/features/devices/presentation/devices_controller.dart`
- `lib/features/devices/presentation/devices_view.dart`
- `test/features/devices/presentation/devices_controller_enrollment_test.dart`
  (new)

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id
- [ ] UI: design gate green; extra elements trace to `GAP-028` --
      design-verify not achieved in this sandbox (see §Deviations); the
      derived row's own elements are unmeasured by `devices.md`'s gate by
      design (task §5) and are proven instead by
      `devices_controller_enrollment_test.dart`
- [ ] Review APPROVE from a **different model** than `executed_by` (rule 5)
- [ ] Squash-merged to `epic_12`; tracker + metrics stamped
- [ ] 🧍 Human `verified` (epic-level batches are fine)

## Open Questions
None new.

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- Read `devices_controller.dart`/`devices_view.dart` in full before writing
  any code, per the task's own warning: `_onDeviceDiscovered` guards
  re-evaluation via `_evaluatingIds`, `verify`/`block` both end in
  `load()` which re-reads only persisted rows, and E06-B01's
  RenderFlex-overflow fix pattern (Expanded/Flexible on the name/label,
  fixed-width trailing buttons) is what the new row's two `Row`s follow.
- `DevicesController` gained: `_DeviceClassification` (`pendingEnrollment`
  / `normal`), `_classifyDiscoveredDevice`, `_ownDeviceIds` (session-cached
  as a shared **Future**, not a resolved value -- see the regression test
  below), a `pendingEnrollments` `RxList<TransportDevice>`, and an
  injectable `currentAccountUid`/`firebaseMetadataService` pair whose
  production default resolves the app-wide permanent
  `DeviceIdentityRepository` singleton via `Get.find` (no
  `devices_binding.dart`/`app/bindings.dart` edit needed or permitted --
  outside this task's `files:`).
- Regression found and fixed during implementation, not left for review:
  the first cache shape (`Set<String>? _ownDeviceIdsCache`, set only after
  both awaits) allowed two concurrent `_onDeviceDiscovered` calls (a real
  discovery batch pushes several devices back-to-back) to both see "not
  cached yet" and both call `readOwnDeviceIds` -- caught by
  `devices_controller_enrollment_test.dart`'s
  `readOwnDeviceIds is fetched at most once per controller lifetime`
  test (failed 2 vs expected 1 on the first run). Fixed by caching the
  in-flight `Future` itself (`_ownDeviceIdsFuture`), not just its resolved
  value.
- `flutter test test/features/devices/presentation/devices_controller_enrollment_test.dart`
  -- 6/6 pass (EARS-RECOVER-5, 6, a 6b own-device-already-known variant,
  7, the session-cache regression, and a null-uid degraded-case test).
- `flutter test` (full suite) -- **1157/1157 pass**, including the
  pre-existing `devices_controller_test.dart` (unmodified, still green)
  and `devices_view_test.dart`.
- `flutter analyze` -- 1 info-level finding, in
  `test/core/calls/call_migration_controller_test.dart` (outside this
  task's `files:`, pre-existing, unrelated to this diff).
- `make design-verify SCREEN=devices` -- `make` itself is not installed in
  this sandbox; ran the underlying
  `node design/tools/verify.mjs --screen devices --impl flutter` (+
  `flutter test test/design/design_probe_test.dart` for the probe dump)
  directly. Full investigation and evidence this task's diff introduces
  no new failures is in §Self-review's Deviations above -- short version:
  the gate does not report a clean PASS for `devices` in this sandbox at
  all, on the unmodified `epic_12` baseline just as much as on this
  task's branch (byte-identical 62.3%/38/61 match against `devices.md`'s
  own base contract either way), so this is a pre-existing
  environment/fixture limitation, not something introduced here. The new
  derived row itself is intentionally outside `devices.md`'s measured
  contract (task §5) and is proven by
  `devices_controller_enrollment_test.dart` instead.
