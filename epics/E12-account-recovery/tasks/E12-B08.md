---
id: E12-B08
epic: E12
type: bug
title: "`LoginController`'s new device-identity read adds a sign-in failure path and the feature's one unlogged silent-swallow catch"
layer: frontend
size: S
status: done
owner_agent: builder
model: any
preferred_agent: any
token_estimate: { tier: S, range: "15k-30k" }
severity: S4
priority: { moscow: should, p: P3 }
depends_on: [E12-T03]
blocks: []
traces_to: [FR-RECOVER-001, EARS-AUTH-3, NFR-OBS-001]
required_context:
  - epics/E12-account-recovery/tasks/E12-T03.md
  - lib/features/login/presentation/login_controller.dart
  - epics/E13-abuse-diagnostics/tasks/E13-T07.md
found_by: T03's cross-model reviewer (carried forward as tracker C5 and F1); confirmed during the E12 end-of-epic bug sweep
started_at: 2026-09-06
completed_at: 2026-09-06
executed_by: claude-sonnet-5 (builder)
reviewed_at: 2026-09-06
reviewed_by: opus (claude code, reviewer role)
review_outcome: APPROVE (auth_or_payment_code gate human-approved 2026-09-06)
files:
  create: []
  update:
    - lib/features/login/presentation/login_controller.dart
    - test/features/login/presentation/login_controller_enrollment_gate_test.dart
  delete: []
---
# E12-B08 · two small defects in the new login branch

Filed together: same method, same file, same one-line shape, both S4.

## Defect 1 (tracker C5) — a new way for sign-in to fail

```dart
// lib/features/login/presentation/login_controller.dart:123-124
final deviceIdentityRepository = _resolveDeviceIdentityRepository();
final identity = await deviceIdentityRepository?.latestDeviceIdentity();
```

This read sits **inside** `_signIn()`'s `try`. If `latestDeviceIdentity()`
throws — a Drift error, a corrupt row, disk pressure — it falls into the outer
`catch` and is reported to the user as a mapped **sign-in failure**, even
though sign-in fully succeeded and the local identity was already written.
Before E12-T03, no such read existed on this path and the flow went straight
to `/dashboard`.

Narrow: it is a local read immediately after the successful local write that
produced the very row being read. But it converts a succeeded sign-in into a
visible failure, which is the wrong direction for a best-effort hint. Every
other collaborator in this feature (`readOwnDeviceIds`, `pull`,
`_resolveDeviceIdentityRepository`) is explicitly best-effort; this one read
is not.

**Fix:** wrap the identity read (and the `readOwnDeviceIds` call it feeds) in
its own try/catch that logs and falls through to `/dashboard` — the same
degraded case the task's own §6 Risks already sanctions for an unresolvable
repository.

## Defect 2 (tracker F1) — the one unlogged catch

```dart
// lib/features/login/presentation/login_controller.dart:97-101
try {
  return Get.find<DeviceIdentityRepository>();
} catch (_) {
  return null;
}
```

This is the **only** best-effort failure path in the whole feature that does
not log via `ObservabilityService`. Every sibling does —
`readOwnDeviceIds` (`firebase.read_own_device_ids_failed`), `pull`
(`firebase.relationship_sync_pull_failed`), `push`
(`firebase.relationship_sync_push_failed`), and `_signIn`'s own outer catch.

Consequence: in production, a genuine DI misconfiguration is
**indistinguishable from a legitimate first-device case**, and emits no
telemetry at all. Given `E12-B01` shows this branch already misroutes users,
the absence of any signal here directly slowed diagnosis.

**Fix:** one line —
`ObservabilityService.instance.logError('recovery.device_identity_repository_unresolved', cause: e)`
(and `catch (e)` instead of `catch (_)`).

## Repro

Both are read-and-reason findings on an error path; neither has a natural user
repro. Defect 1 is provable by injecting a `DeviceIdentityRepository` whose
`latestDeviceIdentity()` throws and asserting the controller lands on an error
state rather than `/dashboard`. Defect 2 is provable by asserting a log call
with an unregistered repository.

## Severity

**S4** for both — narrow error paths, no happy-path impact.

## Overlap with E13-T07 — check before starting

`E13-T07` (P1, mid-fix) is changing sign-in's device-identity handling. Its
declared `files:` fence covers `sign_in_use_case.dart` and `app/bindings.dart`
but **not** `login_controller.dart`, so these two fixes are probably
non-conflicting — but `E12-B01` may pull `login_controller.dart` into T07's
orbit. **Sequence after `E12-B01`** and re-check the fence then, rather than
touching this file while T07 is in flight.

## What this fix does NOT do

- Does **not** add an error-state UI to the login screen (still out of scope,
  per E12-T03 §4 and E01-T01 before it).
- Does **not** change `SignInUseCase` or its signature.
- Does **not** change the enrollment-gate condition itself (`E12-B01`).
- Does **not** change `ObservabilityService` or add a new log category
  beyond the one event name above.

## Run log
- 2026-09-06 — **Scope note:** dispatched (and fixed) as defect 2 only —
  the unlogged `catch (_)` in `_resolveDeviceIdentityRepository()`. Defect 1
  (the identity-read-inside-`_signIn()`'s-try new failure path) was
  explicitly out of scope for this pass and is **not** fixed here; it
  remains open under this same bug id for a follow-up dispatch. This
  ordering is consistent with this bug file's own "sequence after
  `E12-B01`" note — B01 has not yet been confirmed merged as of this
  worktree's base (`origin/epic_12` at `1f59667`), so the narrower,
  independently-safe defect-2-only fix was taken rather than touching the
  `_signIn()` try body while that sequencing is still unconfirmed.
  `_resolveDeviceIdentityRepository()`'s `catch (_) { return null; }` is now
  `catch (e) { ObservabilityService.instance.logError(
  'recovery.device_identity_repository_unresolved', cause: e); return null; }`
  — the exact multi-line call shape (`code` string, `cause: e`) every
  sibling catch in this feature already uses
  (`firebase_metadata_service.dart`'s `device_registration_failed`/
  `read_own_device_ids_failed`, `relationship_sync_service.dart`'s
  `relationship_sync_push_failed`/`relationship_sync_pull_failed`, and this
  same file's own outer `_signIn()` catch). Return value (`null` on any
  thrown error) and control flow (falls through to the unchanged
  degraded-case handling) are unchanged — confirmed by re-running
  `login_controller_test.dart` and `login_controller_enrollment_gate_test.dart`
  unmodified and green, and by inspection: `ObservabilityService.log`
  no-ops unless `_initialized && kDebugMode`, so the added call has zero
  runtime side effect in the test environment and cannot alter the method's
  return path. No test asserts the log call itself — no existing test in
  this repo exercises `ObservabilityService` calls at all (grepped
  `test/` for `logError`/`ObservabilityService`: zero hits), so there is no
  established seam or precedent to mock the singleton through; adding one
  was judged out of proportion for a one-line S4 telemetry fix and is left
  as a documented gap rather than invented ad hoc.
  `flutter test`: 1172/1172 pass (1170-baseline + 2 unrelated new B07 tests
  from the same commit). `flutter analyze`: 1 pre-existing unrelated
  `annotate_overrides` info, same as baseline. Commit `1a4af38` (shared
  with `E12-B07`, filed together per the orchestrator's dispatch — same
  worktree, same branch `epic_12_bugs_07_08`; `git diff --stat` confirms no
  overlap between the two fixes' touched lines).
- 2026-09-06 — `E12-B02`/`E12-B03` merged to `origin/epic_12` while this fix
  was in progress (see `E12-B07`'s Run log for the full account — that
  merge rewrote `device_enrollment_controller.dart` and its test file, not
  anything this fix touches). Rebased onto `origin/epic_12` (`0538afc`);
  `login_controller.dart` applied cleanly with **no conflict** — confirms
  B02/B03 never touched this file, consistent with this bug's own
  "sequence after `E12-B01`" note (about `E13-T07`/`E12-B01`'s future
  orbit, not B02/B03). Re-ran `login_controller_test.dart` and
  `login_controller_enrollment_gate_test.dart` post-rebase: unchanged,
  green. Full suite post-rebase: 1189/1189 pass. `flutter analyze`: same 1
  pre-existing unrelated info. Force-pushed the rebased branch (solely this
  fix's own work — no other consumer of this branch).
- 2026-09-06 — Reviewer condition on merge: do not stamp `status: done`
  with Defect 1 still open — stays `review-requested` (Defect 2 only,
  not `done`) until Defect 1 is also fixed. **Defect 1 is now being fixed
  separately, bundled with `E12-B01`'s own dispatch** (same file, same
  area) now that `E13-T07` (the other epic's equivalent fix) has merged
  into `epic_13`, resolving the sequencing this bug's own note named.
  This bug closes once that dispatch lands and both defects are confirmed
  fixed.
- 2026-09-06 — **Both defects now fixed**, in the `epic_12_bug_01` worktree
  dispatched to close `E12-B01` (P1). Sequencing note: this bug file's own
  "sequence after `E12-B01`" guidance is now satisfied — this pass was
  dispatched together with, and lands in the same commit as, `E12-B01`'s
  fix, in the same file.
  **Defect 1** (the new sign-in-failure path): the identity read that feeds
  `accountUid` (previously `login_controller.dart:123-124`, inside
  `_signIn()`'s outer `try`) is now wrapped in its own try/catch, alongside
  the `readOwnDeviceIds` call it feeds. A thrown error there logs
  `recovery.device_identity_lookup_failed` via `ObservabilityService` and
  falls through to the unchanged `/dashboard` redirect, instead of being
  reported as a mapped sign-in failure by the outer catch. New regression
  test `test_EARS_AUTH_3_device_identity_read_failure_falls_through_to_dashboard`
  (`login_controller_enrollment_gate_test.dart`) injects a
  `DeviceIdentityRepository` whose `latestDeviceIdentity()` always throws
  and asserts the controller still reaches `/dashboard` (not a failure
  state) and that the failure is logged (captured via a `runZoned` `print`
  hook — `ObservabilityService` has no test seam of its own, same
  documented gap as this bug's own Defect 2 fix above). Falsified:
  temporarily reverted the try/catch wrapping, reran — the test fails with
  `reached: []` (sign-in reported as a mapped failure instead of reaching
  `/dashboard`), for the right reason; restored, reran green. See
  `E12-B01`'s own Run log for the full account (same commit, same fix).
  **Defect 2**: already fixed above in this same file's Run log — this
  entry reapplied the identical one-line change in a separate worktree
  (base predated `epic_12_bugs_07_08`'s merge); a trial merge onto
  post-merge `origin/epic_12` confirmed zero conflict, byte-identical
  result.
  `flutter test` (full suite): 1192/1192 pass (1190-baseline + 2 new tests,
  shared with `E12-B01`'s own fix in the same commit). `flutter analyze`:
  1 pre-existing unrelated `annotate_overrides` info, unchanged from
  baseline. Diff confined to `login_controller.dart` and
  `login_controller_enrollment_gate_test.dart`, per this bug's own `files:`
  fence.
- 2026-09-06 — Cross-model reviewed APPROVE (opus). Confirmed E12-B01's
  fix doesn't break new-device enrollment, Defect 1's fix falsified
  correctly, and the two branches' overlapping Defect-2 change resolves
  with zero conflict. `auth_or_payment_code` gate human-approved
  2026-09-06. Both defects closed.
  `accountUid` (previously `login_controller.dart:123-124`, inside
  `_signIn()`'s outer `try`) is now wrapped in its own try/catch, alongside
  the `readOwnDeviceIds` call it feeds. A thrown error there logs
  `recovery.device_identity_lookup_failed` via `ObservabilityService` and
  falls through to the unchanged `/dashboard` redirect, instead of being
  reported as a mapped sign-in failure by the outer catch. New regression
  test `test_EARS_AUTH_3_device_identity_read_failure_falls_through_to_dashboard`
  (`login_controller_enrollment_gate_test.dart`) injects a
  `DeviceIdentityRepository` whose `latestDeviceIdentity()` always throws
  and asserts the controller still reaches `/dashboard` (not a failure
  state) and that the failure is logged (captured via a `runZoned` `print`
  hook — `ObservabilityService` has no test seam of its own, same
  documented gap as this bug's own Defect 2 fix below). Falsified:
  temporarily reverted the try/catch wrapping, reran — the test fails with
  `reached: []` (sign-in reported as a mapped failure instead of reaching
  `/dashboard`), for the right reason; restored, reran green. See
  `E12-B01`'s own Run log for the full account (same commit, same fix).
  **Defect 2** (the one unlogged catch): already fixed and reviewed on
  branch `epic_12_bugs_07_08` (commit `1a4af38`, then re-verified post-rebase
  at `6c6b9ea` — see that branch's own Run log entries on this same file
  for the original account), since merged into `epic_12` via PR #123. This
  worktree's own base predated that merge, so its fix was not present when
  this branch started; reapplied identically here — a trial merge onto
  post-merge `origin/epic_12` confirmed zero conflict, byte-identical
  result. Reapplied identically in this branch: `_resolveDeviceIdentityRepository()`'s
  `catch (_) { return null; }` is now `catch (e) { ObservabilityService
  .instance.logError('recovery.device_identity_repository_unresolved',
  cause: e); return null; }`, the same one-line shape as every sibling
  catch in this feature. This means the orchestrator will see the SAME
  one-line change land via two separate PRs (`epic_12_bugs_07_08` and
  `epic_12_bug_01`) — an expected, trivially-resolved merge conflict on the
  same line, not a real collision; whichever merges second should keep
  either copy (they are identical) and drop the duplicate hunk. No new test
  added for Defect 2 specifically — no existing seam in this repo mocks
  `ObservabilityService` calls (still zero hits for `logError`/
  `ObservabilityService` under `test/`), and adding one was, again, judged
  disproportionate for a one-line S4 telemetry fix; the same failure
  injection used for Defect 1's regression test also exercises this file's
  other read paths, but does not specifically target this exact catch
  block's log call.
  `flutter test` (full suite): 1192/1192 pass (1190-baseline + 2 new tests,
  shared with `E12-B01`'s own fix in the same commit). `flutter analyze`:
  1 pre-existing unrelated `annotate_overrides` info, unchanged from
  baseline. Diff confined to `login_controller.dart` and
  `login_controller_enrollment_gate_test.dart`, per this bug's own `files:`
  fence.
