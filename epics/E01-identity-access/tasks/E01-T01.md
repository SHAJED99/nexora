---
id: E01-T01
epic: E01
type: feature
title: Real Google Sign-In + device identity (replace genesis stubs)
layer: cross-cutting
size: M
status: done
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: must, p: P1 }
depends_on: []
blocks: [E01-T02]
traces_to: [FR-AUTH-001, FR-AUTH-002, FR-AUTH-003, FR-AUTH-004, FR-AUTH-005]
required_context: [ADR-0005, docs/conventions.md, design/screens/welcome.md, design/screens/login.md]
design_contract: n/a
files:
  create:
    - lib/core/auth/google_auth_service.dart
  update:
    - pubspec.yaml
    - android/app/build.gradle.kts
    - android/settings.gradle.kts
    - lib/app/main.dart
    - lib/features/login/domain/sign_in_use_case.dart
    - lib/features/login/presentation/login_controller.dart
    - lib/core/persistence/database.dart
    - lib/core/persistence/database.g.dart
  delete: []
started_at: 2026-08-26
completed_at: 2026-08-26
executed_by: claude-sonnet-5 (builder)
reviewed_at: 2026-08-26
reviewed_by: claude-opus-5 (reviewer)
review_outcome: approve_with_notes (2/3 applied post-review)
---
# E01-T01 · Real Google Sign-In + device identity

## 1. Goal
Replace genesis's stubbed 1-second-delay "sign-in" with a real Firebase
Auth + Google Sign-In flow, and replace the stub device-id generator with
a cryptographically-secure one — the first real identity in the app.

## 2. Business logic
- FR-AUTH-001: Google Authentication via Firebase Auth is the sign-in mechanism.
- FR-AUTH-002/003: device cryptographic identity is independent of account
  identity — generated locally, never derived from or dependent on the
  Firebase Auth session.
- FR-AUTH-004: multiple devices per account are independent rows, not one
  shared record — proven by running this on two devices signed into the
  same Google account.
- FR-AUTH-005: welcome screen offers no auth path other than Google.

## 3. What this task does
- Add `firebase_core`, `firebase_auth`, `google_sign_in` to `pubspec.yaml`
  (direct implementation of already-accepted ADR-0005 / BRD §5 — not a new
  foundational choice).
- Apply the Google Services Gradle plugin so `google-services.json`
  (already present from Firebase provisioning) actually takes effect.
- `Firebase.initializeApp()` in `main.dart` before `runApp`.
- `GoogleAuthService` (`core/auth/`): wraps `google_sign_in` +
  `firebase_auth`'s `signInWithCredential` — returns the signed-in
  Firebase `User` (or throws a mapped `AppFailure`).
- `SignInUseCase` calls the real service instead of `Future.delayed`.
- Device id generation switches from `Random()` to `Random.secure()` (the
  TODO already left in `login_controller.dart` — this task resolves it).
  This is a random device identifier, not a full asymmetric keypair — the
  crypto *protocol* (X3DH keys) is E03's job (OQ-E03-1 still open); this
  task only needs a unique, cryptographically-random device id per FR-AUTH-003.
- Extend the `DeviceIdentities` Drift table (if needed) with an
  `account_uid` column linking to the Firebase Auth uid, so multiple
  devices under one account are queryable.

## 4. What this task does NOT do
- Does NOT implement the X3DH/Double-Ratchet crypto protocol (E03) — device
  id here is a random token, not a keypair.
- Does NOT implement the Firebase account↔device metadata document/collection
  (E01-T02) — this task only gets Auth working and the device row locally.
- Does NOT touch the welcome screen's UI/copy — `continueWithGoogle()`'s
  call site stays the same; only what happens after changes.
- Does NOT add sign-out, account switching, or session-refresh handling —
  first-run sign-in only. Note as a gap if it comes up, don't build it here.
- Does NOT add error-state UI beyond mapping failures to `AppFailure` and
  logging via `ObservabilityService` — a full error screen is out of scope
  unless the design contract already has one to build against.

## 5. Contract

### Functions
```yaml
- signature: "GoogleAuthService.signIn() -> Future<UserCredential>"
  params: {}
  returns: "Firebase UserCredential on success; throws AppFailure('auth.google_sign_in_failed') on any failure (cancelled, network, etc.)"
  purpose: "Wraps google_sign_in + firebase_auth into one call the use case invokes"

- signature: "SignInUseCase.call(deviceId: String) -> Future<void>"
  params: { deviceId: "Random.secure()-generated device identifier" }
  returns: "void — writes the device identity row (with account_uid) via DeviceIdentityRepository"
  purpose: "Orchestrates real Google sign-in + local device-identity persistence"
```

### Data
Drift migration: add `account_uid TEXT NULL` to `device_identities` (additive,
schema version bump per `docs/conventions.md`'s migration rule — reversible,
no backfill needed since this is pre-release).

### External services & flags
Firebase Auth (Google provider) — already enabled + deployed on project
`nexora-b3a97` (see `epics/E01-identity-access/epic.md` OQ-E01-1). No new
secrets: `google-services.json` already present.

## 6. Risks
- Google Sign-In on an emulator/WSA-style device without Play Services can
  fail in ways unrelated to this code — test on the physical/Wi-Fi device
  (`192.168.0.145:5555`), not WSA.
- `Random.secure()` alone is not a substitute for a real device keypair —
  don't let the naming ("cryptographic device identity") tempt inventing
  E03's protocol early. Keep it to a random token.

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS id `eac7c7b` `e010282`
- [x] add firebase_core/firebase_auth/google_sign_in deps, `flutter pub get` `a66a41c`
- [x] apply Google Services Gradle plugin in android/app + settings.gradle.kts `606a8d4`
- [x] `Firebase.initializeApp()` in main.dart `eb08226` (fixed missing
      `WidgetsFlutterBinding.ensureInitialized()` after on-device crash `89a6cdb`)
- [x] `GoogleAuthService` implemented `a078ac3`
- [x] `SignInUseCase` wired to the real service `eac7c7b`
- [x] `Random.secure()` device id generation `e010282`
- [x] Drift migration for `account_uid` `fba01f0`
- [x] widget/integration test updated to cover real (test-doubled) sign-in path `eb08226`
- [x] `flutter analyze` clean, `flutter test` green (4/4) `89a6cdb`
- [ ] run on the Wi-Fi device, manually verify sign-in completes — **partial**:
      app installs and runs (192.168.0.145:5555), welcome screen renders
      correctly (screenshot evidence) after the binding-order fix above.
      Could NOT complete the actual tap-through: synthetic `adb input
      tap`/`swipe`/`monkey` on "Continue with Google" did not register with
      the app (no navigation, no logcat activity after the injected event) —
      likely an ADB synthetic-touch limitation on this device/OEM, not an
      app bug (the same button already works via real taps per the
      genesis walking-skeleton work). A human physical tap, or a
      touch-capable test harness, is needed to finish this checklist item
      and confirm the real Google account picker + landing on `/home`.

## 8. Acceptance criteria (EARS) + test plan
- **EARS-AUTH-1**: WHEN a user taps "Continue with Google", the system SHALL authenticate via Firebase Auth. (FR-AUTH-001)
- **EARS-AUTH-2**: The system SHALL generate a device identity independent of the account identity. (FR-AUTH-003)
- **EARS-AUTH-3**: IF sign-in is cancelled or fails, THEN the system SHALL surface an `AppFailure` rather than crash or hang. (FR-AUTH-001, error path)

### Tests
- `test_EARS_AUTH_1_signs_in_via_firebase` — mocks `GoogleAuthService`, asserts `SignInUseCase` writes a device row with a non-null `account_uid`
- `test_EARS_AUTH_2_device_id_is_secure_random` — asserts device id generation uses `Random.secure()` (not the old `Random()`), distinct across calls
- `test_EARS_AUTH_3_signin_failure_maps_to_app_failure` — mocked service throws, use case surfaces `AppFailure`, does not crash

### Manual
1. ✅ **Done, 2026-08-26 — human physical tap-through.** Real Google account
   picker appeared on the Wi-Fi device, account selected, app showed
   "Signed in — device 99a7011d" and landed on `/home`. Confirms
   EARS-AUTH-1/2/3 for real, closing what the agent's synthetic-touch
   attempts couldn't.
2. ⚪ Not yet done — a second-device/same-account test needs a second
   physical device or a cleared-data reinstall. Not blocking this task;
   carried forward as a manual check for E01-T02 or the epic sweep.

## 9. Self-review + Definition of Done
### Self-review
- [x] Every §7 item done, with commit hashes — on-device manual tap-through
      completed by the human, 2026-08-26 (see §8 Manual)
- [x] `flutter analyze && flutter test` green (0 issues; 5/5 tests pass,
      incl. the migration test added post-review)
- [ ] Diff confined to `files:`; §4 respected — **two files touched outside
      the `files:` list**, see Deviations below
- [x] No secrets or PII logged (`ObservabilityService.logError` only logs
      the stable error `code` + the exception object per existing
      convention; no account email/token/plaintext)
- [x] ADR-0005 (independent sessions) honoured — `generateSecureDeviceId()`
      runs before any Firebase call
      (`lib/features/login/presentation/login_controller.dart`), the device
      row is keyed by that id regardless of sign-in outcome, and
      `account_uid` is an additive, nullable link column only — nothing
      about the device identity derives from or depends on the Firebase
      session

**Deviations from spec/design:**
1. **`lib/features/login/data/device_identity_repository.dart` edited, not in `files:`.**
   The task's own §5 Data contract requires the device row to carry
   `account_uid`, written "via DeviceIdentityRepository". The repository's
   `markSignedIn(int id)` had no way to pass that value through to
   `AppDatabase.markSignedIn`. Treated as a small ambiguity (file-list
   omission, not a spec contradiction): added one optional named param
   (`{String? accountUid}`), backward compatible, no behavior change for
   any other caller. Flagging for reviewer sign-off rather than stopping,
   since without it the task's own stated data contract is unreachable.
2. **Two new test files + one test helper not in `files:`**
   (`test/features/login/domain/sign_in_use_case_test.dart`,
   `test/features/login/presentation/login_controller_test.dart`,
   `test/support/fake_google_auth_service.dart`) and one existing test file
   updated (`test/widget_test.dart`, also not listed). §7's own checklist
   requires "tests written FIRST" and "widget/integration test updated" —
   task files in this repo generally don't enumerate test files in
   `files:`, so treated as implied in-scope, not a violation.
3. **`GoogleAuthService.signIn()` returns `Future<UserCredential>` exactly
   per the §5 contract, but `SignInUseCase` calls a second method,
   `signInAndGetAccountUid()`, not `signIn()` directly.** Reason:
   firebase_auth's `UserCredential` has a package-private constructor, so
   no test double outside the firebase_auth package can construct one
   without standing up the full Firebase platform-channel test harness.
   `signInAndGetAccountUid()` is the seam that lets
   `test_EARS_AUTH_1_signs_in_via_firebase` and
   `test_EARS_AUTH_3_signin_failure_maps_to_app_failure` mock
   `GoogleAuthService` (per the task's own test-plan wording) without that
   harness. `signIn()` itself is unchanged and still does exactly what §5
   specifies.
4. **On-device manual verification** — closed by a human physical
   tap-through, 2026-08-26 (see §8 Manual, item 1). The second-device check
   (§8 Manual item 2) remains open, carried to E01-T02/epic sweep.
5. **Independent review (Opus) flagged 3 non-blocking notes; 2 fixed here:**
   (a) `markSignedIn`'s `accountUid: Value(accountUid)` wrote an explicit
   `NULL` when no uid was passed, which would silently clobber an existing
   account link on any future re-sign-in caller — fixed to
   `Value.absent()` when null. (b) The v1→v2 migration had no test
   exercising `onUpgrade` (every test used `AppDatabase.forTesting`, which
   always takes `onCreate`) — added
   `test/core/persistence/database_migration_test.dart`, hand-building a
   v1 schema and confirming the upgrade adds `account_uid` without losing
   existing rows. (c) `GoogleAuthService.signIn()`'s cancellation branch
   has no direct test (only the use-case-level fake) — left as noted,
   since covering it needs mocking `google_sign_in`'s platform interface,
   not the "genuinely cheap" test it first looked like; not blocking.

**Files actually touched:**
`pubspec.yaml`, `pubspec.lock`, `android/app/build.gradle.kts`,
`android/settings.gradle.kts`, `lib/app/main.dart`,
`lib/core/auth/google_auth_service.dart` (new),
`lib/core/persistence/database.dart`, `lib/core/persistence/database.g.dart`,
`lib/features/login/domain/sign_in_use_case.dart`,
`lib/features/login/presentation/login_controller.dart`,
`lib/features/login/data/device_identity_repository.dart` (deviation 1),
`test/widget_test.dart`, `test/features/login/domain/sign_in_use_case_test.dart` (new),
`test/features/login/presentation/login_controller_test.dart` (new),
`test/support/fake_google_auth_service.dart` (new) (deviation 2),
`test/core/persistence/database_migration_test.dart` (new, post-review fix).

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id, plus the
      manual acceptance step (§8 "Manual" item 1) proven on-device by a
      human, 2026-08-26
- [x] Review APPROVE (with notes, 2 of 3 applied) from Opus — `executed_by`
      Sonnet (rule 5 satisfied)
- [ ] Squash-merged to `epic_01` branch; tracker + metrics stamped
- [ ] 🧍 Human `verified`

## Open Questions
_(none — OQ-E01-1 already resolved at epic level)_

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- 2026-08-26 `flutter analyze`: 0 issues. `flutter test`: 4/4 passed
  (`test_EARS_AUTH_1_signs_in_via_firebase`,
  `test_EARS_AUTH_2_device_id_is_secure_random`,
  `test_EARS_AUTH_3_signin_failure_maps_to_app_failure`, updated
  `test/widget_test.dart`).
- 2026-08-26 Built + installed on the Wi-Fi device (192.168.0.145:5555,
  Android 15). First install crashed before `runApp()`:
  `Firebase.initializeApp()` was called ahead of
  `WidgetsFlutterBinding.ensureInitialized()` in `lib/app/main.dart`,
  throwing "Binding has not yet been initialized" — confirmed via
  `adb screencap` (blank white screen) and `adb logcat` (full stack trace
  through `firebase_core_platform_interface`). Fixed (`89a6cdb`),
  rebuilt, reinstalled: welcome screen now renders correctly (icon, "NEXORA",
  copy, "Continue with Google" button, "End-to-end encrypted mesh" caption)
  — screenshot evidence captured.
- 2026-08-26 Attempted the manual acceptance flow (§8 "Manual"): sent
  `adb shell input tap`, `input swipe`, and `monkey --pct-touch 100` at the
  "Continue with Google" button's on-screen coordinates. `adb logcat
  --pid=<app>` shows the MotionEvent ACTION_DOWN/ACTION_UP being delivered
  to the app's window, but no subsequent navigation or Firebase/Google
  activity followed, and the screenshot after each attempt still shows the
  welcome screen. Could not get a synthetic tap to trigger
  `OnProcessButtonWidget`'s `onTap` on this device — this is a gap in this
  session's ability to *automate* the manual step, not a demonstrated app
  defect (no crash, no error, app stayed idle and responsive). Recommend a
  human perform the physical tap-through before flipping this to
  `verified`.
