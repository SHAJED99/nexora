---
id: E01-T02
epic: E01
type: feature
title: Minimal Firebase account/device metadata wrapper
layer: backend
size: S
status: todo
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: S, range: "5k-15k" }
priority: { moscow: must, p: P2 }
depends_on: [E01-T01]
blocks: []
traces_to: [FR-FB-001, FR-FB-002, FR-AUTH-004]
required_context: [ADR-0005, epics/E11-firebase-sync/epic.md]
design_contract: n/a
files:
  create:
    - lib/core/services/firebase_metadata_service.dart
    - firestore.rules
  update:
    - firebase.json
  delete: []
started_at:
completed_at:
executed_by:
reviewed_at:
reviewed_by:
review_outcome:
---
# E01-T02 · Minimal Firebase account/device metadata wrapper

## 1. Goal
Write the account-id ↔ device-id mapping to Firestore so a user's devices
are discoverable under their account — strictly within the boundary
FR-FB-001/002 already lock (metadata only, never content/keys).

## 2. Business logic
- FR-FB-001: Firebase may store device registry metadata, device public
  identity info, user config.
- FR-FB-002: Firebase must NOT store plaintext, recordings, private/session
  keys, or permanent location history — this task's schema has none of those
  fields, by construction.
- FR-AUTH-004: multiple devices per account — this is what makes that
  queryable across devices/sessions.

## 3. What this task does
- `firebase_metadata_service.dart`: one function writing
  `/users/{uid}/devices/{deviceId}` with `{ deviceId, createdAt,
  lastSeenAt, platform: 'android' }` — no other fields.
- Firestore security rules restricting read/write to the authenticated
  owner (`request.auth.uid == uid`).
- Called from `SignInUseCase` (E01-T01) after the local device row is
  written — best-effort: a Firestore write failure must not block local
  sign-in (offline-first constitution).

## 4. What this task does NOT do
- Does NOT implement E11's fuller scope (revocation, push tokens, version
  policy) — this is the minimal slice E01 needs, nothing more.
- Does NOT sync trust/relationship data (E02's job).
- Does NOT retry failed writes with a queue — best-effort fire-and-forget
  for this task; a real offline-write-queue is E05's job if it turns out
  to be needed here too.

## 5. Contract

### Data
Firestore collection `users/{uid}/devices/{deviceId}` — fields: `deviceId`
(string), `createdAt` (server timestamp), `lastSeenAt` (server timestamp),
`platform` (string, literal `"android"` for v1 per C-001). No other fields.

### Functions
```yaml
- signature: "FirebaseMetadataService.registerDevice(uid: String, deviceId: String) -> Future<void>"
  params: { uid: "Firebase Auth uid", deviceId: "the local device identity's id" }
  returns: "void — swallows and logs (via ObservabilityService) any Firestore error rather than throwing, since this must never block local-first sign-in"
  purpose: "Best-effort registration of this device under the account for FR-AUTH-004 multi-device visibility"
```

### External services & flags
Firestore, same project (`nexora-b3a97`). No new secrets.

## 6. Risks
- Don't let "just add a field" creep in — every field added here needs its
  own FR-FB-001 justification, not "might be useful later."

## 7. Implementation checklist
- [ ] tests written FIRST and failing, named by EARS id
- [ ] `firestore.rules` written, restricting to the owning uid
- [ ] `FirebaseMetadataService.registerDevice` implemented (best-effort, non-blocking)
- [ ] wired into `SignInUseCase` after the local Drift write
- [ ] `flutter analyze` clean, `flutter test` green
- [ ] `firebase deploy --only firestore:rules` (or MCP equivalent) run

## 8. Acceptance criteria (EARS) + test plan
- **EARS-FB-1**: WHEN sign-in completes, the system SHALL register the device under the account in Firestore, containing only metadata (no plaintext/keys/recordings). (FR-FB-001, FR-FB-002)
- **EARS-FB-2**: IF the Firestore write fails, THEN local sign-in SHALL still succeed. (offline-first constitution)

### Tests
- `test_EARS_FB_1_registers_device_metadata_only` — asserts the written document's fields are exactly the allowed set
- `test_EARS_FB_2_firestore_failure_does_not_block_signin` — mocked Firestore throws, `SignInUseCase` still completes and navigates to `/home`

### Manual
1. Sign in on the Wi-Fi device, then check the Firebase console (or `firebase firestore:get`) for the new `users/{uid}/devices/{deviceId}` document.

## 9. Self-review + Definition of Done
### Self-review
- [ ] Every §7 item done, with commit hashes
- [ ] `flutter analyze && flutter test` green
- [ ] Diff confined to `files:`; §4 respected
- [ ] Firestore schema contains no content/keys — spot-checked against FR-FB-002

**Deviations from spec/design** (none, or list):

**Files actually touched:**

### Definition of Done
- [ ] Every §8 criterion passes via a test named by its EARS id
- [ ] Review APPROVE from a different model than `executed_by` (rule 5)
- [ ] Squash-merged to `epic_01` branch; tracker + metrics stamped
- [ ] 🧍 Human `verified`

## Open Questions
_(none)_

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- (key evidence: decisions applied, session refs, gate reports)
