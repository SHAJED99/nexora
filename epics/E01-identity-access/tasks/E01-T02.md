---
id: E01-T02
epic: E01
type: feature
title: Minimal Firebase account/device metadata wrapper
layer: backend
size: S
status: review-requested
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: S, range: "5k-15k" }
priority: { moscow: must, p: P2 }
depends_on: [E01-T01]
blocks: []
traces_to: [FR-FB-001, FR-FB-002, FR-AUTH-004]
required_context: [ADR-0005, epics/E11-firebase-sync/epic.md]
design_contract: n/a
files:
  create:
    - lib/core/services/firebase_metadata_service.dart
    - firestore.rules
  update:
    - firebase.json
  delete: []
started_at: 2026-08-26
completed_at:
executed_by: builder (sonnet)
reviewed_at:
reviewed_by:
review_outcome:
---
# E01-T02 · Minimal Firebase account/device metadata wrapper

## 1. Goal
Write the account-id ↔ device-id mapping to Firestore so a user's devices
are discoverable under their account — strictly within the boundary
FR-FB-001/002 already lock (metadata only, never content/keys).

## 2. Business logic
- FR-FB-001: Firebase may store device registry metadata, device public
  identity info, user config.
- FR-FB-002: Firebase must NOT store plaintext, recordings, private/session
  keys, or permanent location history — this task's schema has none of those
  fields, by construction.
- FR-AUTH-004: multiple devices per account — this is what makes that
  queryable across devices/sessions.

## 3. What this task does
- `firebase_metadata_service.dart`: one function writing
  `/users/{uid}/devices/{deviceId}` with `{ deviceId, createdAt,
  lastSeenAt, platform: 'android' }` — no other fields.
- Firestore security rules restricting read/write to the authenticated
  owner (`request.auth.uid == uid`).
- Called from `SignInUseCase` (E01-T01) after the local device row is
  written — best-effort: a Firestore write failure must not block local
  sign-in (offline-first constitution).

## 4. What this task does NOT do
- Does NOT implement E11's fuller scope (revocation, push tokens, version
  policy) — this is the minimal slice E01 needs, nothing more.
- Does NOT sync trust/relationship data (E02's job).
- Does NOT retry failed writes with a queue — best-effort fire-and-forget
  for this task; a real offline-write-queue is E05's job if it turns out
  to be needed here too.

## 5. Contract

### Data
Firestore collection `users/{uid}/devices/{deviceId}` — fields: `deviceId`
(string), `createdAt` (server timestamp), `lastSeenAt` (server timestamp),
`platform` (string, literal `"android"` for v1 per C-001). No other fields.

### Functions
```yaml
- signature: "FirebaseMetadataService.registerDevice(uid: String, deviceId: String) -> Future<void>"
  params: { uid: "Firebase Auth uid", deviceId: "the local device identity's id" }
  returns: "void — swallows and logs (via ObservabilityService) any Firestore error rather than throwing, since this must never block local-first sign-in"
  purpose: "Best-effort registration of this device under the account for FR-AUTH-004 multi-device visibility"
```

### External services & flags
Firestore, same project (`nexora-b3a97`). No new secrets.

## 6. Risks
- Don't let "just add a field" creep in — every field added here needs its
  own FR-FB-001 justification, not "might be useful later."

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS id `838826c`, `3e358a0`
- [x] `firestore.rules` written, restricting to the owning uid `18a1f9a`
- [x] `FirebaseMetadataService.registerDevice` implemented (best-effort, non-blocking) `838826c`
- [x] wired into `SignInUseCase` after the local Drift write `3e358a0`
- [x] `flutter analyze` clean, `flutter test` green (9/9) — confirmed after `18a1f9a`
- [ ] `firebase deploy --only firestore:rules` (or MCP equivalent) run — **NOT
  done**: deploying rules changes the live `nexora-b3a97` project. Flagging
  this explicitly for a human/orchestrator call before running it, per this
  task's own dispatch instructions, rather than deploying unilaterally.

## 8. Acceptance criteria (EARS) + test plan
- **EARS-FB-1**: WHEN sign-in completes, the system SHALL register the device under the account in Firestore, containing only metadata (no plaintext/keys/recordings). (FR-FB-001, FR-FB-002)
- **EARS-FB-2**: IF the Firestore write fails, THEN local sign-in SHALL still succeed. (offline-first constitution)

### Tests
- `test_EARS_FB_1_registers_device_metadata_only` — asserts the written document's fields are exactly the allowed set
- `test_EARS_FB_2_firestore_failure_does_not_block_signin` — mocked Firestore throws, `SignInUseCase` still completes and navigates to `/home`

### Manual
1. Sign in on the Wi-Fi device, then check the Firebase console (or `firebase firestore:get`) for the new `users/{uid}/devices/{deviceId}` document.

**Status: NOT completed end-to-end.** Ran the app on the Wi-Fi device
(`flutter run -d 192.168.0.145:5555`) — welcome screen rendered correctly.
Attempted `adb shell input tap` on "Continue with Google" at two coordinate
sets (including the correctly display-scale-corrected one); neither
navigated to `/login` or produced any logcat sign-in activity — the same
synthetic-input limitation on this device documented in E01-T01's tracker
(MIUI apparently ignores synthetic taps on this control). Not guessing this
worked: a human doing a real physical tap on the device is still needed to
drive sign-in and let `SignInUseCase` actually call
`FirebaseMetadataService.registerDevice`, after which the Firestore
document should be checked (Firebase MCP tools were available in this
session but there was nothing to check yet — sign-in never completed).

## 9. Self-review + Definition of Done
### Self-review
- [x] Every §7 item done, with commit hashes (except the rules-deploy line,
  explicitly deferred — see §7)
- [x] `flutter analyze && flutter test` green (0 issues; 9/9 tests)
- [x] Diff confined to `files:`; §4 respected (with the one documented
  file-list deviation below)
- [x] Firestore schema contains no content/keys — spot-checked against
  FR-FB-002: only `deviceId`, `createdAt`, `lastSeenAt`, `platform`, keys
  enforced by `expect(data.keys.toSet(), {...})` in
  `test_EARS_FB_1_registers_device_metadata_only`

**Deviations from spec/design:**
1. This task's YAML `files:` list only names
   `lib/core/services/firebase_metadata_service.dart`, `firestore.rules`,
   and `firebase.json` — but §3 ("Called from `SignInUseCase`"), §7's own
   checklist ("wired into `SignInUseCase`"), and the dispatching
   instructions all require touching `lib/features/login/domain/
   sign_in_use_case.dart` (+ its test) and `pubspec.yaml`/`pubspec.lock`
   (new dependency, logged in `docs/conventions.md` per that file's own
   convention). Treated the YAML list as a sharding omission rather than a
   scope fence to route around — the task's own prose is unambiguous about
   needing this wiring, and there is no way to satisfy EARS-FB-1/2 without
   it. Did not stop/escalate since this isn't inventing new behavior, just
   completing what the task itself specifies.
2. `firebase deploy --only firestore:rules` intentionally NOT run — see §7.
3. Manual on-device verification (§8 Manual) not completed — synthetic ADB
   taps did not register with the sign-in button on the Wi-Fi test device
   (same limitation noted in E01-T01). A human physical tap is needed to
   close this out.

**Files actually touched:**
- `lib/core/services/firebase_metadata_service.dart` (new)
- `test/core/services/firebase_metadata_service_test.dart` (new)
- `firestore.rules` (new)
- `firebase.json` (updated — points `firestore.rules` at the new file)
- `lib/features/login/domain/sign_in_use_case.dart` (updated — see
  Deviation 1)
- `test/features/login/domain/sign_in_use_case_test.dart` (updated)
- `pubspec.yaml`, `pubspec.lock` (updated — `cloud_firestore` added)
- `docs/conventions.md` (updated — dependency log entry)

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id —
  `test_EARS_FB_1_registers_device_metadata_only` (both service-level and
  use-case-level), `test_EARS_FB_2_firestore_failure_does_not_block_signin`
  (service-level: `..._is_caught_and_logged_not_thrown`; use-case-level:
  exact EARS-FB-2 name)
- [ ] Review APPROVE from a different model than `executed_by` (rule 5)
- [ ] Squash-merged to `epic_01` branch; tracker + metrics stamped
- [ ] 🧍 Human `verified` — blocked on the physical-tap manual step above,
  and on the human decision re: deploying `firestore.rules`

## Open Questions
_(none — the files: list gap above was resolved as a sharding omission per
Deviation 1, not escalated as a spec gap; nothing here contradicts the
task's own prose or the EARS criteria)_

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- 2026-08-26 Implemented on `epic_01_task_02` (builder, sonnet). Tests
  written first for both `FirebaseMetadataService` (service-level seam
  `writeDeviceMetadata`, mirroring `GoogleAuthService`'s
  `signInAndGetAccountUid` pattern from E01-T01) and `SignInUseCase`
  (injectable `metadataService`); confirmed red before each implementation
  landed. `flutter analyze` clean, `flutter test` green (9/9: EARS-FB-1/2 at
  both service and use-case level, plus all prior E01-T01 tests still
  passing). `firestore.rules` written and wired into `firebase.json`; NOT
  deployed to the live project (see §7/§9). Ran the app on the Wi-Fi device;
  welcome screen renders. Synthetic ADB taps on "Continue with Google" did
  not register (same limitation as E01-T01) — manual on-device sign-in
  verification could not be completed in this session; a human physical tap
  is needed next. Commits: `e73043f` (dependency), `838826c` (service +
  tests), `3e358a0` (use-case wiring + tests), `18a1f9a` (firestore.rules +
  firebase.json).
