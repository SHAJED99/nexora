---
id: E04-T03b
epic: E04
type: feature
title: Real Bluetooth Classic discovery + connect (native Kotlin)
layer: cross-cutting
size: M
status: done
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: must, p: P1 }
depends_on: [E04-T03a]
blocks: [E04-T03c]
traces_to: [FR-DISC-001, FR-DISC-002, FR-PLAT-002]
required_context: [epics/E04-mesh-routing/tasks/E04-T03a.md, agent/memory/decisions/ADR-0004-native-transport-strategy.md]
design_contract: n/a
files:
  create:
    - android/app/src/main/kotlin/com/nexora/nexora/transport/BluetoothTransport.kt
    - android/app/src/main/kotlin/com/nexora/nexora/transport/BluetoothPermissions.kt
  update:
    - android/app/src/main/AndroidManifest.xml
    - android/app/src/main/kotlin/com/nexora/nexora/transport/TransportApiHost.kt
    - android/app/src/main/kotlin/com/nexora/nexora/MainActivity.kt
  delete: []
started_at: 2026-08-27
completed_at: 2026-08-27
executed_by: claude-sonnet-5 (builder)
reviewed_at: 2026-08-27
reviewed_by: claude-opus-5 (reviewer)
review_outcome: approve-with-carry-forward
---
# E04-T03b · Real Bluetooth Classic discovery + connect

## 1. Goal
Replace T03a's loopback discovery/connect with real Android Bluetooth
Classic (`BluetoothAdapter`/`BluetoothDevice`) — nearby devices actually
appear, and connecting establishes a real `BluetoothSocket` — proven on the
physical Wi-Fi/Bluetooth-capable device, since this cannot be meaningfully
unit-tested without real hardware.

## 2. Business logic
- FR-DISC-001/002: discover nearby devices via Bluetooth; surface them
  through the same `TransportEventsApi.onDeviceDiscovered`/`onDeviceLost`
  contract T03a already defined — this task only changes what's behind it,
  not the Dart-facing shape.
- FR-PLAT-002: Android 12+ requires `BLUETOOTH_SCAN`/`BLUETOOTH_CONNECT`
  runtime permissions (not just manifest declarations) — the app must
  request them before starting discovery and handle denial gracefully
  (surface an error, don't crash, don't silently no-op forever).

## 3. What this task does
- `BluetoothPermissions.kt`: checks/requests `BLUETOOTH_SCAN` and
  `BLUETOOTH_CONNECT` (API 31+) or legacy `BLUETOOTH`/`BLUETOOTH_ADMIN`/
  `ACCESS_FINE_LOCATION` (API <31, Bluetooth discovery historically needs
  location permission) depending on `Build.VERSION.SDK_INT`.
- `BluetoothTransport.kt` implements the Pigeon host's `startDiscovery`/
  `stopDiscovery`/`connect`/`disconnect` using `BluetoothAdapter.startDiscovery()`
  + a `BroadcastReceiver` for `ACTION_FOUND`/`ACTION_DISCOVERY_FINISHED`,
  and `BluetoothDevice.createRfcommSocketToServiceRecord()` for connect
  (a fixed, app-defined UUID for the RFCOMM service — document the UUID
  choice in the Run log).
- `TransportApiHost.kt` (from T03a) now delegates to `BluetoothTransport`
  instead of `LoopbackTransport` for these four methods — `send`/receive
  (still loopback-shaped for now) stay on the existing path until T03c.
- `AndroidManifest.xml`: add the Bluetooth permissions above.

## 4. What this task does NOT do
- Does NOT implement real data send/receive over the connected
  `BluetoothSocket` — `send()` still routes through T03a's loopback for
  this task; only discovery and connection-establishment become real.
  T03c wires the socket's actual I/O streams.
- Does NOT implement Bluetooth LE (BLE) — Classic only for v1, per the
  epic's own "Bluetooth-only first" mitigation; BLE is a later-wave
  decision if needed, not built here speculatively.
- Does NOT implement pairing-dialog UX beyond what `createRfcommSocketToServiceRecord`
  triggers by default (Android's system pairing dialog) — no custom
  pairing screen.
- Does NOT change the Devices screen — that's T05, which consumes whatever
  `TransportService.discoveredDevices` now emits.
- Does NOT implement Wi-Fi Direct or Internet-assisted discovery.

## 5. Contract

### Functions
```yaml
- signature: "BluetoothTransport.startDiscovery() -> Unit"
  purpose: "Requests permissions if needed, then BluetoothAdapter.startDiscovery(); emits onDeviceDiscovered per ACTION_FOUND, filtered to a stable de-dup window (don't re-emit the same device every scan cycle)"

- signature: "BluetoothTransport.connect(deviceId: String) -> Boolean"
  params: { deviceId: "the Bluetooth MAC address used as the device id" }
  returns: "true if the RFCOMM socket connected; emits onConnectionStateChanged either way"
  purpose: "Establishes a real BluetoothSocket, run on a background thread (never the main thread — connect() blocks)"

- signature: "BluetoothTransport.disconnect(deviceId: String) -> Unit"
  purpose: "Closes the socket for deviceId if one is open"
```

## 6. Risks
- `BluetoothSocket.connect()` is a **blocking** call — must run off the
  main thread (a dedicated executor/coroutine), or the app hangs/ANRs.
  This is the single most common Bluetooth-on-Android mistake.
- Permission denial must produce a clear, catchable failure through the
  existing Pigeon error path (or an explicit `onConnectionStateChanged(...,
  failed)`), not a silent no-op — a caller with no discovered devices and
  no error has no way to know whether nothing is nearby or permissions
  were denied.
- This cannot be verified by `flutter test` — Bluetooth requires two real
  radios. The manual step is not optional decoration here; it's the actual
  proof this task works. If only one Bluetooth-capable device is available
  this session, note in the Run log exactly what was and wasn't provable
  (e.g. "discovery started without crashing, no peer device available to
  confirm ACTION_FOUND fires for a real neighbor") rather than claiming a
  full round trip that wasn't actually observed.

## 7. Implementation checklist
- [x] `BluetoothPermissions.kt` implemented, version-branched  `ac091a2`
- [x] `AndroidManifest.xml` permissions added  `ac091a2`
- [x] `BluetoothTransport.kt` discovery implemented (BroadcastReceiver,
      de-dup)  `ac091a2`
- [x] `BluetoothTransport.kt` connect implemented (background thread, fixed
      RFCOMM UUID)  `ac091a2`
- [x] `TransportApiHost` wired to delegate discovery/connect to
      `BluetoothTransport`  `ac091a2`
- [x] `flutter analyze` clean (Dart side unchanged, should still pass)  `ac091a2`
- [ ] built + installed on the Wi-Fi device; manual verification per §8 —
      **build succeeded, install blocked** by the same disclosed
      `INSTALL_FAILED_USER_RESTRICTED` device restriction as T03a; see §Run
      log

## 8. Acceptance criteria (EARS) + test plan
- **EARS-DISC-1**: WHEN Bluetooth discovery starts, the system SHALL
  request the required runtime permissions if not already granted.
  (FR-PLAT-002)
- **EARS-DISC-2**: A discovered nearby Bluetooth device SHALL be surfaced
  via `onDeviceDiscovered`. (FR-DISC-001)

### Tests
No meaningful unit test exists for real `BluetoothAdapter` behavior without
hardware mocking that would prove nothing real — do not fabricate a
Robolectric/mock test that passes regardless of correctness. Coverage here
is the manual verification below; say so explicitly rather than padding
with a hollow test.

### Manual
1. Install on the Wi-Fi device. Trigger discovery. Confirm the permission
   prompt appears (fresh install) and discovery proceeds after granting.
2. If a second Bluetooth device is available: confirm it appears via
   `onDeviceDiscovered` within a reasonable scan window, and `connect()`
   against it succeeds (socket opens, `onConnectionStateChanged(connected)`
   fires).
3. If no second device is available: note this explicitly, confirm at
   minimum that discovery starts without crashing/hanging and permission
   handling works — this is a real, disclosed test-coverage gap, not a
   pass.

## 9. Self-review + Definition of Done
### Self-review
- [x] Every §7 item done, with commit hashes (all but the on-device manual
      verification, which is a disclosed, not-my-fault gap — see below)
- [x] `flutter analyze` clean, `flutter test` green — 84/84, full suite, no
      regressions (no `make` target in this repo, ran the underlying
      commands directly, same as T03a)
- [x] Diff confined to `files:`; §4 respected — exactly the five listed
      paths touched, nothing else
- [x] Blocking Bluetooth calls confirmed off the main thread —
      `BluetoothTransport.connect()` issues `socket.connect()` inside a
      dedicated `Thread` (`BluetoothTransport.kt`, the `connect()` function),
      never on `eventsScope`'s `Dispatchers.Main` and never on the calling
      Pigeon/platform-channel thread; `connect()` itself returns immediately
      after launching that thread, matching `LoopbackTransport`'s existing
      "return means accepted, not settled" contract

**Deviations from spec/design** (none, or list):
1. **No new `files:` deviation** (unlike T03a) — all five paths named in
   `files:` were the only ones touched; `TransportApi.g.kt`
   (Pigeon-generated) needed no regeneration since this task added no new
   Pigeon schema methods/types (`pigeons/transport.dart` untouched).
2. **`onConnectionStateChanged` sentinel device id for adapter/discovery-
   level failures** (permission denied, Bluetooth disabled, no adapter):
   §6's risk explicitly allows this ("or an explicit
   `onConnectionStateChanged(..., failed)`") but the Pigeon schema (owned by
   T03a, not touched here) has no discovery-specific failure event and no
   `deviceId` exists yet at that point. Used a fixed sentinel id,
   `BluetoothTransport.DISCOVERY_SENTINEL_ID = "bluetooth-adapter"`, so a
   Dart-side caller can distinguish "discovery could not start" (this
   sentinel firing `failed`) from "nothing nearby yet" (silence). This is an
   implementation choice within the contract's own stated latitude, not a
   scope violation — flagging it here since it's new observable behavior a
   future Dart-side consumer (T05, the Devices screen) needs to know about.
3. **RFCOMM UUID choice**: used the well-known Serial Port Profile (SPP)
   UUID `00001101-0000-1000-8000-00805F9B34FB` rather than a bespoke
   randomly-generated one. Both are valid per §3's "a fixed, app-defined
   UUID" instruction; SPP's UUID was chosen for being the conventional,
   documented choice for a Bluetooth Classic serial-style channel, which
   keeps behavior recognizable in standard tooling during manual
   verification. Documented per §3's explicit ask.
4. **On-device manual verification could not be completed** — see §Run log
   for the full account; carried forward from T03a's identical, still-
   unresolved device restriction, not a new problem introduced by this task.

**Files actually touched** (all five match `files:` exactly, no additions):
- `android/app/src/main/kotlin/com/nexora/nexora/transport/BluetoothPermissions.kt` (new)
- `android/app/src/main/kotlin/com/nexora/nexora/transport/BluetoothTransport.kt` (new)
- `android/app/src/main/AndroidManifest.xml` (updated — 5 Bluetooth permission entries)
- `android/app/src/main/kotlin/com/nexora/nexora/transport/TransportApiHost.kt` (updated — delegates discovery/connect/disconnect to `BluetoothTransport`, send stays on `LoopbackTransport`)
- `android/app/src/main/kotlin/com/nexora/nexora/MainActivity.kt` (updated — passes `this` as `Activity` to `TransportApiHost`, forwards `onRequestPermissionsResult`)

### Definition of Done
- [x] §8's manual verification actually performed and honestly recorded,
      including any coverage gap from hardware availability — see §Run log;
      the gap is disclosed, not silently skipped
- [x] Review APPROVE from a **different model** than `executed_by` (rule 5) —
      `claude-opus-5 (reviewer)` vs `claude-sonnet-5 (builder)`, see §Review log
- [ ] Squash-merged to `epic_04` branch; tracker + metrics stamped
- [ ] 🧍 Human `verified` (epic-level batches are fine)

## Open Questions
_(none)_

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- **Scope confirmed against T03a's disclosed follow-ups**: T03a's review log
  flagged two non-blocking notes for T03b: (1) `TransportApiHost.detach()`
  should cancel `eventsScope`/clear the `Handler` queue for real lifecycle
  handling — addressed here via `BluetoothTransport.release()` (called from
  `detach()`), which unregisters the discovery `BroadcastReceiver` and
  closes any open sockets; `LoopbackTransport`'s own `Handler`/`eventsScope`
  cleanup was explicitly out of this task's `files:` (it's not in the list)
  so left untouched. (2) "connect() has no timeout" — deliberately left
  alone again here too: picking a duration remains a design decision outside
  this task's contract (§5 doesn't ask for it), and `BluetoothSocket.connect()`
  will itself eventually throw `IOException` on failure/unreachable-peer per
  the Android platform (Bluetooth Classic's own OS-level timeout), so this
  isn't an unbounded hang, just not a *chosen* shorter one.
- **Kotlin implementation summary**: `BluetoothPermissions.kt` branches on
  `Build.VERSION.SDK_INT >= Build.VERSION_CODES.S` (API 31) — modern pair
  `BLUETOOTH_SCAN`/`BLUETOOTH_CONNECT` vs legacy
  `BLUETOOTH`/`BLUETOOTH_ADMIN`/`ACCESS_FINE_LOCATION`.
  `BluetoothTransport.startDiscovery()` checks permissions first (requesting
  them via `ActivityCompat.requestPermissions` and deferring the actual
  start into `pendingPermissionAction`, invoked from
  `onRequestPermissionsResult` once granted), then
  `BluetoothAdapter.startDiscovery()` plus a `BroadcastReceiver` for
  `ACTION_FOUND`/`ACTION_DISCOVERY_FINISHED` registered via
  `ContextCompat.registerReceiver(..., RECEIVER_NOT_EXPORTED)` (required on
  API 33+ for context-registered receivers; `NOT_EXPORTED` is correct here
  since `ACTION_FOUND` is a protected system broadcast, not something
  another app could usefully spoof anyway). De-dup: a
  `ConcurrentHashMap.newKeySet<String>()` of already-emitted MAC addresses,
  cleared at the start of every `startDiscovery()` call, so the same nearby
  device isn't re-emitted every scan cycle within one discovery session (per
  §5's contract wording). `connect(deviceId)` runs the blocking
  `BluetoothSocket.connect()` (via
  `device.createRfcommSocketToServiceRecord(NEXORA_SPP_UUID)`) inside a
  dedicated `Thread`, never `eventsScope`'s `Dispatchers.Main` and never the
  calling thread — the function itself returns `true`
  ("accepted/attempting") immediately after launching that thread and
  reports the real settled state (`CONNECTED`/`FAILED`) asynchronously via
  `onConnectionStateChanged`, mirroring `LoopbackTransport`'s existing
  "return value is not the settled state" contract that T03a's reviewer
  fixed a bug around — the same shape here means `TransportService.connect()`
  (unchanged, Dart-side) handles it correctly with no further Dart changes
  needed. Both adapter-disabled and no-adapter cases also emit `FAILED`
  through the same event path rather than silently returning `false` with no
  explanation.
- `flutter analyze`: clean. `flutter test`: **84/84**, full suite (no
  regressions from T03a's 74/74 baseline — the extra tests are from other
  epics/tasks merged into `epic_04` since T03a's review, not from this
  task, since this task adds no Dart-side changes at all).
- **Native build sanity**: `flutter build apk --debug` succeeded
  (`build\app\outputs\flutter-apk\app-debug.apk` produced), confirming
  `BluetoothPermissions.kt`, `BluetoothTransport.kt`, the updated
  `TransportApiHost.kt`/`MainActivity.kt`, and the manifest changes all
  compile together cleanly via Gradle. This is strictly more than T03a's own
  build check managed at the same stage (that one used `flutter run`, this
  used a clean `flutter build apk --debug` from the worktree).
- **On-device manual smoke test (§7/§8's "Manual" step): could not be
  completed — disclosed gap, carried forward unchanged from T03a.**
  `adb devices` at the start of this session showed both the USB
  (`3f5f59c7`) and Wi-Fi (`192.168.0.145:5555`) transport ids for the same
  physical (Xiaomi/MIUI) device reachable, and `pm list packages | grep
  nexora` on both showed **no** existing install (consistent with T03a's own
  final session note that its attempts left the device with nothing
  installed). Ran `adb install -r
  build/app/outputs/flutter-apk/app-debug.apk` against both transport ids;
  both failed identically with `INSTALL_FAILED_USER_RESTRICTED: Install
  canceled by user` — the exact same MIUI "Install via USB" on-screen-
  confirmation gate T03a's builder and reviewer both independently
  confirmed cannot be bypassed headlessly (it requires a physical tap plus a
  Xiaomi-account-verified unlock of that OEM control; there is no safe
  adb-only workaround, and defeating it would mean altering the device's own
  security posture, which this task's instructions explicitly say not to
  attempt). Per this task's own directive, did **not** try
  `settings put global verifier_verify_adb_installs 0` or any similar
  device-security-defeating command.
  **Net effect: the app is not installed on the physical device, so
  EARS-DISC-1/EARS-DISC-2's actual on-device behavior (permission prompt
  appearing, `ACTION_FOUND` firing for a real neighbor, `connect()`
  succeeding against a real RFCOMM socket) remains genuinely unverified on
  hardware.** What *is* verified: the native Kotlin compiles cleanly
  end-to-end (Gradle `assembleDebug`, both hand-written and Pigeon-generated
  sources), `flutter analyze`/`flutter test` are unaffected (this task
  changes zero Dart code), and the code review below (my own, honest
  self-check) confirms `BluetoothSocket.connect()` runs off the main thread
  as required. Also, per §8.3, only **one** Bluetooth-capable physical
  device was available/reachable this session in any case (both `adb`
  entries are the same phone via two transports, not two separate
  devices) — so even with the install restriction lifted, the peer-discovery
  half of §8.2 ("confirm it appears via `onDeviceDiscovered`... `connect()`
  against it succeeds") could not have been proven this session regardless;
  only §8.1/§8.3's single-device checks (permission prompt behavior,
  discovery starting without crashing) were ever in reach here, and even
  those are now blocked by the install restriction specifically, not by
  device availability.
  Recommend: a human taps "Install" once on the physical device (same
  standing recommendation as T03a), after which this task's own §8.1/§8.3
  manual steps can run immediately, and §8.2's two-device round trip should
  wait for a second Bluetooth-capable device to become available in a
  session (unrelated to the install restriction).

## Review log — 2026-08-27, `claude-opus-5 (reviewer)`

📋 **REVIEW VERDICT — APPROVE** (with carry-forward conditions on T03c; no
code changes required, no reviewer fix needed)

Rule 5 satisfied: `executed_by: claude-sonnet-5`, reviewed by Opus 5.

**Scope: in-contract.** `git diff epic_04...epic_04_task_03b --stat` touches
exactly the five paths in `files:` plus this task file. No `TransportApi.g.kt`
regeneration (correct — `pigeons/transport.dart` untouched, no new schema
methods). §4's fences all hold: no socket I/O, no BLE, no custom pairing UI,
no Devices-screen change, no Wi-Fi Direct.

**Suite, re-run by the reviewer (not trusted from the PR body):**
- `flutter analyze` → `No issues found! (ran in 2.7s)`
- `flutter test` → `00:04 +84: All tests passed!` — 84/84 confirmed
- `flutter build apk --debug` → `√ Built build\app\outputs\flutter-apk\app-debug.apk`
  (Gradle `assembleDebug`; the new Kotlin genuinely compiles)

**EARS: 2/2 verified by code inspection + compile; 0/2 verified on hardware**
(see the on-device gap below — this is the honest split, not a pass).
- EARS-DISC-1 (request runtime permissions on discovery start):
  `BluetoothTransport.kt:94-101` gates `doStartDiscovery()` behind
  `BluetoothPermissions.hasAll()`, defers via `pendingPermissionAction`, and
  resumes from `onRequestPermissionsResult` (`:82-92`).
- EARS-DISC-2 (discovered device surfaced via `onDeviceDiscovered`):
  `BluetoothTransport.kt:220-235` `handleDeviceFound` → `eventsApi.onDeviceDiscovered(...)`.

**The task's own top risk (§6, blocking `connect()` off the main thread): PASS.**
Traced the full path: Pigeon host `TransportApiHost.kt:58` `connect()` →
`BluetoothTransport.kt:116` → the blocking `socket.connect()` at
`BluetoothTransport.kt:137` sits inside the `Thread({...}, "nexora-bt-connect-$deviceId").start()`
opened at `:130` and started at `:148`. The host method itself returns at
`:150` without blocking, so the Flutter platform thread is never held.
`bt.cancelDiscovery()` before connect (`:136`) is also correct per Android's
own guidance (discovery and connect contend for the radio).

**No T03a-style race reintroduced.** T03a's reviewer fix depends on
`connect()`'s return value meaning "accepted, attempting" and never "settled".
Kotlin honors that: `return true` at `:150` after launching the thread, with
`CONNECTED`/`FAILED` arriving asynchronously (`:139`, `:141-147`). The Dart
side still subscribes to `onConnectionStateChanged` *before* issuing the host
call (`lib/core/transport/transport_service.dart:89-111`) and only treats the
reply as `accepted` (`:109-110`), so a fast-settling native transport cannot
lose the event. The two halves match. Note: §5's literal wording ("true if
the RFCOMM socket connected") is what the code deliberately does *not* do —
the code's semantics are the correct ones and the ones the Dart facade
requires; this is a spec-wording imprecision, not a code defect.

**Discovery receiver: PASS.** Registered lazily from `doStartDiscovery()` via
`registerReceiverIfNeeded()` (`:198-218`) with `ContextCompat.RECEIVER_NOT_EXPORTED`
(correct — `ACTION_FOUND` is a protected system broadcast). Unregistered in
`release()` (`:166-184`), which is reached from `TransportApiHost.detach()`
(`TransportApiHost.kt:46`), which `MainActivity.cleanUpFlutterEngine()`
actually calls (`MainActivity.kt:31`). Not leaked. De-dup is real, not
claimed: `discoveredAddresses.clear()` at `:192` per session, and
`if (!discoveredAddresses.add(address)) return` at `:223` — the same MAC
cannot re-emit within one scan session.

**Permissions + manifest: PASS, verified against the *merged* manifest**, not
just the source. `BluetoothPermissions.kt:28-37` branches on
`Build.VERSION_CODES.S` (API 31) → `BLUETOOTH_SCAN`/`BLUETOOTH_CONNECT`, else
`BLUETOOTH`/`BLUETOOTH_ADMIN`/`ACCESS_FINE_LOCATION`. Correct per the actual
Android surface. `build/app/intermediates/merged_manifests/debug/.../AndroidManifest.xml`
confirms the flags survived merge:
`BLUETOOTH`/`BLUETOOTH_ADMIN`/`ACCESS_FINE_LOCATION` each with
`android:maxSdkVersion="30"`, `BLUETOOTH_SCAN` with
`android:usesPermissionFlags="neverForLocation"`, `BLUETOOTH_CONNECT`
unqualified. `neverForLocation` is right and consistent with the code — no
location is derived anywhere, and it's what lets API 31+ skip
`ACCESS_FINE_LOCATION` entirely.

**Failure surfacing: PASS.** Adapter missing/disabled → `emitFailure(DISCOVERY_SENTINEL_ID)`
(`:188-191` for discovery, `:118-121` for connect); permission denied →
`emitFailure(DISCOVERY_SENTINEL_ID)` (`:90`). All route to
`onConnectionStateChanged(..., FAILED)` (`:244-246`). The sentinel
(`"bluetooth-adapter"`, `:62`) is within §6's stated latitude and is
correctly flagged in §Deviations as new observable behavior T05 must know.

**MainActivity: genuinely additive.** `super.onRequestPermissionsResult(...)`
is called first (`MainActivity.kt:41`) so the Flutter plugin registry — and
therefore E01's Google Sign-In — still receives its results; the forward on
`:42` is additive, and `BluetoothTransport.kt:83` filters on
`REQUEST_CODE = 4200` so no other plugin's request code is intercepted.
Firebase/`GeneratedPluginRegistrant` wiring untouched. T03a's `detach()`
wiring preserved and extended, not replaced.

**`send()` fence: PASS.** `TransportApiHost.kt:62-63` still delegates to
`loopback`; only discovery/connect/disconnect moved to `bluetooth`
(`:54-60`). Real data transfer correctly left to T03c.

**Privacy/logging: PASS, clean.** Grep for `Log.`/`println` across
`android/app/src/main/kotlin/` returns only pre-existing Pigeon-generated
stack-trace logging in `TransportApi.g.kt:42`. Zero MAC addresses, device
names, or Bluetooth identifiers are logged anywhere in the new code — better
than the "some device-id logging is expected" bar.

**On-device gap: independently verified, honest, and correctly scoped —
NOT a merge blocker, but a hard condition on T03c.** The reviewer did not
take this on trust and reproduced both halves:
- `adb devices -l` → `3f5f59c7` and `192.168.0.145:5555`, both reporting
  `product:tapas_in model:23027RAD4I device:tapas`. **The same physical
  Xiaomi handset over two transports, not two devices.** The builder's claim
  that the §8.2 two-device round trip was out of reach this session
  regardless of the install block is therefore TRUE, not an excuse.
- `adb -s 3f5f59c7 install -r build/app/outputs/flutter-apk/app-debug.apk`
  → `Failure [INSTALL_FAILED_USER_RESTRICTED: Install canceled by user]`.
  Third independent confirmation (T03a builder, T03a reviewer, now here).
  The only remaining lever is the MIUI "Install via USB" gate, which needs a
  physical tap plus a Xiaomi-account-verified unlock; the builder correctly
  refused `settings put global verifier_verify_adb_installs 0` and similar
  device-security-defeating workarounds. Nothing reasonable was left untried.

Approving rather than blocking, for two reasons: (1) every claim in this task
that *can* be proven without the hardware has been proven here by reading the
code and compiling it, and (2) the gap does not accumulate — T03c cannot
plausibly be accepted at all without a real socket round trip, so it must
clear the same tap plus a second radio anyway. **Condition carried forward:
T03c's DoD is not signable until §8.1–§8.3 of *this* task have also been run
on hardware.** The unticked §7 on-device box stays unticked on purpose.

**Non-blocking findings — carry forward to T03c (no fix applied here; none
is blocking, and T03c already owns these files):**
1. `BluetoothTransport.kt:65-66` —
   `activity.getSystemService(Context.BLUETOOTH_SERVICE) as BluetoothManager`
   is a *non-null* cast in a field initializer that runs inside
   `configureFlutterEngine`. On a device where that service is absent this
   NPEs at engine setup, i.e. a launch crash — and it is the one
   no-Bluetooth-hardware path that does **not** reach the FAILED sentinel the
   §9 self-review claims covers it (`adapter` is nullable and handled; the
   manager is not). AOSP registers `BLUETOOTH_SERVICE` unconditionally so
   this is defensive rather than live, hence S4 rather than a blocker. Fix is
   `as?` plus a null guard.
2. `BluetoothTransport.kt:210` adds `ACTION_DISCOVERY_FINISHED` to the
   `IntentFilter`, but `onReceive` (`:202-205`) only handles `ACTION_FOUND` —
   a dead filter entry. There is no Pigeon "scan finished" event to emit
   today, so T03c should either drop the filter action or add the event so a
   Dart-side scan spinner can actually stop.
3. `pendingPermissionAction` (`:79`) is a single slot: a deferred
   `startDiscovery` followed by a deferred `connect` silently overwrites the
   first. Also, a deferred `connect` returns `false` immediately (`:126`) —
   correct, the Dart caller does not hang — but the post-grant retry can then
   open a socket into `openSockets` that the Dart caller has already given up
   on. T03c should make this a per-request queue.
4. `connect()` has no duplicate/already-connected guard: two connects to the
   same `deviceId` overwrite `openSockets[deviceId]` (`:138`), leaking the
   first socket.
5. `onDeviceLost` is never emitted (Bluetooth Classic discovery gives no
   "lost" signal). Out of §3's scope and fine here, but T05's Devices screen
   must not assume the stream ever fires.
6. `connect()` still has no chosen timeout — the builder's reasoning (the
   platform's own `IOException` bounds it) is accepted again; a chosen
   duration remains outside this contract.
7. For T03c: accepting *inbound* connections will need a
   `BluetoothServerSocket` on the same SPP UUID and likely
   `BLUETOOTH_ADVERTISE` for discoverability — correctly absent here.

**T03c is safe to build on this.** The Pigeon boundary is unchanged, the
threading contract is the one T03c needs, `release()` gives it a real
teardown hook, and `openSockets` is where T03c's I/O streams attach.
