---
id: E04-B16
epic: E04
type: bug
title: "Multi-hop relay routing is keyed by a resolved selfDeviceId (E04-B13/B15's own fix), but RoutingEngine only ever knows transport link ids -- a relayed frame can never find a route once RoutingEngine._knownLinks is ever actually populated"
layer: backend
size: M
status: done
owner_agent: planner
model: any
preferred_agent: any
token_estimate: { tier: M, range: "60k-150k" }
severity: S3
priority: { moscow: could, p: P3 }  # currently INERT -- RoutingEngine._knownLinks
  # is empty on every real device today (link_quality_feed.dart's own header
  # comment), so computeRoute() already always returns null regardless of
  # this gap. Becomes live only once that feed lands. Flagging for a human
  # bug_priorities read (severity is the reviewer's call, S3; priority is
  # not asserted here).
depends_on: []
blocks: []
traces_to: [FR-ROUTE-002, FR-ROUTE-003]
required_context:
  - lib/core/messaging/inbound_pipeline.dart
  - lib/core/routing_engine/routing_engine.dart
  - lib/core/routing_engine/relay_engine.dart
  - lib/core/messaging/messaging_stack.dart
  - lib/core/notifications/link_quality_feed.dart
  - epics/E04-mesh-routing/tasks/E04-B12.md
  - epics/E04-mesh-routing/tasks/E04-B13.md
  - epics/E04-mesh-routing/tasks/E04-B15.md
files:
  update:
    - lib/core/messaging/inbound_pipeline.dart
    - lib/core/routing_engine/routing_engine.dart
    - lib/core/messaging/identity_announce.dart
    - test/core/routing_engine/routing_engine_test.dart
    - test/core/routing_engine/relay_engine_test.dart
    - test/core/messaging/identity_announce_test.dart
    - epics/E04-mesh-routing/tasks/E04-B16.md
    - epics/E04-mesh-routing/tracker.md
  create: []
  delete: []
started_at: 2026-09-15
executed_by: "orchestrator session (claude-opus-5) -- reviewer must NOT be opus"
reviewed_at: 2026-09-15
reviewed_by: "claude-sonnet-5 (cross-model, rule 5)"
review_outcome: approve_with_nits
completed_at: 2026-09-15
---
# E04-B16 · Reconcile relay routing's link-id namespace with resolved-identity addressing

## How this was found
`E04-B15`'s own independent reviewer (opus), while confirming that task's
six outbound-addressing fixes, found this while checking whether any of
the fixed features route differently than 1:1 chat. Direct quote:

> On an intermediate relay, `inbound_pipeline.dart:756` re-enqueues by
> `frame.destination` — now a logical `selfDeviceId` (per E04-B12/B13/B15's
> own fix) — and `RoutingEngine.computeRoute` resolves over transport link
> ids, so it returns null and the packet sits until TTL. The fix makes
> direct-neighbour delivery work and multi-hop delivery structurally
> impossible... Not a blocker: inherited from B13, the task's §2.2
> mandated exactly this pattern, and it's currently latent
> (`link_quality_feed.dart:1-9` documents that `computeRoute()` always
> returns null on a real device today). But it becomes live the moment
> the link-quality feed lands, and fixing it needs either the reverse
> lookup B13 §1a forbids or a wire-format change — rule 3.

In plain terms: `E04-B12`/`E04-B13`/`E04-B15` correctly fixed direct
(single-hop, already-connected) message delivery by resolving a peer's
real `selfDeviceId` before addressing a frame. But `RelayEngine`'s own
multi-hop forwarding path (`inbound_pipeline.dart`'s `!isForUs` branch)
re-enqueues a frame by that SAME resolved `frame.destination` — and
`RoutingEngine.computeRoute` (the thing that decides which NEXT-HOP link
to forward a relayed packet over) has only ever known about transport
link ids (Bluetooth MAC addresses), never Signal-Protocol identity
strings. A relay device holding a queued packet addressed to a
`selfDeviceId` has no way to look up "which of my known links gets this
closer to that identity" — `computeRoute()` structurally cannot succeed
for such a packet.

**This is currently INERT, not a live bug** — `RoutingEngine._knownLinks`
is empty on every real device today (confirmed in
`link_quality_feed.dart`'s own header comment: no production populator
exists yet), so `computeRoute()` already always returns `null` for every
packet, identity-addressed or not. This task's own gap has zero
additional real-world effect until that separate, already-known
limitation is resolved. This is filed now, while the shape of the gap is
fresh and well-understood, rather than left as a note that the next
person to build the link-quality feed would have no reason to connect to
an addressing fix three epics back (`L-process-008`'s own pattern — a
cross-task seam owned by no task is exactly how a real gap sits
unaddressed across many tasks).

## What this task does
1. **Confirm the current inert state stays true** at the time this task
   is actually picked up — re-check `RoutingEngine._knownLinks`'s real
   population state and `link_quality_feed.dart`'s own status; if a
   feed has landed in the meantime, this task's own urgency changes and
   that should be reflected in a fresh priority read, not assumed away.
2. **Design a reconciliation**, presenting it as a rule-3 foundational
   choice with trade-offs to a human before implementing (per the
   reviewer's own note: "fixing it needs either the reverse lookup B13
   §1a forbids... or a wire-format change"). Candidate directions, not a
   prescribed answer:
   - Extend `RoutingEngine`'s own link/route model to track BOTH a link's
     transport address AND (once learned) its resolved `selfDeviceId`,
     so `computeRoute` can resolve a `selfDeviceId`-addressed destination
     to a next-hop link without needing a raw reverse lookup on the
     `Relationships` table (`E04-B13`'s own security precondition was
     specifically about that table, not necessarily about `RoutingEngine`
     maintaining its own, separately-scoped mapping fed by the SAME
     already-authenticated announce mechanism `E04-B12` built — but
     confirm this reasoning holds before assuming it's automatically
     safe, since it is still fundamentally a reverse-direction lookup and
     deserves the same scrutiny `E04-B13`'s own security review gave the
     forward direction).
   - A wire-format change carrying BOTH the resolved identity and a
     next-hop transport hint in a relayed frame's own header, so an
     intermediate relay never needs to resolve anything itself — it just
     reads the hint. Real scope: touches `RelayPacketFrame`'s own wire
     format, a rule-3 concern in its own right (format changes affect
     every device running an older build).
   - Something else this task's own implementer identifies once actually
     investigating the current `RoutingEngine`/`RelayEngine` code in
     detail — the two candidates above are starting points, not the only
     two real options.
3. Implement the human-chosen direction, with a test proving a multi-hop
   relay scenario (three simulated nodes, A→B→C, B relaying) can actually
   route once `RoutingEngine._knownLinks` is populated (even if by a test
   double, since the real link-quality feed may not exist yet at
   implementation time).

## What this task does NOT do
- Does NOT build the link-quality feed itself (`RoutingEngine.
  _knownLinks`'s real production populator) — that is separate,
  already-known, already-tracked work this task's own dependency should
  point to once that work is itself filed/found.
- Does NOT touch `E04-B12`/`E04-B13`/`E04-B14`/`E04-B15`'s own already-
  merged, already-reviewed single-hop fixes — this task is additive,
  fixing the multi-hop case those tasks correctly left alone (per their
  own explicit scope fences).
- Does NOT re-litigate `E04-B13`'s own forward-only security constraint
  without a fresh, explicit human decision — if this task's own design
  requires something reverse-lookup-shaped, that is exactly the kind of
  foundational call that needs to go back to a human with trade-offs, not
  be decided unilaterally by whoever implements this task.

## Definition of Done
- [x] Current state re-confirmed, 2026-09-15: `link_quality_feed.dart:7-8` still documents that `RoutingEngine._knownLinks` stays empty on a real device. The gap is still inert; this fix makes it correct before the feed lands.
- [x] Presented to the human as a rule-3 choice with three options (routing alias map / next-hop hint in the frame / defer). **Human decision, 2026-09-15: "Routing alias map (Recommended)"**, via AskUserQuestion.
- [x] Implemented (commit `5aadc77`):
  - `RoutingEngine.recordIdentityAlias`/`forgetIdentityAlias` hold a link-id -> announced-identity map.
  - `_bestRoute` falls back to it for a destination that is not a graph node, so `computeRoute`, `considerMigration` and `onRouteFailure` all resolve identities.
  - An identity claimed by two links resolves to nothing.
  - `IdentityAnnounceService.handleAnnounce` records the alias only for an existing `trusted`/`allowed` relationship (otherwise forgets it).
  - `InboundPipeline._seedKnownDevices` re-seeds aliases for trusted/allowed peers after a restart.
  - No wire-format change. `relationships` is not queried for a reverse lookup at routing time; the alias lives only inside routing and is used only to choose a next hop.
- [x] Multi-hop test: `test_E04_B16_intermediate_relay_forwards_to_the_aliased_next_hop`. B holds a packet A addressed to C's identity, and `RelayEngine` sends it to `C-link`. Plus five `RoutingEngine` alias tests and two announce trust-gate tests.
- [x] Falsified against the commit (restored from HEAD):
  - alias resolution removed: the five alias-dependent tests fail
  - ambiguity check removed: only the ambiguity test fails
  - trust gate removed: only the unknown-peer test fails
- [x] Full `flutter test` 1569/1569; `flutter analyze`: the same 3 pre-existing infos.
- [x] Independent review (claude-sonnet-5, APPROVE WITH NITS, PR #265). The reviewer re-falsified the ambiguity guard and the trust gate. Nits: F1 (Deviations not recorded, fixed below); F2 (alias-poisoning race, recorded as OQ-E04-B16-3).

## Open Questions
- OQ-E04-B16-1: aliases are learned only from direct announces, so they cover THIS device's direct neighbours. That is enough for an intermediate relay to forward to its neighbour (the case this bug is about). An originating device two hops away still cannot resolve a distant identity; that needs identity information to propagate with link-state data, which belongs to whoever builds the link-quality flooding feed.
- OQ-E04-B16-3 (review F2, S3, carried forward to whichever task first populates `RoutingEngine._knownLinks` with real link data): a peer this device already trusts can announce ANOTHER contact's identity. If it does so before that contact's own first announce, the alias is unambiguous, so relayed packets addressed to that contact are forwarded to the malicious peer. Payloads stay end-to-end encrypted, so nothing is exposed and the peer cannot impersonate the contact. But it is misdirection, not just delay: the peer learns size and timing metadata. It also persists: the poisoned alias, and the persisted `remote_self_device_id` behind it, survive until that peer re-announces or stops being trusted, and startup re-seeding restores it. Inert today (no production link feed). It requires an attacker who is already trusted, the same bar as E04-B12 CF#1 and E04-B13 F6. A real fix needs announced identities to be authenticated, which is the project-wide TOFU/announce trust-boundary work already referenced by E04-B26 OQ-3.
- OQ-E04-B16-2: an alias is recorded when a trusted/allowed peer announces, and re-seeded at startup. If a relationship becomes trusted while connected, its alias appears only at the next announce (every fresh connection) or restart.

## Deviations
- `files:` widened from `inbound_pipeline.dart` + `routing_engine.dart` to also include `lib/core/messaging/identity_announce.dart`, the three test files, this task file and `tracker.md`. The human-chosen design needs the alias fed from the announce handler (the only place a link's announced identity is learned), so the trust-gated `recordIdentityAlias`/`forgetIdentityAlias` calls have to live in `IdentityAnnounceService.handleAnnounce`. Recorded after review (F1).

## Review
(pending dispatch)
