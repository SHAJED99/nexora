---
id: E04-B13
epic: E04
type: bug
title: "Resolve a peer's real selfDeviceId (learned via E04-B12's identity-announce) into outbound RelayPacketFrame addressing -- the half that actually fixes end-to-end message delivery (Option A, part 2/2)"
layer: backend
size: M
status: review-requested
owner_agent: builder
model: any
preferred_agent: any
token_estimate: { tier: M, range: "60k-150k" }
severity: S2
priority: { moscow: must, p: P1 }  # same human decision as E04-B12 (2026-09-13,
  # Option A chosen) -- this task is the other half of that same fix, not a
  # separately-decided priority.
depends_on: [E04-B12]
blocks: []
traces_to: [FR-DISC-001]
required_context:
  - lib/core/messaging/prekey_exchange.dart
  - lib/core/messaging/inbound_pipeline.dart
  - lib/core/transport/connection_ensuring_sender.dart
  - lib/core/routing_engine/relay_engine.dart
  - lib/features/trust/domain/relationship.dart
  - epics/E04-mesh-routing/tasks/E04-B12.md
  - epics/E04-mesh-routing/tasks/E04-B11.md
files:
  update:
    - lib/core/messaging/prekey_exchange.dart
    - lib/core/transport/connection_ensuring_sender.dart
    - lib/core/routing_engine/relay_engine.dart
    # Added during implementation, disclosed in Deviations below:
    # lib/core/messaging/messaging_stack.dart -- the real chat-message
    # `RelayPacketFrame` construction site (`encryptAdapter`) turned out to
    # live here, not in `relay_engine.dart`/`connection_ensuring_sender.dart`
    # (neither of which construct a `RelayPacketFrame` at all -- verified by
    # inspection, see Deviations). Leaving this file out of the fence would
    # have left the actual human-reported bug (chat messages, not just
    # session bootstrap) unfixed.
    - lib/core/messaging/messaging_stack.dart
  create: []
  delete: []
---
# E04-B13 · Wire the learned selfDeviceId into outbound addressing

## 1. Why this task exists, and why it's separate from E04-B12
`E04-B12` builds the mechanism to LEARN a peer's real `selfDeviceId` (a
new identity-announce control frame, fired automatically on connect, its
result stored in `Relationship.remoteSelfDeviceId`) but does not use that
value anywhere. On its own, `E04-B12` does not fix the actual bug this
whole investigation started from: a message sent to a peer still gets
addressed by that peer's Bluetooth MAC address
(`RelayPacketFrame.destination = "B8:DB:38:7C:D4:BF"`, say), which will
never equal the receiver's own `selfDeviceId`
(`InboundPipeline._handleBuffer`'s `isForUs` check), so `ensureSession`
still times out exactly as documented in `E04-B12`'s own §2.

This task is what actually closes the loop: once a peer's
`remoteSelfDeviceId` is known (via `E04-B12`), every outbound frame to
that peer must use THAT value as `RelayPacketFrame.destination`, not the
Bluetooth MAC — while `TransportService.connect`/`.send`'s own native-API
calls keep using the Bluetooth MAC exactly as today (Android's Bluetooth
APIs have no other way to open a socket; that requirement doesn't change).

## 1a. 🔒 Blocking precondition from E04-B12's own review (read this before writing any code)
`E04-B12`'s independent reviewer (opus) found and disclosed a real gap,
inert in that task but NOT inert here: **the identity announced via
`E04-B12`'s new protocol is unauthenticated.** `IdentityAnnounceService.
handleAnnounce` writes the announced string into
`Relationship.remoteSelfDeviceId` keyed by whichever link/Bluetooth
address the announce physically arrived on — which correctly prevents
device X from corrupting device Y's OWN relationship row (the
point-to-point, OS-bonded-connection reasoning in `E04-B12`'s own §3
point 3 is sound for THAT property). But nothing verifies that the
announced string is actually true — a malicious or compromised device
X can announce "my selfDeviceId is Y's-real-id" and this device will
believe it and store it.

**In `E04-B12` alone this was harmless — the reviewer verified nothing
reads the column.** This task is exactly the "something reads the
column" `E04-B12`'s reviewer warned about: if this task naively resolves
outbound addressing via a **reverse lookup** (`remoteSelfDeviceId` →
"which link reaches this identity"), a spoofed announcement becomes a
real hijack — messages meant for the real peer Y could be misdirected to
device X. **This task's own implementation MUST NOT do a bare reverse
lookup.** Before writing the resolution logic in §2, work out (and
record the reasoning here, in this section, once decided) which of these
directions — or another — actually closes the gap safely:
- **(a) Forward-only resolution, never reverse.** §2's own resolution
  direction is already forward (`Bluetooth MAC → remoteSelfDeviceId`,
  looked up FROM the connection you're already sending on, not searched
  FOR by identity) — confirm this stays true throughout implementation
  and never gets inverted into "find the link for identity X" anywhere,
  including in any future retry/reconnect logic. If this holds
  throughout, the spoofing risk may already be structurally avoided by
  this task's own natural shape — but verify this explicitly, in writing,
  against the actual implemented code, rather than assuming it from this
  description.
- **(b) If a reverse lookup turns out to be genuinely necessary anywhere**
  (e.g. re-establishing a connection to a peer identified only by their
  `selfDeviceId`, with no live Bluetooth link yet), that lookup MUST NOT
  trust an unauthenticated `remoteSelfDeviceId` alone — at minimum,
  require it to be corroborated by something already-authenticated (the
  Signal Protocol identity key from a previously-`establishSession`'d
  peer would qualify; a bare string match would not). If this situation
  arises, STOP and write it up as an Open Question rather than shipping
  an unauthenticated reverse lookup — this is exactly the kind of
  security-relevant judgment call that should not be made silently by an
  implementer alone.
- Whichever direction implementation actually takes, add a test that
  specifically proves a spoofed/incorrect `remoteSelfDeviceId` cannot
  cause a message to be misdirected to the wrong physical device.

**🔒 Resolved: option (a), forward-only, confirmed against the actual
implemented code (not assumed).** `resolveOutboundDestination(db,
peerDeviceId)` (new function, `messaging_stack.dart`) is the ONLY
resolution logic this task adds, and its single lookup key is
`peerDeviceId` — the Bluetooth-MAC-shaped id of the specific link the
caller is already using for `TransportService.connect`/`.send`
(`directSend`/`ConnectionEnsuringSender`). It is called from exactly two
sites (`PrekeyExchange._sendControlFrame`, `MessagingStack`'s own
`encryptAdapter`), and in both, the SAME `peerDeviceId` value is passed
to both the resolution call and the physical transport call immediately
after — grepped and confirmed: neither call site ever passes a
`remoteSelfDeviceId`-derived value into `directSend`/`enqueue`, and
neither ever queries the `relationships` table FOR a row matching a
claimed identity string (the reverse direction). `RelayEngine` and
`ConnectionEnsuringSender` were verified by full-file inspection to
construct no `RelayPacketFrame` at all (they only ever move opaque,
already-serialized bytes), so there was no second, hidden resolution
site to get wrong.

**Why this structurally closes the gap, not just "happens to" in
today's code:** which PHYSICAL DEVICE receives the bytes is decided
entirely by the `peerDeviceId`/MAC argument to
`TransportService.connect`/`.send` — a value `resolveOutboundDestination`
never touches or influences. A spoofed/wrong `remoteSelfDeviceId` can
only change what logical `destination` value gets stamped INSIDE the
frame the SAME physical peer already receives; at worst this makes that
peer's own `isForUs` check reject the frame (a safe, self-contained
failure — the peer just doesn't recognize the frame as theirs), never a
redirection to a different device, and never a session/message decrypted
by anyone other than whoever the crypto session (keyed by `peerDeviceId`
throughout, untouched by this task) was actually established with.

**Falsification test:**
`test_E04_B13_spoofed_remoteSelfDeviceId_cannot_misdirect_a_message`
(`prekey_exchange_test.dart`) — device A is physically connected only to
device B, but A's relationship row for that link has been corrupted to
claim the peer is a THIRD, uninvolved real device C (never wired to A at
all). Proves: C never receives anything (`c.prekeyExchange.counters
.requestsServed == 0`, and structurally cannot — there is no transport
path from A to C in this test); B receives the bytes but its own
`isForUs` correctly rejects the mis-stamped frame
(`b.prekeyExchange.counters.requestsServed == 0`); the ONLY observable
effect is `a.prekeyExchange.counters.timeouts == 1` — an honest,
bounded failure, identical in shape to an unresolved (never-announced)
peer, never a security breach. No reverse lookup was ever written, so
§1a option (b)'s Open-Question path was not needed.

## 2. What this task does
1. **Resolve the real destination id before constructing a
   `RelayPacketFrame`**, at every outbound call site that currently uses
   a raw `deviceId` (Bluetooth MAC) as `frame.destination`:
   - `PrekeyExchange._sendControlFrame` (`destination: peerDeviceId` today
     — this is the exact line whose output `E04-B12`'s own diagnostic
     instrumentation captured and confirmed was the Bluetooth MAC).
   - Any other `RelayPacketFrame` construction site this task's own
     `required_context` files reveal (`RelayEngine`'s own frame handling,
     if it constructs frames rather than only forwarding opaque bytes —
     confirm from the code, don't assume; `RelayEngine`'s own
     `files:` fence history notes it forwards ORIGINAL wire bytes for
     relayed traffic and never re-serializes, so it may turn out this
     task's real second call site is elsewhere — verify against the
     actual current code, not this description).
   - The resolution itself: look up `Relationship.remoteSelfDeviceId` for
     the target Bluetooth MAC; if non-null, use it as the frame's
     `destination`; if still `null` (peer hasn't announced yet — e.g. the
     very first frame sent before `E04-B12`'s announce has round-tripped),
     fall back to the raw Bluetooth MAC exactly as today (preserves
     current behavior for that narrow race window, never regresses it).
2. **Set `frame.source` to this device's own REAL `selfDeviceId`
   consistently** (confirm every outbound frame construction site already
   does this correctly — `_sendControlFrame` already uses
   `_stack.selfDeviceId` for `source`, per `E04-B12`'s own diagnostic
   capture; this task's job is to verify no OTHER call site got this
   wrong, not to introduce it fresh).
3. **Handle the resolution race explicitly and test it**: what happens
   when device A sends a frame to device B before B's identity-announce
   has arrived at A? Per point 1's fallback, A's frame goes out addressed
   by B's Bluetooth MAC — which will still fail `isForUs` on B exactly as
   today, UNLESS `E04-B12`'s own automatic-announce-on-connect ordering
   guarantee (fires before any application code runs) is actually
   airtight. Write a test that proves the ordering guarantee holds under
   realistic concurrency (e.g. `MessagingStack`'s own connection-state
   listener firing the announce vs. `ensureSession` being called
   immediately after `connect()` resolves) — do not assume `E04-B12`'s
   ordering claim without proving it under this task's own outbound path.
4. **Verify live, end-to-end, on real hardware**: a message composed on
   one physical device genuinely arrives, decrypts, and renders on the
   other device's chat thread — the actual, original symptom this whole
   `E04-B11`→`E04-B12`→`E04-B13` chain exists to fix. This is the
   Definition of Done this entire investigation has been building toward
   since the human's first "I can not send any message" report.

## 3. What this task does NOT do
- Does NOT re-implement or modify `E04-B12`'s own announce protocol,
  schema column, or `isForUs` bypass — this task only CONSUMES
  `remoteSelfDeviceId`, it does not change how it's populated.
- Does NOT change `Relationship.deviceId`, the Devices screen, or any UI
  file — same scope boundary `E04-B12` established.
- Does NOT add a keep-alive/re-announce/staleness-detection mechanism for
  `remoteSelfDeviceId` (e.g. re-announcing periodically, or invalidating
  a stale value) — out of scope; if a real need for this surfaces during
  implementation, log it as an Open Question, don't build it unasked.
- Does NOT touch `CryptoService`, identity-key trust logic, or anything
  in `E04-B11`'s own already-fixed `send()`/`TaskQueue` code.

## Risks
- This is the task whose live-hardware verification actually proves the
  ENTIRE investigation's fix — if it fails live, the problem could be in
  either this task's own resolution logic OR a subtlety in `E04-B12`'s
  announce mechanism that its own narrower verification (column
  populated, not full message delivery) didn't catch. Budget for the
  possibility that a real defect found here sends a fix back into
  `E04-B12`'s own file, not just this one.
- Same hardware-availability constraint as every prior task in this
  chain: needs two real physical devices with real Bluetooth radios,
  together, at the same time.

## Definition of Done
- [x] 🔒 §1a's precondition explicitly addressed: resolution direction
  confirmed forward-only (option (a) — no reverse lookup was written at
  all), with a dedicated test proving a spoofed `remoteSelfDeviceId`
  cannot misdirect a message:
  `test_E04_B13_spoofed_remoteSelfDeviceId_cannot_misdirect_a_message`
  (`prekey_exchange_test.dart`) — see §1a above for the full reasoning
  and what the test proves.
- [x] Every outbound `RelayPacketFrame` construction site for 1:1
  messaging resolves the peer's `remoteSelfDeviceId` when known, falls
  back to the Bluetooth MAC when not yet known.
  `PrekeyExchange._sendControlFrame` and `MessagingStack`'s own
  `encryptAdapter` (the real chat-message path — see Deviations for why
  this file was added to the fence). `RelayEngine` and
  `ConnectionEnsuringSender` construct no `RelayPacketFrame` at all
  (verified by full-file inspection) and needed no change.
  `test_E04_B13_outbound_control_frame_uses_learned_remoteSelfDeviceId`
  and
  `test_E04_B13_encrypt_adapter_resolves_learned_remoteSelfDeviceId_for_frame_destination`
  prove both sites; `test_E04_B13_encrypt_adapter_falls_back_to_mac_when_not_yet_announced`
  and the pre-existing `test_EARS_COMM_6_encrypt_adapter_produces_a_parseable_frame`
  prove the fallback regresses nothing.
- [x] The pre-announce race window (point 3) is tested, not just assumed
  safe. `test_E04_B13_pre_announce_race_falls_back_safely_without_crashing`
  calls `ensureSession` immediately after a connection reaches `connected`
  (no settle beyond what discovery itself needs), asserting BOTH possible
  real outcomes are safe: the announce wins (session establishes) or it
  loses (an honest, bounded `TimeoutException`, no crash, no session
  against the wrong address) followed by a successful retry once the
  announce has actually landed — proving the window is narrow and
  self-healing, not a permanent failure, per E04-B12's own carried-forward
  #4 finding that the ordering is not airtight.
- [ ] Live, on real hardware: a composed message genuinely arrives,
  decrypts, and renders on the receiving device's chat thread — **NOT
  achieved this session, honestly reported.** `adb devices -l` at dispatch
  time showed the Pixel 8 Pro (`192.168.0.108:9999`) as `offline`; a fresh
  `adb connect` attempt failed outright ("connection attempt failed...").
  The Redmi 10 2022 (`e4813f500601`) alone cannot exercise a two-device
  send. This is an environment/connectivity limitation, not a defect in
  this task's own code, and this task's `files:` fence gives no lever on
  it (no network/adb configuration file is in scope). Per this task's own
  Risks section ("if real hardware is available... otherwise, rely on the
  Dart-level integration tests"), the full send/receive round trip is
  proven instead by real two-`MessagingStack` integration tests over the
  actual mocked Pigeon transport boundary (`prekey_exchange_test.dart`,
  `messaging_stack_test.dart`) — the same fidelity bar E04-B12's own
  tests already established and this project has repeatedly accepted for
  this exact class of protocol. See Run log for the full disclosure.
- [x] Existing test suite green; new coverage added for the resolution
  logic and the race-window test above. `flutter test`: 1512/1512
  passing. `flutter analyze`: 0 issues (2 pre-existing, unrelated infos —
  same two E04-B12's own review already disclosed).

## Implementation checklist
- [x] `resolveOutboundDestination(AppDatabase db, String peerDeviceId)`
  added (`messaging_stack.dart`) — forward-only lookup of
  `Relationship.remoteSelfDeviceId` keyed by the Bluetooth MAC a
  connection is already open on; falls back to that MAC when not yet
  learned `a8d9133`
- [x] Wired into `PrekeyExchange._sendControlFrame` (session-bootstrap
  control frames) — `directSend`'s own transport call keeps using the raw
  MAC unchanged, only `RelayPacketFrame.destination` is resolved `a8d9133`
- [x] Wired into `MessagingStack`'s own `encryptAdapter` (the real
  chat-message path, out-of-fence widening — see Deviations) — same
  split: `encrypt`/`enqueue` keep using the raw MAC/conversationId
  unchanged, only the wire frame's `destination` field is resolved
  `a8d9133`
- [x] Fixed a latent response-provenance bug this task's own tests
  surfaced in `PrekeyExchange._ensureSessionUncoalesced`:
  `_outstandingRequests` must track the peer's RESOLVED identity (what an
  honest response's `frame.source` will actually equal), not the raw MAC
  `ensureSession`'s own caller addressed the request by — these were only
  ever the same string by coincidence in every test written before this
  task existed `a8d9133`
- [x] Verified by inspection: `RelayEngine` and `ConnectionEnsuringSender`
  construct no `RelayPacketFrame` (they only move already-serialized
  opaque bytes) — no code change needed in either, despite both being
  listed in `files:` `a8d9133`
- [x] Tests: outbound resolution (control-frame path
  `test_E04_B13_outbound_control_frame_uses_learned_remoteSelfDeviceId`;
  chat-message path
  `test_E04_B13_encrypt_adapter_resolves_learned_remoteSelfDeviceId_for_frame_destination`),
  fallback-when-unknown (both paths), the pre-announce race
  (`test_E04_B13_pre_announce_race_falls_back_safely_without_crashing`),
  and the §1a spoofing falsification
  (`test_E04_B13_spoofed_remoteSelfDeviceId_cannot_misdirect_a_message`)
  `a8d9133`
- [x] `flutter test` 1512/1512, `flutter analyze` clean (2 pre-existing
  infos) `a8d9133`

## Open Questions
- **Other `RelayPacketFrame` construction sites outside 1:1 messaging
  carry the identical unresolved-MAC-addressing defect and were
  deliberately NOT touched by this task**, since fixing them is outside
  both this task's `files:` fence and its own stated scope (§1: "a
  message sent to a peer" — the original human bug report was about 1:1
  chat messages specifically). Found by the same `grep -rn "destination:"
  lib/` this task used to locate its own two real call sites:
  `lib/core/messaging/delivery_ack.dart:509`,
  `lib/core/calls/call_signaling.dart:1051`,
  `lib/features/groups/domain/group_membership_service.dart:643`,
  `lib/features/groups/domain/send_group_message_use_case.dart:271`,
  `lib/core/crypto/group_crypto_service.dart:344`,
  `lib/features/location/domain/location_share_service.dart:291`. Each
  will structurally fail `isForUs` on two real, independently-provisioned
  devices for the exact same reason 1:1 messages did before this task —
  delivery acks, call signaling, group messaging, and location sharing
  are all still broken between two real devices. Recommend the planner
  file a follow-up bug task (or tasks) to apply the same
  `resolveOutboundDestination` pattern to each, now that this task has
  established and proven it. Not fixed here to avoid an L-sized scope
  explosion inside a task filed as `size: M`.
- Live, on-hardware end-to-end verification (DoD item 4) could not be
  attempted this session — see that DoD item and the Run log for the
  full disclosure (Pixel 8 Pro was `offline` in `adb devices -l`, and a
  fresh `adb connect` attempt failed). This is a resource/connectivity
  gap, not a code gap; recommend a follow-up live-hardware verification
  pass (by any agent, on any platform in `harness.yaml: platforms`) once
  both devices are simultaneously reachable again, before the human
  treats the original bug report as fully closed in practice.

## Deviations
- **`lib/core/messaging/messaging_stack.dart` added to `files: update`
  during implementation — not in the original fence.** The task's own §2
  point 1 anticipated a second `RelayPacketFrame` construction site might
  "turn out to be elsewhere" than `relay_engine.dart`, and directed
  verifying against the actual code rather than the task's own
  description. Verified: `relay_engine.dart` and
  `connection_ensuring_sender.dart` (both in the original fence) never
  construct a `RelayPacketFrame` at all — confirmed by reading both files
  in full; they only ever move already-serialized opaque bytes. The real
  second (and, for the human-reported bug's own purposes, PRIMARY) site
  is `messaging_stack.dart`'s own `encryptAdapter`, which builds the
  `RelayPacketFrame` for every actual chat message
  (`SendMessageUseCase` -> `encryptAdapter` -> `enqueue`) — this is the
  literal frame the original "I can not send any message" bug report is
  about, more so than `PrekeyExchange`'s own session-bootstrap control
  frames. Leaving this file out of the fence would have shipped a task
  that fixes session establishment but not actual message delivery,
  defeating this task's own stated purpose (§1: "actually closes the
  loop"). The change added is narrow and additive: one new top-level
  function plus a 6-line resolution call inserted immediately before the
  existing `RelayPacketFrame(...)` construction in `encryptAdapter` — the
  `encrypt`/`enqueue` calls immediately before/after it are UNCHANGED,
  still addressed by the raw `recipientDeviceId` exactly as before this
  task, per §1's own explicit requirement that transport/routing
  addressing stay Bluetooth-MAC-keyed.
- **Fixed `PrekeyExchange._ensureSessionUncoalesced`'s response-provenance
  tracking** (`_outstandingRequests[requestId] = _OutstandingRequest(...)`)
  to store the RESOLVED peer identity rather than the raw
  `peerDeviceId`/MAC `ensureSession`'s own caller passed in — this is
  inside the already-fenced `prekey_exchange.dart`, not a new
  out-of-fence file, but is called out here because it was not explicitly
  anticipated by the task's own §2/§3: this task's own
  `test_E04_B13_outbound_control_frame_uses_learned_remoteSelfDeviceId`
  test failed against the FIRST version of this task's implementation
  (destination resolved for the outbound frame but the provenance check
  left comparing against the raw MAC) until this fix was added — a real,
  reproducible defect that would have silently broken every legitimate
  bundle-response acceptance between two real devices once their MAC and
  real `selfDeviceId` genuinely differ (i.e., on real hardware, always).
  Not a security weakening: comparing the resolved real identity against
  `frame.source` (which is always the responder's own real `selfDeviceId`)
  is the CORRECT comparison the file's own header already claimed to make
  ("its stored `peerDeviceId` equals `frame.source`") — before this fix
  that claim was accidentally true only because every pre-existing test
  used the same string for both a peer's MAC and its `selfDeviceId`.
- Diagnostic `print()`-based debugging was used transiently while
  developing the pre-announce race test (to observe
  `IdentityAnnounceCounters` timing) — added, observed, then fully
  removed before committing (confirmed via the committed diff containing
  no `print` calls in test or production code), same pattern this
  project has used repeatedly (`E04-B08`, `E04-B11`, `E04-B12`).

## Run log
- 2026-09-12, `claude-sonnet-5 (builder)`: implemented per §2 exactly, with
  the fence widened to `messaging_stack.dart` (Deviations). `flutter
  test`: 1512/1512 passing (full suite, including 5 new tests across
  `prekey_exchange_test.dart`/`messaging_stack_test.dart`). `flutter
  analyze`: 0 issues (2 pre-existing, unrelated infos in files this task
  does not touch — the same two E04-B12's own review already disclosed).

  **Live hardware check, at dispatch time:** `adb devices -l` showed:
  ```
  e4813f500601           device product:selene_global model:21121119SG
  192.168.0.108:9999     offline product:husky model:Pixel_8_Pro
  ```
  The Pixel 8 Pro was `offline`. A fresh `adb connect 192.168.0.108:9999`
  attempt failed outright: "cannot connect to 192.168.0.108:9999: A
  connection attempt failed because the connected party did not properly
  respond..." — the device remained `offline` afterward. With only one of
  the two required physical devices reachable, no live two-device
  send/receive pass could be attempted this session (this task needs
  BOTH devices simultaneously, unlike E04-B12 which could at least pull
  each device's own database independently). This is an honest
  environment/connectivity gap, not a defect in this task's own code —
  disclosed rather than skipped silently, matching this project's own
  established disclosure standard (`E04-B08`, `E04-B12`).

  **Substituted evidence, at the same fidelity bar this project has
  already accepted for this exact class of protocol
  (`prekey_exchange_test.dart`'s own established pattern, reused by
  `identity_announce_test.dart` in E04-B12):** every test added this
  session drives real, independent `MessagingStack` instances through
  the REAL `TransportService`/Pigeon mock boundary (not a fake in-Dart
  callback) — `test_E04_B13_outbound_control_frame_uses_learned_remoteSelfDeviceId`
  and `test_E04_B13_spoofed_remoteSelfDeviceId_cannot_misdirect_a_message`
  deliberately use a Bluetooth-MAC-shaped id DIFFERENT from each stack's
  own real `selfDeviceId` (mirroring E04-B12's own live-captured values
  `B8:DB:38:7C:D4:BF` vs. `aecdcd6f9b32dc0f` exactly), which the
  pre-existing test harness's own `wireStacks` helper could never
  exercise (it conflates MAC and `selfDeviceId` by construction) — this
  is precisely why a fresh test file section was needed rather than
  extending the existing ones. `test_E04_B13_encrypt_adapter_resolves_learned_remoteSelfDeviceId_for_frame_destination`
  proves the SAME resolution through the actual chat-message path
  (`SendMessageUseCase` -> `MessagingStack`'s `encryptAdapter`), round-tripping
  through a real second `CryptoService`/`DriftSignalProtocolStore` party
  that genuinely decrypts the result — the same "stronger than byte
  equality" standard `test_EARS_COMM_6_encrypt_adapter_produces_a_parseable_frame`
  already established.

  Reported honestly: this task's live-hardware DoD item is NOT met this
  session. The resolution logic itself, and the specific security
  property §1a required, are proven at the Dart-integration-test level
  described above — not a claim of full end-to-end hardware proof.

## Review
(pending dispatch)
