---
id: E04-B02
epic: E04
type: bug
title: Relay packets in forwarding/delivered state are never TTL-swept or deleted — a relay device accumulates other peers' ciphertext blobs indefinitely, contradicting the epic's "ephemeral" data model
layer: backend
size: S
status: done
owner_agent: builder
model: any
preferred_agent: any
started_at: 2026-08-27
completed_at: 2026-08-27
executed_by: claude-sonnet-5 (builder)
reviewed_at: 2026-08-27
reviewed_by: claude-opus-5 (reviewer)
review_outcome: approved-with-reviewer-fix
token_estimate: { tier: S, range: "5k-15k" }
severity: S3
priority: { moscow: should, p: P2 }
depends_on: [E04-T04]
blocks: []
traces_to: [FR-ROUTE-004, NFR-SEC-001]
required_context: [epics/E04-mesh-routing/tasks/E04-T04.md, epics/E04-mesh-routing/epic.md]
design_contract: n/a
found_by: reviewer (claude-opus-5) during the E04 end-of-epic bug sweep
found_in: E04-T04's state machine vs. epic.md's "local only, ephemeral" data-model claim — no task owned retention
files:
  create:
    - test/core/routing_engine/relay_retention_test.dart
    - test/core/persistence/relay_retention_migration_test.dart
  update:
    - lib/core/routing_engine/relay_engine.dart
    - lib/core/persistence/relay_tables.dart
    - lib/core/persistence/database.dart
    - lib/core/persistence/database.g.dart
    - epics/E04-mesh-routing/epic.md
  delete: []
---
# E04-B02 · Forwarded relay packets are retained forever

## What this bug is NOT
The T04 reviewer flagged `forwarding` as a possible "dead-end terminal
state" where a packet is stuck forever if the next hop silently fails. The
sweep investigated that specific concern and **it is not a defect.** Read
against `relay_engine.dart:190-196`, once `_send` succeeds this device has
genuinely completed its one physical hop, and the next hop is a different
device running its own `RelayEngine`. Retrying from here without an ack or
dedup mechanism would risk duplicate delivery, and ack/dedup is explicitly
E05's job per `E04-T04.md` §4. The code's own reasoning at
`relay_engine.dart:41-43` is correct and the state machine is right.

## The actual defect: nothing ever reclaims those rows
`sweepExpired()` (`relay_engine.dart:212-224`) filters on
`deliveryState.equals(RelayDeliveryState.queued.name)`. Nothing in
`RelayEngine` — or anywhere in the repo — ever deletes a `relay_packets`
row, in any state. So every packet this device ever relays leaves a
permanent row **including its full `payload` BLOB**: another peer's
encrypted message bytes, held on this device forever.

`epic.md:39-42` (§Data model) states the relay store is *"local only,
**ephemeral**"*. As merged, it is local only and permanent. No task owned
retention: T04's §3 says rows are "kept as a row for diagnostics per a later
epic's needs (E13)" — a reasonable decision about *state*, silently extended
into an unbounded-retention policy about *payloads*.

Two consequences:
1. **Storage growth (unbounded).** A device that relays for a busy mesh
   accumulates one full message payload per relayed packet, forever, with no
   cap, no eviction, and no user-visible way to reclaim it.
2. **Data retention (NFR-SEC-001).** A relay hop is a party that was
   supposed to hold someone else's ciphertext *temporarily*. Indefinite
   retention of third-party ciphertext at rest widens the blast radius of a
   device compromise well beyond what the epic's own data model promises.
   This is not a confidentiality break — FR-ROUTE-003 / EARS-ROUTE-3 holds,
   the payload is never decrypted or inspected — it is a retention-window
   defect.

## Severity: S3 (broken, but there is a workaround)
Nothing malfunctions today, and a later epic *can* add cleanup, which is the
workaround — so this is not S2. It is not S4 because it is neither cosmetic
nor minor: it is unbounded on-device storage growth plus indefinite
third-party-ciphertext retention, in direct contradiction of a written
data-model guarantee. Cheap to fix now; progressively more awkward once E05
is generating real relay volume against a schema whose retention semantics
nobody has decided.

Severity per `skills/bug-sweep` is the reviewer's call. **Priority is the
human's** (`bug_priorities` gate, rule 3) — left `TBD-human`.

## Repro (exact, from a fresh state — reviewer-verified 2026-08-27)
Verified with a throwaway probe test on `epic_04` at commit `cbc2efd`.

1. `RoutingEngine(selfId: 'A')` with links A→B and B→D recorded, so the
   route to D is 2 hops (`hops.length == 2`) and the forward is therefore
   marked `forwarding`, not `delivered`.
2. `RelayEngine(selfId: 'A', db: <in-memory>, routingEngine: <that engine>,
   send: (hop, bytes) async => true, clock: () => now)` with an injectable
   `now`, starting at `2026-01-01`.
3. `await enqueue('D', <3 bytes>, 0, Duration(minutes: 1))`
4. `await processQueue()` → the row's `delivery_state` is `forwarding`. ✅
   correct so far.
5. Advance the injected clock by **365 days** — far past the 1-minute TTL —
   and `await sweepExpired()`.

**Expected** (FR-ROUTE-004's expiry semantics; `epic.md`'s "ephemeral"):
a packet past `expires_at` is no longer retained in a form that holds its
payload. At minimum the TTL applies to the payload regardless of which
non-`queued` state the row reached.

**Actual:** probe output —
`swept=0 state=forwarding payload_still_present=3 bytes`.
The sweep returns 0, the state is unchanged, and the payload BLOB is still
there a year after a one-minute TTL expired. The same holds for `delivered`
and for rows already marked `expired`.

## Root cause
`relay_engine.dart:212-218` — `sweepExpired()`'s `where` clause is scoped to
`queued` only:
```dart
..where((t) =>
    t.deliveryState.equals(RelayDeliveryState.queued.name) &
    t.expiresAt.isSmallerOrEqualValue(now))
```
That scoping is correct for its stated job (take expired packets *out of the
active queue*). The gap is that no second mechanism exists for the other
half of the lifecycle: reclaiming storage from rows that have left the queue
by any route — `forwarding`, `delivered`, or `expired`.

## Proposed fix direction (builder to confirm; do not treat as prescriptive)
The diagnostics need (T04 §3 / E13) and the retention need are separable —
E13 wants to know *that* a packet passed through, not *what was in it*.

- Add a payload-reclaiming pass, e.g.
  `RelayEngine.reclaimPayloads({Duration retainFor})`, that nulls or empties
  the `payload` BLOB for any row past `expires_at` in a terminal state
  (`forwarding` / `delivered` / `expired`) while leaving the id,
  destination, size, timestamps and state intact for diagnostics. This
  requires making `payload` nullable — a schema change (v9→v10) and
  therefore additive-migration work with a prior-version test, per
  `docs/conventions.md` "Schema migrations" and the pattern now in
  `test/core/persistence/routing_migration_test.dart`.
- Alternatively, if the human decides diagnostics do not justify keeping
  terminal rows at all, delete them outright past a retention window — a
  smaller change with no schema impact. **This is a data-retention policy
  choice, so it is the human's call (rule 3), not the builder's.** Present
  both options and stop, rather than picking one.
- Either way, write the chosen retention window down in `epic.md`'s §Data
  model so "ephemeral" becomes a number instead of an adjective.

**🧍 Resolved, 2026-08-27:** reclaim (null the `payload` BLOB), not delete
— keep the row (id/destination/size/timestamps/state) for E13 diagnostics.
**Retention window: reclaim as soon as a terminal-state row (`forwarding` /
`delivered` / `expired`) passes its own `expires_at`** — no additional
grace period beyond the TTL the packet was originally enqueued with, since
that TTL is already the caller's stated "how long is this worth keeping"
signal. `epic.md`'s §Data model must record this exact rule as the
"ephemeral" definition. Requires the nullable-`payload` schema change
(v9→v10, additive) per the proposal above.

## Regression test (required before this closes)
`test/core/routing_engine/relay_retention_test.dart` — must fail on
`cbc2efd` and pass after:

- `test_FR_ROUTE_004_expired_forwarding_packet_payload_is_reclaimed` — the
  §Repro above, asserting the payload is gone after the reclaim pass.
- `test_FR_ROUTE_004_delivered_packet_payload_is_reclaimed` — same for the
  1-hop `delivered` path.
- `test_FR_ROUTE_004_live_queued_packet_payload_is_untouched` — the negative
  case: an unexpired `queued` packet must keep its payload, or the fix eats
  in-flight traffic.
- Existing `sweepExpired()` coverage in
  `test/core/routing_engine/relay_engine_test.dart` must stay green.

## Implementation checklist
- [x] `relay_packets.payload` made nullable; schema v9 -> v10, `onUpgrade`
      migration rebuilds the table (SQLite has no ALTER COLUMN) preserving
      every existing row/column `741da1f`
- [x] `RelayEngine.reclaimPayloads()` implemented — nulls `payload` for
      terminal-state (`forwarding`/`delivered`/`expired`) rows past their
      own `expires_at`, leaves `queued` rows untouched `741da1f`
- [x] Regression tests written and confirmed to fail on the pre-fix code
      (`reclaimPayloads` undefined), then pass after the fix `444bc4b`
- [x] v9->v10 migration test hand-builds a real prior-version schema and
      proves the rebuild preserves data for rows in every delivery state
      `444bc4b`
- [x] `epic.md` §Data model states the retention rule explicitly `4597c41`
- [x] `flutter analyze` clean, `flutter test` green (full suite, 123 tests)
      `4597c41`

## Definition of Done
- [x] 🧍 Human has chosen reclaim-vs-delete and the retention window —
      already resolved in this file (2026-08-27): reclaim, no grace period
      beyond the packet's own TTL. Not re-signed here per rule 3.
- [x] Regression tests written FIRST, failing on the current `epic_04` head
      — confirmed via `git stash` of the lib changes + `flutter test`,
      compile-failed with "reclaimPayloads isn't defined"; restored and
      green after the fix
- [x] Any schema change is additive, with a prior-version migration test —
      v9->v10, nullable-only; `relay_retention_migration_test.dart` hand-
      builds v9 and v7 prior schemas
- [x] Full suite green and `flutter analyze` clean — 123/123 tests, 0
      analyze issues
- [x] `epic.md` §Data model states the retention window explicitly
- [x] Reviewer re-verified against the §Repro above — independently, not
      against the builder's claim. See the reviewer entry in §Run log:
      migration rebuild audited statement by statement (one blocking
      atomicity defect found and fixed by the reviewer), reclaim scoping
      and the sweep→reclaim ordering contract verified, negative cases
      confirmed to assert byte-identical survival, null-safety swept
      repo-wide. `flutter analyze` 0 issues, `flutter test` 124/124.

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- 2026-08-27, `claude-sonnet-5 (builder)`: made `relay_packets.payload`
  nullable (schema v9 -> v10, table-rebuild migration since SQLite has no
  ALTER COLUMN) and added `RelayEngine.reclaimPayloads()`, which nulls the
  `payload` BLOB for any terminal-state row (`forwarding`/`delivered`/
  `expired`) once it passes its own `expires_at` — no grace period beyond
  the packet's original TTL, per this file's resolved retention decision.
  `queued` rows (even expired-but-unswept ones) are never touched, so
  in-flight traffic is never eaten.

  Wrote `test/core/routing_engine/relay_retention_test.dart` (the task's
  three named tests plus a fourth negative case for an expired-but-unswept
  queued row) and `test/core/persistence/relay_retention_migration_test.dart`
  (v9->v10 and v7->v10 onUpgrade coverage, following
  `routing_migration_test.dart`'s hand-built-prior-schema pattern). Confirmed
  red-first by stashing the `lib/` changes and re-running the new test file
  — it failed to compile (`reclaimPayloads` undefined) — then restored.

  Necessary side effect of the schema change: `RelayPacketsCompanion`'s
  generated `payload` field is now `Value<Uint8List?>` instead of a bare
  `Uint8List`, so two pre-existing call sites in
  `test/core/persistence/routing_migration_test.dart` (not in this task's
  `files:` list) needed `payload: Value(...)` instead of a bare value to
  keep compiling — a mechanical fix with no behavior change, plus dropping
  an import that became unnecessary. `relay_engine.dart`'s own `enqueue()`
  needed the same one-line fix.

  §Repro verified fixed: `test_FR_ROUTE_004_expired_forwarding_packet_payload_is_reclaimed`
  reproduces the task's exact scenario (2-hop route A->B->D, `forwarding`
  state, clock advanced 365 days past a 1-minute TTL) and asserts the
  payload is gone after `reclaimPayloads()`, while the row (state,
  destination, size) survives. Negative case verified genuinely tested:
  `test_FR_ROUTE_004_live_queued_packet_payload_is_untouched` enqueues a
  packet with no route (stays `queued`, well within its 10-minute TTL),
  calls `reclaimPayloads()`, and asserts both the return count is 0 and the
  payload bytes are still present and byte-identical.

  Full suite: 123/123 passing (117 pre-existing + 6 new). `flutter analyze`:
  0 issues.

- 2026-08-27, `claude-opus-5 (reviewer)`: 📋 **APPROVE (with one reviewer
  fix).** Rule 5 satisfied — `executed_by` = claude-sonnet-5,
  `reviewed_by` = claude-opus-5. Verified independently, not from the PR
  body: `flutter analyze` → 0 issues; `flutter test` → 123/123 on the
  builder's head, 124/124 after the reviewer's added test. Scope is
  in-contract; the only out-of-`files:` change is the disclosed one below.

  **🔴 BLOCKING FINDING (fixed by the reviewer) — `database.dart:165-186`,
  the v9→v10 rebuild was not atomic.** The four statements (CREATE / INSERT
  SELECT / DROP / RENAME) ran bare on the migration connection. Drift does
  **not** wrap `onUpgrade` in a transaction — `drift-2.34.3`'s
  `src/runtime/executor/helpers/engines.dart` `_runMigrations` calls
  `user.beforeOpen(...)` directly, with no `beginTransaction`, and it only
  writes the new `user_version` *after* `onUpgrade` returns (same function,
  `DynamicVersionDelegate` branch). Consequences on a real device, with a
  window that widens with `relay_packets` row count (exactly the unbounded
  growth this bug is about):
  - Kill/crash between CREATE and DROP → `user_version` still 9, but a
    stale `relay_packets_v10` is left behind. Next launch retries the step
    and dies on `table relay_packets_v10 already exists` → **the database
    is permanently unopenable**. Not recoverable by the user.
  - Kill between DROP and RENAME → no `relay_packets` at all, plus the same
    unopenable-retry. **Total relay-store data loss.**
  Fixed by wrapping the four statements in `m.database.transaction(...)`
  (drift's own documented in-migration pattern —
  `src/runtime/query_builder/migration.dart:594-598`), so the step is
  all-or-nothing: a failure rolls the rebuild back, `user_version` stays 9,
  and the next launch retries from a clean v9. Rationale recorded inline in
  `database.dart` so it survives the next migration author. Both migration
  tests still pass, which also proves a transaction is legal inside
  `onUpgrade` here.

  **Column mapping / rebuild correctness — verified, correct.** The
  `INSERT ... SELECT *` is positional, so it is only safe if old and new
  column orders match exactly. They do: the v9 DDL, the new DDL, and
  `$RelayPacketsTable.$columns` (`database.g.dart:2686-2695`) are all
  `id, destination_id, payload, priority, size_bytes, created_at,
  expires_at, delivery_state`. All 8 columns are carried; no column is
  dropped or defaulted. `PRIMARY KEY (id)` is preserved and matches
  `relay_tables.dart:56`. No indices or foreign keys exist on this table,
  so nothing is silently lost with the dropped table. Added a comment
  freezing this positional dependency against a future column reorder.

  **Migration test is real, not a fresh-DB stand-in** —
  `relay_retention_migration_test.dart:110-124` hand-builds the genuine v9
  DDL with `payload BLOB NOT NULL`, seeds three rows in three delivery
  states (`queued`/`forwarding`/`delivered`) with real BLOB bytes plus
  neighbouring `device_identities`/`routes` rows, sets `userVersion = 9`,
  then asserts every column of every row survives (`:175-191`) — not merely
  that the column became nullable. It then proves the constraint itself was
  relaxed both via update (`:196-202`) and via a fresh NULL insert
  (`:210-225`), and that unrelated v7/v8 tables are untouched
  (`:229-233`). The v7→v10 test (`:237-277`) covers the skip-the-rebuild
  path. This is the strongest migration test in the repo.

  **Reclaim logic — verified against the human-resolved contract.**
  `relay_engine.dart:261-266` requires all three of: state ∈
  {`forwarding`,`delivered`,`expired`}, `expires_at <= now`, and
  `payload IS NOT NULL`. No grace period — boundary is `<=`, consistent
  with `processQueue`'s own `row.expiresAt <= now` skip
  (`relay_engine.dart:156`), so there is no window where a row is still
  forwardable but its payload is gone. `queued` is excluded, so in-flight
  traffic is never eaten.

  **The queued/expired ordering question — checked, no race, no trap.** A
  `queued` row past its expiry keeps its payload until `sweepExpired()`
  transitions it to `expired`; `reclaimPayloads()` correctly leaves it
  alone. Calling reclaim *before* sweep is a harmless no-op, not a lost
  reclaim — the row simply becomes eligible on the next reclaim pass. So
  the two methods are order-independent for correctness and only affect
  reclaim *latency* by one cycle. A caller cannot get this wrong in a way
  that loses a live payload or retains a dead one indefinitely, provided it
  calls both periodically. Reclaim is also idempotent (the
  `payload.isNotNull()` guard), so a timer-driven caller never re-writes
  rows it already cleaned. Concurrency: `_attempt` dereferences
  `row.payload!` (`:191`) on a row pre-fetched while `queued`, and reclaim
  only ever touches non-`queued` rows, so the `!` cannot throw even under
  interleaved `processQueue`/`reclaimPayloads` calls.

  **Negative case is genuine, not a smoke test** —
  `relay_retention_test.dart:184-226` asserts `reclaimed == 0` *and*
  `row.payload, orderedEquals([5, 5, 5])` — byte-identical survival of an
  unexpired `queued` packet, plus the state is still `queued`. The
  builder's fourth test (`:228-264`) additionally pins the
  expired-but-unswept `queued` row.

  **Reviewer-added test** (coverage gap, non-blocking):
  `test_FR_ROUTE_004_sweepExpired_then_reclaim_reclaims_expired_payload`.
  The builder's three named tests cover `forwarding` and `delivered` but
  never exercised `expired` — the third terminal state the contract names,
  and the only one reached through a *second* method. The new test drives
  the full `enqueue → expire → reclaim(no-op) → sweepExpired →
  reclaim(1) → reclaim(0)` sequence, pinning both the ordering contract and
  idempotence. Suite is 124/124 with it.

  **Null-safety sweep (nullable column, repo-wide) — clean.** Grepped
  every `payload` reference in `lib/` and `test/`. Exactly one non-null
  dereference exists, `relay_engine.dart:191`, and it is justified above.
  `enqueue`'s `payload.length` (`:120`) reads the non-nullable *parameter*,
  not the column. `relay_engine_test.dart:114`'s `orderedEquals` works
  unchanged against a nullable field. No `row.payload.length`-style hazard
  anywhere.

  **`epic.md` §Data model — states the rule, not an adjective.** `epic.md`
  now defines "ephemeral" as reclaim "the instant a row in a terminal state
  (`forwarding`/`delivered`/`expired`) is past its own `expires_at` … No
  additional grace period beyond the packet's original TTL", and separates
  payload lifetime from row lifetime. Matches the 🧍-resolved decision in
  this file verbatim. Satisfies the task's "a number instead of an
  adjective" requirement.

  **Disclosed deviation accepted** —
  `test/core/persistence/routing_migration_test.dart` (not in `files:`),
  two `payload: Uint8List.fromList(...)` → `payload: Value(...)` call sites
  plus dropping a now-redundant `dart:typed_data` import (drift re-exports
  `Uint8List`). Genuinely mechanical: forced by
  `RelayPacketsCompanion.payload` becoming `Value<Uint8List?>`, wraps the
  identical bytes, and both tests still assert the same pre-existing
  behaviour and pass. Nothing masked.

  **Advisory, NOT blocking — for the planner, not for this task:**
  1. `RelayDeliveryState.failed` is excluded from `terminalStates`. Not a
     live hole: `failed` is never assigned by any code in the repo
     (documented at `relay_engine.dart:56-63`, confirmed by grep — zero
     assignments). But whichever epic first writes `failed` inherits the
     exact retention defect this bug fixed. Deliberately **not** widened
     here: the terminal-state set is the 🧍-resolved wording, and silently
     broadening a human-decided retention policy is rule 3's line. Should
     be carried as an explicit note on whatever task introduces `failed`.
  2. Nothing calls `reclaimPayloads()` in production yet — but nothing
     calls `sweepExpired()` either, and production wiring is explicitly a
     later epic (`relay_engine.dart:13-17`). So the retention *mechanism*
     is delivered and proven, while the retention *guarantee* only becomes
     real once E05 wires both onto a scheduler. Pre-existing epic-level
     condition, out of this task's `files:` scope (a scheduler needs new
     files); flagged for the E04→development human gate rather than fixed
     here.
  3. `RelayPacketsCompanion.insert`'s `payload` is now optional
     (`requiredDuringInsert: false`), so a future insert path could omit a
     payload silently. Only `enqueue()` inserts today, and it always
     supplies one.
