---
id: E04-B37
epic: E04
type: bug
title: "The routing graph never forgets a dead link: LinkQualityFeed records every neighbour but nothing calls removeLink on DISCONNECTED, so computeRoute keeps returning a route and the Dashboard keeps reading \"Connected\" after the peer is gone"
layer: backend
size: S
status: done
owner_agent: builder
model: opus
preferred_agent: any
token_estimate: { tier: M, range: "40k-100k" }
priority: { moscow: must, p: P2 }
depends_on: [E04-B34, E04-B36]
blocks: [E04-B36]
traces_to: [FR-UI-004, FR-ROUTE-009]
required_context:
  - epics/E04-mesh-routing/tasks/E04-B36.md
  - epics/E04-mesh-routing/tasks/E04-B34.md
  - epics/E04-mesh-routing/evidence/E04-B36-2026-09-18-teardown.md
  - lib/core/routing_engine/link_quality_feed.dart
  - lib/core/routing_engine/routing_engine.dart
design_contract: n/a
files:
  create: []
  update:
    - lib/core/routing_engine/link_quality_feed.dart
    - test/core/routing_engine/link_quality_feed_test.dart
    - epics/E04-mesh-routing/tasks/E04-B37.md
    - epics/E04-mesh-routing/tracker.md
  delete: []
started_at: 2026-09-18
completed_at:
executed_by: "orchestrator session (claude-opus-5), written directly -- reviewer must NOT be opus"
reviewed_at: 2026-09-18
reviewed_by: "claude-sonnet-5 (reviewer agent) -- different model than executed_by, rule 5"
review_outcome: approve
severity: S3
found_in: "E04-B36 live re-run, Pixel 8 Pro + Redmi 10 2022, 2026-09-18 01:49:20-01:50:50"
repro:
  - "Two phones, link open. Pixel Dashboard reads \"Connected, Latency 2ms\"."
  - "Turn the peer's Bluetooth off at T0=01:49:20."
  - "Pixel socket gone by 01:49:27.703 (`tick links=0 openSockets=0`); DISCONNECTED reaches Dart -- proven because the latency reading clears."
  - "Dashboard still reads \"Connected\" at T0+52s and T0+90s."
expected: "Once the last link is gone, the Network Status card stops reading \"Connected\" (FR-UI-004)."
actual: "It reads \"Connected\" indefinitely, because the routing graph still contains the dead link and `computeRoute` still returns a route for it."
---
# E04-B37 · The routing graph never forgets a link that has gone down

## 1. Goal
`computeRoute` stops returning a route over a link whose connection has
ended, so every caller that asks "is there a path to this peer?" — the
Dashboard's connectivity reading among them — gets an honest answer.

## 2. Root cause (read from source; the first version of this file was wrong)
The first version of this task blamed `dashboard_controller.dart:373`
(`if (!_discoveredPeerIds.contains(deviceId))`). **That was wrong** — it is a
contributing condition, not the cause. The Dashboard's reading is:

```dart
final reading = _knownPeerIds.isEmpty
    ? ConnectivityReading.noPeers
    : (_hasRouteToAnyKnownPeer() ? ConnectivityReading.connected : noRoute);
```

so even with the peer legitimately still in `_knownPeerIds`, the reading would
fall to `noRoute` — *if* `computeRoute` told the truth. It does not:

- `RoutingEngine._knownLinks` is populated **only** by `recordLinkMeasurement`,
  called **only** from `LinkQualityFeed._onMeasurement`.
- `LinkQualityFeed` subscribes to `transport.linkQuality` and **nothing else**.
  It has no `connectionState` subscription.
- `RoutingEngine.removeLink` has exactly **two** callers in `lib/`:
  `onRouteFailure` (routing_engine.dart:341, a *route failure*, not a link
  going down) and the test-only `NetworkSimulator`.

So no production path removes a link when the transport reports
`disconnected`/`failed`. `_knownLinks[self][peer]` survives, `_bestRouteToNode`
finds it, `computeRoute` returns non-null, `_hasRouteToAnyKnownPeer()` stays
true, and the card reads "Connected" forever. This is `L-process-017`'s shape:
a capability (`removeLink`) with no production caller on the path that needs it.

## 3. What this task does
- `LinkQualityFeed` subscribes to `connectionState(deviceId)` for each
  neighbour it has recorded a measurement for, and calls
  `routing.removeLink(routing.selfId, deviceId)` when that peer reports
  `disconnected` or `failed`.
- Subscriptions are keyed by device id (one per neighbour, never two) and are
  cancelled in `stop()`, so tests and `E14-B02`'s gating do not leak them.
- Lives in `start()`/`stop()`, so `bindings.dart`'s existing
  `if (!blockCommunication) linkQualityFeed.start()` gating applies unchanged
  and no binding edit is needed.

## 4. What this task does NOT do
- **Does not touch `dashboard_controller.dart`.** E04-B34's guard is correct
  once routing is honest: a still-discovered peer should read `noRoute`
  ("nearby, not connected"), not `noPeers`. Both existing E04-B34 tests must
  stay green *unchanged* — that is the check that this fix is in the right
  layer.
- Does not change `RoutingEngine`, `removeLink`, or the cost formula.
- Does not touch Kotlin or E04-B36's transport work.
- Does not add re-add logic: a peer that comes back sends a new
  `LinkQuality` measurement, which re-records the link through the existing
  path.

## 5. Contract
```yaml
- signature: "void _onConnectionState(String deviceId, ConnectionState state)"
  purpose: "on disconnected/failed, remove the self->deviceId link from the routing graph"
- signature: "Map<String, StreamSubscription<ConnectionState>> _connectionSubs"
  purpose: "one connectionState subscription per recorded neighbour, cancelled in stop()"
```

## 6. Risks
- **Removing a link that is still alive.** Only `disconnected`/`failed` may
  remove; `connecting`/`connected` must not. A transient `failed` during a
  collision retry (E04-B32) will drop the link — acceptable, because the next
  measurement re-records it, and a stale "Connected" is the worse failure.
- **Subscription leak.** One per neighbour, keyed, cancelled in `stop()` —
  the same shape `InboundPipeline` already uses (inbound_pipeline.dart:784).
- **Wrong layer.** If this were fixed in the Dashboard, every other
  `computeRoute` caller (`RelayEngine`, `CallSignaling`,
  `MessagingCoordinator`) would keep routing over a dead link. That is why
  §4 forbids touching the controller.

## 7. Implementation checklist
- [x] `LinkQualityFeed` subscribes to `connectionState` per recorded neighbour (`d0b876c`: `_watchConnectionState` called from `_onMeasurement`, keyed by device id)
- [x] `disconnected`/`failed` calls `removeLink(selfId, deviceId)`; other states do not (`d0b876c`: `_onConnectionState`'s early return)
- [x] Subscriptions keyed, idempotent, and cancelled in `stop()` (`d0b876c`)
- [x] Regression test: measurement -> route exists -> disconnected -> route is null (`d0b876c`, 4 new tests)
- [x] Existing E04-B34 and E04-B27 dashboard tests green **unchanged** — 26/26, `dashboard_controller.dart` not in the diff
- [x] Full Dart suite green — 1585/1585
- [x] Live: repeat E04-B36 §8 check 1 -> the card leaves "Connected" — **PASSED 2026-09-18 05:59:38**. Pixel read **Connected, 3ms** pre-stimulus; after the Redmi's radio went off it read **"No route to this peer"** at T₀+43 s and unchanged at T₀+119 s. The 01:49 run read "Connected" at both equivalent points. Evidence: `evidence/E04-B37-2026-09-18-live.md`; raw captures in `.run-artifacts/E04-B37-2026-09-18/`.

### Falsification (2026-09-18)
The fix was committed first (`d0b876c`), then `_watchConnectionState(quality.deviceId)`
was removed from `_onMeasurement` and the suite re-run, then restored with
`git checkout HEAD -- lib/core/routing_engine/link_quality_feed.dart`:

| test | without the fix |
|---|---|
| `..._a_disconnect_removes_the_link_from_the_graph` | **FAILED** — `Expected: null / Actual: Route(destinationId: neighbor-1, hops: [neighbor-1], cost: 1.3)` |
| `..._a_failed_connection_removes_the_link` | **FAILED** — same shape |
| `..._a_different_peers_disconnect_keeps_this_link` | **FAILED** on its `neighbor-2` null assertion |
| `..._a_live_connection_state_keeps_the_link` | **passed** — correct and intended: it asserts the link *survives*, so it must be green either way and is not claimed as falsifying |

Process note: the mutation was run in the same worktree as the concurrently
dispatched review, which briefly showed the reviewer a tree with the fix
absent. That was a sequencing error; the reviewer was told explicitly rather
than left to discover it. A falsification pass should use its own checkout
when a review is in flight.

## 8. Acceptance criteria (EARS)
- **EARS-ROUTE-14**: WHEN a direct neighbour's connection reports
  `disconnected` or `failed`, the system SHALL remove that link from the
  routing graph, so `computeRoute` no longer returns a path over it.
  (FR-ROUTE-009, FR-UI-004)

## 9. Definition of Done
- [x] §7 complete with commit hashes; full Dart suite green (1585/1585)
- [x] Live check quoted in the run log (pass or fail) — PASSED, quoted in `evidence/E04-B37-2026-09-18-live.md` — **still outstanding**: needs both phones; merging does not satisfy this
- [x] Review APPROVE from a different model than `executed_by` (rule 5) — `claude-sonnet-5`, 2026-09-18. Independently re-derived the root cause (item A: `removeLink`'s only pre-fix callers were `onRouteFailure` and the test-only `NetworkSimulator`) and confirmed the guard is exhaustive against the real enum (item C: `transport_api.g.dart:122-127`, exactly four values). Also surfaced a fifth `computeRoute` caller I had not checked, `location_share_service.dart:314`, and found it unharmed.
- [x] Merged; tracker stamped (`c4694d2`); E04-B36's §8 check 1 re-run — **re-run still pending**

## Open Questions
- **OQ-E04-B37-1 — should a `failed` during E04-B32's collision retry remove
  the link?** Today it will. The next measurement re-records it, so the
  window is short, but a noisier environment could flap the reading.
  - **Status:** 🟡 open
  - **Answer:** _deferred; recorded rather than guessed_

## Run log
- 2026-09-18, filed from E04-B36's live re-run, **first version blamed
  `dashboard_controller.dart:373`**. Corrected the same session before any
  code was written: reading `routing_engine.dart` and `link_quality_feed.dart`
  showed the Dashboard guard cannot be the cause, because the reading already
  degrades to `noRoute` when `computeRoute` returns null. The real gap is that
  nothing ever calls `removeLink` on a disconnect. Evidence for the live
  symptom is quoted in `evidence/E04-B36-2026-09-18-teardown.md`.
- **2026-09-18 05:23–06:02, live re-check on hardware (build `7ab2c17`).** Both phones re-imaged with a build carrying B36's Kotlin teardown AND this task's routing fix. Raw logcat captures started before app launch per `L-process-018`.
  - Pre-stimulus: `tick links=1 openSockets=1 armed=true` on both; Pixel Dashboard **Connected, Latency 3ms**.
  - T₀ = 05:59:38 (Redmi `svc bluetooth disable`). Redmi `adapter state=13, tearing down 1 link(s)` at T₀+262 ms; both phones `links=0 openSockets=0` from 05:59:45.
  - **Dashboard: "No route to this peer" at T₀+43 s and T₀+119 s** — the `noRoute` branch, reached because `computeRoute` now returns null once `removeLink` has run. FR-UI-004 satisfied.
  - Incidental, recorded rather than dropped: the test message `k37-t1` itself failed at the APPLICATION layer ("Message not sent — Could not reach this device in time"), almost certainly the prekey handshake between two identities minutes old. The transport link was real regardless (`openSockets=1` both sides, 3 ms latency), and that is what this check exercises.
  - Getting a link at all took ~40 minutes: both apps had been signed out by the reinstall, both hit `E01-B01`'s identity latch, and the Devices screen has **no connect affordance** — `TransportService.connect()`'s only production caller is `ConnectionEnsuringSender`, i.e. a link opens when a message is SENT. Discovery/Verify tapping could never have produced one. Worth knowing before the next live run.
