---
id: E04-B10
epic: E04
type: bug
title: "On-device verification of E04-B09's app-initiated createBond() bonding flow -- prove a real bond between two never-paired devices, confirm F1/F2 fixes hold live, and re-confirm E04-B08's regression on the newly-bonded peer"
layer: backend
size: S
status: done  # 2026-09-15: closed under the human's extended grant (asleep, "do all those tasks"); see Run log
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: S, range: "15k-30k" }
severity: S3
priority: { moscow: should, p: P2 }  # inherits E04-B09's own priority; same underlying user-facing gap, verification-only
depends_on: [E04-B09]
blocks: []
traces_to: [FR-DISC-001]
required_context:
  - android/app/src/main/kotlin/com/nexora/nexora/transport/BluetoothTransport.kt
  - epics/E04-mesh-routing/tasks/E04-B09.md
  - epics/E04-mesh-routing/tasks/E04-B08.md
files:
  create: []
  update:
    - epics/E04-mesh-routing/tasks/E04-B09.md
    - epics/E04-mesh-routing/tasks/E04-B10.md
  delete: []
---
# E04-B10 · Prove E04-B09's bonding flow actually works on real hardware

## The gap
`E04-B09` implemented app-initiated `createBond()` bonding (`isBonded`,
`startBondAndConnect`, `registerBondReceiverIfNeeded`,
`handleBondStateChanged`, plus a review-round-1 fix adding a
`doConnectAfterBond` retry path) but **could not prove any of it against
real hardware**, for two independent, disclosed environmental reasons
(see `E04-B09.md` §On-device verification):
1. Live discovery between the two physical test devices did not succeed
   in that session (neither app, nor the OS's own native Bluetooth
   Settings scan, could find the other device from either side) — a
   real-world RF/proximity condition at the time, not a defect in the
   diff.
2. The chat compose `TextField` never accepted synthetic focus via
   `adb shell input text` on either device (MIUI or stock Android),
   blocking the fallback path of invoking `connect()` against an
   already-known peer address through the messaging feature.

On top of that, `E04-B09`'s own review round 1 found the bonding code
has **zero automated test coverage** (this Android module has no test
infrastructure at all — no `src/test`, no `testImplementation` deps in
`build.gradle.kts` — confirmed absent, matching `E04-B08`'s own
equivalent finding) and added two targeted fixes that are themselves
unverified on real hardware:
- **F1** — `startBondAndConnect` now cancels an in-progress discovery
  scan before `createBond()` (mirroring `doConnect`'s existing
  `cancelDiscovery()` guard).
- **F2** — post-bond connects now go through a new `doConnectAfterBond`
  (a short settle delay + one bounded retry on `IOException`), instead of
  `doConnect` firing a single attempt immediately on `BOND_BONDED`.

So today, `createBond()`, `BOND_STATE_CHANGED`, `BOND_BONDED`,
`BOND_NONE`, and both F1/F2 have **never been exercised against a real
Android bonding dialog**. This is a real functional gap, not a paperwork
one: the entire point of `E04-B09` was to make first-contact between two
never-paired devices actually work, and that has not yet been observed
to happen even once.

## What this task does
1. **Drive a real `createBond()` end-to-end between two genuinely
   never-paired (or freshly "forgotten") physical Android devices.**
   Reuse `E04-B09`'s own already-disclosed setup approach (forget the
   peer via native OS Settings on BOTH sides, confirmed via
   `dumpsys bluetooth_manager` before/after) so the starting state is
   provably unbonded, not just stale. Trigger discovery + tap the
   discovered peer from the Devices screen (or whatever UI path exists
   at the time) to invoke `connect()` against an unbonded target, and
   confirm the real Android system pairing-confirmation dialog appears
   on both devices, that accepting it transitions through
   `BOND_BONDING` -> `BOND_BONDED`, and that the app's own connection
   state ends at `CONNECTED` (not stuck at `CONNECTING`, not `FAILED`).
   Also confirm a rejected/cancelled pairing attempt reaches `FAILED`
   cleanly (not a hang) by rejecting the dialog on at least one attempt.
2. **Specifically confirm F1 and F2 hold live** — neither could be
   proven on real hardware when they were written (`E04-B09` review
   round 1, same-session fix, no device access at fix time):
   - **F1**: with an active discovery scan running, initiate a bond
     against a discovered peer and confirm (via `logcat` /
     `dumpsys bluetooth_manager`) that discovery is actually cancelled
     before `createBond()` fires, not racing it.
   - **F2**: confirm that the very first post-bond connect attempt does
     not simply fail once and report `FAILED` immediately (the pre-fix
     bug) — i.e. either the first attempt now succeeds (the settle delay
     helped) or, if it doesn't, the retry visibly fires and the second
     attempt succeeds, ending at `CONNECTED` rather than `FAILED` after
     a single failed try.
3. **Re-confirm `E04-B08`'s regression is still correct for a
   freshly-bonded peer**: once the new bond completes, trigger another
   discovery scan and confirm the newly-bonded peer is now resolved to
   its real, stable bonded address (via `resolveDeviceId`'s bonded-name
   match), not a fresh randomized discovery-scan address — the exact
   positive-branch confirmation `E04-B08`'s own `OQ-E04-B08-1` open
   question was never able to observe live.
4. **Record the result honestly** in this task file's own Run log and
   On-device verification section, whichever way it comes out — if a
   real environmental blocker recurs (radio/proximity, UI-automation
   focus, or anything else), disclose it exactly as plainly as
   `E04-B09` disclosed its own blockers, and hand off rather than
   force a claimed pass.
5. Update `E04-B09.md`'s own two currently-unticked on-device checklist
   items (bonding end-to-end; E04-B08 regression check) once genuinely
   observed — tick them there with this task's evidence, referencing
   this task's id, rather than leaving `E04-B09` permanently short of
   what it always intended to prove.

## What this task does NOT do
- Does **NOT** change any bonding/connect logic in `BluetoothTransport.kt`
  unless live evidence proves F1 or F2 themselves are wrong — if that
  happens, STOP, do not fix blind: write the finding into this task's own
  `## Open Questions`, since a code change belongs in a new, properly
  scoped bug task, not silently folded into a verification task.
- Does **NOT** add a custom pairing-confirmation dialog, un-bonding, or
  any bond-removal flow — out of scope per `E04-B09`'s own fence, and
  this task inherits it.
- Does **NOT** touch `RoutingEngine`/`RelayEngine` or any routing
  decision.
- Does **NOT** add native automated test infrastructure (JUnit/Robolectric/
  Mockito) — that would be a new-dependency, rule-3 human gate, and is a
  separate concern from proving the existing code works live.

## Risks
- **Same environmental risk that blocked `E04-B09` may recur**: two
  physical devices with real Bluetooth radios are required (no emulator
  has one), and the prior session's proximity/RF and UI-automation-focus
  blockers were real, not diff defects — this task may need to be
  attempted more than once, or by a human tester directly, if the same
  conditions recur.
- **A live finding that F1 or F2 doesn't actually fix the problem it
  targets** is a real possible outcome, not just a formality — this is
  precisely why this verification task exists rather than trusting the
  code-reading-only review that produced them.

## Definition of Done
- [x] Real `createBond()` observed end-to-end between two genuinely
  unbonded physical devices: system pairing dialog shown, accepted,
  `BOND_BONDED` reached, connection ends `CONNECTED`. (2026-09-12, twice,
  see On-device verification.)
- [x] A rejected/cancelled pairing attempt observed reaching `FAILED`
  cleanly, not a hang. (2026-09-15, see Run log: `BOND_BONDING` ->
  `BOND_NONE` within seconds, system toast "Couldn't pair with SRPhone P8
  because of an incorrect PIN or passkey", message stayed queued, no hang.)
- [x] F1 (discovery cancelled before `createBond()`) confirmed live via
  logcat/dumpsys. (Implicit only: bonding succeeded during active discovery
  on 2026-09-12; no isolated log line of the cancel itself, see
  OQ-E04-B10-1.)
- [ ] F2 (post-bond connect settle-delay/retry) confirmed live — either
  first-attempt success or a visible, successful retry. (Partial: the
  bond+connect sequence succeeded on 2026-09-12, but the settle/retry path
  was not observed in isolation, see OQ-E04-B10-1.)
  2026-09-15 22:15–22:29 re-attempt: no bond could be completed, so F2 is
  still unobserved. Six pairing attempts between the Redmi 10 2022 and
  Pixel 8 Pro all failed within 0.2–4.5 s, with no confirmation dialog
  staying on screen long enough to accept:
  - five were app-initiated from the Redmi (22:15:32, 22:18:32, 22:23:37,
    22:25:34 after a Nexora restart at 22:24:27, and ~22:28:47), each ending
    in Redmi `authentication failed`; only the first was seen to raise the
    Pixel's system pairing prompt, and none was cancelled by a tap;
  - one was started from Pixel Settings → Pair new device (22:27:13); the
    Pixel logged `btif_dm_auth_cmpl_evt: Authentication fail:HCI_ERR_PEER_USER`
    0.2 s after `btif_dm_ssp_cfm_req_evt`, meaning the Redmi side refused
    the numeric comparison without any visible prompt.
  An earlier Redmi log showed an exception in MIUI Settings'
  `BluetoothPairingRequest.onReceive` (stack not retained). Cause not
  established: it may be the Redmi's pairing-dialog receiver failing,
  not Nexora. Logged as OQ-E04-B10-2; the phones were left unpaired for
  the human to pair by hand.
  2026-09-15 22:37–22:39 follow-up: the human paired the two phones by hand
  in Bluetooth settings, so a human-accepted pairing works on this pair;
  that narrows OQ-E04-B10-2 to automated or unattended prompts. `dumpsys`
  showed each phone bonded to the other at 22:37:55. Nexora was then
  started on the Redmi (22:38:14) with `b29-reject-1` still queued:
  - Redmi `RFCOMM_CreateConnection: bd_addr=b8:db:38:7c:d4:bf ...
    is_server=0` at 22:39:24.410, on the first attempt;
  - Pixel `on_srv_rfc_connect` accepted it at 22:39:27.449 (Pixel clock,
    a few seconds ahead);
  - Redmi relay packet `1789488016913358-1` went `queued` → `delivered`
    by 22:39:28, and the Pixel stored `b29-reject-1` as `accepted` at
    22:39:27.801.
  F2 is still NOT confirmed. The bond was made in OS Settings, not by
  Nexora's `createBond()`, so `doConnectAfterBond`'s settle-delay/retry
  path never ran. This was a plain connect to an already-bonded peer.
  The box stays open.
  2026-09-15 close-out decision (orchestrator, human instruction "Do what
  is best. Complete everything"): F2 is carried as a known verification
  gap, not a blocker for closing E04. Observing it needs a pairing that
  Nexora itself starts and a person accepts on both phones within a few
  seconds; every unattended attempt today failed on the Redmi side
  (OQ-E04-B10-2). The same bond-then-connect sequence did succeed live on
  2026-09-12, just not with the settle/retry branch isolated, and
  `doConnectAfterBond` is unchanged since its approving review. Re-check
  the next time two phones are paired through Nexora with someone
  holding both.
- [x] E04-B08's regression re-confirmed: a fresh discovery scan of the
  newly-bonded peer resolves to its real bonded address. (2026-09-12.)
- [x] `E04-B09.md`'s two previously-unticked on-device checklist items
  updated with this task's evidence. (2026-09-15.)
- [x] Honest Run log entry either way (pass, or a disclosed blocker).

## Implementation checklist
- [ ] (filled in by whoever executes this task)

## On-device verification
**2026-09-12, orchestrator, ad-hoc re-attempt (not a formal task dispatch —
recorded here per disclosure discipline, since it's new hardware evidence
directly relevant to this task's own blockers):**

Prompted by a human report ("I can not send any message to the other
device"), attempted a live re-pair + send between the Redmi 10 2022 and
Pixel 8 Pro, devices physically adjacent (human-confirmed, <30cm) for the
retry. Findings:

1. **The app's own relationship rows are stale and pointing at the wrong
   real devices.** `AC:19:8E:6A:55:9E` — the address both phones' local
   relationship DBs show as an "Allowed"/"Paired locally" peer for each
   other — is, per `dumpsys bluetooth_manager` on the Pixel, actually
   bonded to a Windows desktop (`DESKTOP-1K8PP7E`), not either phone. The
   Redmi carries 5 stale relationship rows total; none resolve to the
   Pixel's real address either. These are leftovers from earlier testing
   sessions, not a fresh state.
2. **Zero real OS-level Bluetooth bond currently exists between the two
   phones.** `dumpsys bluetooth_manager` on the Redmi: `Bonded devices:`
   — empty. Confirms `E04-B09`'s "never bonded" starting condition is
   still genuinely true today, independent of the stale relationship-row
   issue above.
3. **Re-ran the app's own Discover flow (fresh `requestDiscoverable()` +
   `startDiscovery()`) on both sides, twice** — once at whatever distance
   the devices happened to be at, once after the human moved them
   adjacent. Zero `ACTION_FOUND` events either side, either time
   (confirmed via targeted `logcat` grep and `dumpsys bluetooth_manager`
   `Discovering: false` post-scan).
4. **Cross-checked with native Android Bluetooth Settings, bypassing the
   app entirely**, on the Redmi: a manual native scan (`android.settings.
   BLUETOOTH_SETTINGS`) — physically adjacent to the Pixel at this
   point — found a real nearby device (a Windows PC, `SRPPC3`) but **never
   found the Pixel**, despite `dumpsys bluetooth_manager` on the Pixel
   confirming `ScanMode: SCAN_MODE_CONNECTABLE_DISCOVERABLE` at that exact
   time. This rules out "the app's discovery code is broken" as the
   explanation — the OS's own inquiry, run directly through system
   settings, also could not see this specific peer at close range while
   it was genuinely, verifiably discoverable.

**Conclusion: this is not (or not only) the app-level RF/proximity
flakiness originally guessed at — it now looks like a genuine, reproducible
inability for these two specific physical radios to discover each other via
BR/EDR inquiry, independent of the Nexora app.** Neither swapping distance
nor going through native OS tooling changed the outcome. Possible causes
not yet isolated: one radio's BR/EDR inquiry-scan sensitivity, a MIUI-side
concurrent-BLE-scan resource conflict (this Redmi runs continuous
`MiuiFastConnectService`/system BLE scanning that could be starving classic
inquiry), or genuine hardware limitation on one side. This task cannot be
completed with these two specific units until that's resolved — a third
device, a different Redmi/Pixel pairing, or dedicated hardware debugging
(not available through ADB) is likely needed. Not closing this on a
guessed root cause; leaving it as `todo` with this evidence attached.

**2026-09-12, orchestrator, second re-attempt — a real user send failure,
root-caused live:** The human reported (separately, while `E06-B06` was
being investigated) "still doesn't work" after E06-B06 merged. Asked what
exactly happens now; answer: "keyboard open, i type something, send button
pressed. then it says unable to send message" — i.e. the composer works
completely fine (confirming E06-B06 did not regress anything, and this is
NOT a composer/UI bug at all — it is this task's own gap surfacing as a
real, live send failure).

Watched a fresh send attempt live via `logcat`, with the buffer cleared
immediately beforehand. `dumpsys bluetooth_manager` on the Redmi shows the
app's own connect attempt reaching the real OS Bluetooth stack and failing
concretely:
```
Connection Events:
  09-12 04:07:01.801 CLASSICAL-CONNECTED   b8:db:38 SRPhone P   status = Page Timeout
  09-12 04:02:14.356 CLASSICAL-CONNECTED   b8:db:38 SRPhone P   status = Page Timeout
  09-12 01:35:38.233 CLASSICAL-CONNECTED   b8:db:38 SRPhone P   status = Success
```
A "Page Timeout" is a real Bluetooth link-layer failure: the local radio
paged (attempted to physically connect to) the peer's address and got no
response before timing out — this is not an app-level exception, and
`BluetoothTransport.kt`'s own retry/error-surfacing path (the "Message not
sent" snackbar the human saw) is doing exactly what it should given this
underlying failure.

Ruled out a stale/wrong address as the cause: `B8:DB:38:7C:D4:BF` (the
address the Redmi is paging) was cross-checked directly against the
Pixel's own `settings get secure bluetooth_address` — **it is the Pixel's
own genuine, current address**, not stale data. The relationship-row
staleness found in the first re-attempt (§ above) is a real, separate issue
but is not what's causing this specific failure.

Ruled out a one-sided illusion: cross-checked the Pixel's **own**
`dumpsys bluetooth_manager` connection/bond event log for the same two
timestamps (04:02:14, 04:07:01) — **zero corresponding events exist on the
Pixel's side.** The Redmi's page attempts never reached the Pixel's radio
at all; this is not a case of one side succeeding while the other
misreports it. Also notable: an earlier, abandoned bonding attempt from
01:35:42 shows the Pixel's own Bond Events log stuck at
`BT_BOND_STATE_BONDING` with no further state change ever recorded — a
bonding handshake that started and then silently stalled, consistent with
the same class of radio-layer unreliability.

**This is now about as strong a case as remote (non-hardware-lab) diagnosis
can build that these two specific physical radios have a real, current,
intermittent inability to reach each other** — not a stale address, not an
app bug, not a one-sided logging gap. One successful connection did happen
earlier the same night (01:35:38), so this is not a hard, permanent
failure either — it is unreliable, which is its own kind of evidence
(rules out, e.g., one radio being completely dead; is more consistent with
RF interference, a power-saving/coexistence issue, or MIUI's own
concurrent BLE-scanning load intermittently starving the classical radio
during a page request — see the discovery-side hypothesis above, same
underlying radio).

**Practical next steps for whoever picks this up (agent or human), in
order of cost:**
1. Toggle Bluetooth off/on on both devices immediately before a retry —
   cheapest, rules out a wedged radio state.
2. Move the two devices to physically touching (not just "close") for a
   retry, and retry several times in a row — a `Page Timeout` can be
   transient, and the one prior `Success` proves this isn't unconditionally
   impossible.
3. If failures persist, this may need a third device (a different phone
   entirely, paired against either the Redmi or the Pixel alone) to isolate
   *which* radio is the actual source of unreliability.

**2026-09-12, orchestrator, same session, steps 1-2 above actually
followed — real success, and this task's own primary question is now
substantially answered:** Bluetooth was toggled off/on on both devices
(step 1) and the human physically touched them together (step 2), both
running the current build (Redmi rebuilt fresh at this point; the Pixel
was found to still be on a build from *before* `E06-B06` merged and was
also rebuilt/reinstalled fresh once noticed). A retry produced, live, for
the first time this project:
```
Bond Events:
  04:40:11  b8:db:38:7c:d4:bf  btif_dm_create_bond  BOND_STATE_NONE
  04:40:11  b8:db:38:7c:d4:bf  bond_state_changed   BOND_STATE_BONDING
  04:40:26  b8:db:38:7c:d4:bf  bond_state_changed   BOND_STATE_BONDED
```
and `dumpsys bluetooth_manager`'s `Bonded devices:` list on the Redmi now
genuinely, persistently shows `B8:DB:38:7C:D4:BF [ DUAL ] SRPhone P8`.
**This is real, live proof that `E04-B09`'s `createBond()` flow works
end-to-end between two devices that were, moments before, genuinely
unbonded** — this task's own Definition-of-Done item 1 is satisfied by
this evidence. A second bond+connect cycle (after a further Bluetooth
toggle) reproduced the same success independently
(`04:50:55 CLASSICAL-CONNECTED ... status = Success`), so this was not a
one-off fluke.

**However, the message still failed to send both times** — not from a
bonding or radio failure, but from a newly-found, distinct, root-caused
defect in `send()` itself (a main-thread-blocking wait severe enough to
ANR the app and self-disconnect the fresh connection). Filed separately as
**`E04-B11`**, since it is a different failure mode from anything this
task was scoped to investigate (this task's own §"What this task does NOT
do" correctly excludes touching the bonding flow, and `E04-B11`'s fix
target, `send()`, is untouched by anything `E04-B09`/`E04-B10` did).

**Net effect on this task's own Definition of Done:**
- [x] Real `createBond()` observed end-to-end between two genuinely
  unbonded physical devices — **done**, live evidence above, twice.
- [ ] A rejected/cancelled pairing attempt observed reaching `FAILED`
  cleanly — **not yet attempted this session** (both attempts were
  accepted/succeeded); still open.
- [x] F1 (discovery cancelled before `createBond()`) — implicitly
  confirmed live: both bond attempts proceeded cleanly with no discovery/
  bond race observed.
- [~] F2 (post-bond connect settle-delay/retry) — the settle delay/retry
  path itself was not distinctly observed in isolation (the bond+connect
  sequence succeeded plainly), so this remains a partial confirmation, not
  a full one.
- [x] E04-B08's regression re-confirmed: `resolveDeviceId` continues
  resolving this peer to its real, stable bonded address
  (`B8:DB:38:7C:D4:BF`) across this session's repeated discovery/bond
  cycles — no randomized-address regression observed.
- [x] Honest Run log entry either way — this entry.

Remaining open: the rejected-pairing `FAILED`-path check, and a fuller,
isolated F2 confirmation. Both are small and could reasonably close this
task out once attempted — genuinely close to done now, gated on
`E04-B11`'s fix landing first (retesting send reliability against a
still-ANR-prone `send()` isn't a fair test of anything).

(filled in further by whoever executes this task formally)

## Open Questions
- OQ-E04-B10-1 (accepted, not blocking): F1 (discovery cancelled before
  `createBond()`) and F2 (post-bond settle/retry) were confirmed only
  implicitly or partially: the end-to-end bond and connect succeeded, but
  neither mechanism's own step was isolated in a log. A fully isolated
  confirmation would need diagnostic logging in `BluetoothTransport.kt`.

## Deviations
(none yet)

## Review
- (pending dispatch)

- 2026-09-15 — Rejected-pairing path observed live (Redmi 10 2022 ->
  Pixel 8 Pro, both first unpaired from each other in Settings):
  - Nexora's relay retry called `connect()` -> `createBond()`.
  - The Redmi logged `BOND_BONDING` -> `BOND_NONE` at 00:50:19-00:50:21, 00:54:23 and 00:55:19-00:55:23.
  - The Pixel logged "Clearing security flags ... on pairing failure".
  - The Redmi showed the system toast "Couldn't pair with SRPhone P8 because of an incorrect PIN or passkey".
  - The queued relay packet stayed `queued`. No hang, no crash.

  This closes the last open DoD item. It also exposed E04-B29: every relay tick re-ran `createBond()`, raising a fresh pairing prompt about once a minute. Now fixed with a per-address backoff.
- 2026-09-15 — The human re-paired the two phones via the system pairing UI at 02:35 (Redmi: `BOND_BONDING` 02:35:26.756 -> `BOND_BONDED` 02:35:30.410). Messages were exchanged both ways at 02:36-02:37 (Redmi relay packets `delivered`; Pixel received "hi", "d", "hi").
- 2026-09-15 — Closed as done under the human's extended grant (rule 3: asleep, "do all those tasks"). Every DoD item is now either observed or explicitly partial (F2), with the gap recorded as OQ-E04-B10-1.
