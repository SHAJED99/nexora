---
id: E04-B14
epic: E04
type: bug
title: "PrekeyExchange's bundle-response provenance check binds only to an unauthenticated claimed identity + a guessable request id, not to the physical link a response actually arrived on -- a forged-bundle-response injection path that is newly reachable now that E04-B13 fixed real addressing"
layer: backend
size: S
status: review-requested
owner_agent: builder
started_at: 2026-09-12
executed_by: builder (sonnet-5)
model: any
preferred_agent: any
token_estimate: { tier: S, range: "20k-60k" }
severity: S3
priority: { moscow: should, p: P2 }  # real security gap, but not a regression
  # this session caused (the alternative was the whole feature staying
  # broken) and not exploitable without another device already reachable
  # on the mesh -- P2, not P1, pending a human's own severity read; flagging
  # for a fresh bug_priorities look rather than asserting P1 unilaterally.
depends_on: [E04-B13]
blocks: []
traces_to: [FR-SEC-003]
required_context:
  - lib/core/messaging/prekey_exchange.dart
  - lib/core/messaging/inbound_pipeline.dart
  - lib/core/messaging/identity_announce.dart
  - epics/E04-mesh-routing/tasks/E04-B13.md
  - epics/E09-location-sharing/tasks/E09-B09.md
files:
  update:
    - lib/core/messaging/prekey_exchange.dart
    - lib/core/messaging/inbound_pipeline.dart
  create: []
  delete: []
---
# E04-B14 · Bind bundle-response provenance to the physical link, not just a claimed identity + guessable id

## How this was found
`E04-B13`'s own independent reviewer (opus), while verifying that task's
security fix, found a second, adjacent gap while checking
`PrekeyExchange._takeMatchingCompleter`'s own provenance contract (that
file's own header comment: "only a response ... from the peer it was sent
to, is accepted"). Direct quote from the review:

> `_takeMatchingCompleter` binds a response only on `requestId` + the
> unauthenticated `frame.source`, with no physical-link binding —
> `ControlHandler` (`inbound_pipeline.dart:185`) gets only the frame, while
> `E04-B12` deliberately used a SEPARATE signature carrying `linkDeviceId`
> for its own announce handler. And `requestId` is guessable by
> construction (`'${selfDeviceId}-pkx-$counter'`,
> `prekey_exchange.dart:431`). Before this PR that check was
> unsatisfiable on hardware — fail-closed by accident. After it, it's
> satisfiable, so a forged-`bundleResponse` injection path goes live for
> the first time.

In plain terms: before `E04-B13`, `frame.source` was always this app's
own random/key-derived `selfDeviceId`, which could never match a real
peer's claimed identity anyway (the exact bug `E04-B11`→`E04-B13` fixed),
so this particular check happened to never pass — a real gap that was
accidentally masked by a different, now-fixed bug. Now that addressing is
correct, this check is REACHABLE: a device that can get bytes onto this
device's transport layer at all (a bonded peer, or potentially a relay
hop, depending on how far the mesh's own trust boundary is meant to
extend) could craft a `bundleResponse` claiming a guessable `requestId`
and an arbitrary `frame.source`, and have it accepted as if it were the
genuine response to THIS device's own pending `ensureSession` request —
potentially completing a Signal Protocol session establishment against a
bundle that did not actually come from the intended peer.

**This is the same class of risk already confirmed as a real S1 finding
in `E09-B09`** (`frame.source` trust exploit — decrypt succeeds via TOFU,
claimed address gets trusted as identity) — same root cause
(`frame.source` treated as authoritative without further binding),
different call site.

## What this task does
1. **Root-cause exactly how far this can go** before designing a fix —
   confirm precisely what `establishSession` does with a bundle accepted
   this way (does TOFU trust bind to the FIRST identity key seen, same
   shape as `E09-B09`'s own confirmed exploit? or does something else
   downstream still catch a mismatch?). Do not assume the impact without
   verifying it against the actual `CryptoService`/`DriftSignalProtocolStore`
   code.
2. **Bind `_takeMatchingCompleter`'s acceptance to the physical link the
   response arrived on**, not just the claimed `frame.source` — mirror
   `E04-B12`'s own `IdentityAnnounceService` pattern, which already
   carries a `linkDeviceId` (the Bluetooth-address-keyed connection
   identity, independently known to the receiver regardless of what the
   frame claims) through a separate handler signature for exactly this
   reason. `InboundPipeline`'s own `ControlHandler` seam may need widening
   to carry that same `linkDeviceId` to every registered handler, not just
   the identity-announce one — check whether that's a clean, additive
   change or whether it has wider blast radius across
   `DeliveryAckService`/`GroupMembershipService`/etc., and disclose either
   way.
3. **Make `requestId` unguessable**, not just correctly bound to a link —
   the current `'${selfDeviceId}-pkx-$counter'` scheme is a real,
   independent weakness even after point 2 lands (a link-bound check still
   needs the id itself to not be trivially predictable by anything sharing
   that link). A cryptographically random component is the obvious fix;
   confirm it doesn't break `_outstandingRequests`' own map semantics.
4. Write a test that specifically proves a forged `bundleResponse` (right
   `requestId`, wrong actual originating link) is now rejected.

## What this task does NOT do
- Does NOT re-open or weaken `E04-B13`'s own addressing fix — this task
  adds a stricter check on top, it doesn't touch how `destination` gets
  resolved.
- Does NOT attempt to fully re-architect trust/provenance across every
  control protocol in one pass — if point 2's `ControlHandler` widening
  turns out to have real blast radius across other handlers, that's this
  task's own scope to absorb (it's the direct fix), but do not use this
  task to fix `E09-B09`'s own already-tracked, separate call sites, or
  `E04-B15`'s own separate raw-MAC-addressing gaps in sibling protocols.

## Definition of Done
- [x] Impact confirmed against actual `CryptoService`/session-establishment
  code (not assumed). `CryptoService.establishSession`
  (`crypto_stub.dart:73-86`) wraps `SessionBuilder.processPreKeyBundle`,
  which delegates trust to `DriftSignalProtocolStore.isTrustedIdentity`
  (`drift_signal_store.dart:139-150`): `previous == null` returns `true`
  unconditionally — textbook trust-on-first-use. `ensureSession`'s own
  `containsSession` early-return (`prekey_exchange.dart:475-477`) means
  `establishSession` is reached ONLY when no session yet exists for that
  peer address, which is exactly the case `isTrustedIdentity` has no prior
  identity to compare against. **Confirmed: a forged bundle accepted via
  the pre-fix provenance gap would have been trusted outright as that
  peer's permanent identity** — the identical TOFU shape already confirmed
  as a real S1 in `E09-B09`, not a different or lesser risk class.
- [x] `_takeMatchingCompleter` (or its replacement) binds acceptance to
  the physical link a response arrived on. `prekey_exchange.dart`'s
  `_OutstandingRequest` now stores `linkDeviceId` (the raw physical link
  the outbound request left on); `_takeMatchingCompleter` requires an
  accepted response's actual arrival link to match it, in addition to the
  pre-existing `peerDeviceId`/`frame.source` check. `linkDeviceId` is
  plumbed from `InboundPipeline` via a NEW dedicated registration slot
  (`PrekeyExchangeHandler`/`registerPrekeyExchangeHandler`,
  `inbound_pipeline.dart`) mirroring the already-established
  `IdentityAnnounceHandler` pattern, rather than widening the shared
  `ControlHandler` typedef — see Deviations for why, and for the blast-
  radius check this avoided.
- [x] `requestId` is cryptographically unguessable. `_nextRequestId`
  (`prekey_exchange.dart`) now appends a 128-bit `Random.secure()` suffix;
  the monotonic counter is kept alongside it only to guarantee
  `_outstandingRequests`' own "unique per outstanding request" map
  semantics with certainty, not as part of the unguessability property.
- [x] A test proves a forged response (right id, wrong link) is rejected.
  `test_E04_B14_forged_bundle_response_on_wrong_link_is_rejected`
  (`prekey_exchange_test.dart`) — forges a `bundleResponse` with the REAL,
  captured `requestId` and a claimed `frame.source` that already passes
  the pre-existing E04-B13 check, delivered on a different physical link
  than the one the real request went out on; asserts no session is
  established, `responsesAccepted == 0`, and the real outstanding request
  still times out honestly rather than silently completing via the
  forgery.
- [x] Existing test suite green. `flutter test`: 1513/1513 passing.
  `flutter analyze`: 0 issues (2 pre-existing, unrelated infos — the same
  two E04-B13's own review already disclosed).

## Implementation checklist
- [x] Root-caused the TOFU impact against the real `CryptoService`/
  `DriftSignalProtocolStore` code (see DoD item 1) `3a3e8f6`
- [x] `InboundPipeline` gets a dedicated `PrekeyExchangeHandler`
  registration slot, dispatched before the generic `ControlHandler` map,
  so `kControlKindPrekeyExchange` carries `linkDeviceId` without widening
  the typedef every other control sub-protocol shares `3a3e8f6`
- [x] `PrekeyExchange` self-registers on that slot from its own
  constructor (blast-radius-avoiding alternative to widening
  `ControlHandler` — see Deviations); `_OutstandingRequest` stores
  `linkDeviceId`; `_takeMatchingCompleter` enforces it `3a3e8f6`
- [x] `_nextRequestId` uses a 128-bit `Random.secure()` suffix `3a3e8f6`
- [x] `test_E04_B14_forged_bundle_response_on_wrong_link_is_rejected`
  added, proving the fix `3a3e8f6`
- [x] `flutter test` 1513/1513, `flutter analyze` clean (2 pre-existing
  infos) `3a3e8f6`

## Open Questions
(none — the `ControlHandler`-widening blast radius the task file flagged
as a possible stop condition was investigated and avoided; see Deviations)

## Deviations
- **Did NOT widen the shared `ControlHandler` typedef.** The task file's
  own §2 anticipated this might be needed to carry `linkDeviceId` to
  "every registered handler, not just the identity-announce one," and
  asked to check the blast radius and disclose either way. Checked: five
  OTHER control sub-protocols share `ControlHandler`
  (`DeliveryAckService.handleControlFrame`,
  `GroupMembershipService.handleWireFrame`,
  `GroupCryptoService.handleWireFrame`, `CallSignaling.handleWireFrame`,
  `LocationShareService.handleWireFrame`), each in its own file, all five
  outside this task's `files:` fence — widening the typedef itself would
  have forced editing all five just to keep them compiling, a real,
  large blast radius extending outside this task's own scope, exactly the
  condition the task file names as a reason to stop rather than force a
  fix. **Avoided it entirely**: `PrekeyExchange` gets its OWN dedicated
  registration slot on `InboundPipeline`
  (`PrekeyExchangeHandler`/`registerPrekeyExchangeHandler`), mirroring the
  already-established `IdentityAnnounceHandler` precedent (E04-B12) —
  zero changes to `ControlHandler` or to any of the five other files;
  `inbound_pipeline.dart`'s own dispatch for `kControlKindPrekeyExchange`
  is checked before the generic `_controlHandlers` map is ever consulted,
  so the two mechanisms never conflict.
- **`PrekeyExchange` self-registers from its own constructor**, rather
  than via an external call in `messaging_stack.dart` (the pattern every
  other control sub-protocol, including the pre-existing
  `kControlKindPrekeyExchange` registration, uses). This task's `files:`
  fence does not include `messaging_stack.dart`, so there is no external
  composition-root call site available — the identical constraint,
  reasoning, and resolution E07-T06 already established and documented in
  `inbound_pipeline.dart`'s own header for its group-message handler.
  `messaging_stack.dart`'s pre-existing
  `inbound.registerControlHandler(kControlKindPrekeyExchange,
  prekeyExchange.handleControlFrame)` call still runs, unmodified, but is
  now provably dead in production: `InboundPipeline._handleBuffer` routes
  `kControlKindPrekeyExchange` through the new dedicated slot before the
  generic map is ever consulted. `handleControlFrame` itself keeps its
  original `Future<void> Function(RelayPacketFrame)` signature (an
  additional named `linkDeviceId` parameter is OPTIONAL, so it stays
  assignable to `ControlHandler`) purely so that dead call site keeps
  compiling — Dart's function-subtyping rules make this safe.
- **Constructor self-registration is wrapped in a `try`/`on StateError`
  no-op**, because `test/features/chat/presentation/chat_controller_test.dart`
  already constructs a SECOND `PrekeyExchange` (`_SlowPrekeyExchange`)
  against an already-wired stack, purely to override `ensureSession` for
  timing control — that file's own pre-existing doc comment already
  documented "only the ORIGINAL, registered instance's `handleControlFrame`
  is wired to `stack.inbound`" as the intended contract for the OLD
  registration mechanism. Swallowing the resulting `StateError` keeps that
  exact, pre-existing contract true for the new mechanism too, without
  editing that test file. Production only ever constructs one
  `PrekeyExchange` per `MessagingStack` (`messaging_stack.dart`'s single
  construction site), so this catch cannot mask a real double-registration
  bug there.
- **`frame.source`'s existing check is kept, not replaced**, by the new
  link check — both must now pass. The task file's own §2 asked to bind
  acceptance to the physical link "not just" the claimed identity, which
  reads as additive; a response is now rejected if EITHER check fails.

## Run log
- 2026-09-12, `claude-sonnet-5 (builder)`: implemented per task file §1-4.
  `flutter test`: 1513/1513 passing (full suite, including the new
  `test_E04_B14_forged_bundle_response_on_wrong_link_is_rejected`).
  `flutter analyze`: 0 issues (2 pre-existing, unrelated infos in files
  this task does not touch — the same two E04-B13's own review already
  disclosed). Investigated the `ControlHandler`-widening blast radius the
  task file flagged as a possible stop condition (§2/Risks) and found a
  scoped alternative that avoids it entirely (dedicated registration slot,
  mirroring the already-established `IdentityAnnounceHandler` pattern) —
  see Deviations for the full reasoning and what was checked.

## Review
---

### Round 1 — 2026-09-12 · `claude-opus-5` (independent reviewer, rule 5: ≠ `claude-sonnet-5` builder)

📋 **REVIEW VERDICT — APPROVE**

- **scope:** in-contract. Product diff touches exactly the two `files.update`
  entries (`lib/core/messaging/prekey_exchange.dart`,
  `lib/core/messaging/inbound_pipeline.dart`) plus this task file. One
  observation, not a violation: `test/core/messaging/prekey_exchange_test.dart`
  is edited but was never listed in `files:` — the DoD mandates the test, so
  the fence was under-specified at authoring time, not breached at
  implementation time (`L-process-011` shape).
  §"What this task does NOT do" respected: `resolveOutboundDestination` /
  E04-B13's addressing is untouched; no `E09-B09` or `E04-B15` call site
  was opportunistically edited.
- **DoD:** 4/4 verified independently (evidence below). Suite green.
- **suite (run by the reviewer, own fresh worktree at
  `origin/epic_04_bug_14` = `8fad729`):** `flutter test` → **1513/1513 passed**,
  `EXIT=0`. `flutter analyze` → **2 issues**, both pre-existing `info`s in
  files this task does not touch
  (`test/core/calls/call_migration_controller_test.dart:207`,
  `test/features/login/presentation/login_controller_enrollment_gate_test.dart:18`)
  — matches the builder's claim exactly.
- **design gate:** n/a (no `design_contract:`; backend-only).
- **security:** PASS — see the falsification and adversarial analysis below.

#### Falsification — reproduced by the reviewer, not taken on trust
Neutralised the new guard in place (`prekey_exchange.dart:784`,
`if (linkDeviceId == null || outstanding.linkDeviceId != linkDeviceId) return null;`)
and re-ran `test_E04_B14_forged_bundle_response_on_wrong_link_is_rejected`:

```
00:00 +0 -1: E04-B14: link-bound bundle-response provenance
  test_E04_B14_forged_bundle_response_on_wrong_link_is_rejected [E]
  Expected: false
    Actual: <true>
  test\core\messaging\prekey_exchange_test.dart 779:9
```

Line 779 is
`expect(await a.signalStore.containsSession(SignalProtocolAddress(bMac, 1)), isFalse)`
— i.e. with the guard removed, the forged bundle delivered over the
attacker's link `AA:BB:CC:DD:EE:08` **actually establishes a real Signal
session** for the victim peer. That is the S1-shaped TOFU exploit itself,
demonstrated end-to-end through the real `TransportEventsApi.onDataReceived`
channel — not a proxy assertion. Guard restored verbatim
(`git status --porcelain` clean); prekey suite back to 14/14 green.

#### 1. Blast radius of the dedicated-slot approach — zero, confirmed
- `typedef ControlHandler = Future<void> Function(RelayPacketFrame frame);`
  (`inbound_pipeline.dart:186`) is byte-identical to `development`; no diff
  hunk touches it. The five other sub-protocols (`delivery_ack.dart`,
  `group_membership_service.dart`, `group_crypto_service.dart`,
  `call_signaling.dart`, `location_share_service.dart`) are not in the diff
  at all.
- Dispatch order verified at `inbound_pipeline.dart:789-807`: the
  `controlKind == kControlKindPrekeyExchange` branch is entered and `return`s
  unconditionally **before** `_controlHandlers[controlKind]` is read at line
  809. No double-dispatch is structurally possible, and
  `messaging_stack.dart:341-344`'s surviving legacy generic registration is
  genuinely dead code, not a live second path.
- The branch sits **inside** the `isForUs` block (`isForUs` computed at line
  697, `!isForUs` relay branch returns at line 763), so unlike E04-B12's
  identity-announce bypass this adds no new `isForUs` escape and does not
  change relay-forward behaviour for prekey frames addressed elsewhere.
- Minor, benign ordering change: `innerFrame` is now built before the generic
  `handler == null` check rather than after. Pure allocation, no semantics.

#### 2. `linkDeviceId` provenance — traced to source, unforgeable at the app layer
`_takeMatchingCompleter`'s `linkDeviceId`
← `PrekeyExchange._handleControlFrameOnLink`
← `InboundPipeline._handleBuffer(linkDeviceId, bytes)` (line 630)
← the closure key at `inbound_pipeline.dart:619-620`:
`_stack.transport.incomingData(deviceId).listen((bytes) => _handleBuffer(deviceId, bytes))`
← `TransportService.incomingData` (`transport_service.dart:162-164`), which
filters `_dataController` by `e.deviceId`
← `TransportService.onDataReceived(String deviceId, Uint8List bytes)`
(`transport_service.dart:231`), the Pigeon `TransportEventsApi` callback whose
`deviceId` is stamped by the **native** layer from the socket the bytes were
physically read off.

So `linkDeviceId` is never derived from frame content and cannot be influenced
by a remote peer at the Dart layer. Its residual trust base — the OS Bluetooth
address of a bonded, point-to-point RFCOMM connection — is exactly the base
`identity_announce.dart` already documents and relies on; this fix does not
widen it.

#### 3. Adversarial analysis — no way found to defeat the fix
- **Attacker on a different link** → rejected. Proven above.
- **Attacker *is* the device at the requested link** → accepted, but that
  device is by definition the peer `ensureSession(bMac)` asked for. Not a
  defeat; the residual exposure is E04-B12's unauthenticated
  identity-announce TOFU, tracked separately and untouched here.
- **Malicious relay** → prekey exchange never uses `relayEngine.enqueue`;
  `_sendControlFrame` goes through `_stack.directSend(peerDeviceId, ...)`
  (`prekey_exchange.dart:793, 826-832`), a direct point-to-point send. A
  relayed response would arrive on the relay's link and be rejected. No
  multi-hop legitimate path exists to be broken.
- **Fail-closed too broadly?** Checked all four `ensureSession` call sites
  (`chat_controller.dart:313`, `group_crypto_service.dart:330`,
  `group_membership_service.dart:628`, `location_share_service.dart:275`).
  Every one passes the same raw transport/link device id that `directSend`
  then dials, so `outstanding.linkDeviceId` always equals the arrival link for
  a genuine response. No legitimate production rejection found.
- **Concurrent `ensureSession` race** → coalesced at `_inFlightByPeer` before
  the first `await` (this file's header, "Coalescing is correctness");
  distinct peers get distinct `requestId`s *and* distinct links, and both must
  match. No cross-completion possible.
- **Replay / duplicate** → the pre-existing `completer.isCompleted` check
  (line 787) is unchanged and still runs after the new guard.
- **`bundleUnavailable`** is now link-bound too — a forged "unavailable" can
  no longer be used to abort a genuine handshake.

#### 4. Self-registration + `on StateError` swallow — masks no production bug
`messaging_stack.dart:314` assigns `inbound` and `:334` constructs
`prekeyExchange` — order holds, no `LateInitializationError`. That is the
**only** `PrekeyExchange(` construction site in `lib/` (grep:
`lib/core/messaging/messaging_stack.dart:334` only). The sole second
construction anywhere is `_SlowPrekeyExchange`
(`test/features/chat/presentation/chat_controller_test.dart:121, 437`), whose
own pre-existing doc comment already declares "a second instance cannot
receive the peer's bundle response (only the ORIGINAL, registered instance's
`handleControlFrame` is wired to `stack.inbound`)". The catch is narrowly
typed (`on StateError`) and only `registerPrekeyExchangeHandler` throws it on
that path.

#### 5. `_nextRequestId` — map semantics intact, counter not decorative
`'${_stack.selfDeviceId}-pkx-$_requestCounter-$randomHex'`
(`prekey_exchange.dart:498-508`). `_requestCounter` is still incremented and
still in the string, so uniqueness within an instance's `_outstandingRequests`
lifetime is guaranteed structurally, not statistically; the 128-bit
`Random.secure()` suffix supplies unguessability independently. No wire-format
or length assumption is broken — the length-prefixed `requestId` framing
handles the longer id, proven by the suite and by the test helper that parses
the real id back off the wire. (`packetId` also calls `_nextRequestId()` at
line 818; it draws a *different* random value, so the pending `requestId` is
not leaked on the wire.)

#### Carried-forward observations (not blockers, no fix required in this task)
1. **`counters.responsesUnsolicited` conflates three rejection causes**
   (unknown id, wrong claimed source, wrong link). A live forgery attempt is
   now indistinguishable from an ordinary late duplicate in telemetry. A
   dedicated counter would make this attack observable → backlog item.
2. **Responder-side addressing asymmetry (pre-existing, E04-B15 territory).**
   `_handleBundleRequest(frame.source, ...)` (`prekey_exchange.dart:688-723`)
   replies via `_sendControlFrame(frame.source, ...)` → `directSend` keyed by
   the requester's *logical* `selfDeviceId`, not its Bluetooth MAC. On real
   hardware that is not a dialable transport address. Entirely unmodified by
   this task and explicitly out of its fence, but it means the happy-path
   response leg may still not complete on hardware — fold into E04-B15's scope
   rather than rediscovering it later.
3. **Future-proofing note.** If a later task (E04-B15's direction) changes any
   `ensureSession` caller to pass a logical `selfDeviceId` instead of the link
   id, `outstanding.linkDeviceId` would stop matching the arrival link and
   legitimate responses would be silently dropped as `responsesUnsolicited`.
   Whoever lands that change must update `_ensureSessionUncoalesced`'s
   `linkDeviceId` capture in the same diff.

**Verdict: APPROVE.** No way found to defeat the fix. Recommend
`status: done`, `review_outcome: approved`, `reviewed_by: claude-opus-5`.

