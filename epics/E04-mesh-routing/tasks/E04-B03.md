---
id: E04-B03
epic: E04
type: bug
title: No production source of link-quality measurements exists — the Pigeon transport contract exposes no latency/loss/RSSI, so RoutingEngine's link graph is unfeedable outside the simulator and computeRoute() would always return null on a real device
layer: backend
size: S
status: done
owner_agent: builder
model: any
preferred_agent: any
token_estimate: { tier: S, range: "5k-15k" }
severity: S3
priority: { moscow: should, p: P2 }
depends_on: [E04-T02, E04-T03a, E04-T03b]
blocks: []
traces_to: [FR-ROUTE-001, FR-ROUTE-002, FR-PLAT-003]
required_context: [epics/E04-mesh-routing/tasks/E04-T02.md, epics/E04-mesh-routing/tasks/E04-T03a.md, agent/memory/decisions/ADR-0004-native-transport-strategy.md]
design_contract: n/a
found_by: reviewer (claude-opus-5) during the E04 end-of-epic bug sweep
found_in: seam between E04-T03a/T03b (transport boundary + discovery) and E04-T02 (consumes link measurements) — no task owned producing them
files:
  create: []
  update:
    - pigeons/transport.dart
    - lib/core/transport/generated/transport_api.g.dart
    - android/app/src/main/kotlin/com/nexora/nexora/transport/TransportApi.g.kt
    - epics/E04-mesh-routing/epic.md
  delete: []
started_at: 2026-08-27
completed_at: 2026-08-27
executed_by: claude-sonnet-5 (builder)
reviewed_at: 2026-08-27
reviewed_by: claude-opus-5 (reviewer)
review_outcome: approve
---
# E04-B03 · The routing engine has no production data source

## The finding
`RoutingEngine` decides routes from `_knownLinks`, populated exclusively by
`recordLinkMeasurement(neighborId, {latencyMs, lossRate, batteryDrain})`
(`lib/core/routing_engine/routing_engine.dart:141-155`). Those three values
have **no producer anywhere in `lib/`**. Verified:

```
grep -rn "rssi|Rssi|latencyMs|lossRate|batteryDrain" lib/ --include=*.dart
  (excluding simulation/ and routing_engine's own definitions)  →  0 hits
```

The only thing in the repo that produces them is
`lib/core/routing_engine/simulation/simulated_link.dart` — T01's
deterministic test simulator.

The reason is upstream, in the transport contract. `pigeons/transport_api.dart`
— T03a's task was to "define the transport boundary once" (ADR-0004) —
exposes exactly four native→Dart events:

```dart
void onDeviceDiscovered(TransportDevice device);
void onDeviceLost(String deviceId);
void onConnectionStateChanged(String deviceId, ConnectionState state);
void onDataReceived(String deviceId, Uint8List bytes);
```

and `TransportDevice` carries only `id`, `displayName`, `type`
(`lib/core/transport/generated/transport_api.g.dart:132-143`). No RSSI, no
round-trip time, no loss counter, no throughput sample, no battery hint.
Android's `BluetoothDevice`/`ScanResult` can supply RSSI and the Kotlin side
can time its own round trips, but nothing in the contract carries any of it
across the boundary.

**Consequence on a real device:** `_knownLinks` stays empty →
`computeRoute()` returns `null` for every destination
(`routing_engine.dart:331-332`) → `RelayEngine._attempt` returns early at
`relay_engine.dart:163-167` → every relay packet sits `queued` until it
expires. The mesh discovers peers and can move bytes point-to-point, but
cannot route.

## Why this is a defect and not just "E05's job"
The sweep specifically checked whether this was cleanly deferred, the way
`RelayEngine`'s production wiring is. It is not:

- `RelayEngine` being unconstructed in `lib/` **is** cleanly deferred —
  `relay_engine.dart:13-17` says "Production wiring (a later epic) constructs
  `RelaySendFn` from `TransportService.send`", and `E04-T04.md` §4 fences it.
  The sweep confirms that hand-off is sound: `TransportService.send(String,
  Uint8List) → Future<bool>` (`transport_service.dart:120-121`) matches
  `RelaySendFn` exactly, and `AppDatabase` is already a permanent GetX
  singleton (`lib/app/bindings.dart:20`), so the
  `TransportService`/`RoutingEngine`/`AppDatabase` triple E05 needs is
  genuinely constructible. **No bug there.**
- The link-measurement gap is **nowhere disclosed** — not in `epic.md`, not
  in T02's §4, not in T03a/T03b's fences, not in the analyze report. E04's
  scope claims "Route cost calculation and make-before-break migration"
  (`epic.md:31`) as in-scope and delivered.

And it is not a wiring gap that E05 can absorb: fixing it means adding
methods/fields to the **Pigeon schema**, which ADR-0004 designates as the
one place the transport boundary is defined, plus regenerating bindings and
implementing the native side. The E04 analyze report's own "Contract sanity"
line rests on "One Pigeon schema (T03a) defines the transport boundary once;
T03b/T03c extend its *implementation*, never redefine the contract"
(`epic.md:104`). E05 needing to redefine that contract is exactly the
outcome that line was asserting would not happen.

## Severity: S3 (broken, but there is a workaround)
Not S2: no user-facing function regresses today, because E04 never claimed a
production-wired routing path and E05/E06 are the epics that send real
traffic. The workaround is real and available — extend the Pigeon schema in
E05.

Not S4: this is not cosmetic. It is a load-bearing hole in the epic's
headline deliverable, it invalidates an analyze-report assertion, and
discovering it mid-E05 converts a planned wiring task into an unplanned
ADR-0004 boundary change with native work on both platforms. Catching it
here costs a schema edit and a paragraph; catching it in E05 costs a
re-plan.

Severity per `skills/bug-sweep` is the reviewer's call. **Priority is the
human's** (`bug_priorities` gate, rule 3) — left `TBD-human`. Reviewer's
advisory: this is the one E04 finding where the *cheap* moment to act is
now, even though nothing is on fire.

## Repro (static, reviewer-verified 2026-08-27)
1. `grep -rn "recordLinkMeasurement" lib/` → one hit, its own definition in
   `routing_engine.dart`. No call site in `lib/`.
2. `grep -rn "latencyMs|lossRate|batteryDrain" lib/ --include=*.dart` →
   hits only in `routing_engine.dart`, `route_cost_calculator.dart`, and
   `simulation/`.
3. Read `pigeons/transport_api.dart` → no method or field carries any
   link-quality signal.
4. Therefore on a real device `RoutingEngine._knownLinks` is empty and
   `computeRoute()` returns `null` unconditionally.

**Expected** (FR-ROUTE-001, FR-ROUTE-002; `epic.md:31`): E04 delivers route
cost calculation over real links, or explicitly and visibly defers the
measurement source with a named owner.

**Actual:** neither. The cost formula, Dijkstra, migration policy and relay
queue are all implemented and correctly tested — against simulated inputs
that no production code path can supply.

## Proposed fix direction (builder to confirm; do not treat as prescriptive)
Smallest honest fix, in preference order:

1. **Minimum (documentation, no code):** add an explicit deferral to
   `epic.md` — a §Carry-forward entry naming the gap, the FR ids it blocks,
   and E05 as the owner, plus a correction to the "Contract sanity" analyze
   line noting that the transport contract will need extending. This makes
   the gap impossible for E05 to miss. If the human wants nothing else, this
   alone resolves the "silently absent" half of the defect.
2. **Preferred (small contract change now):** extend the Pigeon schema
   while it is still E04's own boundary — add an `int? rssi` to
   `TransportDevice` and a `void onLinkQuality(String deviceId, int
   latencyMs, double lossRate)` event, leaving the Kotlin side emitting
   nothing yet (a declared-but-unfed event is honest; a missing contract is
   not). Then E05's work is wiring, not redefinition. **Touching the Pigeon
   schema is an ADR-0004 boundary change — present it to the human and stop
   (rule 3) rather than deciding the shape unilaterally.**

**🧍 Resolved, 2026-08-27: option 2, extend the Pigeon schema now.** Add
`int? rssi` to `TransportDevice` and `void onLinkQuality(String deviceId,
int latencyMs, double lossRate)` to `TransportEventsApi`, regenerate both
sides, leave the Kotlin implementation genuinely unfed (no synthetic
values — declared-but-unfed is the honest state, per §3's own "do not
fabricate" instruction). E05 wires the native emission and the
`recordLinkMeasurement` call site; this bug only closes the contract gap.
3. **Do not** synthesize fake measurements to make the engine appear live
   (e.g. defaulting every discovered device to a constant latency). That
   would produce a routing engine that computes confident routes from
   invented data — strictly worse than one that honestly returns `null`.

## Regression test
No new unit test is meaningful for option 1 (a docs change) and option 2's
value is a contract shape, not a behavior — the existing
`test/core/transport/transport_service_test.dart` must stay green after any
regeneration. The real verification for this bug is the traceability pass:
`skills/traceability` should show `recordLinkMeasurement` with a production
caller, or the gap explicitly recorded as deferred with an owner.

## Definition of Done
- [x] 🧍 Human has decided option 1 vs. option 2 (option 2 is an ADR-0004
      boundary change) — resolved 2026-08-27, recorded above in §Proposed
      fix direction
- [x] The gap is recorded in `epic.md` with the blocked FR ids and a named
      owning epic — `epic.md` §Carry-forward, `285cccd`
- [x] The analyze report's "Contract sanity" line is annotated if the
      contract is being extended — `epic.md:104`, `285cccd`
- [x] Full suite green and `flutter analyze` clean — 114/114, `flutter
      analyze` clean, see §Run log
- [x] Reviewer re-verified that no fabricated measurement defaults were
      introduced — independently re-verified by claude-opus-5 2026-08-27,
      see §Review log. Zero production call sites; `rssi` nullable with no
      default on both sides; `onLinkQuality` never invoked.

## Implementation checklist
- [x] `pigeons/transport.dart`: add `int? rssi` to `TransportDevice`, add
      `void onLinkQuality(String deviceId, int latencyMs, double
      lossRate)` to `TransportEventsApi`  `df2d9f2`
- [x] Regenerate Dart + Kotlin bindings via `dart run pigeon --input
      pigeons/transport.dart`; verified reproducible by regenerating a
      second time and diffing (zero diff)  `df2d9f2`
- [x] Confirmed no Kotlin hand-written file needs edits to compile —
      `TransportEventsApi` is a concrete generated class Kotlin *calls*,
      not an interface it implements; `TransportDevice`'s new `rssi` field
      defaults to `null` in the generated Kotlin data class, so existing
      constructor call sites (`BluetoothTransport.kt:407`,
      `LoopbackTransport.kt:39`) compile unchanged  `df2d9f2`
- [x] Found and fixed a compile break the task brief didn't anticipate:
      `lib/core/transport/transport_service.dart`'s `_EventsHandler`
      *does* implement the abstract `TransportEventsApi` (Dart is the
      side satisfying the Flutter API), so it needed a no-op
      `onLinkQuality` override to keep compiling. Not in the task's
      `files:` list — treated as a files-list gap (same class as T03a's
      own Deviation 1), see §Deviations below  `df2d9f2`
- [x] `epic.md` §Carry-forward entry added naming FR-ROUTE-001,
      FR-ROUTE-002, and E05 as owner; "Contract sanity" analyze-report
      line annotated  `285cccd`
- [x] `flutter analyze` clean, `flutter test` 114/114 (full suite, no
      regressions), `flutter build apk --debug` succeeds  see §Run log
- [x] Confirmed via grep: no call site anywhere in `lib/` or
      `android/app/src/main/kotlin` invokes `onLinkQuality` or populates
      `rssi` with any value, real or synthetic  see §Run log

## Self-review
- [x] Every §Implementation checklist item done, with commit hashes
- [x] `flutter analyze` clean; `flutter test` 114/114, full suite, no
      regressions (pre-existing count from `epic_04` branch preserved
      exactly)
- [x] `flutter build apk --debug` succeeds — Kotlin side compiles with no
      hand-written native changes required
- [x] Pigeon-generated output matches fresh codegen exactly — regenerated
      a second time from the edited schema, `git status`/`git diff` empty
      on both generated files after the second run
- [x] No synthetic/fabricated measurement values introduced anywhere —
      `onLinkQuality` is declared but never called; `rssi` is declared but
      never populated with anything but its `null` default. Verified by
      grep across `lib/` and `android/app/src/main/kotlin` excluding the
      generated/schema files themselves: zero hits.
- [x] Diff confined to `files:` plus one disclosed deviation (below); §What
      this task does NOT do respected — `RoutingEngine` untouched,
      `recordLinkMeasurement` not wired to anything
- [x] No secrets or PII in the diff

**Deviations from the task file:**
1. **`lib/core/transport/transport_service.dart` touched, not in `files:`.**
   The task's own briefing assumed the compile risk (if any) would be on
   the Kotlin side; it is actually on the Dart side.
   `TransportEventsApi` is Pigeon's `@FlutterApi()` — the class Dart
   implements to receive native events (`TransportApi` is the reverse,
   `@HostApi()`, which Kotlin implements and Dart only calls). Since
   `transport_service.dart`'s private `_EventsHandler extends
   TransportEventsApi`, adding `onLinkQuality` to the schema made it an
   abstract method `_EventsHandler` must override, and `flutter analyze`
   failed with `non_abstract_class_inherits_abstract_member` until it did.
   Added a single no-op override (`@override void onLinkQuality(String
   deviceId, int latencyMs, double lossRate) {}`), explicitly commented as
   contract-only/E05's-job, with **no new stream, getter, or forwarding
   logic** — the task steered toward minimal Dart-facade plumbing, and a
   no-op is the minimum that compiles. Treated as a files-list gap, not a
   scope violation, on the same precedent as T03a's own Deviation 1 (the
   generated Kotlin file wasn't in T03a's `files: create` list either, but
   was required for `TransportApiHost.kt` to compile).
2. On the Kotlin side, confirmed **no changes were needed** — the opposite
   of what the task's briefing anticipated needing to check. Documented in
   §Implementation checklist for the next reader who re-litigates this.

**Files actually touched:**
- `pigeons/transport.dart` (schema, in `files:`)
- `lib/core/transport/generated/transport_api.g.dart` (generated, in
  `files:`)
- `android/app/src/main/kotlin/com/nexora/nexora/transport/TransportApi.g.kt`
  (generated, in `files:`)
- `lib/core/transport/transport_service.dart` (not in `files:` — see
  Deviation 1)
- `epics/E04-mesh-routing/epic.md` (in `files:`)
- `epics/E04-mesh-routing/tasks/E04-B03.md` (this file)

## Open Questions
_(none — the human-gate decision was already resolved in the task file
before this session started; no new spec gap encountered)_

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- Read `pigeons/transport.dart`, `E04-T03a.md`, and the generated Dart/
  Kotlin output before editing, per rule 8.
- Added `int? rssi` (nullable, no default needed beyond Pigeon's own
  `null`) to `TransportDevice`, and `void onLinkQuality(String deviceId,
  int latencyMs, double lossRate)` to `TransportEventsApi`, both with
  doc comments stating explicitly that they are contract-only and that
  E05 owns wiring real values.
- `dart run pigeon --input pigeons/transport.dart` regenerated both
  `lib/core/transport/generated/transport_api.g.dart` and
  `android/.../transport/TransportApi.g.kt` cleanly, no errors. Re-ran a
  second time to confirm reproducibility — zero diff both times.
- `flutter analyze` initially failed:
  `non_abstract_class_inherits_abstract_member` on
  `transport_service.dart:159` (`_EventsHandler`). Root-caused to
  `TransportEventsApi` being the Flutter-side (`@FlutterApi()`) interface
  that Dart implements, not the Kotlin-side one — the task's own briefing
  had this backwards, framing the risk as a possible Kotlin-side stub.
  Added a no-op `onLinkQuality` override to `_EventsHandler`, per
  §Self-review Deviation 1. `flutter analyze` clean after.
- Verified the Kotlin side genuinely needs no changes: `TransportEventsApi`
  in the generated Kotlin is a concrete class with `suspend fun` methods
  that Kotlin *calls* (`TransportApiHost.kt:26` holds one instance),
  never implements; and `TransportDevice`'s Kotlin data class gives `rssi`
  a `= null` default, so `BluetoothTransport.kt:407` and
  `LoopbackTransport.kt:39`'s existing 3-arg constructor calls compile
  unchanged.
- `flutter test`: **114/114**, full suite, matching the pre-fix count in
  the worktree exactly — no regressions, no new tests added (per the
  task's own §Regression test note: a contract-shape-only change has no
  meaningful new unit test; existing
  `test/core/transport/transport_service_test.dart` stayed green
  throughout).
- `flutter build apk --debug`: succeeded (`assembleDebug`, ~54s), confirming
  the Kotlin side (hand-written and generated) compiles with the extended
  contract.
- Fabrication check (per §3/§Regression test's "do not fabricate"
  instruction): `grep -rn "onLinkQuality|rssi|Rssi" --include=*.kt
  --include=*.dart android/app/src/main/kotlin lib/` excluding the
  generated files and the schema itself → **zero hits**. Nothing calls
  `onLinkQuality`; nothing populates `rssi` with anything but its `null`
  default. `RoutingEngine` was not opened for editing at any point in
  this session.
- Updated `epic.md`: added a §Carry-forward entry naming FR-ROUTE-001/
  FR-ROUTE-002 as blocked and E05 as the owning epic for real
  measurement wiring, and annotated the "Contract sanity" analyze-report
  line to note the contract needed extending here (qualifying, not
  retracting, the original T03b/T03c claim).
- Commits: `df2d9f2` (schema + generated bindings + the one disclosed
  Dart-facade deviation), `285cccd` (epic.md disclosure + analyze-report
  correction).

## Review log

**Reviewer: claude-opus-5. Independent re-verification, 2026-08-27.
Verdict: APPROVE. No reviewer fixes required — nothing was changed in the
product diff.** Rule 5 satisfied (`executed_by: claude-sonnet-5`).

Every claim in §Self-review and §Run log was re-run rather than accepted.

1. **No fabrication — the load-bearing check for this bug. CONFIRMED.**
   Grepped `rssi|Rssi|onLinkQuality|latencyMs|lossRate` (case-insensitive)
   across all of `lib/`, `android/app/src/main/kotlin/`, and `pigeons/`,
   then excluded only the schema and the two generated files. Every
   remaining hit is **pre-existing and outside this diff**:
   `route_cost_calculator.dart:6,16,18,24,78`,
   `routing_engine.dart:127,128,143,144,150,151` (its own
   `recordLinkMeasurement` parameter declarations),
   `simulation/network_simulator.dart:94,105`,
   `simulation/simulated_link.dart` (T01's simulator). The **only** hit
   this diff introduces outside schema/generated is
   `transport_service.dart:188` — `void onLinkQuality(String deviceId, int
   latencyMs, double lossRate) {}`, a genuinely empty body.
   **Zero production call sites populate or emit these fields with any
   value, real or synthetic.** `onLinkQuality` has no caller anywhere;
   `rssi` has no assignment anywhere.
2. **`RoutingEngine` / `recordLinkMeasurement` untouched. CONFIRMED.**
   `git diff --name-only epic_04...epic_04_bug_03 -- lib/core/routing_engine/
   test/` returns **empty**. The diff's six files are exactly
   `pigeons/transport.dart`, the two generated files,
   `transport_service.dart`, `epic.md`, and this task file. Every
   `recordLinkMeasurement` occurrence in the diff is inside a doc comment
   or prose naming E05 as owner — no wiring.
3. **Suites re-run by the reviewer on `epic_04_bug_03` HEAD, not trusted
   from the PR body.** `flutter analyze` → `No issues found! (ran in
   2.1s)`. `flutter test` → `00:04 +114: All tests passed!` (114/114,
   exit 0). `flutter build apk --debug` → `Built
   build\app\outputs\flutter-apk\app-debug.apk` (exit 0), so the Kotlin
   side does compile against the extended contract with no hand-written
   native change. The "clean, 114/114, build succeeds" claim is real as of
   this commit.
4. **Pigeon codegen reproducibility. CONFIRMED, zero drift.** Re-ran `dart
   run pigeon --input pigeons/transport.dart` from the committed schema;
   `git status --porcelain` and `git diff --stat` both came back **empty**
   afterwards. The committed `.g.dart`/`.g.kt` are byte-identical to fresh
   codegen — no hand-editing of generated output, same discipline T03a's
   review established.
5. **Deviation 1 (`transport_service.dart`, not in `files:`) — accepted as
   a files-list gap, not a scope violation.** Genuinely unavoidable:
   Pigeon emits `TransportEventsApi` as `abstract class` with an abstract
   `void onLinkQuality(...)` (`transport_api.g.dart:344,362`), and
   `_EventsHandler extends TransportEventsApi`
   (`transport_service.dart:159`), so Dart's
   `non_abstract_class_inherits_abstract_member` makes the override a
   compile requirement. The only way to avoid touching this file would
   have been to not add the event — i.e. to not do the human-approved fix.
   The builder's root-cause note is also correct on the direction of the
   Pigeon APIs (`@FlutterApi()` is Dart-implemented; the task brief had it
   backwards). The addition is **minimal and correct**: an empty body, no
   stream, no getter, no field, no forwarding, no default value — there is
   no place in it where a synthetic value could later be introduced
   silently, and the comment above it names E05 as the owner.
6. **`epic.md` disclosure — honest, and hard to miss.** §Carry-forward
   states "**Nothing does yet**" in bold, names FR-ROUTE-001 and
   FR-ROUTE-002 as blocked, names **E05** as owning epic, spells out the
   on-device consequence (`computeRoute()` returns `null`, `RelayEngine`
   queues until expiry), and carries the anti-fabrication instruction
   forward. It does not imply the gap is fixed. The "Contract sanity" row
   is re-marked `✅ pass, qualified 2026-08-27 (E04-B03)` — the changed
   marker plus an explicit **Qualification:** paragraph makes the reader
   stop, and the qualification honestly narrows rather than retracts the
   original T03b/T03c claim.
   *Non-blocking observation:* a case could be made for `⚠️` over
   `✅ pass, qualified` on that row. Not a finding — the annotation is
   discoverable and its text is accurate, which is what the DoD item
   asked for.
7. **`rssi` genuinely optional on both sides, no placeholder default.
   CONFIRMED.** Dart: `int? rssi;` with `this.rssi` unrequired in the
   constructor and `rssi: result[3] as int?` in the decoder
   (`transport_api.g.dart:151,138,171`). Kotlin: `val rssi: Long? = null`
   with `pigeonVar_list[3] as Long?` in `fromList`
   (`TransportApi.g.kt:254,264`). Nullable, defaulting to `null` — **not**
   `0` or any other value a future caller could mistake for a real
   reading, which for a dBm field would have been a real trap. Existing
   3-arg construction sites (`BluetoothTransport.kt:407`,
   `LoopbackTransport.kt:39`) compile unchanged and pass no `rssi`, as
   claimed.

**Scope:** in-contract. Diff = `files:` list exactly, plus this task file
and the one disclosed, unavoidable, minimal deviation.
**§What this task does NOT do:** respected — no synthesized measurements,
no `RoutingEngine` edits, no `recordLinkMeasurement` wiring, no native
emission.

**Interaction with the other sweep findings (E04-B01 / E04-B02):**
- *No semantic interaction with either.* B01 (`relay_engine.dart`,
  `routing_engine.dart`) and B02 (`relay_engine.dart`,
  `persistence/`) operate on routing/relay logic and storage; B03 changes
  only the transport contract, and its new fields are unfed, so nothing
  B01/B02 does can read or be affected by them. B01's `considerMigration`
  fix also does not falsify B03's §Carry-forward claim that
  `computeRoute()` returns `null` on-device — that follows from the empty
  `_knownLinks`, which B01 does not populate either.
- *One mechanical merge note:* **B02 also lists
  `epics/E04-mesh-routing/epic.md` in its `files: update`.** B03 edited
  the analyze-table "Contract sanity" row (line ~104) and appended a new
  §Carry-forward section at the end; B02 is scoped to §Data model
  (lines ~39-42, retention window). Different regions, so a clean
  auto-merge is likely — but whichever lands second should re-read
  `epic.md` rather than assume, and should append to the existing
  §Carry-forward section rather than create a second one.
- *Recommended merge order:* B03 first. It is docs + contract only, and
  landing it first means B01/B02 rebase onto an `epic.md` that already
  carries the §Carry-forward section they may want to extend.
