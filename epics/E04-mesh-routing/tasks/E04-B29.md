---
id: E04-B29
epic: E04
type: bug
title: "A queued message to an unpaired peer re-runs createBond() on every relay tick, raising a fresh system pairing prompt about once a minute, indefinitely"
layer: backend
size: S
status: in_review
owner_agent: builder
model: opus
preferred_agent: any
token_estimate: { tier: S, range: "15k-40k" }
severity: S3
priority: { moscow: should, p: P2 }  # user-facing prompt spam on a common path (a message to a not-yet-paired contact)
found_in: "live two-device testing for E04-B10, 2026-09-15 (Redmi logcat: BOND_BONDING -> BOND_NONE at 00:50:19, 00:54:23, 00:55:19, ...)"
depends_on: [E04-B09, E04-B23]
blocks: []
traces_to: [FR-DISC-001]
required_context:
  - epics/E04-mesh-routing/tasks/E04-B09.md
  - epics/E04-mesh-routing/tasks/E04-B10.md
  - android/app/src/main/kotlin/com/nexora/nexora/transport/BluetoothTransport.kt
  - lib/core/transport/connection_ensuring_sender.dart
design_contract: n/a
files:
  create:
    - epics/E04-mesh-routing/tasks/E04-B29.md
  update:
    - android/app/src/main/kotlin/com/nexora/nexora/transport/BluetoothTransport.kt
    - epics/E04-mesh-routing/tracker.md
started_at: 2026-09-15
completed_at:
executed_by: "orchestrator session (claude-opus-5) -- reviewer must NOT be opus"
reviewed_at:
reviewed_by:
review_outcome:
repro:
  - "Unpair two Nexora phones, then send a message from one to the other. RelayEngine's E04-B23 direct-hop path calls ConnectionEnsuringSender -> TransportService.connect on every processQueue tick."
  - "BluetoothTransport.connect sees the peer is not bonded and calls startBondAndConnect -> createBond() every time. When the pairing is not confirmed it ends BOND_NONE and emits FAILED, the packet stays queued, and the next tick repeats it."
  - "Live, Redmi 10 2022 -> Pixel 8 Pro, 2026-09-15: a new bonding attempt, and a new system pairing prompt, roughly every minute for as long as the message stayed queued."
expected: "A failed or rejected pairing is not re-attempted automatically on every tick, so the user is not spammed with pairing prompts."
actual: "Every relay tick raises a new pairing prompt for the same unpaired peer, indefinitely."
---
# E04-B29 · Back off automatic re-pairing after a failed pairing

## 1. Mechanism

`BluetoothTransport` keeps an in-memory, per-address backoff:

- **Recording a failure.** A failed or rejected bond records a failure. That means `BOND_NONE` while waiting on that address, or `createBond()` refusing to start. The next attempt is allowed only after 2 minutes, doubling per consecutive failure up to 30 minutes.
- **While backed off.** `connect()` for a not-yet-bonded address inside its backoff window emits `FAILED` and returns immediately, without calling `createBond()`. That is the same outcome as any other unreachable connect, so the packet simply stays queued.
- **Reset.** A successful bond (`BOND_BONDED`) clears the backoff for that address.

## What this task does NOT do

- Does not change connecting to an already-bonded peer; the backoff applies only to the pairing path.
- Does not change `RelayEngine`, `ConnectionEnsuringSender` or the relay tick interval.
- Does not distinguish a relay retry from a deliberate user tap. Both call the same `connect()`, so a user who re-taps a contact right after rejecting its pairing prompt also waits out the backoff (OQ-E04-B29-1).
- Does not persist the backoff; an app restart resets it.
- Does not add a Kotlin test harness (none exists in this project).

## 2. Definition of Done

- [x] A failed or rejected pairing records an exponential per-address backoff; a successful bond resets it
- [x] `connect()` to an unbonded address inside its backoff fails fast without calling `createBond()`
- [ ] `flutter build apk --debug` compiles clean
- [ ] Live: after a rejected pairing, no new pairing prompt appears on the next relay ticks; one appears again after the backoff
- [ ] Independent review (reviewed_by != executed_by)

## Open Questions

- OQ-E04-B29-1: an explicit user action (for example tapping Connect on the Devices screen) is throttled the same as a relay retry. Telling them apart needs a `connect` API change (a "user-initiated" flag through Pigeon), which is outside this task's fence.

## Run log

- 2026-09-15: filed from E04-B10's live pairing test and implemented directly.
