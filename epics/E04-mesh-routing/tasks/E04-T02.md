---
id: E04-T02
epic: E04
type: feature
title: Routing engine — cost formula, route selection, make-before-break migration, failure recovery
layer: backend
size: M
status: done
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: must, p: P1 }
depends_on: [E04-T01]
blocks: [E04-T04, E04-T05]
traces_to: [FR-ROUTE-001, FR-ROUTE-002, FR-ROUTE-005, FR-ROUTE-006, FR-ROUTE-009]
required_context: [epics/E04-mesh-routing/epic.md, epics/E04-mesh-routing/tasks/E04-T01.md]
design_contract: n/a
files:
  create:
    - lib/core/persistence/routing_tables.dart
    - lib/core/routing_engine/route_cost_calculator.dart
    - lib/core/routing_engine/routing_engine.dart
    - test/core/routing_engine/route_cost_calculator_test.dart
    - test/core/routing_engine/routing_engine_test.dart
  update:
    - lib/core/persistence/database.dart
    - lib/core/persistence/database.g.dart
  delete: []
started_at: 2026-08-27
completed_at: 2026-08-27
executed_by: claude-sonnet-5 (builder)
reviewed_at: 2026-08-27
reviewed_by: claude-opus-5 (reviewer)
review_outcome: approve-with-reviewer-fixes
---
# E04-T02 · Routing engine

## 1. Goal
The decision-making core of the mesh: given known links to neighbors and
candidate multi-hop routes, compute a cost per route, pick the best one,
and migrate to a better route only when it's genuinely better and stable
(make-before-break) — tested entirely against T01's simulator, no real
transport required.

## 2. Business logic
- FR-ROUTE-001 / EARS-ROUTE-1: communication between two devices with no
  direct link works via one or more relays — this task computes *which*
  relay path, not the byte-forwarding itself (T04).
- FR-ROUTE-002, OQ-E04-1 (resolved, epic.md): cost = weighted sum of
  latency, `(1 - reliability)`, battery drain rate, and hop count, with two
  named profiles (`interactive`, `bulk`). Weights are named constants,
  explicitly flagged tunable in a doc comment — not hidden magic numbers.
- FR-ROUTE-006 / EARS-ROUTE-2, OQ-E04-2 (resolved): migrate to a better
  route only when its cost is ≥20% better AND has held that advantage for
  ≥10 consecutive cost-sample ticks (stability window) — both named
  constants. "Make-before-break": the new route must be validated (able to
  carry a test/heartbeat send) before the old route is dropped.
- FR-ROUTE-009 / EARS-ROUTE-4: on route failure (a hop reports `noRoute` or
  repeated loss), search known alternative paths; if none exist, the caller
  gets a clear "no route, queue for retry" result rather than a silent drop.

## 3. What this task does
- `routes` Drift table (per epic.md's Data model): candidate paths (as an
  ordered list of hop node ids) + last-measured cost factors per route,
  keyed by destination.
- `RouteCostCalculator.cost(factors: RouteCostFactors, profile: TrafficProfile) -> double`
  — pure function, the weighted-sum formula above.
- `RoutingEngine`:
  - `recordLinkMeasurement(neighborId, latencyMs, lossRate, batteryDrain)` —
    updates this device's view of a direct neighbor's link quality.
  - `computeRoute(destination, profile) -> Route?` — Dijkstra/best-path over
    known links using the cost calculator as edge weight; `null` if no path
    exists.
  - `considerMigration(destination, profile) -> MigrationDecision` — compares
    the currently-active route's cost against the best known alternative,
    applies the ≥20%/≥10-tick rule, returns `stay` or `migrateTo(route)`.
  - `onRouteFailure(destination) -> Route?` — marks the failed route's
    relevant link as degraded/down, recomputes; returns the next-best route
    or `null` (caller queues/retries per FR-ROUTE-009).

## 4. What this task does NOT do
- Does NOT send or receive any real bytes over Bluetooth/Wi-Fi — this
  engine only decides paths and costs. T04 (relay) and T03's transport
  tasks own moving bytes; this task's tests use T01's simulator exclusively.
- Does NOT implement store-and-forward packet queuing/TTL/priority — T04.
- Does NOT wire into the Devices screen or any UI — T05.
- Does NOT persist route history/analytics beyond the current best-known
  state per destination — no time-series table, no "route quality over
  time" reporting (out of scope for v1, not named in FR-ROUTE-*).
- Does NOT implement `make-before-break`'s actual "send a validation probe
  over the new route" as a real network operation — that requires T03's
  transport. This task's `considerMigration` returns a decision; T04/T03
  are responsible for actually probing before switching. Note this
  explicitly in the Run log as the seam between this task and its consumer.

## 5. Contract

### Data
Migration: new `routes` table — `destination_id TEXT`, `hops TEXT` (JSON
array of node ids, ordered), `last_cost REAL`, `last_measured_at INTEGER`
(epoch ms), `stable_since_tick INTEGER` (for the migration stability
window), PK `(destination_id, hops)` since multiple candidate routes to one
destination coexist.

### Functions
```yaml
- signature: "RouteCostCalculator.cost(factors: RouteCostFactors, profile: TrafficProfile) -> double"
  params: { factors: "{latencyMs, reliability (0-1), batteryDrainRate, hopCount}", profile: "interactive | bulk" }
  returns: "lower is better; weighted sum per §2's formula"
  purpose: "Pure, no I/O — the single place the cost formula lives"

- signature: "RoutingEngine.computeRoute(destination: String, profile: TrafficProfile) -> Route?"
  returns: "the lowest-cost known path to destination, or null if none exists"
  purpose: "Route selection"

- signature: "RoutingEngine.considerMigration(destination: String, profile: TrafficProfile) -> MigrationDecision"
  returns: "MigrationDecision.stay() | MigrationDecision.migrateTo(route: Route) — applies the >=20%/>=10-tick rule"
  purpose: "Make-before-break migration policy (the DECISION only, see §4)"

- signature: "RoutingEngine.onRouteFailure(destination: String) -> Route?"
  returns: "next-best alternative route, or null (caller must queue/retry)"
  purpose: "Failure recovery per FR-ROUTE-009"
```

## 6. Risks
- The migration stability window must be measured in discrete
  `NetworkSimulator.tick()`s in tests, not wall-clock time — keep the
  engine's own notion of "tick"/"sample" simulator-agnostic (an injected
  clock/counter) so production code can drive it from a real periodic timer
  later without this task's tests needing to change.
- Dijkstra/best-path over a small mesh is easy to get right and easy to
  silently get wrong on a cycle or a disconnected component — the test
  suite must include both a straightforward 3-node relay case AND a
  partitioned-graph case (T01's `noRoute`/down-link support exists exactly
  for this).
- Don't let "make-before-break" quietly become "break-then-make" by
  returning a migration decision the caller applies without validating
  first — the seam note in §4 exists so this doesn't get silently dropped
  when T04 wires this in.

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS id `7723ffb`
- [x] `routes` Drift table + migration, schema version bumped `7723ffb`
- [x] `RouteCostCalculator` implemented + unit tests (both profiles) `7723ffb`
- [x] `RoutingEngine.computeRoute` implemented, tested against T01's
      simulator (direct link, 2-hop relay, no-route/partitioned cases) `7723ffb`
- [x] `RoutingEngine.considerMigration` implemented, tested (both the
      20%-threshold and the 10-tick stability window, including a case
      that's better but not yet stable, and one that oscillates and never
      triggers migration) `7723ffb`
- [x] `RoutingEngine.onRouteFailure` implemented + tested `7723ffb`
- [x] `flutter analyze` clean, `flutter test` green `7723ffb`

## 8. Acceptance criteria (EARS) + test plan
- **EARS-ROUTE-1**: Communication between two devices with no direct
  connection SHALL be possible via one or more relays. (FR-ROUTE-001)
- **EARS-ROUTE-2**: Migration to a new route SHALL only be decided once the
  candidate is ≥20% better and stable for ≥10 ticks. (FR-ROUTE-006)
- **EARS-ROUTE-4**: WHEN a route fails, the engine SHALL return an
  alternative if one exists, else a clear no-route signal. (FR-ROUTE-009)

### Tests
- `test_EARS_ROUTE_1_computes_multihop_route_when_no_direct_link`
- `test_EARS_ROUTE_1_returns_null_when_partitioned`
- `test_EARS_ROUTE_2_migrates_only_after_stability_window`
- `test_EARS_ROUTE_2_does_not_migrate_below_20_percent_improvement`
- `test_EARS_ROUTE_2_does_not_migrate_on_a_single_good_sample`
- `test_EARS_ROUTE_4_returns_alternative_on_failure`
- `test_EARS_ROUTE_4_returns_null_when_no_alternative_exists`
- `test_cost_calculator_interactive_profile_weights_latency`
- `test_cost_calculator_bulk_profile_weights_reliability_and_battery`

### Manual
None — no observable app behavior changes in this task.

## 9. Self-review + Definition of Done
### Self-review
- [x] Every §7 item done, with commit hashes — all `7723ffb`
- [x] `flutter test && flutter analyze` green (no `make` target in this
      repo for Flutter; ran the underlying commands directly)
- [x] Diff confined to `files:`; §4 respected — `git status --porcelain`
      shows exactly the 5 created + 2 updated paths in `files:`, nothing
      else
- [x] No secrets or PII logged — this task adds no logging at all (pure
      Dart decision logic + a Drift table)

**Deviations from spec/design:**
1. **`RoutingEngine.recordLinkMeasurement` gained an optional `from`
   parameter**, not present in §3's documented call shape
   (`recordLinkMeasurement(neighborId, latencyMs, lossRate, batteryDrain)`).
   Defaults to `selfId`, so the primary documented use (this device
   recording one of its own direct neighbors) is unaffected. Reason: §6's
   Risk explicitly requires testing a 3-node relay AND a partitioned-graph
   case, which is impossible if the engine can only ever learn about its
   own 1-hop neighborhood — Dijkstra needs the full known-link graph, and
   no separate link-state-flooding mechanism exists in this task's scope
   (T04/T03 own real transport). This is the minimal additive extension
   that makes the required multi-hop tests possible without inventing a
   second recording method. Documented in the function's own doc comment.
2. **`RoutingEngine.setActiveRoute`/`activeRouteFor` are new methods, not
   named in the §5 contract.** `considerMigration`'s documented behavior
   ("compares the currently-active route's cost against the best known
   alternative") requires the engine to track *which* route is currently
   active, and no method in §5 sets that state. Added as the minimal
   scaffolding to make `considerMigration`/`onRouteFailure` testable in
   isolation from a real transport layer, consistent with §4's note that
   T04 owns the actual probe-then-switch; in production T04 would call
   `setActiveRoute` once it has validated a migration candidate.
3. **§4's seam is honored literally**: `considerMigration` never mutates
   `_activeRoutes` itself — it only returns `MigrationDecision.migrateTo`.
   Switching the active route is always an explicit, separate
   `setActiveRoute` call (by the caller/test), so "make-before-break"
   cannot silently become "break-then-make" from inside this task's code.
4. **Migration stability window is sampled by consecutive
   `considerMigration` calls, not `NetworkSimulator.tick()`** — per T01's
   review flag #2 (`tick()` has no per-tick sampling hook to correlate
   against) and this task's own §6 Risk ("keep the engine's own notion of
   tick/sample simulator-agnostic"). One `considerMigration` call = one
   sample; production code drives this from its own periodic timer later
   without any test change, exactly as §6 asks.
5. **No new migration-test file was added**, even though the task
   description pointed at `crypto_migration_test.dart`'s hand-built-schema
   pattern for context. `files:` does not list a new persistence test file
   for this task, and rule 6 makes `files:` the diff ceiling. Instead, the
   v7->v8 `routes`-table migration is exercised implicitly: the existing
   `database_migration_test.dart` (pins a v1 schema) and
   `relationships_migration_test.dart` (pins a v2 schema) both open
   `AppDatabase.forTesting` at `schemaVersion` 8 now, so drift runs every
   `onUpgrade` step — including the new `if (from < 8) createTable(routes)`
   — end to end; both pass (`flutter test test/core/persistence/`). If a
   dedicated `routes`-migration test is wanted, that is a `files:`
   expansion for review to request, not something to add unilaterally.

**Files actually touched:**
- `lib/core/persistence/routing_tables.dart` (new)
- `lib/core/routing_engine/route_cost_calculator.dart` (new)
- `lib/core/routing_engine/routing_engine.dart` (new)
- `test/core/routing_engine/route_cost_calculator_test.dart` (new)
- `test/core/routing_engine/routing_engine_test.dart` (new)
- `lib/core/persistence/database.dart` (updated: `routes` table wired in,
  schema version 7 -> 8, additive `onUpgrade` step)
- `lib/core/persistence/database.g.dart` (regenerated via `build_runner`)

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id (see Run
      log for the id -> test mapping)
- [x] Review APPROVE from a **different model** than `executed_by` (rule 5)
      — `claude-opus-5 (reviewer)` vs `claude-sonnet-5 (builder)`. Caveat:
      the reviewer authored the F2 product-code fix, so rule 5's
      independence does not hold *for that fix* — flagged for
      re-verification at the E04 bug sweep (see Review log).
- [ ] Squash-merged to `epic_04` branch; tracker + metrics stamped
- [ ] 🧍 Human `verified` (epic-level batches are fine)

## Open Questions
_(none — OQ-E04-1/OQ-E04-2 resolved at epic level)_

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- 2026-08-27, `claude-sonnet-5 (builder)`: implemented `RouteCostCalculator`
  (weighted-sum formula, `interactive`/`bulk` profiles, named tunable
  weights) and `RoutingEngine` (`recordLinkMeasurement`, `computeRoute`
  Dijkstra, `considerMigration` make-before-break decision,
  `onRouteFailure`) plus the `routes` Drift table (schema v7 -> v8,
  additive `createTable`). Tests written first against T01's
  `NetworkSimulator` (`test/core/routing_engine/routing_engine_test.dart`)
  and the pure cost formula
  (`test/core/routing_engine/route_cost_calculator_test.dart`).

  **EARS -> test mapping:**
  - EARS-ROUTE-1: `test_EARS_ROUTE_1_computes_multihop_route_when_no_direct_link`,
    `test_EARS_ROUTE_1_prefers_direct_link_over_a_worse_relay`,
    `test_EARS_ROUTE_1_returns_null_when_partitioned`,
    `test_EARS_ROUTE_1_returns_null_when_relay_link_removed_mid_route`
  - EARS-ROUTE-2: `test_EARS_ROUTE_2_does_not_migrate_on_a_single_good_sample`,
    `test_EARS_ROUTE_2_does_not_migrate_below_20_percent_improvement`,
    `test_EARS_ROUTE_2_migrates_only_after_stability_window`,
    `test_EARS_ROUTE_2_oscillating_candidate_never_triggers_migration`
  - EARS-ROUTE-4: `test_EARS_ROUTE_4_returns_alternative_on_failure`,
    `test_EARS_ROUTE_4_returns_null_when_no_alternative_exists`
  - Cost calculator: `test_cost_calculator_interactive_profile_weights_latency`,
    `test_cost_calculator_bulk_profile_weights_reliability_and_battery`
    (plus 2 supporting tests: determinism, hop-count monotonicity)

  **Migration stability window vs. T01's simulator limitations (per T01's
  reviewer flags, read before starting):** `NetworkSimulator.tick()` is a
  bare counter with no per-tick sampling hook — nothing correlates a tick
  to a `considerMigration` sample. Rather than assume the simulator gives
  sample correlation, `RoutingEngine` owns its own notion of "sample":
  every call to `considerMigration` is one sample, tracked via an internal
  `_migrationTracking` map keyed by destination (candidate route's hops +
  a consecutive-sample counter, reset whenever the candidate identity
  changes or the improvement drops below 20%). This makes the 10-sample
  stability window fully deterministic and testable without any real or
  simulated delay — `test_EARS_ROUTE_2_migrates_only_after_stability_window`
  calls `considerMigration` in a tight loop and asserts `stay()` for
  samples 1-9 and `migrateTo(...)` on sample 10. Production code (T04) can
  later drive `considerMigration` from a real periodic timer without any
  change to this logic. Also handled: T01's other flags — `SimulatedLink`
  has no `==`/`hashCode` (tests compare individual link fields, never
  `SimulatedLink` instances, via a `_recordFromSimulatedLink` test helper);
  `send()` ignores payload (a dummy `Uint8List` is passed on every
  simulated send in the relay-proving test); no battery-accumulator reset
  (each test constructs its own fresh `NetworkSimulator`).

  Make-before-break seam (§4): `considerMigration` returns
  `MigrationDecision.stay()`/`migrateTo(route)` only — it never mutates
  the engine's active-route state itself. Switching is always a separate,
  explicit `setActiveRoute` call by the caller, so this task's code cannot
  silently turn "make-before-break" into "break-then-make"; the actual
  validation probe before switching is T04/T03's job, not implemented
  here, per §4.

  `flutter pub run build_runner build --delete-conflicting-outputs` ->
  regenerated `database.g.dart` for the new `Routes` table cleanly (121
  outputs written, no errors).

  `flutter analyze` -> No issues found.
  `flutter test test/core/routing_engine/` -> 24/24 pass (14 new: 4 cost
  calculator + 10 routing engine; 10 pre-existing T01 simulator tests
  unaffected).
  `flutter test test/core/persistence/` -> 3/3 pass, confirming the v1->v8
  and v2->v8 `onUpgrade` chains (including the new `routes` table step)
  run cleanly with no data loss on the pre-existing tables.
  `flutter test` (full suite) -> **94/94 pass** (80 pre-existing + 14 new).
  Grepped the diff for any import of `network_simulator.dart`/
  `simulated_link.dart` from outside `test/` — none; the simulator stays
  test-only per T01 §4/this task's §1.
  Commit `7723ffb` — `feat(E04-T02): routing engine cost formula, Dijkstra
  route selection, make-before-break migration, failure recovery`.

## Review log
- 2026-08-27, `claude-opus-5 (reviewer)` — **APPROVE (with reviewer fixes)**.
  Rule 5 satisfied on the review itself (`opus-5` != `executed_by`
  `sonnet-5`).

  **Suite, run by the reviewer, not trusted from the PR body:**
  `flutter analyze` -> `No issues found!`. `flutter test` -> **94/94** at
  the builder's `760d8db`, confirming the self-report exactly. After the
  fixes below: **95/95**, analyze still clean.

  **Scope: in-contract.** `git diff --name-only epic_04...epic_04_task_02`
  is exactly the 5 created + 2 updated paths in `files:`, plus the task
  file. No stray edits. §4 respected — no transport, no queuing, no UI, no
  DAO/persistence writer beyond the declared table. No logging of any kind
  in the new code (grepped for print/debugPrint/log(/logger -> none), so
  nothing sensitive can leak.

  **Mutation testing rather than test-reading alone.** Every claim below
  was verified by breaking the constant and confirming the suite goes red.

  ### F1 (blocking, fixed) — the 20% threshold had ZERO real coverage
  `test_EARS_ROUTE_2_does_not_migrate_below_20_percent_improvement` passed
  for the wrong reason. It set the active route to `hops: ['X','dest']` and
  then recorded links `A->X` and `X->dest` — so the "candidate" the engine
  computed was `['X','dest']`, *identical hops to the active route*.
  `considerMigration` (routing_engine.dart:204-206) short-circuits on
  `!_hopsEqual(best.hops, active.hops)` before the cost comparison, so the
  threshold expression was never evaluated. Proof: mutating
  `kMigrationImprovementThreshold` from `0.20` to `0.0` left
  `test/core/routing_engine/` fully green (24/24). A test that stays green
  when the constant it exists to protect is deleted is not coverage.
  **Fix:** rewrote the test to use a genuinely distinct candidate
  (`A->Y->dest`, ~5% cheaper) and derived the active route cost from the
  engine instead of a literal, with `expect`s guarding the test's own
  premise (distinct hops, improvement in `(0, threshold)`) so it fails
  loudly if it ever stops testing the threshold. Mutant now fails.

  ### F2 (blocking, fixed) — S2 logic bug: migration compared against a stale cost snapshot
  `considerMigration` read `active.cost` — the cost frozen into the `Route`
  object when it was computed — and never re-derived the active route cost
  from currently-known links. So a route that *rots after* being made
  active is defended by its own historical good score, and the engine never
  migrates off it. Reproduced directly: active direct link `A->dest`
  snapshotted at cost **1.1**, then degraded to 5000ms / 90% loss / 5.0
  battery (real current cost **~70**); a relay `A->R->dest` at cost **2.1**
  — a ~97% real improvement — became available. `considerMigration` returned
  `stay()` for **50 consecutive samples**, indefinitely. The device stays
  wedged on a 90%-loss link while a 33x better route sits unused. This
  defeats the whole point of FR-ROUTE-006 / EARS-ROUTE-2, and no caller can
  work around it from outside the engine.

  This is **L-backend-003, recurrence 5** — `Route.cost` became a cached
  authority for "how good is this route", and the reader
  (`considerMigration`) was never re-audited against the fact that
  `_knownLinks` had become the live source of truth.

  **Fix** (routing_engine.dart): added `_currentCostOf(hops, profile)`,
  which re-sums the route edge costs from `_knownLinks` as they are now
  and returns `null` if any hop is no longer a known link;
  `considerMigration` now compares against that, treating a no-longer-viable
  active route as `double.infinity` (an active route whose links we no
  longer know is not defensible). Added regression test
  `test_EARS_ROUTE_2_migrates_when_active_route_itself_degrades`, which
  asserts the premise that the relay is *worse than the stale snapshot* but
  better than reality — so it can only pass if the comparison is live.
  Reverting the fix turns the suite red.

  ### F3 (S3, doc fixed, scope question left for the planner) — dead `routes` table
  The `routes` table has **no writer and no reader anywhere** (grep over
  `lib/`+`test/` excluding `database.g.dart`: only the `createTable` in the
  migration). `RoutingEngine` is entirely in-memory. Worse, the
  `stableSinceTick` doc comment claimed it "backs the make-before-break
  migration stability window" while the actual authority is the in-memory
  `_migrationTracking.consecutiveSamples` — and those are *different
  quantities* (a sample count, not a tick number). That is the precise
  L-backend-003 trap laid for whoever wires persistence in next.
  **Fixed the docstring** to state plainly that the column is not the
  authority, has no reader/writer today, and must be reconciled by T04.
  Did **not** add a DAO: §5 declares no persistence functions, so writing
  one would be inventing API beyond the contract. **Left for the planner:**
  decide whether T02 or T04 owns `routes` persistence — as it stands the
  schema is declared but the engine loses all route state on restart.

  **Resolved by the planner, 2026-08-27: neither owns it — intentionally
  in-memory only, no follow-up task.** `epic.md`'s own Data model section
  states `routes` (alongside `relay_packets`) is "local only, **ephemeral**"
  — losing route state on restart is consistent with that description, not
  a gap in it. Mesh links are re-measured from scratch every session
  anyway (a stale route from a prior session is not more trustworthy than
  a freshly computed one), so persisting and reloading `RoutingEngine`
  state would add complexity without a real reliability benefit for v1.
  The `routes` Drift table stays declared (harmless, already
  migration-tested) but unused — flagged here rather than removed
  mid-epic, so a future epic with an actual need for cross-restart route
  history (e.g. E13 diagnostics) has a table shape to start from instead
  of inventing one from nothing. T04 must call `RoutingEngine.computeRoute()`
  fresh rather than assume any persisted route state exists.

  ### Verified-good (paranoia found nothing)
  - **Migration counter internal consistency** — both boundary cases named
    in review actually hold. Cost oscillating across the 20% line: the
    below-threshold sample hits `_migrationTracking.remove` (line 209), so
    the counter resets to 0 rather than accumulating stale progress; it
    restarts at 1, not where it left off. Two distinct candidates each
    under the window: tracking is keyed on `candidateHops` and compared
    with `_hopsEqual` (line 214), so a mismatch installs a *fresh* counter
    at 1 — neither candidate is ever credited with the other samples. No
    list-aliasing hazard either: `_bestRoute` builds a new list
    (`hops.reversed.toList()`) each call. `setActiveRoute` also resets
    tracking (line 176), which is correct.
  - **Stability window constant is genuinely covered** — mutating
    `kMigrationStabilityTicks` 10 -> 1 fails 3 tests. Only the threshold
    half (F1) was unprotected.
  - **Cost formula direction is correct per §2** and genuinely tested.
    `interactive` latency:unreliability = 0.01:10; `bulk` = 0.002:20 — so
    interactive weights latency 10x more heavily relative to reliability,
    and bulk weights reliability/battery more heavily. Swapping the two
    weight sets fails 2 tests, so the profile tests bite.
  - **EARS-ROUTE-1 multi-hop and partition tests prove their names.**
    `..._computes_multihop_route_when_no_direct_link` has no `A->C` link at
    all and additionally walks the path over the simulator asserting
    `SendResultDelivered` per hop. `..._returns_null_when_partitioned`
    registers `C` as a real node with no inbound link from the A component,
    so `null` is a genuine no-path result, not a missing-node artifact.
  - **`onRouteFailure` degrades before recomputing** (line 240):
    `removeLink(selfId, active.hops.first)` runs *before* `_bestRoute`, so
    the returned alternative provably cannot route back through the failed
    first hop. Note (not blocking): it evicts the whole first-hop link, so
    sibling routes through that neighbor are dropped too even when the real
    failure was further downstream; they recover on the next
    `recordLinkMeasurement`. Acceptable and conservative for v1.
  - **v7->v8 migration coverage — the builder claim HOLDS, weakly.**
    `database_migration_test.dart` sets `raw.userVersion = 1` and
    `relationships_migration_test.dart` pins v2, then both open
    `AppDatabase.forTesting` at `schemaVersion` 8, so drift really does run
    `onUpgrade(from:1, to:8)` / `(2, 8)` including
    `if (from < 8) createTable(routes)`. The step executes and does not
    throw. But nothing asserts the resulting table shape, columns or
    composite PK, because no code reads the table at all (F3). So: real
    execution coverage, no behavioural coverage. Accepted for this task —
    it becomes meaningful only when the F3 persistence question is answered.

  ### Note for T04 / the epic sweep (not blocking this task)
  `recordLinkMeasurement`'s optional `from` param is a reasonable, minimal,
  additive deviation and correctly documented — multi-hop Dijkstra is
  untestable without some way to seed non-local links, and it defaults to
  `selfId` so the documented call shape is unchanged. It is *not* test-only
  though: it is public API, so once T04 feeds it from real link-state
  gossip, any node can assert arbitrary links between arbitrary third
  parties and steer this device routing. That is inherent to link-state
  routing, not a bug introduced here, but T04 must authenticate link-state
  claims before trusting them. Recorded for the E04 sweep.

  **Reviewer changed only:** `routing_engine.dart` (F2 + helper),
  `routing_tables.dart` (F3 doc), `routing_engine_test.dart` (F1 rewrite +
  F2 regression test) — all inside `files:`. Because the reviewer authored
  the F2 product-code fix, rule 5 independence does not hold for that fix
  specifically; it must be re-verified at the E04 bug sweep by a model that
  did not write it.
