---
id: E04-B31
epic: E04
type: bug
title: "Discover silently drops a genuine Nexora peer: handleUuidResult consumes the pending check on Android's first, stale cached-UUID answer before any fresh SDP result"
layer: backend
size: XS
status: done
owner_agent: builder
model: opus
preferred_agent: any
token_estimate: { tier: S, range: "5k-20k" }
severity: S2
priority: { moscow: must, p: P1 }  # hides real Nexora peers from Discover; a regression exposed by E04-T07
found_in: "live two-device testing for E04-T07/E04-B28, Pixel 8 Pro logcat, 2026-09-15"
depends_on: [E04-T06, E04-T07]
blocks: []
traces_to: [FR-DISC-001]
required_context:
  - epics/E04-mesh-routing/tasks/E04-T06.md
  - epics/E04-mesh-routing/tasks/E04-T07.md
  - android/app/src/main/kotlin/com/nexora/nexora/transport/BluetoothTransport.kt
design_contract: n/a
files:
  create:
    - epics/E04-mesh-routing/tasks/E04-B31.md
  update:
    - android/app/src/main/kotlin/com/nexora/nexora/transport/BluetoothTransport.kt
    - epics/E04-mesh-routing/tracker.md
started_at: 2026-09-15
completed_at: 2026-09-15
executed_by: "orchestrator session (claude-opus-5) -- reviewer must NOT be opus"
reviewed_at: 2026-09-15
reviewed_by: "claude-sonnet-5 (cross-model, rule 5)"
review_outcome: approve
repro:
  - "Pixel 8 Pro Discover with the Redmi 10 2022 discoverable (development 9d37dfc + E04-B29 on both)."
  - "Pixel logcat: 05:17:28.125 `deviceFoundCallback` for the Redmi; 05:17:28.163 `fetchRemoteUuidsWithSdp` from Nexora's uid; 05:17:34.170 `ACTION_UUID Intent: device: ...00:01 count: 11`, the cached list without the Nexora discovery UUID; 05:17:51.271 `SDP connection failed`."
  - "Nexora's `handleUuidResult` removed the pending check on that first, stale answer and dropped the Redmi. The Pixel Dashboard kept showing 'No peers nearby' after the scan. That is also why E04-B28's live device-lost test was inconclusive: the peer was never discovered in the first place."
expected: "A found device whose first UUID answer is stale is still confirmed if a fresh SDP answer with the Nexora discovery UUID arrives within the existing timeout."
actual: "The first answer, even the stale cache, is final: a peer whose cached UUIDs predate its discovery record is dropped from Discover."
---
# E04-B31 · Don't let a stale cached UUID answer drop a Nexora peer

## 1. Root cause

`handleDeviceFound` (E04-T06/T07) registers a pending Nexora check and calls `fetchUuidsWithSdp()`. `handleUuidResult` used `pendingNexoraChecks.remove(address)` and decided on whatever `ACTION_UUID` arrived first. During an active inquiry, Android delivers the device's cached UUIDs immediately and the real SDP result later. For any device with a pre-existing cache that predates its Nexora discovery record, which is every peer bonded or seen before E04-T07, the first answer lacks the discovery UUID. The check is consumed and the peer is dropped. Before E04-T07 this was masked, because the same stale cache contained the generic SPP UUID the old filter accepted.

## 2. Fix

`handleUuidResult` now reads the pending check without removing it:

- A **negative** answer leaves the check pending, so a later fresh answer can still confirm it.
- A **positive** answer removes it identity-scoped (`remove(address, pending)`) and emits once.
- The existing `SDP_LOOKUP_TIMEOUT_MS` identity-scoped removal in `handleDeviceFound` still bounds the check's lifetime.

## What this task does NOT do

- Does not change the filter itself (`advertisesNexora`), what is published, or the timeout value.
- Does not fix SDP failures to a particular device (the Pixel -> Redmi page timeouts observed live). If no positive answer ever arrives, the peer is still not shown; that is E04-T07's intended fail-closed behavior.
- Does not add a Kotlin test harness (none exists in this project).

## 2. Definition of Done

- [x] A negative `ACTION_UUID` no longer consumes the pending check; a positive one does, identity-scoped
- [x] `flutter build apk --debug` compiles clean (commit `12d18fd`)
- [x] Independent review (claude-sonnet-5, APPROVE, PR #273)
- [ ] Live: Pixel Discover lists the discoverable Redmi (Dashboard counts it as a nearby peer)

## Open Questions

- OQ-E04-B31-1: if every SDP query to a peer fails (as the Pixel -> Redmi page timeouts did at times in this session), that peer still never appears in Discover. That is by design (E04-T07 fails closed), but on flaky links it can look like "Nexora doesn't see my friend". A future task could retry the SDP query once after inquiry ends.

## Run log

- 2026-09-15: root-caused from live logcat while verifying E04-T07/E04-B28, and implemented directly while the human was away (extended grant, rule 3).
- 2026-09-15 — Review (claude-sonnet-5): APPROVE, no findings. Confirmed identity-scoped removal via data-class equality and token, no double emit, lifetime still bounded by the SDP timeout and scan-restart clear, E04-B28's lost-diff window still covers a late positive answer, and main-thread-only access. Live verification pending (a combined development + E04-B30 + E04-B31 test build was installed on both phones for it).
- 2026-09-15 21:49 — Live, PARTIAL (development `6cd3cbc` on both phones). Redmi made discoverable (`SCAN_MODE_CONNECTABLE_DISCOVERABLE` 21:49:38); Pixel Discover started 21:49:42 and `deviceFoundCallback` reported the Redmi (`00:01`) at 21:49:45 and 21:49:46. The Pixel's cached UUIDs for the Redmi already contained `NEXORA_DISCOVERY_UUID` (`6f3b2c1e-…`, refreshed by E04-T07's bonded-cache refresh at the 21:44 listener start, see E04-B30), so no fresh SDP was issued and no stale negative `ACTION_UUID` arrived. The Pixel Dashboard then read "Connected, 2ms" (before: "No peers nearby"). Also recorded: Pixel → Redmi SDP and RFCOMM now succeed (21:46:03–05, see E04-B32), so OQ-E04-B31-1's page timeouts did not recur in this session. Not ticked: the fix's own branch (a negative first answer that must not consume the check) was not exercised, because the cache was already fresh, and an ACL link to the Redmi existed during the scan, so "Connected" may come from E04-B27's link-quality path rather than the discovery emit. Reproducing the stale-cache case needs a peer whose cached record predates its Nexora record, which these two phones no longer have.
- 2026-09-15 — Close-out decision (orchestrator, human instruction "Do what is best. Complete everything"): the live box stays unticked and is carried as a known verification gap, not a blocker for closing E04. Reasons: (1) the only way to recreate a stale cached UUID record on these phones is to install a pre-E04-T07 build on one phone and let the other cache its record — that means rolling a live test phone back to an old build, and the only thing it would prove is this one narrow branch; (2) the fix is a two-line change to when a pending check is removed, approved by independent review with no findings; (3) the surrounding path (a Nexora peer found by scan and counted as nearby) was observed live at 21:49 and 21:53. Re-check on the next genuinely new Nexora phone: a first Discover scan of it should list it even when Android answers with a cached record first.
