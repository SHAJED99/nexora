---
id: E04-B15
epic: E04
type: bug
title: "Delivery acks, calls, groups, and location sharing likely have the identical raw-Bluetooth-MAC-addressing defect E04-B13 just fixed for 1:1 chat -- deliberately deferred from that task's own scope, still broken device-to-device"
layer: backend
size: M
status: done
owner_agent: builder
executed_by: builder (claude-sonnet-5)
started_at: 2026-09-12
completed_at: 2026-09-12
reviewed_at: 2026-09-12
reviewed_by: claude-opus-5 (independent reviewer; rule 5 -- executed_by was claude-sonnet-5; two rounds, CHANGES then APPROVE)
review_outcome: approve
model: any
preferred_agent: any
token_estimate: { tier: M, range: "60k-150k" }
severity: S2
priority: { moscow: should, p: P2 }  # 1:1 text chat (the original bug
  # report) is fixed by E04-B13; these are real but secondary features
  # by comparison. Flagging for a human bug_priorities look rather than
  # asserting a priority unilaterally, since the relative importance of
  # calls/groups/location vs. this session's own time budget is a
  # judgment call.
depends_on: [E04-B13]
blocks: []
traces_to: [FR-DISC-001, FR-COMM-001]
required_context:
  - lib/core/messaging/prekey_exchange.dart
  - lib/core/messaging/messaging_stack.dart
  - lib/core/messaging/delivery_ack.dart
  - lib/core/calls/call_signaling.dart
  - lib/core/messaging/group_membership_service.dart
  - lib/core/crypto/group_crypto_service.dart
  - lib/core/messaging/location_share.dart
  - epics/E04-mesh-routing/tasks/E04-B12.md
  - epics/E04-mesh-routing/tasks/E04-B13.md
files:
  update:
    # Confirmed for real against `grep -rn "RelayPacketFrame(" lib/` (13
    # real construction sites) -- see DoD for the full classification.
    # Corrected from the original guess-list (L-process-011 shape): the
    # real file is `location_share_service.dart`, not `location_share.dart`
    # (that file has no `RelayPacketFrame` construction site at all);
    # `prekey_exchange.dart` and `send_group_message_use_case.dart` were
    # not in the original guess-list but turned out to be real construction
    # sites (the carried-forward E04-B14 finding, and the group-message
    # send path, respectively) -- both already covered by this task's own
    # `required_context`/scope, just missing from the fence itself.
    - lib/core/messaging/delivery_ack.dart
    - lib/core/calls/call_signaling.dart
    - lib/core/messaging/prekey_exchange.dart
    - lib/features/groups/domain/group_membership_service.dart
    - lib/features/groups/domain/send_group_message_use_case.dart
    - lib/core/crypto/group_crypto_service.dart
    - lib/features/location/domain/location_share_service.dart
    - test/core/messaging/delivery_ack_test.dart
    - test/core/messaging/prekey_exchange_test.dart
    - test/core/calls/call_signaling_test.dart
    - test/features/groups/domain/group_membership_service_test.dart
    - test/features/groups/domain/send_group_message_use_case_test.dart
    - test/core/crypto/group_crypto_service_test.dart
    - test/features/location/domain/location_share_service_test.dart
  create: []
  delete: []
---
# E04-B15 · Extend E04-B13's outbound-identity-resolution fix to the other control protocols

## 1. Why this task exists
`E04-B13` fixed the raw-Bluetooth-MAC-vs-real-`selfDeviceId` addressing
mismatch (root-caused in `E04-B12`) for exactly two `RelayPacketFrame`
construction sites: `PrekeyExchange._sendControlFrame` (session
bootstrap) and `MessagingStack`'s own `encryptAdapter` (1:1 text chat —
the literal feature the original human bug report, "I can not send any
message," was about). That task's own reviewer confirmed via a full grep
that **13 `RelayPacketFrame(` construction sites exist in `lib/`** —
meaning at least 11 more sites were not touched, deliberately, to keep
`E04-B13` at a reviewable `size: M`.

Every one of those other sites almost certainly has the IDENTICAL defect:
addressed by Bluetooth MAC, which can never match a real peer's
`InboundPipeline._handleBuffer` `isForUs` check on real, independently
provisioned hardware, for exactly the same reason `E04-B12`'s own root
cause explains. Candidate features affected (confirm each individually,
do not assume the list is complete or that every guess below is
correct): `DeliveryAckService` (read receipts/delivery ticks — E06-T08),
`CallSignaling` (E10), `GroupMembershipService`/`GroupCryptoService`
group messaging (E07), `LocationShare` (E09). Each of these features
likely appears to "work" in this codebase's own existing test suite
(mocked transport, no real addressing mismatch possible in a test
double) while being silently broken on two real physical devices — the
exact same gap class `E04-B11`→`E04-B13` spent a full session finding for
1:1 chat, now suspected across the rest of the app's real-device surface.

## 2. What this task does
1. **Enumerate every `RelayPacketFrame(` construction site in `lib/`**
   (re-run the grep `E04-B13`'s reviewer used, don't trust this task
   file's own guess-list above) and classify each: already fixed
   (`E04-B13`'s two sites), needs the identical fix, or is a relay/forward
   site that legitimately never constructs a frame (like
   `RelayEngine`/`ConnectionEnsuringSender`, already confirmed clean by
   `E04-B13`'s reviewer).
2. **Apply the same fix pattern `E04-B13` established** to every
   remaining real construction site: resolve `Relationship.
   remoteSelfDeviceId` for the target Bluetooth MAC when known, fall back
   to the raw MAC when not yet announced — reuse `E04-B13`'s own
   `resolveOutboundDestination` helper (or an equivalent already-reviewed
   function) rather than re-deriving the logic independently per call
   site.
3. **Write a test per affected feature** proving the fix, following
   `E04-B13`'s own established test shape (a real two-`MessagingStack`
   integration test over the actual transport boundary, not a mock that
   would hide the exact defect being fixed).
4. If real hardware is available at implementation time, attempt live
   verification for at least one non-chat feature (e.g. a delivery ack
   round trip) to build additional confidence beyond the Dart-level
   proof — but per `E04-B13`'s own precedent, a genuine hardware
   unavailability is an acceptable, honestly-disclosed limitation, not a
   reason to block this task indefinitely.

## 3. What this task does NOT do
- Does NOT touch 1:1 chat's own two already-fixed sites
  (`E04-B13`'s own `files:` fence) — this task is purely the remaining
  sites.
- Does NOT fix `E04-B14`'s own bundle-response-provenance gap — separate,
  already-filed task.
- Does NOT add new features to calls/groups/location — this is purely an
  addressing-correctness fix to features that already exist in this
  codebase.

## Definition of Done
- [x] Every `RelayPacketFrame(` construction site in `lib/` classified
  (fixed / needs fix / legitimately no-op). `grep -rn "RelayPacketFrame("
  lib/` returns 14 raw hits; one (`messaging_stack.dart:30`) is a comment,
  not code, leaving **13 real construction sites** — matching E04-B13's
  own reviewer's independently-confirmed count exactly. Full
  classification:
  - **Already fixed by E04-B13 (2):** `messaging_stack.dart:733`
    (`encryptAdapter`), `prekey_exchange.dart` (`_sendControlFrame`,
    the outbound-request leg).
  - **Fixed by this task (6):** `lib/core/messaging/delivery_ack.dart`
    (`_sendAck`), `lib/core/calls/call_signaling.dart` (`_sendFrame`),
    `lib/features/groups/domain/group_membership_service.dart`
    (`_sendOne`),
    `lib/features/groups/domain/send_group_message_use_case.dart`
    (`send`'s per-recipient loop), `lib/core/crypto/group_crypto_service.dart`
    (`_sendOne`), `lib/features/location/domain/location_share_service.dart`
    (`share`) — exactly the six sites E04-B13's own Open Questions
    enumerated, confirmed unfixed by re-reading each file in full before
    changing it.
  - **Legitimately raw-MAC by design (1):**
    `lib/core/messaging/identity_announce.dart:171` — the announce
    protocol's whole job is to LEARN a peer's real `selfDeviceId`ability
    before either side knows it; there is no `remoteSelfDeviceId` yet to
    resolve, and this `controlKind` bypasses `isForUs` entirely
    (`InboundPipeline._handleBuffer`'s own documented bypass), so raw-MAC
    addressing here was never the defect and is unchanged.
  - **Legitimate non-issues, no outbound addressing at all (4):**
    `lib/core/messaging/relay_packet_frame.dart:100` (the class's own
    `const` constructor — a definition, not a call site),
    `relay_packet_frame.dart:261` (`deserialize`'s own return — decoding
    already-received bytes, not constructing a new outbound frame),
    `lib/core/messaging/inbound_pipeline.dart:674` and `:777` — both
    rebuild an ALREADY-RECEIVED frame with its leading `controlKind` byte
    stripped, for internal dispatch to a registered handler; neither is
    a new outbound construction.
- [x] Every site needing the fix has it, using the same resolution
  pattern `E04-B13` established: `resolveOutboundDestination(_stack.db,
  peerDeviceId)` resolves the frame's `destination` field only; every
  transport/routing call (`directSend`, `relayEngine.enqueue`,
  `routingEngine.computeRoute`) keeps using the raw Bluetooth MAC
  unchanged, exactly matching E04-B13's own split. `0152854`
  - **Also fixed, the carried-forward E04-B14 finding (§ below):**
    `PrekeyExchange._handleBundleRequest`'s reply leg, previously
    addressed by the requester's claimed logical identity
    (`frame.source`) for BOTH the frame's `destination` and the physical
    transport dial — the dial now uses the request's actual arrival
    link (`linkDeviceId`) via a new `_sendControlFrame` `transportTarget`
    parameter, while the frame's `destination` field is unchanged
    (`peerDeviceId`/`frame.source`, which was already correct). Trust
    evaluation for the inbound request is now keyed by that same
    physical link too (relationships are MAC-keyed, never keyed by a
    claimed logical id) — see the Open Questions/Deviations below for
    the residual, out-of-fence gap this investigation also surfaced.
- [x] A test per affected feature, proving the fix:
  `test_E04_B15_ack_frame_destination_resolves_via_resolveOutboundDestination`
  (`delivery_ack_test.dart`),
  `test_E04_B15_invite_frame_destination_uses_learned_remoteSelfDeviceId`
  (`call_signaling_test.dart`),
  `test_E04_B15_group_control_frame_destination_uses_learned_remoteSelfDeviceId`
  (`group_membership_service_test.dart`),
  `test_E04_B15_group_message_frame_destination_uses_learned_remoteSelfDeviceId`
  (`send_group_message_use_case_test.dart`),
  `test_E04_B15_key_distribution_frame_destination_uses_learned_remoteSelfDeviceId`
  (`group_crypto_service_test.dart`),
  `test_E04_B15_share_frame_destination_uses_learned_remoteSelfDeviceId`
  (`location_share_service_test.dart`),
  `test_E04_B15_bundle_request_reply_uses_physical_link_not_claimed_source`
  (`prekey_exchange_test.dart`, the carried-forward finding's own fix),
  `test_E04_B15_blocked_peer_evaluated_by_physical_link_not_claimed_source`
  (`prekey_exchange_test.dart`, added round 2 per reviewer finding F1 —
  the trust-evaluation half of the same fix; falsified: fails when
  `evaluationKey` is reverted to `peerDeviceId`, passes restored) — all
  real two-`MessagingStack` integration tests over the actual mocked
  Pigeon transport boundary, using a Bluetooth-MAC-shaped id deliberately
  DIFFERENT from each stack's own real `selfDeviceId` (E04-B13's own
  established convention), never a mock that would hide the exact defect
  being fixed. Every test proves the real, observable effect (a session
  establishes, a group applies a rename, B's own chain/fix/message row is
  populated) — not merely that a function was called.
  `test_E04_B14_forged_bundle_response_on_wrong_link_is_rejected`
  (pre-existing, unmodified) re-run and still green, proving this task's
  own response-leg fix did not silently break E04-B14's link-binding
  check — the explicit regression proof this task's own carried-forward
  note demanded.
- [x] Existing test suite green. `flutter test`: 1520/1520 passing (full
  suite, 8 new tests, run twice for stability). `flutter analyze`: 0
  issues (2 pre-existing, unrelated infos — the same two this whole
  E04-B12→B14 chain has already disclosed each time).

## Open Questions
- **`DeliveryAckService._sendAck` and `CallSignaling`'s
  accept/decline/ring reply path (in response to an inbound `invite`)
  have a DEEPER, related gap this task's own `files:` fence cannot
  close.** Investigated while implementing this task, not anticipated by
  its own §1/§2: both are REPLY-shaped call sites (their own
  `peerDeviceId`/`session.peerDeviceId` argument is always a value
  LEARNED from a previously-received frame's `frame.source` — i.e.
  already a logical `selfDeviceId`, never a Bluetooth MAC), unlike the
  six sites this task fixed (all locally-initiated sends addressed by a
  MAC the user/system already holds, e.g. `Relationship.deviceId` /
  `GroupMembers.deviceId`). This task's own `resolveOutboundDestination`
  fix for these two sites is a real, harmless, but STRUCTURALLY INERT
  no-op for the frame's `destination` field (the lookup key is never
  MAC-shaped, so it can never match a `relationships` row and always
  falls back to itself, which happens to already be the correct logical
  destination) — the actual remaining defect is the TRANSPORT DIAL,
  which still attempts to connect/enqueue using that same logical id,
  never a MAC. On real hardware this means: (a) `DeliveryAckService`'s
  own ack replies, and (b) `CallSignaling`'s own accept/decline/ring
  replies to an inbound invite, likely still fail to physically leave
  the device. Unlike `PrekeyExchange._handleBundleRequest` (fixed in
  this same task), neither `DeliveryAckService.handleControlFrame` nor
  `CallSignaling.handleWireFrame` receives a `linkDeviceId` at all — both
  are registered on `InboundPipeline`'s SHARED, generic `ControlHandler`
  typedef (`Future<void> Function(RelayPacketFrame)`), not a dedicated
  link-bound slot the way `E04-B14` built for `PrekeyExchange`. Widening
  `ControlHandler` itself to carry link data would force editing all
  five of its sharers (`delivery_ack.dart`, `group_membership_service.dart`,
  `group_crypto_service.dart`, `call_signaling.dart`,
  `location_share_service.dart`) just to keep them compiling — exactly
  the blast radius `E04-B14`'s own Deviations explicitly investigated and
  declined to take on, for the identical reason. Recommend a dedicated
  follow-up bug task (mirroring `E04-B14`'s own dedicated-slot pattern,
  scoped to `inbound_pipeline.dart` + `delivery_ack.dart` +
  `call_signaling.dart` + `messaging_stack.dart`'s wiring) rather than
  silently leaving this undiscovered or forcing it into this task's own
  fence. Not fixed here: out of `files:` (would require editing
  `inbound_pipeline.dart`/`messaging_stack.dart`, neither fenced by this
  task), and a real, separate design decision (a third dedicated
  `ControlHandler`-alternative slot, or something else) that should not
  be made silently inside an addressing bug-fix task.
- Live, on real hardware: **not achieved this session, honestly
  reported** — same environment constraint every task in this chain has
  disclosed. `adb devices -l` at dispatch time and at completion time
  both show only `e4813f500601` (Redmi 10 2022); the Pixel 8 Pro remains
  unreachable. A single device cannot exercise any two-device test. Per
  this task's own §2 point 4 ("a genuine hardware unavailability is an
  acceptable, honestly-disclosed limitation"), the full send/receive
  round trip for every fixed feature is instead proven by real
  two-`MessagingStack` integration tests over the actual mocked Pigeon
  transport boundary — the same fidelity bar this project has
  established and repeatedly accepted for this exact class of protocol
  fix (E04-B12/B13/B14).

## Carried forward from E04-B14's own review (opus, 2026-09-13)
`PrekeyExchange._handleBundleRequest` (`prekey_exchange.dart:688-723`)
replies to an inbound bundle REQUEST via `_sendControlFrame(frame.source,
...)` → `directSend`, keyed by the REQUESTER's logical `selfDeviceId` —
not a dialable Bluetooth MAC on real hardware. This is the mirror-image
of the exact bug `E04-B13` fixed for OUTBOUND requests, on the RESPONSE
leg instead. Untouched by `E04-B13`/`E04-B14` (out of both tasks' own
fences), but means the response leg of a first-contact bundle exchange
may still not complete on real devices even after this whole chain's
fixes. **This task's own §1 enumeration should confirm whether this is
already covered by its own scope, or needs to be named as its own,
narrower fix inside this task.**

**Also carried forward, a future-hazard note for whoever implements
this task**: if this task's own fix changes what value any
`ensureSession` caller captures as its own outbound `linkDeviceId` (see
`E04-B14`'s own `_ensureSessionUncoalesced` capture point), that same
change must be made consistently — `E04-B14`'s new link-binding check in
`_takeMatchingCompleter` depends on the captured value continuing to
match the link a response actually arrives on. Do not change one without
the other.

**Resolved (this session).** Confirmed real, and fixed:
`_handleBundleRequest` now receives `linkDeviceId` (threaded through
`handleControlFrame`'s existing switch, already parameterized by
E04-B14) and uses it as `_sendControlFrame`'s new `transportTarget` —
the physical dial target — while the frame's own `destination` field
stays keyed by `peerDeviceId` (`frame.source`, unchanged, since that
was already correct: `resolveOutboundDestination`'s lookup on a claimed
logical id can never match a MAC-keyed `relationships` row, so it
always fell back to the claimed id itself, which is exactly the right
`destination` value). Trust evaluation
(`_evaluateConnectionRequest`)/the E10-T05 notice are now keyed by
`linkDeviceId` too, closing a latent, related bypass: before this fix
they were keyed by the claimed logical id, which can never match a
MAC-keyed `relationships` row either, so a `blocked` peer's request
could never actually be evaluated against its real stored relationship
row.

**This did NOT change what `_ensureSessionUncoalesced` captures as
`outstanding.linkDeviceId`** (the future-hazard note above) — that
capture point is untouched; only `_handleBundleRequest`'s own reply
path changed. Proven, not just asserted:
`test_E04_B14_forged_bundle_response_on_wrong_link_is_rejected`
(pre-existing, unmodified) still passes, and this task's own
`test_E04_B15_bundle_request_reply_uses_physical_link_not_claimed_source`
explicitly asserts `responsesUnsolicited == 0` on the legitimate,
correctly-addressed reply — proving E04-B14's link-binding check still
accepts a real reply after this fix, not merely that it still rejects
a forged one.

## Deviations
- **`PrekeyExchange._sendControlFrame` gained a new optional
  `transportTarget` named parameter**, not named in this task's own §2 —
  needed once implementation revealed that the reply leg's frame
  `destination` (correctly `frame.source`, the claimed logical identity)
  and its physical transport dial (needed to be `linkDeviceId`, the
  request's real arrival link) are two DIFFERENT values, unlike every
  other call site this task fixed (where one raw MAC serves both roles).
  Reusing `_sendControlFrame` with a second, optional parameter — rather
  than duplicating its resolution/framing logic at the
  `_handleBundleRequest` call site, or forcing the reply through the
  same single-argument shape and accepting a broken transport dial —
  keeps E04-B13's own established `resolveOutboundDestination` reuse
  discipline intact. `transportTarget` defaults to `null`, so the
  outbound-request call site (`_ensureSessionUncoalesced` →
  `_sendControlFrame(peerDeviceId, ...)`, no `transportTarget`) is
  byte-for-byte unchanged.
- **`DeliveryAckService._sendAck` and `SendGroupMessageUseCase.send`'s
  per-recipient loop had their new `resolveOutboundDestination` call
  moved inside their own existing `try`/`catch`**, discovered via a real
  test failure (not by inspection alone): the full suite run together
  with `messaging_coordinator_test.dart` intermittently failed with
  `Bad state: Can't re-open a database after closing it`, traced to
  `DeliveryAckService._sendAck` — called fire-and-forget, `unawaited`,
  from `MessagingStack`'s own `inbound.delivered` subscription
  (`messaging_stack.dart:363-366`), with no catch at that call site
  either. The new `await resolveOutboundDestination(...)` line, first
  written OUTSIDE the method's existing `try` block, could throw after a
  test (or, in production, `MessagingStack.dispose()`) had already
  closed the underlying database, escaping as an unhandled async-zone
  error this method's own doc comment already promises never happens
  ("Never throws"). Moved inside the `try`, mirroring every other fixed
  call site (`call_signaling.dart`, `group_membership_service.dart`,
  `group_crypto_service.dart`, `location_share_service.dart` all already
  had their construction site naturally inside an existing `try` that
  wraps the whole send). `send_group_message_use_case.dart`'s own loop
  got the identical treatment even though its symptom was not directly
  observed failing (its own `try` only wrapped `_enqueue`) — the same
  reasoning applies: a resolution failure for one recipient must not be
  allowed to abort the whole fan-out loop, matching this file's own
  explicit "one unreachable/failing recipient must never abort another"
  contract. Full suite re-run twice after the fix, clean both times.
- **Investigated and disclosed, not fixed: `DeliveryAckService`'s and
  `CallSignaling`'s own reply-shaped transport-dial gap** — see Open
  Questions above for the full reasoning. Out of this task's own
  `files:` fence (would require editing `inbound_pipeline.dart`'s shared
  `ControlHandler` typedef or adding new dedicated slots, plus
  `messaging_stack.dart`'s wiring), and the exact blast-radius shape
  `E04-B14`'s own Deviations already investigated and declined for the
  identical reason. Recommending a follow-up task rather than silently
  fixing or silently ignoring it.

## Run log
- 2026-09-12, `claude-sonnet-5 (builder)`: re-ran the grep §1 demands
  (`grep -rn "RelayPacketFrame(" lib/`) rather than trusting this task
  file's own candidate list; confirmed 13 real construction sites
  (matching E04-B13's own reviewer's independently-confirmed count),
  read every file in full before touching it (per this task's own
  "don't force the same fix pattern blindly" instruction), and confirmed
  none of the six sibling features route through relay/fan-out in a way
  that changes the fix shape — every one of them constructs its own
  `RelayPacketFrame` directly (never delegating to `RelayEngine`/
  `ConnectionEnsuringSender` to build one), so the identical
  `resolveOutboundDestination`-on-the-`destination`-field-only pattern
  applies uniformly. Applied the fix to all six, plus the carried-forward
  `_handleBundleRequest` response-leg finding (confirmed real, root-caused,
  and fixed — see the "Carried forward" section's own Resolved note
  above for the full reasoning and why it required a new
  `_sendControlFrame` parameter rather than reusing the six-site pattern
  verbatim). Investigated, and disclosed rather than fixed, a deeper
  related gap in `DeliveryAckService`/`CallSignaling`'s own reply paths
  (see Open Questions) — genuinely out of this task's own `files:` fence.

  Wrote one real two-`MessagingStack` integration test per fixed feature
  (7 total, including the carried-forward fix), each using a
  Bluetooth-MAC-shaped id deliberately different from the peer's own
  `selfDeviceId` (E04-B13's own established convention) and asserting the
  real, observable receive-side effect — never a mock that would hide
  the defect. Found and fixed a real regression during this work (see
  Deviations): the new `resolveOutboundDestination` calls in
  `DeliveryAckService._sendAck`/`SendGroupMessageUseCase.send` needed to
  move inside their own existing `try`/`catch`, discovered via an
  intermittent cross-file test failure, not by inspection.

  **Live hardware check, at completion time:** `adb devices -l`:
  ```
  e4813f500601           device product:selene_global model:21121119SG device:selene transport_id:1
  ```
  Only the Redmi 10 2022 is reachable; the Pixel 8 Pro remains offline/
  unreachable, unchanged from every prior task in this chain. A single
  device cannot exercise a two-device test — per this task's own §2
  point 4, relied on the Dart-level integration tests described above,
  honestly disclosed rather than silently skipped.

  `flutter test`: 1520/1520 passing (full suite, run twice for
  stability — the second run specifically to confirm the async-error
  race found and fixed during this session is genuinely gone, not merely
  intermittent). `flutter analyze`: 0 issues (2 pre-existing, unrelated
  infos in files this task does not touch — the same two every prior
  task in this chain has already disclosed:
  `test/core/calls/call_migration_controller_test.dart:207`,
  `test/features/login/presentation/login_controller_enrollment_gate_test.dart:18`).

- 2026-09-12 (round 2), `claude-sonnet-5 (builder)`: addressed round 1's
  sole blocking finding (F1) — the trust-evaluation fix inside
  `_handleBundleRequest` (`evaluationKey = linkDeviceId ?? peerDeviceId`)
  closes a real blocked-peer bypass but had no regression test behind it,
  since the pre-existing `test_EARS_COMM_15_blocked_peer_gets_no_bundle`
  goes through `wireStacks`, which conflates a peer's MAC with its
  `selfDeviceId` by construction and so cannot distinguish the two
  evaluation keys. Added
  `test_E04_B15_blocked_peer_evaluated_by_physical_link_not_claimed_source`
  (`prekey_exchange_test.dart`) — same "real hardware-shaped ids" pattern
  as this task's other 7 new tests (A's Bluetooth MAC deliberately
  different from A's own `selfDeviceId`), B's relationship row for the
  block stored keyed by A's real link MAC (the only way a real row is
  ever stored), and NO row at all keyed by A's claimed logical identity.

  **Falsified per this task's own established discipline, not merely
  written and trusted:** reverted `evaluationKey` to `peerDeviceId`
  (the pre-fix behaviour) and re-ran the new test in isolation — it
  failed for the right reason (`ensureSession` resolved instead of
  timing out: the blocked peer's request was silently served a real
  bundle). Restored the fix verbatim (confirmed via `git diff` showing
  no residual change) and re-ran — green. This is exactly the reviewer's
  own probe, now committed as a permanent regression test rather than a
  reviewer-only artifact that gets deleted after the review.

  `flutter test`: 1521/1521 passing (full suite, +1 from round 1).
  `flutter analyze`: 0 issues (the same 2 pre-existing infos).

## Review
(pending dispatch)

---

### Round 1 — 2026-09-12 · `claude-opus-5` (independent reviewer, rule 5: ≠ `claude-sonnet-5` builder)

📋 **REVIEW VERDICT — CHANGES**

One required item (F1). Everything else in this diff is approve-grade and
independently verified below — this is not a diffuse "reviewer found
something" rejection: six real fixes, a correct carried-forward security
fix, and an honest disclosure record. F1 is a single missing regression
test, in a file this task already fences, protecting a security property
the task file itself claims to have closed.

- **scope:** in-contract. Product diff touches 7 files; the fence was
  corrected at implementation time and the correction is honest and
  verified (`location_share.dart` does not exist as a construction site —
  `location_share_service.dart` does; `prekey_exchange.dart` and
  `send_group_message_use_case.dart` were genuinely missing from the
  original guess-list, both squarely inside §1's enumeration mandate and
  §"Carried forward"). `## What this task does NOT do` respected: E04-B13's
  two sites are untouched (`git diff` shows no hunk in
  `messaging_stack.dart`); no new features; `resolveOutboundDestination`
  itself unmodified.
- **suite (run by the reviewer, own fresh worktree at `e3df36e`):**
  `flutter test` → **1520/1520 passed, EXIT=0**, run **twice**
  independently. No flakiness observed.
  `flutter analyze` → **2 issues, both pre-existing `info`s** in files this
  diff does not touch (`call_migration_controller_test.dart:207`,
  `login_controller_enrollment_gate_test.dart:18`). Matches the Run log.
- **design gate:** n/a (no `design_contract:`, backend-only).
- **security:** one FAIL (F1, test-coverage only — the code is correct),
  three PASS (F2, F3, F4).

---

#### F1 — ❌ REQUIRED: the blocked-peer bypass this diff closes has **no test behind it**

`prekey_exchange.dart:727` changed `_evaluateConnectionRequest(peerDeviceId)`
→ `_evaluateConnectionRequest(evaluationKey)` where
`evaluationKey = linkDeviceId ?? peerDeviceId`. The task file (§"Carried
forward", Resolved note) claims this closes "a latent, related bypass:
before this fix they were keyed by the claimed logical id, which can never
match a MAC-keyed `relationships` row either, so a `blocked` peer's request
could never actually be evaluated against its real stored relationship row."

**That claim is true — I proved it — and nothing in the committed suite
proves it.**

*Reviewer probe* (written independently, run, then deleted — not part of the
PR). Device B has BLOCKED device A, stored the only way it can be: keyed by
A's real Bluetooth MAC (`relationships.deviceId` is the transport id, PK).
A's real `selfDeviceId` differs from that MAC, as it always does on real
hardware. A calls `ensureSession(bMac)`:

```
with the fix        (evaluationKey = linkDeviceId):  refused=1  served=0   OK
reverted to pre-fix (evaluationKey = peerDeviceId):  refused=0  served=1   BYPASS
```

`served=1` is **a device the user explicitly blocked being handed a prekey
bundle** — FR-BLOCK-001 / EARS-COMM-15 defeated on real hardware. The diff
genuinely fixes it. But:

*Mutation test (reviewer-run).* Reverting **only** that one line
(`evaluationKey = linkDeviceId ?? peerDeviceId` → `peerDeviceId`) and
running `test/core/messaging/prekey_exchange_test.dart` **plus**
`test/core/notifications`: **"All tests passed!" — 79/79 green.** The
pre-existing `test_EARS_COMM_15_blocked_peer_gets_no_bundle`
(`prekey_exchange_test.dart:254`) cannot see the difference because it goes
through `wireStacks`, which conflates a peer's MAC with its `selfDeviceId`
by construction — the exact harness limitation E04-B13's own Run log
already identified as the reason that task needed a fresh test section.

This is the shape the review skill names explicitly: a test that passes on
broken code is a green light with no wiring behind it. The failure mode is
silent — a future refactor reverting this line produces zero test failures
and a blocked contact is served a bundle again.

**Required:** add one regression test to
`test/core/messaging/prekey_exchange_test.dart` (already in this task's
fence) asserting that an inbound bundle request arriving on a link whose
`relationships` row is `blocked` is **refused** (`requestsRefused == 1`,
`requestsServed == 0`) when `frame.source` claims a *different*, unblocked
logical identity. The scaffolding in this task's own
`test_E04_B15_bundle_request_reply_uses_physical_link_not_claimed_source`
is the right starting shape; the reviewer's probe above is exactly this and
took ~70 lines. Nothing else about the fix needs to change — **the code is
correct as written**; only the guard around it is missing.

---

#### F2 — OK: enumeration independently re-verified: 13/13, **no misclassification found**

`grep -rn "RelayPacketFrame(" lib/` re-run by the reviewer → 14 raw hits,
one (`messaging_stack.dart:30`) a comment, **13 real sites**. Every
classification checked against the actual code, not the description:

| site | builder's class | reviewer |
|---|---|---|
| `messaging_stack.dart:733`, `prekey_exchange.dart:871` | already fixed (B13) | confirmed |
| `delivery_ack.dart:524`, `call_signaling.dart:1059`, `group_crypto_service.dart:351`, `group_membership_service.dart:650`, `send_group_message_use_case.dart:290`, `location_share_service.dart:299` | fixed here | confirmed |
| `identity_announce.dart:171` | raw-MAC by design | confirmed — and **stronger than claimed**: `InboundPipeline._handleBuffer` checks the announce bypass at line 666, *before* `isForUs` is computed (line 697) and before the `!isForUs` relay branch (699), so `destination` is structurally never read for this controlKind. There is nothing to resolve and nothing that could consume a resolution. |
| `relay_packet_frame.dart:100` | constructor declaration | confirmed, not a call site |
| `relay_packet_frame.dart:261` | `deserialize`'s return | confirmed, decodes received bytes |
| `inbound_pipeline.dart:674`, `:777` | inbound rewrap | confirmed — both copy `destination: frame.destination` verbatim off an already-received frame, purely to strip the leading `controlKind` byte for handler dispatch. Neither is outbound; neither can be addressed. |

No genuine construction site is hiding in the "non-issue" bucket.

#### F3 — OK: all six fixes apply E04-B13's pattern correctly, and all six are load-bearing

Spot-checked **all six** (not the three required), at the level that
matters: *is the resolution key the same value the transport/routing call
uses, and is it forward-only?*

| site | resolution key | transport/routing key | same? |
|---|---|---|---|
| `delivery_ack.dart:520/536` | `peerDeviceId` | `directSend(peerDeviceId)` | yes |
| `call_signaling.dart:1055/1078,1096` | `peerDeviceId` | `computeRoute` / `enqueue` / `directSend`, all `peerDeviceId` | yes |
| `group_crypto_service.dart:346/361` | `recipientDeviceId` | `enqueue(recipientDeviceId)` | yes |
| `group_membership_service.dart:645/660` | `peerDeviceId` | `enqueue(peerDeviceId)` | yes |
| `send_group_message_use_case.dart:289/302` | `recipientId` | `_enqueue(recipientId)` | yes |
| `location_share_service.dart:294/314,321,336` | `peerDeviceId` | `computeRoute`/`enqueue`/`directSend`, all `peerDeviceId` | yes |

`grep -rn "resolveOutboundDestination" lib/` → every occurrence is a
forward call keyed by a transport id; the function itself
(`messaging_stack.dart:221`) is unmodified by this diff and is still the
single PK lookup on `relationships.deviceId`. **No reverse lookup was
introduced anywhere** — §1a option (a) still holds repo-wide.

*Mutation test (reviewer-run).* Reverted **all six** `destination:
destination` back to their raw ids simultaneously and ran the six feature
test files: **exactly 6 failures, one per feature**, no collateral, no
missed site. Restored verbatim → green. All six new tests are genuinely
load-bearing.

#### F4 — OK: the `transportTarget` fix is correct and does **not** reopen E04-B14

This was the most delicate item and it holds up under attack.

**The two values are structurally kept apart.** `_sendControlFrame`
(`prekey_exchange.dart:845-909`) resolves the frame's `destination` from
`peerDeviceId` *unconditionally* (line 858) and dials
`transportTarget ?? peerDeviceId` (line 904). `transportTarget` is never
passed to `resolveOutboundDestination`, and `destination` is never passed
to `directSend`. The only caller supplying it is `_handleBundleRequest`
(lines 749, 759), which passes `linkDeviceId` — never `frame.source`, never
the resolved destination.

**E04-B14's own property is untouched.** The hazard B14's review named
(carried-forward #3: "if a later task changes any `ensureSession` caller to
pass a logical `selfDeviceId`, `outstanding.linkDeviceId` stops matching")
is **not** triggered — `_ensureSessionUncoalesced`'s capture point
(`prekey_exchange.dart:593-594`) is byte-identical to `development`; the
diff contains no hunk there. `_takeMatchingCompleter`'s link guard is
likewise unmodified. The response leg and the request leg were changed
independently, which is exactly right.

*Both proofs reproduced by the reviewer, not taken on trust:*
- `test_E04_B14_forged_bundle_response_on_wrong_link_is_rejected`
  (pre-existing, **confirmed unmodified** in the diff) → green on this branch.
- `test_E04_B15_bundle_request_reply_uses_physical_link_not_claimed_source`
  → green, and its `responsesUnsolicited == 0` assertion is the real
  proof that B14's guard still *accepts* a legitimate reply, not merely
  that it still rejects a forgery. This is the right assertion to have
  demanded, and it is a genuinely good test: B's mock `connect`/`send`
  return `false` for any id != `aMac`, so the pre-fix code physically
  cannot get the reply off the device.
- *Mutation test (reviewer-run):* neutralised the fix
  (`transportTarget ?? peerDeviceId` → `peerDeviceId`) → **exactly one
  failure**, the new B15 test, timing out after ~21s (the reply never
  leaves B). B14's forged-response test stayed green, correctly — it
  guards a different leg. Restored verbatim → 15/15 green.

**Tried to break it, could not:**
- *`transportTarget` null* → falls back to `peerDeviceId`, i.e.
  byte-for-byte the pre-B15 behaviour. Fails **safe**: it degrades to the
  old undialable-address failure, never to something more permissive. Null
  occurs only via the provably-dead legacy generic registration (B14's
  review established `inbound_pipeline.dart:794` intercepts
  `kControlKindPrekeyExchange` before `_controlHandlers` is read — still
  true on this branch) and old direct-call tests.
- *`transportTarget` attacker-controlled?* No. It is `linkDeviceId`, traced
  by B14's review to the native Pigeon `onDataReceived(deviceId, …)`
  callback — stamped by the OS from the socket the bytes were read off,
  never derived from frame content. A remote peer cannot influence it at
  the Dart layer.
- *Does the reply go to the wrong device?* The reverse: **pre-fix it could.**
  Pre-fix the dial target was `frame.source`, attacker-chosen — a crafted
  request naming a victim's address would make this device dial the
  victim and push a bundle at them (a reflection primitive). Post-fix the
  reply can only go back down the link the request arrived on. This is a
  **strengthening**, not a weakening.
- *Does the frame's `destination` stay correct?* Yes, and the reasoning in
  the new doc comment checks out: `resolveOutboundDestination` keyed on a
  claimed logical id cannot match a MAC-keyed PK, so it falls back to
  `frame.source` — which is precisely what the original requester's own
  `isForUs` will compare against. Verified against
  `inbound_pipeline.dart:697`.

Nothing E04-B12 or E04-B14 established is weakened. **PASS.**

#### F5 — OK: the self-caught regression is genuinely fixed, not merely made rarer

`delivery_ack.dart:506-540`: the `try` now opens *before* the
`resolveOutboundDestination` await, and the catch is a bare `catch (_)`.
Everything remaining outside the `try` (lines 494-504) is synchronous and
non-throwing — `_clock()`, `DeliveryAck.serialize()`, two byte copies.
There is therefore **no await and no DB access outside the guard**, so the
"unhandled async-zone error from an `unawaited` call" mode is structurally
impossible on this path, not just less likely. The method's own
"**Never throws**" doc contract is restored intact.
`send_group_message_use_case.dart:278-303` got the same treatment, and its
placement is additionally correct for its *other* stated contract
(best-effort fan-out: one recipient's resolution failure must not abort the
loop). Both `counters.sent++` / `_enqueue` semantics are unchanged.
Corroborated empirically by two clean full-suite runs.

#### F6 — OK: the disclosed-not-fixed `DeliveryAckService`/`CallSignaling` gap is out of scope — **more so than the builder claims**

Formed independently, and I reach the same conclusion by a stronger route.

Verified the gap is real: `_sendAck`'s only two callers pass
`message.senderDeviceId` / `row.senderDeviceId`
(`delivery_ack.dart:345-346, 364`), a column `ReceiveMessageUseCase`
populates from `frame.source` — a logical `selfDeviceId`, never a MAC. So
`directSend` dials an undialable address and the ack never leaves the
device. The builder's own new test comment discloses this candidly
("proves the code is correctly WIRED … not a claim that this resolves
against a real MAC today") rather than letting the green test imply the
feature works. That is the correct disclosure discipline and I want it on
record as such.

But the builder frames the blast radius as "the same `ControlHandler`
widening E04-B14 declined". **It is larger than that.** The ack's peer id
does not come from a control handler at all — it comes from the *message*
receive path and from the `messages.senderDeviceId` column's own semantics.
Plumbing a link id there means touching `ReceiveMessageUseCase`,
`InboundPipeline.delivered`, `messaging_stack.dart`'s subscription wiring,
and arguably the meaning of `senderDeviceId`/`conversationId` (which
`handleControlFrame`'s ownership check at `delivery_ack.dart:399` also
depends on — EARS-MSG-8). That is a schema-semantics decision, not a
plumbing change, and it is rule-3 territory. **Deferral is correct**, and
the follow-up task should be filed with this larger scope, not the smaller
one the Open Question implies, or whoever picks it up will under-budget it.

#### F7 — 🟡 carried-forward (S3, NOT blocking, no fix in this task): multi-hop relay and the resolved destination

The Run log says the six features were checked and "none … route through
relay/fan-out in a way that changes the fix shape". That is true of frame
*construction*. It does not cover *routing*, and there the model does
differ from the direct-send 1:1 path this pattern was designed on.

Four of the six fixed sites hand the frame to `relayEngine.enqueue`, and
`call_signaling.dart:1083` / `location_share_service.dart:314` branch
explicitly on `route.hops.length > 1` (FR-CALL-002 / E07-T10 — multi-hop is
a *designed* path for these two, unlike a direct-neighbour ack). On the
**originating** device this is fine: `enqueue` is passed the raw MAC, and
`RelayEngine._attempt` routes on `row.destinationId`
(`relay_engine.dart:315`), never on the frame's own field. Correct.

On an **intermediate relay**, though, `inbound_pipeline.dart:756-761`
re-enqueues using `frame.destination` — which, post-fix, is the final
peer's *logical* `selfDeviceId`. `RoutingEngine.computeRoute`
(`routing_engine.dart:218`) resolves over a graph of transport link ids, so
it returns `null`, `_attempt` returns without forwarding, and the packet
sits queued until TTL. Pre-fix, `frame.destination` was a MAC and could
resolve. So the fix makes direct-neighbour delivery work and makes
multi-hop delivery structurally impossible — inverted from before.

Why this is **not** a blocker here:
1. It is inherited from E04-B13 (the 1:1 chat path has the identical
   property and shipped approved), not introduced by this task.
2. This task's §2.2 explicitly directs reusing `resolveOutboundDestination`
   rather than re-deriving per site. The builder followed the contract.
3. It is currently **latent**: `link_quality_feed.dart:1-9` documents that
   without its subscriber `RoutingEngine._knownLinks` "stays empty on a real
   device and `computeRoute()` always returns `null`" — multi-hop does not
   function today regardless of addressing.
4. Fixing it needs either the reverse lookup E04-B13 §1a forbids on security
   grounds, or a relay-header/wire-format change — a rule-3 architecture
   call that must not be made inside an addressing bug fix.

**Recommend the planner file this as its own bug task** (S3) covering the
whole mesh, not just this task's six sites, and gate it behind the
link-quality-feed work: *once the routing graph is populated, every frame
whose `destination` has been resolved to a logical id becomes unrelayable.*
Naming it now is cheap; rediscovering it after the feed lands is not.

#### F8 — live hardware: legitimately unavailable, correctly disclosed
Reviewer ran the same check: only `e4813f500601` (Redmi) attached, Pixel 8
Pro absent — identical to E04-B12/B13/B14. The substituted evidence is real
two-`MessagingStack` integration over the actual Pigeon mock boundary, with
MAC and `selfDeviceId` deliberately made different in every new test (I
confirmed this by reading each of the 7). Same bar this project has
repeatedly accepted for this protocol class. Accepted for merge; **not**
sufficient for the human to call calls/groups/location verified in practice
— and per F6, delivery acks and call replies are known to still fail on
hardware for a separate, disclosed reason.

---

#### Summary for the implementer
Land **one** test (F1) and this is an APPROVE. The enumeration is right, all
six fixes are right and provably load-bearing, the `transportTarget` fix is
right and does not weaken E04-B14 or E04-B12 in any way I could find, the
self-caught try/catch regression is genuinely closed, and the disclosure
record is the best in this chain. F7 is for the planner, not for you.

**Reviewer did not merge this PR** (explicit instruction; the merge remains
the orchestrator's action).

---

### Round 2 — 2026-09-12 · `claude-opus-5` (independent reviewer, rule 5: ≠ `claude-sonnet-5` builder)

📋 **REVIEW VERDICT — APPROVE**

F1 is closed. Re-verified on `1a76cd3` in a fresh worktree; round 1's F2–F6
and F8 findings are unchanged and not re-litigated (no product code moved —
see below). F7 remains a non-blocking planner item, now with a filing
recommendation.

- **scope:** `git diff 3d6b31b..1a76cd3 --stat` → **two files, zero product
  code**: `test/core/messaging/prekey_exchange_test.dart` (+77) and this
  task file (+40). `lib/` is byte-identical to the commit I approved
  F2–F6 against, so none of round 1's verification needed redoing. This is
  the correct shape for an F1-only response — the finding was "the code is
  right, the guard is missing", and the builder changed exactly the guard.
- **suite (run by the reviewer, own fresh worktree at `1a76cd3`):**
  `flutter test` → **1521/1521 passed, EXIT=0** (+1 vs round 1, matching
  the single added test). `flutter analyze` → **2 issues, both the same
  pre-existing `info`s** (`call_migration_controller_test.dart:207`,
  `login_controller_enrollment_gate_test.dart:18`). Builder's claim
  confirmed exactly.

#### F1 — ✅ CLOSED. The new test is accurate, sufficient, and independently falsified

`test_E04_B15_blocked_peer_evaluated_by_physical_link_not_claimed_source`
(`prekey_exchange_test.dart:978-1053`) is structurally equivalent to the
probe I built independently in round 1, and the three properties that make
it load-bearing are all genuinely present — checked by reading, not by
trusting the comment:

1. **The two evaluation keys are provably different values.** A's stack is
   `newStack('device-a-real-id', …)` while its link id is
   `aMac = 'AA:BB:CC:DD:EE:11'`. Without this the test is vacuous — it is
   exactly what `wireStacks` conflates and why
   `test_EARS_COMM_15_blocked_peer_gets_no_bundle` could not see the bug.
2. **The block is stored the only way a real one ever is**, keyed by the
   physical link: `RelationshipRepository(b.db).upsert(aMac,
   RelationshipState.blocked)`. Critically, **no row keyed by the claimed
   logical id (`device-a-real-id`) exists at all**, so the pre-fix key
   genuinely finds nothing and falls through to `unknown`.
3. **The assertions reach the real consequence, not just a counter.**
   `requestsRefused == 1`, `requestsServed == 0`, the issuable one-time
   prekey count is unchanged (no bundle actually consumed), and no session
   is established. The prekey-count assertion is a genuine addition over my
   own probe — it proves nothing was spent, not merely that a counter moved.

**Falsification reproduced by the reviewer, not taken on trust.** Reverted
`evaluationKey = linkDeviceId ?? peerDeviceId` → `peerDeviceId` and ran the
whole `prekey_exchange_test.dart` file:

```
00:04 +12 -1: test_E04_B15_blocked_peer_evaluated_by_physical_link_not_claimed_source [E]
  Expected: throws <Instance of 'TimeoutException'>
    Actual: <Instance of 'Future<void>'>
  test\core\messaging\prekey_exchange_test.dart 1029:17
00:05 +15 -1: Some tests failed.
```

**Exactly one failure, no collateral**, and it fails for the strongest
possible reason: without the fix `ensureSession` does not time out — it
*succeeds*. A blocked peer is served a real bundle and a real Signal
session is established with it. That is the FR-BLOCK-001 / EARS-COMM-15
defeat itself, surfacing end-to-end rather than as a proxy counter
assertion. Restored verbatim → `git status --porcelain` clean, file back to
16/16 green, full suite 1521/1521.

The guard now fails loudly if this line is ever reverted, which was the
entire point of F1. **Nothing further required.**

#### F7 — recommendation: file it now, as its own bug task

Reviewer's answer to the coordinator's explicit question: **file it, do not
leave it as a note.** Reasoning, in the terms `skills/bug-sweep` uses:

- It is a **cross-task seam that belongs to no task** — the exact class the
  sweep exists to catch, and exactly the shape of `L-process-008`: a
  carried-forward observation with no reader until something forces one. In
  E06 that pattern let an S1 sit unaddressed across eight tasks.
- It is **latent but scheduled to become live**: it is inert only because
  `link_quality_feed.dart:1-9` documents `RoutingEngine._knownLinks` staying
  empty on a real device today. Whoever lands that feed will make every
  resolved-destination frame unrelayable, and will have no reason to connect
  their work to an addressing fix three epics away unless a task file says so.
- It **cannot be fixed inside an addressing task** — it needs either the
  reverse lookup E04-B13 §1a forbids on security grounds, or a relay-header /
  wire-format change. That is a rule-3 foundational call that must be
  presented to the human with trade-offs, which is itself a reason it needs a
  file of its own rather than a paragraph in someone else's Review section.

Suggested shape: `type: bug`, **severity S3** (real defect, currently
unreachable, no workaround once reachable), scope the *whole mesh* rather
than this task's six sites (E04-B13's chat path has the identical property),
`depends_on` / `blocks` the link-quality-feed work, and `files:` starting at
`lib/core/messaging/inbound_pipeline.dart` (the `relayEngine.enqueue(
frame.destination, …)` call at :756-761) plus `lib/core/routing_engine/`.
**Priority stays the human's call** (🧍 `bug_priorities`) — the reviewer sets
severity only.

#### Round-2 summary
Round 1's single required item is closed with a test that is accurate,
genuinely load-bearing, and falsified by me independently of the builder's
own falsification. No product code changed between rounds, so the
enumeration (13/13, no misclassification), the six load-bearing fixes, the
`transportTarget` fix's non-weakening of E04-B14/E04-B12, the closed
try/catch regression, and the accepted hardware-unavailability disclosure
all stand as verified in round 1. Recommend `status: done`,
`review_outcome: approve`, `reviewed_by: claude-opus-5`.

**Reviewer did not merge this PR** (explicit instruction; the merge remains
the orchestrator's action).
