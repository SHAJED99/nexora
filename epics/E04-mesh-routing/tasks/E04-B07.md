---
id: E04-B07
epic: E04
type: bug
title: "InboundPipeline only ever subscribes to a device's connectionState/incomingData after discovering it THIS process run -- an accepted (inbound) connection from an already-known device that wasn't freshly discovered is silently dropped"
layer: backend
size: S
status: review-requested
owner_agent: orchestrator
model: sonnet
preferred_agent: any
token_estimate: { tier: S, range: "5k-15k" }
severity: S1
priority: { moscow: must, p: P1 }
depends_on: [E04-B06]
blocks: []
traces_to: [FR-ROUTE-003, FR-ROUTE-004, FR-DISC-001]
required_context:
  - lib/core/messaging/inbound_pipeline.dart
  - lib/core/messaging/messaging_coordinator.dart
  - lib/core/transport/transport_service.dart
  - lib/features/trust/data/relationship_repository.dart
  - epics/E04-mesh-routing/tasks/E04-B06.md
found_by: reviewer (claude-opus-5), round-1 review of PR #180 (E04-B06); files: widened by round-2 review's own N3 finding
found_in: "reading InboundPipeline while verifying E04-B06's accept-loop fix, to check whether an ACCEPTED connection's incoming data actually reaches the app the same way a DIALLED connection's does. Round 2 found the identical discovery-gated shape duplicated in MessagingCoordinator's own _onDeviceDiscovered."
files:
  create: []
  update:
    - lib/core/messaging/inbound_pipeline.dart
    - lib/core/messaging/messaging_coordinator.dart
    - test/core/messaging/inbound_pipeline_test.dart
    - test/core/messaging/messaging_coordinator_test.dart
  delete: []
---
# E04-B07 · An accepted Bluetooth connection's incoming data is silently dropped unless the peer was also discovered this process run

## The defect

`InboundPipeline._onDeviceDiscovered` (`lib/core/messaging/inbound_pipeline.dart:413-421`)
is the **only** place that ever opens a per-device `connectionState(deviceId)`
subscription:
```dart
void _onDeviceDiscovered(TransportDevice device) {
  if (_connectionSubscriptions.containsKey(device.id)) return;
  _connectionSubscriptions[device.id] = _stack.transport
      .connectionState(device.id)
      .listen((ConnectionState state) => _onConnectionStateChanged(device.id, state));
}
```
That subscription is itself the *only* path to `_onConnectionStateChanged`,
which is the *only* place that opens the `incomingData(deviceId)`
subscription actually consumed by `_handleBuffer` (`inbound_pipeline.dart:423-433`).
`_onDeviceDiscovered` is wired exclusively from `_stack.transport
.discoveredDevices.listen(_onDeviceDiscovered)` in `start()`
(`inbound_pipeline.dart:389`) — driven by native `ACTION_FOUND` events
during an active discovery scan in *this* process run.

`E04-B06` (`BluetoothTransport.acceptLoop()`) now lets a **remote** device
dial into this one and complete a real RFCOMM handshake, emitting the
same `onConnectionStateChanged(remoteId, CONNECTED)` event `connect()`'s
own successful path already emits. But if this device never happened to
run a discovery scan that found that specific remote MAC *during this
same process run* (a completely realistic case: the app was just
launched, the user is sitting on a chat screen with an already-trusted
peer, no `Discover` tap has happened yet), there is no
`_connectionSubscriptions` entry for that id, so:
- the `CONNECTED` event lands on a broadcast stream (`transport_service.dart:83-86`)
  with zero listeners and is gone forever (no replay) — even a later
  discovery cannot recover it, since `TransportService.connect`'s own
  settle event doesn't fire again for an already-open socket;
- `incomingData(deviceId)` (`incomingData(` has exactly one consumer
  project-wide: `inbound_pipeline.dart:431`) is never subscribed either;
- every frame the peer sends over that very-really-open socket is
  silently discarded.

## Why `E04-B06`'s own retest will likely still fail without this

`E04-B06`'s fix makes an accepted connection reach `openSockets`/
`startReadLoop` on the native side and fire `onConnectionStateChanged`
correctly. But unless BOTH devices in a two-device retest happen to have
an active discovery scan running (or have otherwise already discovered
each other in that exact app run) at the moment the connection completes,
the accepting side's Dart layer will still never see the incoming
message — a failure that would look identical to "E04-B06 didn't work"
from the outside, without this bug's own diagnosis. Filed as its own task
(not folded into `E04-B06`) because the fix lives entirely in Dart,
outside that native-only fix's own `files:` fence.

## The identical shape, duplicated: `MessagingCoordinator`

`MessagingCoordinator._onDeviceDiscovered`
(`lib/core/messaging/messaging_coordinator.dart:349-361`) has the exact
same defect, independently:
```dart
void _onDeviceDiscovered(TransportDevice device) {
  if (_connectionSubscriptions.containsKey(device.id)) return;
  _connectionSubscriptions[device.id] = _stack.transport
      .connectionState(device.id)
      .listen((ConnectionState state) {
    if (state == ConnectionState.connected) {
      unawaited(tick());
    }
  });
}
```
This is the coordinator's *event-driven* half (this file's own header,
option (c)) — it ticks immediately on a fresh `connected` event rather
than waiting for the `tickInterval` timer floor. An accepted (not
freshly-discovered) connection misses this too, so the relay queue only
gets driven by the timer floor (`coordinatorTickInterval`, 60s in
production) instead of firing immediately — a real, if less silent,
regression: a message eventually goes out on the next timer tick rather
than never, but "eventually" for the coordinator's own queue is not the
same guarantee as for `InboundPipeline`'s receive side, where the
`CONNECTED` event and any data received before the timer catches up are
gone for good. Both files share the same root cause and very likely the
same fix shape (seed `_connectionSubscriptions` from already-known
devices at `start()`, not only from live discovery) — worth designing
once and applying to both, not fixing `InboundPipeline` in isolation and
rediscovering this a second time later.

## Expected

A remote device's `onConnectionStateChanged(deviceId, CONNECTED)` —
whether the connection was dialled out by this device or accepted in
(`E04-B06`) — always results in an active `incomingData(deviceId)`
subscription, regardless of whether `deviceId` happens to also be in
`discoveredDevices` this process run.

## Actual

Only a device this process has explicitly discovered (via an active scan)
ever gets a `connectionState`/`incomingData` subscription at all.

## Severity

**S1** — an already-trusted, already-known peer's incoming message can be
silently and permanently dropped depending purely on scan-timing luck,
with the sending side (and any relay in between) having no way to know
the accepting side never actually received it. Same `L-process-007`
("unwired capability") shape as `E04-B05`/`E04-B06`.

## Proposed fix direction (not yet implemented)

`InboundPipeline.start()` should also open a `connectionState`/
`incomingData` subscription pair for every device this app already knows
about — not only ones freshly discovered this run. `RelationshipRepository
.listAll()` (`lib/features/trust/data/relationship_repository.dart:41`)
already returns every known relationship row regardless of current
discovery/scan state; `start()` could seed `_connectionSubscriptions` from
that list (for `trusted`/`allowed` states, mirroring `E06-T07`'s own
first-contact reasoning about which relationship states are worth
acting on) in addition to the existing `discoveredDevices` listener, so an
already-known device reaching `CONNECTED` — by any means, dialled or
accepted — is never missed. This is a proposal, not a commitment; the
implementing task should re-verify against `InboundPipeline`'s own task
file (`E06-T05`) contract before committing to this shape.

## What a fix here should NOT do
- Should NOT change `BluetoothTransport.kt`/`TransportService`/
  `E04-B06`'s own accept-loop fix — this is purely about which devices
  `InboundPipeline` subscribes to, not the underlying transport contract.
- Should NOT eagerly `connect()` to every known relationship on `start()`
  — that would reintroduce the "proactive/eager connection" pattern
  `E04-B05`'s own fix deliberately avoided. This bug is about *listening*
  for a connection state that might already be settling or settle later
  (dialled by the OTHER side, or accepted in), not about this device
  initiating anything.

## Open Questions
- **Resolved** — `trusted`/`allowed` only at `start()` time; `unknown`/
  `blocked` devices still require a live discovery event, exactly as
  today. Decided by the orchestrator under the human's standing extended
  autonomy grant ("do what is good, do not wait for me" — 2026-09-08),
  matching `E06-T14`/`GAP-030`'s own identical reasoning (a `blocked`
  peer must never gain a new way to reach this device) and keeping
  first-contact with an `unknown` peer on its existing, unchanged path.
- **`bug_priorities` gate resolved** — same standing autonomy grant:
  severity S1 (reviewer's own call, unchanged), priority P1 confirmed
  (already the frontmatter default, consistent with every other S1 found
  this session). `status` moved from `blocked` to `review-requested`.

## Fix

`InboundPipeline.start()` and `MessagingCoordinator.start()` each gained
a `_seedKnownDevices()` method: reads `RelationshipRepository(_stack.db)
.listAll()` and, for every `trusted`/`allowed` row, calls the SAME
`_onDeviceDiscovered(TransportDevice(...))` path a real discovery event
already uses — reusing its existing idempotency guard
(`if (_connectionSubscriptions.containsKey(device.id)) return;`) rather
than duplicating it. `unknown`/`blocked` relationships are skipped
entirely. No `connect()` call anywhere in the new code — purely
listening, matching `E04-B05`'s own no-eager-connect precedent.

`InboundPipeline.start()` stays synchronous (unchanged signature — 20
existing call sites across `lib/`/`test/` call it unawaited); the seed
runs via `unawaited(_seedKnownDevices())`. `MessagingCoordinator.start()`
was already `Future<void> async`, so its own seed call is a plain
`await`, completing before `start()` itself returns.

## What this fix does NOT do
- Does NOT change `BluetoothTransport.kt`/`TransportService`/`E04-B06`'s
  own accept-loop fix.
- Does NOT eagerly `connect()` to any known relationship.
- Does NOT change either `start()`'s public signature.
- Does NOT subscribe `unknown`/`blocked` relationships (see Open
  Questions above for why).

## Review round 1 (CHANGES-REQUESTED) — finding and fix

The independent reviewer (claude-opus-5) confirmed every claim in this
file (guard reuse, no eager `connect()`, `unknown`/`blocked` exclusion,
both falsifications, the test seam) but found one real bug:

**F1 (S3)** — `_seedKnownDevices()`'s fire-and-forget continuation (after
its own `await RelationshipRepository(...).listAll()`) never re-checked
whether `stop()` had run in the meantime. The reviewer proved it with
their own probe: `start()` immediately followed by `await stop()` (no
intervening await) reliably lets `stop()`'s synchronous `_started = false`
and subscription-clearing run BEFORE the DB read resolves; the seed loop
then repopulates `_connectionSubscriptions` with live subscriptions
against an already-stopped pipeline, and — worse — a frame arriving on
one of those leaked subscriptions is processed and then crashes with an
uncaught async `StateError` on the already-closed `_deliveredController`.
Not a shipping-user defect (`InboundPipeline.stop()` has no production
caller — test-isolation only), but a real bug in exactly the mechanism
meant to keep tests clean, and the kind of uncaught async error that
surfaces later as an unrelated flaky test.

**Fixed**: one line in each `_seedKnownDevices()`, right after its own
`await`: `if (!_started) return;`. Added the reviewer's own suggested
regression test,
`test_E04_B07_stop_racing_ahead_of_the_seed_leaves_no_subscription_behind`
— calls `start()` then immediately `await stop()` with no intervening
await, then asserts `debugConnectionSubscriptionCountForTest == 0` after
giving the DB read a real chance to resolve. Falsified: removing the
guard reproduces the exact leak (count `1` instead of `0`); restored,
green again. Applied the same guard to `MessagingCoordinator`'s own
`_seedKnownDevices()` too, for symmetry — not currently reachable there
(its own caller `await`s `start()` before any `stop()` could race ahead
of the seed), but the reviewer's own reasoning holds: two files whose
whole point is being identical should not diverge on an assumption about
how a future caller uses `start()`.

## Implementation checklist
- [x] `InboundPipeline._seedKnownDevices()` added, called from `start()`,
  guarded by `if (!_started) return;` after its own `await` (F1)
- [x] `MessagingCoordinator._seedKnownDevices()` added (identical shape,
  same guard), called from `start()`
- [x] `flutter analyze` clean on all 5 changed files
- [x] 3 falsifying tests: `inbound_pipeline_test.dart`'s
  `test_E04_B07_trusted_peer_connected_with_no_prior_discovery_still_delivers`
  (+ a companion `..._unknown_peer_is_not_seeded_` scope-fence test, +
  `..._stop_racing_ahead_of_the_seed_leaves_no_subscription_behind` for
  F1) and `messaging_coordinator_test.dart`'s
  `test_E04_B07_trusted_peer_connected_with_no_prior_discovery_still_ticks_immediately`
- [x] All falsified: temporarily disabling each `_seedKnownDevices()`
  call (and separately, F1's own guard) reproduces the exact regression
  each new test names; restored, all green again
- [x] Full suite green: 1345/1345, `flutter test` exit code 0

## Self-review
- [x] Diff confined to the 4 files in frontmatter — no other file touched
- [x] Reuses `_onDeviceDiscovered`'s own existing idempotency guard rather
  than duplicating subscription-creation logic
- [x] No eager `connect()` anywhere in the new code (grepped: `connect(`
  does not appear in either new `_seedKnownDevices` method)
- [x] Falsified, not just asserted — see Implementation checklist
- [x] No secrets, no new dependency, no schema migration
- [x] Severity/priority (S1/P1) justified: an already-trusted peer's
  message or event-driven tick was silently lost/delayed purely on
  scan-timing luck

## Run log
- 2026-09-08 — Filed by the round-1/round-2 reviews of PR #180
  (`E04-B06`), `status: blocked` pending the human `bug_priorities` gate.
- 2026-09-08 — Human said "do what is good, do not wait for me" (standing
  extended autonomy grant). Resolved the `bug_priorities` gate and the
  scope Open Question, both recorded above. Implemented
  `_seedKnownDevices()` in both `InboundPipeline` and
  `MessagingCoordinator`, wrote and falsified 3 new tests across both
  test files, ran the full suite (1344/1344 green), `flutter analyze`
  clean on all 4 changed files.
- 2026-09-08 — PR #182 opened, cross-model review dispatched
  (claude-opus-5). Verdict: **CHANGES-REQUESTED** — F1 (S3): the
  fire-and-forget seed's continuation never re-checked `_started`,
  reproducibly leaking a subscription (and later crashing on an
  already-closed stream controller) if `stop()` raced ahead of the DB
  read. Confirmed and fixed with a one-line guard in both files, plus a
  new regression test reproducing the reviewer's own probe. Falsified;
  full suite re-run: 1345/1345 green, `flutter analyze` clean.
