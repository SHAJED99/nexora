---
id: E04-T05
epic: E04
type: feature
title: Wire real device discovery into the Devices screen
layer: frontend
size: S
status: done
owner_agent: builder-ui
model: sonnet
preferred_agent: any
token_estimate: { tier: S, range: "15k-30k" }
priority: { moscow: must, p: P1 }
depends_on: [E04-T03b]
blocks: []
traces_to: [FR-DISC-001, FR-UI-004]
required_context: [design/screens/devices.md, epics/E02-trust-blocking/tasks/E02-T02.md, epics/E04-mesh-routing/tasks/E04-T03a.md]
design_contract: design/screens/devices.md
files:
  create: []
  update:
    - lib/features/devices/presentation/devices_controller.dart
    - test/features/devices/presentation/devices_controller_test.dart
  delete: []
started_at: 2026-08-27
completed_at: 2026-08-27
executed_by: claude-sonnet-5 (builder)
reviewed_at: 2026-08-27
reviewed_by: claude-opus-5 (reviewer)
review_outcome: approve-with-review-fixes
---
# E04-T05 · Wire real device discovery into the Devices screen

## 1. Goal
Replace the Devices screen's "Discover" no-op (built as a stub in E02-T02,
`design/gaps.md` GAP-004) with real nearby-device discovery, using T03's
`TransportService` — the screen's design/layout does not change, only what
powers the "Discover" action and the list.

## 2. Business logic
- FR-DISC-001: tapping "Discover" starts real Bluetooth discovery
  (`TransportService`'s discovery stream, T03a/T03b) instead of showing a
  "not available yet" snackbar.
- FR-UI-004: a discovered device that has no existing `Relationship` row
  shows as Unknown, per E02's existing trust-state model — this task does
  NOT invent a new state; a discovered device is evaluated through the
  same `RelationshipRepository`/`EvaluateConnectionRequestUseCase` E02
  already built.
- Discovered-but-not-yet-connected devices are additive to the existing
  relationship list, not a replacement — a device the user has already
  evaluated (trusted/blocked/allowed) keeps showing even if it's not
  currently in Bluetooth range, per the design contract's existing
  element set (no new elements this task can invent without a design gap).

## 3. What this task does
- `DevicesController.discover()`: replaces the snackbar no-op with
  `TransportService.startDiscovery()`; subscribes to
  `discoveredDevices`, and for each newly-seen device id not already in
  `relationships`, calls `EvaluateConnectionRequestUseCase` (E02) to get
  its current trust state (defaults to `unknown` for a never-seen device)
  and adds it to the displayed list.
- Stop discovery (`TransportService.stopDiscovery()`) when the controller
  is disposed (`onClose`) — don't leave a scan running after the user
  leaves the screen.
- No change to `verify()`/`block()` — those already work against
  `RelationshipRepository` per E02.

## 4. What this task does NOT do
- Does NOT change `design/screens/devices.md` or add any new UI element —
  if real discovery reveals a need for one (e.g. a "scanning..." spinner
  the current design doesn't have), log it to `design/gaps.md` and use the
  closest existing design primitive, per rule 2 (never silently invent).
- Does NOT implement `connect()`/pairing UI — discovery only, for this
  task. A discovered-but-unconnected device shows in the list as Unknown;
  actually connecting happens implicitly later (E05/E06's send flow) or in
  a follow-up task if the design calls for an explicit "connect" action —
  not decided here.
- Does NOT touch route cost/quality display — epic.md's screens table
  doesn't name a cost/quality UI element for `/devices`, and inventing one
  would be exactly the kind of silent addition rule 2 forbids. If route
  visibility (FR-UI-004's "one-tap-away" detail view) is wanted, that's a
  separate, explicitly-designed follow-up.
- Does NOT change how E02's existing relationship list loads/persists —
  `load()`'s existing behavior for already-known devices is unchanged.

## 5. Contract

### Functions
```yaml
- signature: "DevicesController.discover() -> void"
  purpose: "Starts TransportService discovery; subscribes to discoveredDevices, merges newly-seen device ids into the displayed relationships list via EvaluateConnectionRequestUseCase"

- signature: "DevicesController.onClose() -> void"
  purpose: "Stops discovery (TransportService.stopDiscovery()) — overridden GetX lifecycle hook"
```

### UI
- **Design contract:** `design/screens/devices.md` — unchanged, build
  against the existing contract, not a new mockup.
- No new route, no new states beyond what E02 already built
  (loading/empty/data are already handled).
- Gate: `make design-verify SCREEN=devices` if the Flutter-capable gate
  exists by the time this task runs (OQ-E00-3 status permitting); if not,
  manual on-device comparison against the existing golden, same as E02's
  practice.

## 6. Risks
- Real Bluetooth discovery can surface the same device repeatedly (each
  `ACTION_FOUND` per scan cycle, per T03b) — de-dup by device id before
  calling `EvaluateConnectionRequestUseCase` again for a device already in
  the list, or the list will flicker/duplicate.
- Discovery permission denial (T03b's concern) must surface as something a
  user understands, not a silently empty list forever — if
  `TransportService.startDiscovery()` fails, show whatever error affordance
  the existing design contract supports (or the closest primitive, logged
  as a gap if none fits).

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS id  `7a13e41`
- [x] `discover()` wired to real `TransportService`  `7a13e41`
- [x] de-dup logic for repeated discovery events  `7a13e41`
- [x] `onClose()` stops discovery  `7a13e41`
- [x] `flutter analyze` clean, `flutter test` green  `7a13e41`
- [ ] on-device manual check: tap Discover, confirm no crash and (if a
      second device is available) a real device appears in the list as
      Unknown — **not possible this run**: `adb devices -l` returned zero
      attached devices (not even one physical phone, let alone two) for the
      whole session; see §Run log

## 8. Acceptance criteria (EARS) + test plan
- **EARS-DEV-3**: WHEN "Discover" is tapped, the system SHALL start real
  device discovery instead of a no-op. (FR-DISC-001)
- **EARS-DEV-4**: A newly-discovered device with no prior relationship
  SHALL display as Unknown, using the existing trust evaluation. (FR-UI-004)

### Tests
- `test_EARS_DEV_3_discover_starts_transport_discovery` — mocked
  `TransportService`, assert `startDiscovery()` called
- `test_EARS_DEV_4_discovered_device_evaluated_as_unknown_by_default`
- `test_discover_deduplicates_repeated_device_events`
- `test_on_close_stops_discovery`

### Manual
1. On-device: tap Discover, confirm no crash/hang. **Not run** — no device
   was attached this session (see §Run log).
2. If a second device is reachable: confirm it appears in the list within
   a reasonable window, shown as Unknown per the existing design. **Not
   run** — same reason.

## 9. Self-review + Definition of Done
### Self-review
- [x] Every §7 item done, with commit hashes — all but the on-device manual
      check, which was not possible this session (no device attached)
- [x] `make test && make lint` green — `flutter analyze`: no issues;
      `flutter test`: 103/103 passed (full suite), including the 4 new
      discovery tests
- [x] Diff confined to `files:`; §4 respected — only
      `lib/features/devices/presentation/devices_controller.dart` and its
      test changed; no design/gaps.md, route cost/quality, or `connect()`
      code touched
- [x] Design gate green or manual comparison PASS (state which) — **n/a,
      stated**: this task changes zero UI elements/layout (design contract
      untouched, `DevicesView` not in `files:`), so `devices.md`'s existing
      contract and E02-T02's own design-verify result are unaffected; no
      new design-verify run was needed or performed
- [x] Loading/error/empty states still present (unchanged from E02) — not
      touched by this diff; `load()`/`loading`/empty-list rendering is
      unchanged

**Deviations from spec/design** (none, or list — each with its spec/ADR
reason):
- §7's on-device manual check and §8's manual test plan could not be
  executed: `adb devices -l` returned zero attached devices for the entire
  session (checked at start and again after an `adb kill-server` /
  `start-server` cycle to rule out a stale daemon) — not even one physical
  phone was reachable, so this is a harder miss than E04-T03b/T03c's
  "two transport ids, one phone" problem. This is a device-availability gap,
  not a spec/design deviation; the automated test suite (4 new tests over a
  mocked platform-channel boundary, same pattern as
  `test/core/transport/transport_service_test.dart`) is the only evidence
  available this run. Real two-device on-device verification remains
  outstanding and should be attempted the next time a genuine second
  physical device is confirmed reachable (verify via
  `adb -s <id> shell getprop ro.product.model`/`ro.serialno` before trusting
  device count, per the lesson from earlier E04 tasks).
- `DevicesController`'s constructor gained two optional named parameters
  (`transportService`, `evaluateConnectionRequestUseCase`) defaulting to
  real instances, so `DevicesBinding`'s existing two-positional-arg call
  site (outside this task's `files:` scope) keeps compiling unchanged while
  tests can inject fakes. This is an additive, backward-compatible
  constructor change, not a new API surface the task didn't authorize —
  the two new functions/behaviors themselves (`discover()` wired to
  `TransportService`, `onClose()` stopping discovery) are exactly §5's
  contract.

**Files actually touched:**
- `lib/features/devices/presentation/devices_controller.dart`
- `test/features/devices/presentation/devices_controller_test.dart`

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id —
      `test_EARS_DEV_3_discover_starts_transport_discovery`,
      `test_EARS_DEV_4_discovered_device_evaluated_as_unknown_by_default`
      (plus dedup + onClose tests beyond the two EARS ids)
- [x] UI: design gate green; extra elements trace to an approved
      `design/gaps.md` entry — n/a, no UI elements added/changed
- [x] Review APPROVE from a **different model** than `executed_by` (rule 5) —
      reviewed by claude-opus-5, implemented by claude-sonnet-5
- [ ] Squash-merged to `epic_04` branch; tracker + metrics stamped
- [ ] 🧍 Human `verified` (epic-level batches are fine)

## Open Questions
_(none)_

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- 2026-08-27: Resumed after a session-limit interruption; prior session had
  already produced the full `discover()`/`onClose()` implementation and the
  4 new tests (uncommitted). Re-verified rather than trusted: read the task
  file, `git diff` on both files, then ran `flutter analyze` (clean) and
  `flutter test` (full suite, 103/103 green, including all 4 new discovery
  tests) before touching anything further.
- Committed the pre-existing uncommitted work as `7a13e41` —
  `feat(E04-T05): wire real Bluetooth discovery into DevicesController`.
- Checked device availability for §8's manual on-device test: `adb devices
  -l` returned an empty list (no attached devices at all), confirmed again
  after `adb kill-server && adb start-server` to rule out a stale ADB
  daemon hiding a connected device. Earlier E04 tasks this session (T03b/
  T03c) hit a *different* problem — two transport ids resolving to one
  physical phone (caught via `adb -s <id> shell getprop ro.product.model`).
  This run's problem is simpler and worse: zero devices, so that
  disambiguation check wasn't even reachable. Real two-device Bluetooth
  discovery round-trip verification (§8 Manual) remains unexecuted; logged
  as a deviation above rather than silently skipped.
- Design gate: not run. This task's diff touches only
  `DevicesController` (business logic), not `DevicesView` or
  `design/screens/devices.md` — no design-verify run is meaningful here
  since no rendered element, copy, or token changed.

### Review pass — 2026-08-27, claude-opus-5 (reviewer), rule 5 satisfied
Independently re-run, not taken on the builder's word:
- `flutter analyze` → **No issues found!** (2.1s)
- `flutter test` → **103/103 green** at `71617f3` (builder's claim verified
  as real at review time), **104/104** after the review fixes below.
- Scope: `git diff --name-only epic_04...epic_04_task_05` returns exactly
  three paths — the two `files:` entries plus this task file. `git log
  epic_04..epic_04_task_05 -- design/ devices_view.dart devices_binding.dart`
  is **empty**: `design/screens/devices.md`, `DevicesView` and
  `DevicesBinding` are provably untouched. §4's fence held.
- EARS-DEV-3 → `test_EARS_DEV_3_discover_starts_transport_discovery` asserts
  a flag set inside the *mocked platform-channel handler* for
  `TransportApi.startDiscovery`, i.e. the host call genuinely crossed the
  Pigeon boundary. Real wiring, not a stub assertion.
- EARS-DEV-4 → `test_EARS_DEV_4_discovered_device_evaluated_as_unknown_by_default`
  runs a **real** `EvaluateConnectionRequestUseCase` over a real Drift
  in-memory `RelationshipRepository` with no row for the device;
  `unknown` comes out of `evaluate_connection_request_use_case.dart:38`, not
  out of a hardcoded test expectation. Verified genuine.
- `test_on_close_stops_discovery` asserts the mocked
  `TransportApi.stopDiscovery` handler actually fired — proves the call, not
  the method's existence. Genuine.
- `verify()`/`block()` (E02) are byte-identical in the diff — untouched.
- Design gate: **n/a, and the reasoning holds.** The diff contains zero
  rendered elements, copy or tokens; `DevicesView` is unchanged, so
  `devices.md`'s element/copy/token tables cannot have drifted. This is a
  correct n/a, not a skipped gate.

**Review findings — both fixed in this pass (small, in-file, in-scope):**
- 🔴 **F1 (S3, fixed) — de-dup set permanently blacklisted device ids.**
  `_discoveredIds` was added and never removed. A discovered Unknown device
  has no `Relationship` row, so the next `load()` — which **both**
  `block()` and `verify()` call — wipes it from `relationships`, after which
  the id was still in the set and the device could **never reappear** for the
  rest of the screen's life, even though real Bluetooth discovery re-announces
  it every scan cycle (T03b). This contradicts §2's "additive to the existing
  relationship list". Proven with a failing regression test first
  (`Expected: length <1> / Actual: []`), then fixed: the set is renamed
  `_evaluatingIds` and is now an **in-flight guard only**, released in a
  `finally` (so a failed evaluation cannot blacklist either), with a
  post-await re-check before `relationships.add`. Steady-state de-dup is the
  `relationships` membership check. The original §6 concurrent-re-announce
  case still passes — `test_discover_deduplicates_repeated_device_events` is
  green unchanged.
  New test: `test_rediscovery_after_load_readds_unpersisted_device`.
- 🟠 **F2 (S4, fixed) — unhandled async error out of `onClose()`.**
  `unawaited(_transportService.stopDiscovery())` does not swallow errors, it
  only silences the lint. A native `stopDiscovery` can throw (Bluetooth off
  or permission revoked mid-session, T03b), which would surface as an
  app-level zone error during teardown. Added `.catchError`, mirroring the
  handling `discover()` already had on `startDiscovery`. Also null out
  `_discoverySubscription` after cancel.
- ✅ Listener-leak check (asked for explicitly): clean. `discover()` uses
  `??=`, so repeated taps reuse one subscription; `onClose()` cancels it. No
  other stream/timer/listener is created in the diff.

**Follow-ups — out of this task's `files:` scope, correctly NOT touched here.
Epic-sweep / follow-up-task candidates, not blockers on this PR:**
- **S3** — `DevicesBinding` still calls the two-positional-arg constructor, so
  production builds a **fresh `TransportService()` per `DevicesController`**,
  and `onClose()` never `dispose()`s it. Each screen open leaks four
  broadcast `StreamController`s and re-registers the global
  `TransportEventsApi.setUp` handler for suffix `''`, silently clobbering the
  previous instance's. The right fix is injecting the app-wide
  `TransportService` from the binding — `devices_binding.dart` is outside
  `files:`, so the builder's optional-named-parameter workaround was the
  correct in-scope choice, but the binding should follow.
- **S4** — `lib/features/devices/presentation/devices_view.dart:89` still
  documents the button as "no-op — real discovery is E04's job". Stale as of
  this task; view is out of scope.

📋 **REVIEW VERDICT — APPROVE** (with the two review fixes above applied)
- scope: in-contract (2 `files:` paths + this task file; design/view/binding
  provably untouched)
- EARS: 2/2 verified, each traced to a test that exercises real wiring
- suite: pass — `flutter analyze` clean, `flutter test` 104/104
- design gate: n/a — correctly n/a (zero rendered elements changed), reasoning
  audited and accepted
- security: n/a — no auth/payment/RBAC/money path in this diff
- 🧍 **Outstanding human gate — on-device verification.** See the cumulative
  note below; NOT signed by the reviewer.

**🧍 Cumulative on-device-verification debt across E04 — reviewer's judgment**
This is the third consecutive E04 task shipped on mocked-boundary evidence
alone, and the trend is the wrong way:
- T03b — real Bluetooth Classic discovery + connect: one phone reachable, two
  transport ids resolving to it; no true two-device round trip.
- T03c — same single-phone constraint.
- T05 — **zero** devices attached all session (`adb devices -l` empty,
  re-confirmed after an `adb kill-server`/`start-server` cycle).

Each task's own disclosure is honest and each automated suite is genuinely
green, so none of them is individually a blocker. But the *union* is: the
entire real-radio surface of E04 — permission prompts, `ACTION_FOUND` repeat
cadence, discovery→connect timing, `stopDiscovery` behaviour when Bluetooth is
toggled off — has **never executed against real hardware**. Every green test in
this epic mocks precisely the boundary the epic exists to cross. F1 above is
the concrete proof that this class of gap is real and reachable by review, not
theoretical: a genuine list-lifecycle defect that no mocked test in the
delivered set caught.

**Reviewer's position: E04 must NOT be declared epic-complete on mocked
evidence.** A human physical-device pass — two real phones, both confirmed
distinct via `adb -s <id> shell getprop ro.serialno` before trusting the
device count — is required before the E04 → `development` gate, and should be
scoped to at minimum: T03b's discover+connect round trip, T05's Discover tap
(no crash, a real peer appears as Unknown), and the Bluetooth-off /
permission-denied paths. Recommend the orchestrator carry this as an explicit
open item into the E04 bug sweep and the human gate rather than letting three
separate "device wasn't available" deviations net out to zero.

Reviewer did not merge to any branch and did not sign any 🧍 human-gate line.
