---
id: E04-B28
epic: E04
type: bug
title: "The native Android transport never emits onDeviceLost, so TransportService.lostDevices never fires on a real device -- a peer that leaves stays 'nearby' forever and E04-B27's stale-latency fix is inert on hardware"
layer: backend
size: S
status: done
owner_agent: builder
model: opus
preferred_agent: any
token_estimate: { tier: S, range: "15k-40k" }
severity: S3
priority: { moscow: should, p: P3 }
found_in: "E04-B27 cross-model review F1 (claude-sonnet-5), 2026-09-15"
depends_on: [E04-B27]
blocks: []
traces_to: [FR-UI-004, FR-DISC-001]
required_context:
  - epics/E04-mesh-routing/tasks/E04-B27.md
  - android/app/src/main/kotlin/com/nexora/nexora/transport/BluetoothTransport.kt
  - pigeons/transport.dart
design_contract: n/a
files:
  create:
    - epics/E04-mesh-routing/tasks/E04-B28.md
  update:
    - android/app/src/main/kotlin/com/nexora/nexora/transport/BluetoothTransport.kt
    - epics/E04-mesh-routing/tracker.md
started_at: 2026-09-15
completed_at: 2026-09-15
executed_by: "orchestrator session (claude-opus-5) -- reviewer must NOT be opus"
reviewed_at: 2026-09-15
reviewed_by: "claude-sonnet-5 (cross-model, rule 5; 2 rounds)"
review_outcome: approve_with_nits
repro:
  - "grep the android tree: `onDeviceLost` appears only in the generated `TransportApi.g.kt`; neither `BluetoothTransport.kt` nor `LoopbackTransport.kt` calls it (E04-B27 review F1)."
  - "Live, Pixel 8 Pro, 2026-09-15: the Dashboard showed 'No peers nearby' next to 'Latency 1ms' long after the peer's connection ended."
expected: "When a previously discovered peer is no longer found by a later scan, the transport reports it lost, so the Dashboard's and Network Settings' 'nearby' state and E04-B27's latency clearing actually work on a device."
actual: "`lostDevices` never fires; the Dart consumers (Dashboard, Network Settings) and their tests are correct but inert on hardware."
---
# E04-B28 · Emit onDeviceLost from the native transport

## 1. Mechanism

- **Tracking:** `BluetoothTransport` keeps the resolved ids each scan confirmed (`emitDiscoveredDevice`), plus the set from the previous completed scan.
- **Trigger:** `ACTION_DISCOVERY_FINISHED` was registered but never handled; it now runs `handleDiscoveryFinished`.
- **Grace period:** it waits `SDP_LOOKUP_TIMEOUT_MS`, because E04-T06's SDP confirmation can arrive after the inquiry ends.
- **Superseded scans:** it does nothing if a newer scan has started.
- **Emit:** otherwise it emits `onDeviceLost(id)` for every id the previous scan confirmed and this one did not.
- **Own cancels ignored** (review F1): Android also broadcasts `ACTION_DISCOVERY_FINISHED` when a scan is cancelled. This file cancels discovery itself in several places: `stopDiscovery`, the restart in `doStartDiscovery`, before `createBond`, before connecting, and after bonding. All of them now go through `cancelDiscoveryQuietly`, which marks the next finish as cancellation-caused (only if a scan was actually running), so a truncated scan is never diffed.

Only scan results take part. A peer reached through an accepted connection is never reported lost by this path.

## What this task does NOT do

- Does not change the Pigeon schema (`onDeviceLost` already exists) or any Dart code. `TransportService`, Dashboard and Network Settings already consume `lostDevices`.
- Does not report a peer lost when its RFCOMM connection drops. Connection state already travels via `onConnectionStateChanged`.
- Does not add a Kotlin test harness (none exists in this project).
- Does not touch E04-T07's UUID filter (a separate, parallel branch; no overlap in the edited regions).

## 2. Definition of Done

- [x] `onDeviceLost` is emitted on scan completion for ids the previous scan confirmed and this one did not
- [x] `flutter build apk --debug` compiles clean (executor, and the reviewer via `gradlew :app:compileDebugKotlin`)
- [ ] Live: a peer found in scan 1 and absent from scan 2 is reported lost, and the Dashboard stops listing it as nearby and clears its latency
- [x] Independent review (claude-sonnet-5, round 1 CHANGES REQUESTED, round 2 APPROVE WITH NITS, PR #266)

## Open Questions

- OQ-E04-B28-1: a device that is simply not discoverable during the second scan (e.g. its 120 s visibility window expired) is reported lost even though it is still in range. This matches what a scan can observe; its link-quality events (E04-B27) re-add it as soon as a live link reports.

- OQ-E04-B28-2: a scan cancelled by ANOTHER app (or the system) is indistinguishable from a completed one, so its partial result set is still diffed. Only this app's own cancels are excluded.

- OQ-E04-B28-3 (review round 2, S4, accepted): if Android coalesces `doStartDiscovery`'s immediate cancel-then-restart into a single finish broadcast, that broadcast is consumed as the cancel's, and the new scan's lost-diff is deferred to the following scan. Effect: a peer that left can stay "nearby" for one extra scan cycle. Self-healing; nothing is ever wrongly reported lost.

## Run log

- 2026-09-15: filed from E04-B27's review and implemented directly.
- 2026-09-15 — Review round 1 (claude-sonnet-5): CHANGES REQUESTED.
  - F1 (S3): a cancelled scan also fires `ACTION_DISCOVERY_FINISHED`, and this file's own `cancelDiscovery()` calls did not bump the scan generation. Tapping one peer to connect mid-scan would report other peers lost. Fixed with `cancelDiscoveryQuietly` plus a consume-once flag checked at the top of `handleDiscoveryFinished`.
  - The missing tracker row was added.
- 2026-09-15 — Review round 2 (claude-sonnet-5): APPROVE WITH NITS.
  - Nit 1: a failed `cancelDiscovery()` would have left the flag set. Fixed by clearing it when the cancel returns false, and the comment's "guaranteed" wording was corrected.
  - Nit 2: recorded as OQ-E04-B28-3.
  - Live verification remains open: the phones are currently unpaired (see E04-B10).
- 2026-09-15 — Live attempt: INCONCLUSIVE.
  - Setup: two Pixel 8 Pro Discover scans (05:20:20 with the Redmi 10 2022 discoverable; 05:23:31 intended as not discoverable), then the Pixel Dashboard checked after each.
  - Result: after both scans the Dashboard read "No peers nearby".
  - Why it tells us nothing about lost-events: the Redmi was never counted as discovered in scan 1, so there was nothing to report lost. Pixel logcat shows the cause: Android answered Nexora's `fetchUuidsWithSdp()` with the Redmi's stale cached UUIDs first (`ACTION_UUID ... count: 11`, 05:17:34 in the same session), and `handleUuidResult` consumed the pending check on that answer. That is filed and fixed as E04-B31.
  - Second confound: MIUI kept the Redmi `SCAN_MODE_CONNECTABLE_DISCOVERABLE` past its 120 s window, so scan 2 was not a clean "peer gone" either.
  - The live DoD box stays open until it is re-run on a build with E04-B31.
