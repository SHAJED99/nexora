---
id: E09-B08
epic: E09
type: bug
title: "No reactive trigger for the location privacy sweep — a blocked peer's stored coordinate survives until the app next restarts, not immediately"
layer: backend
size: S
status: blocked
owner_agent: builder
model: any
preferred_agent: any
token_estimate: { tier: S, range: "15k-30k" }
severity: S3
priority: { moscow: should, p: P3 }
depends_on: [E09-B06]
blocks: []
traces_to: [FR-LOC-004, FR-LOC-003]
required_context:
  - epics/E09-location-sharing/tasks/E09-B06.md
  - epics/E09-location-sharing/tasks/E09-B02.md
  - lib/features/trust/data/relationship_repository.dart
  - lib/core/messaging/messaging_stack.dart
found_by: reviewer (independent, cross-model), mandatory follow-up condition attached to round-1 APPROVE of PR #49 (E09-B06)
found_in: "E09-B06's own stop condition — a table-wide relationship-change stream did not exist and adding one was outside that bug's fence. The reviewer required this be tracked as its own bug rather than left as an Open Question in a closed file (AGENTS.md rule 8, L-process-007 — the exact pattern that created E09-B06 in the first place)."
files:
  create: []
  update:
    - lib/features/trust/data/relationship_repository.dart
    - lib/core/messaging/messaging_stack.dart
    - test/features/trust/data/relationship_repository_test.dart
    - test/core/messaging/messaging_stack_test.dart
  delete: []
---
# E09-B08 · the privacy sweep only runs at startup, not on the block itself

## The defect

`E09-B06` wired `LocationShareService.pruneFixesForNonVisiblePeers()` to run
once, at `MessagingStack.create()`. That closes `E09-B02`'s original S2
finding down to: every *observable* surface (`LocationReadModel.read()`/
`watch()`) already re-evaluates the visibility policy live and correctly
reports `LocationUnavailable` for a blocked peer from the instant of the
block — but the underlying row in `location_fixes` is not physically
deleted until the app next restarts.

`RelationshipRepository` (`lib/features/trust/data/relationship_repository.dart`)
exposes only a per-peer `watchState(deviceId)` stream (`E09-B01`), not a
table-wide "any relationship changed" stream — so `E09-B06` had no in-fence
way to trigger the sweep reactively, and stopped rather than expanding
scope into E02's file.

## Expected
A blocked peer's stored coordinate is deleted promptly, not deferred to the
next process start.

## Actual
The row survives on local disk (plaintext SQLite, per E03's still-unowned
carry-forward on at-rest encryption) for the remainder of the app session
after a block, readable by anything with device storage access. Not
readable through this app's own UI/API by that point (E09-B01/B06 already
close that surface), but present on disk.

## Severity
**S3** — real, but bounded and workaround-able. The reviewer's own
assessment (PR #49 round 1): "bounded by process lifetime, workaround
exists" (restarting the app). Not S2: no live surface in the app can
observe or transmit the stale row; it is purely a data-at-rest residue.

## Fix direction
Add a table-wide watch stream to `RelationshipRepository` — e.g.
`Stream<void> watchAnyChange()` or similar, mirroring `watchState`'s
`watchSingleOrNull()`-based emit-on-listen shape but over
`select(relationships).watch()` (any row, any column) rather than one row.
Subscribe to it in `MessagingStack` and call
`pruneFixesForNonVisiblePeers()` on each emission (debounced/coalesced if a
bulk import or multi-row change would otherwise fire the sweep once per
row — check whether `RelationshipRepository.upsert` is ever called in a
loop before assuming this needs guarding).

## What this fix does NOT do
- Does **not** change `pruneFixesForNonVisiblePeers()`'s own logic — already
  correct and tested (`E09-B02`).
- Does **not** add a `Timer` or poll — event-driven off the new stream only.
- Does **not** touch `LocationReadModel`/`LocationVisibilityPolicy` — the
  live-read gate is already correct and unaffected by this bug.

## Regression test (write it first)
`test_EARS_LOC_5_blocking_a_peer_deletes_their_fix_immediately_not_at_next_launch`
— seed a visible peer's fix, construct `MessagingStack` once, THEN block the
peer via `RelationshipRepository.upsert(...)` without restarting the stack,
and assert the row is gone shortly after (await the sweep's own completion
signal or a short delay), with no second `MessagingStack.create()` call in
the test.

## Open Questions
- **OQ-E09-B08-1** (2026-09-07): Fix direction (a) — a plain `Stream`/
  `StreamController` `upsert` pushes into directly, sidestepping Drift's
  reactive-query machinery — has now been implemented and falsified. It
  does not avoid the hang this bug's own Run log first found; it only
  changes which widget test trips over it (`dashboard_view_test.dart`'s
  `test_dashboard_view_matches_contract_elements` this time, not the
  original four), for a materially different reason: bisection (see this
  session's Run log entry) proved the hang is triggered by ANY unawaited,
  real, uncontrolled query against this codebase's `NativeDatabase`-backed
  `AppDatabase` fired synchronously from a reactive callback before
  `tester.pumpWidget`/`pumpAndSettle` — not by Drift's `.watch()`
  specifically, and not by the plain-stream subscription itself (a
  no-op-callback variant does not hang). What would actually unblock this:
  (b) confirmed, deeper `flutter_test`/`drift`/`NativeDatabase` internals
  knowledge on whether/how a widget test exercising a reactively-triggered
  DB write can safely cross a `pumpAndSettle` boundary (e.g. `tester
  .runAsync()` wrapping, and whether that would need to be added to EVERY
  widget test that constructs a real `MessagingStack` — a blast radius
  outside this bug's current `files:` fence), or (c) a redesign where the
  privacy sweep is NOT fire-and-forget — e.g. `RelationshipRepository
  .upsert` itself awaits the sweep's completion (or some other synchronous
  coupling) rather than triggering it via an unawaited listener callback —
  which is a different shape than this bug's fix direction describes and
  would need its own sign-off before an agent builds it, since it changes
  where responsibility for running the sweep sits (arguably a layering
  question: should `relationship_repository.dart`, a `trust` data file,
  know anything about the `location` feature's sweep at all, even
  indirectly through a generic notification stream it's already carrying).
  Not a rule-3 foundational decision on its own, but genuinely beyond what
  this session can resolve by continuing to guess at implementation
  shapes — surfaced here rather than forced through a third attempt.

## Feedback log
- 2026-09-04 — Priority set at the `bug_priorities` gate under the decision
  authority explicitly delegated to the agent for this session: **P3 —
  should, not blocking `epic_09` → `development`.** Per the reviewer's own
  advice on PR #49: the residual risk after E09-B06 is data-at-rest only,
  bounded by process lifetime, with a user-performable workaround. Not
  worth holding the epic gate on. Severity is the reviewer's and is **S3**.

## Run log
- 2026-09-04 — Attempted the fix direction exactly as specified: added
  `RelationshipRepository.watchAnyChange()` (a table-wide `Stream<void>`
  over `.select(relationships).watch()`, mirroring `watchState`'s shape)
  and subscribed to it in `MessagingStack.create()`, re-running
  `pruneFixesForNonVisiblePeers()` on each emission. Both regression tests
  named in this bug file passed
  (`test_EARS_LOC_5_blocking_a_peer_deletes_their_fix_immediately_not_at_next_launch`
  in `messaging_stack_test.dart`, plus two direct `watchAnyChange` unit
  tests in `relationship_repository_test.dart`) — confirmed failing
  without the fix (fix survived on disk after a same-session block) and
  passing with it.
- **Discovered while running the full suite: this fix hangs unrelated
  widget tests.** `flutter test` (no filter) stalled indefinitely; bisected
  to `test/features/chat/presentation/chat_view_test.dart`,
  `conversations_view_test.dart`, `conversations_groups_test.dart`, and
  `dashboard_view_test.dart` — each timed out after the runner's own
  10-minute cap on `tester.pumpAndSettle()`. Root-caused with a minimal
  repro: a widget test that constructs a real `MessagingStack` (all four
  above do, via GetX bindings) hangs in `pumpAndSettle()` the moment
  `MessagingStack.create()` holds a **live Drift `.watch()` subscription**
  against `NativeDatabase.memory()` — confirmed by reducing the
  subscription's callback to a no-op `(_) {}` and reproducing the exact
  same hang, so this is not this bug's own callback logic (verified
  `pruneFixesForNonVisiblePeers()` only reads `relationships`/
  `location_fixes` and writes `location_fixes` — no write-back to
  `relationships` that could explain a self-triggering loop). Also tried
  cancelling the subscription explicitly in `MessagingStack.dispose()`
  (rather than relying on `db.close()` alone) — did not help, because the
  hang occurs **inside** the test body's own `pumpAndSettle()` call, before
  `tearDown`/`dispose()` is ever reached.
- **Reverted.** All production and test changes rolled back to
  `origin/epic_09`'s baseline (verified: `git status --short` clean,
  `chat_view_test.dart`'s previously-hanging test passes in <1s on the
  reverted tree). Shipping a fix for a P3, bounded, workaround-able
  data-at-rest issue at the cost of hanging an unbounded number of
  present and future widget tests is not a trade this session's delegated
  authority covers — this is now a real technical obstacle, not a scoped
  implementation choice.
- **Re-blocked, escalated.** This needs one of: (a) a different reactive
  trigger that isn't a live Drift `.watch()` stream held open across a
  widget test's lifetime (e.g. a plain `Stream`/`StreamController` the
  block use case pushes into directly, sidestepping Drift's reactive query
  machinery entirely — `RelationshipRepository.upsert`'s two callers are
  known and few), or (b) confirmation from someone with deeper
  Drift/`flutter_test` internals knowledge that a specific stream-teardown
  order avoids the hang (this session tried the obvious one — cancel in
  `dispose()` — and it didn't). Neither is a rule-3 foundational decision;
  this is squarely re-scoping an implementation approach, but it is beyond
  what this session was able to root-cause further without risking the
  same "ship something that looks done" mistake `L-process-015`-adjacent
  lessons warn against. Left `status: blocked`, not `done` and not
  silently reverted-and-forgotten — this Run log entry is the record.

- 2026-09-07 — Attempted fix direction (a) exactly as the prior Run log
  entry specified: `RelationshipRepository.watchAnyChange()` rebuilt as a
  PLAIN `Stream<void>` — a broadcast `StreamController` per `AppDatabase`
  instance (keyed by `Expando`, since `MessagingStack` and every
  `upsert`-caller construct their own separate `RelationshipRepository(db)`
  wrapper around the SAME shared `db` — an instance-level controller would
  have silently missed cross-instance notifications), pushed into directly
  by `upsert()` after its write completes, with ZERO Drift reactive-query
  machinery anywhere in it. Subscribed in `MessagingStack`'s constructor,
  cancelled first (before `coordinator.stop()`/`db.close()`) in `dispose()`
  — an explicit, bounded lifecycle, exactly as this task's own instructions
  asked for.
  - Both regression tests passed on the first try: six new `watchAnyChange`
    unit tests in `relationship_repository_test.dart` (including one
    proving the notification is shared across DIFFERENT
    `RelationshipRepository` instances on the same `db`, and one proving no
    cross-`AppDatabase` leakage), and
    `test_EARS_LOC_5_blocking_a_peer_deletes_their_fix_immediately_not_at_next_launch`
    in `messaging_stack_test.dart` — confirmed RED on the pre-fix code
    (5s timeout, the fix survives on disk after a same-session block) and
    GREEN with the fix (polled-and-confirmed-null within milliseconds).
  - Per this task's own instruction to check the four traditionally-affected
    widget tests BEFORE the full suite: **reproduced a hang again**, but in
    a DIFFERENT test than before —
    `dashboard_view_test.dart:test_dashboard_view_matches_contract_elements`
    (`pumpAndSettle timed out`, then every subsequent test in that file
    `did not complete`). Bisected with three targeted variants, each run in
    isolation against that one test:
    1. Subscription present, listener callback replaced with a true no-op
       (`(_) {}`) — **passes fast** (< 1s). Rules out "merely holding a
       live subscription open" (the plain-`StreamController` shape itself)
       as the cause — this is NOT a repeat of the original Drift-`.watch()`
       "never completes/closes" failure mode.
    2. Subscription present, listener callback replaced with
       `unawaited(Future<void>.delayed(const Duration(milliseconds: 1)))`
       (an unrelated real Future, touching nothing) — **passes fast**.
       Rules out "any unawaited Future kicked off synchronously during
       `upsert`, before `pumpWidget`" as sufficient on its own.
    3. Subscription present, listener callback replaced with
       `unawaited(db.select(db.locationFixes).get())` — a trivial, one-shot,
       real query against the SAME `NativeDatabase.memory()` the widget
       test's `MessagingStack` already owns, fired unawaited, still before
       `pumpWidget` — **reproduces the exact hang**, byte-for-byte the same
       `pumpAndSettle timed out` signature.
  - **Root cause, more precisely than the prior attempt could state it**:
    this is NOT specific to Drift's `.select(...).watch()` reactive-query
    machinery (this task's own working theory when it was re-blocked). It
    is that ANY real, uncontrolled async query against this codebase's
    `NativeDatabase.memory()`-backed `AppDatabase`, fired-and-forgotten
    (`unawaited`) from a callback that runs synchronously inside a widget
    test body BEFORE `tester.pumpWidget`/`pumpAndSettle`, leaves that query
    still in flight when `pumpAndSettle` starts pumping frames — and
    `pumpAndSettle` then hangs, apparently never observing settlement while
    that database round-trip is still outstanding. A one-shot `.get()` with
    an empty result set (there are zero `location_fixes` rows in this test)
    reproduces it just as reliably as `pruneFixesForNonVisiblePeers()`'s
    full logic did — the actual query content is irrelevant; only
    "unawaited + touches the real database + still pending when pumping
    starts" matters. This reads as a `flutter_test`/`NativeDatabase`
    interaction hazard (most likely: the query's completion depends on a
    real asynchronous hop — an isolate message or FFI callback posted back
    through the platform's real event loop — that the test zone's
    synchronous pump/settle loop does not properly wait for or yield to,
    the same class of problem `tester.runAsync()` exists to work around,
    though this session did not attempt that fix here since it would live
    in the TEST file, not in production code, and does not change the
    underlying production-code hazard: this reactive-trigger design fires
    exactly this kind of unawaited real DB query from inside `upsert()`'s
    synchronous call stack, in production as much as in a test).
  - **Reverted again**, this attempt fully: `relationship_repository.dart`,
    `messaging_stack.dart`, and both test files rolled back to
    `origin/development`'s baseline. Verified clean (`git status --short`
    empty) and verified `dashboard_view_test.dart`'s previously-hanging test
    passes in <1s on the reverted tree.
  - **Re-blocked again.** Fix direction (a), as specified, is now falsified
    — it does not avoid the hang, it just moves which widget test trips
    over it, for a materially different underlying reason than originally
    diagnosed. This narrows what would actually unblock this bug: NOT "swap
    Drift's `.watch()` for a plain `Stream`" (already tried, insufficient)
    but either (b) from the prior entry — real `flutter_test`/`drift`
    internals knowledge on how to let a widget test's `MessagingStack`
    perform unawaited real DB work safely across a `pumpAndSettle` boundary
    (e.g. whether every such widget test needs `tester.runAsync()` wrapping
    around any code path that can trigger one, which is a wider blast
    radius than this bug's own `files:` fence covers) — or (c), a new
    option this session's evidence opens up: making the sweep NOT
    fire-and-forget at all, i.e. having something await its completion
    synchronously as part of the same call that changes the relationship,
    which is a materially different design (coupling `upsert()`'s own
    Future to the sweep's completion, or restructuring where the trigger
    lives) that this bug's current `files:` fence and fix direction do not
    describe, and this session is not authorizing itself to improvise past
    that. Left `status: blocked` — not `done`, not silently
    reverted-and-forgotten. No PR opened; this Run log entry and
    `OQ-E09-B08-1` above are the record.

## Handoff
- **Tried**: fix direction (a) from the prior Run log entry, implemented
  exactly as specified (plain broadcast `Stream<void>` via
  `RelationshipRepository.watchAnyChange()`, `Expando`-keyed by
  `AppDatabase` so every wrapper instance around the same `db` observes it,
  pushed from `upsert()` after its write, subscribed and explicitly
  cancelled in `MessagingStack`). Both this bug's regression tests passed
  clean, first try.
- **Why blocked**: the four traditionally-affected widget tests were run
  BEFORE the full suite, per this task's own instruction, and one
  (`dashboard_view_test.dart`) still hung — bisected to a root cause
  broader than Drift's `.watch()`: ANY unawaited real query against the
  test's `NativeDatabase`, fired synchronously before `pumpWidget`, hangs
  `pumpAndSettle` regardless of what triggers it or what it queries. See
  `OQ-E09-B08-1` for the full bisection evidence.
- **What would unblock**: either confirmed `flutter_test`/`drift`
  internals guidance on crossing a `pumpAndSettle` boundary safely with
  in-flight real DB work (option (b)), or a redesign that makes the sweep
  synchronous with `upsert` rather than fire-and-forget (option (c),
  outside this bug's current fix direction and `files:` fence — needs its
  own sign-off before an agent builds it).
- **Suggested next**: route back through the planner/reviewer to pick
  between (b) and (c) (or accept the residual risk and close this as
  `wontfix` given its own S3/P3 severity/priority — a live option this
  session is not authorized to choose unilaterally) before a third
  implementation attempt. Whoever picks this up next should read this
  Run log's three-variant bisection first — it rules out two plausible
  guesses (subscription lifetime, unawaited-Future-in-general) that would
  otherwise cost another cycle to re-discover.
