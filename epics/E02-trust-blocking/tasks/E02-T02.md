---
id: E02-T02
epic: E02
type: feature
title: Devices screen — real relationship list, trust/block actions
layer: frontend
size: M
status: todo
owner_agent: builder-ui
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: must, p: P2 }
depends_on: [E02-T01]
blocks: []
traces_to: [FR-TRUST-003, FR-TRUST-004, FR-TRUST-005, FR-BLOCK-001]
required_context: [design/screens/devices.md, docs/routes.md, docs/conventions.md]
design_contract: design/screens/devices.md
files:
  create:
    - lib/features/devices/presentation/devices_view.dart
    - lib/features/devices/presentation/devices_controller.dart
    - lib/features/devices/presentation/devices_binding.dart
  update:
    - lib/app/routes.dart
    - lib/app/bindings.dart
    - docs/routes.md
  delete: []
started_at:
completed_at:
executed_by:
reviewed_at:
reviewed_by:
review_outcome:
---
# E02-T02 · Devices screen — real relationship list, trust/block actions

## 1. Goal
Build the real `/devices` screen against its design contract, wired to
E02-T01's relationship data — the first screen where a user actually sees
and acts on trust state.

## 2. Business logic
- Renders each known relationship with its real state (Trusted/Allowed/
  Unknown/Blocked) — matching the contract's four example rows'
  states/icons/colors exactly (design/screens/devices.md elements 13-14,
  21-22, 29-30, 37-38).
- The "Verify" button (contract element 31, shown only on Unknown-state
  rows) and the kebab (`more_vert`) menu on other rows are the entry
  points into E02-T01's `BlockUseCase` / trust actions.
- Bottom nav (Dashboard/Conversations/Devices/Settings) — Devices is the
  active tab; other tabs route to their existing/future screens per
  `docs/routes.md`.

## 3. What this task does
- Real `/devices` route, screen, controller, GetX binding.
- Reads relationships via `RelationshipRepository` (E02-T01) — no live
  device discovery yet (E04 doesn't exist), so the list is whatever
  relationships already exist locally (empty state if none).
- Kebab menu action: "Block" (calls `BlockUseCase`) at minimum — matching
  what's structurally supported by the contract's generic `more_vert`
  button without inventing new menu items the design doesn't show.
- "Verify" button on Unknown rows: promotes the relationship to Allowed
  (the only state transition FR-TRUST-006's not-yet-built config would
  otherwise gate — safe minimal behavior, logged as a deviation if it
  turns out to need more).
- The "Discover" button (contract element 6) is present and tappable but
  intentionally a no-op/disabled-with-tooltip for now — real discovery is
  E04's job. This is a real gap, logged below and in `design/gaps.md`.

## 4. What this task does NOT do
- Does NOT implement real device discovery (E04).
- Does NOT implement the group-invisibility behavior (E07/no groups yet).
- Does NOT touch the Settings screen (E02-T03).
- Does NOT add menu items or states the design contract doesn't show —
  if a real trust workflow needs more than Block/Verify, that's a new
  `design/gaps.md` entry for human approval, not an invented button.

## 5. Contract

### UI
- **Design contract:** `design/screens/devices.md` — build from this
- **Route:** `/devices`, added to `docs/routes.md` (already scaffolded as
  "to be filled by owning feature epics" in genesis T04)
- **States required:** populated list (contract's default state), empty
  state (no relationships yet — not shown in the contract, derived per
  rule 2: reuse the contract's existing empty-space/typography, no new
  visual language; log in `design/gaps.md`)
- Gate: `make design-verify SCREEN=devices` — **NOTE:** OQ-E00-3 (no
  Flutter-capable design-fidelity gate exists yet) means this can't
  actually run yet. This task's own first sub-step should attempt wiring
  it (see Risks) since E06's task file already flagged this epic as the
  first candidate to close that gap.

## 6. Risks
- OQ-E00-3: the design-fidelity gate can't verify a compiled Flutter app.
  If closing that gap turns out to be more than a small addition, don't
  let it balloon this task — do a manual side-by-side check against the
  golden screenshot (`design/golden/devices/default@390x844/page.png`)
  and note the gate is still open, rather than scope-creeping into
  building the whole gate here.

## 7. Implementation checklist
- [ ] tests written FIRST and failing, named by EARS id
- [ ] `/devices` route + screen + controller + binding
- [ ] relationship list rendering (Trusted/Allowed/Unknown/Blocked visuals matching contract)
- [ ] Block action wired to `BlockUseCase`
- [ ] Verify action wired (Unknown → Allowed)
- [ ] empty state (no relationships) — logged in `design/gaps.md`
- [ ] `flutter analyze` clean, `flutter test` green
- [ ] manual on-device check against the golden screenshot

## 8. Acceptance criteria (EARS) + test plan
- **EARS-DEV-1**: The Devices screen SHALL render each relationship's real state, matching the design contract's visual language for Trusted/Allowed/Unknown/Blocked. (FR-TRUST-003)
- **EARS-DEV-2**: WHEN a user taps Block on a device, the system SHALL call `BlockUseCase` and reflect the Blocked state immediately. (FR-BLOCK-001)

### Tests
- `test_EARS_DEV_1_renders_relationship_states`
- `test_EARS_DEV_2_block_action_updates_state`

### Manual
1. Run on the Wi-Fi device, navigate to Devices, confirm layout matches the golden screenshot closely (no design-verify gate yet — manual check).
2. Block a test relationship, confirm the row updates.

## 9. Self-review + Definition of Done
### Self-review
- [ ] Every §7 item done, with commit hashes
- [ ] `flutter analyze && flutter test` green
- [ ] Diff confined to `files:`; §4 respected
- [ ] UI: loading/error/empty states present
- [ ] Every ADR touching these files is honoured

**Deviations from spec/design** (none, or list):

**Files actually touched:**

### Definition of Done
- [ ] Every §8 criterion passes via a test named by its EARS id
- [ ] UI: design gate green, or explicitly noted as not-yet-available (OQ-E00-3) with a manual check substituted
- [ ] Review APPROVE from a different model than `executed_by` (rule 5)
- [ ] Squash-merged to `epic_02` branch; tracker + metrics stamped
- [ ] 🧍 Human `verified`

## Open Questions
- **OQ-E02-T02-1 — "Discover" button has no real behavior yet.** No device discovery exists until E04. Logged as GAP in `design/gaps.md`, traced to FR-DISC-001, deferred to E04.
  - **Status:** ⚪ deferred
  - **Answered by:** claude-code (orchestrator), during sharding
  - **Date:** 2026-08-26

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- (key evidence: decisions applied, session refs, gate reports)
