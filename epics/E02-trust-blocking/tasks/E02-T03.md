---
id: E02-T03
epic: E02
type: feature
title: Settings screen — top-level menu
layer: frontend
size: S
status: todo
owner_agent: builder-ui
model: sonnet
preferred_agent: any
token_estimate: { tier: S, range: "15k-50k" }
priority: { moscow: should, p: P2 }
depends_on: [E02-T01]
blocks: []
traces_to: [FR-TRUST-006, FR-TRUST-007]
required_context: [design/screens/settings.md, docs/routes.md]
design_contract: design/screens/settings.md
files:
  create:
    - lib/features/settings/presentation/settings_view.dart
    - lib/features/settings/presentation/settings_controller.dart
    - lib/features/settings/presentation/settings_binding.dart
  update:
    - lib/app/routes.dart
    - lib/app/bindings.dart
    - docs/routes.md
    - design/gaps.md
  delete: []
started_at:
completed_at:
executed_by:
reviewed_at:
reviewed_by:
review_outcome:
---
# E02-T03 · Settings screen — top-level menu

## 1. Goal
Build the real `/settings` screen as a navigation hub, exactly as designed
— eight rows (Account, Privacy & Security, Security Center, Network,
Storage, Battery, Notifications, About).

## 2. Business logic
This screen is a menu, not a settings-editing surface — none of its rows
have a designed sub-screen yet. FR-TRUST-006 (auto-accept rules,
block-list management, location-access toggle) would live under "Privacy
& Security", but no design contract exists for that sub-screen. Per rule
2, this task builds only what the contract shows and logs the missing
sub-screen as a gap rather than inventing one.

## 3. What this task does
- Real `/settings` route, screen, controller, binding — the eight menu
  rows exactly as `design/screens/settings.md` specifies (icons, labels,
  descriptions, chevron affordance).
- Each row is tappable; since no sub-screens are designed/built yet,
  tapping any row shows a lightweight "Coming soon" acknowledgement
  (e.g. a `SnackBar`) rather than a dead tap or a fabricated screen.
- Adds one `design/gaps.md` entry: the "Privacy & Security" sub-screen
  needed for FR-TRUST-006's actual controls (auto-accept-trusted,
  auto-accept-specific, require-auth-for-unknown, block-list view,
  location-access toggle) doesn't exist in the design source — traced to
  FR-TRUST-006, status 🟡 proposed, needs 🧍 human approval before any
  future task builds it.

## 4. What this task does NOT do
- Does NOT build any sub-screen (Account, Privacy & Security, etc.) — none
  are designed yet.
- Does NOT implement any of FR-TRUST-006's actual toggles/config
  persistence — that's blocked on the gap above being resolved.
- Does NOT touch the Devices screen (E02-T02).

## 5. Contract

### UI
- **Design contract:** `design/screens/settings.md`
- **Route:** `/settings`
- **States required:** the single default state the contract shows
- Gate: `make design-verify SCREEN=settings` — same OQ-E00-3 caveat as
  E02-T02; substitute a manual golden-screenshot comparison if the gate
  isn't wired yet.

## 6. Risks
- Don't let "Coming soon" turn into silently-invented sub-screens later —
  each sub-screen needs its own design contract and gap resolution first.

## 7. Implementation checklist
- [ ] tests written FIRST and failing, named by EARS id
- [ ] `/settings` route + screen + controller + binding, all 8 rows
- [ ] tap → "Coming soon" acknowledgement (no fabricated navigation)
- [ ] `design/gaps.md` GAP entry added for the Privacy & Security sub-screen
- [ ] `flutter analyze` clean, `flutter test` green
- [ ] manual on-device check against the golden screenshot

## 8. Acceptance criteria (EARS) + test plan
- **EARS-SET-1**: The Settings screen SHALL render all eight menu rows exactly as the design contract specifies. (design contract, rule 2)
- **EARS-SET-2**: WHEN a user taps a row with no built sub-screen, the system SHALL acknowledge the tap without navigating to a fabricated screen. (rule 2 — no invented UI)

### Tests
- `test_EARS_SET_1_renders_all_eight_rows`
- `test_EARS_SET_2_tap_shows_coming_soon_not_fake_navigation`

### Manual
1. Run on the Wi-Fi device, navigate to Settings, compare against the golden screenshot.

## 9. Self-review + Definition of Done
### Self-review
- [ ] Every §7 item done, with commit hashes
- [ ] `flutter analyze && flutter test` green
- [ ] Diff confined to `files:`; §4 respected
- [ ] No invented sub-screens; gap logged instead

**Deviations from spec/design** (none, or list):

**Files actually touched:**

### Definition of Done
- [ ] Every §8 criterion passes via a test named by its EARS id
- [ ] UI: design gate green, or manual check substituted with OQ-E00-3 noted
- [ ] Review APPROVE from a different model than `executed_by` (rule 5)
- [ ] Squash-merged to `epic_02` branch; tracker + metrics stamped
- [ ] 🧍 Human `verified`

## Open Questions
- **OQ-E02-T03-1 — Privacy & Security sub-screen has no design source.** FR-TRUST-006's real controls need it. Logged in `design/gaps.md`, needs 🧍 human approval (a design pass) before any future task builds it.
  - **Status:** 🟡 open
  - **Answer:** _<empty>_
  - **Answered by:** _<empty>_
  - **Date:** _<empty>_

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- (key evidence: decisions applied, session refs, gate reports)
