---
id: E03-B03
epic: E03
type: bug
title: Decrypt failures have no catchable type — InvalidMessageException is not exported from the libsignal barrel
layer: backend
size: S
status: review-requested
owner_agent: builder
model: any
preferred_agent: any
token_estimate: { tier: S, range: "3k-8k" }
severity: S3
priority: { moscow: should, p: P3 }
depends_on: [E03-T03]
blocks: []
traces_to: [FR-SEC-001]
required_context: [epics/E03-e2e-encryption/tasks/E03-T03.md]
design_contract: n/a
found_by: reviewer (claude-opus-5), carried from the E03-T03 review, confirmed in the E03 bug sweep
found_in: seam between E03-T03 and its consumers (E05/E06)
files:
  create: []
  update:
    - lib/core/crypto/crypto_stub.dart
    - test/core/crypto/crypto_service_test.dart
  delete: []
started_at: 2026-08-30
completed_at: 2026-08-30
executed_by: claude-sonnet-5 (builder) — closure-by-reference only, see Run log
reviewed_at: —
reviewed_by: —
review_outcome: —
---
# E03-B03 · No catchable type for the commonest decrypt failure

## Severity: S3 (broken, but there's a workaround)
Nothing is broken *today* — no caller exists yet (E03-T03 §4: the API is real
but unused until E05/E06). The defect is that the seam E03 hands to its
consumers forces one of two bad choices, in a security path:

- **catch-all** (`try { decrypt() } catch (_) { }`) — which swallows genuine
  bugs (a `StateError` from a missing `init`, an `ArgumentError` from a wrong
  message type, an `UntrustedIdentityException` that a UI *must* surface as a
  safety-number warning) alongside routine undecryptable traffic; or
- **runtime-type string matching**, the workaround already in this repo's own
  test suite (`test/core/crypto/crypto_service_test.dart:86-89`).

The workaround exists and works, hence S3 rather than S2. But it is a
string-compare against a private class name in a third-party package — it
breaks silently on any rename, and it is the pattern E05/E06 would copy.

Priority is the human's (`bug_priorities` gate). Reviewer's advisory: P3 —
this is genuinely "next epic" work, best done alongside whichever epic first
writes a `catch` around `decrypt()`.

## Repro
1. Grep the installed package barrel:
   `libsignal_protocol_dart-0.8.2/lib/libsignal_protocol_dart.dart`.
2. Note which exception files it exports.

### Expected
A consumer of `CryptoService.decrypt()` can distinguish, **by type**:
- MAC / key-derivation failure (wrong key — the routine "cannot read this"
  case, and the case both of E03-T03's strongest security proofs assert on),
- `DuplicateMessageException` (replay),
- `NoSessionException` (no session),
- `InvalidKeyIdException` (missing prekey),
- `UntrustedIdentityException` (safety-number change — must reach the user).

### Actual (reviewer-verified 2026-08-27)
The barrel exports `duplicate_message_exception` (:2),
`invalid_key_exception` (:21), `invalid_key_id_exception` (:22),
`legacy_message_exception` (:23), `no_session_exception` (:24) and
`untrusted_identity_exception` (:49) — but **not**
`src/invalid_message_exception.dart`. The one failure mode that means "the
crypto ran and the key was wrong" is the one type a caller cannot name.

Evidence that this already bit us: `crypto_service_test.dart:86-89` has to
do
```dart
final Matcher _isInvalidMessageException = predicate<Object>(
  (e) => e.runtimeType.toString() == 'InvalidMessageException', ...
```
with a comment explaining that naming the type would require an
`implementation_imports` lint violation.

## Root cause
A packaging omission in `libsignal_protocol_dart` v0.8.2 — not in NEXORA
code. But E03 owns the seam: `crypto_stub.dart` is the public
`core/crypto` API, and it currently lets the library's un-nameable exception
escape to callers unchanged.

## Fix direction (not prescriptive)
1. **A NEXORA-owned exception taxonomy** — `crypto_stub.dart` catches the
   library's exceptions inside `decrypt()`/`encrypt()`/`establishSession()`
   and rethrows small app-owned types (e.g. `CryptoDecryptFailure`,
   `CryptoReplayRejected`, `CryptoUntrustedIdentity`, `CryptoNoSession`)
   preserving the original as a `cause`. Cleanest for consumers, insulates
   the app from the library's packaging; costs a mapping layer that must not
   accidentally collapse two distinct failures into one.
2. **Re-export with a targeted lint ignore** — `// ignore: implementation_imports`
   on an import of `src/invalid_message_exception.dart` plus a re-export from
   our own file. Two lines; keeps the library's names; leaves callers coupled
   to a package-private path.
3. **Upstream** — file the export omission with the package and pin/patch.
   Right long-term answer, wrong timescale to block on.

Reviewer's advisory: **Option 1**, chosen when E05/E06's error handling is
designed rather than in isolation — the taxonomy should be shaped by what the
UI actually needs to distinguish (specifically: which failures are silent and
which must raise a safety-number warning). That is why this is a bug task and
not a two-line fix committed during the sweep.

## 4. What this task does NOT do
- Does NOT change `decrypt()`'s success-path behaviour or return type.
- Does NOT swallow any exception — every failure must still throw.
- Does NOT change the five E03-T03 proof tests' *intent*; if Option 1 is
  taken they must be updated to assert the new types (and must still fail
  against a broken implementation — re-run the E03-T03 mutation check).
- Does NOT weaken any assertion to `throwsA(isException)` or `throwsA(anything)`
  — the E03-T03 review specifically tightened one such assertion
  (`crypto_service_test.dart:388-392`); regressing it would undo that.

## Regression test
Replace the `_isInvalidMessageException` runtime-type-name predicate with a
real `isA<...>()` matcher over whichever type the approved option introduces,
and add one test asserting that a MAC failure and a replay are distinguishable
by type from the same `catch` site.

## Open Questions
- **OQ-E03-B03-1** — Option 1 (app-owned taxonomy) vs Option 2 (re-export).
  Not a rule-3 gate (no schema, no dependency, no architecture change), so
  this is the implementer's call within the fix direction — but it should be
  made *with* the epic that first consumes it. Recommend scheduling this bug
  with E05/E06 rather than inside E03.
  - **Status:** 🟢 resolved — **Option 1** (app-owned taxonomy), exactly as the
    reviewer's advisory recommended. Already implemented and reviewed under
    E06-T02, which is the epic that first wrapped a `catch` around
    `decrypt()` (T05's receive pipeline) — precisely the scheduling this
    question recommended. See Run log.
  - **Answer:** Option 1, chosen by the E06-T02 implementer/reviewer pair,
    not re-litigated here.
  - **Answered by:** claude-sonnet-5 (E06-T02 builder) / claude-opus-5
    (E06-T02 reviewer)
  - **Date:** 2026-08-29

## 7. Implementation checklist
- [x] approach chosen and recorded in the Run log — **Option 1 (app-owned
      taxonomy)**, already implemented by `91caca7` (E06-T02) `91caca7`
- [x] every library exception reachable from `establishSession`/`encrypt`/
      `decrypt` enumerated, with its meaning, in a dartdoc block —
      `lib/core/crypto/crypto_failures.dart:29-69` `91caca7`
- [x] `crypto_service_test.dart`'s runtime-type-name predicate removed from
      every call site that goes through `CryptoService`'s own seam (the 2
      remaining uses of `_isInvalidMessageException`, lines ~331/335/417,
      are intentionally left on the two probes that call `SessionCipher`
      directly, bypassing `CryptoService` — see Run log) `91caca7`
- [x] MAC-failure vs replay distinguishable by type from one catch site —
      `CryptoDecryptFailureReason.invalidMessage` vs `.duplicateMessage`
      from the single `catch (e) { throw mapSignalException(e); }` in
      `decrypt()` (`crypto_stub.dart:144-146`) `91caca7`
- [x] E03-T03's five proof tests still green and still falsifiable (re-run
      the T03 mutation check: break `decrypt()`'s PreKey branch, confirm
      they fail) — re-run fresh in this task, see Run log `0a02cb1`
- [x] `flutter analyze` clean, `flutter test` green — re-verified fresh in
      this task: `flutter analyze` 0 issues, `flutter test` 384/384 `0a02cb1`

## 9. Definition of Done
- [x] A consumer can catch each documented failure mode by type, with no
      string comparison and no `implementation_imports` violation —
      `CryptoDecryptFailure`/`CryptoDecryptFailureReason`
      (`crypto_failures.dart`), thrown from `decrypt()`/`establishSession()`
      (`crypto_stub.dart:84`, `:145`) `91caca7`
- [x] No exception is swallowed or broadened — every `mapSignalException`
      branch preserves the original as `cause`; `unknown` catches anything
      unrecognised without dropping it (`crypto_failures.dart:139-145`)
- [ ] Independent review (rule 5) — this task file's *closure* needs review
      even though the underlying code was already reviewed under E06-T02
      (`e66e114`, claude-opus-5, APPROVE)

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- **2026-08-30, claude-sonnet-5 (builder): this bug was already fixed —
  closure-by-reference, no new code.** On picking up this task
  (`epic_03_task_B03`, branched off `development` — see §Deviations below),
  `lib/core/crypto/crypto_stub.dart` and
  `test/core/crypto/crypto_service_test.dart` — the two files this task's
  `files:` lists — were both already in the exact end state this bug asks
  for. Confirmed by direct inspection before writing anything:
  - `crypto_stub.dart`'s `decrypt()`/`establishSession()` already wrap their
    library calls in `try { ... } catch (e) { throw mapSignalException(e); }`
    (lines 84, 137-146), rethrowing the app-owned
    `CryptoDecryptFailure`/`CryptoDecryptFailureReason` taxonomy defined in
    `lib/core/crypto/crypto_failures.dart` — a file whose header literally
    reads "the decrypt/session-establishment exception taxonomy (E03-B03,
    closed here per E06-T02)".
  - `crypto_service_test.dart`'s `_isInvalidMessageException` runtime-type-
    name predicate — this bug's own repro evidence — is already retired from
    every assertion that goes through `CryptoService`'s public seam,
    replaced by `_isInvalidMessageFailure` (`isA<CryptoDecryptFailure>()`
    matching `reason == invalidMessage`) at lines 232 and 258. It survives
    only at line 417, on a probe that calls `SessionCipher`/`SessionBuilder`
    directly, bypassing `CryptoService` entirely (still correctly sees the
    library's raw exception there — this bug's seam is `CryptoService`'s
    boundary, not the library itself).
  - **Git history confirms why**: `git log --oneline --grep="E06-T02"` shows
    `feat(E06-T02): relay wire-packet framing + ciphertext type tag +
    decrypt exception taxonomy` (`91caca7` code / `4edd984` docs), reviewed
    and approved by claude-opus-5 (`e66e114`), later squash-merged to
    `epic_06` (`a0e4031`) and, with the rest of E06, merged to `development`
    (`1cde254`). E06-T02.md §2 names this bug explicitly: "**E03-B03**,
    deferred by E03's retro 'to whichever of E05/E06 first wraps a `catch`
    around `decrypt()`': ... E06 is that epic". This is exactly OQ-E03-B03-1's
    own recommendation ("Recommend scheduling this bug with E05/E06 rather
    than inside E03") being followed — just followed *before* this bug task
    itself was formally dispatched/closed, since E06-T02 needed the same fix
    for its own EARS-COMM-5/`mapSignalException` contract regardless of this
    task file's status.
  - **OQ-E03-B03-1 resolved**: Option 1 (app-owned taxonomy) — see
    E06-T02.md §3's "`crypto_failures.dart` — the exception taxonomy
    E03-B03 asked for" and its Run log's "Exception taxonomy" entry, plus
    the reviewer's independent verification in E06-T02.md's Review log.
    Not re-litigated or re-implemented here.
- **Fresh verification performed in this task (no code changed — see
  §Deviations for why a docs-only branch still re-ran the falsification
  step rather than trusting E06-T02's own record):**
  1. `flutter analyze` → **0 issues**.
  2. `flutter test` (full suite) → **384/384 pass**
     (`test/core/crypto/crypto_service_test.dart` alone: 5/5).
  3. **Re-ran the E03-T03 mutation check** (this task's own §7 requirement,
     independent of E06-T02's own falsification record): temporarily changed
     `decrypt()`'s `PreKeySignalMessage` branch from
     `cipher.decrypt(ciphertext)` to
     `cipher.decryptFromSignal(SignalMessage.fromSerialized(ciphertext.serialize()))`
     — the same "wrong library call on the PreKey branch" mutation E03-T03's
     reviewer used. Result: all 5 of E03-T03's proof tests
     (`test_EARS_SEC_1_two_party_roundtrip`,
     `test_EARS_SEC_2_relay_cannot_decrypt`,
     `test_EARS_SEC_3_forward_secrecy`,
     `test_EARS_SEC_3_post_compromise_recovery`,
     `test_EARS_SEC_3_replay_rejected`) failed, each surfacing
     `CryptoDecryptFailure(invalidMessage): message key failed MAC
     verification (cause: InvalidMessageException)` — the wrapper's own
     typed failure, correctly propagated even under a broken implementation,
     confirming the taxonomy doesn't accidentally mask a real bug. Mutation
     reverted; `git diff`/`git status` confirmed clean (byte-identical to
     before); `flutter test` re-run green (5/5) after restoring.
- **Deviations from the dispatch instructions:**
  - **Branch base: `development`, not `epic_03`.** The dispatch instructed
    branching `epic_03_task_B03` off `epic_03`. `epic_03` no longer exists
    as a branch (local or on `origin`) — E03 was fully merged and its epic
    branch cleaned up well before E05/E06 began; `git branch -a` /
    `git log --all --grep` confirm E03-T03's work and this bug's fix both
    already live on `development` via the E05/E06 merge chain. There is no
    surviving integration point named `epic_03` to branch from or open a
    review against. `development` is the closest existing branch that
    contains both this task's dependency (E03-T03, done) and this bug's
    actual fix (E06-T02, done, reviewed, merged) — recorded here rather than
    silently rerouted, per rule 6/handoff §6.
  - **No production diff.** `files:` lists `crypto_stub.dart` and
    `crypto_service_test.dart` for update; neither needed a change, since
    both already satisfy every checklist/DoD item (see above). Editing them
    anyway to manufacture a diff would be busywork with no behavioral
    purpose and would risk drifting from E06-T02's already-reviewed, already-
    falsified implementation. Only this task file changed.
  - **`crypto_failures.dart` untouched, and correctly out of scope.** It is
    not in this task's `files:` list (it belongs to E06-T02's `files:`
    instead) — it was read for verification but not edited, per rule 6.
- **Suite (fresh, this task, `development` tip `6a4bd9f`)**: `flutter
  analyze` → 0 issues. `flutter test` → 384/384 pass, no regressions.
- **Commit (docs, this file only)**: see commit hash below.
