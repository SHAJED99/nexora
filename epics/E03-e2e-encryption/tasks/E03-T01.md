---
id: E03-T01
epic: E03
type: feature
title: Add libsignal_protocol_dart + Drift-backed protocol store
layer: backend
size: M
status: done
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: must, p: P1 }
depends_on: []
blocks: [E03-T02]
traces_to: [FR-SEC-004]
required_context: [ADR-0003, docs/conventions.md, epics/E03-e2e-encryption/epic.md]
design_contract: n/a
files:
  create:
    - lib/core/persistence/crypto_tables.dart
    - lib/core/crypto/drift_signal_store.dart
    - test/core/crypto/drift_signal_store_test.dart
    - test/core/persistence/crypto_migration_test.dart
  update:
    - pubspec.yaml
    - lib/core/persistence/database.dart
    - lib/core/persistence/database.g.dart
  delete: []
started_at: 2026-08-27
completed_at: 2026-08-27
executed_by: claude-sonnet-5 (builder)
reviewed_at: 2026-08-27
reviewed_by: claude-opus-5 (reviewer)
review_outcome: approve-with-notes
---
# E03-T01 · Add libsignal_protocol_dart + Drift-backed protocol store

## 1. Goal
Give the app a real, persistent implementation of the Signal protocol's
storage interfaces (identity, prekeys, signed prekeys, sessions) so T02/T03
have somewhere durable to keep key material — the foundation every later
crypto operation reads and writes through.

## 2. Business logic
- FR-SEC-004 / EARS-SEC-3: the protocol implementation must be the accepted
  X3DH + Double Ratchet family (ADR-0003), not ad hoc — this task wires the
  specific library named in OQ-E03-1 (`libsignal_protocol_dart`, resolved
  2026-08-27) into the app.
- Session/ratchet state is local-only, never synced to Firebase (`FR-FB-002`,
  epic.md "Data model") — these tables are not part of any sync path.
- Identity keys, once generated, must never be regenerated silently — a
  second call after first-run must be a no-op that returns the existing
  identity, not a fresh one (regenerating would orphan every session a peer
  has with this device).

## 3. What this task does
- Add `libsignal_protocol_dart` (mixin.dev, pure Dart, GPL-3.0 — accepted by
  the human, see OQ-E03-1) to `pubspec.yaml`.
- Add Drift tables for the four things the library's store interfaces need
  to persist: local identity (identity keypair + registration id, one row),
  signed prekeys, one-time prekeys, and sessions (keyed by remote address —
  `name` + `deviceId` per the library's `SignalProtocolAddress`).
- Implement `DriftSignalProtocolStore` — a single class implementing the
  library's `IdentityKeyStore`, `PreKeyStore`, `SignedPreKeyStore`, and
  `SessionStore` interfaces, backed by the tables above via `AppDatabase`.
  (`SenderKeyStore`/group state is explicitly out of scope — see §4.)
- Bump the Drift schema version and write a real `onUpgrade` migration
  (adding the new tables), proven by a hand-built prior-version-schema test
  in the same style as `test/core/persistence/database_migration_test.dart`.

## 4. What this task does NOT do
- Does NOT generate any actual key material or call any of the store's
  methods from application code yet — this task is the storage seam only.
  Key generation is E03-T02.
- Does NOT implement `SenderKeyStore` (group/Sender-Keys persistence) — that
  belongs to E07 when group encryption is built, even though the same
  library will provide it later.
- Does NOT touch `lib/core/crypto/crypto_stub.dart` or wire anything into
  `main.dart` — nothing calls this store yet, so nothing observable changes
  in the running app after this task.
- Does NOT add any UI, screen, or route.
- Does NOT implement encrypt/decrypt or session establishment (X3DH) logic —
  that's T03. This task only proves data survives a write→read→app-restart
  cycle correctly.

## 5. Contract

### Data
Migration `000X_add_signal_protocol_stores` (schema version bump, additive,
reversible — pre-release, no backfill needed):
- `signal_identity` — singleton row: `id INTEGER PK`, `identity_key_pair BLOB`
  (serialized `IdentityKeyPair`), `registration_id INTEGER`.
- `signal_signed_prekeys` — `id INTEGER PK` (the prekey id from the library,
  not autoincrement), `record BLOB` (serialized `SignedPreKeyRecord`).
- `signal_one_time_prekeys` — `id INTEGER PK` (prekey id), `record BLOB`
  (serialized `PreKeyRecord`).
- `signal_sessions` — `address_name TEXT`, `address_device_id INTEGER`,
  `record BLOB` (serialized `SessionRecord`), PK on
  `(address_name, address_device_id)`.

All BLOB columns store the library's own `.serialize()` bytes — this task
does not invent a serialization format.

### Functions
```yaml
- signature: "DriftSignalProtocolStore.getIdentityKeyPair() -> Future<IdentityKeyPair>"
  params: {}
  returns: "the persisted identity keypair; throws StateError if none exists yet (T02's job to create one first)"
  purpose: "IdentityKeyStore interface method"

- signature: "DriftSignalProtocolStore.getLocalRegistrationId() -> Future<int>"
  params: {}
  returns: "the persisted registration id; throws StateError if none exists yet"
  purpose: "IdentityKeyStore interface method"

- signature: "DriftSignalProtocolStore.saveIdentity(address: SignalProtocolAddress, identityKey: IdentityKey?) -> Future<bool>"
  params: { address: "remote party's protocol address", identityKey: "their identity key, or null to remove" }
  returns: "true if this replaced a previously-trusted different identity key (a safety-number change) for the caller to surface later; false otherwise"
  purpose: "IdentityKeyStore interface method — trust-on-first-use bookkeeping, not yet acted on by UI"

- signature: "DriftSignalProtocolStore.isTrustedIdentity(address: SignalProtocolAddress, identityKey: IdentityKey, direction: Direction) -> Future<bool>"
  params: { address: "remote party", identityKey: "the key being evaluated", direction: "SENDING or RECEIVING" }
  returns: "true (trust-on-first-use — no prior identity recorded, or it matches); false only if it contradicts a previously-saved different identity key"
  purpose: "IdentityKeyStore interface method"

- signature: "DriftSignalProtocolStore.loadPreKey(preKeyId: int) -> Future<PreKeyRecord>"
  params: { preKeyId: "one-time prekey id" }
  returns: "the record; throws InvalidKeyIdException if absent"
  purpose: "PreKeyStore interface method"

- signature: "DriftSignalProtocolStore.storePreKey(preKeyId: int, record: PreKeyRecord) -> Future<void>"
  purpose: "PreKeyStore interface method"

- signature: "DriftSignalProtocolStore.removePreKey(preKeyId: int) -> Future<void>"
  purpose: "PreKeyStore interface method — a one-time prekey is consumed exactly once, per X3DH"

- signature: "DriftSignalProtocolStore.loadSession(address: SignalProtocolAddress) -> Future<SessionRecord>"
  returns: "existing session record, or a fresh empty SessionRecord if none exists (per the library's documented contract for this method)"
  purpose: "SessionStore interface method"

- signature: "DriftSignalProtocolStore.storeSession(address: SignalProtocolAddress, record: SessionRecord) -> Future<void>"
  purpose: "SessionStore interface method — called after every ratchet step, must be fast and durable"
```

## 6. Risks
- The library's store interfaces are `async` in `libsignal_protocol_dart`
  (unlike the original Java) — confirm exact method signatures against the
  installed package version, don't guess from the Java API docs.
- Getting the singleton-row pattern for `signal_identity` wrong (e.g.
  allowing two rows) would let a bug silently generate a second identity —
  enforce with a unique constraint or a fixed `id = 0`, not just "insert
  once and hope."
- BLOB round-tripping: write a test that serializes, writes, reads, and
  deserializes a real record object (not just raw bytes) to catch any
  library-version serialization quirk early.

## 7. Implementation checklist
- [x] add `libsignal_protocol_dart` to pubspec.yaml, `flutter pub get`  `3e3a1e3`
- [x] Drift tables added (`crypto_tables.dart`), schema version bumped,
      `onUpgrade` migration written  `6cbe89b`
- [x] `DriftSignalProtocolStore` implements all four store interfaces  `b2e3bc6`
- [x] migration test (hand-built prior schema, prove upgrade adds tables
      without touching existing data)  `7359e85`
- [x] round-trip test per store method (identity, prekey, signed prekey,
      session)  `7359e85`
- [x] `flutter analyze` clean, `flutter test` green  `7359e85`
- [ ] tests written FIRST and failing, named by EARS id — **deviation**: the
      real package API had to be confirmed before writing meaningful tests
      (see Run log); tests were written immediately after the store
      implementation instead of strictly before it, but before any
      round-trip was manually verified to pass. See §9 Deviations.

## 8. Acceptance criteria (EARS) + test plan
- **EARS-SEC-3a**: The system SHALL persist Signal protocol key material
  (identity, prekeys, sessions) via the library named in OQ-E03-1, not an
  ad hoc format. (FR-SEC-004)

### Tests
- [x] `test_EARS_SEC_3a_identity_roundtrips` — save then load an `IdentityKeyPair`
  + registration id, assert equality (`test/core/crypto/drift_signal_store_test.dart`)
- [x] `test_EARS_SEC_3a_identity_generation_is_singleton` — second identity
  write attempt no-ops, never silently produces a second identity
  (`test/core/crypto/drift_signal_store_test.dart`)
- [x] `test_EARS_SEC_3a_prekey_roundtrips` — store/load/remove a `PreKeyRecord`
  (`test/core/crypto/drift_signal_store_test.dart`)
- [x] `test_EARS_SEC_3a_signed_prekey_roundtrips` — store/load a `SignedPreKeyRecord`
  (`test/core/crypto/drift_signal_store_test.dart`)
- [x] `test_EARS_SEC_3a_session_roundtrips` — store/load a `SessionRecord` for a
  given `SignalProtocolAddress` (`test/core/crypto/drift_signal_store_test.dart`)
- [x] `test_EARS_SEC_3a_migration_adds_signal_tables` — hand-built prior-version
  DB upgrades cleanly, new tables present, old data untouched
  (`test/core/persistence/crypto_migration_test.dart`)

Additional (not required by §8, added for coverage): `test_EARS_SEC_3a_identity_missing_throws`,
`test_EARS_SEC_3a_session_delete_all_removes_every_device_for_name`.

### Manual
None — no observable app behavior changes in this task.

## 9. Self-review + Definition of Done
### Self-review
- [x] Every §7 item done, with commit hashes
- [x] `flutter test && flutter analyze` green (repo has no `make` target;
      ran the Flutter equivalents directly — 42/42 tests pass, analyze clean)
- [x] Diff confined to `files:`; §4 respected
- [x] No secrets or PII logged; identity/session BLOBs never logged (this
      task adds no logging at all — pure storage seam)
- [x] ADR-0003 honoured (library implements the accepted protocol family —
      X3DH + Double Ratchet, `libsignal_protocol_dart` per OQ-E03-1)

**Deviations from spec/design:**
1. **Tests not strictly red-first.** §6 flags that the library's real store
   interface signatures needed confirming against the installed package
   (not guessed from Java docs) before tests could be written meaningfully.
   I read the installed `libsignal_protocol_dart-0.8.2` source first (its
   `IdentityKeyStore`/`PreKeyStore`/`SignedPreKeyStore`/`SessionStore`
   abstract classes, `SignalProtocolStore`, and the record classes'
   constructors/`serialize()` methods) to get accurate signatures, then
   implemented the store, then wrote the round-trip/migration tests. Tests
   were still written and run before this task was called done — the
   sequencing relative to the implementation is the deviation, not the
   presence of tests. No task-file API sketch turned out to need
   correcting beyond the `getIdentity(address)` method (present on the
   real `IdentityKeyStore` interface, not explicitly listed in §5's
   function sketch, but required to implement the interface at all).
2. **`saveIdentity`/`isTrustedIdentity`/`getIdentity` (remote-peer trust
   bookkeeping) are in-memory only, not Drift-persisted.** §5's Data
   contract lists exactly four things needing Drift persistence — local
   identity, signed prekeys, one-time prekeys, sessions — and does not
   define a table for per-remote-address trusted identity keys; §8's test
   plan also has no test exercising this trust surviving an app restart.
   Reading §5 and §8 together, the simplest consistent interpretation is
   that this task's persistence scope is the four named tables only, and
   `IdentityKeyStore`'s IdentityKey-trust bookkeeping (distinct from E02's
   already-existing `Relationships` device-level trust table) is
   structurally present so the interface compiles and is usable, but not
   yet durable — matching §5's own note that these methods are
   "trust-on-first-use bookkeeping, not yet acted on by UI." If persisting
   safety-number-style identity-key trust across restarts is actually
   required before T02/T03 land, that is a real spec gap in this task's
   Data contract and belongs in a follow-up task/ADR note, not something I
   should resolve by inventing a fifth table outside `files:`. Flagging
   here rather than in `## Open Questions` because it does not block T02
   (identity/prekey generation) or T03 (session establishment / X3DH) —
   both only need the four persisted stores this task delivers.

**Files actually touched:**
- `pubspec.yaml`, `pubspec.lock` (dependency addition)
- `lib/core/persistence/crypto_tables.dart` (new)
- `lib/core/persistence/database.dart` (tables registered, schema v3->v4,
  `onUpgrade` migration)
- `lib/core/persistence/database.g.dart` (regenerated via `build_runner`)
- `lib/core/crypto/drift_signal_store.dart` (new)
- `test/core/crypto/drift_signal_store_test.dart` (new)
- `test/core/persistence/crypto_migration_test.dart` (new)

All within the task's `files:` list.

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id
- [x] Review APPROVE from a **different model** than `executed_by` (rule 5) —
      `executed_by: claude-sonnet-5`, `reviewed_by: claude-opus-5`, APPROVE
      with notes 2026-08-27 (see Run log § Review)
- [ ] Squash-merged to `epic_03` branch; tracker + metrics stamped
- [ ] 🧍 Human `verified` (epic-level batches are fine)

## Open Questions
- **OQ-E03-T01-1 — remote-peer identity-key trust is not persisted.**
  *(raised by the reviewer 2026-08-27, promoted from §9 Deviation #2. Does
  NOT block this task's merge — the diff is in-contract. DOES block E03-T03.)*
  - `lib/core/crypto/drift_signal_store.dart:31` keeps
    `_trustedIdentities` in a plain in-memory `Map`, so `saveIdentity` /
    `isTrustedIdentity` / `getIdentity` forget every recorded peer identity
    key on process restart. `isTrustedIdentity` then takes the
    trust-on-first-use branch (`drift_signal_store.dart:110-113`) and
    returns `true` for **any** key.
  - The builder's judgement that this "does not block T02 or T03" is right
    for T02 and **wrong for T03**: `E03-T03.md:53` states the trust check is
    "already provided by T01/T02's `isTrustedIdentity`", and `E03-T03.md:101`
    specifies `establishSession` "throws `UntrustedIdentityException` if
    remoteBundle's identity key contradicts a previously-trusted one for
    this address". With in-memory-only trust, that criterion is satisfiable
    by an in-process test while the real guarantee — MITM / safety-number-
    change detection across an app restart — does not hold. That is a green
    test with no durable wiring behind it.
  - The builder was correct NOT to fix it here: a fifth table is outside
    this task's `files:` and §5 Data contract (rule 6), and a schema
    migration is a 🧍 human call (rule 3).
  - **Decision needed from planner + human before E03-T03 is built:** add a
    `signal_trusted_identities` table (`address_name`, `address_device_id`,
    `identity_key BLOB`, PK on the address pair) as a new task or as an
    extension of E03-T02's scope, with an EARS criterion that asserts trust
    survives a reopen of the database. E03-T02 (local identity + prekey
    generation) may proceed as-is in the meantime.

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- Confirmed real `libsignal_protocol_dart-0.8.2` package API (installed via
  `flutter pub get`) before writing the store, since §6 flags the store
  interfaces as `async` and warns not to guess from Java docs:
  - `IdentityKeyStore` additionally declares `Future<IdentityKey?>
    getIdentity(SignalProtocolAddress address)`, not listed in §5's
    function sketch but required to implement the interface.
  - `isTrustedIdentity`'s `identityKey` parameter is `IdentityKey?`
    (nullable), not `IdentityKey` as sketched in §5.
  - `SignalProtocolStore` is a ready-made abstract class
    (`IdentityKeyStore` extended `with PreKeyStore, SessionStore,
    SignedPreKeyStore`) — `DriftSignalProtocolStore extends
    SignalProtocolStore` rather than implementing all four interfaces
    separately; same effective contract as §3's "single class implementing
    the library's four store interfaces."
  - Record classes' constructors/serialization: `IdentityKeyPair(publicKey,
    privateKey)` / `.fromSerialized(bytes)` / `.serialize()`;
    `PreKeyRecord(id, ECKeyPair)` / `.fromBuffer(bytes)` / `.serialize()`;
    `SignedPreKeyRecord(id, timestamp, keyPair, signature)` /
    `.fromSerialized(bytes)` / `.serialize()`; `SessionRecord()` (fresh) /
    `.fromSerialized(bytes)` / `.serialize()` / `.isFresh()`.
  - Test helpers `generateIdentityKeyPair()`, `generateSignedPreKey(...)`,
    `Curve.generateKeyPair()` are exported from the package root
    (`package:libsignal_protocol_dart/libsignal_protocol_dart.dart`) and
    used in tests to build real record objects rather than raw bytes, per
    §6's "BLOB round-tripping" risk.
- `saveLocalIdentityIfAbsent(IdentityKeyPair, int)` was added as a public
  method beyond §5's function list — it is the write side of the
  `signal_identity` singleton row (the interface itself only reads local
  identity). Named to make the singleton/no-op contract from §2 ("a second
  call after first-run must be a no-op") explicit and testable; T02's key
  generation calls this.
- `flutter pub get` resolved `libsignal_protocol_dart: ^0.8.2` cleanly
  (transitively pulled `x25519`, `pointycastle`, `protobuf`, `ed25519_edwards`,
  `optional`, `adaptive_number`).
- `flutter pub run build_runner build --delete-conflicting-outputs`
  regenerated `database.g.dart` for the new tables + schema bump (`-o
  --delete-conflicting-outputs` flag itself was reported ignored by this
  build_runner version but the build still completed cleanly and produced
  the expected generated code).
- Final verification: `flutter analyze` — 0 issues. `flutter test` — 42/42
  passing (full suite, including this task's 8 new tests and every
  pre-existing test in the repo).

### Review — 2026-08-27, claude-opus-5 (reviewer), rule 5 satisfied
**Verdict: APPROVE with notes. Ready to merge to `epic_03`.** No product-code
changes were made by the reviewer — nothing needed fixing. One Open Question
was raised (OQ-E03-T01-1) which gates E03-T03, not this merge.

**Suites re-run independently by the reviewer in the worktree (not trusted
from the PR body):**
- `flutter analyze` → `No issues found! (ran in 1.8s)`, exit 0. Builder's
  claim confirmed.
- `flutter test` → `00:02 +42: All tests passed!`, exit 0. 42/42 confirmed.

**Scope (in-contract).** `git diff --stat epic_03...epic_03_task_01` = 9 files:
the 7 in `files:`, plus `pubspec.lock` (lockfile, allowed) and this task file.
No out-of-scope edits. §4 respected — `crypto_stub.dart` untouched, `main.dart`
untouched, nothing calls the store yet, no UI/route added, no `SenderKeyStore`.

**EARS-SEC-3a — 1/1 verified, 6/6 planned tests present and real.** Each
round-trip test builds a genuine library record object via the package's own
`generateIdentityKeyPair()` / `generateSignedPreKey()` / `Curve.generateKeyPair()`
and asserts on the *deserialized* object, not on raw bytes — this is what §6's
"BLOB round-tripping" risk asked for, and it is done correctly
(`test/core/crypto/drift_signal_store_test.dart:33-37, 75-82, 100-103`).

**Migration (v3→v4) — REAL proof, not superficial.**
`test/core/persistence/crypto_migration_test.dart` hand-builds the exact prior
v3 DDL (`device_identities` *including* `account_uid`, plus `relationships`),
inserts a row into each, sets `raw.userVersion = 3`, then opens `AppDatabase`
and asserts (a) the pre-existing rows survive intact
(`crypto_migration_test.dart:49-55`) and (b) all four `signal_*` tables exist
and are queryable (`:58-61`). Because the v3 tables already exist and
`userVersion` is 3, the `onCreate` path would have thrown — so this genuinely
exercises `onUpgrade(from: 3, to: 4)`. The migration itself
(`lib/core/persistence/database.dart` `if (from < 4)`) is four `createTable`
calls and nothing else: additive, no `ALTER`, no drop, no backfill, cannot
corrupt or lose existing data. Matches the established pattern in
`test/core/persistence/database_migration_test.dart`.

**`signal_identity` singleton (§6 risk) — adequately enforced.** Fixed
`id = 0` via `const int _localIdentityRowId = 0`
(`drift_signal_store.dart:19`), used in every read
(`:40, :53, :77`) and the single write (`:83`). PK is `{id}` with no
autoincrement (`crypto_tables.dart:19-24`), and the write is a plain
`insert()` — **not** `insertOnConflictUpdate` — after a check-for-existing
(`drift_signal_store.dart:76-88`), so even a racing second call raises a
unique-constraint error rather than replacing the identity. This is the
"fixed `id = 0`" option §6 explicitly blessed, not "insert once and hope."
Proven by `test_EARS_SEC_3a_identity_generation_is_singleton`, which asserts
both that the first identity survives and that `rows` has length 1
(`drift_signal_store_test.dart:54-59`).
*Note (non-blocking, deliberately not fixed):* there is no DB-level
`CHECK (id = 0)`, so a *future* code path outside this class could insert a
second identity at a different id. Not fixed here because (a) §6 named fixed-
id-0 as sufficient, (b) no such code path exists — the only writer hardcodes
0, and (c) changing the DDL of an already-committed security-adjacent
migration is a rule-3 schema call, not a reviewer's scoped fix. Worth adding
if E03-T02 introduces any second writer to this table.

**No key material logged — PASS.** `grep -rniE "print\(|debugPrint|log\(|logger|stdout|Sentry"` across
`lib/core/crypto/`, `crypto_tables.dart`, `database.dart` and both new test
files returns nothing. The store contains no logging of any kind; no
`toString()` override exposes a BLOB; error messages carry only ids
(`drift_signal_store.dart:133`, `:173`) and never key bytes.

**Library signatures — verified against installed source, not assumed.**
Read `~/AppData/Local/Pub/Cache/hosted/pub.dev/libsignal_protocol_dart-0.8.2/lib/src/state/{identity_key_store,pre_key_store,session_store,signed_pre_key_store,signal_protocol_store}.dart`.
Every abstract member is implemented with an exactly matching signature,
including the nullable `IdentityKey?` on both `saveIdentity` and
`isTrustedIdentity` and the `Direction` enum (`sending`/`receiving`).
`SignalProtocolStore` is indeed `IdentityKeyStore with PreKeyStore,
SessionStore, SignedPreKeyStore`, so `extends SignalProtocolStore` is the
same contract as §3's "implements the four interfaces" — accepted. The tests
exercise real library types throughout; there are **no stubs, fakes or mocks
standing in for the library** anywhere in this diff.
Also verified the two equality assumptions the in-memory trust map depends on:
`SignalProtocolAddress` has value `==`/`hashCode` (`signal_protocol_address.dart:17-25`)
so `Map` lookups by a freshly-constructed equal address work, and `IdentityKey`
`==` delegates to `DjbECPublicKey`'s byte comparison
(`identity_key.dart:26-31`, `ecc/djb_ec_public_key.dart:29-32`) so
`previous != identityKey` is a real key comparison, not reference identity.
Both were latent bug candidates; both are fine.

**Lessons check (rule 8) — neither backend lesson recurs.**
- L-backend-001 (`Value(null)` vs `Value.absent()`): not applicable — none of
  the four new tables has a nullable column, and every `Companion.insert`
  supplies all non-default fields. No partial-update path exists.
- L-backend-002 (unbounded `await`): every `await` in the store is a local
  Drift/SQLite call. No network, no external seam, no future that can hang
  pending. No timeout needed.

**ADR-0003 — honoured.** `libsignal_protocol_dart` 0.8.2 is exactly the
library resolved in OQ-E03-1 (epic.md, human-answered 2026-08-27), and the
GPL-3.0 acceptance note is carried into `pubspec.yaml` at the dependency.
Session/ratchet state is local-only — no Firebase reference anywhere in the
diff, satisfying FR-FB-002.

**Deviation #1 (tests not strictly red-first) — accepted.** §6 itself
instructed the builder to confirm real signatures against the installed
package rather than guess from Java docs, which necessarily precedes writing
meaningful tests. Tests exist, are real, and are green. The Run log documents
exactly what the package inspection found, including the two places §5's
sketch was wrong (`getIdentity` missing; `identityKey` nullable) — that is
the correct handling of rule 6, and the corrections should be folded back
into future task sketches.

**Deviation #2 (in-memory trust bookkeeping) — accepted for THIS task, but
promoted to `## Open Questions` as OQ-E03-T01-1.** The builder's reasoning
about scope is correct and inventing a fifth table would have been the wrong
call. The builder's conclusion that it "does not block T03" is not; see
OQ-E03-T01-1 for the evidence (`E03-T03.md:53` and `:101`). Recorded there
rather than as CHANGES here because the defect is in the specification, not
in the diff.

**Handoff:** E03-T02 is safe to start immediately. E03-T03 must not start
until OQ-E03-T01-1 is resolved by the planner and the human.
