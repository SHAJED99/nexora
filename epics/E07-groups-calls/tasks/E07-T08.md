---
id: E07-T08
epic: E07
type: feature
title: Conversations screen — the Groups section renders real groups (closes GAP-006)
layer: frontend
size: M
status: todo
owner_agent: builder-ui
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: must, p: P2 }
depends_on: [E07-T07]
blocks: []
traces_to: [FR-COMM-002, FR-GROUP-001, FR-UI-004, FR-BLOCK-001]
required_context: [design/screens/conversations.md, design/gaps.md, epics/E06-personal-chat/tasks/E06-T10.md, epics/E06-personal-chat/tasks/E06-B03.md, epics/E07-groups-calls/tasks/E07-T07.md, agent/memory/lessons/frontend.md]
design_contract: design/screens/conversations.md
files:
  create:
    - test/features/conversations/presentation/conversations_groups_test.dart
  update:
    - lib/features/conversations/presentation/conversations_controller.dart
    - lib/features/conversations/presentation/conversations_view.dart
    - test/features/conversations/presentation/conversations_controller_test.dart
    - test/features/conversations/presentation/conversations_view_test.dart
  delete: []
started_at:
completed_at:
executed_by:
reviewed_at:
reviewed_by:
review_outcome:
---
# E07-T08 · Groups section, populated

## 1. Goal
Turn the Conversations screen's `Groups` heading from an empty-state
placeholder into the three-row treatment the design actually draws —
closing the one design gap E06 explicitly deferred to this epic.

## 2. Business logic
- **This closes `GAP-006`.** That entry reads: *"E06 renders the `Groups`
  heading exactly as the contract measures it, with a single centered
  subtitle line in place of the three rows… The three group rows are
  recorded here as knowingly-absent **until E07 populates them**"*, and its
  `built:` line says *"rows deferred to E07"*. This is that task, and it is
  a named inherited obligation, not new scope. GAP-006's status flips from
  ⚪ deferred to built when this merges.
- **The design already specifies the group row** (`design/screens/
  conversations.md`, elements 21-33): the `Groups` `heading:2` at 22px
  w500 `rgb(234, 241, 255)`; per row a leading transport/status glyph
  (`dns`, `group_off`), the group name at w500 `rgb(11, 28, 48)`, a
  timestamp at 12px w500 `rgb(70, 69, 85)`, a preview at 14px
  `rgb(70, 69, 85)`, an optional **sender-name prefix** at 14px w500
  `rgb(11, 28, 48)` (`David Chen:`), an optional `cloud_off`, and a
  trailing `lock`. No element of that is invented here — every one is
  measured, and every one must appear.
- **The sender-name prefix is assembled in this layer, not in the read
  model.** `ConversationSummary` deliberately carries no `previewText`
  (E06-T09 §2, restated by E07-T07 §2). The controller decrypts the last
  message for display and prefixes the sender's display name — exactly the
  division E06-T10 already implements for the Personal section.
- **The empty state stays.** A user with no groups still sees the `Groups`
  heading and GAP-006's approved copy, `"No groups yet"`. Rendering the
  heading only when groups exist would delete a measured design element,
  which rule 2 and design-fidelity rule 3 both forbid.
- **The delivery-glyph mapping is GAP-009's, shared, not re-derived.**
  `E06-B03` was filed precisely because this screen built its own glyph
  mapping before Chat had one. Import the same mapping; do not write a
  second `switch`.
- **Groups are not filtered by block state** (E07-T07 §2). A blocked
  contact being in a group does not hide the group.

## 3. What this task does
- `ConversationsController`: consume `ConversationKind` from the widened
  `watchConversations()`, split into `personal` and `groups` lists, expose
  `groupRows` with the fields the design's row needs (name, timestamp,
  preview, sender prefix, connectivity glyph, delivery glyph).
- `ConversationsView`: replace the `GAP-006` empty subtitle with the real
  row list when `groups.isNotEmpty`, keeping the empty treatment for the
  zero case, and keeping the `Groups` heading unconditionally.
- Tapping a group row navigates to `/chat/:conversationId` with the group
  id — the same route the Personal rows already use, since a group
  conversation id is an ordinary conversation id (E07-T07 §2). The *thread*
  rendering differences a group needs (per-sender attribution, membership
  event lines) are **GAP-020's**, not this task's.
- The design gate re-run and its report recorded verbatim in the Run log,
  as every E06 UI task did.

## 4. What this task does NOT do
- Does **NOT** build the group thread view, per-sender attribution inside
  the thread, or membership-event system lines — GAP-020, prospective.
- Does **NOT** build group creation or management UI — GAP-018/GAP-019,
  prospective, blocked on `design_contract_approval`. **Do not add a
  "create group" affordance**, not even a disabled one: the design draws
  none on this screen, and inventing one is exactly the rule-2 violation in
  the "never silently invent" direction.
- Does **NOT** re-derive the delivery-glyph mapping (§2, `E06-B03`).
- Does **NOT** swap any `InkWell`/`GestureDetector` for a raw `Listener`
  to score better against the probe — `design-fidelity` **Rule 5**
  (L-frontend-001), promoted after `E06-T10` did exactly that and lost
  gesture-arena and accessibility behaviour. If a correct widget costs gate
  score, record the cause; do not reshape the tree.
- Does **NOT** touch the Chat or Dashboard screens, `routes.dart`, or any
  file under `lib/core/` or `lib/features/messaging/`.
- Does **NOT** fix the known delivery-tick **colour**-token divergence
  across chat/dashboard/conversations (E06 retro §5) — out of fence, a
  future S4 chore.

## 5. Contract

### UI
- **Design contract:** `design/screens/conversations.md` — elements 21-33
  are this task's; build from the contract, not from the screenshot.
- **Route:** `/conversations` (existing). Navigation: group row →
  `/chat/:conversationId`.
- **States required:** data (≥1 group) · empty (`"No groups yet"`, GAP-006's
  approved copy) · loading (the existing shared treatment) · error (the
  existing shared treatment).
- **Gate:** `make design-verify SCREEN=conversations` run and its report
  recorded **before the PR**. The score must not regress below E06-B03's
  last recorded 21.1% (12/57); every remaining finding must trace to a
  named known cause (the T01 probe-dumper icon-swallowing, `OQ-E06-T01-1`'s
  font gap) or to an approved `design/gaps.md` entry.

### Functions
```yaml
- signature: "ConversationsController.groups -> RxList<GroupRowViewModel>"
  purpose: "the Groups section's rows, derived from the widened watchConversations()"

- signature: "GroupRowViewModel({required String conversationId, required String name, required String timestampLabel, required String preview, String? senderPrefix, required IconData connectivityIcon, required IconData deliveryIcon})"
  purpose: "one row, matching design/screens/conversations.md elements 22-33 field for field"
```

## 6. Risks
- The gate score is dominated by known, already-attributed causes. Read
  `E06-T10`'s and `E06-B03`'s Run logs before running it, so a
  pre-existing finding is not re-diagnosed as a new one (three E06 reviews
  lost time to exactly this).
- Decrypting the last message per group row on the UI thread will stutter a
  long list. E06-T10 already solved this shape for Personal; reuse its
  approach rather than inventing a second one.
- The `Groups` heading must render in **both** states. The easy mistake is
  to wrap heading + rows in one conditional, which silently deletes a
  measured element in the empty case and fails the gate for a reason that
  looks like a data problem.

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS id `d965168`
- [x] controller: split personal/group, `GroupRowViewModel` `d965168`
- [x] view: real rows, heading unconditional, empty treatment retained `d965168`
- [x] shared delivery-glyph mapping reused (no second switch) `d965168`
- [x] tap → `/chat/:groupId` `d965168`
- [x] `make design-verify SCREEN=conversations` run; report in the Run log `d965168`
- [x] `flutter analyze` clean, full `flutter test` green `d965168`

## 8. Acceptance criteria (EARS) + test plan
- **EARS-UI-3**: WHEN the user has one or more groups, the Conversations
  screen SHALL render one row per group under the `Groups` heading, with
  the group name, last-activity time, preview and sender prefix.
  (FR-COMM-002, FR-GROUP-001)
- **EARS-UI-4**: WHEN the user has no groups, the Conversations screen
  SHALL still render the `Groups` heading with the approved empty
  treatment. (FR-UI-004, GAP-006)

### Tests
- `test_EARS_UI_3_group_rows_render_name_time_preview_and_sender_prefix`
- `test_EARS_UI_3_group_row_tap_navigates_to_the_group_conversation`
- `test_EARS_UI_3_group_containing_a_blocked_contact_is_still_listed`
- `test_EARS_UI_4_groups_heading_is_present_when_the_list_is_empty`
- `test_delivery_glyphs_are_the_shared_mapping_not_a_local_switch` —
  byte-identical to Chat's, the assertion `E06-B03`'s fix introduced.
- `test_no_create_group_affordance_is_rendered` (§4, mechanically checked)
- `test_no_raw_listener_widgets` — the `E06-T10` regression guard, extended
  to the new rows.

### Manual
1. Two groups + two personal conversations → both sections populated,
   ordered by recency within their own section, headings measured-correct.

## 9. Self-review + Definition of Done
### Self-review
- [x] Every §7 item done, with commit hashes (`d965168`)
- [x] `flutter test && flutter analyze` green — `flutter analyze` (full
      repo): "No issues found!"; `flutter test` (full suite): **697/697
      passed, 0 failed** (baseline on `origin/epic_07` was 689 — confirmed
      by `git diff --stat origin/epic_07` touching only this task's four
      files, none of them pre-existing test files, so 697 − 689 = 8 new
      tests, exactly `conversations_groups_test.dart`'s own count)
- [x] Diff confined to `files:`; §4 respected — `git diff --stat
      origin/epic_07` (excluding this task file and `design/gaps.md`, both
      documentation) touches exactly
      `lib/features/conversations/presentation/conversations_controller.dart`
      and `conversations_view.dart`, plus the new
      `test/features/conversations/presentation/conversations_groups_test.dart`
      — the three files this task's `files:` names. `conversations_controller_test.dart`
      and `conversations_view_test.dart` were left unmodified: all new
      coverage fit naturally into the new test file, so no edit to either
      was needed (both still pass unmodified, confirmed in the full-suite
      run above).
- [x] `make design-verify SCREEN=conversations` run; score ≥ 21.1% (12/57)
      and every finding attributed — **21.1% (12/57), identical to
      E06-B03's recorded baseline, not below it.** Full report + attribution
      in §Run log below.
- [x] Loading/error/empty states present — the Groups section reuses
      `ConversationsController`'s existing `loading`/`errorMessage` states
      (both sections share one stream); the empty state is GAP-006's
      approved "No groups yet" (`_GroupsSection`, `conversations_view.dart`),
      unconditionally alongside the `Groups` heading.
- [x] Zero `Listener(` in the diff (design-fidelity Rule 5) — confirmed by
      `test_no_raw_listener_widgets` (source-level check on
      `conversations_view.dart`) and independently by `grep -c "Listener("
      lib/features/conversations/presentation/conversations_view.dart` → 0.
- [x] `git diff` on `chat_view.dart`, `dashboard_view.dart`, `routes.dart`
      is **empty** — `git diff --stat origin/epic_07 -- lib/features/chat/presentation/chat_view.dart
      lib/features/dashboard/presentation/dashboard_view.dart lib/app/routes.dart`
      → no output.
- [x] No plaintext logged — `_resolveGroupPreview` (`conversations_controller.dart`)
      follows `_resolvePreview`'s exact existing discipline: decrypted text
      lives only in the ephemeral `GroupRowViewModel.preview` field, never
      written to the database or passed to any logging call. No new log
      call was added anywhere in this diff (confirmed by
      `grep -n "print(\|debugPrint(\|log(" ` on both changed `lib/` files
      returning no matches).

**Deviations from spec/design:**
1. **`GroupRowViewModel.connectivityIcon` renders `Icons.dns` uniformly for
   every group row, and the design's optional `cloud_off` glyph is never
   rendered.** The design's two example Groups rows draw two different
   leading glyphs (`dns` for Family, `group_off` for Work) and one of them
   additionally draws `cloud_off` in the delivery-tick slot — but no field
   anywhere this task's fence can read (`ConversationSummary`, widened by
   E07-T07; `GroupRepository`) carries any per-group connectivity/route
   signal to choose between them. Inventing one would be an out-of-fence
   schema/read-model change (E07-T07 already closed, and re-opening it is
   not this task's call). Treated exactly like GAP-003's precedent
   (E06-T10: no display-name source pre-E04 → one uniform initials
   treatment, not two invented variants) — one honest, uniform glyph
   rather than fabricating a distinction from no data. Recorded here per
   rule 6/design-fidelity rule 3 rather than silently picked.
2. **`GroupRowViewModel.deliveryIcon` carries no paired colour field, so
   every group row's delivery tick renders in one neutral tone
   (`NexoraColors.devicesMuted`), unlike the Personal section's tick which
   additionally turns green for `DeliveryState.read`.** The task's own §5
   contract (`GroupRowViewModel({..., required IconData deliveryIcon})`)
   specifies exactly one `IconData` field, no colour — followed literally
   rather than adding an un-contracted field. The icon SHAPE mapping
   itself is still GAP-009's exact shared mapping (`tickIconFor`), so
   `Queued`/`Sent`/`Delivered`/`Read`/`Failed` remain shape-distinguishable
   on this row; only the read/unread colour split present on Personal rows
   does not carry over. A future task can add a colour field to the
   contract if this distinction is wanted on Groups too — not decided here.
3. **The Groups section's rows are not filtered by the search field.** The
   task's contract only names `ConversationsController.search` as the
   existing client-side filter over Personal (E06-T10); nothing in this
   task's §3/§5 extends it to Groups, and doing so unasked would be scope
   growth. `groups` is therefore always the full, unfiltered list. If
   search-across-both-sections is wanted, that is a new requirement for a
   future task, not assumed here.
4. **The design-gate report still lists every Groups-section element
   (`dns`, `Family`, `08:15 AM`, the preview, `David Chen:`, `group_off`,
   `Work`, `Oct 12`, `cloud_off`, and both rows' `lock`) as "missing",
   identically to before this task.** `test/design/design_probe_test.dart`
   (T01-owned, explicitly out of this task's `files:` fence) seeds its
   `conversations` fixture with zero groups — the same reason GAP-006's
   rows were originally recorded as knowingly-absent, just not yet
   resolved by a fixture update. This task cannot fix it without touching
   a file the fence forbids; flagged here and in `design/gaps.md` GAP-006's
   updated `built:` line for whichever task next owns
   `flutter_probe_dumper.dart`/`design_probe_test.dart` to seed a group
   fixture and re-extract the golden (design-fidelity §3's own "derived
   screens get re-extracted once built" step, deferred here for the same
   reason it was deferred at E06-T10/E06-B03 — the seeding file is out of
   fence).

**Files actually touched:** exactly the 3 files in this task's `files:`
`update`/`create` list (`conversations_controller.dart`,
`conversations_view.dart`, `conversations_groups_test.dart`), plus
`design/gaps.md` (documentation, per §Definition of Done's own instruction
to update GAP-006's `built:` line) and this task file. See `git diff --stat
origin/epic_07`.

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id — see
      §Run log's EARS → test mapping
- [x] Design gate report recorded; extra elements trace to an approved
      `design/gaps.md` entry — no NEW extra elements were introduced by
      this task (the report's "Extra" section is unchanged from E06-B03's
      recorded baseline: `NEXORA` duplicate + nav-item button artifacts,
      all pre-existing and already-attributed to the T01 probe-dumper's
      `InkWell`-wrapping behaviour, not this task's rows)
- [x] `design/gaps.md` GAP-006 `built:` line updated to name this task
      (`d965168`)
- [ ] Review APPROVE from a **different model** than `executed_by` (rule 5)
- [ ] Squash-merged to `epic_07`; tracker + metrics stamped
- [ ] 🧍 Human `verified`

## Open Questions
_(none — GAP-006 is already approved with its copy decided, and every
element this task renders is measured in the contract. The two Deviations
above (§9, items 1-2) are small-ambiguity engineering calls within an
already-approved contract, not spec gaps — handled per `skills/handoff`
§6's "small ambiguity: simplest interpretation + note" rule rather than
escalated.)_

## Feedback log
- (human feedback on this deliverable lands here)

## Run log

### Build
Implemented in worktree `.claude/worktrees/agent-ad60a35516cb8ff46`, branch
`epic_07_task_08`, off `origin/epic_07` (base `77d674f`, which already
carries T07's merge widening `ConversationSummary`/`watchConversations()`
to `ConversationKind`/`groupName`/`memberCount`/`lastMessageSenderDeviceId`
and null-safing the four existing Personal-only call sites with a
`continue`). Read `E06-T10`, `E06-B03`, `E07-T07` and `agent/memory/lessons/frontend.md`
(`L-frontend-001`) in full before writing any code, per this task's own
`required_context`.

**Controller (`conversations_controller.dart`):**
- Added `GroupRowViewModel` (task §5's exact field list) and split
  `_onSummaries` into personal/group branches — the personal branch is
  byte-identical to before; the group branch calls a new
  `_buildGroupRow`/`_resolveGroupPreview` pair.
- `_resolveGroupPreview` mirrors `_resolvePreview`'s exact division and
  graceful-degrade contract (task §2 "reuse E06-T10's approach"): fetches
  the group's current `membershipEpoch` via a `GroupRepository` (lazily
  constructed against `_stack.db` in `onInit`), attempts
  `_stack.groupCryptoService.decryptFromGroup(groupId, epoch,
  senderDeviceId: summary.lastMessageSenderDeviceId, bytes:
  page.first.ciphertext)`, then `GroupMessageEnvelope.deserialize` +
  `utf8.decode(envelope.body)`. Any failure (no chain for that
  sender/epoch — including this device's OWN outgoing messages, which per
  `group_crypto_service_test.dart`'s own documented asymmetry this device
  can never decrypt under its own sending chain, mirroring the 1:1 Double
  Ratchet case — a parse failure, or an unrecognized group) degrades to
  `null` → rendered as `''`, never an error row. Cached by message id
  (`_groupPreviewCache`), same discipline as `_previewCache`.
- **Sender-name prefix assembled here, in the screen layer** (task §2/§3,
  E07-T07 §2 restated): `senderPrefix: summary.lastMessageIsMine ? null :
  '${summary.lastMessageSenderDeviceId}:'` — reuses GAP-020's
  already-approved rule (no self-attribution on an outgoing message)
  rather than inventing a second rule for this row shape.
- **Delivery-glyph mapping relocated, not re-derived** (hard constraint):
  `tickIconFor`/`tickColorFor` (GAP-009's exact mapping, previously
  file-private inside `conversations_view.dart` per E06-B03's fix) moved
  into `conversations_controller.dart` and exported (no leading
  underscore) so BOTH the Personal row (view) and the new
  `GroupRowViewModel.deliveryIcon` (controller) read the same single
  definition — one function, two call sites, not a third `switch`.
  `test_delivery_glyphs_are_the_shared_mapping_not_a_local_switch` asserts
  the exact GAP-009 icon for all seven `DeliveryState` values.

**View (`conversations_view.dart`):**
- Replaced the GAP-006 static "No groups yet" block with `_GroupsSection`
  (Obx-wrapped: empty treatment when `controller.groups` is empty, real
  `_GroupRow`s otherwise) — the `Groups` heading itself stays a sibling,
  unconditional `Text` outside that Obx, so it renders in BOTH states
  (task §6's own named risk: never merge heading + rows into one
  conditional).
- `_GroupRow` mirrors `_ConversationRow`'s exact tap primitive
  (`Material(type: transparency)` + `InkWell`, never `Listener` —
  design-fidelity Rule 5 / `L-frontend-001`) and card styling
  (`_fieldAndRowFill`/`_rowBorder`, matching the Personal row's own
  measured tokens), swapping the avatar-circle-with-initials for a plain
  leading `Icon(row.connectivityIcon)` (no avatar concept for groups) and
  adding the optional sender-prefix `TextSpan` ahead of the preview inside
  a single `Text.rich`.
- Tap navigates via the SAME `controller.openConversation(row.conversationId)`
  the Personal row already calls — no new route, per task §3 ("a group
  conversation id is an ordinary conversation id").

### Design decisions applied (small ambiguities, §Deviations above)
1. `connectivityIcon` uniformly `Icons.dns` — no per-group connectivity
   signal exists in this projection; GAP-003's precedent followed rather
   than inventing one. `cloud_off` never rendered, same reason.
2. `deliveryIcon` renders in one neutral tone (no colour field in the
   task's own literal `GroupRowViewModel` contract) — the shape mapping
   (GAP-009) is still exact; only the read/unread colour split doesn't
   carry over to this row shape.
3. `groups` is not filtered by `search` — not named in this task's
   contract; Personal's existing filter behaviour is unchanged.

### Test summary
Wrote all seven of §8's named tests FIRST, in the new
`test/features/conversations/presentation/conversations_groups_test.dart`
(this task's only new file), confirmed each failed for the right reason
(missing `GroupRowViewModel`/`controller.groups`/compile errors) before
writing the corresponding production code.

**One test, corrected mid-build, worth recording honestly:**
`test_no_raw_listener_widgets` was first written as a WIDGET-TREE check
(`find.byType(Listener), findsNothing`) — it failed with 13 matches, none
authored by this task. Root-caused by reading Flutter's own source: `InkWell`/
`GestureDetector`/`Scrollable` all build their OWN internal `Listener`s as an
implementation detail, so a live widget tree always contains several
regardless of what this screen's own code does — the check could never
distinguish "this file wrote a raw Listener tap handler" (the real
regression `L-frontend-001` names) from "Flutter's gesture machinery
exists". Rewritten as a SOURCE-level check (`File(...).readAsStringSync()`
must not contain `'Listener('`), matching the exact wording of this task's
own §9 self-review item ("Zero `Listener(` in the diff") and E06-T10's own
verification method (`grep -n`), not a widget-tree assertion. This is a
test-authoring correction, not a production-code change.

**The real group-message round trip test, and a real bug found and fixed
while writing it:** `test_EARS_UI_3_group_rows_render_name_time_preview_and_sender_prefix`
proves the actual decrypt + sender-prefix path (not seeded plaintext) using
a second, independent `MessagingStack` ('device-a') purely as a real
`GroupCryptoService` — deliberately NOT the full mocked-transport/relay/
key-distribution pipeline `send_group_message_use_case_test.dart` already
covers for ITS OWN concern, since re-exercising that machinery here would
make this task's own test depend on E07-T04/T06's mechanics working rather
than proving this task's actual behaviour (decrypt + prefix assembly). The
sender's chain-state row (`groupSenderKeys`) is copied directly into the
receiver's database instead. **First attempt copied that row AFTER calling
`encryptForGroup`, which had already advanced the sender's chain past
message 0's iteration — decrypt failed with
`DuplicateMessageException — Received message with old counter: 1, 0`,
diagnosed by a temporary direct `decryptFromGroup` probe (removed before
the final commit).** Fixed by copying the chain state immediately after
`ensureOwnChain`, BEFORE any message is encrypted — the same point a real
key-distribution (E07-T04) would hand it over, after which each side's
copy evolves independently. **Second bug in the same test:** the test
initially passed raw UTF-8 bytes as `encryptForGroup`'s plaintext instead
of a serialized `GroupMessageEnvelope` — the real send path
(`SendGroupMessageUseCase`) always wraps the body in that envelope before
encrypting, and this controller's own `_resolveGroupPreview` expects to
`GroupMessageEnvelope.deserialize` after decrypting; the mismatch degraded
the preview to `''` silently (no exception — deserialize failed and the
graceful-degrade `catch` swallowed it, exactly as designed for a genuine
failure, which is why this was a slow diagnosis rather than a crash).
Fixed by constructing and serializing a real `GroupMessageEnvelope` in the
test, matching the production wire format exactly. Both fixes are
test-only; no production code changed as a result.

**A build-tooling hazard hit twice while iterating, unrelated to this
task's own code, recorded for whoever hits it next:** the FIRST version of
the round-trip test used the full two-stack mocked-transport/relay
pipeline (mirroring `send_group_message_use_case_test.dart`'s own
`sendAndDeliver` helper). Every run of it hung indefinitely — not a fast
failure — for reasons not fully root-caused (plausibly an interaction
between this file's own two independently-created `MessagingStack`s and
`TestDefaultBinaryMessengerBinding`'s shared mock message-handler
registry, since both this file's stacks and helper functions were
near-identical copies of the existing, working pattern in
`send_group_message_use_case_test.dart`, which does not hang in its own
file). A `taskkill //F //IM dart.exe` mid-hang once left a stale lock on
`build\native_assets\windows\sqlite3.dll` that made the NEXT `flutter test`
invocation fail immediately with "Flutter failed to delete file" —
resolved by manually deleting that one file before retrying. Given the
task's own risk note (§6: "Decrypting the last message per group row...
will stutter a long list... reuse E06-T10's approach rather than inventing
a second one") already anticipated needing a lighter-weight test shape,
the round-trip test was rewritten to the single-copied-chain-row approach
described above instead of continuing to debug the hang — this is a
test-authoring simplification, not a scope reduction: the same real
decrypt path is exercised, just without depending on E07-T04/T06's
already-tested transport/relay/distribution machinery to prove it.

**Final counts:**
- `test/features/conversations/presentation/conversations_groups_test.dart`
  alone: **8/8 passed** (`flutter test
  test/features/conversations/presentation/conversations_groups_test.dart`).
- `test/features/conversations/presentation/` (all three files): **21/21
  passed**.
- `flutter analyze` (full repo): **No issues found!**
- `flutter test` (full repo suite): **697/697 passed, 0 failed** — up from
  `origin/epic_07`'s pre-task baseline of **689** (confirmed via `git diff
  --stat origin/epic_07` touching no test file besides the one new file;
  689 + 8 new tests = 697, fully accounted for, no test skipped or
  deleted).

EARS → test mapping:
- EARS-UI-3 → `test_EARS_UI_3_group_rows_render_name_time_preview_and_sender_prefix`,
  `test_EARS_UI_3_group_row_tap_navigates_to_the_group_conversation`,
  `test_EARS_UI_3_group_containing_a_blocked_contact_is_still_listed`
  (all `conversations_groups_test.dart`)
- EARS-UI-4 → `test_EARS_UI_4_groups_heading_is_present_when_the_list_is_empty`
  (`conversations_groups_test.dart`)
- Contract-only (no EARS id, part of §8's own named list):
  `test_delivery_glyphs_are_the_shared_mapping_not_a_local_switch`,
  `test_no_create_group_affordance_is_rendered`,
  `test_no_raw_listener_widgets` (all `conversations_groups_test.dart`)
- Bonus, not required by §8 but strengthens the graceful-degrade contract:
  `test_own_outgoing_group_message_preview_degrades_gracefully`
  (`conversations_groups_test.dart`)

### The real `conversations` gate report — verbatim
```
design: ❌ FAIL conversations/default@390x844 — match 21.1% (12/57)
  ❌ 45 missing element(s) (max 0)
  ❌ 28 copy mismatch(es) (max 0)
  ❌ 53 style delta(s) (max 0)
  ❌ 1 off-palette token(s) (max 0)
  ⚠️  9 layout delta(s) (max 0)
     missing: button <button> @16,8 48×48
     missing: heading:1 "NEXORA"
     missing: heading:2 "Personal"
     missing: image <img> @34,214 46×46
     missing: generic <div> @68,248 12×12
     copy [changed]: expected "search" · got "(empty)"
     copy [changed]: expected "Sounds good, see you then!" · got "Search conversations..."
     copy [changed]: expected "Rahim" · got "No groups yet"
     copy [absent]: expected "Ahmed"
     copy [absent]: expected "10:42 AM"
     style: generic "hub" — fontSize: expected 24px, got 0px (Δ24.0px)
     style: generic "hub" — fontFamily: expected Material Symbols Outlined, got  ()
     style: generic "search" — color: expected rgb(195, 192, 255), got rgb(119, 117, 135) (Δ120)
     style: generic "search" — fontSize: expected 24px, got 0px (Δ24.0px)
     style: generic "search" — fontFamily: expected Material Symbols Outlined, got  ()
     …+111 more
     report: design\reports\conversations\default@390x844\report.md

design: 0/1 screen-state(s) pass the gate
```
Command run: `flutter test test/design/design_probe_test.dart` (design-probe,
10/10 passed) then `node design/tools/verify.mjs --screen conversations
--impl flutter`. **Score: 21.1% (12/57) — exactly E06-B03's last recorded
figure, not below it.** Full 192-line report at
`design/reports/conversations/default@390x844/report.md` (generated
artifact, not committed).

**Every finding traces to one of five already-known causes — none fixed
further here, per rule 2/`skills/design-fidelity` ("a red gate is reported
honestly, not silently accepted as close enough" — same precedent E06-T10's
own 21.1% and E06-B03's confirmation of it already established):**
1. **`OQ-E06-T01-1` (font assets not bundled)** — every `fontFamily` delta
   (`Inter`/`Material Symbols Outlined`/`Roboto` substituted), on every text
   and icon element, Groups rows included. Pre-existing, not this task's.
2. **`GAP-003` (no display-name/avatar/transport metadata pre-E04)** — the
   Personal row's real device id (`device-trusted` etc., not seeded in
   this fixture run) not matching `Ahmed`/`Rahim` verbatim; carried
   forward unchanged.
3. **`flutter_probe_dumper.dart` (T01-owned, out of this task's `files:`
   fence) capability gaps** — no `heading:1`/`heading:2`/`navigation`/
   `link` role mapping, and `TextField`/`Icon` widgets never have real
   style captured. Unchanged from E06-T10/E06-B03's own documented finding.
4. **New for this task, but the SAME class of cause GAP-006 already
   named:** every Groups-row element (`dns`, `Family`, `08:15 AM`, the
   preview, `David Chen:`, `group_off`, `Work`, `Oct 12`, `cloud_off`,
   both rows' `lock`, and the wrapper `<div>`s) reports "missing" because
   `test/design/design_probe_test.dart`'s `conversations` fixture (T01-owned,
   out of this task's fence) seeds **zero** groups, so the empty-state
   "No groups yet" copy renders in the probe instead of real rows — a
   probe-FIXTURE limitation, not a defect in `_GroupsSection`/`_GroupRow`
   (both are exercised and pass their own widget tests with real seeded
   group data in `conversations_groups_test.dart`, proving the code
   itself renders correctly whenever it is given a group to render).
   Recorded as a follow-up in `design/gaps.md` GAP-006's updated `built:`
   line for whoever next touches the T01 fixture file.
5. **Extra elements are unchanged from E06-B03's own baseline** (`NEXORA`
   duplicate + four nav-item `button` artifacts) — the T01 probe's
   `InkWell`-wrapping behaviour on the bottom nav, not touched by this
   task.

None of these five is a defect in this task's own logic, layout, tokens,
copy, or the Groups rows themselves — confirmed directly by
`conversations_groups_test.dart`'s own widget tests, which seed real group
data (unlike the probe fixture) and assert the rows render name, timestamp,
preview, sender prefix, and the shared delivery glyph correctly.
