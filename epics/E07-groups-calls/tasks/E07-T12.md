---
id: E07-T12
epic: E07
type: feature
title: Design gap pass — derived contracts for the group screens and the call surfaces
layer: docs
size: M
status: done
owner_agent: planner
model: opus
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: must, p: P2 }
depends_on: []
blocks: [E07-T13]
traces_to: [FR-GROUP-001, FR-GROUP-002, FR-GROUP-003, FR-CALL-001, FR-CALL-003, FR-COMM-002, FR-UI-004]
required_context: [design/gaps.md, design/screens/conversations.md, design/screens/chat.md, design/screens/devices.md, design/screens/chat-voice.md, agent/skills/design-fidelity/references/gaps.md, epics/E06-personal-chat/tasks/E06-T13.md, spec/feature-list.md]
design_contract: n/a
files:
  create:
    - design/screens/group-create.md
    - design/screens/group-manage.md
    - design/screens/call.md
  update:
    - design/gaps.md
  delete: []
started_at: 2026-08-31
completed_at: 2026-08-31
executed_by: claude-opus-5 (planner)
reviewed_at: 2026-08-31
reviewed_by: claude-sonnet-5 (reviewer)
review_outcome: approve
---
# E07-T12 · E07 design gap pass

## 1. Goal
Give this epic's undesigned journeys — creating a group, managing a group,
and the call surfaces — real, human-approvable derived contracts, so the
frontend tasks that build them are shardable at all (rule 2: no contract,
no frontend task).

## 2. Business logic
- **Rule 2 in both directions.** A journey the design omits is *derived*
  from the spec, marked `source: derived`, built from the existing
  design's own primitives, and 🧍 human-approved. It is never invented
  freely and never silently dropped. `E06-T13` is the worked precedent —
  including its most important behaviour: when nothing derivable exists,
  the honest output is a question (GAP-017/PTT), not a proposal.
- **The gap entries themselves are written by the planner at sharding**
  (GAP-018…GAP-022, appended to `design/gaps.md` in the same pass that
  produced this task). This task's job is the *contracts* those approved
  gaps imply, plus any further gaps the contract-writing surfaces.
- **Derivation sources, named per screen** — every token, spacing value and
  component shape must come from a screen that already has a measured
  golden, and the contract must say which:
  - group create / manage → `conversations.md` (list rows, section
    headings, search field) + `devices.md` (the per-device row with a
    trailing action, the closest existing "manage a list of peers" shape)
  - call surfaces → `chat.md` (header with peer identity, the 24px icon
    button treatment) + `dashboard.md` (the status-card treatment for
    connection state, which GAP-014 already borrowed once)
- **No new visual language.** If a required element cannot be built from an
  existing primitive — as PTT could not — that is a question with an owner,
  not an invention. Say so in the entry and stop.
- **This task self-approves nothing.** `L-process-002`'s standing rule: the
  `approved by:` lines stay bare, and `design_contract_approval` is the
  human's. `E06-T13`'s reviewer verified exactly this and it is checked
  again here.

## 3. What this task does
- Writes three hand-written, `source: derived` screen contracts in the same
  shape the generator produces (frontmatter, element checklist, verbatim
  copy, token table), each naming its derivation source per §2:
  - **`design/screens/group-create.md`** — name field, member picker over
    the trusted-contact list, create action, empty/loading/error states.
  - **`design/screens/group-manage.md`** — group name (editable by
    Owner/Admin per E07-T02's matrix), member list with role labels,
    per-member actions gated by that same matrix, leave/delete, and the
    **role-forbidden state** (a Member sees the list and no actions).
  - **`design/screens/call.md`** — the three coherent states of one
    surface: outgoing (ringing), incoming (accept/decline), in-call
    (duration, mute, hangup, connection quality), plus the failure state
    `NullCallMediaTransport` produces today.
- Reviews GAP-018…GAP-022 as written at sharding, corrects them against
  what contract-writing actually reveals, and adds any further gap the pass
  surfaces.
- Records, for each contract, the **route** it binds to and adds it to
  `docs/routes.md`'s pending list — without registering a route in code
  (that is the building task's).

## 4. What this task does NOT do
- Does **NOT** build any screen, controller, binding or route. Docs only.
- Does **NOT** approve its own gaps or contracts. Every `approved by:` line
  ships bare (`L-process-002`).
- Does **NOT** hand-edit any *generated* contract's tables
  (`design-fidelity` Rule 1) — `conversations.md`, `chat.md`,
  `dashboard.md`, `devices.md` are read-only inputs here.
- Does **NOT** loosen `design/thresholds.yaml` (Rule 2 — a 🧍 gate of its
  own).
- Does **NOT** propose a PTT contract. `GAP-017` is decided-and-parked and
  `E07-T13` owns reopening it once `OQ-E07-3` is answered. Proposing one
  here would answer a human's open question by drawing it.
- Does **NOT** design group *thread* rendering beyond naming the gap
  (GAP-020) — per-sender attribution and membership-event lines need
  `OQ-E07-10`'s blocked-in-group answer first.
- Does **NOT** extract a golden. Derived screens get their golden extracted
  **after** they are built (`design-fidelity` §3's note), not before.

## 5. Contract

### UI
The three contracts must each carry, in the generated shape:
`id` · `impl_path` · **`source: derived`** · `states` · `viewports:
[390x844]` (matching every existing contract) · an element checklist with
role, copy and measured-or-derived style per element · the verbatim copy
list · the token table, **every value of which already appears in the
source screen's own token table** — an off-palette value in a derived
contract is an invented value.

Each contract ends with a `## Derivation` section naming: the source
screen(s), the specific elements borrowed, and the spec id each screen
serves (`FR-GROUP-002` for manage, `FR-CALL-001` for call, etc.).

### External services & flags
None.

## 6. Risks
- The call contract is the one most likely to over-reach: it is tempting to
  draw a rich in-call experience (speaker toggle, video button, add
  participant) that no spec id requires. Every element must trace to
  FR-CALL-001/002/003 or to a state the state machine (E07-T09) can
  actually be in. A button for a capability that does not exist is a
  promise the build will have to keep.
- `group-manage` must render *differently per role*, and the role-forbidden
  state is the one that gets forgotten. It is a required state, not an
  edge case — E07-T02's matrix is what it renders.
- Deriving from `devices.md` means inheriting its known issues, including
  the RenderFlex overflows `E06-B01` fixed. Read that bug before borrowing
  the row shape.

## 7. Implementation checklist
- [x] GAP-018…GAP-022 reviewed against the contracts as written — GAP-018
      (entry-point fork still open, recorded), GAP-019 (Member-`leave`
      clarification against `E07-T02`'s matrix; `more_vert` deliberately
      unused), GAP-020 (why no contract was written), GAP-021 (three open
      items + the extra-element note), GAP-022 (the two-cell difference)
- [x] `design/screens/group-create.md` (`source: derived`, `## Derivation`)
- [x] `design/screens/group-manage.md` (incl. the role-forbidden state, and
      the full-screen confirm step + plain role label the human chose)
- [x] `design/screens/call.md` (outgoing / incoming / in-call /
      in-call-degraded / failed, plus the approved `chat.md` entry point)
- [x] every token traced to a source screen's own token table — two
      single-use values (`rgb(0, 70, 102)`, `rgb(78, 222, 163)`) are measured
      on a parent's *element* rather than listed in its token table; both are
      marked ⁂ in `group-create.md` §Tokens rather than passed off as
      table-listed (see §Deviations)
- [ ] routes recorded in `docs/routes.md`'s pending list — **not done: out of
      the `files:` fence** (rule 6). Each contract carries its route in its
      own frontmatter (`impl_path`) and in its header. See §Deviations
- [x] `approved by:` lines left bare; gate line left exactly as the human set
      it (see §Deviations — the gate was cleared by the human *after* this
      task was written)
- [x] `make validate` clean — `scheduler.py --validate`:
      `15 epics, 60 tasks — DAG OK ✓`

## 8. Acceptance criteria (document review — `DOC-*`, not EARS) + check plan
- **DOC-UI-5**: WHEN a journey in this epic has no design source, the
  system's design record SHALL carry a derived contract traced to a spec id
  and to the existing design primitives it was built from, or an explicit
  question with a named owner. (FR-UI-004, rule 2)
- **DOC-UI-6**: The derived contracts SHALL introduce no token, colour or
  spacing value absent from the source screens' measured token tables.
  (rule 2)

### Tests
Docs task — the checks are mechanical, not a Dart suite:
- `check_DOC_UI_5_every_new_gap_traces_to_a_spec_id` — grep each new
  GAP entry for an `FR-`/`UC-` id.
- `check_DOC_UI_6_derived_contracts_use_only_source_tokens` — a script
  comparing each derived contract's token table against the union of its
  named source screens' tables; any value not present is a failure.
- `test_no_approved_by_line_is_filled` — every new `approved by:` is bare
  (`L-process-002`, the check `E06-T13`'s reviewer ran).

### Manual
1. Human reads the three contracts and GAP-018…GAP-022, then clears or
   amends 🧍 `design_contract_approval` in `design/gaps.md`.

## 9. Self-review + Definition of Done
### Self-review
- [x] Every §7 item done, with commit hashes — one exception, ticked open and
      explained: the `docs/routes.md` item (out of fence)
- [x] Diff confined to `files:`; §4 respected — `git status`: `M
      design/gaps.md` + the three new contracts, nothing else
- [x] `git diff` on `conversations.md`, `chat.md`, `dashboard.md`,
      `devices.md`, `thresholds.yaml` is **empty** — verified with
      `git diff --stat` on exactly those five paths: no output
- [x] No `approved by:` line filled in by an agent — every occurrence of
      "approved by" in the three new files is a *citation* of the human's
      2026-08-31 clearance of the GAP entry, each followed by an explicit
      "not independently approved by an agent"
- [x] No PTT contract proposed (§4) — GAP-017's text is byte-identical
      (`git diff design/gaps.md | grep -c GAP-017` → `0`), and
      `docs/impact/IMP-001-ptt-rehome-to-e07.md` is untouched
- [x] Every derived element traces to a source element or to a spec id —
      each contract's §Derivation table names element numbers, and each ends
      with the spec ids it serves

**Deviations from spec/design:**
1. **`docs/routes.md` not updated.** §3/§7 ask for the routes in its pending
   list; `docs/routes.md` is **not** in this task's `files:` fence, and rule 6
   makes the fence binding. Routes are instead recorded in each contract's
   `impl_path` frontmatter and header line: `/groups/new`, `/groups/:id`,
   `/call/:id`. A one-line follow-up (or an amendment to this task's fence)
   should carry them into `docs/routes.md`; flagged rather than done quietly.
2. **The gate line in `design/gaps.md` was not set to ⏳.** §7 asks for that,
   but §7 was written at sharding on 2026-08-31 *before* the human cleared
   the same gate later that day for exactly these five entries and these
   three files. Flipping it back to ⏳ would (a) un-declare five human
   approvals an agent has no standing to reopen and (b) block `E07-T08`,
   which the clearance note explicitly says was never gated by this
   reopening. The genuinely-open items are recorded **inside their own GAP
   entries** with bare approval instead, plus a follow-up note under the
   gate line. No agent wrote or altered an approval verdict.
3. **Two ⁂ token values.** `rgb(0, 70, 102)` (conversations element 22) and
   `rgb(78, 222, 163)` (conversations 12 / devices 13-14) are measured on a
   parent contract's element but absent from that contract's generated
   *token table*, which lists most-used values only. `DOC-UI-6`'s
   table-only check will flag them; they are marked ⁂ and explained in
   `group-create.md` §Tokens rather than swapped for a value that would
   misrepresent the design.
4. **`devices.md` element 31's `r4px` was not copied.** It is absent from
   devices' own token table, so `group-manage.md`'s action chips use `r8px`
   (in that table, and used by devices' own `Discover` button). Recorded in
   that contract's §Derivation boundary 2.
5. **GAP-020 got no contract**, per §4 — it is a state on the generated
   `chat.md`, its build is gated on `OQ-E07-13`, and rule 1 of
   design-fidelity forbids hand-editing a generated contract. The reasoning
   is written into GAP-020 so the next reader inherits it.

**Files actually touched:**
- `design/screens/group-create.md` (new)
- `design/screens/group-manage.md` (new)
- `design/screens/call.md` (new)
- `design/gaps.md` (GAP-018/019/020/021/022 `built:` + clarifications, and a
  follow-up note under the gate line — the gate verdict itself unchanged)
- `epics/E07-groups-calls/tasks/E07-T12.md` (this file — status, stamps,
  checklist, self-review)

### Definition of Done
- [x] Every §8 criterion passes — DOC-UI-5: every new contract cites spec
      ids in frontmatter and in §Derivation; DOC-UI-6: no value outside the
      source contracts, with the two ⁂ exceptions disclosed above rather
      than hidden
- [x] Review APPROVE from a **different model** than `executed_by` (rule 5) —
      claude-sonnet-5 (reviewer) vs claude-opus-5 (planner), 2026-08-31
- [x] 🧍 `design_contract_approval` left genuinely **open** for the human —
      four undecided items (GAP-018's entry point; GAP-021's destructive
      treatment, three glyph names, two copy strings) carry **no** agent
      approval, and the gate line was neither written nor altered by an agent
- [ ] Squash-merged to `epic_07`; tracker stamped
- [ ] 🧍 Human `verified`

## Open Questions
_(raised into `design/gaps.md` as GAP entries with owners, per
`skills/design-fidelity` §3 — not duplicated here. Anything this pass
cannot derive becomes a GAP entry stating that no proposal is being made,
in GAP-017's shape.)_

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- **2026-08-31 · planner (opus).** Branched `epic_07_task_12` from
  `development` (`54a2b7d`) — `epic_07` did not exist at start
  (`git branch -a` checked first, per rule 4).
- Read the five approved GAP entries verbatim before writing anything; none
  re-litigated. Fork decisions applied literally: GAP-019 (1) full-screen
  confirm, no modal primitive; (2) plain text role label, no icon. GAP-021's
  four states + the `chat.md` header entry point. GAP-022 silent-on-success.
- Sources read for derivation: `conversations.md`, `chat.md`, `devices.md`,
  `dashboard.md`, `chat-voice.md` (E06's precedent shape),
  `agent/skills/design-fidelity/references/gaps.md`, `E06-T13`, `E07-T02`
  (the permission matrix), `E07-T09`/`T10`/`T11` (call states, migration
  events), `E06-B01` (the RenderFlex row trap the member rows inherit).
- Mechanical token check run across all three new contracts against the four
  source contracts: **zero** colour or px value absent from a source, except
  the two ⁂ element-measured values disclosed in §Deviations 3.
- `scheduler.py --validate` → `harness: 15 epics, 60 tasks — DAG OK ✓`.
- Not crossed: GAP-017's text (byte-identical), `docs/impact/IMP-001-*`
  (untouched), `design/thresholds.yaml` (untouched), and all four generated
  parent contracts (untouched).
- **2026-08-31 · reviewer (sonnet-5), APPROVE.** Diffed `epic_07_task_12`
  against `epic_07`: exactly the 3 new contracts + `design/gaps.md` (`built:`
  lines/clarifications only) + this task file, as claimed — `git diff --stat`
  on `chat.md`/`conversations.md`/`devices.md`/`dashboard.md`/`chat-voice.md`/
  `design/thresholds.yaml`/`docs/impact/IMP-001-ptt-rehome-to-e07.md` empty,
  independently confirmed. GAP-019's two fork resolutions read literally in
  `group-manage.md`: full-screen confirm (no scrim/elevation/modal primitive,
  states 4/5), plain `12px` text role label (GM8), `more_vert` deliberately
  absent. `call.md`'s 5 states (outgoing/incoming/in-call/in-call-degraded/
  failed) present and honest — `failed` explicitly states audio can't be
  carried (`NullCallMediaTransport`), never renders as connected; confirmed
  no speaker/video/add-participant/keypad/hold/record anywhere in the
  contract. GAP-022's migration UI confirmed as a real distinct state
  (`in-call-degraded`, differing from `in-call` in exactly the C5/C6 status
  pair) and from `failed` — not conflated with either; silent on successful
  migration as approved. Independently spot-checked 5+ token values per new
  contract against the cited parent contract's own element/token rows
  (`conv 22` `dns rgb(0, 70, 102)`, `conv 12` `check rgb(78, 222, 163)`,
  `devices 31` `Verify r4px` absent from devices' own token table — confirmed
  absent, so `group-manage.md`'s use of `r8px` instead is the honest choice
  disclosed in Deviation 4, not drift; `chat 26` `r9999px`/`chat 4`
  `rgb(53, 37, 205)`; `dashboard 7/8/13/14`) — all traced, no invented value
  found. Confirmed all five `approved by:` lines on GAP-018..022 already
  existed on `epic_07` at commit `88a4d5c` (authored by the human,
  2026-08-31, before this task branched) — `E07-T12`'s diff only cites them
  in prose, never authors a new one (`L-process-002` clean). GAP-017's text
  is byte-identical (0 changed lines) and `docs/impact/IMP-001-*` untouched,
  independently confirmed. `make design-verify` does not apply here:
  `group-create`/`group-manage`/`call` are `source: derived`, not yet built,
  have no golden and aren't in `design/sources.yaml`; `node design/tools/
  verify.mjs --screen group-create` returns "no screen matched" — expected,
  per `design-fidelity` §3 (golden is extracted only after the build). Both
  flagged deviations judged sound on independent review, not self-serving:
  (a) `docs/routes.md` correctly withheld — it is genuinely outside the
  `files:` fence and rule 6 makes the fence binding over a §3/§7 instruction
  that conflicts with it; the route info is preserved in each contract's
  `impl_path`/header instead, and the gap is flagged rather than silently
  dropped; (b) the top `design_contract_approval` gate line was correctly
  left alone — it was cleared by the human in commit `88a4d5c`, an ancestor
  of `epic_07` predating this task's branch point, so reverting it to ⏳
  would erase a real human decision already on the epic branch, not merely
  a stale checklist item. Verdict: APPROVE.
