---
id: E07-T16
epic: E07
type: task
title: "Conversations header: the create-group entry point, so /groups/new is reachable by a user rather than only by direct navigation"
layer: frontend
size: XS
status: todo
started_at:
completed_at:
executed_by:
reviewed_at:
reviewed_by:
review_outcome:
owner_agent: builder-ui
model: any
preferred_agent: any
token_estimate: { tier: S, range: "10k-30k" }
priority: { moscow: must, p: P1 }
depends_on: [E07-T15, E07-B05]
blocks: []
traces_to: [FR-GROUP-001, FR-COMM-002, FR-UI-001]
required_context:
  - design/screens/group-create.md
  - design/screens/conversations.md
  - design/gaps.md
design_contract: design/screens/conversations.md
files:
  create:
    - epics/E07-groups-calls/tasks/E07-T16.md
  update:
    - lib/features/conversations/presentation/conversations_view.dart
    - lib/features/conversations/presentation/conversations_controller.dart
    - test/features/conversations/presentation/conversations_groups_test.dart
    - design/screens/group-create.md
    - design/gaps.md
  delete: []
---
# E07-T16 · The create-group entry point

## 1. Goal
`/groups/new` was built and merged (`E07-T15`, #325) and fixed (`E07-B05`,
#332), and until now **no screen linked to it**. This task adds the one
affordance that makes the group journey startable by a user.

## 2. The decision this implements
`design/screens/group-create.md` §Open proposed the affordance and left it
undecided; `GAP-018`'s clearance explicitly did not cover it.

**Human decision, 2026-09-25 — approved as proposed:** a 48×48 `r9999px`
button in the `conversations.md` header carrying the existing `add` glyph at
`24px` `rgb(195, 192, 255)`, with the instruction to *"treat the resulting
measured-contract delta honestly; do not edit the contract merely to conceal
it."*

## 3. Contract
- `_Header` (elements 1-5) gains one trailing icon button, in the same
  `Material`/`InkWell`/`SizedBox(48,48)` shape the `search` action already
  uses. `Icons.add`, `size: 24`, `NexoraColors.welcomeHeading`
  (`rgb(195, 192, 255)`) — every value already in use on this screen.
- `ConversationsController.openCreateGroup()` navigates to
  `Routes.groupCreate`.
- Ordering: `search` then `add`, so the existing element indices 4-5 keep
  their positions and the new element is appended rather than inserted.

## 4. What this task does NOT do
- Does **NOT** hand-edit `conversations.md`'s measured element table to add
  the new element. It is a *measured* contract; `design-fidelity` rule 1 and
  `E07-T12` §4 both forbid that, and the human's decision says so explicitly.
  The resulting extra-element finding is expected and is traced to
  `group-create.md` §Open / `GAP-018`.
- Does **NOT** change any other element, colour, spacing or copy on
  `conversations.md`.
- Does **NOT** add a FAB or a row inside the `Groups` section — both were
  considered and rejected in `group-create.md` §Open.
- Does **NOT** touch the group thread (`GAP-020`) or group row tap behaviour
  (`E07-B01`).

## 5. Acceptance criteria (EARS)
- **EARS-GROUP-21**: WHEN the user taps the create-group affordance in the
  Conversations header, the system SHALL navigate to `/groups/new`.
  (`FR-GROUP-001`)

## 6. Test strategy
`E07-B05` shipped because every test asserted against an injected navigation
seam and nothing exercised the real destination. This task does not repeat
that:

- the tap test asserts `Get.currentRoute` is `Routes.groupCreate` after a
  real tap on a real widget, **and**
- asserts that `appPages` — the application's own route table — actually
  registers a page under that name. A route string that matches nothing is
  a silent no-op in GetX, which is exactly how an inert affordance would
  pass a weaker test.

## 8. The design-gate measurement, stated honestly

Both runs are real, on this branch, with and without the button
(`make design-probe` + `make design-verify SCREEN=conversations IMPL=flutter`):

| | match | missing | copy | style | layout |
|---|---|---|---|---|---|
| without the button | **38.6%** (22/57) | 35 | 16 | 47 | 14 |
| with the button | **40.4%** (23/57) | 34 | 16 | **51** | **16** |

**The headline going up is not a win, and must not be read as one.** The
verifier's positional matcher absorbed the new button into a measured-but-
unbuilt `button @281,9 48×48` slot and aligned its `add` glyph against the
expected copy `10:42 AM` — an artifact, not a fidelity improvement. The real
cost of the change is **+4 style deltas and +2 layout deltas**.

The screen fails the gate either way and did before this task: 35 of its 57
measured elements are missing because `conversations.md`'s golden contains
**mock conversation data** (`Ahmed`, `Rahim`, `Sounds good, see you then!`)
that a real build has no way to produce. That is a pre-existing,
separately-owned problem — not this task's regression and not this task's to
fix. Nothing here edits the contract to improve either number.

## 7. Definition of Done
- [x] The button renders in the header with the specified geometry and colour
      — asserted as a real 48×48 box in the widget tree, not as a claim
- [x] Tapping it reaches the real registered `/groups/new` route — asserted
      against `Get.currentRoute` **and** against the real `appPages` table.
      Falsified: with the route string typo'd, GetX silently stayed on
      `/conversations` (`Expected: '/groups/new'` / `Actual: '/conversations'`)
      — which is exactly the silent no-op a weaker test would have missed
- [x] `flutter analyze` clean; the 25 conversations tests green
- [x] `make design-verify SCREEN=conversations` run **twice**, with and
      without the button, and the delta reported in §8 rather than hidden
- [ ] Independent review (rule 5)
