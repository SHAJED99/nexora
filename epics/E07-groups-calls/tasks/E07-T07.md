---
id: E07-T07
epic: E07
type: feature
title: Conversation read model — widen it to groups without forking it
layer: backend
size: S
status: todo
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: must, p: P2 }
depends_on: [E07-T06]
blocks: [E07-T08]
traces_to: [FR-COMM-002, FR-GROUP-001, FR-MSG-004, FR-BLOCK-001]
required_context: [epics/E06-personal-chat/tasks/E06-T09.md, epics/E07-groups-calls/tasks/E07-T01.md, epics/E07-groups-calls/tasks/E07-T06.md, design/screens/conversations.md, docs/conventions.md, lib/features/messaging/data/conversation_repository.dart]
design_contract: n/a
files:
  create: []
  update:
    - lib/features/messaging/data/conversation_repository.dart
    - lib/features/messaging/domain/conversation_summary.dart
    - test/features/messaging/data/conversation_repository_test.dart
  delete: []
started_at:
completed_at:
executed_by:
reviewed_at:
reviewed_by:
review_outcome:
---
# E07-T07 · Read model widened to groups

## 1. Goal
Let the Conversations screen ask one repository for both its sections, so
the Groups list is the same projection as the Personal list rather than a
second, subtly different one.

## 2. Business logic
- **This is the widening E06-T09 planned for.** That task fixed
  `conversationId == peerDeviceId` for 1:1 and wrote, verbatim: *"Group
  conversations (E07) will need a different id space; that is E07's problem
  and this task's `conversationId` is documented as opaque to callers so
  E07 can widen it without rewriting the screens."* This task performs that
  widening — it is a named inherited obligation, not new scope.
- **One query, two kinds.** `ConversationSummary` gains a `kind`
  (`personal | group`) and the group-only fields the design's Groups rows
  need (`design/screens/conversations.md` elements 21-33 draw a title, a
  preview prefixed with the sender's name — `David Chen:` — and status
  glyphs). The repository must still return the whole list in **one grouped
  query**, exactly as E06-T09's `test_EARS_COMM_17_list_is_a_single_query`
  requires; widening the projection must not reintroduce the N+1 that test
  exists to prevent.
- **Blocking applies asymmetrically and deliberately.** A blocked peer's
  1:1 conversation is excluded from the list (FR-BLOCK-001, unchanged). A
  *group* containing a blocked peer is **not** excluded — the group is
  still yours. `spec/feature-list.md`'s "Blocked users are mutually
  invisible within a shared group" is the rule that applies inside the
  thread, and it belongs to whichever task renders group messages; this
  task records the obligation rather than silently deciding the thread's
  behaviour. See `OQ-E07-10`.
- **A deleted group leaves the list.** `groups.is_deleted` excludes it,
  while its rows are retained (E07-T01 §2).
- **The preview is still metadata, and the ciphertext is still opaque
  here** — E06-T09's §2 rule holds unchanged. The repository does not
  decrypt, does not import `CryptoService`, and `ConversationSummary`
  still has no `previewText` field. The sender-name prefix the design draws
  is assembled in the screen layer, from the group member list this
  projection exposes.

## 3. What this task does
- **`ConversationSummary` gains:** `kind` (`ConversationKind.personal |
  .group`), `groupName` (null for personal), `memberCount` (null for
  personal), `lastMessageSenderDeviceId` (already implied by
  `lastMessageIsMine`, now explicit so the screen can render the
  `<name>:` prefix without a second query). `peerDeviceId` and
  `relationshipState` become nullable for the group case — a group has no
  single peer and no single relationship state, and faking one would be a
  lie the screen would then render.
- **`listConversations()` / `watchConversations()`** extended: the same
  single grouped query, `UNION`-ing the personal projection with a group
  projection over `groups` ⋈ `messages` ⋈ `group_members`, blocked *peers*
  still excluded, deleted groups excluded, ordered `lastMessageAt DESC`
  across both kinds.
- **`watchConversations()` also re-emits on group-table changes** — a
  rename or a membership change must move the list without a message
  arriving.
- `messagesPage` / `watchConversation` / `unreadCount` need **no signature
  change**: they already take an opaque `conversationId`, and a group id
  is one. Assert that with a test rather than assuming it.

## 4. What this task does NOT do
- Does **NOT** decrypt, and does **NOT** add `previewText` (E06-T09 §2,
  restated because this is exactly the task where "just for the Groups
  rows" becomes tempting).
- Does **NOT** write. Read model only.
- Does **NOT** add a table, column, index or migration. If the group
  projection genuinely cannot be written against E07-T01's tables, stop and
  raise it — do not add an index here.
- Does **NOT** filter blocked members' messages inside a group thread
  (`OQ-E07-10` — the obligation is recorded, the owner is named, the
  behaviour is not guessed here).
- Does **NOT** implement search, mute, pin, or archive.
- Does **NOT** touch any UI file. E07-T08 consumes this.
- Does **NOT** modify `GroupRepository` or anything under
  `lib/features/groups/`.

## 5. Contract

### Functions
```yaml
- signature: "enum ConversationKind { personal, group }"

- signature: "ConversationSummary({required String conversationId, required ConversationKind kind, String? peerDeviceId, RelationshipState? relationshipState, String? groupName, int? memberCount, required String lastMessageId, required int lastMessageAt, required bool lastMessageIsMine, required String lastMessageSenderDeviceId, required DeliveryState lastMessageState, required int unreadCount})"
  purpose: "one summary type for both kinds; personal-only and group-only fields are nullable rather than faked"

- signature: "ConversationRepository.listConversations() -> Future<List<ConversationSummary>>"
  returns: "personal + group entries interleaved by lastMessageAt DESC; blocked peers excluded; deleted groups excluded; still ONE grouped query"

- signature: "ConversationRepository.watchConversations() -> Stream<List<ConversationSummary>>"
  returns: "emits on changes to `messages` OR `groups` OR `group_members`, coalesced as before"
```

### Data
**None.** No migration; `schemaVersion` unchanged.

## 6. Risks
- The single-query property is the one thing most likely to be lost here.
  E06-T09's `test_EARS_COMM_17_list_is_a_single_query` must still pass
  **unmodified**, and a new assertion must show that growing the group
  count does not grow the query count either. If the union genuinely
  requires two round trips, that is a finding to record, not to hide.
- `watchConversations` now watches three tables. E06-T09's coalescing
  window already exists; widening the watch without widening the coalesce
  turns a membership burst into a query storm — the exact risk E06-T09 §6
  named and tested.
- Nullable `peerDeviceId` will make every existing call site a compile
  error. That is the point — each one is a place that assumed 1:1. Fix them
  in `files:`-declared files only; if a call site outside the fence breaks,
  that is a fence amendment to raise, not to quietly widen.

## 7. Implementation checklist
- [ ] tests written FIRST and failing, named by EARS id
- [ ] `ConversationKind` + widened `ConversationSummary`
- [ ] group projection unioned into the one grouped query
- [ ] deleted groups excluded; blocked *peers* still excluded
- [ ] `watchConversations` watches groups + group_members, still coalesced
- [ ] `messagesPage`/`unreadCount` proven to work unchanged on a group id
- [ ] E06-T09's existing tests pass **unmodified**
- [ ] `flutter analyze` clean, full `flutter test` green

## 8. Acceptance criteria (EARS) + test plan
- **EARS-COMM-33**: WHEN the conversation list is read, the system SHALL
  return personal and group conversations in one list ordered by most
  recent activity, excluding blocked peers and deleted groups.
  (FR-COMM-002, FR-BLOCK-001)
- **EARS-COMM-34**: WHEN a group is renamed or its membership changes, the
  conversation list stream SHALL emit an updated list without a message
  arriving. (FR-GROUP-002)

### Tests
- `test_EARS_COMM_33_personal_and_group_interleave_by_recency`
- `test_EARS_COMM_33_deleted_group_is_absent_and_its_rows_are_retained`
- `test_EARS_COMM_33_group_containing_a_blocked_peer_is_still_listed`
- `test_EARS_COMM_33_list_is_still_a_single_query` — 2 personal + 2 groups
  vs. 10 personal + 10 groups: same query count.
- `test_EARS_COMM_34_rename_emits_without_a_new_message`
- `test_messages_page_works_unchanged_on_a_group_conversation_id`
- `test_summary_has_no_preview_text_field_and_no_crypto_import`

### Manual
None — pure data layer.

## 9. Self-review + Definition of Done
### Self-review
- [ ] Every §7 item done, with commit hashes
- [ ] `flutter test && flutter analyze` green
- [ ] Diff confined to `files:`; §4 respected
- [ ] E06-T09's tests are present and **unmodified** in the diff
- [ ] `grep -in "offset"` on the repository → no SQL OFFSET
- [ ] No crypto import; no `previewText` field
- [ ] `git diff` on `lib/core/persistence/` and `lib/features/groups/` is
      **empty**

**Deviations from spec/design** (none, or list):

**Files actually touched:**

### Definition of Done
- [ ] Every §8 criterion passes via a test named by its EARS id
- [ ] Review APPROVE from a **different model** than `executed_by` (rule 5)
- [ ] Squash-merged to `epic_07`; tracker + metrics stamped
- [ ] 🧍 Human `verified`

## Open Questions
- **OQ-E07-10 — what does "blocked users are mutually invisible within a
  shared group" actually mean in the thread?**
  `spec/feature-list.md`'s Trust & Blocking module states it as a UC;
  `FR-BLOCK-001` says a blocked user cannot communicate with the blocker.
  Options: **(a)** a blocked member's group messages are received, stored
  and hidden from this device's rendering only; **(b)** they are dropped at
  the inbound pipeline and never stored; **(c)** they render as a "message
  from a blocked contact" placeholder. *Advisory: **(b)*** — it matches
  what the 1:1 path already does with a blocked peer, keeps one blocking
  mechanism rather than two, and avoids storing content the user has said
  they do not want. **Owner: the task that renders a group thread**
  (prospective, blocked on GAP-020) — recorded here because this task is
  where the question surfaced and it must not be answered by omission in
  the read model.
  - **Status:** 🟡 important — **owner: the group-thread UI task**;
    revisit trigger: GAP-020's contract approval
  - **Answer:** _<empty>_
  - **Answered by:** _<human>_
  - **Date:** _<YYYY-MM-DD>_

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- (key evidence: decisions applied, session refs, gate reports)
