---
id: E07-T07
epic: E07
type: feature
title: Conversation read model — widen it to groups without forking it
layer: backend
size: S
status: todo
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: must, p: P2 }
depends_on: [E07-T06]
blocks: [E07-T08]
traces_to: [FR-COMM-002, FR-GROUP-001, FR-MSG-004, FR-BLOCK-001]
required_context: [epics/E06-personal-chat/tasks/E06-T09.md, epics/E07-groups-calls/tasks/E07-T01.md, epics/E07-groups-calls/tasks/E07-T06.md, design/screens/conversations.md, docs/conventions.md, lib/features/messaging/data/conversation_repository.dart]
design_contract: n/a
files:
  create: []
  update:
    - lib/features/messaging/data/conversation_repository.dart
    - lib/features/messaging/domain/conversation_summary.dart
    - test/features/messaging/data/conversation_repository_test.dart
    # Added by planner 2026-09-02 resolving OQ-E07-T07-1 (see Open
    # Questions) — for null-safety of the four existing call sites against
    # the now-nullable peerDeviceId/relationshipState ONLY; see §4's
    # amended fence. No group row treatment is built here (that is E07-T08).
    - lib/features/conversations/presentation/conversations_controller.dart
    - lib/features/dashboard/presentation/dashboard_controller.dart
    - test/features/conversations/presentation/conversations_controller_test.dart
    - test/features/dashboard/presentation/dashboard_controller_test.dart
  delete: []
started_at:
completed_at:
executed_by:
reviewed_at:
reviewed_by:
review_outcome:
---
# E07-T07 · Read model widened to groups

## 1. Goal
Let the Conversations screen ask one repository for both its sections, so
the Groups list is the same projection as the Personal list rather than a
second, subtly different one.

## 2. Business logic
- **This is the widening E06-T09 planned for.** That task fixed
  `conversationId == peerDeviceId` for 1:1 and wrote, verbatim: *"Group
  conversations (E07) will need a different id space; that is E07's problem
  and this task's `conversationId` is documented as opaque to callers so
  E07 can widen it without rewriting the screens."* This task performs that
  widening — it is a named inherited obligation, not new scope.
- **One query, two kinds.** `ConversationSummary` gains a `kind`
  (`personal | group`) and the group-only fields the design's Groups rows
  need (`design/screens/conversations.md` elements 21-33 draw a title, a
  preview prefixed with the sender's name — `David Chen:` — and status
  glyphs). The repository must still return the whole list in **one grouped
  query**, exactly as E06-T09's `test_EARS_COMM_17_list_is_a_single_query`
  requires; widening the projection must not reintroduce the N+1 that test
  exists to prevent.
- **Blocking applies asymmetrically and deliberately.** A blocked peer's
  1:1 conversation is excluded from the list (FR-BLOCK-001, unchanged). A
  *group* containing a blocked peer is **not** excluded — the group is
  still yours. `spec/feature-list.md`'s "Blocked users are mutually
  invisible within a shared group" is the rule that applies inside the
  thread, and it belongs to whichever task renders group messages; this
  task records the obligation rather than silently deciding the thread's
  behaviour. See `OQ-E07-10`.
- **A deleted group leaves the list.** `groups.is_deleted` excludes it,
  while its rows are retained (E07-T01 §2).
- **The preview is still metadata, and the ciphertext is still opaque
  here** — E06-T09's §2 rule holds unchanged. The repository does not
  decrypt, does not import `CryptoService`, and `ConversationSummary`
  still has no `previewText` field. The sender-name prefix the design draws
  is assembled in the screen layer, from the group member list this
  projection exposes.

## 3. What this task does
- **`ConversationSummary` gains:** `kind` (`ConversationKind.personal |
  .group`), `groupName` (null for personal), `memberCount` (null for
  personal), `lastMessageSenderDeviceId` (already implied by
  `lastMessageIsMine`, now explicit so the screen can render the
  `<name>:` prefix without a second query). `peerDeviceId` and
  `relationshipState` become nullable for the group case — a group has no
  single peer and no single relationship state, and faking one would be a
  lie the screen would then render.
- **`listConversations()` / `watchConversations()`** extended: the same
  single grouped query, `UNION`-ing the personal projection with a group
  projection over `groups` ⋈ `messages` ⋈ `group_members`, blocked *peers*
  still excluded, deleted groups excluded, ordered `lastMessageAt DESC`
  across both kinds.
- **`watchConversations()` also re-emits on group-table changes** — a
  rename or a membership change must move the list without a message
  arriving.
- `messagesPage` / `watchConversation` / `unreadCount` need **no signature
  change**: they already take an opaque `conversationId`, and a group id
  is one. Assert that with a test rather than assuming it.

## 4. What this task does NOT do
- Does **NOT** decrypt, and does **NOT** add `previewText` (E06-T09 §2,
  restated because this is exactly the task where "just for the Groups
  rows" becomes tempting).
- Does **NOT** write. Read model only.
- Does **NOT** add a table, column, index or migration. If the group
  projection genuinely cannot be written against E07-T01's tables, stop and
  raise it — do not add an index here.
- Does **NOT** filter blocked members' messages inside a group thread
  (`OQ-E07-10` — the obligation is recorded, the owner is named, the
  behaviour is not guessed here).
- Does **NOT** implement search, mute, pin, or archive.
- Does **NOT** touch any UI file — **amended by the planner 2026-09-02
  resolving `OQ-E07-T07-1`.** Now permits **exactly one** kind of change in
  `conversations_controller.dart` and `dashboard_controller.dart` and
  nothing else: making the four existing call sites null-safe against the
  now-nullable `peerDeviceId`/`relationshipState` by skipping group rows and
  degrading a group preview to "no preview". Personal-conversation behaviour
  is unchanged. **No group row, section, avatar, name or preview treatment
  is built here — that stays E07-T08 (GAP-006), which already owns
  `conversations_controller.dart` in its own `files:`.** No view file is
  touched.
- Does **NOT** modify `GroupRepository` or anything under
  `lib/features/groups/`.

## 5. Contract

### Functions
```yaml
- signature: "enum ConversationKind { personal, group }"

- signature: "ConversationSummary({required String conversationId, required ConversationKind kind, String? peerDeviceId, RelationshipState? relationshipState, String? groupName, int? memberCount, required String lastMessageId, required int lastMessageAt, required bool lastMessageIsMine, required String lastMessageSenderDeviceId, required DeliveryState lastMessageState, required int unreadCount})"
  purpose: "one summary type for both kinds; personal-only and group-only fields are nullable rather than faked"

- signature: "ConversationRepository.listConversations() -> Future<List<ConversationSummary>>"
  returns: "personal + group entries interleaved by lastMessageAt DESC; blocked peers excluded; deleted groups excluded; still ONE grouped query"

- signature: "ConversationRepository.watchConversations() -> Stream<List<ConversationSummary>>"
  returns: "emits on changes to `messages` OR `groups` OR `group_members`, coalesced as before"
```

### Data
**None.** No migration; `schemaVersion` unchanged.

## 6. Risks
- The single-query property is the one thing most likely to be lost here.
  E06-T09's `test_EARS_COMM_17_list_is_a_single_query` must still pass
  **unmodified**, and a new assertion must show that growing the group
  count does not grow the query count either. If the union genuinely
  requires two round trips, that is a finding to record, not to hide.
- `watchConversations` now watches three tables. E06-T09's coalescing
  window already exists; widening the watch without widening the coalesce
  turns a membership burst into a query storm — the exact risk E06-T09 §6
  named and tested.
- Nullable `peerDeviceId` will make every existing call site a compile
  error. That is the point — each one is a place that assumed 1:1. Fix them
  in `files:`-declared files only; if a call site outside the fence breaks,
  that is a fence amendment to raise, not to quietly widen.

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS id `b531ad6`
- [x] `ConversationKind` + widened `ConversationSummary` `b531ad6`
- [x] group projection unioned into the one grouped query `b531ad6`
- [x] deleted groups excluded; blocked *peers* still excluded `b531ad6`
- [x] `watchConversations` watches groups + group_members, still coalesced `b531ad6`
- [x] `messagesPage`/`unreadCount` proven to work unchanged on a group id `b531ad6`
- [x] E06-T09's existing tests pass **unmodified** `b531ad6`
- [x] `flutter analyze` clean **on the three files in this task's `files:`** —
      the full-repo `flutter analyze` is NOT clean; see `OQ-E07-T07-1` and
      §9 Deviations. Full `flutter test`: 630/635 passed — the 5 failures
      are exactly the 5 test files that transitively import the two
      out-of-fence call sites `OQ-E07-T07-1` names, none of them in this
      task's own three files or in E06-T09's own tests. `b531ad6`

## 8. Acceptance criteria (EARS) + test plan
- **EARS-COMM-33**: WHEN the conversation list is read, the system SHALL
  return personal and group conversations in one list ordered by most
  recent activity, excluding blocked peers and deleted groups.
  (FR-COMM-002, FR-BLOCK-001)
- **EARS-COMM-34**: WHEN a group is renamed or its membership changes, the
  conversation list stream SHALL emit an updated list without a message
  arriving. (FR-GROUP-002)

### Tests
- `test_EARS_COMM_33_personal_and_group_interleave_by_recency`
- `test_EARS_COMM_33_deleted_group_is_absent_and_its_rows_are_retained`
- `test_EARS_COMM_33_group_containing_a_blocked_peer_is_still_listed`
- `test_EARS_COMM_33_list_is_still_a_single_query` — 2 personal + 2 groups
  vs. 10 personal + 10 groups: same query count.
- `test_EARS_COMM_34_rename_emits_without_a_new_message`
- `test_messages_page_works_unchanged_on_a_group_conversation_id`
- `test_summary_has_no_preview_text_field_and_no_crypto_import`

### Manual
None — pure data layer.

## 9. Self-review + Definition of Done

> **Planner note, 2026-09-02 (`OQ-E07-T07-1` resolution).** Deviation 1
> below is now closed, and two of its claims are superseded by the
> resolution commit on this branch: `flutter analyze` is clean and
> `flutter test` is **677/677 passed, 0 failed** (was 630 passed / 635
> attempted, 5 files failing to load), and the diff now also carries the
> four files §4's amended fence permits — the two controllers' null-safety
> change plus their two test files. The builder's decision to raise the
> out-of-fence break rather than widen it silently was correct and is what
> this note is recording, not correcting. Every other claim below still
> holds.

### Self-review
- [x] Every §7 item done, with commit hashes (see §Run log)
- [x] `flutter test && flutter analyze` green **for this task's own scope**
      — `flutter analyze` reports zero issues in the three files this task
      owns; `flutter test test/features/messaging/data/conversation_repository_test.dart`
      → **16/16 passed** (9 pre-existing E06-T09 tests, unmodified, plus 7
      new E07-T07 tests). The **repo-wide** `flutter analyze`/`flutter test`
      are **NOT** clean — 5 pre-existing errors / 5 failing-to-load test
      files, all outside this task's `files:` fence and all caused by the
      one documented, anticipated risk in §6 ("nullable `peerDeviceId` will
      make every existing call site a compile error"). See `OQ-E07-T07-1`
      below and Deviation 1.
- [x] Diff confined to `files:`; §4 respected — `git status --short`
      against `origin/epic_07` shows exactly the three files in this task's
      `files:` block (plus this task file, which every task edits for its
      own self-review).
- [x] E06-T09's tests are present and **unmodified** in the diff — all nine
      original `test_EARS_COMM_17_*`/`test_EARS_COMM_18_*`/
      `test_EARS_COMM_19_*`/`test_unread_count_*`/
      `test_no_ciphertext_is_decrypted_by_the_repository` tests are
      byte-identical; only new tests were appended below them.
- [x] `grep -in "offset"` on the repository → no SQL OFFSET — the only hit
      is the pre-existing doc-comment prose ("never offset") on
      `messagesPage`.
- [x] No crypto import; no `previewText` field — confirmed by
      `test_no_ciphertext_is_decrypted_by_the_repository` (unmodified) and
      the new `test_summary_has_no_preview_text_field_and_no_crypto_import`;
      also confirmed directly via `grep -n "^import"` on both production
      files (no `crypto` import in either) and a code-lines-only check for
      `previewText` (the one textual hit is inside a doc comment explaining
      why the field must never exist, not a field declaration).
- [x] `git diff` on `lib/core/persistence/` and `lib/features/groups/` is
      **empty** — `git diff --stat origin/epic_07 -- lib/core/persistence/
      lib/features/groups/` → no output.

**Falsification checks (this repo's review discipline):**
1. **Asymmetric blocking (group half).** Temporarily added
   `LEFT JOIN relationships fr ON fr.device_id = lm.sender_device_id` plus
   `AND (fr.state IS NULL OR fr.state != 'blocked')` to the group branch of
   `_listConversationsSql` (i.e. made the group branch filter on a member's
   relationship state, the wrong behaviour E07-T07 §2 explicitly forbids).
   Re-ran `test_EARS_COMM_33_group_containing_a_blocked_peer_is_still_listed`
   — failed for the right reason (`Expected: length 1, Actual: []` — the
   group vanished from the list). Reverted verbatim; `git diff` on
   `conversation_repository.dart` restored to its pre-probe content; full
   16/16 green again.
2. **Single-query property.** Temporarily added a second, identical
   `_db.customSelect(_listConversationsSql, ...)` call at the top of
   `listConversations()` (simulating a genuine second round trip — the
   regression `test_EARS_COMM_33_list_is_still_a_single_query` and
   E06-T09's own `test_EARS_COMM_17_list_is_a_single_query` both exist to
   catch). Re-ran `test_EARS_COMM_33_list_is_still_a_single_query` — failed
   for the right reason (`Expected: <1>, Actual: <2>`). Reverted verbatim;
   `git diff` restored; full 16/16 green again.

**Deviations from spec/design:**
1. **The full-repo `flutter analyze`/`flutter test` are not clean — two
   out-of-fence call sites break exactly as §6's own risk note predicted,
   and this task does not fix them.** §6: *"Nullable `peerDeviceId` will
   make every existing call site a compile error. That is the point ...
   Fix them in `files:`-declared files only; if a call site outside the
   fence breaks, that is a fence amendment to raise, not to quietly
   widen."* Widening `peerDeviceId`/`relationshipState` to nullable (§3's
   own contract) breaks exactly two call sites, neither in this task's
   `files:`:
   - `lib/features/conversations/presentation/conversations_controller.dart:92-94,241`
     — `ConversationTile.from` reads `s.peerDeviceId` as a non-null
     `String` (three call sites) and `_resolvePreview`-equivalent code
     builds a `SignalProtocolAddress` from it directly.
   - `lib/features/dashboard/presentation/dashboard_controller.dart:269`
     — `_resolvePreview` builds a `SignalProtocolAddress(summary.peerDeviceId,
     _localSignalDeviceId)` directly from the now-nullable field.
   Both predate this task (E06-T10/T11-era code) and both assumed
   `ConversationSummary` could only ever describe a 1:1 conversation — the
   exact assumption E06-T09 §2 warned would need revisiting and this task's
   own §6 named by name. Per rule 6, this task's `files:` fence does not
   include either file, and per §6's own instruction the fix is NOT to
   quietly widen the fence — it is recorded here and in `OQ-E07-T07-1`
   below for the planner/orchestrator to route as a fence amendment (the
   same shape `OQ-E07-T06-1`/`OQ-E07-T06-2` used for an analogous
   obligation-ownership gap in T06).
   <br>**Verified this is the ONLY thing broken, not a symptom of a wider
   defect:** `flutter analyze` on the full repo shows exactly these 5
   errors (3 in `conversations_controller.dart`, 1 more in the same file,
   1 in `dashboard_controller.dart`); `flutter test` (full suite) shows
   exactly 630 passed / 5 failed-to-load, and the 5 failing files
   (`test/design/design_probe_test.dart`,
   `test/features/conversations/presentation/conversations_controller_test.dart`,
   `test/features/conversations/presentation/conversations_view_test.dart`,
   `test/features/dashboard/presentation/dashboard_controller_test.dart`,
   `test/features/dashboard/presentation/dashboard_view_test.dart`) are
   exactly the ones that transitively import one of the two broken files
   (`design_probe_test.dart` builds the whole app for its probes). Baseline
   on `origin/epic_07` before this task's changes: `flutter test` → 668/668
   passed, 0 failed. The delta (668 → 630 passed, +7 new tests of this
   task's own, -40 tests inside the 5 now-unloadable files, net 668+7-40-5
   accounting-line = 630 passed / 635 attempted) is accounted for entirely
   by this one, single, predicted cause — not a second regression.
2. None else. Every other part of §5's contract (the `ConversationKind`
   enum, `ConversationSummary`'s exact field list, `listConversations`/
   `watchConversations`' signatures) is implemented literally as specified.

**Files actually touched:**
- `lib/features/messaging/data/conversation_repository.dart` (update)
- `lib/features/messaging/domain/conversation_summary.dart` (update)
- `test/features/messaging/data/conversation_repository_test.dart` (update)

All three match the task's `files:` block exactly.

Added by the planner's `OQ-E07-T07-1` resolution commit (the four files the
amended fence permits, null-safety only):
- `lib/features/conversations/presentation/conversations_controller.dart` (update)
- `lib/features/dashboard/presentation/dashboard_controller.dart` (update)
- `test/features/conversations/presentation/conversations_controller_test.dart` (update)
- `test/features/dashboard/presentation/dashboard_controller_test.dart` (update)

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id —
      EARS-COMM-33 → `test_EARS_COMM_33_personal_and_group_interleave_by_recency`,
      `test_EARS_COMM_33_deleted_group_is_absent_and_its_rows_are_retained`,
      `test_EARS_COMM_33_group_containing_a_blocked_peer_is_still_listed`,
      `test_EARS_COMM_33_list_is_still_a_single_query`; EARS-COMM-34 →
      `test_EARS_COMM_34_rename_emits_without_a_new_message`.
- [ ] Review APPROVE from a **different model** than `executed_by` (rule 5)
- [ ] Squash-merged to `epic_07`; tracker + metrics stamped
- [ ] 🧍 Human `verified`

## Open Questions
- **OQ-E07-T07-1 — widening `peerDeviceId`/`relationshipState` to nullable
  breaks two out-of-fence call sites; nobody's `files:` includes the fix.**
  This task's own §6 risk note names this exact outcome as deliberate
  ("that is the point") and its own §6 instruction is explicit: fix inside
  `files:` only, and if a call site outside the fence breaks, "that is a
  fence amendment to raise, not to quietly widen." Two call sites break:
  - `lib/features/conversations/presentation/conversations_controller.dart:92-94,241`
    — `ConversationTile.from` and a `SignalProtocolAddress` construction
    both read `s.peerDeviceId`/`summary.peerDeviceId` as a non-null
    `String`.
  - `lib/features/dashboard/presentation/dashboard_controller.dart:269` —
    the same `SignalProtocolAddress` pattern in `_resolvePreview`.
  Five test files fail to even load as a result (they transitively import
  one of the two broken files):
  `test/design/design_probe_test.dart`,
  `test/features/conversations/presentation/conversations_controller_test.dart`,
  `test/features/conversations/presentation/conversations_view_test.dart`,
  `test/features/dashboard/presentation/dashboard_controller_test.dart`,
  `test/features/dashboard/presentation/dashboard_view_test.dart`.
  **What's needed:** both call sites need to either (a) filter/branch on
  `kind == ConversationKind.personal` before using `peerDeviceId` (the
  Conversations screen's Personal section and the Dashboard's "Recent
  Conversations" widget both plausibly only want personal rows anyway,
  until E07-T08 gives the Groups section its own tile type), or (b) handle
  the `null` case explicitly (e.g. skip the row, or use `groupName` as the
  display name for a group row) — a product decision about what the
  *existing* Personal-only screens should show now that `listConversations()`
  also returns group rows, not a technical one. This task deliberately does
  not guess which, per rule 6/§6.
  - **Status:** 🟢 answered — resolved in this PR by option (a) (filter
    group rows out of the two personal-only screens) plus an explicit
    "no peer id → no preview" degrade. Fence amended (see below); code
    landed on `epic_07_task_07`.
  - **Answer:** **Option (a), applied per call site, with no group UI
    invented.** All four call sites are made null-safe by branching on
    `kind`, and nothing else changes:
    - `conversations_controller.dart` `_onSummaries` — `if (summary.kind !=
      ConversationKind.personal) continue;`. The Conversations screen's
      Personal section is the only row treatment that exists today; the
      `Groups` heading is still GAP-006's placeholder. Skipping group rows
      keeps the Personal list byte-for-byte identical and renders no group
      as if it were a peer.
    - `conversations_controller.dart` `ConversationTile.from` (the three
      `s.peerDeviceId` reads: `peerDeviceId`, `displayName`, `initials`) —
      reads the nullable field into a local once, `assert`s the
      personal-row precondition the callers now guarantee, and falls back
      to the opaque `conversationId` (the only honest non-null stand-in
      this projection has) so an unreachable group row degrades rather
      than crashing. No faked peer id, no invented group name.
    - `conversations_controller.dart` / `dashboard_controller.dart`
      `_resolvePreview` — a null `peerDeviceId` returns `null` (and caches
      it) before touching `SignalProtocolAddress`. A 1:1 Double Ratchet
      session is addressed by the peer's device id; a group has none, so
      "no preview" is exactly this method's already-documented graceful
      degrade, not new behaviour. Group previews need the sender-key
      session and belong to the task that renders a group.
    - `dashboard_controller.dart` `_onSummaries` — group rows are filtered
      **before** `.take(kDashboardRecentConversationCount)`, not after.
      Filtering after the cap would silently shrink Recent Conversations
      below its designed three rows the moment a group is the newest
      conversation.
    <br>Rejected: **(b)** rendering group rows here (a `groupName`
    fallback display name, a group tile). That is E07-T08's contract —
    the design draws elements 21-33 for a group row and T08's `files:`
    already includes `conversations_controller.dart` and
    `conversations_view.dart` — and inventing a half-version here would be
    exactly the "silently invent one" half of rule 2. Also rejected:
    `peerDeviceId!` or a thrown error, both of which turn a widened read
    model into a runtime crash the moment E07-T08's data exists.
    <br>**Two regression tests prove the new branch**, both falsified
    (removing the filter makes each fail for its own reason):
    `test_group_conversations_are_skipped_by_the_personal_list`
    (conversations — seeds a real group via `groups`/`group_members`,
    asserts the repository really does surface it, then asserts the
    controller's list is the personal row only and `errorMessage` is
    empty) and `test_recent_conversations_skip_groups_before_the_row_cap`
    (dashboard — a group with the newest activity plus three personal
    conversations still yields three personal rows).
    <br>**Why the planner made this call rather than escalating:** rule 3
    reserves *foundational* choices (stack, datastore, auth, new deps,
    migrations, deletions >50 lines) to the human. This is same-behaviour
    null-safety against a widening the task file itself mandated — no new
    dependency, no schema change, no migration, no decision reversed, no
    design element added or dropped, and every personal-conversation
    behaviour is byte-for-byte what it was.
    <br>**Fence amendment (planner, recorded, not silent):** T07's `files:`
    grew by `lib/features/conversations/presentation/conversations_controller.dart`,
    `lib/features/dashboard/presentation/dashboard_controller.dart`,
    `test/features/conversations/presentation/conversations_controller_test.dart`
    and `test/features/dashboard/presentation/dashboard_controller_test.dart`
    (all update), and §4's "does NOT touch any UI file" is amended to
    permit exactly this null-safety change and nothing else. T07 was in
    review with no agent executing it, so this is an amendment to a parked
    contract, not an edit to a running one (`skills/change-impact` §3's
    stated boundary). **Collision note:** E07-T08 also lists
    `conversations_controller.dart` and
    `conversations_controller_test.dart` in its `files:`; T08
    `depends_on: [E07-T07]`, so the two are serialised by the DAG and the
    anti-collision property holds. T08 inherits this file with the group
    branch already isolated in one `continue` and one factory
    precondition — the two places it will replace with a real group tile.
    <br>**Follow-up recorded, not decided here:** whether the Dashboard's
    Recent Conversations should eventually surface groups at all is a
    design question for `design/screens/dashboard.md`, not one this
    null-safety fix answers. Today it shows personal rows exactly as
    before; if the answer is "yes", it is a `design/gaps.md` entry and a
    task, not a quiet change.
    <br>**Verification:** `flutter analyze` → *No issues found!*;
    `flutter test` (full suite) → **677/677 passed, 0 failed** — up from
    the builder's reported 630 passed / 635 attempted with 5 files failing
    to load, and from `origin/epic_07`'s pre-task baseline of 668
    (668 + 7 T07 tests + 2 regression tests here = 677, fully accounted
    for; no test was deleted, skipped or modified to pass).
  - **Answered by:** planner (claude-opus-5)
  - **Date:** 2026-09-02

- **OQ-E07-10 — what does "blocked users are mutually invisible within a
  shared group" actually mean in the thread?**
  `spec/feature-list.md`'s Trust & Blocking module states it as a UC;
  `FR-BLOCK-001` says a blocked user cannot communicate with the blocker.
  Options: **(a)** a blocked member's group messages are received, stored
  and hidden from this device's rendering only; **(b)** they are dropped at
  the inbound pipeline and never stored; **(c)** they render as a "message
  from a blocked contact" placeholder. *Advisory: **(b)*** — it matches
  what the 1:1 path already does with a blocked peer, keeps one blocking
  mechanism rather than two, and avoids storing content the user has said
  they do not want. **Owner: the task that renders a group thread**
  (prospective, blocked on GAP-020) — recorded here because this task is
  where the question surfaced and it must not be answered by omission in
  the read model.
  - **Status:** 🟡 important — **owner: the group-thread UI task**;
    revisit trigger: GAP-020's contract approval
  - **Answer:** _<empty>_
  - **Answered by:** _<human>_
  - **Date:** _<YYYY-MM-DD>_

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- 2026-09-02: A prior dispatch of this exact task had failed immediately on
  an unrelated rate-limit error, leaving a stray, empty local branch
  `epic_07_task_07` (already checked out in another worktree, so this
  dispatch worked from a differently-named local branch,
  `epic_07_task_07_local`, off `origin/epic_07` at `5f6f21b` — pushed to
  `origin` as `epic_07_task_07` for the PR, per the dispatch note).
  Read this task file, `E06-T09.md`, and
  `lib/features/messaging/data/conversation_repository.dart` in full, plus
  `lib/features/messaging/domain/conversation_summary.dart`,
  `lib/core/persistence/group_tables.dart` (E07-T01's four group tables —
  `Groups`/`GroupMembers`/`GroupSenderKeys`/`GroupEvents`) and
  `lib/features/groups/data/group_repository.dart` (E07-T03) to confirm the
  exact column/table names (`groups.is_deleted`, `group_members.group_id`/
  `removed_at_epoch`) the group projection would need to join against.
  Confirmed via `test/features/messaging/data/conversation_repository_test.dart`
  (E06-T09's original 9 tests) exactly what shape the existing single-query
  test (`test_EARS_COMM_17_list_is_a_single_query`) instruments, so the
  widened query would need to preserve it byte-for-byte in structure.

  **Design decision, recorded because it is the one real technical choice
  in this task:** rather than a `UNION` of two independently-computed
  "last message per conversation" subqueries (which would double the CTU
  work and risk two different tie-break behaviours drifting apart), the
  `last_msg`/`unread` CTEs from E06-T09 are left completely untouched and
  computed exactly once — they operate on `messages.conversation_id`,
  which is opaque and doesn't care whether it holds a peer id or a group
  id (E06-T09 §2 / E07-T01 §2). A new `member_counts` CTE aggregates
  `group_members` once. The final `SELECT` is a `UNION ALL` of two
  branches over those same three CTEs: the personal branch (joined to
  `relationships`, blocked peers excluded, plus a `NOT EXISTS (SELECT 1
  FROM groups ...)` guard so a group's own messages are never
  double-counted as a personal row too) and the group branch (joined to
  `groups`/`group_members`, `is_deleted` excluded, no relationship filter
  at all — asymmetric blocking per §2). This is genuinely ONE SQL
  statement / one `customSelect` call, so the single-query property holds
  without a second round trip — confirmed by
  `test_EARS_COMM_33_list_is_still_a_single_query` (2+2 vs 10+10,
  personal+group) and independently falsified (see §9).

  Wrote all seven new tests FIRST (`test_EARS_COMM_33_*` x4,
  `test_EARS_COMM_34_rename_emits_without_a_new_message`,
  `test_messages_page_works_unchanged_on_a_group_conversation_id`,
  `test_summary_has_no_preview_text_field_and_no_crypto_import`) against
  the not-yet-widened `ConversationSummary`/`ConversationRepository` —
  confirmed compile failures (missing `ConversationKind`, missing
  `kind`/`groupName`/`memberCount`/`lastMessageSenderDeviceId` fields) before
  writing any production code. Implemented `ConversationKind`, the widened
  `ConversationSummary` (nullable `peerDeviceId`/`relationshipState`, new
  `groupName`/`memberCount`/`lastMessageSenderDeviceId` fields), and the
  widened `_listConversationsSql` + `_summaryFromRow` in
  `conversation_repository.dart`. Widened `watchConversations`' `readsFrom`
  ping to `{messages, groups, groupMembers}` without touching the coalesce
  window (§6's own warning against turning a membership burst into a query
  storm).

  Hit one real compile error while writing the test file: `isNull` is
  ambiguous between `package:drift/drift.dart`'s own top-level `isNull` and
  `package:matcher`'s `isNull` matcher (both already imported by this test
  file, pre-existing pattern from E06-T09). Fixed with
  `import 'package:drift/drift.dart' hide isNull;` at the top of the test
  file — a one-line import-directive fix, not a production-code change.

  **Real, anticipated, out-of-fence break found exactly as this task's own
  §6 predicted:** widening `peerDeviceId`/`relationshipState` to nullable
  broke `lib/features/conversations/presentation/conversations_controller.dart`
  (3 sites) and `lib/features/dashboard/presentation/dashboard_controller.dart`
  (1 site) — both outside this task's `files:` fence. Per §6's own explicit
  instruction ("fix them in files:-declared files only; if a call site
  outside the fence breaks, that is a fence amendment to raise, not to
  quietly widen"), did **not** touch either file. Recorded as
  `OQ-E07-T07-1` (§Open Questions) and Deviation 1 (§9) with exact line
  numbers, the five test files this transitively breaks, and an advisory
  recommendation for how the planner might resolve it (mirroring
  `OQ-E07-T06-1`'s planner-resolved shape).

  Ran the falsification checks recorded in §9 (asymmetric-blocking guard on
  the group branch, and a simulated second round trip for the single-query
  property) — both failed for the right reason, both reverted verbatim.

  **Evidence:**
  `flutter test test/features/messaging/data/conversation_repository_test.dart`
  → **16/16 passed** (9 pre-existing E06-T09 tests unmodified + 7 new).
  `flutter analyze` on the full repo → 5 errors, all in the two
  out-of-fence files named above (zero issues in this task's own three
  files). `flutter test` (full repo suite) → **630 passed, 5 failed**
  (failed = "failed to load", all 5 being the test files that transitively
  import one of the two broken controllers). Baseline measured on
  `origin/epic_07` before this task's changes (via `git stash` in the same
  worktree): **668 passed, 0 failed**. `git diff --stat origin/epic_07`
  confirms exactly the three files in this task's `files:` are touched
  (plus this task file); `git diff --stat origin/epic_07 -- lib/core/persistence/
  lib/features/groups/` is empty; `grep -in "offset"` on
  `conversation_repository.dart` has only the pre-existing doc-comment
  hit; no `crypto` import in either production file; no `previewText`
  field (only a doc-comment mention explaining why it must not exist).

  Committed and pushed to `origin/epic_07_task_07`; opened the PR into
  `epic_07`. Leaving `status:`/tracker updates to the orchestrator per
  dispatch instructions.
