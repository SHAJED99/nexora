---
id: E07-T02
epic: E07
type: feature
title: Group role permission matrix — who may do what, decided in one place
layer: backend
size: S
status: review-requested
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: must, p: P2 }
depends_on: [E07-T01]
blocks: [E07-T03]
traces_to: [FR-GROUP-001, FR-GROUP-002, FR-GROUP-003]
required_context: [epics/E07-groups-calls/tasks/E07-T01.md, spec/srs.md, spec/product-rules.md, docs/conventions.md]
design_contract: n/a
files:
  create:
    - lib/features/groups/domain/group_permissions.dart
    - test/features/groups/domain/group_permissions_test.dart
  update: []
  delete: []
started_at: 2026-08-31
completed_at: 2026-08-31
executed_by: builder-sonnet
reviewed_at:
reviewed_by:
review_outcome:
---
# E07-T02 · Group role permission matrix

## 1. Goal
One pure, total function that answers "may this role perform this action on
this group?", so that the protocol layer (E07-T03), the fan-out layer
(E07-T06) and any future UI all enforce the *same* answer instead of three
similar ones.

## 2. Business logic
- **FR-GROUP-002 enumerates the Owner's powers exactly:** rename the group,
  add members, remove members, assign administrators, transfer ownership,
  delete the group. That list is the spec; it is not a starting point to
  extend.
- **FR-GROUP-003 is deliberately vague** — "Admins shall be able to perform
  permitted group-management actions; Members shall participate in normal
  communication." The spec does not enumerate which management actions an
  Admin gets, so this task does not invent a generous answer. The matrix
  below takes the *narrow, reversible* reading — an Admin may do the things
  a wronged Owner can trivially undo (add a member, remove a non-admin
  member, rename), and may not do the things that are irreversible or
  self-elevating (grant/revoke admin, transfer ownership, delete the
  group). Recorded as `OQ-E07-5` so the human can widen it cheaply; a
  narrow matrix that is later widened is a one-line change, a wide matrix
  that is later narrowed is a security incident.
- **Every rule is evaluated against the acting device's *current* role in
  *that* group**, never against a cached role from another group and never
  against a role a control frame claims for itself. The permission decision
  is always made locally against local state (E07-T03 §2).
- **Self-actions are distinct from actions on another member.** Leaving a
  group is not "removing a member"; every role may leave. An Owner may not
  leave without transferring ownership first — otherwise the single-owner
  invariant E07-T01 enforces in the index becomes unsatisfiable.
- **A removed member has no role and therefore no permission at all**, not
  even to read. `removed_at_epoch IS NOT NULL` is a hard `false` for every
  action.

## 3. What this task does
- **`GroupAction`** — one enum value per FR-GROUP-002/003 action:
  `rename`, `addMember`, `removeMember`, `removeAdmin`, `grantAdmin`,
  `revokeAdmin`, `transferOwnership`, `deleteGroup`, `leave`, `sendMessage`.
- **`GroupPermissions.allows({required GroupRole actorRole, required
  GroupAction action, GroupRole? subjectRole})`** — a pure, total, `const`-
  friendly function returning `bool`. No I/O, no database, no clock.
- **`GroupPermissions.check(...)`** — the same decision returning a typed
  `AppFailure('group.forbidden')` instead of a bare `false`, for callers
  that need to surface a reason (`docs/conventions.md`'s single error
  envelope, the one E06-T02's taxonomy established).
- The matrix as an explicit, readable table in the source, with a test that
  asserts **every** `(role × action × subjectRole)` combination — a matrix
  with an untested cell is a matrix with a guess in it.

## 4. What this task does NOT do
- Does **NOT** read or write the database. This is a pure function over
  roles; the caller loads the roles. A permission function that queries is
  a permission function nobody can test exhaustively.
- Does **NOT** enforce anything. E07-T03 calls it; this task provides it.
- Does **NOT** widen `GroupRole` or add a fourth role.
- Does **NOT** decide the Admin matrix beyond the narrow reading in §2 —
  widening is `OQ-E07-5`, a human call, not an implementer's judgement.
- Does **NOT** touch trust/blocking (E02). Whether a *blocked* peer may be
  added to a group is E07-T03's composition of this matrix with
  `RelationshipRepository`, not a new role here.
- Does **NOT** add a `GroupRepository`, a controller, or a UI affordance.

## 5. Contract

### Functions
```yaml
- signature: "enum GroupAction { rename, addMember, removeMember, removeAdmin, grantAdmin, revokeAdmin, transferOwnership, deleteGroup, leave, sendMessage }"
  purpose: "one value per FR-GROUP-002/003 action; removeMember and removeAdmin are separate because the Admin matrix distinguishes them"

- signature: "bool GroupPermissions.allows({required GroupRole actorRole, required GroupAction action, GroupRole? subjectRole})"
  params:
    actorRole: "the acting device's CURRENT role in THIS group, loaded by the caller"
    subjectRole: "the target member's current role, for member-directed actions; null for group-directed ones"
  returns: "true iff the matrix permits it. Total — never throws, never returns null"
  purpose: "the single source of truth for FR-GROUP-002/003"

- signature: "AppFailure? GroupPermissions.check({required GroupRole actorRole, required GroupAction action, GroupRole? subjectRole})"
  returns: "null when allowed; AppFailure('group.forbidden') otherwise"
  purpose: "the same decision in the project's error envelope, for callers that surface a reason"
```

**The matrix (the contract itself — the implementer may not deviate):**

| Action | Owner | Admin | Member |
|---|---|---|---|
| `rename` | ✅ | ✅ | ❌ |
| `addMember` | ✅ | ✅ | ❌ |
| `removeMember` (subject = member) | ✅ | ✅ | ❌ |
| `removeAdmin` (subject = admin) | ✅ | ❌ | ❌ |
| `removeMember` (subject = owner) | ❌ | ❌ | ❌ |
| `grantAdmin` | ✅ | ❌ | ❌ |
| `revokeAdmin` | ✅ | ❌ | ❌ |
| `transferOwnership` | ✅ | ❌ | ❌ |
| `deleteGroup` | ✅ | ❌ | ❌ |
| `leave` | ❌ (transfer first) | ✅ | ✅ |
| `sendMessage` | ✅ | ✅ | ✅ |

## 6. Risks
- The tempting shortcut is `if (role == owner) return true;` at the top.
  Do not: the Owner's `leave` is a deliberate `false`, and a blanket
  owner-allows-everything line silently deletes that row from the matrix.
- `removeMember` with `subjectRole == null` is a programming error, not a
  permission decision. Assert loudly rather than defaulting to `false` —
  a silent `false` here reads at the call site as "not permitted" and
  hides the bug.
- Exhaustive testing means `GroupRole.values × GroupAction.values ×
  (GroupRole.values + null)` = 120 cells. Generate them in a loop against
  a literal expectation table; do not hand-write 120 `expect`s and do not
  test only the interesting ones.

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS id `2eb3997`
- [x] `GroupAction` enum `2eb3997`
- [x] `GroupPermissions.allows` — the §5 matrix, no shortcuts `2eb3997`
- [x] `GroupPermissions.check` — `AppFailure('group.forbidden')` `2eb3997`
- [x] exhaustive 120-cell matrix test `2eb3997`
- [x] `flutter analyze` clean, full `flutter test` green `2eb3997`

## 8. Acceptance criteria (EARS) + test plan
- **EARS-GROUP-6**: WHEN an Owner performs any of rename, add member,
  remove member, grant/revoke admin, transfer ownership or delete, the
  system SHALL permit it. (FR-GROUP-002)
- **EARS-GROUP-7**: IF a Member or an Admin attempts an action the matrix
  reserves for a higher role, THEN the system SHALL deny it with
  `group.forbidden`. (FR-GROUP-003)

### Tests
- `test_EARS_GROUP_6_owner_may_perform_every_fr_group_002_action` — the six
  actions FR-GROUP-002 names, by name.
- `test_EARS_GROUP_7_admin_cannot_grant_admin_or_transfer_or_delete`
- `test_EARS_GROUP_7_member_cannot_perform_any_management_action`
- `test_EARS_GROUP_7_denial_returns_group_forbidden_failure`
- `test_matrix_is_exhaustive_and_matches_the_contract_table` — all 120
  cells against the literal §5 table.
- `test_owner_cannot_leave_without_transferring_first`
- `test_nobody_can_remove_the_owner`

### Manual
None — pure function.

## 9. Self-review + Definition of Done
### Self-review
- [x] Every §7 item done, with commit hashes (`2eb3997`)
- [x] `flutter test && flutter analyze` green — `flutter analyze`: *No
      issues found!*; `flutter test`: **519/519** (391 pre-existing +
      128 new: 120 exhaustive matrix cells + 8 named tests)
- [x] Diff confined to `files:`; §4 respected — `git status --short`
      shows exactly the two files in the `files:` block; no database
      import, no controller, no `GroupRepository` added
- [x] No database import, no `dart:io`, no clock in the new file —
      `group_permissions.dart` imports only `GroupRole` (an enum) from
      `core/persistence/group_tables.dart` and `AppFailure` from
      `core/auth/google_auth_service.dart`; no `drift`, no `dart:io`, no
      `DateTime.now()`
- [x] Every cell of the §5 table appears in the test's expectation table —
      `group_permissions_test.dart`'s `perActorFor` enumerates all
      `GroupRole.values × GroupAction.values × (GroupRole.values + null)`
      = 120 cells and the exhaustive `group(...)` block asserts every one

**Deviations from spec/design:**
- The §5 table conditions on `subjectRole` only for `removeMember` and
  `removeAdmin`. Three cells needed a decision the table doesn't spell out
  literally, each a small, reversible-reading choice made per the
  "simplest interpretation that satisfies the acceptance criteria" rule
  (not escalated to a new Open Question — none of these widens or narrows
  what any role can actually do beyond what §5 and §6 already say):
  1. `removeMember` with `subjectRole == GroupRole.admin` → `false` for
     every actor. Rationale: the task's own text says "`removeMember` and
     `removeAdmin` are separate because the Admin matrix distinguishes
     them" — an admin subject must go through `removeAdmin`, not
     `removeMember`. Denying the wrong-action combination (rather than
     silently succeeding as if it were a plain-member removal) keeps that
     distinction real instead of decorative.
  2. `removeAdmin` with `subjectRole` = `owner` or `member` → `false` for
     every actor, symmetric reasoning: `removeAdmin` only ever targets an
     admin per §5's row.
  3. `removeMember`/`removeAdmin` with `subjectRole == null` → throws
     `ArgumentError`, per §6's explicit instruction ("Assert loudly rather
     than defaulting to `false`") generalized from `removeMember` (§6's
     literal example) to `removeAdmin` (the only other action the table
     conditions on `subjectRole`), for the same reason: "remove whom" is
     not answerable without a subject, and a silent `false` would read at
     the call site as a real permission decision. All other actions leave
     `subjectRole` unconstrained (any value including `null` gives the
     same answer), matching the §5 table's literal silence on it for
     those rows.

**Files actually touched:**
- `lib/features/groups/domain/group_permissions.dart` (create)
- `test/features/groups/domain/group_permissions_test.dart` (create)

Both match the task's `files:` block exactly.

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id
      (`test_EARS_GROUP_6_owner_may_perform_every_fr_group_002_action`,
      `test_EARS_GROUP_7_admin_cannot_grant_admin_or_transfer_or_delete`,
      `test_EARS_GROUP_7_member_cannot_perform_any_management_action`,
      `test_EARS_GROUP_7_denial_returns_group_forbidden_failure`,
      `test_matrix_is_exhaustive_and_matches_the_contract_table`,
      `test_owner_cannot_leave_without_transferring_first`,
      `test_nobody_can_remove_the_owner`)
- [ ] Review APPROVE from a **different model** than `executed_by` (rule 5)
- [ ] Squash-merged to `epic_07`; tracker + metrics stamped
- [ ] 🧍 Human `verified`

## Open Questions
- **OQ-E07-5 — how much may an Admin actually do?** FR-GROUP-003 says
  "permitted group-management actions" without enumerating them, so the
  spec is genuinely silent and this task takes the narrow reading (§2).
  Options: **(a) narrow** — Admin may rename, add, and remove *members*
  only (this task's matrix); **(b) medium** — Admin may additionally grant
  admin, but not revoke it or transfer ownership; **(c) broad** — Admin may
  do everything except delete the group and transfer ownership.
  *Advisory: **(a)**.* Widening later is a one-line matrix change plus a
  test; narrowing later means telling users a capability they had is gone,
  and any Admin who used it in the meantime has already made irreversible
  changes.
  - **Status:** 🟡 important (not blocking — (a) is a safe default and is
    what ships if this is never answered)
  - **Answer:** _<empty>_
  - **Answered by:** _<human>_
  - **Date:** _<YYYY-MM-DD>_

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- 2026-08-31: Read `E07-T01.md` (merged — `GroupRole` enum and the four
  group tables live in `lib/core/persistence/group_tables.dart`), `docs/conventions.md`
  ("Enums" — `.name`, never index), `design/gaps.md` GAP-019 (this task's
  matrix is the group-manage UI's per-row action source, not a second
  UI-side rule), and `agent/memory/lessons/backend.md` (no lesson applies
  directly to a pure, synchronous, no-I/O function — the counter/atomicity
  lessons are about durable state this file never touches). Confirmed
  `AppFailure` lives in `lib/core/auth/google_auth_service.dart` and is
  already imported the same way by `lib/features/messaging/domain/send_message_use_case.dart`.
  Branched `epic_07_task_02` off `origin/epic_07` (has E07-T01 + E07-T12
  merged) per rule 4.
  Wrote `test/features/groups/domain/group_permissions_test.dart` first —
  120-cell exhaustive matrix generated in a loop against a literal
  per-action/per-subject expectation table, plus the named EARS-GROUP-6/7
  tests and two explicit null-subject-throws tests. Ran it: failed to
  compile (`Undefined name 'GroupPermissions'`/`'GroupAction'`) — the
  correct red, since the source file didn't exist yet.
  Implemented `lib/features/groups/domain/group_permissions.dart`:
  `GroupAction` enum (10 values, one per FR-GROUP-002/003 action),
  `GroupPermissions.allows` (the §5 table exactly — no `if (role ==
  owner) return true` shortcut per §6's risk note, since Owner's `leave`
  is a deliberate `false`), `GroupPermissions.check` wrapping it in
  `AppFailure('group.forbidden')`. Three cells the §5 table doesn't
  literally enumerate (`removeMember`/`removeAdmin` against the "wrong"
  subject role, and both against a `null` subject) were resolved per §6's
  own instruction and logged in §Self-review's Deviations rather than
  raised as a new Open Question — none of them widens or narrows what any
  role can actually do.
  Ran the test again: 128/128 green (120 matrix cells + 8 named tests).
  `flutter analyze`: 0 issues. Full `flutter test`: **519/519** green (391
  pre-existing + 128 new). Committed `2eb3997`. `git status --short`
  confirms the diff is confined to exactly the two files in `files:`.
  Pushed `epic_07_task_02` to origin, opened PR into `epic_07`;
  `status: review-requested`.
