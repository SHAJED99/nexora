---
id: E07-T11
epic: E07
type: feature
title: Make-before-break call route migration — establish and validate, then switch, then tear down
layer: backend
size: M
status: todo
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: must, p: P2 }
depends_on: [E07-T09, E07-T10]
blocks: []
traces_to: [FR-CALL-003, FR-CALL-001, FR-CALL-002, FR-ROUTE-009]
required_context: [epics/E04-mesh-routing/tasks/E04-T02.md, epics/E04-mesh-routing/tasks/E04-B01.md, epics/E07-groups-calls/tasks/E07-T09.md, epics/E07-groups-calls/tasks/E07-T10.md, lib/core/routing_engine/routing_engine.dart, spec/srs.md]
design_contract: n/a
files:
  create:
    - lib/core/calls/call_migration_controller.dart
    - test/core/calls/call_migration_controller_test.dart
  update:
    - lib/core/calls/call_session.dart
    - lib/core/calls/call_signaling.dart
  delete: []
started_at:
completed_at:
executed_by:
reviewed_at:
reviewed_by:
review_outcome:
---
# E07-T11 · Make-before-break migration

> ⛔ **This task owns the epic's `EARS-CALL-1` and it has a hard boundary.**
> The *control* of make-before-break — evaluate, establish, validate,
> switch, tear down — is fully buildable and testable today against
> `RoutingEngine` and the `CallMediaTransport` seam. The *media* half —
> actually carrying two audio paths at once during the overlap — cannot be
> built until `OQ-E07-3` (real-time transport) is answered by the human.
> This task builds the control and drives a media transport through the
> seam; it does not invent the transport. See `OQ-E07-12`.

## 1. Goal
FR-CALL-003, mechanically: when a better connection appears mid-call, the
system evaluates it, establishes and validates it, migrates the call onto
it, and only then terminates the old route — so the call does not drop.

## 2. Business logic
- **FR-CALL-003 names four ordered steps and the order is the
  requirement:** *evaluate → establish and validate → migrate → terminate
  the old route*. "Make-before-break" is precisely the claim that step 4
  happens after step 3. A controller that tears down first and reconnects
  fast is break-before-make and does not satisfy this requirement no matter
  how short the gap is.
- **Evaluation is already built and must not be re-implemented.**
  `RoutingEngine.considerMigration(destinationId, profile)` (E04-T02)
  returns `MigrationDecision.stay()` or `.migrateTo(route)`, already
  applying the ≥20% improvement threshold and the 10-consecutive-sample
  stability rule (both TUNABLE constants owned by E04). This controller
  drives it on a timer with `TrafficProfile.realtime` (E07-T10) and acts on
  its answer. **Do not add a second migration heuristic**, and do not tune
  E04's constants from here.
  - Note the sampling contract: `kMigrationStabilityTicks` counts
    `considerMigration` *calls*, not wall-clock time (the engine reads no
    clock). The controller's tick interval therefore sets the real-world
    stability window, and it is an injected value, never a literal.
- **Validate means "a packet made the round trip on the candidate route",
  not "the route was computed".** A computed route whose first hop has
  silently dropped is exactly the case E04-B01 distinguishes
  `noteAttemptedRoute` from `setActiveRoute` for. The controller sends a
  probe over the candidate, waits for its echo within a bounded timeout,
  and only on success calls `setActiveRoute`. A failed probe means: stay,
  do not tear anything down, and do not mark the candidate active.
- **The old route is torn down only after the media transport reports the
  new one carrying.** The controller asks the `CallMediaTransport`
  (E07-T09's seam) to attach the new route, waits for `CallMediaHealth`
  to report it live, and only then detaches the old. With
  `NullCallMediaTransport` this step reports "no media" and the controller
  **stays on the old route** — the correct behaviour for an unbuilt media
  path is to change nothing, never to migrate blind.
- **A migration is never allowed to end the call.** Every failure mode —
  probe timeout, media attach failure, candidate lost mid-switch — resolves
  to "stay on the current route", counted and logged. That is the whole
  point of FR-CALL-003, and it is the invariant the tests must attack
  hardest.
- **Route failure is distinct from route improvement.**
  `RoutingEngine.onRouteFailure` (FR-ROUTE-009) already handles "the
  current route died" — that is break-then-make by necessity, and this
  controller must call it rather than reimplementing recovery. Migration
  (this task) is the optional, opportunistic path; failure recovery is the
  mandatory one. Conflating them is how an opportunistic optimisation ends
  up in the crash path.

## 3. What this task does
- **`CallMigrationController`** — started when a `CallSession` reaches
  `active`, stopped on `ending`:
  - a `Timer.periodic` (injected interval, default 1 s) calling
    `considerMigration(peerDeviceId, TrafficProfile.realtime)`
  - on `.migrateTo(route)`: `noteAttemptedRoute` → send a
    `CallSignalKind.pathProbe` frame over the candidate → await its echo
    within a bounded timeout → `setActiveRoute` → `media.attach(newRoute)`
    → await `CallMediaHealth.live` → `media.detach(oldRoute)`
  - any failure at any step: abandon, stay, increment a counter, leave the
    active route exactly as it was
  - on a route failure signal: delegate to `RoutingEngine.onRouteFailure`,
    not to the migration path
- **`CallSignalKind.pathProbe` / `.pathProbeEcho`** added to E07-T09's
  frame — the probe is an ordinary signaling frame at the `realtime` band,
  so validation uses the same authenticated channel as everything else.
- **`CallSession` gains an observable `activeRoute` and a
  `migrations` event stream** (`attempted`, `validated`, `completed`,
  `abandoned(reason)`), so the (prospective) call UI and any future
  diagnostics read one source of truth.
- Counters: `callMigrationAttempted`, `callMigrationProbeTimeout`,
  `callMigrationMediaFailed`, `callMigrationCompleted`,
  `callMigrationAbandoned`.

## 4. What this task does NOT do
- Does **NOT** implement, choose, or add a real-time media transport
  (`OQ-E07-3`, 🧍 human). Does **NOT** add an audio/codec/WebRTC
  dependency.
- Does **NOT** carry two live audio paths simultaneously — that is the
  media half, and it belongs to whatever implements
  `CallMediaTransport`. This task defines and drives the sequence the
  implementation must satisfy (`OQ-E07-12`).
- Does **NOT** modify `RoutingEngine`, `RouteCostCalculator`,
  `RelayEngine`, or E04's TUNABLE thresholds. It is a caller.
- Does **NOT** add a second migration heuristic, a hysteresis of its own,
  or a "force migrate" API.
- Does **NOT** handle route failure recovery — it delegates to
  `onRouteFailure`.
- Does **NOT** migrate non-call traffic. Messaging already routes
  per-packet; migration is a *session* concept and applies only to calls.
- Does **NOT** add a table, column or migration.

## 5. Contract

### Functions
```yaml
- signature: "CallMigrationController({required RoutingEngine routing, required CallSignaling signaling, required CallMediaTransport media, Duration tickInterval = const Duration(seconds: 1), Duration probeTimeout = const Duration(milliseconds: 1500)})"
  purpose: "both durations injected, never literals at a call site (the MessagingStack.coordinatorTickInterval pattern)"

- signature: "CallMigrationController.start(CallSession session) -> void"
- signature: "CallMigrationController.stop() -> Future<void>"
  purpose: "cancels the timer and every in-flight probe; safe to call twice"

- signature: "CallMigrationController.evaluateOnce() -> Future<MigrationOutcome>"
  returns: "stayed | probeFailed | mediaFailed | migrated(Route) -- exposed so tests drive the sequence deterministically instead of waiting on a timer"

- signature: "enum MigrationOutcome { stayed, probeFailed, mediaFailed, migrated }"

- signature: "CallSession.activeRoute -> Route?"
- signature: "CallSession.migrations -> Stream<CallMigrationEvent>"
  purpose: "attempted | validated | completed | abandoned(reason)"
```

**The ordered sequence is contractual (FR-CALL-003) and an implementation
may not reorder it:**
`considerMigration → noteAttemptedRoute → probe → echo received →
setActiveRoute → media.attach(new) → health live → media.detach(old)`

### Data
**None.**

## 6. Risks
- **The single most likely defect is tearing down early.** Write
  `test_EARS_CALL_1_old_route_is_still_attached_until_the_new_one_is_live`
  first, with a media transport that records the exact order of
  attach/detach calls, and make it fail for the right reason before
  writing the controller.
- `considerMigration` requires an *active* route to migrate away from
  (`_activeRoutes[destinationId]`), and it returns `stay()` when there is
  none. The controller must therefore `setActiveRoute` once when the call
  becomes `active`, or migration will never trigger and every test will
  pass. This is a silent-no-op trap: assert that the first evaluation of a
  call with a better alternative actually attempts.
- A probe echo arriving *after* its timeout must be ignored, not applied
  late. A late echo applied after the controller moved on will switch the
  route out from under a completed decision.
- Timer + async probe + media futures is three concurrency sources on one
  session. Serialise: at most one migration in flight, and a tick during an
  in-flight migration is dropped, not queued.

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS id `1efa3d6`
      — written and confirmed red (compile-failed against the not-yet-existing
      `CallMigrationController`/`MigrationOutcome`/`CallMigrationEvent`
      symbols), then implementation added to turn them green, per §6's own
      "make it fail for the right reason before writing the controller".
- [x] the order-recording fake `CallMediaTransport` `1efa3d6`
      (`_RecordingCallMediaTransport`, `test/core/calls/call_migration_controller_test.dart`)
- [x] `pathProbe`/`pathProbeEcho` frames `1efa3d6`
- [x] `evaluateOnce` — the full ordered sequence `1efa3d6`
- [x] every failure path resolves to "stay", proven per path `1efa3d6`
- [x] one-migration-in-flight serialisation; late echo ignored `1efa3d6`
- [x] `onRouteFailure` delegation (not reimplemented) `1efa3d6`
- [x] `start`/`stop` with timer + probe cancellation `1efa3d6`
      — `stop()` also completes any in-flight probe/health-wait completer
      early (`_pendingWait`) so an in-flight wait is actually cancelled, not
      merely left to run out its own timeout with its result ignored.
- [x] `flutter analyze` clean, full `flutter test` green `1efa3d6`, `c278ad2`

## 8. Acceptance criteria (EARS) + test plan
- **EARS-CALL-1** *(epic-level)*: WHEN a better route becomes available
  mid-call, the system SHALL migrate without dropping the call
  (make-before-break). (FR-CALL-003)
- **EARS-CALL-9**: WHEN a candidate route is selected, the system SHALL
  validate it with a round-trip probe before making it active.
  (FR-CALL-003)
- **EARS-CALL-10**: IF validation or media attachment on the candidate
  route fails, THEN the system SHALL remain on the existing route and the
  call SHALL NOT end. (FR-CALL-003, FR-CALL-001)
- **EARS-CALL-11**: WHEN the active route fails outright, the system SHALL
  use route-failure recovery rather than the migration path.
  (FR-ROUTE-009)

### Tests
- `test_EARS_CALL_1_migration_completes_in_the_contract_order` — the fake
  transport's recorded call order equals §5's sequence exactly.
- `test_EARS_CALL_1_old_route_is_still_attached_until_the_new_one_is_live`
- `test_EARS_CALL_1_call_state_stays_active_across_a_migration`
- `test_EARS_CALL_9_probe_timeout_means_no_setActiveRoute`
- `test_EARS_CALL_9_late_echo_after_timeout_is_ignored`
- `test_EARS_CALL_10_media_attach_failure_leaves_the_old_route_active`
- `test_EARS_CALL_10_null_media_transport_never_migrates`
- `test_EARS_CALL_10_no_failure_path_ends_the_call` — parameterised over
  every failure mode; the session is `active` in all of them.
- `test_EARS_CALL_11_route_failure_delegates_to_on_route_failure`
- `test_only_one_migration_in_flight`
- `test_stability_window_is_the_engines_not_a_second_one` — a candidate
  better for fewer than `kMigrationStabilityTicks` evaluations does not
  migrate, and the controller adds no threshold of its own.

### Manual
1. Three devices; A calls B directly; bring C online as a better two-hop
   path (or degrade the direct link) → the migration events stream shows
   `attempted → validated → completed` and the call stays `active`
   throughout. With `NullCallMediaTransport` the run stops at
   `abandoned(mediaFailed)` and the call still stays `active` — the
   expected v1 outcome.

## 9. Self-review + Definition of Done
### Self-review
- [x] Every §7 item done, with commit hashes — see §7
- [x] `flutter test && flutter analyze` green — `flutter analyze`: "No
      issues found!"; `flutter test` (full suite): **680 passed, 0 failed**
      (668 pre-existing on `origin/epic_07`'s tip (confirmed by a clean
      stash-and-run against that exact commit before implementing) + 12 new,
      all in `test/core/calls/call_migration_controller_test.dart`). One
      unrelated test
      (`test/features/groups/domain/group_key_rotation_service_test.dart`,
      `test_offline_member_missing_a_rotation_fails_loudly_not_silently`)
      failed once under a full-suite run and passed both standalone and on a
      second full-suite re-run — the same one-off cross-test-interaction
      flake shape E07-T10's own run log already recorded for a different,
      equally unrelated test; not touched by, or related to, this task's
      `files:`.
- [x] Diff confined to `files:`; §4 respected — `git diff --stat
      origin/epic_07 HEAD` touches exactly the 4 files this task's own
      `files:` fence names (2 created, 2 updated), nothing else
- [x] `git diff` on `routing_engine.dart`, `route_cost_calculator.dart`,
      `relay_engine.dart` is **empty** — confirmed via
      `git diff origin/epic_07 HEAD -- lib/core/routing_engine/routing_engine.dart lib/core/routing_engine/route_cost_calculator.dart lib/core/routing_engine/relay_engine.dart`
      (zero output)
- [x] No second improvement threshold or stability counter exists
      (`grep -n "0.20\|StabilityTicks" lib/core/calls/` → zero hits) —
      confirmed; two doc comments that originally quoted this exact grep
      pattern / named `RoutingEngine.kMigrationStabilityTicks` were reworded
      to describe the same facts without the literal substrings (`c278ad2`)
      once the check's own dispatch's literal wording was actually run
      against the diff
- [x] `pubspec.yaml` unchanged; no audio/media dependency —
      `git diff origin/epic_07 HEAD -- pubspec.yaml pubspec.lock` is empty
- [x] Every failure path proven to leave the call `active` —
      `test_EARS_CALL_10_no_failure_path_ends_the_call`, parameterised over
      probe-never-echoes, media-attach-fails, media-attaches-but-never-live,
      and candidate-lost-mid-switch (`stop()` called while a migration is
      in flight); every branch asserts `session.state == CallState.active`
      and `session.endReason == null`

**Deviations from spec/design:**

1. **`NullCallMediaTransport.attach` no longer ends the `CallSession` itself
   — that reaction moved to `CallSignaling._track`'s own new
   `_attachInitialMedia` helper.** §5's contract names
   `CallMediaTransport.attach`/`detach`/`health` verbatim and does not
   authorize changing their behaviour; this is a narrow, necessary
   correction, not a reinterpretation of the seam's shape. Before this
   change, `NullCallMediaTransport.attach` called
   `session.endLocally(CallEndReason.failed)` directly (E07-T09's own
   design, correct for that task's own use: the *initial* attach on
   `CallState.active`, where "no media transport" genuinely means "this
   call cannot happen" — EARS-CALL-5). This controller calls
   `CallMediaTransport.attach` a SECOND time, on an already-established
   call, to test whether a *migration candidate*'s media can come up — and
   EARS-CALL-10 requires that specific failure to leave the call `active`,
   never end it. Reusing `NullCallMediaTransport` unchanged would have made
   `test_EARS_CALL_10_null_media_transport_never_migrates` impossible to
   satisfy: any attach attempt, from either caller, would always end the
   call. The fix relocates the ending decision to the one caller for which
   it is actually correct (`CallSignaling._track`, reacting to the
   *initial* attach only) and leaves `CallMediaTransport.attach` itself
   pure — it reports success/failure and nothing more, matching the
   abstract class's own pre-existing doc comment ("Returns an `AppFailure`
   if media could not be established; `null` on success" — it never said
   *and ends the call*). `CallSignaling`'s own EARS-CALL-5 test coverage is
   unaffected: the net behaviour through `CallSignaling.invite`/`accept` is
   byte-for-byte identical (confirmed: the accept-then-media-fails
   integration inside
   `test_EARS_CALL_2_invite_accept_reaches_active_on_both_ends`,
   `call_signaling_test.dart`, passes unmodified — that file is not in this
   task's `files:` and was not touched).
2. **`considerMigration`'s own stability-tracking is per-destination and
   resets to a fresh 1-sample count the moment it returns `migrateTo`
   (`RoutingEngine`'s own pre-existing behaviour, not something this task
   changed).** A consequence worth naming explicitly since it shapes one
   test's own expectation: after ANY migration attempt (successful or
   abandoned) for a destination, the very next `considerMigration` call for
   that same destination starts counting from 1 again, even if the same
   candidate is still objectively better. `test_EARS_CALL_9_late_echo_after_timeout_is_ignored`
   asserts this directly (a second `evaluateOnce()` call immediately after
   an abandoned attempt correctly returns `stayed`, not another immediate
   `migrated`) as evidence the controller is not left in a stuck state by a
   late echo — not as a claim that this task changed the engine's own
   sampling contract.
3. **§9's own literal grep line quoted the very substrings it exists to
   detect, inside this file's own doc comments describing that check** (see
   the self-review bullet above) — corrected by rewording rather than by
   weakening the check itself.

**Files actually touched:**
- `lib/core/calls/call_migration_controller.dart` (created)
- `test/core/calls/call_migration_controller_test.dart` (created)
- `lib/core/calls/call_session.dart` (updated — `CallSignalKind.pathProbe`/
  `.pathProbeEcho` appended; `CallMigrationEvent` sealed hierarchy;
  `CallSession.activeRoute`/`recordActiveRoute`/`migrations`/
  `recordMigrationEvent`)
- `lib/core/calls/call_signaling.dart` (updated — `_kindTags` for the two
  new kinds; `CallMediaHealth.live` appended; `NullCallMediaTransport`'s
  session-ending side effect relocated to `CallSignaling._attachInitialMedia`;
  `CallSignaling.sendPathProbe`/`pathProbeEchoes` and the
  `_handlePathProbe`/`_handlePathProbeEcho` inbound handlers)

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id —
      EARS-CALL-1: `test_EARS_CALL_1_migration_completes_in_the_contract_order`,
      `test_EARS_CALL_1_old_route_is_still_attached_until_the_new_one_is_live`,
      `test_EARS_CALL_1_call_state_stays_active_across_a_migration`;
      EARS-CALL-9: `test_EARS_CALL_9_probe_timeout_means_no_setActiveRoute`,
      `test_EARS_CALL_9_late_echo_after_timeout_is_ignored`; EARS-CALL-10:
      `test_EARS_CALL_10_media_attach_failure_leaves_the_old_route_active`,
      `test_EARS_CALL_10_null_media_transport_never_migrates`,
      `test_EARS_CALL_10_no_failure_path_ends_the_call`; EARS-CALL-11:
      `test_EARS_CALL_11_route_failure_delegates_to_on_route_failure`; plus
      `test_only_one_migration_in_flight`,
      `test_stability_window_is_the_engines_not_a_second_one`, and a
      `CallSignalingFrame` round-trip test for the two new kinds
      (`test/core/calls/call_migration_controller_test.dart` — the
      pre-existing `'every kind round-trips to its own tag'` test in
      `call_signaling_test.dart`, unmodified, also automatically covers
      both new kinds since it iterates `CallSignalKind.values`)
- [ ] Review APPROVE from a **different model** than `executed_by` (rule 5)
- [ ] Squash-merged to `epic_07`; tracker + metrics stamped
- [ ] 🧍 Human `verified`

## Open Questions
- **OQ-E07-12 — the media half of make-before-break needs the transport
  decision first.** This task proves the *control* sequence: evaluate,
  probe, validate, switch, tear down, and never drop the call. What it
  cannot prove is that audio actually keeps flowing across the switch,
  because carrying two paths concurrently is a property of the media
  transport, and no media transport exists (`OQ-E07-3`, 🧍 human). The
  three shapes the answer takes, depending on that decision:
  **(a)** a datagram-style transport over the existing mesh — the
  controller simply addresses frames to the new next hop and the overlap is
  free (a few duplicated packets); **(b)** a connection-oriented native
  channel — the overlap requires holding two native connections open,
  which is a Pigeon API change (`ADR-0004`) and a real platform-side cost;
  **(c)** WebRTC — ICE restart handles this internally and the controller
  becomes a thin driver over someone else's migration logic.
  *Advisory: settle `OQ-E07-3` first; this task's seam is deliberately
  shaped so all three plug in without changing the sequence.* The media
  half is listed as a prospective task in `epic.md` §Tasks, not sharded.
  - **Status:** 🟡 open — **blocked by `OQ-E07-3` (human)**; owner: the
    prospective media-path task
  - **Answer:** _<empty>_
  - **Answered by:** _<human>_
  - **Date:** _<YYYY-MM-DD>_

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- 2026-09-02, builder session. Note: a prior dispatch of this exact task
  failed immediately due to an unrelated API rate-limit error before any
  real work was done (no branch existed) — this session started completely
  fresh. Branched `epic_07_task_11` off `origin/epic_07` at `5f6f21b`. Read
  the task file in full, `epics/E07-groups-calls/tasks/E07-T09.md` and
  `-T10.md` (both merged, done), `lib/core/routing_engine/routing_engine.dart`
  in full (`considerMigration`/`noteAttemptedRoute`/`setActiveRoute`/
  `onRouteFailure`), `lib/core/calls/call_session.dart` and
  `call_signaling.dart` in full, and `agent/memory/lessons/backend.md` (3
  entries, none directly on-point for this area but noted).
- Designed the contract additions in `call_session.dart`
  (`CallSignalKind.pathProbe`/`.pathProbeEcho` appended; `CallMigrationEvent`
  sealed hierarchy; `CallSession.activeRoute`/`migrations`) and
  `call_signaling.dart` (`sendPathProbe`/`pathProbeEchoes`,
  `_handlePathProbe`/`_handlePathProbeEcho`, `CallMediaHealth.live`) before
  writing the controller, per §5's own contract.
- Found and fixed the `NullCallMediaTransport` conflict described in
  §9 Deviation 1 before writing any tests against it: E07-T09's
  `NullCallMediaTransport.attach` ends the `CallSession` itself, which is
  correct for `CallSignaling`'s own *initial* attach (EARS-CALL-5) but
  directly contradicts EARS-CALL-10 the moment this controller reuses the
  same seam for a *migration* attach on an already-active call. Relocated
  the ending decision to `CallSignaling._attachInitialMedia` (the one
  caller for which it is correct), leaving `CallMediaTransport.attach`
  itself pure. Confirmed EARS-CALL-5's own existing coverage in
  `call_signaling_test.dart` (not in this task's `files:`, not touched)
  still passes unmodified after the change.
- Wrote `test/core/calls/call_migration_controller_test.dart` FIRST against
  the not-yet-existing `CallMigrationController`/`MigrationOutcome`/
  `CallMigrationEvent` symbols — compile-failed as expected (red for the
  right reason, §6's own instruction). Chose the harness deliberately:
  `CallMigrationController`'s contract requires a concrete `CallSignaling`,
  not an interface, so every test backs it with a real (throwaway,
  single-device, never wired to a mocked transport) `MessagingStack`, and a
  small `CallSignaling` subclass (`_FakeSignaling`) overriding only the two
  new public methods (`sendPathProbe`/`pathProbeEchoes`) for deterministic,
  fast probe/echo delivery with no real crypto session or mocked Pigeon
  channel needed — mirroring `call_signaling_test.dart`'s own documented
  "purely local" harness choice. `RoutingEngine`/`CallMediaTransport` are
  never faked away from their real logic: `_RecordingRoutingEngine`
  overrides four methods purely to append order markers, always delegating
  to `super` for the actual E04 decision; `_RecordingCallMediaTransport` is
  a fresh, from-scratch implementation of E07-T09's own abstract seam,
  exactly as `NullCallMediaTransport` is.
- Hit and fixed two genuine timing bugs while turning the tests green (both
  in the tests, not the implementation):
  1. A broadcast `Stream<CallMigrationEvent>` event added synchronously (or
     near-synchronously) inside the same async chain an `await`-ing caller
     resumes from is **not** guaranteed delivered to a listener by the time
     that `await` resolves — the exact "pump the event queue" note
     E07-T09's own run log already recorded for a different broadcast
     stream in this codebase. Fixed by adding `await pumpEventQueue();`
     after each `evaluateOnce()` call whose test then reads
     `session.migrations` events, rather than a manual `Future.delayed`.
  2. A fake `CallMediaTransport.attach` that emits `CallMediaHealth.live`
     via `scheduleMicrotask`/synchronously inside `attach()` itself races
     the controller's own `media.health.listen(...)` call, which only
     happens AFTER `attach()`'s returned `Future` has already resolved —
     the emission can fire before anyone is listening and the event is
     lost (broadcast streams never replay). Fixed by having
     `_RecordingCallMediaTransport` (and `_FakeSignaling`'s echo) schedule
     their deferred event via a real `Timer`, which only fires on a later
     event-loop turn, after the awaiting code has had a chance to
     subscribe. Documented on both fakes' own doc comments so a future
     editor does not "simplify" this back into a race.
  3. `test_EARS_CALL_10_no_failure_path_ends_the_call`'s "probe never
     echoes" scenario initially used `_FakeSignaling`'s default
     `autoEcho = true`, so the probe actually validated and the scenario
     silently exercised the happy path instead of a failure mode — caught
     by the scenario's own `expect(outcome, isNot(MigrationOutcome.migrated))`
     assertion failing loudly (`Actual: MigrationOutcome.migrated`). Fixed
     by adding an explicit `autoEcho: false` parameter to that scenario.
- Confirmed `considerMigration`'s own stability tracking is per-destination
  and restarts from 1 sample the instant it returns `migrateTo` (§9
  Deviation 2) — not something this task changed, but load-bearing for
  `test_EARS_CALL_9_late_echo_after_timeout_is_ignored`'s own final
  assertion (a second `evaluateOnce()` call right after an abandoned
  attempt correctly returns `stayed`, proving the controller is not stuck).
- `flutter analyze`: clean, "No issues found!", both before and after the
  grep-check doc-comment fix (`c278ad2`).
- Baseline: stashed this task's changes and ran the full suite against
  `origin/epic_07`'s exact tip (`5f6f21b`) to get an authoritative
  before-count rather than trusting the dispatch note's own "665 after
  T10" figure (which predates T12/T13/T14's own merges): **668 passed, 0
  failed**. Restored the stash, re-ran: **680 passed, 0 failed** (668 + 12
  new, all in `call_migration_controller_test.dart`). Rebased cleanly onto
  `origin/epic_07`'s further-advanced tip (`20eeead`, two doc-only T07
  commits, zero overlap with this task's files) — fast-forward, zero
  conflicts. Re-ran the full suite post-rebase: one unrelated test
  (`group_key_rotation_service_test.dart`'s
  `test_offline_member_missing_a_rotation_fails_loudly_not_silently`)
  failed once, then passed both standalone and on a full re-run — the same
  one-off cross-test flake shape E07-T10's own run log already recorded for
  a different, equally unrelated test; confirmed **680/680** on the clean
  re-run, `git diff` confirms this task never touched that test file or
  anything under `lib/core/messaging/group_key_rotation*`.
- Self-review checklist's own literal grep line
  (`grep -n "0.20\|StabilityTicks" lib/core/calls/`) initially returned two
  hits — both doc comments in `call_migration_controller.dart` quoting the
  check itself / naming `RoutingEngine.kMigrationStabilityTicks` by
  reference, never a re-declaration. Reworded rather than argued around
  (`c278ad2`); re-ran the grep to confirm zero hits, then re-ran
  `flutter analyze`/`flutter test` (680/680 unchanged) to confirm the
  wording-only change broke nothing.
- Manual scenario (task file §8) not run against real hardware or a real
  three-device mesh (no device pair available in this environment) —
  covered instead by `test_EARS_CALL_1_migration_completes_in_the_contract_order`
  (drives the exact `attempted → validated → completed` sequence the
  manual script describes) and `test_EARS_CALL_10_null_media_transport_never_migrates`
  (drives the exact `NullCallMediaTransport` stop-at-`abandoned(mediaFailed)`,
  call-stays-`active` outcome the manual script's second sentence
  describes), the same fidelity level T09's own manual-script coverage
  established for its equivalent scenario.
