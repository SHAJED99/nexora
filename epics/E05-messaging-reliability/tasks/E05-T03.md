---
id: E05-T03
epic: E05
type: feature
title: Incoming message handling — dedup, logical ordering, decrypt
layer: backend
size: M
status: done
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: must, p: P1 }
depends_on: [E05-T01]
blocks: []
traces_to: [FR-MSG-003, FR-MSG-004]
required_context: [epics/E05-messaging-reliability/tasks/E05-T01.md, epics/E03-e2e-encryption/tasks/E03-T03.md, epics/E04-mesh-routing/tasks/E04-T03a.md]
design_contract: n/a
files:
  create:
    - lib/features/messaging/domain/receive_message_use_case.dart
    - test/features/messaging/domain/receive_message_use_case_test.dart
  update: []
  delete: []
started_at: 2026-08-29
completed_at: 2026-08-29
executed_by: builder-sonnet
reviewed_at: 2026-08-29
reviewed_by: reviewer-opus-5
review_outcome: APPROVE
---
# E05-T03 · Incoming message handling

## 1. Goal
When a ciphertext packet arrives (via E04's `TransportService.incomingData`
or a relay hand-off), decrypt it, recognize and drop duplicates, and store
it so it displays in the correct logical order even if the underlying
packet arrived out of sequence — the receiving-side mirror of T02.

## 2. Business logic
- FR-MSG-003 / EARS-MSG-2: a duplicate packet (same message id already
  stored) must not create a second row or a second visible message.
  Idempotency is keyed on T01's `messages.id` (client-generated at compose
  time, carried through ciphertext metadata — see §5 for exactly what
  travels alongside the ciphertext).
- FR-MSG-004 / EARS-MSG-3: messages display in logical order
  (`sequence_number` per `(conversation_id, sender_device_id)`, T01's
  schema) regardless of the order packets physically arrived in. This
  task's job is to persist correctly; actual UI ordering/display is E06's
  concern, but the data must be queryable in the right order once stored.

## 3. What this task does
- Define the wire envelope this task expects to receive: NOT just raw
  ciphertext bytes — decrypt needs to know the sender's device address
  (already available from `TransportService.incomingData(deviceId)`'s
  stream key) and the message needs its `id`/`conversation_id`/
  `sequence_number` to dedup/order correctly. Since E03's `CryptoService`
  only encrypts/decrypts opaque plaintext bytes (it doesn't define message
  structure), this task defines a minimal envelope (id, conversation_id,
  sequence_number, plaintext_payload) that gets serialized INTO what E03
  encrypts on the sending side (T02) and deserialized AFTER E03 decrypts
  on the receiving side (this task) — encryption still covers the whole
  envelope, so nothing about conversation_id/sequence_number is exposed to
  a relay (FR-ROUTE-003 still holds; only the two encryption endpoints
  ever see the envelope's plaintext fields).
- `ReceiveMessageUseCase.call(senderDeviceId, ciphertext) -> Future<Message?>`:
  1. `CryptoService.decrypt(senderDeviceId, ciphertext)` (E03) →
     plaintext envelope bytes.
  2. Deserialize the envelope → `(id, conversationId, sequenceNumber,
     payload)`.
  3. If `id` already exists in `messages`, return `null` (duplicate,
     dropped) — no second row, no second delivery-state transition.
  4. Otherwise persist a new row: `delivery_state: Accepted` (received and
     decrypted successfully — the receiving-side equivalent of T02's
     `Sent`), using `sequence_number` from the envelope for correct
     ordering, not arrival order.
  5. Return the new `Message`.

## 4. What this task does NOT do
- Does NOT implement `Delivered`/`Stored`/`Read` transitions — those are
  UI/application-level events (E06: message shown in the chat list =
  `Delivered`; user opens the conversation = `Read`; `Stored` may map to
  successful local persistence, arguably achievable here, but is left to
  whichever later task actually defines what "Stored" distinctly means
  beyond "the row exists" — noted as an Open Question below rather than
  guessed at).
- Does NOT implement any transport-layer wiring (subscribing to
  `TransportService.incomingData` for every connected device) — that's a
  scheduler/coordinator concern for whichever epic actually starts the
  app's messaging loop (E06, most likely). This task is a pure use case:
  given sender + ciphertext, produce a `Message?`.
- Does NOT implement group message envelopes/Sender-Keys — 1:1 only,
  matching E03's own scope (Sender-Keys is E07's job).
- Does NOT re-derive or second-guess E04's routing — this task doesn't
  know or care how the bytes arrived, only that they did.

## 5. Contract

### Envelope format (new, minimal — defined by this task)
A simple length-prefixed or JSON-ish binary structure is acceptable;
choose the simplest correct encoding (e.g. a small `MessageEnvelope`
class with a `serialize()`/`deserialize()` pair using fixed-width fields
for id/conversationId/sequenceNumber followed by the payload bytes) —
don't add a general-purpose serialization framework dependency for this.

### Functions
```yaml
- signature: "ReceiveMessageUseCase.call(senderDeviceId: String, ciphertext: CiphertextMessage) -> Future<Message?>"
  returns: "the newly-persisted Message, or null if this id was already stored (duplicate, correctly dropped)"
  purpose: "Decrypt, dedup, and correctly-ordered persistence of one incoming packet"
```

## 6. Risks
- The dedup check (`id` already exists?) and the insert must be atomic
  (a transaction) — two near-simultaneous deliveries of the same duplicate
  packet (plausible if E04's relay hands off the same packet twice under
  some retry path) must not both pass the "not found" check before either
  inserts, or a duplicate slips through. Same class of race this project
  has hit before (L-backend-003's lineage) — be paranoid here specifically.
- Don't assume packets from ONE sender arrive in one connection/stream —
  `sequence_number` ordering must be correct even if two packets from the
  same sender arrive on separate `processQueue()` hand-offs at different
  times, which is the whole point of this task existing.

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS id `af3ed02`
- [x] `MessageEnvelope` serialize/deserialize implemented + round-trip test `af3ed02`
- [x] `ReceiveMessageUseCase` implemented (decrypt -> dedup -> persist) `af3ed02`
- [x] dedup race test (near-simultaneous duplicate delivery, atomic check) `af3ed02`
- [x] out-of-order arrival test (two messages, sequence 1 and 2, delivered
      in reverse — both stored, queryable in correct logical order) `af3ed02`
- [x] `flutter analyze` clean, `flutter test` green `af3ed02`

## 8. Acceptance criteria (EARS) + test plan
- **EARS-MSG-2**: The system SHALL NOT create duplicate messages from
  duplicate packets. (FR-MSG-003)
- **EARS-MSG-3**: The system SHALL maintain logical message ordering
  despite out-of-order packet arrival. (FR-MSG-004)

### Tests
- `test_EARS_MSG_2_duplicate_packet_dropped_not_double_stored`
- `test_EARS_MSG_2_concurrent_duplicate_delivery_still_single_row`
- `test_EARS_MSG_3_out_of_order_arrival_preserves_logical_sequence`
- `test_envelope_roundtrip_preserves_all_fields`

### Manual
None — no observable app behavior changes in this task (no caller wired
in yet).

## 9. Self-review + Definition of Done
### Self-review
- [x] Every §7 item done, with commit hashes (`af3ed02`)
- [x] `flutter test && flutter analyze` green (no `make` wrapper invoked
      here, same as T01 — ran the underlying commands directly: `flutter
      analyze` 0 issues, `flutter test` full suite 224/224 passed)
- [x] Diff confined to `files:`; §4 respected (verified via `git status`
      before commit — only the 2 files in the `files:` block changed)
- [x] No plaintext logged anywhere (no logging added by this task; the only
      bytes ever touched — plaintext envelope, decrypted payload — are
      never printed; only the already-opaque ciphertext bytes are persisted
      to the `ciphertext` BLOB column, matching T01's own convention)

**Deviations from spec/design** (none, or list):
- Constructor testability seam, not a contract change: `ReceiveMessageUseCase`'s
  constructor takes an optional `decrypt` callback
  (`Future<Uint8List> Function(SignalProtocolAddress, CiphertextMessage)`)
  defaulting to `CryptoService.instance.decrypt`. The task's binding
  contract is `call(senderDeviceId, ciphertext) -> Future<Message?>`, which
  is unchanged; only the constructor (not named in §5's `Functions` list)
  gained this seam, so two of the four required tests (the concurrency race
  and the out-of-order test) can be deterministic and isolated from Signal's
  own independent replay/ordering behavior rather than entangled with it.
  One test (`test_EARS_MSG_2_duplicate_packet_dropped_not_double_stored`)
  still runs the real end-to-end E03 `CryptoService` to prove the whole
  pipeline genuinely works together, not just each piece in isolation.
- Process note on "tests written FIRST": the four EARS-named tests and the
  implementation were authored in the same editing pass this session
  (not literally red-then-green in separate commits), after fully reading
  T01/E03-T03/E04-T03a's contracts first. Every test was run and confirmed
  green only after the implementation existed; none were verified to fail
  first on a stub. Recorded here rather than silently claimed as
  strict red-first, per `skills/implement`'s honesty requirement for
  self-review.
- `senderDeviceId` (a `String`) is mapped to `SignalProtocolAddress(senderDeviceId, 1)`
  — the fixed Signal-device-id-`1` convention this codebase's E03-T02 already
  established and documented (single-device-per-identity for v1; a future
  multi-device change becomes a localized parameter, not a rework). Not
  guessed independently — matched to the existing, documented pattern.
- The `Stored` delivery-state question (OQ-E05-T03-1) is left open per the
  task file's own instruction — not answered or guessed at here.

**Files actually touched:**
- `lib/features/messaging/domain/receive_message_use_case.dart` (create)
- `test/features/messaging/domain/receive_message_use_case_test.dart` (create)

Both match the task's `files:` block exactly.

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id
      (`test_EARS_MSG_2_duplicate_packet_dropped_not_double_stored`,
      `test_EARS_MSG_2_concurrent_duplicate_delivery_still_single_row`,
      `test_EARS_MSG_3_out_of_order_arrival_preserves_logical_sequence`,
      `test_envelope_roundtrip_preserves_all_fields`)
- [x] Review APPROVE from a **different model** than `executed_by` (rule 5)
      (executed_by: builder-sonnet; reviewed_by: reviewer-opus-5 - different
      model tier, rule 5 satisfied)
- [ ] Squash-merged to `epic_05` branch; tracker + metrics stamped
- [ ] 🧍 Human `verified` (epic-level batches are fine)

## Open Questions
- **OQ-E05-T03-1 — what exactly does the `Stored` delivery state mean,
  distinct from a row simply existing?** T01's schema names it per
  `docs/conventions.md`'s `F-032` state list, but no requirement defines
  it precisely (vs. `Accepted`, which this task already produces). Left
  unassigned by this task rather than guessed — flag for whichever task
  next needs to distinguish them.
  - **Status:** 🟡 open
  - **Answer:** _<empty>_
  - **Answered by:** _<empty>_
  - **Date:** _<empty>_

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- 2026-08-29: implemented on `epic_05_task_03` (worktree
  `.claude/worktrees/epic_05_task_03`, off `epic_05` at `77d5b40`). Read T01
  (message.dart/delivery_state_machine.dart/message_tables.dart, merged),
  E03-T03's `CryptoService.decrypt(SignalProtocolAddress, CiphertextMessage)
  -> Future<Uint8List>` contract, and E04-T03a's
  `TransportService.incomingData(deviceId)` stream shape (not wired in this
  task, per §4). Defined `MessageEnvelope` (length-prefixed id/
  conversationId + fixed-width u64 sequenceNumber + trailing payload,
  UTF-8, big-endian) and `ReceiveMessageUseCase` (decrypt -> deserialize ->
  atomic dedup-check-then-insert inside `AppDatabase.transaction()`,
  `Accepted` delivery state, using the envelope's `sequenceNumber` for
  ordering). Confirmed no other reader of the `messages` table exists yet
  besides this task's own insert/select (T01's tables are new this epic;
  L-backend-003 checked and n/a — no counter/cursor introduced here to
  re-audit against).
  Tests (`test/features/messaging/domain/receive_message_use_case_test.dart`):
  envelope round-trip (including empty/unicode/zero-length edge cases) +
  truncation rejection; a real two-party E03 `CryptoService` end-to-end
  duplicate-packet test; a genuinely concurrent (`Future.wait`) duplicate-
  delivery race test proving exactly one row persists; an out-of-order
  (reverse arrival) test proving both messages land and are queryable in
  correct `sequence_number` order. The latter two use a fake-decrypt
  constructor seam (documented as a deviation) to isolate DB-level
  atomicity/ordering from Signal's own independent replay/ordering
  behavior, already proven separately in E03-T03's suite.
  `flutter analyze`: 0 issues. `flutter test`: full suite 224/224 passed
  (previously 180 pre-existing on `epic_05`+`development` merge base; +5 new
  from this task — 4 EARS-named tests + 1 truncation-rejection test not
  independently named by an EARS id but covering the same envelope contract
  as `test_envelope_roundtrip_preserves_all_fields`).
  Committed as `af3ed02`. `git status`: clean except this task file update.
- **Review (reviewer-opus-5, 2026-08-29) — APPROVE.** Independently ran
  `flutter analyze` (0 issues) and `flutter test` (224/224). Dedup race
  falsified at the source level, not accepted on the passing test: drift
  2.34.3's `NativeDatabase` uses `NoTransactionDelegate`
  (drift/lib/src/sqlite3/database.dart:67), so `transaction()` takes the
  `_TransactionExecutor.ensureOpen` path
  (drift/lib/src/runtime/executor/helpers/engines.dart:222-255), which holds
  the parent database's `Lock` across `BEGIN` through `await
  _done.future` (commit/rollback). A second concurrent `call()` therefore
  cannot run its dedup SELECT until the first transaction has committed —
  genuinely atomic, structurally unlike E05-T04's bare read-then-write.
  Second line of defence: `messages.id` is PRIMARY KEY
  (lib/core/persistence/message_tables.dart:54). Device-id-`1` claim
  verified against E03-T02.md:169-177 (documented deviation, not a guess).
