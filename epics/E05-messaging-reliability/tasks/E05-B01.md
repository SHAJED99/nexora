---
id: E05-B01
epic: E05
type: bug
title: The send side never produces the MessageEnvelope the receive side requires — T02 encrypts raw plaintext, T03 deserializes an envelope from it, so no message this epic sends can be received by this epic's receiver
layer: backend
size: M
status: done
owner_agent: builder
model: any
preferred_agent: any
started_at: 2026-08-29
completed_at: 2026-08-29
executed_by: "claude-sonnet-5 (builder), round 2: claude-sonnet-5 (builder)"
reviewed_at: 2026-08-29
reviewed_by: "claude-opus-5 (reviewer) — round 1 CHANGES (F1 blocking, F2 disclosure), round 2 APPROVE"
review_outcome: approved
token_estimate: { tier: M, range: "50k-150k" }
severity: S2
priority: { moscow: must, p: P2 }
depends_on: [E05-T02, E05-T03]
blocks: []
traces_to: [FR-MSG-003, FR-MSG-004, FR-MSG-001]
required_context: [epics/E05-messaging-reliability/tasks/E05-T02.md, epics/E05-messaging-reliability/tasks/E05-T03.md, epics/E05-messaging-reliability/epic.md]
design_contract: n/a
found_by: reviewer (claude-opus-5) during the E05 end-of-epic bug sweep
found_in: seam between E05-T02 (owns the sending path) and E05-T03 (owns the wire envelope) — T03's contract assigns the serialize step to T02, T02's contract never mentions it, so no task owned it
files:
  create:
    - test/features/messaging/domain/message_envelope_seam_test.dart
    - lib/features/messaging/domain/message_envelope.dart
  update:
    - lib/features/messaging/domain/send_message_use_case.dart
    - lib/features/messaging/domain/receive_message_use_case.dart
    - epics/E05-messaging-reliability/tasks/E05-T02.md
  delete: []
---
# E05-B01 · The wire envelope has a consumer but no producer

## The defect
`E05-T03.md` §3 defines the wire format and states plainly who writes it:

> this task defines a minimal envelope (id, conversation_id, sequence_number,
> plaintext_payload) **that gets serialized INTO what E03 encrypts on the
> sending side (T02)** and deserialized AFTER E03 decrypts on the receiving
> side (this task)

`receive_message_use_case.dart:40-42` repeats the claim as settled fact in
the source itself ("the sending side (T02) serializes this envelope INTO the
plaintext before encrypting").

**T02 does no such thing.** `send_message_use_case.dart:123` is:

```dart
ciphertext = await _encrypt(recipientDeviceId, plaintext);
```

`plaintext` is the caller's raw bytes, passed straight through. No
`MessageEnvelope` is constructed anywhere in `lib/` outside
`receive_message_use_case.dart`'s own class declaration — `grep -rn
"MessageEnvelope(" lib/` returns only the constructor definition, and every
construction site in the repo is inside
`test/features/messaging/domain/receive_message_use_case_test.dart`, which
fabricates its own envelopes.

So the receive side's dedup key (`envelope.id`) and its ordering key
(`envelope.sequenceNumber`) are recovered from a structure the send side
never emits.

## Why the green suite did not catch it
Both tasks pass their own tests because each stands up its own half of the
seam:
- T02's tests inject an `encrypt` seam and assert sequencing/state
  transitions; the word `Envelope` does not appear in
  `send_message_use_case_test.dart` at all.
- T03's tests build a `MessageEnvelope`, serialize it, hand it to a fake
  `decrypt`, and assert dedup/ordering.

Neither test ever puts T02's output into T03's input. That is the definition
of a seam bug: task-level tests prove tasks, only the sweep proves the epic.

## Repro (reviewer-verified 2026-08-29 on `epic_05` @ `af86907`)
Throwaway probe, deleted after the run. Identity "encryption" is used — the
single most favourable case, where the receiver decrypts bytes *identical*
to what the sender encrypted, so nothing else can be blamed:

1. `SendMessageUseCase(db: <in-memory>, selfDeviceId: 'device-A',
   encrypt: (r, p) async => p, enqueue: (d, payload, pr, ttl) async
   { onTheWire = payload; return 'relay-1'; })`
2. `await useCase.call('conv-1', 'device-B', utf8.encode('hello world'))`
3. Feed `onTheWire` to `MessageEnvelope.deserialize(...)` — exactly what
   `ReceiveMessageUseCase.call` does at `receive_message_use_case.dart:200`
   with the decrypted plaintext.

**Expected** (T03 §3 step 2, FR-MSG-003/FR-MSG-004): the envelope
deserializes, yielding the sender's `id`, `conversationId` and
`sequenceNumber`, so the receiver can dedup and order.

**Actual:** probe output —
```
PROBE wire bytes = [104, 101, 108, 108, 111, 32, 119, 111, 114, 108, 100]
PROBE deserialize threw: FormatException: MessageEnvelope: truncated,
  missing id bytes or conversationId-length header
```
The bytes on the wire are the literal plaintext `hello world`. Every message
this epic can send is unreceivable by this epic's receiver.

## Second, worse failure mode: silent mis-parse
`FormatException` is the *lucky* outcome. `deserialize`
(`receive_message_use_case.dart:108-152`) trusts the first big-endian `u32`
as `idLength` and only length-checks the buffer. A raw payload that happens
to begin with small-valued bytes — entirely plausible for the binary
payloads E06 will actually send (a length-prefixed blob, a protobuf, an
image chunk) — parses "successfully" into a garbage `id`, garbage
`conversationId` and garbage `sequenceNumber`, and is then **inserted as a
real message row** at `:215-227`. Two consequences worth naming:
- a corrupt row with a nonsense conversation/order lands in the store, and
- because dedup is keyed on `messages.id` (`:203-205`), a garbage id that
  collides with an existing row causes a *legitimate* message to be silently
  dropped as a "duplicate".

There is no magic number, no version byte, and no length check that the
declared field lengths sum to the buffer size.

## Severity: S2 (major function broken, no workaround)
The epic's central value — "messages that reliably arrive exactly once, in
order" (`epic.md` §Business goal) — cannot happen at all across the two
components this epic built to do it. EARS-MSG-2 (no duplicates) and
EARS-MSG-3 (logical ordering) are demonstrated only against test-fabricated
envelopes, never against anything the product can produce. There is no
workaround short of writing the missing code.

Not S1: nothing crashes or loses data *today*, because no production caller
wires `SendMessageUseCase` to `ReceiveMessageUseCase` yet (T02 §4, T03 §4 —
transport wiring is deferred). It becomes S1 the moment E06 wires them, via
the silent-mis-parse path above.

Severity is the reviewer's call per `skills/bug-sweep`. **Priority is the
human's** (`bug_priorities` gate, rule 3) — left `TBD-human`. Reviewer's
advisory: fix before E06 starts, because E06's first act is to wire exactly
this seam, and it will otherwise be built on a false comment in
`receive_message_use_case.dart:40-42`.

## Root cause
A contract written in one task about *another* task's behaviour, with no
gate that checks the other task agrees. T03 was sharded and implemented
after T02 was already merged (`ea7c9fb` then `984f83e`), and T03's author
described the sending side's obligation in T03's own file and source. T02's
file — the binding contract for the sending side — never mentions an
envelope: its §3 step 2 says only "calls `CryptoService.encrypt(...)` to get
the ciphertext", and its §5 signature takes an opaque `plaintext`. Neither
reviewer was wrong within their own task's fences; the obligation lived in
neither `files:` list.

## Related gap in the same seam (fix together)
The two halves also disagree on the *type* crossing the transport:
- T02 hands `RelayEngine.enqueue` a `Uint8List`
  (`send_message_use_case.dart:188`).
- T03 accepts a `CiphertextMessage` object
  (`receive_message_use_case.dart:194-197`).

Nothing in the repo converts the relayed bytes back into a
`CiphertextMessage` (`SignalMessage`/`PreKeySignalMessage` reconstruction is
never called anywhere in `lib/`). Whatever fixes the envelope must also
decide, and write down, how the wire bytes are re-typed on arrival —
otherwise the same "no producer" gap simply moves one layer out.

## Proposed fix direction (builder to confirm; not prescriptive)
- Move `MessageEnvelope` out of `receive_message_use_case.dart` into a file
  both sides import, so the wire format has exactly one definition with two
  users rather than one user and a comment. (This changes `files:` for both
  T02 and T03's source — declare it.)
- In `SendMessageUseCase.call`, build the envelope from the id and
  sequence number it already assigns, `serialize()` it, and encrypt *that*.
  Note the ordering constraint this creates: today the sequence number is
  assigned **inside** the transaction at `send_message_use_case.dart:149-170`,
  *after* encryption at `:123` — but the envelope must contain the sequence
  number, so it must be known before encryption. Re-deriving the order
  safely without reintroducing the read-then-write race that T02's §6 and
  L-backend-003 are about is the real work in this fix, and it must not be
  hand-waved: reserve the number inside a transaction first, then encrypt,
  then persist, keeping the critical section free of the crypto call.
- Harden `deserialize` against the silent mis-parse: reject any buffer where
  the declared lengths do not account for the buffer exactly, and consider a
  version/magic byte so a future format change is detectable rather than
  mis-parsed.
- Correct the now-false comment at `receive_message_use_case.dart:40-42` and
  T02's §3/§5 so the contract and the code say the same thing.

## Regression test (required before this closes)
`test/features/messaging/domain/message_envelope_seam_test.dart` — must fail
on `af86907` and pass after:
- `test_EARS_MSG_2_sent_message_is_receivable_by_receive_use_case` — the
  §Repro end to end with an identity encrypt/decrypt pair: send via
  `SendMessageUseCase`, take the exact bytes handed to `enqueue`, feed them
  to `ReceiveMessageUseCase` on a *second* database, and assert the received
  row's `id`, `conversationId` and `sequenceNumber` equal the sender's.
- `test_EARS_MSG_2_duplicate_of_a_really_sent_message_is_dropped` — deliver
  the same real wire bytes twice; the second call returns `null` and no
  second row exists. (Today's dedup test proves this only for fabricated
  envelopes.)
- `test_EARS_MSG_3_out_of_order_delivery_of_really_sent_messages_orders_by_sequence`
  — send seq 0 and 1 from the sender, deliver in reverse, assert stored
  order.
- `test_envelope_deserialize_rejects_a_non_envelope_payload` — the silent
  mis-parse case: a raw payload whose leading bytes form plausible small
  lengths must be rejected, not turned into a row.
- The existing T02/T03 suites must stay green.

## Implementation checklist
- [x] Round 2 (review CHANGES): F1 mis-parse regression test replaced with a
      genuinely discriminating buffer, falsified (fails without the
      version-byte check, passes with it); F2 crash-window hazard disclosed
      in source header + E05-T02.md §6; OQ-E05-B01-1 appended to
      `tracker.md`'s Event log `4b15381`
- [x] Envelope moved to a shared location, imported by both use cases `0195176`
- [x] `SendMessageUseCase` serializes the envelope before encrypting, with
      the sequence-number ordering constraint solved without reintroducing
      the read-then-write race (cite how, in the run log) `0195176`
- [x] Wire-bytes → `CiphertextMessage` re-typing decided and written down
      (documented as a remaining gap + OQ-E05-B01-1, not silently closed —
      see §Related gap and the Run log) `0195176`
- [x] `deserialize` hardened against exact-length / mis-parse (format-version
      byte + explicit non-assert bounds check) `0195176`
- [x] Regression tests written FIRST, confirmed failing on `af86907`
      (3 of 4 sub-tests red for the right reason; see Run log for the 4th's
      honest caveat) `0195176`
- [x] False comment at `receive_message_use_case.dart:40-42` corrected; T02
      §3/§5 updated to state the envelope obligation `0195176`
- [x] `flutter analyze` clean, `flutter test` green (full suite) `0195176`

## Definition of Done
- [x] Every §Regression test passes; the seam test fails on the pre-fix commit
- [x] Review APPROVE from a different model than `executed_by` (rule 5) —
      reviewed by claude-opus-5, implemented by claude-sonnet-5
- [x] Reviewer re-verified against the §Repro above, independently — reviewer
      ran the falsification itself (removed the version-byte check + its
      `offset += 1` from `MessageEnvelope.deserialize`:
      `test_envelope_deserialize_rejects_a_non_envelope_payload` FAILED with
      `Expected: throws FormatException / Actual: <Closure: () =>
      MessageEnvelope>` — i.e. the buffer parsed successfully into a garbage
      envelope, proving the test discriminates; restored, 4/4 pass), plus a
      manual byte-level trace of the buffer through the pre-fix parser at
      `6b638c9` (parses to id="foo", conv="bar", seq=7, payload=[1,2,3]).
      `flutter analyze` clean, `flutter test` 249/249.
- [x] 🧍 Human priority set (`bug_priorities` gate) — P2 (2026-08-29)

## Open Questions
- **OQ-E05-B01-1 — who reconstructs a `CiphertextMessage` from relayed wire
  bytes, and how?** `ReceiveMessageUseCase.call`'s `ciphertext` parameter is
  typed `CiphertextMessage` (T03's own binding contract, unchanged by this
  fix), but E04's `RelayEngine`/transport only ever carries raw `Uint8List`
  wire bytes — confirmed by reading `send_message_use_case.dart` (hands
  `RelayEngine.enqueue` a `Uint8List`, never a `CiphertextMessage`) and by
  grepping `lib/` for any call to `PreKeySignalMessage(...)` or
  `SignalMessage.fromSerialized(...)` (`libsignal_protocol_dart` 0.8.2's own
  reconstruction constructors) — there are none. Both constructors exist and
  are trivial to *call*, but the library exposes no single
  type-discriminating "reconstruct whichever one this is" entry point: a
  `PreKeySignalMessage` and a steady-state `SignalMessage` are not
  self-describing from their bytes alone in this library version (both just
  read the same version-bits byte; the library relies on an out-of-band
  message-type tag it doesn't itself define). Building an untested
  try-PreKey-then-SignalMessage heuristic here, with no real caller and no
  test that would exercise it, was judged worse than naming the gap
  honestly — per this bug file's own §Proposed fix direction, which offered
  "explicitly document ... rather than silently leaving the false impression
  it's solved" as an acceptable resolution. Whoever first wires a live
  transport receive path (most likely E06, per T02/T03's own §4 scope
  fences) needs to either carry an explicit type tag alongside the wire
  bytes (cheapest: E04's own relay-packet framing, if it has room) or
  implement and test the try-both fallback.
  - **Status:** 🟡 open
  - **Answer:** _<empty>_
  - **Answered by:** _<empty>_
  - **Date:** _<empty>_

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- 2026-08-29, `claude-opus-5 (reviewer)`: found and verified during the E05
  end-of-epic bug sweep. Probe output recorded in §Repro; probe file deleted
  after the run.
- 2026-08-29, `claude-sonnet-5 (builder)`: fixed on branch `epic_05_bug_01`
  (worktree `.claude/worktrees/epic_05_bug_01`, off `epic_05` @ `6b638c9` —
  all 5 E05 tasks merged: `77d5b40`, `ea7c9fb`, `984f83e`, `6b638c9`,
  `af86907`).

  **Regression tests written FIRST, confirmed failing on pre-fix code.**
  Before writing any fix, temporarily reverted `send_message_use_case.dart`
  and `receive_message_use_case.dart` to their pre-fix (`epic_05` HEAD)
  content via `git stash` (keeping the new test file), and ran
  `test/features/messaging/domain/message_envelope_seam_test.dart` against
  that pre-fix code:
  - `test_EARS_MSG_2_sent_message_is_receivable_by_receive_use_case`,
    `test_EARS_MSG_2_duplicate_of_real_message_is_dropped`,
    `test_EARS_MSG_3_out_of_order_delivery_of_real_messages_orders_by_sequence`
    all failed with `FormatException: MessageEnvelope: truncated, missing id
    bytes or conversationId-length header` — the exact failure mode in
    §Repro, now reached by driving both real use cases end to end instead of
    a standalone probe. Confirmed RED for the right reason.
  - `test_envelope_deserialize_rejects_a_non_envelope_payload` **passed even
    pre-fix** — disclosed honestly rather than hidden: the crafted
    non-envelope buffer happens to decode a huge `conversationIdLength` under
    the old (no version-byte) format, so pre-fix `deserialize` also throws
    `FormatException`, just for a different reason (truncation, not format-
    version rejection) than what this fix actually adds. This one sub-test
    doesn't discriminate pre/post-fix on its own; the other three do. Kept
    in the suite because it is still the correct assertion for the hardening
    this bug requires, and it exercises a real path (a payload that is NOT
    one of these envelopes must never parse into a usable row) regardless of
    which exception message fired.
  - Restored the fix (`git stash pop`) and re-ran: all 4 sub-tests pass.

  **Envelope moved.** `MessageEnvelope` now lives in
  `lib/features/messaging/domain/message_envelope.dart` (new file, added to
  this bug's `files:` per its own §Proposed fix direction's instruction to
  declare it), imported directly by `send_message_use_case.dart` and by
  `receive_message_use_case.dart`, which also `export`s it so the existing
  `test/features/messaging/domain/receive_message_use_case_test.dart` (T03's
  own suite, out of this bug's `files:` scope, left untouched) keeps
  compiling and passing unchanged against the new shared location.

  **Sequence-number-before-encryption ordering, solved without reintroducing
  the read-then-write race.** `send_message_use_case.dart`'s `call()` is now
  three phases:
  1. Inside `_db.transaction()`: read `MAX(sequence_number)` for
     `(conversationId, selfDeviceId)` and INSERT the row immediately — a
     `Queued` row with an empty placeholder `ciphertext` (`Uint8List(0)`,
     valid against the NOT NULL blob column) — in the exact same transaction
     shape T02's own review already verified (drift's `NativeDatabase` /
     `NoTransactionDelegate` holds the connection lock across `BEGIN`
     through `COMMIT`, so a second overlapping `transaction()` cannot issue
     its own `MAX()` read until the first has committed). This IS the
     reservation: the row/sequence-number pair is durably committed before
     any crypto call is attempted.
  2. Outside any transaction: build `MessageEnvelope(id, conversationId,
     sequenceNumber, payload: plaintext)` (the sequence number is now known)
     and call `_encrypt(recipientDeviceId, envelope.serialize())`.
  3. `UPDATE messages SET ciphertext = ? WHERE id = ?` with the real bytes,
     then hand off to `RelayEngine.enqueue` and transition to `Sent`/`Failed`
     exactly as before this fix.

  The transactional critical section is therefore UNCHANGED in shape from
  what T02's review already independently verified (source-level: drift's
  lock-holding transaction path; empirical: the 60-way concurrent stress
  probe) — it is still exactly "read MAX + insert one row", with no crypto
  call ever held inside it. Re-read that review's reasoning before making
  this change and confirmed nothing about the lock/transaction mechanics
  changed, only *when* the transaction runs relative to encryption (earlier,
  not later) — the concurrency guarantee does not depend on what happens
  after the transaction commits, so moving encryption from "inside-after" to
  "outside-after-a-different-point" doesn't touch it. Did not re-run the
  60-way stress probe (probe was reviewer-authored and deleted after its
  original run per that review's own note); the existing
  `test_sequence_numbers_distinct_under_concurrent_sends_same_conversation`
  in `send_message_use_case_test.dart` (T02's own suite, untouched, out of
  this bug's `files:` scope) still passes, which is at minimum a
  non-regression signal for the same transaction shape.

  **New behavioral consequence, disclosed:** because the sequence number is
  now reserved (and a row persisted) BEFORE the no-session check, a
  no-session `AppFailure` now leaves a `Failed` row behind where previously
  no row was persisted at all. `send_message_use_case_test.dart`'s
  `test_no_session_surfaces_app_failure_not_crash` only asserts no row is
  left at `Queued` (not that no row exists), so it still passes unmodified.
  Documented in both the source file's header comment and E05-T02.md §5.

  **CiphertextMessage re-typing gap: documented, not silently solved.**
  Read `crypto_stub.dart`'s real `CryptoService.decrypt` signature
  (`SignalProtocolAddress, CiphertextMessage -> Future<Uint8List>`) and
  confirmed via `libsignal_protocol_dart` 0.8.2's own source
  (`pre_key_signal_message.dart`, `signal_message.dart`) that
  `PreKeySignalMessage(bytes)` / `SignalMessage.fromSerialized(bytes)`
  constructors exist but neither is self-describing without an out-of-band
  type tag this codebase doesn't yet carry anywhere. Grepped `lib/` for any
  existing call to either constructor: none. Rather than invent an untested
  heuristic with no real caller to exercise it, wrote this up as
  OQ-E05-B01-1 (above) and as a header comment in
  `receive_message_use_case.dart`, naming the exact gap and the two ways to
  close it later — per the bug file's own §Proposed fix direction, which
  explicitly allowed documenting this as an alternative to solving it now.

  **Hardening.** Added a leading `envelopeFormatVersion` byte
  (`0xE5`) to the wire format and an explicit (non-`assert`, so it survives
  release builds) bounds check after parsing. A non-envelope payload with
  plausible-looking small length fields is now rejected on the version-byte
  mismatch before any length field is trusted.

  **Comment/contract corrections.** Rewrote the false claim at
  `receive_message_use_case.dart`'s top (pre-fix: stated T02 already
  serializes the envelope, which was false; post-fix: the same claim is now
  actually true, reworded to say so plainly and to date the fix).
  `epics/E05-messaging-reliability/tasks/E05-T02.md` §3/§5 updated to state
  the actual envelope obligation, with an inline note flagging the update as
  E05-B01's fix (not a silent rewrite of history).

  **Suite.** `flutter analyze`: 0 issues. `flutter test` (full suite):
  **249/249 pass** (245 pre-existing across all of E05's tasks + 4 new from
  this bug's regression test file). No existing T02/T03 test was modified;
  both suites stayed green throughout.

  Commit `0195176` — `fix(E05-B01): produce and consume the wire envelope on
  both sides of the send/receive seam` (code only: `send_message_use_case.dart`,
  `receive_message_use_case.dart`, `message_envelope.dart` (new),
  `message_envelope_seam_test.dart` (new)). This task-file update is a
  separate, subsequent docs commit per this project's convention of
  separating code and docs commits.

  `git status` after both commits: clean.

- 2026-08-29, `claude-sonnet-5 (builder)`: review round 2, two findings
  (CHANGES) addressed.

  **F1 (blocking) -- discriminating mis-parse regression test.** The
  original `test_envelope_deserialize_rejects_a_non_envelope_payload` buffer
  ([0x00, idLen=3 "foo", convLen=0x00000000, seq=1]) was replayed against the
  pre-fix layout by hand: byte 0 (`0x00`) forms the high byte of a legacy
  `idLen` field spanning bytes 0-3, which reads as `0`, and the NEXT 4 bytes
  (`0x03, 0x66, 0x6f, 0x6f` -- the declared idLen plus "fo" of "foo") get
  read as legacy `conversationIdLength`, a huge bogus value that throws on
  truncation both pre- and post-fix -- so the test never actually exercised
  the version-byte check, exactly as the reviewer found. Replaced the buffer
  with the reviewer's own suggested shape: the OLD pre-fix layout with no
  version byte at all (`idLen=3 "foo", convLen=3 "bar", seq=7, payload=[1,2,3]`).
  Rewrote the test's own comment to state the actually-true discriminating
  property instead of the old (now provably false) one.

  Falsification performed as required before considering this done:
  temporarily deleted the version-byte read+check block from
  `MessageEnvelope.deserialize` (`message_envelope.dart`), ran just this
  sub-test -- it FAILED (`Actual: <Instance of 'MessageEnvelope'>`, i.e. the
  buffer parsed successfully into the garbage envelope, exactly the silent
  mis-parse this bug is about). Restored the check verbatim (`git diff`
  confirmed byte-for-byte identical to before the falsification edit), reran
  -- passes. This is the falsification the bug file's own §Regression test
  section requires and the original test never actually satisfied.

  **F2 (must document) -- crash-window hazard between the two writes.**
  Named explicitly, per the finding: a process kill between phase 1's commit
  (sequence number reserved, `Queued` row inserted with placeholder
  `ciphertext: Uint8List(0)`) and phase 3's `UPDATE` (real ciphertext) leaves
  a `Queued` row with EMPTY ciphertext durably persisted, indistinguishable
  from a healthy `Queued` row by any column today -- a hazard this fix's
  reorder introduced (no row existed pre-fix until encryption succeeded).
  Not fixed (no live caller/retry-scanner exists yet to need it fixed; that's
  E06's likely concern), only disclosed: added to
  `send_message_use_case.dart`'s header comment next to the existing
  encrypt-failure disclosure, and to `E05-T02.md` §6 Risks as a new bullet
  naming it for whoever builds the retry/outbox scanner.

  **Tracker follow-up.** Appended a one-line-plus-context entry to
  `epics/E05-messaging-reliability/tracker.md`'s Event log (append-only,
  nothing else touched) recording that OQ-E05-B01-1 exists (previously only
  in `E05-B01.md` and a source header) and pointing at it, plus the
  reviewer's own suggested cheapest fix (carry `CiphertextMessage.getType()`
  alongside the wire bytes in E04's relay-packet framing, rather than a
  try-both-message-types heuristic).

  **Suite.** `flutter analyze`: 0 issues. `flutter test` (full suite):
  **249/249 pass** -- same count as round 1 (the F1 test buffer changed
  content, not test count).

  Commit `4b15381` -- `fix(E05-B01): discriminating mis-parse regression
  test + crash-window disclosure`.

  `git status` after commit: clean. `status` set back to
  `review-requested`.
