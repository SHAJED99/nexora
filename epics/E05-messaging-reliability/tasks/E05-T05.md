---
id: E05-T05
epic: E05
type: feature
title: Conflict resolution — security-restrictive precedence
layer: backend
size: S
status: done
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: S, range: "15k-30k" }
priority: { moscow: must, p: P1 }
depends_on: []
blocks: []
traces_to: [FR-MSG-007]
required_context: [epics/E05-messaging-reliability/epic.md, lib/features/trust/domain/relationship.dart]
design_contract: n/a
files:
  create:
    - lib/features/messaging/domain/conflict_resolver.dart
    - test/features/messaging/domain/conflict_resolver_test.dart
  update: []
  delete: []
started_at: 2026-08-27
completed_at: 2026-08-27
executed_by: claude-sonnet-5 (builder)
reviewed_at: 2026-08-29
reviewed_by: claude-opus-5 (reviewer)
review_outcome: approve
---
# E05-T05 · Conflict resolution — security-restrictive precedence

## 1. Goal
When two devices (this device and a peer, or two of the user's own
devices via sync) disagree about a piece of security-relevant state —
trust, location sharing, membership — resolve the conflict by always
favoring the more restrictive value, never the more permissive one.

## 2. Business logic
- FR-MSG-007 / EARS-MSG-4: exactly four named precedence pairs (epic.md
  §Scope), each independently a security control:
  - `BLOCK` beats `TRUST` (blocking always wins over any trust state)
  - `LOCATION-OFF` beats `LOCATION-ON` (sharing stops if either side
    disagrees)
  - `REVOKED` beats `ACTIVE` (a revoked credential/session stays revoked)
  - `REMOVED` beats `MEMBER` (group removal wins over membership)
  - The epic's own risk note is explicit: don't assume a single generic
    "restrictive wins" rule generalizes correctly to all four pairs
    untested — each pair is its own named test case.

## 3. What this task does
- `ConflictResolver` (or four small typed resolvers, implementer's
  judgment on shape — see §6): a pure function per precedence pair,
  taking two values of that pair's type and returning the more
  restrictive one. E.g.:
  - `resolveTrust(RelationshipState a, RelationshipState b) -> RelationshipState`
    — if either side is `blocked` (E02's existing `RelationshipState`
    enum), the result is `blocked`, regardless of the other side.
  - `resolveLocationSharing(bool a, bool b) -> bool` — `false` (off) wins
    if either is `false`.
  - `resolveMembershipRevocation`/`resolveGroupMembership` — analogous,
    using whatever revoked/removed representations already exist, or
    simple bool/enum types if no richer type exists yet in this codebase
    (session revocation and group membership aren't modeled anywhere yet
    as of E05 — use the simplest honest representation, don't invent a
    richer domain model speculatively ahead of the epics that actually
    own sessions/groups, E12/E07).
- Each resolver is a small, independently testable pure function — no I/O,
  no persistence, no wiring into where conflicts are actually detected
  (that's T04's sync mechanism's job, once the gap-fill protocol exists —
  see T04's Open Question).

## 4. What this task does NOT do
- Does NOT implement conflict *detection* — figuring out that two devices
  disagree about a value in the first place is T04's sync mechanism's job
  (and, per T04's own Open Question, the gap-fill protocol that would
  surface such disagreements isn't built yet either). This task only
  provides the pure resolution function once a conflict is known.
- Does NOT wire into E02's `RelationshipRepository`, E07's future group
  membership model, or E12's future session/revocation model — those
  integrations happen when those systems exist to be wired to. This task
  is deliberately upstream of all of them: a pure, reusable precedence
  function.
- Does NOT add UI or persistence for conflict resolution — no "conflict
  detected" screen, no audit log of resolved conflicts (E13 territory if
  ever wanted).
- Does NOT generalize beyond the four named pairs — no generic
  "more-restrictive-wins" framework applied to arbitrary future state
  types without its own explicit precedence definition.

## 5. Contract

### Functions
```yaml
- signature: "ConflictResolver.resolveTrust(a: RelationshipState, b: RelationshipState) -> RelationshipState"
  purpose: "blocked beats everything; among non-blocked states, use RelationshipState's own existing ordering if E02 already defines one, else document the chosen ordering explicitly here"

- signature: "ConflictResolver.resolveLocationSharing(a: bool, b: bool) -> bool"
  returns: "false if either input is false"
  purpose: "off wins"

- signature: "ConflictResolver.resolveRevocation(a: bool, b: bool) -> bool"
  params: { a: "true = revoked", b: "true = revoked" }
  returns: "true (revoked) if either input is true"
  purpose: "revoked wins"

- signature: "ConflictResolver.resolveMembership(a: bool, b: bool) -> bool"
  params: { a: "true = removed", b: "true = removed" }
  returns: "true (removed) if either input is true"
  purpose: "removed wins"
```

## 6. Risks
- E02's `RelationshipState` has more than two values (trusted/allowed/
  unknown/blocked) — "restrictive wins" needs a real ordering among all
  four, not just a `blocked`-vs-not check, if both inputs are non-blocked
  (e.g. `unknown` vs `allowed` — which wins?). Read E02's existing domain
  model (`lib/features/trust/domain/relationship.dart`) for whether an
  ordering already exists there to reuse, rather than inventing a second,
  possibly-inconsistent one here.
- Resist building a generic/reusable "conflict resolution framework" —
  the epic names exactly four pairs; four small functions are more honest
  and more testable than one generic mechanism speculatively covering
  cases nobody asked for yet.

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS id `c7bbf73`
- [x] `resolveTrust` implemented, including all pairwise combinations of
      E02's four `RelationshipState` values (not just blocked-vs-other) `c7bbf73`
- [x] `resolveLocationSharing` implemented `c7bbf73`
- [x] `resolveRevocation` implemented `c7bbf73`
- [x] `resolveMembership` implemented `c7bbf73`
- [x] `flutter analyze` clean, `flutter test` green `c7bbf73`

## 8. Acceptance criteria (EARS) + test plan
- **EARS-MSG-4**: WHEN resolving conflicts, the system SHALL favor the
  more security-restrictive state, for each of the four named precedence
  pairs. (FR-MSG-007)

### Tests
- `test_EARS_MSG_4_block_beats_trust` — all pairwise combinations
  involving `blocked` on either side
- `test_EARS_MSG_4_trust_state_full_ordering` — every pairwise combination
  of E02's four `RelationshipState` values, asserting the documented
  ordering holds both directions (`resolve(a,b) == resolve(b,a)` —
  symmetry is itself worth asserting explicitly)
- `test_EARS_MSG_4_location_off_beats_on`
- `test_EARS_MSG_4_revoked_beats_active`
- `test_EARS_MSG_4_removed_beats_member`

### Manual
None — pure functions, no observable app behavior changes in this task.

## 9. Self-review + Definition of Done
### Self-review
- [x] Every §7 item done, with commit hashes
- [x] `flutter analyze && flutter test` green (0 issues; 163/163 tests pass
      full suite, up from 124 — 39 new tests added)
- [x] Diff confined to `files:`; §4 respected — only the 2 files listed
      (create), nothing else touched
- [x] All four pairs tested exhaustively, not just the happy-path
      direction, per the epic's own risk note — `resolveTrust` covers all
      16 pairwise `RelationshipState` combinations plus explicit symmetry;
      the other three pairs each test both orderings, both-true and
      both-false

**Deviations from spec/design** (none, or list):
- None. E02's `relationship.dart` does not define a total ordering across
  all four `RelationshipState` values (only blocked-always-denies and
  trusted/allowed-both-permit), so per the task's own §5/§6 instruction
  this task defines the ordering explicitly within its own file
  (`blocked > unknown > allowed > trusted`, documented with rationale in
  `conflict_resolver.dart`'s doc comment), rather than inventing a second
  copy inside E02's file (out of scope) or reusing a nonexistent one.

**Files actually touched:**
- `lib/features/messaging/domain/conflict_resolver.dart` (new)
- `test/features/messaging/domain/conflict_resolver_test.dart` (new)

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id — all 5
      test groups in `conflict_resolver_test.dart` are named
      `test_EARS_MSG_4_*`
- [x] Review APPROVE from a **different model** than `executed_by` (rule 5) —
      reviewed by claude-opus-5, built by claude-sonnet-5; `flutter analyze`
      0 issues and `flutter test` 163/163 (39 T05 tests) re-run by the
      reviewer on f0a021d, not taken from the PR body
- [ ] Squash-merged to `epic_05` branch; tracker + metrics stamped
- [ ] 🧍 Human `verified` (epic-level batches are fine)

## Open Questions
_(none)_

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- 2026-08-27 — builder (claude-sonnet-5) — read E02's
  `lib/features/trust/domain/relationship.dart` and `E02-T01.md` per the
  task's §6 risk note; confirmed no total ordering across all four
  `RelationshipState` values exists there to reuse (only
  blocked-always-denies via `isConnectionPermitted`, and
  trusted/allowed-both-permit). Defined the ordering explicitly in this
  task's own file: `blocked > unknown > allowed > trusted` (most to least
  restrictive) — `trusted` ranks least restrictive because E02-T01's
  `EvaluateConnectionRequestUseCase` grants it auto-accept
  (skip-authentication), a capability `allowed` doesn't have, making
  `allowed` the more restrictive of the two permitting states; `unknown`
  ranks more restrictive than both because it does not permit a
  connection by default, but less restrictive than `blocked` because it's
  the no-information default rather than an explicit deliberate denial.
  Wrote `test/features/messaging/domain/conflict_resolver_test.dart`
  first (5 groups, all named `test_EARS_MSG_4_*`), confirmed it failed to
  compile (undefined `ConflictResolver`) for the right reason, then
  implemented `lib/features/messaging/domain/conflict_resolver.dart` as
  four small separately-named pure static methods (no generic framework,
  per §6). `flutter analyze`: 0 issues. `flutter test`: 163/163 green
  (full suite; 124 pre-existing + 39 new, none broken). Commit `c7bbf73`.
  status -> `review-requested`.
- 2026-08-29 — reviewer (claude-opus-5) — **APPROVE**. Suites re-run by the
  reviewer on `f0a021d`: `flutter analyze` 0 issues, `flutter test` 163/163,
  T05 file alone 39/39. Scope in-contract (diff = the 2 declared `create`
  files + this task file's own bookkeeping); §4 respected — no detection, no
  repository wiring, no generic framework, no UI/persistence.
  Independently judged the `blocked > unknown > allowed > trusted` ordering
  against `lib/features/trust/domain/relationship.dart` and confirmed it
  correct: `isConnectionPermitted` (relationship.dart:35-43) is monotone in
  this chain, so `resolveTrust` returning the chain-minimum is provably never
  more permissive than either input. The `allowed < trusted` step is grounded
  in a real spec id (FR-TRUST-004, `spec/srs.md:37` — trusted auto-accepts and
  skips authentication), not agent taste. The `blocked > unknown` tiebreak is
  the load-bearing call (both deny under `isConnectionPermitted`); ranking them
  equal and resolving to `unknown` would erase a deliberate block, so the
  chosen order is the safe one.
  Non-blocking observations (no fix required in this task):
  (1) `conflict_resolver.dart:61-62` ranks via `indexOf`, which yields `-1`
  for any value absent from `_trustRestrictiveness` — i.e. a *more* restrictive
  rank than `blocked`. If `RelationshipState` ever gains a fifth value,
  `resolveTrust(newValue, blocked)` would return `newValue`, silently defeating
  "blocked beats everything", and the mirror-logic test at
  `conflict_resolver_test.dart:47-58` would compute the same wrong expectation
  and still pass. Latent only — the enum is a documented closed set
  (relationship.dart:8-11) and all four values are listed at
  `conflict_resolver.dart:50-55`. Flagged for whoever extends the enum.
  (2) For the E05 bug sweep: `resolveTrust` has no causality input, so once
  T04's gap-fill protocol lands, a peer holding `unknown` will pull an
  established `trusted` down to `unknown` on every sync — a restrictive
  ratchet that could stop trust propagating between the user's own devices.
  Correct per this task's contract (detection/causality is explicitly T04's,
  §4), but a real cross-task seam to exercise at sweep time.
