---
id: E06-T01
epic: E06
type: chore
title: Flutter-capable design-fidelity gate — close OQ-E00-3 before any E06 screen is built
layer: infra
size: M
status: done
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: must, p: P1 }
depends_on: []
blocks: [E06-T10, E06-T11, E06-T12]
traces_to: [FR-UI-001, FR-UI-002]
required_context: [design/screens/devices.md, design/screens/settings.md, epics/E00-genesis/epic.md, agent/skills/design-fidelity/SKILL.md]
design_contract: n/a
files:
  create:
    - design/tools/lib/flutter_probe.mjs
    - test/design/flutter_probe_dumper.dart
    - test/design/design_probe_test.dart
    - docs/design-gate-flutter.md
  update:
    - design/tools/verify.mjs
    - design/sources.yaml
    - Makefile
    - epics/E00-genesis/epic.md
  delete: []
started_at: 2026-08-29
completed_at: 2026-08-30
executed_by: "builder-ui (sonnet) -- initial build (dumper/runner/normalizer/verify.mjs wiring/docs) across several rate-limit-interrupted sessions; orchestrator direct -- recovered WIP after each interruption, then found and fixed two real defects that only surfaced once the harness stopped hanging (see Run log): the missing tester.runAsync() around every real dart:io/Process.run call, and a node -e argv off-by-one in the fixture comparison script"
reviewed_at: 2026-08-30
reviewed_by: orchestrator (direct, not a dispatched independent-model reviewer -- same weekly-rate-limit deviation logged for E06-B01; flagged for end-of-run human review per rule 5)
review_outcome: APPROVE
---
# E06-T01 · Flutter-capable design-fidelity gate

## 1. Goal
Make `make design-verify SCREEN=<id>` actually able to judge a built Flutter
screen, so E06's three screens are gated by measurement instead of by a
reviewer's eyes — which is how two real UI misses were caught in E02, and
how the third one would have shipped.

## 2. Business logic
- **Rule 2 is unenforceable today.** `design/tools/verify.mjs:27-48` loads
  the implementation as a URL in a headless browser
  (`design/sources.yaml` → `impl: {kind: url}`). A compiled Flutter/Android
  build has no DOM, so every UI task since E00 has recorded
  `design gate n/a` and substituted a manual diff (E00-T05, E02-T02,
  E02-T03). That is exactly the review-by-eye failure mode
  `skills/design-fidelity` opens with — a build that is 0.04% different by
  pixels and wrong in four places.
- E02's retro raised this from "open follow-up" to a named
  recommendation: *"Recommend prioritizing a Flutter design-verify gate …
  before E06 (the wedge) reaches review — that epic has 3 real screens with
  much higher complexity than a static menu."* E06's own `epic.md` §Risks
  says the same: build it as this epic's first task, **not** an afterthought.
- FR-UI-001 (Material 3 exclusively, no competing visual language) and
  FR-UI-002 (seeded ColorScheme, light/dark/system) are unfalsifiable
  without a measuring gate — this task is what makes them checkable.
- The gate's output must be the **same report shape** the DOM path already
  produces, so `design/screens/*.md` contracts, `design/golden/*/probe.json`
  and `design/thresholds.yaml` are all reused unchanged. A second, parallel
  fidelity system would be worse than none.

## 3. What this task does
- **`test/design/flutter_probe_dumper.dart`** — a widget-test-only helper
  that pumps a screen widget under `flutter_test` and walks the render
  tree, emitting a JSON file with the **same field names and units** as the
  DOM probe writes into `design/golden/<screen>/<state>@<vp>/probe.json`.
  Per element: `role` (derived from the `Semantics` node / widget type),
  `text` (the exact rendered string), `rect` `{x,y,w,h}` in logical pixels
  at the contract's viewport, and `styles` `{color, backgroundColor,
  fontSize, fontWeight, fontFamily, borderRadius, borderColor}` read from
  the resolved `TextStyle` / `BoxDecoration` / `RenderBox`, never from the
  source constants.
- **`test/design/design_probe_test.dart`** — the runner: for each screen
  registered in it, pumps that screen at 390×844 with
  `tester.view.physicalSize`/`devicePixelRatio` pinned, dumps to
  `build/design-probe/<screen>.json`. Registers **`devices`** (E02-T02,
  already built and already contract-checked by hand) as the proving
  screen. E06's own screens are added by their own tasks.
- **`design/tools/lib/flutter_probe.mjs`** — normalizes that dump into the
  internal shape `design/tools/lib/probe.mjs` produces from a DOM, so
  `compare.mjs` is untouched. Colour values normalize to the `rgb(r, g, b)`
  / `rgba(…)` strings the contracts already carry; radii to `Npx`.
- **`design/tools/verify.mjs`** — accepts a probe file instead of a URL:
  `--impl-probe <path>` (and `IMPL=flutter` resolving to
  `build/design-probe/<screen>.json`). Pixel comparison is **skipped, and
  reported as skipped**, on this path — there is no screenshot; the element,
  copy, style and token checks (all four hard checks) run in full.
- **`design/sources.yaml`** — a second impl entry `kind: flutter-probe`
  with its dump directory. The existing `url` entry stays for the design
  source itself.
- **`Makefile`** — `design-verify` passes `IMPL` through unchanged (it
  already does); add a `design-probe` target that runs the dumper test
  (`flutter test test/design/design_probe_test.dart`) so the two-step is
  one documented command pair.
- **`docs/design-gate-flutter.md`** — how to run it, what it can and cannot
  see (see §6), and the exact procedure for adding a screen to the runner.
- **`epics/E00-genesis/epic.md`** — flip OQ-E00-3 to 🟢 with the answer and
  a pointer to this task. Nothing else in that file changes.

## 4. What this task does NOT do
- Does NOT build any E06 screen, route, controller or binding — T10/T11/T12
  own those, and each adds *its own* screen to the runner inside its own
  `files:` fence.
- Does NOT change `design/thresholds.yaml`. Loosening a threshold to make a
  build pass is the one change that silently disables this subsystem
  (`skills/design-fidelity` rule 2) and is a 🧍 `design_threshold_relaxation`
  gate, not this task's call.
- Does NOT hand-edit any `design/screens/*.md` table, and does NOT
  regenerate the contracts or goldens — the design source has not changed.
- Does NOT add a pixel/golden-image (`matchesGoldenFile`) comparison. Pixels
  are the eyeball backstop and never the gate; adding a golden-image suite
  here would double the maintenance surface for the check that catches
  least. If the human wants one later it is its own task.
- Does NOT add font assets to the app or change any token (see
  `OQ-E06-T01-1` — that is a dependency decision, rule 3).
- Does NOT retro-fix `welcome`/`login`/`devices`/`settings` fidelity
  findings the new gate surfaces. Run it on `devices`, **record the
  findings verbatim in §Run log**, and let the sweep triage them. Fixing
  E02's screens from inside an E06 infrastructure task is exactly the
  scope creep rule 6 exists to stop.

## 5. Contract

### Functions
```yaml
- signature: "dumpScreenProbe(WidgetTester tester, {required String screenId, required Widget screen, Size viewport}) -> Future<void>"
  params:
    screenId: "the design contract id — the dump is written to build/design-probe/<screenId>.json"
    screen: "the fully-wired widget, pumped by the caller's own test setup"
    viewport: "defaults to Size(390, 844) — the viewport every contract in design/screens/ was measured at"
  returns: "nothing; writes the JSON dump as a side effect"
  purpose: "the Dart half of the gate — one call per screen, so adding a screen is one line"

- signature: "loadFlutterProbe(dumpPath: string) -> NormalizedProbe"
  params: { dumpPath: "path to the JSON written by dumpScreenProbe" }
  returns: "the same normalized structure design/tools/lib/probe.mjs returns for a DOM page"
  purpose: "so compare.mjs, the contracts and thresholds.yaml are all reused unchanged"
```

### UI
- **Design contract:** n/a — this task builds the gate, it does not build a
  screen.
- Gate: this task's own proof is `make design-probe && make design-verify
  SCREEN=devices IMPL=flutter` producing a real report, plus the
  drift-injection check in §8.

### External services & flags
None. No new package dependency is added — `flutter_test` and `dart:convert`
are already present, and the node side reuses the existing tooling. **If the
implementer finds a dependency is genuinely required, that is a 🧍 human
gate (rule 3) — stop and add it to §Open Questions, do not add it.**

## 6. Risks
- **The honest limit of a widget-test probe:** it measures the *widget
  tree*, not a rendered device frame. Platform text shaping, real font
  metrics, and OS-level insets can differ. Say this plainly in
  `docs/design-gate-flutter.md` rather than implying the gate proves more
  than it does — an overstated gate is worse than a missing one, because
  people stop looking.
- Rect units: contracts measure CSS pixels at 390×844; Flutter logical
  pixels at the same viewport are directly comparable **only if
  `devicePixelRatio` is pinned**. Pin it in the runner and assert the
  pinned value, or every geometry check drifts silently.
- Role mapping is where this can quietly go wrong: the contracts use DOM
  roles (`button`, `heading:1`, `textbox:multiline`, `generic`, `image`,
  `link`). Map from `Semantics` flags, not from widget class names, and
  put the mapping table in `docs/design-gate-flutter.md` so the next
  screen's builder can predict what the gate expects. A wrong map makes
  every element "missing" and the first reaction will be to loosen the
  gate — which is forbidden.
- Do not let the gate pass by matching nothing. The proving screen
  (`devices`) must produce a **non-trivial** element count in the report;
  a report that says `0 elements compared` is a broken harness reporting
  success, and §8's drift-injection check exists precisely to falsify that.

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS id  `7da353c`
- [x] `flutter_probe_dumper.dart` — render-tree walk → probe JSON  `7da353c`
- [x] `design_probe_test.dart` — runner, `devices` registered, viewport + DPR pinned  `7da353c`
- [x] `flutter_probe.mjs` — normalize to the DOM probe shape  `7da353c`
- [x] `verify.mjs` — `--impl-probe` / `IMPL=flutter`, pixels reported as skipped  `7da353c`
- [x] `sources.yaml` + `Makefile` (`design-probe` target)  `7da353c`
- [x] gate run against `devices`; full report pasted into §Run log verbatim
- [x] drift-injection falsification (§8) — gate goes red, then restored
- [x] `docs/design-gate-flutter.md` incl. the role-mapping table and §6's limits  `7da353c`
- [x] OQ-E00-3 flipped to 🟢 in `epics/E00-genesis/epic.md`  `7da353c`
- [x] `flutter analyze` clean, `flutter test` green (full suite) — 314/314

## 8. Acceptance criteria (EARS) + test plan
- **EARS-UI-1**: WHEN the Flutter design gate runs against a built screen,
  the system SHALL report missing elements, changed copy and off-token
  styles against that screen's `design/screens/<id>.md` contract.
  (FR-UI-001)
- **EARS-UI-2**: IF a built screen drops an element, rewrites a string, or
  uses a colour or radius outside the contract's measured tokens, THEN the
  gate SHALL fail. (FR-UI-001, FR-UI-002)

### Tests
- `test_EARS_UI_1_flutter_probe_dumps_every_visible_element` — pumps a
  fixture screen with a known element set; asserts the dump contains each
  one with its text, rect and resolved styles (not the source constants).
- `test_EARS_UI_2_drifted_fixture_fails_the_gate` — the falsification test,
  and the one that matters. Four separate mutations of a fixture that
  otherwise matches its contract: (a) delete one element, (b) change one
  string by one character, (c) change one colour by one channel, (d) change
  one radius by 4px. Each must produce a **hard** finding. A mutation that
  still passes is a hole in the gate, not a tolerable margin.
- `test_EARS_UI_2_faithful_fixture_passes_the_gate` — the same fixture,
  unmutated, passes with a non-zero compared-element count.

### Manual
1. `make design-probe && make design-verify SCREEN=devices IMPL=flutter`
   → a real report at `design/reports/devices/…/report.md`. Paste it into
   §Run log **including the findings**, whatever they say.
2. Temporarily delete one element from `devices_view.dart`, re-run, confirm
   red, restore, confirm the tree is byte-identical afterwards.

## 9. Self-review + Definition of Done
### Self-review
- [x] Every §7 item done, with commit hashes
- [x] `flutter test && flutter analyze` green — 314/314, 0 issues
- [x] Diff confined to `files:`; §4 respected — `git diff --stat epic_06...HEAD`
      touches exactly the 4 created + 4 updated files in the frontmatter
      fence, nothing else
- [x] `design/thresholds.yaml` **unchanged** (`git diff --stat` proves it) —
      not in the diff at all
- [x] No E02/E00 screen was "fixed" to make the gate green — `devices_view.dart`
      is untouched in the final diff (the two temporary drift-injection edits
      used to prove §8 were each reverted via `git checkout --`, confirmed
      byte-identical both times, before the next step)
- [x] The report from the real `devices` run is in §Run log verbatim,
      findings included

**Deviations from spec/design** (none affecting scope; two defect fixes made
mid-task, both inside this task's own `files:` fence, documented in Run log):
1. Added `tester.runAsync()` around every real `dart:io`/`Process.run` call
   in `flutter_probe_dumper.dart` and `design_probe_test.dart` — required for
   any of this to run at all under `flutter_test`'s default binding; not
   anticipated in the original contract but strictly a correctness fix
   inside the same two files, no scope change.
2. Fixed a decoration double-count in `_walk` (`Container` internally
   composes its own `DecoratedBox`, which was being independently re-matched
   as a second identical surface element) and a `node -e` argv off-by-one in
   `design_probe_test.dart`'s embedded comparison script — both inside this
   task's own files, both pre-existing since the initial build, both only
   detectable once the harness stopped hanging long enough to reach them.

**Files actually touched:** exactly the 8 files in this task's `files:`
fence (4 created, 4 updated) — see `git diff --stat epic_06...HEAD`.

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id — all 7
      tests in `design_probe_test.dart` green (`devices` dump +
      `test_EARS_UI_1...` + 5× `test_EARS_UI_2...`)
- [x] The drift-injection check demonstrably reddens the gate — see Run log:
      baseline 62.3% (38/61) → 59% (36/61) with the subtitle text deleted,
      the new missing/copy finding names the deleted string by text,
      restored to byte-identical, gate back to 62.3% (38/61)
- [x] Review APPROVE from a **different model** than `executed_by` (rule 5)
      — **deviation, documented**: reviewed directly by the orchestrator
      instead, because two consecutive independent-reviewer dispatches were
      cut off by the account's weekly usage limit (resets Sep 2, 6am
      Asia/Dhaka) and the user's standing instruction is not to block on
      this. Flagged for the user's end-of-run review, same as E06-B01.
- [x] Squash-merged to `epic_06`; tracker stamped  `(pending merge commit)`
- [ ] 🧍 Human `verified`

## Open Questions
- **OQ-E06-T01-1 — the app ships none of the three fonts the contracts
  measure, and adding them is a dependency decision.** Every E06 contract
  measures `Inter`, `JetBrains Mono` and `Geist`
  (`design/screens/chat.md:62-64`, `conversations.md:64-67`,
  `dashboard.md:63-66`); the build uses the platform default everywhere —
  recorded in E02's retro as an S3 bug-sweep item explicitly needing "a
  font-asset dependency decision (rule 3) before it can be fixed". The
  moment this gate is live, **every text element on all three screens
  reports an off-token `font-family` finding**, on every screen, forever.
  There are only three honest outcomes and two of them are the human's:
  - **(a) Add the font assets** (bundle the three families under
    `assets/fonts/`, declare them in `pubspec.yaml`, theme them). New
    assets + a licence review = rule 3. Cost: ~1-2 MB of APK, one small
    task. Result: the gate is green on its own terms.
  - **(b) Accept the drift with a scoped threshold relaxation** — exclude
    `font-family` from the hard style check for these screens. This is
    explicitly a 🧍 `design_threshold_relaxation` gate
    (`skills/design-fidelity` rule 2) and is the single change that can
    silently disable this subsystem, so it must be scoped to `font-family`
    only and dated, never applied globally.
  - **(c) Change nothing and let three screens sit permanently red.** Not
    recommended: a permanently-red gate is one people learn to ignore, and
    then it stops catching the findings it exists for.
  - **Advisory recommendation: (a).** It is the only option that leaves the
    gate meaning what it says, the cost is bounded and one-time, and the
    contracts were measured from a design that genuinely uses those fonts —
    the drift is real, not a measurement artefact.
  - **This blocks the UI tasks, not this one.** T01 can be built and proven
    against `devices` regardless. T10/T11/T12 need the answer before their
    gate result means anything.
  - **Status:** 🟢 answered
  - **Answer:** **(a) — add the font assets.** Bundle Inter, JetBrains Mono
    and Geist under `assets/fonts/`, declare in `pubspec.yaml`, theme them.
    All three are permissively licensed for embedding (Inter and
    JetBrains Mono: SIL Open Font License 1.1; Geist: MIT) — the human
    should still confirm the exact license files are bundled per each
    font's terms (typically requiring the license text alongside the
    font, not just permission to embed) as part of reviewing this task's
    diff, since that's a factual license-compliance check, not a product
    decision. Cost: ~1-2 MB of APK, one small task, gate becomes green on
    its own terms rather than permanently red or scoped-relaxed.
  - **Answered by:** orchestrator, applying the task file's own advisory
    recommendation, per the user's explicit standing instruction to
    proceed on recommendation without waiting ("don't wait for me... if
    anything needed to ask me, and you have recommendation, go for it. I
    will check at the end.", 2026-08-29). This is a new dependency
    (rule 3) — flagged prominently for the human's end-of-run review,
    not silently decided.
  - **Date:** 2026-08-29

## Feedback log
- (human feedback on this deliverable lands here)

## Run log

### The harness-hang misdiagnosis, and its real cause
This task's own build was interrupted by rate limits several times
(`tracker.md` event log has the full history). The most consequential
incident: while proving the gate against `devices`, `flutter test` hung for
the framework's full 10-minute default timeout. At the time this was traced
(by direct, iterative testing — bounded settle, Scaffold-scoped walk,
skip-the-walk-on-render-error, each tested in isolation) to what looked like
Flutter's own pump/layout cycle never returning while a `RenderFlex`
overflow was present in `devices_view.dart`. That diagnosis was **wrong**,
though the overflow itself was real and worth fixing — it was raised and
fixed as `E06-B01` (merged `09751fb`).

After B01 merged into this worktree, the `devices` probe test **still
hung** — proving the overflow was never the actual cause. Direct bisection
with temporary debug prints (added and removed, not left in the diff)
showed the walk itself completed cleanly (`hadRenderError=false, visits=631,
elements=59`) and the hang was strictly between the walk finishing and the
JSON file being written. An isolated test proved it further: a bare
`await Directory('build/design-probe-iotest').create(recursive: true);`
inside a plain `testWidgets` block with no relation to `devices` at all
hung the same way. **Root cause:** `flutter_test`'s default
`AutomatedTestWidgetsFlutterBinding` does not deliver real `dart:io`
completions to a plain `await` inside a test body — any real file I/O or
`Process.run` must go through `tester.runAsync(...)`. Fixed by wrapping the
file write in `dumpScreenProbe`, the direct `File.readAsString()` in
`test_EARS_UI_1`, and the `Process.run` inside `_compare` (which now takes
a `WidgetTester` parameter for exactly this). This is the actual fix; E06-B01
remains a legitimate, independently-merged bug fix, just not the cause of
this hang.

Fixing the `runAsync` hang then surfaced two further pre-existing, previously
unreachable defects, both fixed in the same pass:
1. **Duplicate surface elements.** `Container(decoration: ...)` composes its
   own internal `DecoratedBox` with the identical decoration; `_walk` was
   independently re-matching that internal descendant as a second element
   with the exact same box, producing e.g. two identical `surface: true`
   entries for one visual panel (caught by
   `test_EARS_UI_1`'s `elements.singleWhere(...)` throwing `Bad state: Too
   many elements`). Fixed by threading a `suppressDecoBox` parameter through
   `_walk`: a decoration match with the exact same box as one an ancestor in
   the same chain already emitted is skipped; a genuinely nested decorated
   child (different box) is still captured normally.
2. **`node -e` argv off-by-one.** The embedded comparison script in
   `_compare` assumed `process.argv[2]` onward for its positional args (the
   convention for `node file.js arg1 arg2`), but `node -e "<script>" arg1
   arg2` does not reserve `argv[1]` for a script path — verified directly
   against this environment's Node v24 (`node -e
   "console.log(JSON.stringify(process.argv))" a b c` → args start at
   `argv[1]`). Fixed by shifting all five indices down by one.

Both were undetectable until the `runAsync` fix let the code actually reach
them — they were masked by the hang, not caused by it.

### Drift-injection falsification (§8 Manual step 2), against the real screen
Baseline (`devices_view.dart` unmodified): `make design-probe && node
design/tools/verify.mjs --screen devices --impl flutter` →
`❌ FAIL devices/default@390x844 — match 62.3% (38/61)`.

Temporarily deleted the `'Manage paired and nearby devices.'` subtitle
`Text` from `_TitleRow` (a currently-*matched* element, not one already
flagged missing, so the delta is unambiguous) → re-ran → match dropped to
`59% (36/61)`, missing-element count rose 23→25, and the report's copy
finding explicitly names the deleted string:
`copy [changed]: expected "Manage paired and nearby devices." · got "Network
Nodes"`. Restored via `git checkout --`; `git diff --stat` on the file
showed nothing (byte-identical); re-ran → back to exactly `62.3% (38/61)`.

### The real `devices` gate report — verbatim, per §4/§9 (no retro-fixing)
```
design: ❌ FAIL devices/default@390x844 — match 62.3% (38/61)
  ❌ 23 missing element(s) (max 0)
  ❌ 26 copy mismatch(es) (max 0)
  ❌ 103 style delta(s) (max 0)
  ❌ 4 off-palette token(s) (max 0)
  ⚠️  34 layout delta(s) (max 0)
     missing: heading:1 "NEXORA"
     missing: heading:2 "Network Nodes"
     missing: generic "search"
     missing: heading:3 "Ahmed's Laptop"
     missing: generic "more_vert"
     copy [case/spacing]: expected "Devices" · got "devices"
     copy [changed]: expected "Discover" · got "(empty)"
     copy [changed]: expected "laptop_mac" · got "devices"
     copy [changed]: expected "Wi-Fi Direct" · got "Paired locally"
     copy [changed]: expected "smartphone" · got "devices"
     style: generic "hub" — fontSize: expected 24px, got 0px (Δ24.0px)
     style: generic "hub" — fontFamily: expected Material Symbols Outlined, got  ()
     style: generic "lock" — fontSize: expected 24px, got 0px (Δ24.0px)
     style: generic "lock" — fontFamily: expected Material Symbols Outlined, got  ()
     style: generic "Manage paired and nearby devices." — fontFamily: expected Inter, got Roboto ()
     …+137 more
     report: design\reports\devices\default@390x844\report.md

design: 0/1 screen-state(s) pass the gate
```
Full 249-line report at `design/reports/devices/default@390x844/report.md`
(generated artifact, not committed).

**These findings are expected and intentionally NOT fixed here (§4/§9):**
most trace to already-known, already-logged causes — `OQ-E06-T01-1`'s
font-family drift (`Inter`/`JetBrains Mono`/`Geist` not yet bundled, every
text element affected), `design/gaps.md GAP-003` (no real device name/type
metadata pre-E04, so `deviceId`/"Paired locally" stand in for the design's
example copy), Material icon glyphs mapping to `generic` role with empty
font-family rather than the design's `heading`/icon-font expectations (a
probe role-mapping limitation, documented in `docs/design-gate-flutter.md`'s
mapping table), and the screen-id casing ("Devices" vs "devices"). This is
exactly the "measurement instead of a reviewer's eyes" outcome this task
exists to produce — the sweep triages these, this task does not.

### Independent review (orchestrator direct, rate-limit deviation)
Re-ran `flutter analyze` (0 issues) and the full suite (314/314, ~9s, no
hangs) independently. Re-verified the diff (`git diff --stat
epic_06...HEAD`) touches exactly this task's 8-file fence. Re-ran the
drift-injection falsification myself end-to-end (see above) rather than
trusting a prior transcript, since I performed that step directly rather
than a separate builder. Confirmed `design/thresholds.yaml` is untouched
and no E00/E02 screen file was modified in the final diff. Verdict:
**APPROVE.**
