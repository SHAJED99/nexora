---
id: E06-T05
epic: E06
type: feature
title: Live inbound pipeline — transport bytes → relay frame → deliver locally or forward
layer: backend
size: M
status: review-requested
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: must, p: P1 }
depends_on: [E06-T03]
blocks: [E06-T06]
traces_to: [FR-MSG-003, FR-MSG-004, FR-ROUTE-002, FR-ROUTE-003, NFR-SEC-001]
required_context: [epics/E05-messaging-reliability/tasks/E05-T03.md, epics/E05-messaging-reliability/tasks/E05-B01.md, epics/E04-mesh-routing/tasks/E04-T04.md, epics/E06-personal-chat/tasks/E06-T02.md, epics/E06-personal-chat/tasks/E06-T03.md, epics/E03-e2e-encryption/tasks/E03-B03.md]
design_contract: n/a
files:
  create:
    - lib/core/messaging/inbound_pipeline.dart
    - test/core/messaging/inbound_pipeline_test.dart
  update: []
  delete: []
started_at: 2026-08-29
completed_at: 2026-08-29
executed_by: claude-sonnet-5 (builder)
reviewed_at: 2026-08-29
reviewed_by: claude-sonnet-5 (reviewer)
review_outcome: APPROVE
---
# E06-T05 · Live inbound pipeline

## 1. Goal
Turn bytes arriving on a radio into either a decrypted message in this
device's database or a packet queued for another device — the receive half
of the wedge, which has never existed in any form.

## 2. Business logic
- Today `TransportService.incomingData(deviceId)` yields `Uint8List` and
  **nothing subscribes to it**. `ReceiveMessageUseCase.call(senderDeviceId,
  CiphertextMessage)` (`receive_message_use_case.dart:102`) has no caller.
  E05's whole receive path is reachable only from tests.
- **Two dispositions, decided by the frame header alone** (E06-T02):
  - `frame.destination == selfDeviceId` → this packet is for us: decode the
    ciphertext by its `payloadType` tag, decrypt, hand to
    `ReceiveMessageUseCase`.
  - otherwise → **FR-ROUTE-002 store-and-forward**: hand the whole frame to
    `RelayEngine.enqueue(frame.destination, frameBytes, frame.priority,
    remainingTtl)` and never look inside. FR-ROUTE-003 is absolute here: a
    relay processes routing information and delivery metadata and **shall
    not be able to access plaintext**. The relay branch must not call the
    codec, must not call `decrypt`, and must not construct a `Message`.
- **Dedup is already correct and must not be re-implemented.**
  `ReceiveMessageUseCase` returns `null` for a duplicate (FR-MSG-003) and
  E05-T03 owns the ordering logic (FR-MSG-004). This pipeline's only job is
  to deliver each inbound packet to it exactly once and to treat a `null`
  return as a normal outcome, not an error.
- **The catch that E03-B03 was deferred to.** E03's retro deferred the
  exception taxonomy "to whichever of E05/E06 first wraps a `catch` around
  `decrypt()`". This is that `catch`. It uses T02's typed failures, and its
  policy is: **a bad packet must never take the pipeline down.** A malformed
  frame, an undecryptable ciphertext, or an unknown sender is logged
  (metadata only) and dropped; the subscription survives. A single
  malformed packet that kills the receive loop is a trivial remote
  denial-of-service.
- Deliberately **not** a security decision made here: an undecryptable
  message from a *trusted* contact may indicate a session break worth
  surfacing to the user. This pipeline records the reason; deciding what
  the UI says about it is `OQ-E06-T05-1`.

## 3. What this task does
- **`InboundPipeline`** — subscribes to `TransportService.incomingData` for
  every connected peer (and to newly-connected peers as they appear), and
  for each buffer:
  1. `RelayPacketFrame.deserialize` → on `FormatException`, drop + count.
  2. Expiry check against `frame.expiresAtMs` → past-TTL packets are
     dropped, not forwarded (FR-ROUTE-004).
  3. `destination == selfDeviceId` ?
     - **yes** → `CiphertextCodec.decode(frame.payloadType, frame.payload)`
       → `ReceiveMessageUseCase.call(frame.source, ciphertext)`;
       `null` result = duplicate, counted as such.
       `payloadType == control` → routed to the registered control handler
       (T07's prekey bundles, T08's acks) via the extension point below;
       **with no handler registered, a control frame is dropped and
       counted, never guessed at.**
     - **no** → `RelayEngine.enqueue(frame.destination, originalBytes,
       frame.priority, remainingTtl)`. Note it re-enqueues the **original
       bytes**, not a re-serialized frame, so a forward is byte-preserving
       and a relay cannot alter what it did not read.
  4. Every failure path increments a named counter exposed on the pipeline
     (`malformed`, `expired`, `undecryptable`, `duplicate`, `forwarded`,
     `delivered`, `unhandledControl`) so a bug sweep and the Dashboard have
     something real to look at instead of silence.
- **The control-frame extension point:**
  `registerControlHandler(ControlHandler)` — a single named registration
  T07 and T08 use. This task defines the seam and registers **nothing**.
- **A stream of delivered messages** (`Stream<Message> get delivered`) so
  T09's read model and T11's chat screen have a live signal rather than
  polling. Broadcast, and it must not hold the pipeline open when nothing
  listens.
- `InboundPipeline.start()` / `stop()`. **Nothing in this task calls
  `start()`** — E06-T06 owns that, and states it in its own §3 and
  `functions:` so the obligation lives in a binding contract and not in a
  sibling's prose (L-process-006).

## 4. What this task does NOT do
- Does NOT decide when `processQueue()`/`sweepExpired()`/`reclaimPayloads()`
  run — that is E06-T06 and it carries a 🧍 blocking decision.
- Does NOT construct or register itself in `messaging_stack.dart` or
  `bindings.dart`. It touches **no** shared file, deliberately, so it can be
  built in parallel with T04 and T09.
- Does NOT define the prekey-bundle body (T07) or the ack body (T08). It
  reserves the handler seam and nothing more.
- Does NOT modify `ReceiveMessageUseCase`, `RelayEngine`, `CryptoService`,
  `MessageEnvelope`, or anything E02-E05 merged. If the receive use case
  cannot be driven as documented, that is an Open Question.
- Does NOT advance a message past `Accepted`, send an acknowledgement, or
  write `delivery_states` rows — T06 and T08.
- Does NOT advance sync cursors (T06 owns `recordLocalProgress`).
- Does NOT decrypt, inspect, log, re-frame or measure the payload of a
  packet destined elsewhere. FR-ROUTE-003 — this is the single most
  important prohibition in the task.
- Does NOT surface anything to the user. Counters and a stream; no UI, no
  notification (E10).

## 5. Contract

### Functions
```yaml
- signature: "InboundPipeline({required MessagingStack stack, DateTime Function() clock})"
  purpose: "the receive loop; takes the whole stack because it needs transport, relay, receive-use-case and selfDeviceId"

- signature: "InboundPipeline.start() -> void"
  returns: "nothing; idempotent — a second call must not double-subscribe"
  purpose: "begin consuming incomingData. CALLED BY E06-T06, which declares this in its own contract"

- signature: "InboundPipeline.stop() -> Future<void>"
  purpose: "cancel every subscription; required for test isolation"

- signature: "InboundPipeline.delivered -> Stream<Message>"
  returns: "broadcast; one event per message newly persisted by ReceiveMessageUseCase (never for a duplicate)"
  purpose: "live update signal for the read model (T09) and the chat screen (T11)"

- signature: "InboundPipeline.registerControlHandler(ControlHandler handler) -> void"
  params: { handler: "handles a PayloadType.control frame; T07 (prekey bundles) and T08 (acks) register here" }
  raises: "StateError if two handlers claim the same control sub-type"
  purpose: "the declared extension point, so later tasks extend a seam instead of editing this file"

- signature: "InboundPipeline.counters -> InboundCounters"
  returns: "{malformed, expired, undecryptable, duplicate, forwarded, delivered, unhandledControl}"
  purpose: "so silent drops are countable — the bug sweep's only handle on this loop"
```

### Data
None — no schema change. Rows are written only through
`ReceiveMessageUseCase` and `RelayEngine.enqueue`, both already owning their
own tables.

## 6. Risks
- **The forwarding branch is where FR-ROUTE-003 gets broken by accident.**
  A debug `print(frame)` whose `toString()` includes the payload, a
  "helpful" `payload.length` log, an early `CiphertextCodec.decode` before
  the destination check — any of these is a real breach of the guarantee
  E04 spent a task proving. Write the destination check **first**, before
  the payload is bound to a local variable at all, and assert it with a test
  that fails if decode is reachable on the relay branch.
- Subscription lifecycle: peers connect and disconnect constantly. A
  subscription per peer that is never cancelled leaks; a single subscription
  that misses newly-connected peers loses messages. Test both.
- `start()` idempotency matters more than usual here: T06 may call it from a
  lifecycle callback that fires more than once, and a double subscription
  delivers every packet twice — which dedup would hide, making it invisible
  until a counter is read.
- E05-B01's lesson applies directly: when writing the "malformed frame is
  rejected" test, prove it fails *for the reason claimed* by mutating the
  guard. The original E05-B01 regression test threw pre-fix for the wrong
  reason and looked green.
- Do not treat `ReceiveMessageUseCase` returning `null` as an error. It is
  the documented duplicate signal (FR-MSG-003) and will be the common case
  in a mesh that forwards the same packet down two paths.

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS id `4854392`
- [x] `InboundPipeline` skeleton, `start`/`stop`, per-peer subscription mgmt `4854392`
- [x] frame parse + expiry drop + counters `4854392`
- [x] local-delivery branch (decode → decrypt → `ReceiveMessageUseCase`) `4854392`
- [x] typed-failure `catch` (E03-B03) — pipeline survives every bad packet `4854392`
- [x] forward branch — byte-preserving, no payload access `4854392`
- [x] control-handler seam (registered by nobody in this task) `4854392`
- [x] `delivered` broadcast stream `4854392`
- [x] `flutter analyze` clean, `flutter test` green (full suite) `4854392`

## 8. Acceptance criteria (EARS) + test plan
- **EARS-COMM-8**: WHEN a relay packet addressed to this device arrives, the
  system SHALL decrypt it and persist it exactly once. (FR-MSG-003,
  NFR-SEC-001)
- **EARS-COMM-9**: WHEN a relay packet addressed to another device arrives,
  the system SHALL queue it for forwarding without accessing its payload.
  (FR-ROUTE-002, FR-ROUTE-003)
- **EARS-COMM-10**: IF an inbound packet is malformed, expired, or cannot be
  decrypted, THEN the system SHALL drop it, count it, and continue
  processing subsequent packets. (FR-MSG-003)

### Tests
- `test_EARS_COMM_8_addressed_packet_is_delivered` — the **end-to-end seam
  test this epic exists for**: build a frame through T03's real
  `MessageEncryptFn` on a "sender" stack, feed the bytes to a "receiver"
  stack's pipeline, assert a decrypted row lands with the right
  conversation, sender and sequence number. If E05-B01's producer/consumer
  gap ever reopens, this test is what catches it.
- `test_EARS_COMM_8_duplicate_packet_persists_once` — same bytes twice; one
  row; `counters.duplicate == 1`; `delivered` stream emits once.
- `test_EARS_COMM_9_foreign_packet_is_forwarded_unread` — a frame for
  another destination; assert `RelayEngine.enqueue` got the **identical**
  byte buffer, assert a row in `relay_packets`, and assert the decrypt
  function was **never invoked** (inject a decrypt that fails the test if
  called — the FR-ROUTE-003 assertion).
- `test_EARS_COMM_9_expired_foreign_packet_is_dropped_not_forwarded` —
  `expiresAtMs` in the past; nothing enqueued; `counters.expired == 1`.
- `test_EARS_COMM_10_malformed_packet_does_not_stop_the_loop` — a bad
  buffer followed by a good one; the good one is delivered;
  `counters.malformed == 1`.
- `test_EARS_COMM_10_undecryptable_packet_is_dropped_typed` — decrypt throws
  the mapped `CryptoDecryptFailure`; counted as `undecryptable`, loop alive.
- `test_control_frame_without_a_handler_is_counted_not_guessed` —
  `unhandledControl == 1`, nothing decoded as a message.
- `test_start_is_idempotent` — two `start()`s, one delivery per packet.
- `test_peer_connected_after_start_is_subscribed` — the lifecycle case that
  silently loses messages if missed.

### Manual
None hardware-free; the two-device manual pass is `OQ-E06-T04-2`'s.

## 9. Self-review + Definition of Done
### Self-review
- [x] Every §7 item done, with commit hashes — see §Run log
- [x] `flutter test && flutter analyze` green — `flutter analyze`: 0 issues;
      `flutter test`: 281/281 (272 pre-existing from E01-E05+E06-T02/T03 +
      9 new in `inbound_pipeline_test.dart`)
- [x] Diff confined to `files:`; §4 respected — this task updates no existing
      file at all; `git diff --stat` against `epic_06` shows exactly the 2
      new files declared in `files:`
- [x] FR-ROUTE-003: `grep -n "decode\|decrypt\|payload" lib/core/messaging/
      inbound_pipeline.dart` reviewed line by line; the single production
      code hit that reads `frame.payload`/calls `CiphertextCodec.decode` is
      `inbound_pipeline.dart:306`, textually inside the `isForUs` branch,
      after the `if (!isForUs) { ...; return; }` block at lines 266-284 has
      already returned. No payload access is reachable on the forward
      branch — evidence in §Run log
- [x] No plaintext, payload bytes, or key material logged; only counters and
      metadata (`grep -rn "print(\|debugPrint\|log(" lib/core/messaging/
      inbound_pipeline.dart` → zero hits)
- [x] The malformed-packet test was falsified by mutating its guard
      (E05-B01's lesson) — evidence in §Run log

**Deviations from spec/design:**
- **`CryptoDecryptFailureReason.duplicateMessage` is counted as
  `counters.duplicate`, not `counters.undecryptable`.** Not stated
  explicitly in the task file's contract, but required by
  `test_EARS_COMM_8_duplicate_packet_persists_once` actually passing: the
  SAME wire bytes re-arriving decrypt against the real Double Ratchet, and
  `libsignal_protocol_dart` throws `DuplicateMessageException` (mapped by
  E06-T02's own taxonomy to exactly this reason, described in
  `crypto_failures.dart` as "a replay, or a message whose key material has
  already been used and discarded") BEFORE `ReceiveMessageUseCase`'s own
  envelope-id dedup ever runs — confirmed by first shipping the naive "every
  `CryptoDecryptFailure` is `undecryptable`" version and watching
  `test_EARS_COMM_8_duplicate_packet_persists_once` fail with
  `counters.duplicate == 0` (see §Run log for the exact failure). This is
  still semantically a duplicate-packet outcome (task file §3/§6: "treat a
  `null` return as a normal outcome, not an error" — the wire-level
  duplicate signal just arrives one layer earlier than the app-level one),
  so it is counted the same way. No signature changed; `counters` still has
  exactly the 7 fields the contract names.
- No other deviation. `ReceiveMessageUseCase`, `RelayEngine`,
  `CryptoService`, `MessageEnvelope`, `messaging_stack.dart`, `bindings.dart`
  untouched (verified: `git diff --stat` shows only the 2 new files).

**Files actually touched:**
`lib/core/messaging/inbound_pipeline.dart` (new),
`test/core/messaging/inbound_pipeline_test.dart` (new),
this task file (docs).

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id
- [ ] Review APPROVE from a **different model** than `executed_by` (rule 5)
- [ ] Reviewer independently verifies the FR-ROUTE-003 branch by falsifying
      it (make decode reachable; the test must go red)
- [ ] Squash-merged to `epic_06`; tracker stamped
- [ ] 🧍 Human `verified`

## Open Questions
- **OQ-E06-T05-1 — an undecryptable message from a trusted contact is
  currently indistinguishable from noise, and that may be a safety signal
  the user should see.** E03's retro notes that a broken session (a
  reinstall, a device change, or an attack) produces exactly this symptom,
  and Signal-family apps normally surface it as a safety-number warning.
  This pipeline counts it and drops it. Whether E06's UI should show
  anything — a per-conversation banner, nothing at all, or a Devices-screen
  prompt to re-verify — is a product decision, not a builder's.
  **Advisory recommendation: count it now, show nothing in E06, and shard
  the surface in the epic that owns safety numbers** — showing a scary
  warning for what is usually a benign reinstall is its own harm, and there
  is no design source for the treatment (a `design/gaps.md` entry would be
  needed first). Not blocking: the counter and the failure reason are
  recorded either way, so the answer changes UI, not this pipeline.
  - **Status:** 🟢 resolved
  - **Answer:** Count it now (this task already does), show nothing in E06's
    UI, and shard the surface (safety-number-style warning) into whichever
    future epic owns safety numbers / re-verification — a `design/gaps.md`
    entry for the treatment must precede that surface work, per the task
    file's own reasoning about avoiding a scary warning for what's usually a
    benign reinstall.
  - **Answered by:** orchestrator, applying the task file's own advisory
    recommendation, per the user's explicit standing instruction ("dont wait
    for me. Just do. If anything needed to ask me, and you have
    recommendation, go for it. I will check at the end.")
  - **Date:** 2026-08-29

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- 2026-08-29, `claude-sonnet-5 (builder)`: implemented on branch
  `epic_06_task_05` (worktree `.claude/worktrees/epic_06_task_05`, off
  `epic_06` @ `b667780`).

  **Context read first.** `agent/memory/lessons/backend.md` (L-backend-003 —
  about counters/cursors, not this task's own shape, so no direct action;
  read as instructed). `epics/E06-personal-chat/tasks/E06-T02.md` in full,
  including its Run log's falsification transcripts (the reference
  discipline this task's own falsifications below match), and its produced
  files `relay_packet_frame.dart` / `ciphertext_codec.dart` /
  `crypto_failures.dart`. `messaging_stack.dart` (E06-T03) for the
  `MessagingStack` shape this task's constructor consumes, and its own test
  file for the two-party session-establishment pattern this task's E2E
  tests reuse. `relay_engine.dart` (E04-T04) for `RelayEngine.enqueue`'s
  exact signature. `receive_message_use_case.dart` (E05-T03) for
  `ReceiveMessageUseCase.call`'s contract and its own header note (E05-B01's
  "known remaining gap") that explicitly names this task as the one that
  must build the wire-bytes-to-`CiphertextMessage` bridge.

  **Design.** `InboundPipeline` chains three `TransportService` streams —
  `discoveredDevices` -> `connectionState(id)` -> `incomingData(id)` —
  since the service exposes no single "list of connected peers"; a newly
  discovered id gets a `connectionState` listener, and only a transition to
  `connected` opens an `incomingData` subscription (idempotently, via
  `Map.putIfAbsent`), so a disconnected/failed peer's subscription is torn
  down rather than leaking (§6 risk). `start()` guards on a `bool _started`
  so a second call is a no-op, not a second `discoveredDevices`
  subscription. `_handleBuffer` computes `isForUs` immediately after
  `RelayPacketFrame.deserialize` succeeds, before `frame.payload` is bound
  to any local variable anywhere in the file — the forward branch reads
  `frame.destination`/`frame.priority`/`frame.expiresAtMs` only and
  re-enqueues the ORIGINAL wire `bytes` (not `frame.payload`, not a
  re-serialized frame) via `RelayEngine.enqueue`, then returns, before the
  file's only `frame.payload` read (inside the `isForUs`-only branch) is
  ever reached.

  **FR-ROUTE-003 audit (self-review evidence).**
  `grep -n "decode\|decrypt\|payload" lib/core/messaging/inbound_pipeline.dart`
  reviewed line by line: every hit before the forward branch's `return` (at
  what is now line 283) is a comment; the one production-code hit that reads
  `frame.payload` / calls `CiphertextCodec.decode` is at line 306, inside
  the `isForUs` branch, structurally unreachable from the `if (!isForUs) {
  ...; return; }` block above it.

  **FR-ROUTE-003 falsification (manual, per §6/§9).** Temporarily inserted
  `CiphertextCodec.decode(frame.payloadType, frame.payload);` into the
  forward branch (before `RelayEngine.enqueue`), then ran
  `test_EARS_COMM_9_foreign_packet_is_forwarded_unread`. Result: the crafted
  4-byte garbage payload made `decode` throw
  `CryptoDecryptFailure(unknown): unrecognised failure: _Exception`,
  uncaught (the forward branch has no surrounding try/catch), which
  propagated as an unhandled async error out of the stream listener; the
  test's own assertion also failed for the right reason —
  `expect(relayRows, hasLength(1))` -> `Actual: []` (0 rows), since
  `RelayEngine.enqueue` was never reached. Mutation reverted; `git diff`
  confirmed byte-identical to the pristine version; re-ran green (9/9 in
  `inbound_pipeline_test.dart`).

  **E05-B01 malformed-packet falsification (manual, per §6/§9).** Temporarily
  removed the `try`/`on FormatException` guard around
  `RelayPacketFrame.deserialize(bytes)` in `_handleBuffer` (left the raw
  call unguarded), then ran
  `test_EARS_COMM_10_malformed_packet_does_not_stop_the_loop`. Result: the
  empty-buffer input made `RelayPacketFrame.deserialize` throw
  `FormatException: RelayPacketFrame: empty buffer, missing frameVersion
  byte`, uncaught; the test's own assertion failed for the right reason —
  `expect(pipeline.counters.malformed, 1)` -> `Expected: <1> Actual: <0>`
  (the counter was never incremented, since the catch that increments it was
  the removed code). Mutation reverted; re-ran green.

  **Deviation found during implementation, not assumed up front.** The first
  version of `_handleBuffer`'s `on CryptoDecryptFailure` catch counted every
  reason as `undecryptable`. Running the suite against that version failed
  `test_EARS_COMM_8_duplicate_packet_persists_once` with
  `Expected: <1> Actual: <0>` on `counters.duplicate` — the SAME wire bytes
  pushed twice decrypt against the real Double Ratchet (this pipeline uses
  `stack.receiveMessage`'s default real `CryptoService.instance.decrypt`,
  not an injected fake), and `libsignal_protocol_dart` throws
  `DuplicateMessageException` on the second attempt (mapped by E06-T02's own
  taxonomy to `CryptoDecryptFailureReason.duplicateMessage`) BEFORE
  `ReceiveMessageUseCase`'s own envelope-id dedup check ever runs. Fixed by
  branching on `failure.reason` inside the catch: `duplicateMessage` counts
  as `counters.duplicate`, everything else as `counters.undecryptable` — see
  §9 Deviations for the full reasoning. Re-ran green afterward.

  **Tests.** 9 new tests in `inbound_pipeline_test.dart`, named by EARS id,
  driven through the REAL `TransportService`/`TransportEventsApi` Pigeon
  boundary (mocked native side, mirroring `transport_service_test.dart`'s
  own pattern) rather than a fake stream, so the discoveredDevices ->
  connectionState -> incomingData subscription chain is genuinely exercised,
  not assumed. The E2E seam test
  (`test_EARS_COMM_8_addressed_packet_is_delivered`) builds a real X3DH
  session between two full `MessagingStack`s, sends through
  `senderStack.sendMessage.call` (E05-T02, via T03's composition root), and
  feeds the resulting `sent.ciphertext` (the exact wire frame
  `_encryptAdapter` produced — already proven identical by
  `messaging_stack_test.dart`'s own round-trip test) into the receiver's
  pipeline as a simulated `onDataReceived` event.

  **Suite.** `flutter analyze`: 0 issues. `flutter test` (full suite):
  **281/281 pass** (272 pre-existing from E01-E05 + E06-T02/T03 + 9 new in
  `inbound_pipeline_test.dart`).

  Commit `4854392` — `feat(E06-T05): live inbound pipeline -- transport
  bytes to relay frame to deliver locally or forward`.

  `git status` after commit: clean; `git diff --stat` against `epic_06`
  shows exactly the 2 files declared in `files:`.
