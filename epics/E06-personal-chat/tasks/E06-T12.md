---
id: E06-T12
epic: E06
type: feature
title: Dashboard screen — real connectivity status, recent conversations, and FR-UI-004's one-tap detail
layer: frontend
size: M
status: review-requested
owner_agent: builder-ui
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: should, p: P2 }
depends_on: [E06-T04, E06-T11]
blocks: []
traces_to: [FR-UI-004, FR-UI-001, FR-UI-003, FR-COMM-001, FR-ROUTE-009]
required_context: [design/screens/dashboard.md, design/gaps.md, docs/routes.md, docs/design-system.md, epics/E06-personal-chat/tasks/E06-T04.md, epics/E06-personal-chat/tasks/E06-T09.md]
design_contract: design/screens/dashboard.md
files:
  create:
    - lib/features/dashboard/presentation/dashboard_view.dart
    - lib/features/dashboard/presentation/dashboard_controller.dart
    - lib/features/dashboard/presentation/dashboard_binding.dart
    - test/features/dashboard/presentation/dashboard_view_test.dart
    - test/features/dashboard/presentation/dashboard_controller_test.dart
  update:
    - lib/app/routes.dart
    - docs/routes.md
    - test/design/design_probe_test.dart
    - lib/features/login/presentation/login_controller.dart
    - test/widget_test.dart
  delete: []
started_at: 2026-08-30
completed_at: 2026-08-30
executed_by: claude-sonnet-5 (builder-ui)
reviewed_at: 2026-08-30
reviewed_by: independent reviewer (round 1, CHANGES — one off-palette color bug) + independent reviewer (round 2, APPROVE)
review_outcome: APPROVE
---
# E06-T12 · Dashboard screen

## 1. Goal
The app's front door: tell the user in one line whether they are connected,
put the technical detail exactly one tap away, and show what they were last
talking about.

## 2. Business logic
- **Rule 2.** `design/screens/dashboard.md` is law: 46 checklist elements,
  37 verbatim strings. `Network Status`, `Primary Node Connection`,
  `Connected`, `Encryption`, `Secure`, `Latency`, `Local Storage`,
  `45% used`, `Smart Mode - Older than 10 days`, `Recent Conversations`,
  `Message queued — will send when connected.` and the four nav labels are
  copy and are exact — including the em dash in the queued line.
- **EARS-COMM-2 / FR-UI-004** is this screen's reason to exist: *"The
  default view SHALL communicate connectivity simply ('You're connected');
  route/transport detail SHALL be one tap away."* The design supplies the
  simple half; GAP-012 supplies the other half by making the Network Status
  card navigate to `/devices`, a surface that already exists and already
  shows peers and transports — rather than inventing a screen.
- **`Latency` / `24ms` is real, or it is nothing.** Its only honest source
  is E06-T04's link-quality feed. E04-B03's standing prohibition applies at
  the UI layer too: *"do not synthesize fake measurements … strictly worse
  than one that honestly returns null."* With no measurement, show the
  neutral placeholder GAP-013 describes — never `24ms` as if it were
  measured.
- **`Connected` is one of three readings, not a constant** (GAP-013):
  connected, no peers, no route to the selected peer. This app is
  offline-first; "not connected" is a normal state, and FR-ROUTE-009 makes
  route failure an expected condition.
- **`Local Storage 45% used` has no data source until E08** (GAP-011).
  Render the card with its measured labels; use the real on-disk figure if
  it is cheaply available from the Drift file, otherwise a placeholder
  **disclosed in §Deviations and in GAP-011** — never a fabricated
  percentage presented as measured.
- **Recent Conversations** comes from `ConversationRepository`
  (T09) — the same read model as T10, so the two screens cannot disagree
  about recency or about who is blocked.
- The `Message queued — will send when connected.` row (elements 33-34) is
  a real state this epic finally produces: a `Queued` message, rendered
  with `radio_button_unchecked` exactly as measured — and it is the design's
  own evidence that GAP-009's glyph mapping for the Chat screen is drawn
  from the design rather than invented.

## 3. What this task does
- `/dashboard` route + `DashboardView` + `DashboardController` +
  `DashboardBinding`, three-file GetX pattern. `/dashboard` supersedes the
  genesis placeholder `/home` as the post-login destination.
- Network Status card: connection reading (GAP-013), `Encryption` /
  `Secure` from `MessagingStack.status` (T03) — `Secure` only when crypto
  actually initialized, `Latency` from T04's most recent measurement or the
  GAP-013 placeholder. The whole card is the tap target for `/devices`
  (GAP-012).
- Local Storage card per GAP-011.
- Recent Conversations: the top N summaries from
  `watchConversations()`, rendered with the contract's row treatment
  including the three distinct state glyphs the design draws
  (`check_circle`, `lock`, `radio_button_unchecked`). Tapping a row opens
  `/chat/<id>` (T11).
- Bottom nav with `Dashboard` active (elements 35-45).
- Registers the screen in `test/design/design_probe_test.dart`.
- Loading / empty / error states, including `MessagingStack.status ==
  unavailable` — the screen must render honestly when the stack failed to
  come up (T03 §3 built that path specifically so this screen could).

## 4. What this task does NOT do
- Does NOT implement storage management, retention, or Smart Mode (E08 —
  GAP-011). The card's labels are static copy until then.
- Does NOT build a new route-detail screen. GAP-012 routes to `/devices`
  deliberately; inventing a screen is the thing rule 2 forbids most
  directly.
- Does NOT change the Devices screen, or fix any E02 finding the new design
  gate surfaces there — that is a bug-sweep item, not this task's.
- Does NOT synthesize a latency, a battery figure, a storage percentage, or
  a peer count.
- Does NOT touch `lib/app/bindings.dart` or any backend file.
- Does NOT implement notifications, background status, or connectivity
  change alerts (E10).

## 5. Contract

### UI
- **Design contract:** `design/screens/dashboard.md`
- **Route:** `/dashboard`, added to `lib/app/routes.dart` and
  `docs/routes.md`; becomes the post-login destination in place of `/home`.
  Nav to: `/devices` (the Network Status card, GAP-012), `/chat/<id>`
  (a recent row), and the three other nav targets.
- **States required:** loading · data · empty (no conversations yet — reuse
  GAP-007's approved treatment rather than inventing a second one) · error
  (`MessagingStack.status == unavailable`).
- **Gate:** `make design-probe && make design-verify SCREEN=dashboard
  IMPL=flutter` **green before the PR** (rule 2).

### Functions
```yaml
- signature: "DashboardController({required MessagingStack stack, required ConversationRepository repo, required LinkQualityFeed links, required CryptoService crypto})"
  purpose: "GetX controller; deps injectable for tests, defaulting to registered instances"

- signature: "DashboardController.networkStatus -> Rx<NetworkStatusVm>"
  returns: "{reading: connected|noPeers|noRoute, latencyMs: int?, encryptionSecure: bool}"
  purpose: "FR-UI-004's simple half; latencyMs null means no measurement, never a default"

- signature: "DashboardController.recent -> RxList<ConversationTile>"
  returns: "top N conversation summaries, same view model as T10's rows"
  purpose: "Recent Conversations, from the same read model the list screen uses"

- signature: "DashboardController.openNetworkDetail() -> void"
  purpose: "FR-UI-004's 'one tap away' — navigates to /devices (GAP-012)"

- signature: "DashboardController.storageUsage -> Rx<StorageUsageVm>"
  returns: "{percentUsed: int?, isMeasured: bool}"
  purpose: "GAP-011 — isMeasured false renders the placeholder, never a fake number"
```

## 6. Risks
- The temptation to make the screen look like the mockup by filling
  `24ms`, `45% used` and `Connected` with plausible constants is enormous,
  and every one of those is the exact failure E04-B03's human decision
  prohibited. A screen that shows confident invented numbers is worse than
  one that shows a placeholder, because nobody can tell it is lying.
- Three glyph states in the Recent Conversations rows must map to the same
  delivery states as the Chat screen's ticks (GAP-009). Two screens with two
  mappings is exactly the "state name defined in two documents" problem
  E05-B03 was. Read the mapping from one place — the shared
  `ConversationTile` view model T10 defines.
- `/dashboard` replacing `/home` touches the post-login flow. Verify the
  login journey end to end; a broken redirect here breaks the app's entry.
- Expect gate iteration, as with T10 and T11. Never loosen the threshold.

## 7. Implementation checklist
- [x] 🧍 GAP-011/012/013 approved and `OQ-E06-T01-1` answered **before
      starting** — confirmed against `design/gaps.md` (all three approved
      2026-08-29) before this task's worktree was touched
- [x] tests written FIRST and failing, named by EARS id `1c955eb`
- [x] route + view + controller + binding; `/home` superseded `1c955eb`
- [x] Network Status card, three readings, real latency or placeholder `1c955eb`, `d3d4873`
- [x] card taps through to `/devices` (FR-UI-004) `1c955eb`
- [x] Local Storage card per GAP-011 `1c955eb`
- [x] Recent Conversations from the shared read model, rows → `/chat/<id>` `1c955eb`
- [x] bottom nav `1c955eb`, `d3d4873`
- [x] loading / empty / error (stack unavailable) states `1c955eb`
- [x] screen registered in `test/design/design_probe_test.dart` `1c955eb`
- [x] `node design/tools/verify.mjs --screen dashboard --impl flutter`
      run (`make` not on `PATH` in this environment — ran the underlying
      node script directly, same engine `make design-verify` invokes) —
      **not green**, 28.6% (18/63); every finding traced in §Run log,
      none silently worked around (rule 2)
- [x] `flutter analyze` clean, `flutter test` green (full suite: 382
      passed, 0 failed) `1c955eb`, `a98a28b`, `d3d4873`

## 8. Acceptance criteria (EARS) + test plan
- **EARS-COMM-2** *(epic-level)*: The default view SHALL communicate
  connectivity simply, and route/transport detail SHALL be one tap away.
  (FR-UI-004)
- **EARS-COMM-26**: IF no link measurement exists, THEN the screen SHALL
  NOT display a latency value. (FR-ROUTE-007, FR-UI-004)
- **EARS-COMM-27**: WHEN the messaging stack is unavailable, the Dashboard
  SHALL still render and SHALL say so. (FR-COMM-001)

### Tests
- `test_EARS_COMM_2_status_card_shows_a_simple_reading` — one word, no
  route/transport detail on the default view.
- `test_EARS_COMM_2_status_card_taps_through_to_devices` — one tap, the
  `/devices` route (the FR-UI-004 assertion).
- `test_EARS_COMM_26_no_measurement_shows_no_latency` — with an empty link
  feed, `24ms` and any other numeric latency is absent (assert the absence
  of a digit-bearing latency string, not just of the literal `24ms`).
- `test_EARS_COMM_26_real_measurement_is_displayed` — feed a measurement;
  that value appears.
- `test_EARS_COMM_27_unavailable_stack_still_renders` — `status ==
  unavailable`; the screen pumps, says so, and does not throw.
- `test_recent_conversations_use_the_shared_read_model` — assert the same
  ordering and blocked-exclusion as T10's list for identical data.
- `test_dashboard_view_matches_contract_elements` — the in-suite contract
  check.

### Manual
1. `make design-probe && make design-verify SCREEN=dashboard IMPL=flutter`
   → green; report in §Run log.
2. Log in → land on `/dashboard`; tap the status card → `/devices`; tap a
   recent row → that chat.

## 9. Self-review + Definition of Done
### Self-review
- [x] Every §7 item done, with commit hashes (`1c955eb`, `a98a28b`, `d3d4873`)
- [x] `flutter test && flutter analyze` green — `flutter analyze`: "No
      issues found!"; `flutter test` (full suite): 382 passed, 0 failed
- [x] Diff confined to `files:`; §4 respected — with two disclosed
      exceptions, both logged below (`login_controller.dart`,
      `test/widget_test.dart`), everything else is exactly the 8 files this
      task's own `files:` fence names
- [x] UI: `node design/tools/verify.mjs --screen dashboard --impl flutter`
      run — **not green, 28.6% (18/63)**; every finding traced below, none
      silently worked around or hidden (`make` is not on `PATH` in this
      environment; the underlying node script IS the engine `make
      design-verify` calls, per `design/tools/verify.mjs`'s own Makefile
      target — same comparison, same thresholds)
- [x] Loading / error / empty states present — `DashboardView`'s
      `errorMessage`/`loading`/empty-`recent` branches, covered by
      `test_EARS_COMM_27_unavailable_stack_still_renders` and
      `test_dashboard_view_matches_contract_elements`
- [x] **No synthesized measurement anywhere** — see the dedicated
      "No-synthesis audit" subsection of §Run log below; every displayed
      number traces to a real source or is the disclosed placeholder
- [x] Every extra or missing element traces to an approved `design/gaps.md`
      entry or an already-documented T01 probe-dumper limitation, listed
      below and in §Run log
- [x] `design/thresholds.yaml` and `design/screens/dashboard.md` unchanged
      — neither appears in `git diff --stat`

**Deviations from spec/design** (each with its spec/ADR reason):

1. **`lib/features/login/presentation/login_controller.dart` and
   `test/widget_test.dart` are touched, though neither is in this task's
   `files:` fence.** This task's own §3/§6 explicitly requires "`/dashboard`
   supersedes the genesis placeholder `/home` as the post-login destination"
   and names "verify the login journey end to end" as a risk — and
   `login_controller.dart:47`'s `Get.offNamed('/home')` was the ONLY place
   in the app that literal destination is hardcoded. There is no way to
   satisfy the task's own explicit instruction without touching this one
   line in this one file; the omission from `files:` is a gap in the task
   frontmatter, not license to skip the instruction (rule 6: a genuine
   contradiction between a task's body and its own fence is logged and
   worked through, not silently obeyed to the letter while breaking the
   task's own stated goal). `test/widget_test.dart` (the genesis
   walking-skeleton test) then broke as a direct, mechanical consequence —
   it asserted landing on `/home`'s `HomeView` — and needed its registered
   page/assertions updated to `/dashboard` (a minimal stub page, not the
   real `DashboardView`, since wiring a full `MessagingStack` into that
   genesis-scope test is out of scope; its actual assertions — the
   login→navigation seam and the real Drift write/read-back — are
   unaffected by which widget the destination route renders). Both changes
   are one/a few lines, documented here, and were the minimum needed to keep
   the full suite green after this task's own required behavior change.
2. **`Local Storage`'s `45% used` has no honest denominator, so it is
   NEVER measured, not just "not yet".** GAP-011 allows "the real on-disk
   figure if it is cheaply available from the existing Drift file size."
   The sqlite file's byte size IS cheaply obtainable (same
   `getApplicationDocumentsDirectory()` + `nexora.sqlite` construction
   `database.dart:292-297` already uses) — but "45% **used**" requires a
   quota/total to divide that byte count by, and no such quota exists
   anywhere in this schema, ADR, or spec (E08 owns storage management and
   has not built one). Computing a percentage against an invented
   denominator would itself be exactly the fabricated-figure defect this
   whole task exists to avoid — so `StorageUsageVm.isMeasured` is
   permanently `false` for E06, and the card renders "Not yet measured"
   (same measured 14px/`loginBody` style the design's own "45% used" uses)
   rather than reading the sqlite file size and dividing it by nothing.
   `Smart Mode - Older than 10 days` stays static copy, per GAP-011.
3. **`Latency`'s placeholder copy ("Not measured yet") and the connectivity
   readings' copy ("No peers nearby" / "No route to this peer") are real
   copy substitutions for the design's literal `24ms`/`Connected`**, per
   GAP-013's own approved answer — these are DATA the design's example
   happens to draw one value of, not fixed chrome, the same "data, not
   copy" treatment T10/T11 already established for device ids/message
   bodies.
4. **The delivery-tick glyph function (`_tickIconFor`/`_tickColorFor`) in
   `dashboard_view.dart` is a byte-for-byte duplicate of `chat_view.dart`'s
   own private function of the same name**, per this task's own §6 risk
   ("read the mapping from one place"): Dart's privacy model makes literal
   cross-file reuse of a `library`-private function impossible (the same
   constraint `_localSignalDeviceId` is re-declared under, four times, in
   `messaging_stack.dart`/`conversations_controller.dart`/
   `chat_controller.dart`/`dashboard_controller.dart`) — the SAME
   `ConversationTile.lastMessageState` `DeliveryState` value is read by both
   functions and both implement the identical GAP-009 mapping, so the two
   screens cannot disagree about what a glyph means, only about which file
   contains the (identical) switch statement.
5. **Design-fidelity gate is not green — every finding traced, not fixed by
   reshaping the widget tree (rule 2, Lesson 1 from T10's own reviewer
   finding)**:
   - **The dominant cause: `flutter_probe_dumper.dart`'s (T01-owned, out of
     this task's `files:` fence) `_isInteractive`/`insideInteractive`
     classification swallows every `Icon`/`Text` nested inside an
     `InkWell`/`Material` into one opaque `button` element.** This screen
     has MORE of its content inside a real tap target than `conversations`
     or `chat` did: GAP-012 makes the WHOLE Network Status card the tap
     target for `/devices` (not just one icon), and this task's own §3
     makes each ENTIRE Recent Conversations row the tap target for
     `/chat/<id>` (avatar + name + time + tick + preview, all one `InkWell`)
     — exactly the shape T10's Conversations rows already used and whose
     reviewer explicitly told this epic to keep (`GestureDetector`/
     `InkWell`/`Material`, never `Listener`, even at gate-score cost). This
     screen is not reshaped around the probe's limitation the way the
     original (reverted) `conversations_view.dart` draft was — every tap
     target here is the same accessible, correct primitive T10's reviewer
     mandated, and the resulting `button`-swallowing cost is the same,
     already-documented T01 capability gap (T10 Run log "Real defects found
     and fixed" item 1; T11 Run log "Every interactive element is
     InkWell/Material/TextField"), not a new one and not a defect in this
     screen's own logic.
   - **`OQ-E06-T01-1` (font assets not bundled)** — every `fontFamily`
     delta (`Inter`/`JetBrains Mono`/`Material Symbols Outlined` expected,
     `Roboto`/empty got). Pre-existing, already-approved-to-fix-later gap.
   - **`GAP-003` (no display-name/avatar/transport metadata pre-E04)** —
     real device ids (`device-a`, etc., per this task's own §2 instruction
     that the design's example copy is data) don't literally match
     `Family`/`Rahim`/`Ahmed`, and no avatar image exists to render.
   - **`GAP-011`/`GAP-013`** — the placeholder/real-data copy substitutions
     named in Deviations 2-3 above.
   - **One off-palette token was a real, fixed defect — not a false
     positive as this Run log originally claimed.** Round-1 review
     correctly caught this: the `'NEXORA'` header text used
     `NexoraTextStyles.devicesBrandTitle`, which resolves to
     `NexoraColors.welcomeHeading` = `rgb(195, 192, 255)` — but
     `design/screens/dashboard.md`'s own measured token table has no such
     value for this element; it measures `rgb(53, 37, 205)`
     (`NexoraColors.loginBrand`), the SAME colour the adjacent `hub`/`lock`
     icons on that exact header row already correctly use. `welcomeHeading`
     is real and correct for `devicesBrandTitle`'s actual home screens
     (Devices/Conversations, whose dark headers do measure that value) —
     it was simply the wrong shared constant to reuse here. **Fixed**
     (`dashboard_view.dart`, drafted via an OpenRouter free model —
     `minimax/minimax-m2.7:free`, reviewed and applied by the orchestrator,
     not auto-applied): the `'NEXORA'` `Text` now carries its own inline
     `TextStyle(fontSize: 28, fontWeight: FontWeight.w600, color:
     NexoraColors.loginBrand)` instead of reusing `devicesBrandTitle`
     wholesale. `tokens.dart` itself is untouched — Devices/Conversations
     keep using `devicesBrandTitle` unchanged. Re-verified: off-palette
     findings **2 → 1**, `flutter analyze` clean, `flutter test` 382/382
     (unchanged), gate re-run: 28.6% (18/63, match % unchanged — off-palette
     wasn't counted toward it either way, only the finding-list category).
   - **The one remaining off-palette finding is a genuine false positive**,
     independently confirmed by the round-1 reviewer: `rgba(11, 28, 48,
     0.102)` is the same `0x1A`-alpha-byte rounding artifact (`26/255 =
     0.10196...`) T11's own Run log already documented for its
     `devicesRowBorder`/`_headerComposerBorder` reuse — an unavoidable
     8-bit alpha quantization of the correct `0.1` value, not an invented
     colour.
   - Full report: `design/reports/dashboard/default@390x844/report.md`.

**Files actually touched:** `lib/features/dashboard/presentation/{dashboard_view,dashboard_controller,dashboard_binding}.dart`,
`test/features/dashboard/presentation/{dashboard_view_test,dashboard_controller_test}.dart`,
`lib/app/routes.dart`, `docs/routes.md`,
`test/design/design_probe_test.dart` — all in `files:` — plus, per
Deviation 1 above: `lib/features/login/presentation/login_controller.dart`
(one line) and `test/widget_test.dart` (destination page + two
assertions).

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id — see §Run
      log's EARS → test mapping
- [ ] Design gate green; extras trace to approved gaps — **not green**
      (28.6%, 18/63); every finding traced above and in §Run log, none
      hidden or untraced. Per rule 2/`skills/design-fidelity`, a red gate is
      reported honestly, not silently accepted as "close enough" — this is
      a reviewer/sweep triage call, the same precedent T10's 21.1%/T11's
      76.7% already established for this exact class of finding.
- [ ] Review APPROVE from a **different model** than `executed_by` (rule 5)
      — pending
- [ ] Squash-merged to `epic_06`; tracker stamped with the gate % — pending
- [ ] 🧍 Human `verified` — pending

## Open Questions
_(none of this task's own; it was gated on GAP-011/012/013's approval and on
`OQ-E06-T01-1`, both confirmed clear before work began — see §7. See
§9 Deviation 1 for the one out-of-fence, disclosed change this task's own
explicit instruction required.)_

## Feedback log
- (human feedback on this deliverable lands here)

## Run log

### Build
Implemented in worktree `.claude/worktrees/epic_06_task_12` off `epic_06`
(base `d68347e`, which already has T01-T11 merged). Commits:
- `1c955eb` — `feat(E06-T12): Dashboard screen -- route + view + controller
  + binding, real connectivity/latency/storage placeholders`
- `a98a28b` — `test(E06-T12): update genesis widget_test.dart for
  /dashboard redirect (E06-T12 Deviation)`
- `d3d4873` — `fix(E06-T12): dashboard layout overflow + reduce off-palette
  false positives (bg/dot token fixes)`

### Design decisions applied
- **`DashboardController`'s four contract params map to real, already-
  registered singletons via `DashboardBinding`**: `stack` = `Get.find
  <MessagingStack>()`; `links` = `Get.find<LinkQualityFeed>()` (E06-T04's
  already-permanent instance); `repo` = a fresh `ConversationRepository`
  built from `stack.db`/`stack.selfDeviceId` (E06-T09 never registered a
  shared instance, same pattern `ConversationsBinding`/`ChatBinding`
  already use); `crypto` = `stack.cryptoService`. `bindings.dart` is
  untouched (out of this task's `files:` fence, same as T10/T11).
- **Connectivity reading (GAP-013) is derived from real transport/routing
  state the controller listens to directly via `LinkQualityFeed`'s own
  public `transport`/`routing` fields** (never touching
  `link_quality_feed.dart`/`routing_engine.dart`/`transport_service.dart`,
  all outside this task's `files:` fence): a `Set<String> _knownPeerIds`
  built from `transport.discoveredDevices`/`lostDevices`; `noPeers` when
  empty, else `connected` when `routing.computeRoute(peerId,
  TrafficProfile.interactive)` is non-null for at least one known peer,
  else `noRoute`. `DashboardController.onInit()` also calls
  `_links.start()` — idempotent per `LinkQualityFeed`'s own contract, so in
  production (where `bindings.dart` already started the SAME singleton)
  this is a harmless no-op, and in a test that constructs a fresh
  `LinkQualityFeed` this is what actually closes the
  `transport.linkQuality -> RoutingEngine.recordLinkMeasurement` loop this
  screen's own `computeRoute` calls depend on — found directly:
  `test_EARS_COMM_2_reading_becomes_connected_once_a_route_exists` failed
  (`noRoute` instead of `connected`) until this call was added, because
  nothing was feeding the routing engine.
- **Latency (EARS-COMM-26) is the most recent `LinkQuality.latencyMs` from
  `transport.linkQuality`'s stream, held in a private `int? _latestLatencyMs`
  that starts `null` and is never defaulted.** No measurement -> the
  "Not measured yet" placeholder; the literal digit-bearing string is
  asserted ABSENT by `test_EARS_COMM_26_no_measurement_shows_no_latency`
  via `RegExp(r'\dms\b')` over every rendered `Text`, not just an assertion
  that `"24ms"` specifically is absent (task §8's own instruction).
- **Encryption `Secure`/`Not secure` reads `MessagingStack.status.isReady`**
  (T03's contract: `isReady` is true only after `CryptoService.init`
  actually succeeded) — never assumed. In practice this screen's own
  `onInit()` already routes a not-ready stack to the whole-screen error
  state (EARS-COMM-27) before the Network Status card ever renders, so
  `encryptionSecure: false` is a defensive branch this screen's own
  control flow makes unreachable today, not a state a real user can
  currently see; kept in the view model and tested directly
  (`test_encryption_secure_reflects_messaging_stack_status`) per the task's
  own §5 contract, rather than removed as dead code.
- **Recent Conversations reuses `ConversationTile` from
  `conversations_controller.dart` directly (`show ConversationTile`)** —
  not a redefinition. `kDashboardRecentConversationCount = 3` caps the list
  at the design's own three example rows
  (`test_recent_conversations_capped_at_the_designs_row_count`). Preview
  decryption is the SAME best-effort, graceful-degrade logic
  `ConversationsController._resolvePreview` already implements, duplicated
  per Dart's privacy model (this file's header explains why, same as the
  four `_localSignalDeviceId` declarations already in this codebase).
- **The `Message queued — will send when connected.` row (task §2's
  "real state this epic finally produces")**: `_RecentRowStatusLine` shows
  this EXACT string (not the decrypted preview) when
  `tile.lastMessageIsMine && tile.lastMessageState ==
  DeliveryState.queued`, with the `radio_button_unchecked` glyph — tested
  directly by `test_queued_own_message_shows_the_exact_queued_copy`.
- **`/dashboard` supersedes `/home` as the post-login destination**
  (`login_controller.dart`, Deviation 1) — verified end to end by
  `test/widget_test.dart`'s walking-skeleton test, which now asserts
  landing on `/dashboard` after a real sign-in + Drift write, not `/home`.

### No-synthesis audit (§2/§6's central discipline)
- **`Latency`**: real, from `TransportService.linkQuality`'s most recent
  event, or absent (no digit-bearing string) — never a default. Proven by
  `test_EARS_COMM_26_no_measurement_shows_no_latency` (asserts absence via
  regex over every rendered `Text`, not just literal `"24ms"`) and
  `test_EARS_COMM_26_real_measurement_is_displayed`/
  `test_EARS_COMM_2_reading_becomes_connected_once_a_route_exists` (a
  pushed `onLinkQuality` event through the real generated Pigeon codec,
  same falsification pattern `link_quality_feed_test.dart` established).
- **Connectivity reading**: real, from `discoveredDevices`/`lostDevices`/
  `computeRoute` — three states proven by
  `test_EARS_COMM_2_default_reading_is_no_peers_with_no_peers_known`,
  `test_EARS_COMM_2_reading_becomes_connected_once_a_route_exists`,
  `test_EARS_COMM_2_reading_is_no_route_when_a_peer_is_known_but_unreachable`.
  Muted grey dot (`NexoraColors.loginBody`, == the contract's own measured
  `rgb(70, 69, 85)`) for both non-connected readings, no new colour token.
- **Encryption**: real, from `MessagingStack.status.isReady` — proven by
  `test_encryption_secure_reflects_messaging_stack_status` and (the
  unavailable half) `test_EARS_COMM_27_unavailable_stack_reports_an_honest_error`/
  `test_EARS_COMM_27_unavailable_stack_still_renders`.
- **`Local Storage` `45% used`**: `StorageUsageVm.isMeasured` is `false`,
  permanently, for this task — no quota exists to compute a percentage
  against (Deviation 2 above explains why "cheaply available" does not
  apply here even though the raw file size would be). Proven by
  `test_storage_usage_is_the_disclosed_placeholder_never_a_fabricated_percentage`.
  The rendered placeholder is "Not yet measured", never a number.
- **Recent Conversations' delivery ticks**: read directly from
  `ConversationTile.lastMessageState` (the real, persisted
  `DeliveryState`), mapped through the SAME function `chat_view.dart` uses
  (Deviation 4) — never a fabricated or independently-invented state.

### The real `dashboard` gate report — verbatim (final run; not fixed
further per T10/T11's own established precedent — see §4/§9 Deviation 5)
```
design: ❌ FAIL dashboard/default@390x844 — match 28.6% (18/63)
  ❌ 45 missing element(s) (max 0)
  ❌ 31 copy mismatch(es) (max 0)
  ❌ 79 style delta(s) (max 0)
  ❌ 2 off-palette token(s) (max 0)
  ⚠️  15 layout delta(s) (max 0)
     missing: generic <body> @0,0 390×844
     missing: heading:1 "NEXORA"
     missing: generic "lock"
     missing: heading:2 "Network Status"
     missing: generic <div> @238,97 119×26
     copy [changed]: expected "Primary Node Connection" · got "NEXORA"
     copy [changed]: expected "Secure" · got "Local Storage"
     copy [changed]: expected "Family" · got "(empty)"
     copy [changed]: expected "check_circle" · got "(empty)"
     copy [absent]: expected "lock"
     style: generic "hub" — fontSize: expected 24px, got 0px (Δ24.0px)
     style: generic "hub" — fontFamily: expected Material Symbols
       Outlined, got  ()
     style: generic "warning" — fontSize: expected 18px, got 0px (Δ18.0px)
     style: generic "warning" — fontFamily: expected Material Symbols
       Outlined, got  ()
     style: generic "Smart Mode - Older than 10 days" — fontFamily:
       expected JetBrains Mono, got Roboto ()
     …+140 more
     report: design\reports\dashboard\default@390x844\report.md

design: 0/1 screen-state(s) pass the gate
```
Full report at `design/reports/dashboard/default@390x844/report.md`
(generated artifact, not committed).

Iteration history (per §6's own "expect gate iteration" note): first real
run was **0% (0/63)** — the impl probe dumped an empty tree with
`renderError: true`. Root-caused directly (not guessed): a real
`RenderFlex` overflow of 22px in the Network Status card's `Latency` row
(`Row([Icon, SizedBox, Text('Latency'), Spacer(), Text(...)])` — the
`Spacer` plus the "Not measured yet"/`"${latencyMs}ms"` trailing text
didn't fit at 316px available width). Fixed by wrapping both trailing
value `Text`s (`Encryption`'s `Secure`/`Not secure` and `Latency`'s value)
in `Flexible(..., overflow: TextOverflow.ellipsis)`. Re-ran: **30.2%
(19/63)**. Then fixed two real, cheap, non-probe-workaround defects found
by reading the report against the contract's own token table (which lists
NO `rgb(255,255,255)` usage anywhere on this screen): `Scaffold`/header/
bottom-nav backgrounds were `Colors.white`, changed to the contract's own
measured `rgb(248,249,255)` (`_cardFill`); the connectivity dot was a
`BoxDecoration` circle, changed to `Icon(Icons.circle, ...)` to match the
design's own literal "circle" glyph element and read its colour as
foreground the way the golden capture does. Final: **28.6% (18/63)** — a
lower raw match count than the intermediate step but a real reduction in
off-palette findings (4 → 2) and style deltas (86 → 79); the remaining gap
is dominated by the T01 probe-dumper's `InkWell`-swallowing limitation
(§9 Deviation 5), which no further widget-tree change can fix without
either reverting to `Listener` (forbidden — Lesson 1, T10's own reviewer
finding) or removing the GAP-012/task-§3-required tap targets, neither of
which is this task's call to make.

### Falsification (rule: evidence, not vibes)
- **`test_EARS_COMM_2_reading_becomes_connected_once_a_route_exists`**:
  before `_links.start()` was added to `onInit()`, this test failed
  (`Expected: connected / Actual: noRoute`) because nothing fed
  `RoutingEngine.recordLinkMeasurement` despite a real `onLinkQuality`
  event being pushed — confirming the assertion exercises the real
  producer→consumer loop, not a stub. Restored the fix; green.
- **The layout overflow**: reproduced directly with `FlutterError.onError`
  wired to print the full error (`A RenderFlex overflowed by 22 pixels on
  the right` at `dashboard_view.dart:298`, the `Latency` row) before
  applying the `Flexible` fix; confirmed clean (`tester.pump()` with no
  captured exception) after.

### Test summary
`flutter analyze`: "No issues found!". `flutter test` (full suite): **382
passed, 0 failed** (up from T11's 363 — 9 controller tests + 8 view tests
in `test/features/dashboard/` + 1 new `dashboard` design-probe test in
`test/design/design_probe_test.dart`, plus `test/widget_test.dart`'s
existing test updated in place, not added).

EARS → test mapping:
- EARS-COMM-2 (FR-UI-004, epic-level) →
  `test_EARS_COMM_2_status_card_shows_a_simple_reading`,
  `test_EARS_COMM_2_status_card_taps_through_to_devices` (both
  `dashboard_view_test.dart`),
  `test_EARS_COMM_2_default_reading_is_no_peers_with_no_peers_known`,
  `test_EARS_COMM_2_reading_becomes_connected_once_a_route_exists`,
  `test_EARS_COMM_2_reading_is_no_route_when_a_peer_is_known_but_unreachable`
  (`dashboard_controller_test.dart`)
- EARS-COMM-26 → `test_EARS_COMM_26_no_measurement_shows_no_latency`,
  `test_EARS_COMM_26_real_measurement_is_displayed` (both files)
- EARS-COMM-27 → `test_EARS_COMM_27_unavailable_stack_still_renders`
  (`dashboard_view_test.dart`),
  `test_EARS_COMM_27_unavailable_stack_reports_an_honest_error`
  (`dashboard_controller_test.dart`)
- Contract-only (no EARS id, part of §5/§9):
  `test_recent_conversations_use_the_shared_read_model`,
  `test_recent_conversations_capped_at_the_designs_row_count`,
  `test_storage_usage_is_the_disclosed_placeholder_never_a_fabricated_percentage`,
  `test_encryption_secure_reflects_messaging_stack_status`,
  `test_dashboard_view_matches_contract_elements`,
  `test_recent_conversations_row_tap_opens_the_chat`,
  `test_queued_own_message_shows_the_exact_queued_copy`
