---
id: E06-T08
epic: E06
type: feature
title: Delivery acknowledgements — Accepted / Delivered / Read have a producer
layer: backend
size: M
status: review-requested
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: should, p: P2 }
depends_on: [E06-T07]
blocks: []
traces_to: [FR-MSG-002, FR-MSG-003, FR-COMM-001, NFR-SEC-001]
required_context: [epics/E05-messaging-reliability/tasks/E05-T01.md, epics/E05-messaging-reliability/tasks/E05-T03.md, epics/E05-messaging-reliability/tasks/E05-B03.md, epics/E06-personal-chat/tasks/E06-T02.md, epics/E06-personal-chat/tasks/E06-T05.md, epics/E06-personal-chat/tasks/E06-T06.md, design/screens/chat.md]
design_contract: n/a
files:
  create:
    - lib/core/messaging/delivery_ack.dart
    - test/core/messaging/delivery_ack_test.dart
  update:
    - lib/core/messaging/messaging_stack.dart
    - lib/core/messaging/inbound_pipeline.dart
    - lib/core/messaging/prekey_exchange.dart
    - test/core/messaging/prekey_exchange_test.dart
  delete: []
started_at: 2026-08-30
completed_at: 2026-08-30
executed_by: claude-sonnet-5
reviewed_at: 2026-08-30
reviewed_by: independent reviewer (model, opus)
review_outcome: APPROVE
---
# E06-T08 · Delivery acknowledgements

## 1. Goal
Make the tick marks the design draws mean something: give
`Accepted`/`Delivered`/`Read` an actual producer, so a sender learns what
happened to their message instead of the state machine stopping at `Sent`
forever.

## 2. Business logic
- **The gap, found by applying E04-B03's union-sufficiency check
  (L-process-005) to E06's own scope.** `DeliveryState` has seven states
  (`delivery_state_machine.dart:13`). E05-T02 writes `queued`/`sent`/
  `failed`; E05-T03 writes `accepted` on the **receiving** device.
  **Nothing ever tells the sender.** `delivered` and `read` have no
  producer anywhere, on any device, and never have.
- **The design assumes otherwise.** `design/screens/chat.md` draws three
  distinct tick treatments — `check` (element 25), grey `done_all`
  (element 22), green `done_all` (element 14). Two of the three have no
  data source without this task. GAP-009 records the mapping; this task is
  what makes two-thirds of it real.
- **E05-B03's resolution constrains the vocabulary.** The human resolved
  (Option A) that `Sent` means *durably enqueued locally* — it is applied
  on an INSERT that cannot fail, so it says nothing about the radio. That
  makes the *remote* states the only ones carrying real information, which
  is precisely why this task matters more than it looks.
- **Acknowledgements are metadata about ciphertext, never content.** An ack
  carries a message id and a state, both of which a relay already sees in
  the frame header (FR-ROUTE-004). It must not carry, and must not require,
  anything derived from plaintext — FR-ROUTE-003.
- **Read receipts are a privacy choice, not a technical default.** Sending
  "I read your message" tells someone when you looked at your phone. Every
  serious messenger makes this a setting. There is no spec id and no design
  source for that setting — see `OQ-E06-T08-1`; until it is answered this
  task builds the *mechanism* and leaves `read` acks **off**.

## 3. What this task does
- **The ack control sub-protocol**, as `PayloadType.control` frames
  (E06-T02) over `InboundPipeline`'s `ControlHandler` seam (E06-T05):
  `deliveryAck { messageId, state, observedAtMs }` where `state` ∈
  `{accepted, delivered, read}`.
- **Producing acks on the receiving device:**
  - `accepted` — emitted when `ReceiveMessageUseCase` persists a new
    message (it already writes `accepted` locally; this sends the same fact
    back). Not emitted for a duplicate.
  - `delivered` — emitted at the same point. On this app `accepted` and
    `delivered` collapse: there is no separate "handed to the UI layer"
    moment, and inventing an artificial delay between them to justify two
    states would be fiction. **Both are sent; the sender records the higher
    one.** This is stated here rather than left implicit because a future
    reader will otherwise assume a step is missing.
  - `read` — the mechanism exists and is **disabled** pending
    `OQ-E06-T08-1`. Behind a single named constant, not scattered `if`s.
- **Consuming acks on the sending device:** validate that the ack's
  `messageId` belongs to a message this device sent to *that* peer, then
  transition via `DeliveryStateMachine` (never a raw enum write) and append
  the `delivery_states` row through `MessagingCoordinator.recordStored`'s
  existing path. An ack for an unknown or foreign message id is dropped and
  counted — an unvalidated ack lets any peer rewrite any message's state.
- **Monotonic only.** `DeliveryStateMachine` already rejects backward
  transitions (`accepted → sent` throws). A late or out-of-order ack must be
  a no-op, not an exception that kills the pipeline: check
  `canTransition` before `transition`.
- Registered as a control handler in `messaging_stack.dart` — **see
  `OQ-E06-T08-2`**: `InboundPipeline`'s control-handler seam is a single
  named slot already occupied by E06-T07's `PrekeyExchange`, so this task
  also carries a small, scoped retrofit of that seam and of T07's existing
  wire framing to make room for a second control sub-protocol. Do not
  attempt to register a second handler into the existing single-slot
  `registerControlHandler` as-is — it throws `StateError`.

## 4. What this task does NOT do
- Does NOT enable read receipts (`OQ-E06-T08-1`).
- Does NOT define what `Stored` means or produce it — that is
  `OQ-E06-T11-2`, inherited from OQ-E05-T03-1, and it is a UI-semantics
  question this task deliberately does not pre-empt.
- Does NOT retry, re-request or reconcile missing acks. If an ack is lost,
  the message simply stays at its last known state. Building a
  reconciliation protocol here would be the gap-fill protocol wearing a
  disguise (see OQ-E06-T06-2, owned by E11).
- Does NOT write `RelayDeliveryState.failed` or introduce a delivery
  timeout that fails a message. If a future task adds one, OQ-E06-T06-3's
  reclaim-set constraint binds it.
- Does NOT modify `DeliveryStateMachine`, `ReceiveMessageUseCase`,
  `SendMessageUseCase`, or the `messages`/`delivery_states` schema.
- Does NOT render anything. T11 reads the state; this task produces it.

## 5. Contract

### Functions
```yaml
- signature: "DeliveryAck.serialize() -> Uint8List"
  purpose: "{messageId, state, observedAtMs} as a control-frame body, same framing idiom as the rest"

- signature: "DeliveryAck.deserialize(Uint8List bytes) -> DeliveryAck"
  raises: "FormatException; an unknown state value is rejected, never coerced"

- signature: "DeliveryAckService.onMessageStored(Message message, String senderDeviceId) -> Future<void>"
  purpose: "receiving side — emits accepted + delivered acks back to the sender; never for a duplicate"

- signature: "DeliveryAckService.handleControlFrame(RelayPacketFrame frame) -> Future<void>"
  purpose: "sending side — validates ownership, then advances state through DeliveryStateMachine"

- signature: "DeliveryAckService.markRead(String messageId) -> Future<void>"
  purpose: "emits a read ack IF read receipts are enabled; a no-op while OQ-E06-T08-1 is open. Called by T11 when a message is displayed."

- signature: "DeliveryAckService.counters -> DeliveryAckCounters"
  returns: "{sent, accepted, rejectedForeign, rejectedUnknownMessage, staleIgnored}"
  purpose: "so 'why is this message still on one tick?' is answerable"
```

### Data
No schema change — `delivery_states` (E05-T01) is the existing sink and
finally gets its second writer.

## 6. Risks
- **An unvalidated ack is a state-injection primitive.** Any peer could mark
  any message `read`. The ownership check (this device sent it, to that
  peer) is the security control, not a sanity check — test it as such.
- Ack storms: two acks per message doubles mesh traffic for text chat, and
  NFR-BATT-001 asks for less, not more. Keep acks small, do not ack an ack,
  and never ack a duplicate.
- `accepted` and `delivered` arriving in either order over a mesh: the
  transition guard must make the later-and-lower one a no-op. Test with the
  acks deliberately reversed.
- The `read`-disabled constant must genuinely gate the *emission*, not just
  the UI. A disabled feature that still puts frames on the wire has leaked
  the exact information it was meant to withhold.

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS id  `fb3ecc3`
- [x] `DeliveryAck` codec + malformed matrix  `b1c88a6`
- [x] receiving side: accepted + delivered emission (not on duplicates)  `b1c88a6`
- [x] sending side: ownership validation + monotonic transition  `b1c88a6`
- [x] `read` path implemented and disabled behind one named constant  `b1c88a6`
- [x] the `InboundPipeline`/`PrekeyExchange` control-seam retrofit (`OQ-E06-T08-2`) — leading `controlKind` byte, keyed dispatch, T07's existing tests still green unchanged in behavior  `b1c88a6`
- [x] registered as a control handler in `messaging_stack.dart`  `b1c88a6`
- [x] `flutter analyze` clean, `flutter test` green (full suite)  `b1c88a6`

## 8. Acceptance criteria (EARS) + test plan
- **EARS-MSG-7**: WHEN a recipient persists a new message, the sender SHALL
  observe that message advance from `Sent` to `Delivered`. (FR-MSG-002)
- **EARS-MSG-8**: IF an acknowledgement refers to a message this device did
  not send, or did not send to that peer, THEN the system SHALL ignore it
  and SHALL NOT change any delivery state. (FR-MSG-002, NFR-SEC-001)
- **EARS-MSG-9**: IF acknowledgements arrive out of order, THEN the system
  SHALL never move a message backwards through the state machine.
  (FR-MSG-002)

### Tests
- `test_EARS_MSG_7_sender_sees_delivered_after_receiver_stores` — two
  stacks, a real send through T05's pipeline, acks travelling as real
  control frames; assert the sender's row reaches `delivered` and a
  `delivery_states` row exists for it.
- `test_EARS_MSG_7_duplicate_delivery_does_not_re_ack` — the same packet
  twice; one ack pair; `counters.sent == 2` not 4.
- `test_EARS_MSG_8_foreign_ack_is_rejected` — an ack for a message id owned
  by a third device, and an ack from the wrong peer for a real message id:
  both rejected, both counted, state unchanged.
- `test_EARS_MSG_9_out_of_order_acks_do_not_regress_state` — `delivered`
  then `accepted`; final state `delivered`; no exception.
- `test_read_acks_are_not_emitted_while_disabled` — `markRead` puts **zero
  bytes** on the wire (assert on the transport fake, not on the UI).

### Manual
1. 🧍 Two devices: send a message, watch the tick change on the sender's
   screen when the recipient's app receives it. (Depends on T11 for the
   visual; the state change is observable in the DB before that.)

## 9. Self-review + Definition of Done
### Self-review
- [x] Every §7 item done, with commit hashes
- [x] `flutter test && flutter analyze` green — full suite: **337 passed, 0
      failed**; `flutter analyze` (whole project): **No issues found!**
      (§Run log has the exact commands/output)
- [x] Diff confined to `files:`; §4 respected — see "Files actually touched"
      below; `git status --short`/`git diff --stat` against `epic_06` show
      only the six files this task's fence declares, nothing else.
- [x] Every state write goes through `DeliveryStateMachine` —
      `grep -rn "deliveryState\s*=" lib/core/messaging/` → **zero raw
      writes** (empty result; full command run in §Run log). Every write in
      `delivery_ack.dart` goes through `Message.withDeliveryState`, which
      itself calls `DeliveryStateMachine.transition`.
- [x] Read receipts emit nothing on the wire while disabled — proven at the
      transport level (`test_read_acks_are_not_emitted_while_disabled`
      asserts `sendCallCount == 0` on the mocked `TransportApi.send`
      channel after calling `markRead`, not by reading `kReadReceiptsEnabled`'s
      value).
- [x] No message content, only ids and states, in any ack or log —
      `DeliveryAck`'s wire body is `{messageId, state, observedAtMs}` only;
      `grep -n "print(\|log\.\|debugPrint" lib/core/messaging/delivery_ack.dart`
      → no matches (§Run log).

**Deviations from spec/design:**
1. **`test/core/messaging/prekey_exchange_test.dart` is declared in this
   task's `update:` fence (per `OQ-E06-T08-2`'s own resolution) but ends up
   byte-identical — no edit was needed.** The retrofit (`OQ-E06-T08-2`,
   option (a)) moves the `controlKind` framing entirely into
   `PrekeyExchange._sendControlFrame` (prepend) and
   `InboundPipeline._handleBuffer` (strip, before calling the registered
   handler) — `PrekeyExchange.handleControlFrame`'s own body, and therefore
   every existing test that either drives frames through the real
   `InboundPipeline`/transport loopback OR calls `handleControlFrame`
   directly with a hand-built, un-prefixed `_ControlBody` payload (this
   file's own `test_unsolicited_bundle_response_is_dropped`), sees exactly
   the same shape of `frame.payload` it always did. Confirmed by running
   `test/core/crypto/prekey_bundle_codec_test.dart` (8 tests) and
   `test/core/messaging/prekey_exchange_test.dart` (7 tests) after the
   retrofit: all 15 green, unchanged from T07's own reported count (§Run
   log). The file stays in the fence rather than being removed from it,
   since `OQ-E06-T08-2`'s own resolution explicitly names it as a file this
   task might need to touch — it turned out not to.
2. **`DeliveryAckService.onMessageStored` is wired automatically from
   `MessagingStack`'s own constructor** (subscribing to
   `inbound.delivered`), rather than left for a caller to invoke per
   message. The task's §5 contract declares `onMessageStored` as a public
   method without specifying its own call site; `messaging_coordinator.dart`
   (which already subscribes to the same `inbound.delivered` stream for
   `recordStored`) is explicitly OUT of this task's `files:` fence, so
   wiring the subscription there was not an option. `messaging_stack.dart`
   IS in the fence and is exactly where `PrekeyExchange`'s own control-
   handler registration already happens for the identical "needs a
   fully-constructed `this`" reason — this subscription follows that same,
   already-established pattern. `InboundPipeline.delivered` only ever fires
   for a newly-persisted, NON-duplicate message (`ReceiveMessageUseCase`
   returning `null` short-circuits before that stream is touched), which is
   also exactly how "never for a duplicate" (task file §3) is satisfied
   with zero new dedup logic. `onMessageStored` itself remains independently
   callable and independently tested (this task's own unit tests call it
   indirectly by driving a real inbound receipt, and directly through
   `handleControlFrame`/`markRead` for the other call paths) — nothing about
   its own signature or contract changed.
3. **`DeliveryAckService._sendAck` swallows a `transport.send` failure**
   rather than letting it propagate, even though `PrekeyExchange
   ._sendControlFrame` (this file's own stylistic precedent) does not. Found
   while running the full suite after wiring `deliveryAckService` into
   `messaging_stack.dart`'s constructor (deviation 2 above): because that
   wiring makes ack-sending automatic and unconditional, a *pre-existing*
   test in `messaging_coordinator_test.dart` (`test_start_calls
   _inbound_pipeline_start`, out of this task's `files:` fence — not edited)
   started failing, because its receiver stack has no mock handler for its
   OWN outbound `TransportApi.send` channel (it never needed one before this
   task, since only the SENDER side used to call `send`). The resulting
   `MissingPluginException`, thrown inside an `unawaited(...)` future
   (`messaging_stack.dart`'s own subscription), surfaced as an uncaught
   async zone error against whatever test happened to be running — a
   real self-review catch, not something either test file's own assertions
   would show. Fixed by wrapping the one `await
   _stack.transport.send(...)` call in `_sendAck` in a `try/catch` that
   swallows any exception without incrementing `counters.sent` — consistent
   with task file §4's own stated contract ("does NOT retry, re-request or
   reconcile missing acks … if an ack is lost, the message simply stays at
   its last known state"): a failed *send* is one more way an ack can be
   lost, not a new failure mode this file needs to surface, and per rule 8
   this is the kind of miss that should be checked for on any future
   fire-and-forget stream subscription added to a composition root.

**Files actually touched:**
- `lib/core/messaging/delivery_ack.dart` (new)
- `test/core/messaging/delivery_ack_test.dart` (new)
- `lib/core/messaging/messaging_stack.dart` (updated — `DeliveryAckService`
  construction, keyed control-handler registration for both sub-protocols,
  `inbound.delivered` subscription)
- `lib/core/messaging/inbound_pipeline.dart` (updated — `OQ-E06-T08-2`
  retrofit: `Map<int, ControlHandler>`, keyed `registerControlHandler`,
  `controlKind`-byte read/strip in `_handleBuffer`)
- `lib/core/messaging/prekey_exchange.dart` (updated — `OQ-E06-T08-2`
  retrofit: `kControlKindPrekeyExchange` constant, `_sendControlFrame`
  prepends it; `handleControlFrame`'s own body unchanged)
- `test/core/messaging/prekey_exchange_test.dart` (declared in the fence per
  `OQ-E06-T08-2`; not edited — see Deviation 1)

Exactly the task's declared `files:` set — no other file touched.

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id — see §Run
      log for the full EARS -> test mapping.
- [x] Review APPROVE from a **different model** than `executed_by` (rule 5)
      -- `executed_by: claude-sonnet-5`, reviewed by opus; independent
      re-verification, not a summary of the builder's self-report.
- [ ] Squash-merged to `epic_06`; tracker stamped
- [ ] 🧍 Human `verified`

## Open Questions
- **OQ-E06-T08-1 — should read receipts be sent, and is that a user
  setting?** Nothing in `spec/` mentions read receipts; `FR-MSG-002` lists
  `Read` as a state but never says the *sender* learns about it, and
  `design/screens/chat.md` draws a green `done_all` (element 14) that
  strongly implies they do. FR-TRUST-006 enumerates the privacy toggles the
  product offers and read receipts are not among them, so adding one is new
  scope, not an omission. Options: (a) always send — matches the design's
  implication, simplest, and tells every contact when you opened your
  phone; (b) never send — the green `done_all` then never appears and the
  design element is permanently unreachable, which is a design-fidelity
  finding, not a neutral choice; (c) a per-account setting defaulting to
  off, added to the Settings screen — the standard answer, and it needs an
  SRS amendment plus a `design/gaps.md` entry because there is no design
  source for the row. **Advisory recommendation: (c), defaulting to off**,
  with the setting sharded into E02's settings surface rather than invented
  inside a chat task. Until then the mechanism ships disabled and GAP-009
  records that the green `done_all` is unreachable, so the gate reports it
  honestly rather than the state being hidden.
  - **Status:** 🟡 open (important, non-blocking — owner: human; the
    mechanism is built either way, only the constant and a settings row
    change)
  - **Answer:** _<empty>_
  - **Answered by:** _<empty>_
  - **Date:** _<empty>_

- **OQ-E06-T08-2 — `InboundPipeline`'s control-handler seam is a single
  named slot, and E06-T07's `PrekeyExchange` already occupies it before
  this task starts.** Found during E06-T07's independent review (the
  orchestrator, not this task's own builder): `inbound_pipeline.dart`
  (E06-T05) declares exactly one nullable `_controlHandler` field, and
  `registerControlHandler` explicitly throws `StateError('a control handler
  is already registered')` on a second call — by design
  (`inbound_pipeline.dart`'s own doc comment: "this task defines one named
  registration slot"). `MessagingStack.create()` already calls
  `inbound.registerControlHandler(prekeyExchange.handleControlFrame)`
  (E06-T07). This task's own §3/§7 also say "registered as a control
  handler in `messaging_stack.dart`" without knowing the slot is taken —
  built as currently scoped, wiring `DeliveryAckService` the same way
  throws at `MessagingStack.create()` time, breaking the whole stack, not
  just acks. This is the same cross-task-obligation shape the retro named
  L-process-006 (E06-T06-4 was its most recent prior instance, one task
  earlier in this same epic).

  Two further facts narrow the fix: (1) `PayloadType` (`relay_packet_frame.dart`,
  E06-T02) has exactly one generic non-message tag, `control(3)` — this
  task's own contract already commits to reusing that SAME tag rather than
  requesting a new wire-level `PayloadType`, so the fix belongs inside the
  control channel's own dispatch, not as a new top-level frame type (which
  would itself be a rule-3 wire-protocol change this task does not need).
  (2) E06-T07's own control-body framing (`prekey_exchange.dart`'s
  `_ControlBody`, tag bytes 1/2/3 for request/response/unavailable) has no
  spare leading byte reserved for "which feature owns this frame" — two
  independent tag schemes sharing one byte range risks one sub-protocol's
  bytes accidentally parsing as valid input to the other's codec, a
  silent-misrouting risk, not just a `StateError`.

  | Option | How it works | Trade-offs |
  |---|---|---|
  | **(a) Leading `controlKind` byte + keyed dispatch** | Every `PayloadType.control` payload gains one new leading byte (`1`=prekey exchange, `2`=delivery ack) before the existing sub-protocol body. `InboundPipeline._controlHandler` becomes `Map<int, ControlHandler>`; `registerControlHandler(int controlKind, ControlHandler handler)` keys by it; an unrecognized `controlKind` is dropped silently, matching today's malformed-frame behavior. Requires retrofitting E06-T07's already-built (not yet merged at the time this was written) wire format and its own tests to prepend the new byte — a small, mechanical, behavior-preserving change to two of T07's files, which is why they are added to this task's `files:` fence rather than left for a third task. | Smallest actual change; keeps one shared, generic control channel exactly as T02/T05 intended; no new `PayloadType` wire value. Cost: touches two files this task did not otherwise need to touch, and anyone reading T07's diff in isolation will not see this byte's origin — mitigated by this Open Question being the pointer. |
  | **(b) A dedicated `PayloadType.deliveryAck` tag** | Add a fourth `PayloadType` value; `InboundPipeline` dispatches straight to a second, independent handler slot keyed by `PayloadType` itself; T07's wire format and files are untouched. | Zero retrofit of T07. But it is a new wire-level enum value (`relay_packet_frame.dart`, already-merged E06-T02 code) for something this task's own §3 already called a "control sub-protocol," which is exactly the generic-channel design T02/T05 built `control` to avoid needing per-feature — the wrong direction for a category `PayloadType.control` exists to hold, and it does not generalize: a THIRD future control sub-protocol repeats this exact question a second time. |
  | **(c) `DeliveryAckService` wraps `PrekeyExchange`'s handler** | `messaging_stack.dart` registers one combined dispatcher that tries `PrekeyExchange`'s codec first, falls back to `DeliveryAck`'s on failure. | No wire-format change at all. But it is exactly the "try each codec until one doesn't throw" hazard named above — a `DeliveryAck` frame whose bytes happen to parse as a well-formed (if nonsensical) `_ControlBody` would silently misroute into prekey-exchange logic instead of failing loudly. Rejected: correctness risk, not just inelegance. |

  - **Advisory recommendation: (a).** It is the smallest change, requires no
    new wire-level `PayloadType`, and keeps `control` the single generic
    channel the original design intended — the retrofit cost to two of
    T07's files is real but mechanical (prepend one byte on send, read and
    strip it on receive; T07's own `_ControlBody` tag scheme, trust gate,
    coalescing and provenance logic are all unchanged) and is exactly the
    kind of forward-owned obligation `L-process-006` says should be named
    on the task that triggers it, not discovered as a runtime crash when
    T08 is built.
  - **Status:** 🟢 answered
  - **Answer:** **Option (a).** `controlKind` byte added: `1` = prekey
    exchange (retrofit of E06-T07's existing format), `2` = delivery ack
    (this task, when built). `InboundPipeline.registerControlHandler`
    signature changes to accept the `controlKind` it is registering for.
    This task's builder must re-run E06-T07's full existing test suite
    (`test/core/crypto/prekey_bundle_codec_test.dart`,
    `test/core/messaging/prekey_exchange_test.dart`) after the retrofit and
    confirm it is still fully green with unchanged behavior — the retrofit
    must be provably behavior-preserving for T07, not just for T08's own
    new tests.
  - **Answered by:** orchestrator, applying its own advisory
    recommendation, per the user's explicit standing instruction to
    proceed on recommendation without waiting ("don't wait for me... if
    anything needed to ask me, and you have recommendation, go for it. I
    will check at the end.", 2026-08-29). Flagged for the human's
    end-of-run review, same as OQ-E06-T06-4.
  - **Date:** 2026-08-30

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- **Worktree/branch:** `.claude/worktrees/epic_06_task_08`, branch
  `epic_06_task_08` off `epic_06` @ `493128d` (T07/B01/tracker all merged;
  confirmed as the base, not `development`).
- **Commits:** `fb3ecc3` (failing tests, EARS-MSG-7/8/9 + codec, written and
  confirmed red — compile failure on `deliveryAckService` not existing —
  before any production code), `b1c88a6` (DeliveryAckService + DeliveryAck
  codec + the `InboundPipeline`/`PrekeyExchange` controlKind retrofit +
  `messaging_stack.dart` wiring + the `_sendAck` failure-swallowing fix
  found while running the full suite).
- **Test counts:** `test/core/messaging/delivery_ack_test.dart`: **8 new
  tests, all green** —
  - `test_delivery_ack_codec_round_trips_every_state`
  - `test_delivery_ack_codec_rejects_malformed_matrix`
  - `test_EARS_MSG_8_foreign_ack_is_rejected_unknown_message_id`
  - `test_EARS_MSG_8_foreign_ack_is_rejected_wrong_peer_and_third_device`
  - `test_EARS_MSG_9_out_of_order_acks_do_not_regress_state`
  - `test_read_acks_are_not_emitted_while_disabled`
  - `test_EARS_MSG_7_sender_sees_delivered_after_receiver_stores`
  - `test_EARS_MSG_7_duplicate_delivery_does_not_re_ack`

  Full suite (`flutter test`): **337 passed, 0 failed** (up from T07's own
  reported 321 + this task's 8 new tests + 8 more from tasks merged to
  `epic_06` between T07 and this task's base commit). `flutter analyze`
  (whole project): **No issues found!**

- **EARS -> test mapping:**
  - EARS-MSG-7 (sender observes `Sent` -> `Delivered` once the recipient
    persists) -> `test_EARS_MSG_7_sender_sees_delivered_after_receiver_stores`
    (two real `MessagingStack`s, a real `SendMessageUseCase.call`, the wire
    frame delivered onto B's real `InboundPipeline`, B's real
    `DeliveryAckService` sending real `accepted`+`delivered` control frames
    back over a real transport loopback into A's real `InboundPipeline` —
    asserts A's `messages` row reaches `delivered` AND both `delivery_states`
    rows exist) and
    `test_EARS_MSG_7_duplicate_delivery_does_not_re_ack` (the identical wire
    packet delivered to B twice -> `b.inbound.counters.duplicate == 1`,
    `b.deliveryAckService.counters.sent == 2` not 4 — proves "never for a
    duplicate" end to end, not merely by reading the `inbound.delivered`
    stream's own contract).
  - EARS-MSG-8 (foreign/unknown ack ignored, no state change) ->
    `test_EARS_MSG_8_foreign_ack_is_rejected_unknown_message_id`
    (`rejectedUnknownMessage == 1`) and
    `test_EARS_MSG_8_foreign_ack_is_rejected_wrong_peer_and_third_device`
    (both the wrong-peer-for-my-own-message case AND the
    third-device-authored-message case -> `rejectedForeign == 2`, both
    messages' `deliveryState` columns provably unchanged).
  - EARS-MSG-9 (out-of-order acks never regress state) ->
    `test_EARS_MSG_9_out_of_order_acks_do_not_regress_state` — `delivered`
    (a 2-hop jump from `sent`) applied first, then a late/stale `accepted`
    -> `counters.accepted == 1`, `counters.staleIgnored == 1`, final state
    `delivered`, no exception. This test is also what caught a real bug in
    the first version of `handleControlFrame`: a single
    `DeliveryStateMachine.canTransition(current, target)` check incorrectly
    rejects `sent -> delivered` as illegal (it is 2 hops, not 1) —
    confirmed red for that reason (`Actual: 'accepted'`, not `'delivered'`)
    before the fix (walking the happy-path order one legal
    `DeliveryStateMachine`-validated hop at a time), confirmed green after.
  - `test_read_acks_are_not_emitted_while_disabled` — zero
    `TransportApi.send` calls recorded on the mocked platform channel after
    calling `markRead`, asserted at the transport level per task file §9's
    own requirement, not by reading `kReadReceiptsEnabled`'s value.
  - `test_delivery_ack_codec_round_trips_every_state` /
    `..._rejects_malformed_matrix` — round trip for all three
    `DeliveryAckState` values; malformed matrix covers an empty buffer, an
    unknown state tag, a truncated length prefix, a length prefix claiming
    more bytes than remain, a truncated `observedAtMs`, and trailing
    garbage after an otherwise-valid buffer — all throw `FormatException`,
    never a partially-built `DeliveryAck`.

- **`OQ-E06-T08-2` retrofit — T07's own suite re-verified unchanged.**
  ```
  $ flutter test test/core/crypto/prekey_bundle_codec_test.dart test/core/messaging/prekey_exchange_test.dart
  ...
  00:02 +15: All tests passed!
  ```
  15/15 green (8 codec + 7 exchange), matching T07's own reported count
  exactly. `handleControlFrame`'s own body is byte-for-byte unchanged;
  `test_unsolicited_bundle_response_is_dropped` (which calls
  `handleControlFrame` directly with a hand-built, un-prefixed
  `_ControlBody` payload, bypassing `InboundPipeline` entirely) is the
  strongest evidence this retrofit is behavior-preserving for T07: it never
  goes through the new `controlKind`-stripping code path at all, and still
  passes because `handleControlFrame` never changed.

- **Required greps** (run from the worktree root):
  ```
  $ grep -rn "deliveryState\s*=" lib/core/messaging/
  (no matches — zero raw writes)

  $ grep -rn "kReadReceiptsEnabled" lib/core/messaging/
  lib/core/messaging/delivery_ack.dart:103:// [kReadReceiptsEnabled] is the single named constant [markRead] gates on —
  lib/core/messaging/delivery_ack.dart:139:const bool kReadReceiptsEnabled = false;
  lib/core/messaging/delivery_ack.dart:350:  /// no-op while `OQ-E06-T08-1` is open (this file's header/[kReadReceiptsEnabled]).
  lib/core/messaging/delivery_ack.dart:353:    if (!kReadReceiptsEnabled) return;

  $ grep -n "print(\|log\.\|debugPrint" lib/core/messaging/delivery_ack.dart
  (no matches)
  ```

- **A bug found and fixed mid-task** (see §9 Deviation 3): the first version
  of `DeliveryAckService._sendAck` let `transport.send`'s exception
  propagate uncaught out of the `unawaited(...)` future
  `messaging_stack.dart`'s own `inbound.delivered` subscription starts —
  this only surfaced when running the FULL suite (not this task's own test
  file in isolation), as a failure in an out-of-fence test
  (`messaging_coordinator_test.dart: test_start_calls_inbound_pipeline_start`)
  whose receiver stack had never needed its own outbound send mock before
  this task made ack-sending automatic. Fixed by wrapping the send in
  `_sendAck`'s own `try/catch`, consistent with task file §4's documented
  "a lost ack is not an error" contract. Full suite green (337/337) after
  the fix; re-ran in isolation and in the full run to confirm no other
  regression.

- **Manual smoke test (§8, item 1):** not run in this session — it depends
  on T11 for the visual and is explicitly a 🧍 human step (task file §8),
  same as T06/T07's own deferred manual steps.

### Independent review -- 2026-08-30 (opus, != `executed_by`)

**Verdict: APPROVE.** Every material claim re-verified by the reviewer, not
read off the builder's transcript.

- **Suite, re-run by the reviewer** in this worktree: `flutter test` ->
  **+337, All tests passed**; `flutter analyze` -> **No issues found!**
  Both numbers match the builder's report exactly.
- **`OQ-E06-T08-2` retrofit is behaviour-preserving for T07 -- re-run
  independently:** `flutter test test/core/crypto/prekey_bundle_codec_test.dart
  test/core/messaging/prekey_exchange_test.dart` -> **+15, All tests passed**,
  the same count T07's own Run log records. `git diff epic_06...HEAD --
  test/core/messaging/prekey_exchange_test.dart` is **empty** -- T07's tests
  pass byte-identical, so the retrofit changed no T07 test expectation to
  make itself green.
- **Falsification of the retrofit (reviewer's own mutation, restored
  verbatim):** removing the strip at `lib/core/messaging/inbound_pipeline.dart:334`
  (`payload: frame.payload.sublist(1)` -> `payload: frame.payload`) fails
  **both** suites -- `prekey_exchange_test.dart` loses 4 tests
  (`test_EARS_COMM_14_first_message_establishes_a_session`,
  `..._concurrent_ensure_session_uses_one_bundle`,
  `test_EARS_COMM_15_blocked_peer_gets_no_bundle`,
  `test_EARS_COMM_16_changed_identity_key_fails_loudly`) and
  `test_EARS_MSG_7_sender_sees_delivered_after_receiver_stores` fails with
  `Expected: 'delivered' Actual: 'sent'`. So the prepend/strip symmetry is
  genuinely covered end-to-end for **both** `controlKind` values, not only
  T08's. This was the single highest-risk claim in the task; it holds.
- **EARS-MSG-8 ownership is a real control, falsified:** mutating
  `lib/core/messaging/delivery_ack.dart:398-399` to drop the
  `row.conversationId != frame.source` half makes
  `test_EARS_MSG_8_foreign_ack_is_rejected_wrong_peer_and_third_device` fail
  (`rejectedForeign` Expected `<2>` Actual `<1>`) -- the wrong-peer ack gets
  through. Not a tautology; the test asserts the peer binding specifically,
  not just "some rejection happened". Restored, green.
- **EARS-MSG-9 falsified:** neutering the guard at `delivery_ack.dart:422`
  fails `test_EARS_MSG_9_out_of_order_acks_do_not_regress_state`
  (`counters.accepted` Expected `<1>` Actual `<2>`). Note for the record:
  **no exception was thrown even with the guard removed** -- the forward-only
  walk loop at `delivery_ack.dart:436` is a second, independent barrier, so
  "never regresses" is structurally guaranteed and "never throws" is proven
  twice over. Restored, green.
- **Read receipts are off at the transport, not the UI:** flipping
  `kReadReceiptsEnabled` (`delivery_ack.dart:139`) to `true` fails
  `test_read_acks_are_not_emitted_while_disabled` with `sendCallCount`
  Expected `<0>` Actual `<1>` -- the assertion is on the mocked
  `TransportApi.send` Pigeon channel, i.e. actual bytes on the wire.
  Restored, green.
- **Reviewer's own probe (one the builder never saw):** an ack with
  `state: read` from the correct peer against a `deliveryState: failed`
  message -- the message stays `failed`, `staleIgnored == 1`,
  `accepted == 0`. A hostile or confused peer cannot resurrect a failed
  message. (Probe file deleted after the run; worktree clean.)
- **Every delivery-state write goes through `DeliveryStateMachine`:**
  `grep -rn "deliveryState\s*=" lib/` returns only generated Drift code
  (`database.g.dart`), the two column declarations, and one `==` comparison
  in `message.dart:85` -- **zero raw writes in hand-written code**. The one
  DB write in the new file (`delivery_ack.dart:455-457`) persists
  `message.deliveryState.name`, and that value can only have come from
  `Message.withDeliveryState` (`delivery_ack.dart:452`), which itself calls
  `DeliveryStateMachine.transition`.
- **No content in acks or logs:** the wire body is
  `[u8 stateTag][u32 len][messageId][u64 observedAtMs]`
  (`delivery_ack.dart:195-219`) -- no ciphertext, no plaintext, no key
  material. `grep -niE "print\(|debugPrint|log\(|logger"
  lib/core/messaging/delivery_ack.dart` -> no matches. Acks are also never
  themselves acked: the control branch of `_handleBuffer` returns
  (`inbound_pipeline.dart:341`) before the ciphertext branch that feeds
  `inbound.delivered`, so no ack storm (NFR-BATT-001).
- **Scope:** `git diff --stat epic_06...HEAD` = `delivery_ack.dart` (new),
  `delivery_ack_test.dart` (new), `inbound_pipeline.dart`,
  `messaging_stack.dart`, `prekey_exchange.dart`, plus this task file.
  Exactly the `files:` fence (incl. the two retrofitted T07 files
  `OQ-E06-T08-2` added to it); `prekey_exchange_test.dart` was in the fence
  but needed no edit, which is a permission not an obligation. Section 4
  respected: `delivery_state_machine.dart`, `ReceiveMessageUseCase`,
  `SendMessageUseCase`, `messaging_coordinator.dart` and the schema are all
  untouched. Deviations 1-3 read as honest and in-contract; deviation 3
  (swallowing a `transport.send` failure) is consistent with section 4's
  "a lost ack is not an error" and correctly does **not** increment
  `counters.sent` on failure, so the counters stay truthful.
- **Trace gate:** `OQ-E06-T08-2` is answered; `OQ-E06-T08-1` is open but
  explicitly *non-blocking* (the mechanism ships either way) -- not a
  blocking question against `traces_to`, so the gate is clear.
- **Design gate:** n/a (`design_contract: n/a`; this task renders nothing).

**Non-blocking observations -- for the epic bug sweep, not for this task:**
1. **`frame.source` is unauthenticated at this layer (S3, epic-level, NOT a
   T08 defect).** `InboundPipeline._onConnectionStateChanged` calls
   `_handleBuffer(bytes)` (`inbound_pipeline.dart:252-254`) and deliberately
   discards the transport peer id, because in a multi-hop mesh a relayed
   frame legitimately arrives from a different hop than its `source`.
   Control frames are unsigned (inherited from E06-T02/T05). So the
   ownership check binds an ack to the *claimed* peer id: a hostile mesh
   node that knows a peer device id and a message id can advance that
   message's tick marks. Blast radius is a wrong tick -- no content, no key
   material, no state regression. This is EARS-MSG-8 implemented **exactly
   as written**, and it cannot be fixed inside T08's fence (or at the
   transport-peer level at all); the real fix is signed control frames,
   which is a spec/epic question. Recommend it be raised as an OQ or bug
   task at the E06 sweep.
2. **Ordinal coupling.** The monotonicity comparison at
   `delivery_ack.dart:422/436` depends on `DeliveryState`'s *declaration
   order* (and on `failed` being declared last, which is why the reviewer's
   failed-resurrection probe passes). The file documents this at
   `delivery_ack.dart:439-446`, but reordering that enum would silently
   change ack semantics. Worth a lesson (`skills/retro`) rather than a
   change here.
