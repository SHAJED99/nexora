---
id: E11-B06
epic: E11
type: bug
title: "The public device directory (E11-T06) has a first-writer-wins ownership race that reopens the impersonation risk ADR-0008 claims to close (all three findings resolved, see Run log)"
layer: infra
size: M
status: done
owner_agent: planner
model: any
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
severity: S2
priority: { moscow: should, p: P3 }
depends_on: [E11-T06, E11-B01]
blocks: []
traces_to: [FR-FB-001, NFR-PRIV-001, ADR-0008]
required_context:
  - agent/memory/decisions/ADR-0008-firebase-cross-account-visibility.md
  - epics/E11-firebase-sync/tasks/E11-T06.md
  - epics/E11-firebase-sync/tasks/E11-B01.md
  - database.rules.json
  - lib/core/services/device_directory_service.dart
found_by: reviewer (independent, cross-model, claude-opus-5) during a retroactive rule-5 re-review of E11-T02, extended to `directory` since that reviewer owns the whole rules file
found_in: "the write rule at directory/$deviceId -- (!data.exists() || data.child('ownerUid').val() === auth.uid) is first-writer-wins with no binding between the device id and the identity being published under it."
files:
  create:
    - lib/core/crypto/identity_key_hex.dart
    - test/core/crypto/identity_key_hex_test.dart
  update:
    - database.rules.json
    - docs/firebase-schema.md
    - lib/core/services/firebase_paths.dart
    - lib/core/services/firebase_boundary.dart
    - lib/core/services/device_directory_service.dart
    - lib/features/login/presentation/login_controller.dart
    - test/core/services/firebase_rules_test.dart
    - test/core/services/device_directory_service_test.dart
    - test/core/observability/observability_service_test.dart
    - test/features/login/presentation/login_controller_test.dart
    - agent/memory/decisions/ADR-0008-firebase-cross-account-visibility.md
  delete: []
---
# E11-B06 · directory-entry squatting and cross-account `ownerUid` readability

## The defect

Two findings on `directory/$deviceId`, both distinct from `E11-B05`
(which was about `users/**`'s missing container-level `.validate`).

**1. Directory-entry squatting (S2).** `database.rules.json`'s write rule:

```
".write": "auth != null && newData.child('ownerUid').val() === auth.uid
           && (!data.exists() || data.child('ownerUid').val() === auth.uid)"
```

is first-writer-wins: whoever publishes to a given `$deviceId` first owns
it forever after (`data.exists()` then requires the SAME uid to write
again). Nothing binds `$deviceId` cryptographically to the
`identityPublicKey`/`prekeyBundle` being published under it.
`ADR-0008` itself notes device ids "leak wherever they are shared" —
i.e. to every mesh peer a device has ever contacted. Any authenticated
user who learns a device id can publish their OWN identity key under it
before the real owner ever does, and the true owner is then permanently
locked out. `E11-B02`'s fix (binding `identityPublicKey` to the key
embedded in `prekeyBundle`) only proves internal self-consistency — a
squatter's entry is perfectly self-consistent and passes that check.
Consumers doing `lookupDevice` on that id get an attacker's identity key
for a victim's device, which is exactly the impersonation `ADR-0008`'s
option 2 was chosen to close.

**2. Cross-account `ownerUid` readability (S3).** `directory/$deviceId`'s
`.read": "auth != null"` grants read on the WHOLE node, `ownerUid`
included. Any authenticated user holding two device ids can read both
entries' `ownerUid` and learn whether they belong to the same Firebase
account — device-linkage/account-correlation, a distinct privacy surface
`ADR-0008`'s cost analysis (`"one public key + revocation flag per
device id"`) never priced in. `ownerUid` is a write-ownership marker;
nothing in `lookupDevice` reads it back.

**3. No unpublish path (S4, likely intentional, undocumented).** The
write rule denies deletion (on delete, `newData.child('ownerUid').val()`
is null, never equal to `auth.uid`) — only a `revokedAt` update is
possible. Flagged so it is a documented decision, not an oversight.

## Why this is not currently exploitable

Per `E11-B01` (already filed, `won't-fix-in-E11`): `DeviceDirectoryService`
is never constructed anywhere in `lib/` — nothing in the running app
publishes to or reads from `directory/` in production yet. All three
findings here require a real caller to exist before they mean anything
concrete. `E11-B01`'s own resolution deferred exactly this kind of
consequence to "the first real consumer (E07 TOFU replacement or E06-T07
prekey fallback)" — this bug is that deferred consequence, named now so
it has a reader when that consumer gets built, rather than discovered
again from scratch.

## Severity

**S2** for finding 1 (a real identity-impersonation vector once wired,
matching the class of exploit `ADR-0008` was written to prevent) · **S3**
for finding 2 (a real but narrower privacy leak, cross-account
correlation not cross-account content access) · **S4** for finding 3
(a plausible intentional gap, needs a documented answer not a fix).

## Fix direction (for whoever wires the first consumer, not this bug)

- **Finding 1** needs an actual binding between `$deviceId` and the
  identity being published — e.g. requiring `$deviceId` to be derived
  from (or signed by) the identity key itself, so squatting a specific
  id without controlling its corresponding private key becomes
  infeasible. This is close to `ADR-0008`'s own territory (a protocol
  design choice, not a rules tweak) and should go back through rule 3 if
  it isn't already covered by the ADR's existing text.
- **Finding 2** is fixable within the rules file alone: move `ownerUid`
  to a separate, owner-only-readable node (e.g. `directory_private/
  $deviceId/ownerUid`), written in the same client-side call as the
  public entry, with `.read` restricted to `auth.uid === $deviceId`'s
  owner. `lookupDevice`'s current callers don't read `ownerUid` at all,
  so this is additive, not a breaking schema change to existing readers.
- **Finding 3**: a human decision — either document "revoke, don't
  delete" as final, or add an owner-only delete path if unpublishing is
  actually wanted.

## What this bug does NOT do

- **Finding 1 is now fixed (round 4, 2026-09-07)** — see the Run log's
  round-4 entry for the implementation, and `ADR-0008`'s addendum for the
  corrected mechanism (no bootstrap reshape was actually needed; the
  round-3 entry's "E01/E03 reshape" premise was wrong, corrected in place
  rather than deleted).
- Does **not** revisit `ADR-0008`'s core decision (public directory,
  option 2) — these are gaps within that decision's implementation, not
  a challenge to the decision itself.
- Does **not** claim `E11-B02`'s fix (identity/prekey self-consistency)
  is wrong — it does exactly what it says; this bug documents what it
  was never meant to catch.
- The finding-2/3 fix does **not** touch `lookupDevice` or any of its
  future consumers (`E07`, `E06-T07`) — `ownerUid` was never read by
  `lookupDevice`, so moving it to its own node changes nothing about
  what a reader sees.

## Feedback log
- 2026-09-04 — Filed during `E09-B05`-pattern retroactive rule-5
  re-reviews of E10/E11's first two tasks (this session).
- 2026-09-05 — Priority stamped at the `bug_priorities` gate under the
  decision authority explicitly delegated to the agent for this session:
  **P3, matching `E11-B01`'s own precedent** for a deferred-consequence
  finding unreachable without a real `DeviceDirectoryService` caller. Not
  P2: nothing is exploitable today, and forcing a fix now would mean
  guessing at finding 1's actual resolution (a protocol-level binding
  between `$deviceId` and the published identity) ahead of the rule-3
  architecture call that fix genuinely needs — the priority stamp
  schedules *when* this gets attention, not *what* the fix looks like,
  which stays open below. Severity is the reviewer's and is **S2**.

## Run log
- 2026-09-05 — **Findings 2 and 3 resolved** on branch `epic_11_bug_06` off
  `origin/development`, following a human decision on both open questions
  (recorded in `ADR-0008`'s 2026-09-05 addendum) plus the newly-decided
  direction for finding 1 (not yet implemented, see below).
  - **Finding 2** (cross-account `ownerUid` readability): moved `ownerUid`
    to its own top-level node, `directory_private/$deviceId/ownerUid`,
    `.read`-restricted to the caller whose `auth.uid` already matches the
    stored value. `directory/$deviceId`'s own `.write` rule now reads
    this node (see round 2 below for the exact expression -- the first
    draft here was itself broken) to authorize a write, replacing its
    previous self-contained `ownerUid` field check. Both nodes are
    written together as one atomic multi-location `update()`
    (`DeviceDirectoryService.writeDirectoryData`) — required for the rule
    to see the sibling write at all, not merely for crash-safety (round 2
    below); a crash between them also cannot leave a
    public entry with no ownership marker. `firebase_rules_test.dart`
    gained `test_EARS_FB_20_directory_private_rules` (mirrors the removed
    ownerUid-immutability assertions, re-targeted at the new node) plus a
    fix to two pre-existing tests whose `startsWith('/directory')` filter
    would have wrongly swept up the new `/directory_private/...` paths
    too (a latent test bug this change exposed, not introduced by it).
  - **Finding 3** (no unpublish path): confirmed final by the human --
    documented in `ADR-0008`'s addendum. Originally recorded here as "no
    code change (the rule already denied deletion)" -- **corrected at
    round 3 review**: finding 2's own refactor had silently dropped this
    property as an unreviewed side effect (see round 3 below); it required
    an explicit one-clause fix to restore, not zero code change.
  - **Finding 1** (directory-entry squatting) is **not fixed** --
    direction chosen (derive `$deviceId` from the identity key), but RTDB
    rules have no hash/signature primitive to enforce it, so real
    enforcement needs a Cloud Function (new dependency) or a bootstrap-
    sequencing change spanning `E01`/`E03` (`ADR-0005`'s device-id-before-
    identity-key ordering). Recorded in `ADR-0008`'s addendum as a chosen
    direction, not a completed fix. `status` stays `blocked` on this
    finding alone.
  - Verification: `flutter test test/core/services/firebase_rules_test.dart
    test/core/services/device_directory_service_test.dart
    test/core/services/firebase_boundary_test.dart` — 56/56 green. Full
    suite: 1144/1144. `flutter analyze`: clean (one pre-existing,
    unrelated lint note).
- 2026-09-05 (round 2) — **Cross-model review (Opus) found the round-1
  fix's `.write` expression -- `root.child('directory_private/'+$deviceId+
  '/ownerUid').val() === auth.uid` -- was itself broken**: `root` in a
  Realtime Database multi-location `update()` reflects the PRE-write
  snapshot, never a sibling path written in the SAME update, so a brand
  new device's very first publish (when `directory_private/$deviceId/
  ownerUid` does not exist yet) would be denied FOREVER --
  `DeviceDirectoryService.publish` swallows the resulting
  `PERMISSION_DENIED` (best-effort, catch-and-log), so this would have
  failed silently in production. The reviewer verified this against a
  real Firebase RTDB emulator (`@firebase/rules-unit-testing`), not by
  reasoning alone, and proposed the Firebase-documented remedy:
  `newData.parent().parent().child('directory_private').child($deviceId)
  .child('ownerUid')` (walking up from the rule's own location through
  the merged multi-location update to reach the same sibling).
  - **I independently re-verified the proposed remedy** (not merely
    applying it on the reviewer's word) using the same emulator tooling:
    a 6-case test suite (fresh-device first publish; a second account's
    squat attempt on an existing entry, denied; the true owner's own
    re-publish/rotation, still succeeds; an unauthenticated write, denied;
    the private node's own immutability in isolation; reading `ownerUid`
    as a different account, denied) — all 6 pass against the exact
    `database.rules.json` content in this worktree, on a real emulator, not
    the JSON-structure-only checks `firebase_rules_test.dart` itself is
    limited to (its own header already documents this limitation).
  - Applied the fix: `database.rules.json`'s `.write` expression,
    `firebase_rules_test.dart`'s two exact-string assertions for it,
    `docs/firebase-schema.md`'s prose, and
    `device_directory_service.dart`'s doc comment (which had overstated
    the atomicity requirement as "crash-safety" when it is actually
    REQUIRED for the rule to see the sibling write at all, not merely
    safer under a crash).
  - **Second reviewer finding, recorded not fixed**: this fix makes
    finding 1's squatting attack CHEAPER, not just unchanged. Before this
    fix, squatting `directory/$deviceId` required a complete,
    self-consistent public entry (passing `E11-B02`'s identity/prekey
    binding check). Now, an attacker needs only a single string write to
    `directory_private/$deviceId/ownerUid` (permitted as any first write,
    per that node's own first-writer-wins rule) to permanently lock out
    the true owner — the public entry is irrelevant to the squat itself.
    This does not change `status: blocked` on finding 1 (the fix is still
    a protocol-level `$deviceId`-to-identity binding, per the ADR
    addendum), but the addendum below now records that this fix migrated
    and cheapened finding 1's attack rather than leaving it unchanged.
  - Re-verification: same 3-file suite — 56/56 green (assertions updated
    for the new expression string). Full suite: 1144/1144. `flutter
    analyze`: clean.
- 2026-09-05 (round 3) — **Cross-model review (Opus) reproduced round 2's
  emulator claims independently from scratch** (own 24-case suite, own
  test harness, byte-identical rules file confirmed via md5) and confirmed
  the `newData.parent().parent()` fix genuinely works at this exact
  depth/multi-path shape -- but found **a new regression neither prior
  round caught**: the refactor's ownership check no longer reads `newData`
  at `directory/$deviceId`'s own value at all, so on a DELETE (where
  `newData` there is null/absent), the check was vacuously satisfiable and
  the owner's own `remove()`/`update({path: null})` started succeeding --
  silently reopening finding 3's "revoke, don't delete, permanent" human
  decision as an unreviewed side effect. Non-owner delete stayed denied
  (not a cross-account hole), but three documents in the same round-2
  commit (`ADR-0008`'s addendum, this file, `docs/firebase-schema.md`)
  asserted the opposite ("no code change, already denied deletion") --
  a decision-integrity failure the reviewer specifically flagged as
  worse than a plain missed bug, since the docs actively said the wrong
  thing.
  - Fix (verified by the reviewer, then independently re-verified by me
    before applying, per the same falsify-before-trust discipline as
    rounds 1/2): added `newData.exists() &&` to the `.write` expression.
    Re-ran my own 6-case emulator suite from round 2 PLUS two new
    delete-denial cases (`remove()` and multi-location
    `update({path: null})`) against the updated rules file -- 8/8 pass.
  - Updated: `database.rules.json`'s `.write` expression;
    `firebase_rules_test.dart`'s exact-string assertion (now includes
    `newData.exists() &&`) plus a NEW dedicated test asserting that
    clause's presence specifically (the reviewer's own note: "no test
    guards this" -- true before this commit); corrected the three docs'
    false "no code change" claims to describe what actually happened.
  - Re-verification: same 3-file suite — 57/57 green (+1 for the new
    dedicated delete-denial-guard test). Full suite: 1145/1145 (+1 from
    the same new test). `flutter analyze`: clean.
- 2026-09-05 (round 4, final) — **Cross-model review (Opus) APPROVE**,
  with its own from-scratch 39-assertion emulator harness (not a reuse of
  any prior round's script) covering every required property plus an
  active hunt for a fourth hidden one. Confirmed: no regression on fresh
  publish, owner rotation, or squat-denial; the delete-denial fix holds
  for `remove()`, `set(null)`, AND `update({path: null})`; falsified both
  the emulator probe (guard removed → 34/39, deletes start succeeding)
  and the Dart test (guard removed from the rules file → `firebase_rules_test.dart`
  fails `+31 -2`, the exact string and the dedicated test both catching
  it). Independently re-derived the historical claim about the pre-B06
  rule (checked out `f8813ce~1`, probed it directly) rather than trusting
  this file's own account of it. Found one MORE delete path this fix
  closes that nobody had enumerated — collapsing the node to empty by
  nulling every field individually (`update({identityPublicKey: null,
  prekeyBundle: null})`) — confirmed denied now, was allowed before.
  `revokedAt` writes (the one operation "revoke, don't delete" actually
  depends on) confirmed unaffected. Read behavior confirmed unchanged (a
  `.write`-only diff, correctly).
  - Two carried-forward, non-blocking notes for the epic tracker rather
    than this bug: (a) an owner can still un-revoke by deleting only
    `revokedAt` while the node itself survives — allowed both before and
    after this fix, `ADR-0008` says revoked entries aren't removed but
    does not say revocation is one-way, a deliberate-or-not question for
    whoever wires the first real consumer; (b) the reviewer's own
    retro candidate above (emulator-backed checks for any future
    cross-path/`newData` rules change) is endorsed, not just proposed.
  - Ready to merge.
  - Process note (the reviewer's own, recorded here rather than silently
    dropped): this `.write` expression has carried three distinct
    properties across three rounds -- ownership, first-publish-through-
    merged-root visibility, and delete-denial -- and only the Dart
    JSON-structure tests guard any of them mechanically; the other two
    were only ever caught by an actual emulator. Filed as a retro
    candidate: `database.rules.json` changes touching cross-path/`newData`
    semantics should get an emulator-backed check before merge, not only
    a JSON-structure one, given this exact line has now hidden two
    separate real defects behind "looks right."

- 2026-09-07 — **Finding 1's implementation mechanism decided: bootstrap-
  sequencing change (E01/E03), not a Cloud Function.** This project has
  zero server-side compute today — no Cloud Function, no backend service
  of any kind (confirmed: no `functions/` directory, no
  `firebase-functions` reference anywhere in `agent/memory/decisions/`) —
  ADR-0002/ADR-0005 both keep this architecture client-heavy by design
  (RTDB rules do the enforcement Firebase-side; `database.rules.json` is
  already this project's one and only server-side authorization
  surface). Introducing a Cloud Function to enforce the `$deviceId` ⟷
  identity-key binding would be a genuinely new infrastructure category —
  its own deployment/hosting surface, cold-start latency on every
  directory write, and ongoing operational cost — for a binding that a
  bootstrap-sequencing reshape (deriving `$deviceId` from the identity
  key at the point the identity is first established, in E01/E03, before
  either is ever published to `directory/`) can make client-side-
  enforceable instead, consistent with how every other integrity property
  in this schema is enforced (`database.rules.json` alone, no server
  logic). This is still a foundational reshape of E01/E03's own bootstrap
  order, not a small patch — it needs its own task-sharding pass (files:,
  functions:, EARS criteria) before it is dispatchable, which has NOT
  happened yet; this entry records the chosen mechanism, not a completed
  fix. `status` stays `blocked` pending that sharding pass.
  **Corrected below (round 4, same day) — the "bootstrap reshape" premise
  in this entry was wrong; kept here verbatim as the historical record of
  that mistake, not edited away.**

- 2026-09-07 (round 4) — **Finding 1 implemented — the "bootstrap-
  sequencing reshape" above turned out to be unnecessary.** Before
  writing a single line, re-read `lib/app/main.dart` directly instead of
  trusting this file's own prior claim, and found `MessagingStack.create()`
  already runs BEFORE `runApp()` — before any screen, including login,
  is ever shown — and `IdentityService.ensureLocalIdentity()` runs
  UNCONDITIONALLY inside it, regardless of whether `selfDeviceId` is
  known yet (confirmed by reading `messaging_stack.dart:536-559`: the
  identity/crypto try-block runs and only THEN, separately, does an empty
  `selfDeviceId` downgrade `status` to `unavailable` — the identity
  keypair generation itself never depended on that check). So this
  device's identity keypair is genuinely already available at the exact
  moment `LoginController._signIn()` runs, on every launch including the
  very first one. No E01/E03 reshape was needed — only plumbing.
  - **Files actually touched** (all 10 match `files:` above, no
    additions): `lib/core/crypto/identity_key_hex.dart` (new — hex
    encode/decode for identity key bytes, replacing base64 specifically
    for `identityPublicKey`, since a standard-base64 string can contain
    `/`, which the Firebase SDK's `.child(path)` always treats as a path
    separator — hex never does); `lib/core/services/
    device_directory_service.dart` (`_publish`/`lookupDevice` now use hex
    for `identityPublicKey`; `prekeyBundle` untouched, still base64);
    `lib/features/login/presentation/login_controller.dart`
    (`_deriveDeviceIdFromLocalIdentity`, called only when no
    `existingDeviceId` exists — a genuine first-ever sign-in — resolving
    the app-wide `MessagingStack` via `Get.find`, falling back to the
    pre-existing `generateSecureDeviceId()` on any failure, additive and
    never a new way to fail sign-in); `database.rules.json`
    (`directory/$deviceId`'s `.write` rule gained
    `&& newData.child('identityPublicKey').val() === $deviceId`);
    `docs/firebase-schema.md` (documents both the encoding change and the
    new rule clause); five test files updated for the new hex convention
    (`device_directory_service_test.dart`,
    `observability_service_test.dart`, `firebase_rules_test.dart`'s own
    exact-string write-rule assertion) plus two new test files
    (`identity_key_hex_test.dart`, and new tests in
    `login_controller_test.dart`).
  - **A real bug caught by my own tests before it ever shipped**: the
    first `hexDecodeBytes` implementation used
    `int.tryParse(pair, radix: 16)` per byte-pair, which leniently accepts
    a leading `+`/`-` sign (`int.tryParse('+1', radix: 16) == 1` in Dart)
    — a malformed string like `'ab+1'` would have silently decoded as two
    real bytes instead of being rejected. Since this function guards
    attacker-influenced remote data (`lookupDevice`'s `identityPublicKey`
    field), this was a real correctness/security gap, not a style nit.
    Caught by `identity_key_hex_test.dart`'s own
    `test_hexDecodeBytes_rejects_malformed_input` group; fixed by
    validating each byte-pair against a strict `^[0-9a-fA-F]{2}$` regex
    before parsing, never relying on `int.tryParse`'s sign-prefix
    leniency.
  - **A second, more consequential bug caught before it shipped**: the
    first `_deriveDeviceIdFromLocalIdentity` implementation gated on
    `messagingStack.status.isReady`. Writing the regression test that
    mirrors `main.dart`'s own REAL first-launch construction
    (`MessagingStack.create(selfDeviceId: '')`) proved `status.isReady` is
    ALWAYS `false` in exactly the one case this method exists for — a
    genuinely fresh install has no `selfDeviceId` yet, which is precisely
    what makes `MessagingStackStatus` report `unavailable`
    (`messaging_stack.dart:554-559`). Gating on that flag would have made
    the entire derivation dead code in production — it would never once
    have fired outside a test, silently falling back to
    `generateSecureDeviceId()` on every real first sign-in while still
    passing any test that (wrongly) constructed a `MessagingStack` with a
    non-empty `selfDeviceId`. Fixed by dropping the `isReady` check
    entirely and relying solely on the `try`/`catch` around the actual
    identity-keypair read, which is the correct and sufficient guard.
    `test_E11_B06_first_sign_in_derives_device_id_from_local_identity`'s
    first test asserts `messagingStack.status.isReady == false` as an
    explicit sanity check specifically so this can't silently regress
    back.
  - `flutter test`: full suite, 1318/1318 green (1305 baseline + 10 new
    `identity_key_hex_test.dart` tests + 3 new `login_controller_test.dart`
    tests). Three pre-existing tests initially broke from the encoding
    change alone (`device_directory_service_test.dart`,
    `observability_service_test.dart`, `firebase_rules_test.dart`'s
    exact-string write-rule assertion) — all three were fixture/assertion
    updates for the new hex convention, not signs of a real regression;
    fixed and reconfirmed green.
  - `flutter analyze`: 2 issues, both pre-existing/unrelated, matching the
    documented baseline exactly.
  - **What this does NOT yet prove**: the new `database.rules.json`
    clause has not been verified against a live `@firebase/
    rules-unit-testing` emulator (unlike rounds 1-3's ownership-rule
    changes, which were) — this project has no emulator harness wired
    into its committed test suite (`firebase_rules_test.dart`'s own
    header: "still not brought in... a new dev dependency, rule 3").
    `firebase_rules_test.dart`'s structural check confirms the rule
    STRING is exactly what was intended, but not that the RTDB server
    evaluates it as expected. Flagging this explicitly for the reviewer,
    consistent with every prior round's own practice of emulator-verifying
    a live rule change before merge — recommend the reviewer bring up a
    real emulator for this one clause specifically (a handful of
    `set()`/`update()` calls against the new `.write` expression: a
    legitimate first publish where `$deviceId` matches `identityPublicKey`
    succeeds; a squat attempt where they disagree is denied; the
    already-verified ownership/delete-denial clauses from rounds 1-3
    still hold unchanged).

## Review (round 4)

- **Verdict: ✅ APPROVE.**
- `reviewed_by`: `opus` (`claude-opus-5`, fresh reviewer subagent, zero shared
  context with the implementing session, own worktree `wt-E11-B06-review`,
  own from-scratch emulator harness). **Correction to this review's own
  rule-5 disclosure (added by the orchestrator session after the fact):**
  the reviewer's draft of this section claimed round 4's implementation
  "was carried out by the orchestrator session, which also runs on
  opus" — that is factually wrong. Round 4 (this fix) was implemented by
  the orchestrator session running on `claude-sonnet-5`, not `opus`; the
  reviewer had no `executed_by` field in this file to read (this task's
  frontmatter template predates that field) and appears to have
  mis-inferred the implementer's model from rounds 1-3's own `opus`
  attribution elsewhere in this file. `reviewed_by` (`opus`) genuinely
  differs from `executed_by` (`claude-sonnet-5`) — rule 5 is satisfied
  cleanly, not a disclosed gap. Corrected in place rather than left
  standing, per this project's own convention of fixing a wrong claim
  visibly instead of silently; the reviewer's actual verification work
  below (emulator harness, falsifications, suite/analyze reproduction) is
  unaffected by this correction and required no re-review — none of it
  depended on which model implemented round 4.
- `reviewed_at`: 2026-09-08 · `review_outcome`: approved

**Scope — in-contract.** `git diff --stat origin/development` touches 12
files; all 12 are in this file's own (round-4-updated) `files:` fence, plus
this task file itself. `lib/core/services/firebase_paths.dart` and
`firebase_boundary.dart` are listed in the fence but untouched — narrower
than declared, not wider. No out-of-scope edit.

**Emulator verification (the gap round 4 explicitly asked the reviewer to
close).** Brought up a real Firebase RTDB emulator (`firebase-tools`
13.28.0, `firebase emulators:exec --only database`) driven by a from-scratch
19-assertion `@firebase/rules-unit-testing` harness against a byte-identical
copy of this branch's `database.rules.json` (md5 `a8f95a29…`, diffed against
the worktree file). **19/19 pass:**
- (a) legitimate first publish, `$deviceId === identityPublicKey`, atomic
  multi-location `update()` of `directory/$id` + `directory_private/$id/ownerUid`
  — **succeeds**; the written value reads back intact.
- (b) squat — a second account publishing its OWN identity key under a
  different (victim) `$deviceId` — **denied**. Also denied: squatting an
  already-published entry with a substituted key, and *the true owner
  herself* publishing a mismatched key under her own id.
- (c) every property rounds 1-3 established still holds, unchanged:
  owner re-publish/rotation succeeds · `revokedAt` write succeeds ·
  `remove()`, `set(null)`, multi-location `update({path: null})` and
  round 4's collapse-by-nulling-every-field all **denied** · non-owner
  delete denied · `directory_private/$id/ownerUid` unreadable cross-account,
  readable by its owner, un-overwritable by another account ·
  unauthenticated publish denied · cross-account read of a `directory` entry
  still **allowed** (`ADR-0008` option 2 intact — this is a `.write`-only
  diff, correctly).
- A second account's own legitimate first publish under its OWN key still
  succeeds — the fix is not "deny everything".

**Falsification 1 (the rule).** Stripped
`&& newData.child('identityPublicKey').val() === $deviceId` from the rules
file and re-ran the identical probe: **17/19 — and precisely the two squat
assertions flip to allowed** ("Expected request to fail, but it succeeded" on
the cross-account squat at a victim device id, and on publishing a mismatched
key under one's own id). Every other assertion is unmoved, confirming the
clause is doing exactly the one job claimed and nothing else. Rules file
restored and md5-reconfirmed identical to the branch afterwards.

**Falsification 2 (the hex validation).** Reverted `hexDecodeBytes`'s strict
`^[0-9a-fA-F]{2}$` per-pair check back to the lenient
`int.tryParse(pair, radix: 16)` the round-4 log says was the original bug:
`identity_key_hex_test.dart` goes **red for the right reason** —
`test_hexDecodeBytes_rejects_malformed_input`'s base64-shaped case fails on
`'ab+1'` specifically (Dart's `int.tryParse('+1', radix: 16) == 1`). The
claimed bug and the claimed catch are both real. File restored verbatim;
`git status` clean.

**`_deriveDeviceIdFromLocalIdentity`'s reasoning — independently verified,
not taken on trust.** Read `lib/core/messaging/messaging_stack.dart:534-560`
directly: `identityService.ensureLocalIdentity()` runs inside the
unconditional crypto/identity try-block, and only *afterwards*, as a separate
statement, does `if (status.isReady && selfDeviceId.isEmpty)` downgrade
`status` to `unavailable`. `lib/app/main.dart:53-73` passes `selfDeviceId:
localIdentity?.deviceId ?? ''` and awaits `MessagingStack.create` **before**
`runApp()`. So on a genuine first launch `status.isReady` is provably `false`
while the identity keypair provably already exists — gating the derivation on
`isReady` would indeed have made it dead code in production. Dropping the
gate is safe: the only failure modes left are `Get.find<MessagingStack>()`
throwing (caught, logged, `null`) and the keypair read throwing (caught,
logged, `null`), and a `null` falls through to the pre-existing
`generateSecureDeviceId()` — additive, never a new way to fail sign-in.
`test_E11_B06_first_sign_in_derives_device_id_from_local_identity` asserts
`messagingStack.status.isReady == false` as a load-bearing sanity check, so
this cannot silently regress. `Get.put(messagingStack, permanent: true)` at
`lib/app/bindings.dart:150` confirms the singleton the resolver looks up
really is registered.

**Suite, run by me in the review worktree** (not trusted from the PR body):
`flutter test` → **1318/1318 green**, matching the claim exactly.
`flutter analyze` → **2 issues**, both `info`, both in files this diff does
not touch (`call_migration_controller_test.dart:177`,
`login_controller_enrollment_gate_test.dart:18`) — the documented baseline.

**Security lens — PASS.**
- No private key material anywhere in the diff.
  `_deriveDeviceIdFromLocalIdentity` calls `identityKeyPair.getPublicKey()`
  only (`login_controller.dart`); `_publish` hex-encodes
  `bundle.getIdentityKey()` only (`device_directory_service.dart:174`). The
  derived device id **is** the public identity key, which `ADR-0008` option 2
  already publishes in the clear at the same node — no new disclosure.
- Both encodings originate from the same `DriftSignalProtocolStore`
  (`messaging_stack.dart:537` builds `IdentityService(db, resolvedStore)` and
  line 631 hands that same `resolvedStore` out as `signalStore`), so the
  derived `$deviceId` and the later-published `identityPublicKey` are the
  same bytes by construction, not by coincidence.
- Identity-key rotation does not exist in this codebase
  (`IdentityService.ensureLocalIdentity` calls
  `_store.saveLocalIdentityIfAbsent`, `identity_service.dart:85-89`), so the
  binding cannot strand a device by rotating out from under it. See
  carried-forward note (c) for the day that changes.
- Error logging is code + cause type only, no key or message content —
  consistent with `docs/conventions.md`.
- No `.isIn(...)`/unbounded-id-list surface in this diff. No auth/payment
  *strategy* change: this hardens an existing rule, it does not introduce or
  alter an auth mechanism, so no new rule-3 stop is created.

**Carried-forward observations for the epic tracker (non-blocking, NOT
grounds for CHANGES — none is in this bug's scope):**
- **(a) The residual lock-out DoS survives this fix, and this fix does not
  claim otherwise.** Emulator-confirmed in a second probe: an attacker can
  still write `directory_private/<victimIdentityKeyHex>/ownerUid` alone
  (first-writer-wins on that node), after which the true owner's own
  legitimate publish is **permanently denied**. Impersonation is genuinely
  closed — the attacker still cannot publish a substituted key at that id —
  so what remains is denial-of-service, not identity substitution. This is
  exactly round 2's already-recorded carry-forward, unchanged in kind but now
  slightly harder to aim: the attacker must learn the victim's identity
  public key (not merely a device id) to target it. Worth its own bug when
  the first real `DeviceDirectoryService` consumer is wired.
- **(b) Legacy random device ids can no longer publish at all.**
  Emulator-confirmed: a pre-existing device whose `deviceId` is a random
  token is now denied by the new clause on every `directory/` write. No live
  impact today — per `E11-B01`, `DeviceDirectoryService` is still never
  constructed in `lib/`, so nothing in production publishes — but whoever
  wires the first consumer must handle already-installed devices (re-derive,
  or accept that they never appear in the directory).
- **(c) If identity-key rotation is ever introduced**, the
  `$deviceId === identityPublicKey` binding makes a rotated device's publish
  permanently fail. Not a defect now (no rotation path exists); a hard
  constraint on any future rotation design, and it belongs in `ADR-0008`
  before such a design starts.
- **(d) Round 3's retro candidate is now three-for-three vindicated** —
  emulator-backed verification of any `database.rules.json` change touching
  cross-path or `newData` semantics found or confirmed something a
  JSON-structure test could not, again this round. Recommend promoting it
  from a retro candidate to a standing rule.

**Findings requiring change: none.** Round 4 disclosed its own two pre-ship
bugs and its one unverified claim honestly and accurately; the unverified
claim is now verified, and both disclosed bugs reproduce and re-fix exactly
as described. Merge-ready.
