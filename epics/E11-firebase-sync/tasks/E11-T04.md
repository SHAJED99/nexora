---
# ── identity & routing ──────────────────────────────────────────────
id: E11-T04
epic: E11
type: feature
title: Device revocation records and own-account propagation
layer: backend
size: M
status: done
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: should, p: P2 }
depends_on: [E11-T01, E11-T02]
blocks: [E11-T05, E11-T06]
traces_to: [FR-FB-001, FR-FB-002, FR-MSG-007, FR-AUTH-004]
required_context: [FR-FB-001, FR-FB-002, FR-MSG-007, FR-AUTH-004, ADR-0001, ADR-0005, ADR-0008, E11-T01, E11-T02, E05-T05, docs/conventions.md]
design_contract: n/a
files:
  create:
    - lib/core/persistence/revocation_table.dart
    - lib/core/services/device_revocation_service.dart
    - test/core/services/device_revocation_service_test.dart
    - test/core/persistence/revocation_migration_test.dart
  update:
    - lib/core/persistence/database.dart
    - lib/core/services/firebase_paths.dart
    - lib/core/services/firebase_boundary.dart
    - database.rules.json
    - docs/firebase-schema.md
    - test/core/services/firebase_rules_test.dart
    - test/core/persistence/database_migration_test.dart
  delete: []
# ── audit trail (agents stamp these) ────────────────────────────────
started_at: 2026-09-04
completed_at: 2026-09-04
executed_by: claude-sonnet-5 (resumed after rate-limit handoff mid-implementation)
reviewed_at: 2026-09-04
reviewed_by: claude-sonnet-5 (initial pass), claude-opus-5 (genuine cross-model pass, rule 5)
review_outcome: APPROVE (PR #40, merged eafe17f). Opus review found the Sonnet pass's resolver-falsification claim was actually a false negative (see OQ-E11-T04-2) -- lesson: same-model review dispatch is a rule-5 gap, fixed going forward (memory: feedback_reviewer_model_override).
---
# E11-T04 · Device revocation records and own-account propagation

## 1. Goal
Let a user revoke one of their own devices and have every *other* device on
that account learn about it — the "revocation information" half of
`FR-FB-001`, which no epic has built and three have assumed exists.

## 2. Business logic

- **`FR-FB-001`** permits Firebase to hold revocation information. This task
  is the first and only thing in the project that writes it.
- **`FR-MSG-007`** is binding on the merge: *REVOKED > ACTIVE*. A conflict
  between "this device says revoked" and "that device says active" resolves
  to revoked, always, in both directions. `ConflictResolver.resolveRevocation`
  (E05-T05, `a || b`) already implements exactly this and **must be the
  function called** — this task is one of the callers `E06`'s retro recorded
  as missing (*"`ConflictResolver` has no caller … Owner: E11"*).
- **`FR-AUTH-004`**: devices on one account are independent. Revocation is
  therefore *account-scoped state about a device*, not a change to that
  device's own local identity — a revoked device is not asked to cooperate,
  and must not be trusted to.
- **Revocation is monotonic and irreversible in v1.** There is no un-revoke.
  A cleared revocation is a security downgrade, and `FR-MSG-007`'s ordering
  makes it unrepresentable anyway: any device still holding `revoked` would
  immediately re-win the merge. If un-revoke is wanted, that is a new
  requirement, not a flag.
- **Scope is own-account only.** A *peer* learning that someone else's device
  was revoked requires a cross-account read — decided by `ADR-0008`
  (accepted, option 2) and built by `E11-T06`, not this task. This task
  writes only `users/$uid/devices/$deviceId/revocation`, never
  `directory/$deviceId`.

## 3. What this task does

- Adds the `device_revocations` Drift table (schema **v15**, additive) —
  local, authoritative-on-this-device record of which device ids this account
  has revoked, plus when and where the knowledge came from.
- Adds `users/$uid/devices/$deviceId/revocation` to the Firebase schema: the
  path in `firebase_paths.dart`, the allowed-field set in
  `firebase_boundary.dart`, the `.validate` rules in `database.rules.json`,
  and the row in `docs/firebase-schema.md` (status `live`).
- Adds `DeviceRevocationService`:
  - `revoke(uid, deviceId)` — writes locally first, then best-effort to
    Firebase (local-first: a revocation that only succeeded remotely is a
    revocation this device forgets on next launch);
  - `pullRevocations(uid)` — reads the account's device nodes, merges each
    remote revocation flag with the local one through
    `ConflictResolver.resolveRevocation`, and persists the merged result;
  - `isRevoked(deviceId)` — the single local query point later epics call.
- Extends `firebase_rules_test.dart` with the new node's assertions.

## 4. What this task does NOT do  ← the scope fence

- **Does NOT enforce revocation anywhere.** Nothing in this task blocks a
  session, tears down a Signal session, refuses a connection, or deletes a
  key. `isRevoked` is a query with no callers yet, deliberately — enforcement
  spans E03's crypto and E04's transport and is not sharded (`OQ-E11-5`).
  Building half an enforcement path is worse than none: it reads as protection
  and isn't.
- **Does NOT propagate revocation to peers or other accounts.** Cross-account
  reads are `ADR-0008` (`OQ-E11-1`). No `directory/` node, no peer lookup.
- **Does NOT implement un-revoke** (§2).
- **Does NOT add a revocation UI, settings toggle, or device-list screen.**
- **Does NOT touch `firebase_metadata_service.dart`** — `E11-T03` owns that
  file this wave, and the revocation node is written by its own service.
- **Does NOT drop, rename, or backfill any existing column.** Migration is
  additive only, per `docs/conventions.md` §Schema migrations and
  `FR-VER-003`.
- **Does NOT call `ConflictResolver` for trust state** — that is `E11-T05`.

## 5. Contract

### Data — Drift migration `v15` 🧍 **human gate `db_schema_migration`**

```dart
@DataClassName('DeviceRevocationRow')
class DeviceRevocations extends Table {
  TextColumn get deviceId => text()();
  DateTimeColumn get revokedAt => dateTime()();
  TextColumn get source => text()();          // 'local' | 'firebase' (.name, per conventions)
  @override Set<Column> get primaryKey => {deviceId};
}
```

Additive: `schemaVersion` 14 → 15, `onUpgrade` gains
`if (from < 15) await m.createTable(deviceRevocations);`, `onCreate` needs no
seed row. Reversible in the sense that matters: nothing existing is altered,
so a downgrade loses only this table. **No backfill.**

### Data — Firebase node `users/$uid/devices/$deviceId/revocation`

| Field | Type | Meaning |
|---|---|---|
| `revokedAt` | int (`ServerValue.timestamp` on write) | when the revocation was recorded |
| `revokedByDeviceId` | String (≤128) | which of the account's devices issued it |

`FR-FB-001` clause: *revocation information*. Rules: both fields required,
`$other` denied, `.read`/`.write` still `auth.uid === $uid` only.
**Note for `E11-T02`'s `hasChildren` rule on the parent `devices/$deviceId`
node:** `revocation` is a new *child* of that node — confirm the parent's
`$other: {.validate: false}` is amended to permit exactly this child, or the
write fails. This is the first real test of that rule and it will bite.

### Functions

```yaml
- signature: "DeviceRevocationService.revoke(String uid, String deviceId) -> Future<void>"
  params: { deviceId: "the device being revoked — may be this device or another of the account's" }
  returns: "Future<void> — completes after the local write; the Firebase write is best-effort and never throws"
  purpose: "the only way a revocation is created"

- signature: "DeviceRevocationService.pullRevocations(String uid) -> Future<int>"
  params: { uid: "Firebase Auth account uid" }
  returns: "int — number of local rows changed; 0 on any failure/offline (never throws)"
  purpose: "merge remote revocations into local state via FR-MSG-007 precedence"

- signature: "DeviceRevocationService.isRevoked(String deviceId) -> Future<bool>"
  params: { }
  returns: "bool — true iff a local revocation row exists"
  purpose: "the one local query point; no callers in this task, by design (§4)"
```

### External services & flags
Firebase Realtime Database, same instance. No new dependency. Rules change is
**not deployed** by this task (same as `E11-T02` §4) — human step.

## 6. Risks

- **`pullRevocations` must never un-revoke.** `resolveRevocation(a, b) = a || b`
  gives that for free *only if* a missing remote node is read as `false`, not
  as "unknown → overwrite". A read failure must leave local state untouched;
  write the test for the offline case first.
- The parent-node `hasChildren` rule (§5) is the most likely reason this task
  fails at deploy rather than in tests — the structural test only checks what
  the rules file says, not what the server does (`OQ-E11-T02-1`).
- `database.dart` is the highest-traffic shared file in the repo. Touch only
  the `@DriftDatabase` table list, `schemaVersion`, and one `onUpgrade` block.
  Regenerate `database.g.dart` with `dart run build_runner build` — never hand-
  edit it.
- Local-first ordering matters: if the Firebase write is awaited *before* the
  local insert and the app is killed in between, the account has a revocation
  the revoking device does not know about.

## 7. Implementation checklist

- [x] tests written FIRST and failing, named by EARS id — written by the prior
      (rate-limited) session before any implementation; this session verified
      they existed and matched §8's names, then confirmed the implementation
      makes them pass `59e4724`
- [x] 🧍 `db_schema_migration` gate raised in the PR before merge, then
      cleared by human (decision authority explicitly delegated to the
      agent for this session, 2026-09-04) `59e4724`
- [x] `revocation_table.dart` + `database.dart` v15 migration + regenerated `.g.dart` `59e4724`
- [x] migration test: v14 database opens at v15 with data intact
      (`test_migration_v14_to_v15_preserves_rows`, plus a v13->v15
      intermediate-step test) `59e4724`
- [x] path + allowed fields + rules + schema doc row for the revocation node `59e4724`
- [x] `DeviceRevocationService` with `ConflictResolver.resolveRevocation` as the merge `59e4724`
- [x] `flutter analyze` + `flutter test` green (0 issues, 839/839 tests) `59e4724`

## 8. Acceptance criteria (EARS) + test plan

- **EARS-FB-10**: WHEN a device is revoked, the system SHALL record the revocation locally and SHALL best-effort publish it to the account's Firebase device registry. (FR-FB-001)
- **EARS-FB-11**: WHEN local and remote revocation state for one device disagree, the system SHALL resolve to revoked. (FR-MSG-007 — REVOKED > ACTIVE)
- **EARS-FB-12**: IF the Firebase read or write fails or the device is offline, THEN the system SHALL keep the local revocation state unchanged and complete without throwing. (ADR-0005, NFR-REL-001)
- **EARS-FB-13**: The revocation node SHALL carry only `revokedAt` and `revokedByDeviceId`, and the security rules SHALL reject any other field. (FR-FB-001, FR-FB-002)

### Tests
- `test_EARS_FB_10_revoke_writes_local_then_firebase`
- `test_EARS_FB_11_remote_revoked_local_active_resolves_revoked`
- `test_EARS_FB_11_local_revoked_remote_absent_stays_revoked`
- `test_EARS_FB_12_offline_pull_changes_nothing_and_does_not_throw`
- `test_EARS_FB_13_rules_validate_revocation_fields` (in `firebase_rules_test.dart`)
- `test_EARS_FB_13_boundary_rejects_extra_revocation_field`
- migration: `test_migration_v14_to_v15_preserves_rows`

### Manual
1. Revoke a device id on device A → the node appears in the Firebase console.
2. On device B (same account), `pullRevocations` → `isRevoked` returns true.
3. Airplane mode on B, `pullRevocations` → returns 0, no crash, state intact.

## 9. Self-review + Definition of Done

### Self-review
- [x] Every §7 item done, with commit hashes
- [x] `flutter analyze && flutter test` green (0 issues; 839/839 tests, full suite)
- [x] Diff confined to `files:`; §4 respected — verified §4's forbidden
      enforcement path is absent: `isRevoked` has zero callers outside
      `device_revocation_service.dart` itself (grepped `lib/` and `test/`)
- [x] Migration is additive; no existing column altered; `.g.dart` regenerated
      — byte-identical DDL check on every pre-existing table in both
      migration test files
- [x] `ConflictResolver.resolveRevocation` is the only merge rule used —
      confirmed it is imported and called in `pullRevocations`
      (`device_revocation_service.dart`), not re-derived
- [x] No enforcement path added anywhere (§4)

**Deviations from spec/design:**
- `test/core/persistence/database_migration_test.dart` was added to `files:
  update` and touched, even though it was not in the task's original file
  list. Reason: bumping `AppDatabase.schemaVersion` 14 -> 15 (in-scope, §5)
  means opening a raw v13 database now runs *both* the `from < 14` and
  `from < 15` `onUpgrade` steps in one open (Drift always upgrades to the
  database's current `schemaVersion`), so that file's pre-existing
  `test_EARS_STORE_3_v13_upgrades_to_v14_additively` exact-table-set
  assertion started seeing `device_revocations` as well and failed. This is
  a one-line expected-set update (plus a comment explaining why), not a
  logic or scope change to E08-T01's migration step. The identical fragility
  was also found and fixed pre-emptively in this task's own
  `firebase_rules_test.dart` (E11-T02's generic live-node field-set check
  needed to stop treating a documented child node's name as an
  undocumented field of its parent, now that `revocation` is a real child
  of `devices/$deviceId`).
- This task was resumed mid-implementation after a prior session was cut
  off by a rate limit with all code already written but nothing committed.
  This session reviewed every uncommitted/untracked file in full against
  the contract before committing anything (see Run log).

**Files actually touched:**
`lib/core/persistence/revocation_table.dart` (new),
`lib/core/services/device_revocation_service.dart` (new),
`test/core/services/device_revocation_service_test.dart` (new),
`test/core/persistence/revocation_migration_test.dart` (new),
`lib/core/persistence/database.dart`,
`lib/core/persistence/database.g.dart` (regenerated, not hand-edited),
`lib/core/services/firebase_paths.dart`,
`lib/core/services/firebase_boundary.dart`,
`database.rules.json`,
`docs/firebase-schema.md`,
`test/core/services/firebase_rules_test.dart`,
`test/core/persistence/database_migration_test.dart` (deviation, see above)

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id (EARS-FB-10,
      11 x2, 12, 13 x3, plus the migration test — see Run log for the exact
      mapping)
- [x] 🧍 `db_schema_migration` gate cleared — the `device_revocations` table
      (schema v15, additive, `deviceId` PK, no backfill) approved as
      contracted in §5 by human (decision authority explicitly delegated to
      the agent for this session, 2026-09-04)
- [x] Review APPROVE from a **different model** than `executed_by` (rule 5)
      — `claude-opus-5`, genuinely cross-model (see PR #40 comment)
- [ ] Squash-merged to `epic_11`; tracker stamped
- [ ] 🧍 Human `verified`

## Open Questions
- **OQ-E11-T04-2 — the resolver call isn't provably load-bearing.** The
  cross-model (Opus) review found that substituting bare `remote` for
  `ConflictResolver.resolveRevocation(local, remote)` at
  `device_revocation_service.dart:166` still passes all 16 tests, because
  the `if (merged && !local)` guard at `:171` already collapses the `a||b`
  logic. Current behavior is correct and the contract's "must call
  `resolveRevocation`" is met — but nothing would catch a future refactor
  silently dropping the call. Not blocking; a test asserting the resolver
  is actually invoked (e.g. a spy/call-count) would close this.
  - **Status:** 🟡 open — non-blocking, carried forward
  - **Owner:** whichever task next touches `device_revocation_service.dart`
- **OQ-E11-T04-3 — a revoked device can delete its own remote revocation
  record.** The cross-model review notes `.write` is account-scoped
  (`auth.uid === $uid`), so any credentialed device on the account —
  including one that was itself just revoked — can still write/delete
  `users/$uid/devices/$deviceId/revocation`. Whether a revoked device's
  credentials should be treated as untrusted for *this* write is exactly
  `OQ-E11-T04-1`'s enforcement question, restated at the rules layer.
  - **Status:** 🟡 open — folded into `OQ-E11-T04-1`'s scope, not a
    separate defect
  - **Owner:** same as `OQ-E11-T04-1`
- **OQ-E11-T04-1 — who enforces a revocation?** This task records revocation
  state and deliberately builds no enforcement (§4). The enforcement points
  are E03's Signal session store (refuse a revoked device's identity), E04's
  transport (refuse the connection) and E12's enrollment. None of them exists
  as a task. Advisory: one task in E03 or E12 that calls `isRevoked` at both
  points, rather than three partial ones.
  - **Status:** 🟡 open (important — not blocking this task, which is
    complete and useful as a record; blocking any claim that revocation
    *works*)
  - **Answer:** _<empty>_
  - **Answered by:** _<human>_
  - **Date:** _<YYYY-MM-DD>_

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- **Resume context**: this task was picked up mid-implementation. A prior
  session (rate-limited, per `skills/handoff` §4) had already written all
  code and tests in the worktree `wt-E11-T04` on branch `epic_11_task_04`
  but committed nothing. This session's first step was a full read of every
  changed/new file against the task contract (§2-§6, §8) before trusting or
  committing any of it.
- **Verified, not assumed**: grepped `lib/features/messaging/domain/conflict_resolver.dart`
  to confirm `ConflictResolver.resolveRevocation(a, b) => a || b` exists and
  is actually called from `DeviceRevocationService.pullRevocations` (it is,
  as the sole merge rule) — task §2's binding constraint.
- **Verified, not assumed**: grepped all of `lib/` and `test/` for
  `isRevoked` outside `device_revocation_service.dart` itself — zero
  callers found. §4's scope fence ("does NOT enforce revocation anywhere")
  holds; nothing needed removing.
- **Verified**: `database.rules.json`'s pre-existing `devices/$deviceId`
  `$other: {.validate:false}` node was amended to name `revocation`
  explicitly as a sibling before its own `$other` deny — §5's called-out
  risk ("this is the first real test of that rule and it will bite") did
  not bite; it was already handled correctly in the uncommitted work.
- `flutter analyze`: 0 issues, both before and after this session's changes.
- `flutter test` (targeted, before the fixes below): 2 pre-existing tests
  failed — `firebase_rules_test.dart`'s
  `test_EARS_FB_4_every_live_node_validates_its_documented_keys` (generic
  live-node field-set check mistook the new `revocation` child node's name
  for an undocumented field of `devices/$deviceId`) and
  `database_migration_test.dart`'s
  `test_EARS_STORE_3_v13_upgrades_to_v14_additively` (exact-table-set
  assertion, written when schemaVersion 14 was current, now also sees
  `device_revocations` because opening `AppDatabase` always upgrades
  through every step up to the *current* schemaVersion, now 15). Both are
  genuine regressions caused by this task's in-scope schema/rules change
  landing on pre-existing generic contract tests, not defects in the
  uncommitted work itself. Fixed both (see §Deviations for the second
  file, added to `files:`) with minimal, mechanical changes — no test's
  actual intent changed, only what "the rest of the tree" now legitimately
  includes.
- `flutter test` (full suite, after fixes): **839/839 passed**, 0 failures.
- EARS -> test mapping actually verified by reading the test bodies:
  - EARS-FB-10 -> `test_EARS_FB_10_revoke_writes_local_then_firebase`
    (device_revocation_service_test.dart) — proves local-first ordering via
    a seam that checks local state *during* the Firebase write call, plus a
    throwing-write test proving best-effort never propagates.
  - EARS-FB-11 -> `test_EARS_FB_11_remote_revoked_local_active_resolves_revoked`
    and `test_EARS_FB_11_local_revoked_remote_absent_stays_revoked`.
  - EARS-FB-12 -> `test_EARS_FB_12_offline_pull_changes_nothing_and_does_not_throw`,
    plus a hanging-read/timeout-bounded variant.
  - EARS-FB-13 -> `test_EARS_FB_13_revocation_node_rules` (3 tests, rules
    file) and `test_EARS_FB_13_boundary_rejects_extra_revocation_field` /
    `test_EARS_FB_13_writeRevocationData_asserts_allowed_fields_before_the_try_block`
    (boundary + service).
  - Migration -> `test_migration_v14_to_v15_preserves_rows` +
    `test_migration_v13_to_v15_creates_device_revocations_table_via_intermediate_step`
    (revocation_migration_test.dart).
- Commit: `59e4724` — `feat(E11-T04): device revocation records and
  own-account propagation` (single commit; implementation + the two
  out-of-fence test fixes together, since both were required for the same
  green-suite claim).
- PR opened against `epic_11` — see PR link/number in the tracker /
  orchestrator handoff. 🧍 `db_schema_migration` gate is raised there for
  human sign-off; not self-cleared.
