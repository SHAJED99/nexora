---
# ── identity & routing ──────────────────────────────────────────────
id: E11-T05
epic: E11
type: feature
title: Own-account relationship-config mirror (trust/block state across a user's own devices)
layer: backend
size: M
status: done
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: should, p: P2 }
depends_on: [E11-T01, E11-T02, E11-T04]
blocks: [E11-T06]
traces_to: [FR-TRUST-007, FR-FB-001, FR-MSG-007]
required_context: [FR-TRUST-007, FR-FB-001, FR-MSG-007, ADR-0005, ADR-0008, E11-T01, E11-T02, E11-T04, E02-T01, E02-T02, lib/features/trust/domain/relationship.dart, lib/features/trust/data/relationship_repository.dart, lib/features/messaging/domain/conflict_resolver.dart]
design_contract: n/a
files:
  create:
    - lib/core/services/relationship_sync_service.dart
    - test/core/services/relationship_sync_service_test.dart
  update:
    - lib/core/services/firebase_paths.dart
    - lib/core/services/firebase_boundary.dart
    - database.rules.json
    - docs/firebase-schema.md
    - test/core/services/firebase_rules_test.dart
  delete: []
# ── audit trail (agents stamp these) ────────────────────────────────
started_at: 2026-09-04
completed_at: 2026-09-04
executed_by: builder (claude-sonnet-5)
reviewed_at: 2026-09-04
reviewed_by: claude-opus-5 (genuine cross-model)
review_outcome: APPROVE (PR #44, merged). Own-account-only boundary independently traced and confirmed clean; ConflictResolver.resolveTrust falsification-verified as load-bearing. One non-blocking test-hygiene note (S4, epoch-seconds timestamp granularity in a redundant-write guard test) carried forward, not blocking.
---
# E11-T05 · Own-account relationship-config mirror

## 1. Goal
Let a user's trust/block decision about one peer device show up on their
*other* devices — `FR-TRUST-007`'s "relevant relationship configuration
shall synchronize", read narrowly (per `ADR-0008`) as **a user's own
devices agreeing with each other**, not one account learning another
account's relationship state.

## 2. Business logic
- **`ADR-0008`'s declined-option-3 boundary is binding here.** This task
  does **not** read or write anything under another account's uid. It
  mirrors `users/$uid/relationships/$peerDeviceId` — `$uid` is always the
  authenticated caller's own account. Cross-account relationship exchange
  (`OQ-E09-T02-1`'s original ask) stays an accepted, permanent limitation
  per `ADR-0008`'s Consequences.
- **The merge rule already exists and must be reused, not reinvented.**
  `ConflictResolver.resolveTrust(RelationshipState a, RelationshipState b)`
  (`conflict_resolver.dart:60`) picks the more restrictive of two states
  (`_trustRestrictiveness`, more restrictive wins) — this is `FR-MSG-007`'s
  own ordering and is exactly the semantics two of a user's devices
  disagreeing about a peer need: if one device already blocked someone,
  a second device that never heard about it must not un-block them by
  silently overwriting. `E06`'s retro named this function as built and
  uncalled since E05; this task is one of its two intended callers
  (`E11-T04` is the other, for revocation).
- **Local-first, same shape as `E11-T04`.** A relationship change is
  authoritative locally the instant `RelationshipRepository.upsert` returns;
  the Firebase mirror is best-effort and never blocks or reverses a local
  decision.
- **`RelationshipState` has four values** (`trusted`, `allowed`, `unknown`,
  `blocked`) — the Firebase field is the enum's `.name` string, per
  `docs/conventions.md` §Enums, never an index.

## 3. What this task does
- Adds `users/$uid/relationships/$peerDeviceId` to the Firebase schema — the
  path in `firebase_paths.dart`, the allowed-field set (`state:String`,
  `updatedAt:int(ServerValue)`) in `firebase_boundary.dart`, the
  `.validate` rules in `database.rules.json` restricting `state` to the
  four known enum-name strings, and the row in `docs/firebase-schema.md`
  (status `live`, superseding its `reserved` placeholder from `E11-T01`).
- Adds `RelationshipSyncService`:
  - `push(uid, deviceId, RelationshipState state)` — writes locally via
    `RelationshipRepository.upsert` first, then best-effort mirrors to
    Firebase; a Firebase failure is logged and swallowed, never surfaced to
    the caller of a trust/block action.
  - `pull(uid)` — reads every `relationships/$peerDeviceId` node under the
    caller's own uid, merges each remote `state` with the corresponding
    local one through `ConflictResolver.resolveTrust`, and persists the
    merged result via `RelationshipRepository.upsert` only when it differs
    from what is already stored.
- Extends `firebase_rules_test.dart` with the new node's assertions
  (own-uid read/write only; a peer's uid must not be able to read or write
  `users/<other-uid>/relationships/...`).

## 4. What this task does NOT do  ← the scope fence
- **Does NOT let one account read another account's relationships.** Every
  path this task touches is `users/$uid/relationships/...` for the caller's
  own `$uid`. `ADR-0008`'s directory (`E11-T06`) is a completely separate
  node (`directory/$deviceId`) with different data and different rules;
  this task does not touch it.
- **Does NOT call `ConflictResolver` for revocation** — that is `E11-T04`,
  already built and merged before this task starts (`depends_on`).
- **Does NOT decide *when* to call `push`/`pull`.** Wiring these into
  `EvaluateConnectionRequestUseCase` or the Devices/Trust screen's actions
  is a caller's decision outside this task's `files:` fence — this task
  ships the service, proven, with no caller yet, the same shape `E11-T04`
  shipped `isRevoked` in.
- **Does NOT add a UI, a settings toggle, or a sync-status indicator.**
- **Does NOT change `RelationshipState`'s values, `_trustRestrictiveness`'s
  ordering, or any file under `lib/features/trust/domain/`.**
- **Does NOT touch `firebase_metadata_service.dart` or the revocation
  node/table** — those are `E11-T03`/`E11-T04`'s files.

## 5. Contract

### Data — the Realtime Database node
| Path | Fields | Notes |
|---|---|---|
| `users/$uid/relationships/$peerDeviceId` | `state:String` (one of `trusted`/`allowed`/`unknown`/`blocked`), `updatedAt:int(ServerValue)` | own-uid only; `$peerDeviceId` is the remote device id, not a foreign uid |

### Functions
```yaml
- signature: "RelationshipSyncService.push(String uid, String deviceId, RelationshipState state) -> Future<void>"
  params: { uid: "the caller's own Firebase Auth uid", deviceId: "the peer device id this relationship is about" }
  returns: "void; never throws — a Firebase failure is logged and swallowed"
  purpose: "mirror a local trust/block decision to the account's other devices, best-effort"

- signature: "RelationshipSyncService.pull(String uid) -> Future<void>"
  params: { uid: "the caller's own Firebase Auth uid" }
  returns: "void; merges every remote relationship into local storage via ConflictResolver.resolveTrust"
  purpose: "reconcile relationship state written by another of this account's devices"
```

### External services & flags
Firebase Realtime Database, same instance as `E11-T01`/`T04`. No new
package, no new permission.

## 6. Risks
- **Merge direction matters.** `resolveTrust(a, b)` must be called with
  *(local, remote)* or *(remote, local)* consistently — since the function
  is commutative in outcome (more-restrictive wins regardless of argument
  order) this is lower-risk than `E11-T04`'s cursor-argument-order risk, but
  test it explicitly rather than assuming commutativity without proof.
- **`pull` must not thrash writes.** Only call `RelationshipRepository.upsert`
  when the merged state actually differs from the stored one, or every
  `pull` call generates a spurious `updatedAt` bump and a redundant
  `push`-shaped write-amplification loop if a caller ever chains the two.
- A device with no local relationship for a peer that Firebase has one for
  must merge as if the local side were `unknown` (the domain default), not
  crash on a missing row.

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS id `0893a83`
- [x] `firebase_paths.dart`/`firebase_boundary.dart` extended for the
      relationships node `0893a83`
- [x] `database.rules.json` — own-uid-only read/write, `state` restricted
      to the four enum-name strings `0893a83`
- [x] `docs/firebase-schema.md` row flipped `reserved` → `live` `0893a83`
- [x] `RelationshipSyncService.push`/`pull` implemented and tested `0893a83`
- [x] `firebase_rules_test.dart` extended (own-uid pass, foreign-uid denied) `0893a83`
- [x] `flutter analyze` + `flutter test` green `0893a83`

## 8. Acceptance criteria (EARS) + test plan
- **EARS-FB-14**: WHEN a relationship's state changes locally, the system
  SHALL persist it locally first and best-effort mirror it to the account's
  own Firebase relationship node, without blocking or reversing the local
  change on a Firebase failure. (FR-TRUST-007)
- **EARS-FB-15**: WHEN a remote relationship state disagrees with the local
  one for the same peer device, the system SHALL resolve to the more
  restrictive of the two via `ConflictResolver.resolveTrust`. (FR-MSG-007)
- **EARS-FB-16**: The system's Firebase security rules SHALL grant read and
  write on `users/$uid/relationships/*` only to the authenticated owner of
  `$uid`. (FR-FB-001, ADR-0005, ADR-0008)

### Tests
- `test_EARS_FB_14_push_writes_local_first_then_best_effort_remote`
- `test_EARS_FB_14_remote_failure_does_not_throw_or_revert_local`
- `test_EARS_FB_15_pull_merges_to_more_restrictive_state`
- `test_EARS_FB_15_pull_no_op_when_merged_state_matches_stored`
- `test_EARS_FB_16_foreign_uid_read_denied` / `..._write_denied` (rules test)

## 9. Self-review + Definition of Done
### Self-review
- [x] Every §7 item done, with commit hashes (`0893a83`)
- [x] `flutter analyze && flutter test` green (0 analyze issues; 862/862
      tests pass, full suite)
- [x] Diff confined to `files:`; §4 respected — no UI, no wiring into
      `EvaluateConnectionRequestUseCase` or any caller, no change to
      `RelationshipState`/`_trustRestrictiveness`/anything under
      `lib/features/trust/domain/`, `firebase_metadata_service.dart` and
      the revocation node untouched
- [x] No cross-account path appears anywhere in the diff — every path is
      `users/$uid/relationships/$peerDeviceId` for the caller's own `uid`;
      `deviceId`/`peerDeviceId` is always a remote device id, never a
      foreign account's uid

**Deviations from spec/design**:
- Tests and implementation were developed together rather than strictly
  red-then-green: the service was written first from the task's §5
  contract, then the test file, then both run together. This is not pure
  TDD, but it did surface two genuine defects in my first draft of the
  tests (not the implementation) — my initial assumption that a peer with
  no local relationship should merge a remote `allowed`/`trusted` state
  onto the *stored* value was wrong given `ConflictResolver`'s documented
  ordering (`blocked > unknown > allowed > trusted`, most-restrictive
  first): `unknown` (the default for "no local row") already outranks
  both `allowed` and `trusted`, so `resolveTrust(unknown, allowed)` stays
  `unknown` and correctly creates no row. Both tests were corrected to
  assert the actual, correct behavior (confirmed by running them red
  before the fix and green after), rather than adjusting the service to
  match a wrong test expectation — see
  `test/core/services/relationship_sync_service_test.dart`, the two tests
  named "a remote state LESS/MORE restrictive than unknown".
- No other deviation. `ConflictResolver.resolveTrust` is called exactly
  once, in `pull`, as `resolveTrust(localState, remote)` — never
  re-derived.

**Files actually touched:**
`lib/core/services/firebase_paths.dart`,
`lib/core/services/firebase_boundary.dart`,
`lib/core/services/relationship_sync_service.dart` (new),
`database.rules.json`, `docs/firebase-schema.md`,
`test/core/services/relationship_sync_service_test.dart` (new),
`test/core/services/firebase_rules_test.dart` — exactly the task's
`files:` list, no more.

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id —
      EARS-FB-14: `test_EARS_FB_14_push_writes_local_first_then_best_effort_remote`,
      `test_EARS_FB_14_remote_failure_does_not_throw_or_revert_local`;
      EARS-FB-15: `test_EARS_FB_15_pull_merges_to_more_restrictive_state`,
      `test_EARS_FB_15_local_more_restrictive_than_remote_stays_local`,
      `test_EARS_FB_15_pull_no_op_when_merged_state_matches_stored`;
      EARS-FB-16: `test_EARS_FB_16_writeRelationshipData_asserts_allowed_fields_before_the_try_block`,
      `test_EARS_FB_16_boundary_rejects_extra_relationship_field`, plus
      the `test_EARS_FB_16_relationship_node_rules` group in
      `firebase_rules_test.dart` (own-uid inheritance, foreign-uid denied,
      state restricted to the four enum names)
- [ ] Review APPROVE from a **different model** than `executed_by` (rule 5)
- [ ] Squash-merged to `epic_11`; tracker stamped
- [ ] 🧍 Human `verified` (epic-level batch is fine)

## Open Questions
- (none — no spec gap encountered; the task's §5/§6 contract was
  sufficient to implement without improvisation)

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- 2026-09-04: branched `epic_11_task_05` off `origin/epic_11` (tip
  `77c1313`, T01/T02/T04 already merged). Read `epic.md`, `ADR-0008`
  (accepted, option 2), `relationship.dart`, `relationship_repository.dart`,
  `conflict_resolver.dart`, and E11-T04's `device_revocation_service.dart`
  as the direct implementation pattern to mirror (same best-effort/
  local-first/timeout/boundary-guard shape, same test-seam style via
  subclassing + overriding the Firebase read/write methods).
  `FirebasePaths.relationship`/`relationships`,
  `FirebaseNodeKind.relationship`, the `database.rules.json` node and the
  `docs/firebase-schema.md` row flip were added exactly as T01's schema
  table reserved them. `RelationshipSyncService.push`/`pull` implemented;
  `pull` calls `ConflictResolver.resolveTrust(localState, remote)` with
  `localState` defaulting to `RelationshipState.unknown` when no local row
  exists, writing only when the merged result differs from what's stored
  (avoids the write-amplification risk named in the task's §6). Full suite
  green: `flutter analyze` 0 issues, `flutter test` 862/862 passing.
  Commit `0893a83`.

---

## Descoped after completion (2026-09-06)

> This task file is left otherwise **unedited** — it is the historical record
> of what was built and why, and it was correct work against the spec as it
> stood.

`FR-TRUST-007` and `EARS-FB-14`/`EARS-FB-15` were **descoped by human
decision on 2026-09-06** (`docs/impact/IMP-002-descope-fr-trust-007.md`), and
this task's deliverable — `lib/core/services/relationship_sync_service.dart`
and its test file — was deleted by `E12-B11`.

Not a defect in this task. `push` never gained a production caller, and
`E12-B03`'s human-decided fix (a dedicated enrollment-grant channel) correctly
removed `pull`'s only one. With `ADR-0005`'s independent-per-device sessions,
nothing else needed an account's own devices to agree on peer-relationship
state. `FirebasePaths.relationship`/`relationships`,
`FirebaseNodeKind.relationship` and the `database.rules.json` node this task
also added survive as unused-but-guarded surface; see `IMP-002` §Residual
dead surface.
