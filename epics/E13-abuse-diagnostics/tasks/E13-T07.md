---
id: E13-T07
epic: E13
type: feature
title: Wire the rate limiter into production — connection requests and device registration currently unwired
layer: backend
size: S
status: review-requested
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: S, range: "15k-30k" }
priority: { moscow: must, p: P1 }
depends_on: [E13-T01, E13-T02]
blocks: []
traces_to: [FR-ABUSE-001]
required_context:
  - epics/E13-abuse-diagnostics/tasks/E13-T02.md
  - lib/features/trust/domain/evaluate_connection_request_use_case.dart
  - lib/features/login/data/device_identity_repository.dart
  - lib/core/messaging/messaging_stack.dart
  - lib/features/devices/presentation/devices_controller.dart
  - lib/features/login/domain/sign_in_use_case.dart
  - lib/app/bindings.dart
  - lib/core/abuse/rate_limiter.dart
design_contract: n/a
files:
  create: []
  update:
    - lib/core/messaging/messaging_stack.dart
    - lib/features/devices/presentation/devices_controller.dart
    - lib/features/login/domain/sign_in_use_case.dart
    - lib/app/bindings.dart
    - lib/core/abuse/rate_limiter.dart
    # Added in review round 2 (F1 fix) — the reviewer's own finding named
    # this file as the site of the bug and directed the fix into it (see
    # `## Feedback log` and `## Run log` round-2 entries). Not in this
    # task's original fence; disclosed here rather than silently widened.
    - lib/features/login/presentation/login_controller.dart
  delete: []
started_at: 2026-09-05
completed_at: 2026-09-05
executed_by: sonnet (claude code, builder role)
reviewed_at: 2026-09-06 (3 rounds: initial + F1/F3 fix + F6 fix)
reviewed_by: opus (claude code, reviewer role)
review_outcome: APPROVE (auth_or_payment_code gate cleared by human 2026-09-06)
---
# E13-T07 · Wire the rate limiter into production

## 1. Goal
`E13-T02` built and tested a real admission-check mechanism for
connection-request spam and device-registration abuse — and its own
cross-model reviewer confirmed both mechanisms are correct and
falsifiable in isolation, then flagged that **neither is reachable in
the running app**: every real construction site was left outside that
task's own `files:` fence by design (`OQ-E13-T02-1`), so both `RateLimiter?`
parameters default to `null` and the gates silently no-op. `EARS-ABUSE-4`
and `EARS-ABUSE-5` are currently **false** in production. This task closes
that gap — this is the `L-process-007` "unwired capability" shape
(previously seen in E09-B02/B06), named now with an owning task so it
does not sit unresolved through the rest of this epic the way it did
across eight tasks in E06.

## 2. Business logic
`FR-ABUSE-001`. Four real construction sites need a real `RateLimiter`
threaded through, per `OQ-E13-T02-1`'s own text plus one site its own
Open Question missed (found by `E13-T02`'s reviewer):

1. `lib/core/messaging/messaging_stack.dart` — construction site for
   `EvaluateConnectionRequestUseCase(RelationshipRepository(db))` needs
   `rateLimiter: RateLimiter(db)` added.
2. `lib/features/devices/presentation/devices_controller.dart` — its
   own default-fallback construction of `EvaluateConnectionRequestUseCase`
   needs a `db` (or a pre-built `RateLimiter`) threaded into
   `DevicesController`'s own constructor to build a real default.
3. `lib/features/login/domain/sign_in_use_case.dart` — the existing
   `createDeviceIdentity(deviceId)` call needs `accountUid: accountUid`
   added (the value is already in scope one line earlier).
4. **`lib/app/bindings.dart:90`** (the site `OQ-E13-T02-1` did not name —
   confirmed by `E13-T02`'s reviewer via direct construction-site audit):
   `DeviceIdentityRepository(Get.find<AppDatabase>())` needs a
   `rateLimiter:` argument too. Wiring site 3 alone, without this one,
   leaves `EARS-ABUSE-5` still inert — the singleton
   `DeviceIdentityRepository` the app actually resolves through `Get.find`
   is built here, not at the call site `sign_in_use_case.dart` merely
   invokes.

**Precondition, not a follow-up**: `E13-T01`'s own `OQ-E13-T01-1` (no
eviction/cleanup policy for stale `RateLimitCounters` rows) must be
resolved as PART OF this task, not after it. `E13-T02`'s reviewer probed
this directly: 500 requests with a rotating, attacker-chosen `deviceId`
produced 500 permanent database rows and zero denials — because
`connection_request:$deviceId` is keyed on a value the remote peer
controls, with no cap on distinct keys. Wiring the gate live, without
addressing this first, converts a currently-unreachable storage-
exhaustion vector into a reachable one — trading one open `FR-ABUSE-001`
gap for a different one.

## 3. What this task does
- Threads a real `RateLimiter` into the four sites above.
- Resolves `OQ-E13-T01-1`: adds a bound on `RateLimitCounters` row count
  or a periodic/lazy eviction of expired-window rows (your own
  implementation choice — a simple approach: evict rows whose
  `windowStartMs + window` has already passed, on some natural trigger
  this codebase already has rather than a new polling timer — e.g.
  piggy-back on `allow()`'s own write path, matching the "no new driver"
  discipline `E08-T06`/`E10-T08` established).
- Resolves the UI-conflation finding from `E13-T02`'s review: a
  rate-limited peer currently renders identically to a user-blocked peer
  in `devices_view.dart` (both are `RelationshipState.blocked`). Decide
  and implement a fix — either a distinct outcome the UI can
  distinguish, or an explicit, documented decision that this conflation
  is acceptable (if so, say why in the Run log; do not silently leave it
  unaddressed now that it's reachable).
- Decides the partial-auth-state question `E13-T02`'s review also named:
  `sign_in_use_case.dart` signs in, then writes the device row, then
  calls `markSignedIn` — once the gate is live, a denial between steps 1
  and 3 leaves a user Google-authenticated with no local device identity.
  Decide what happens (retry guidance, a specific error state, etc.) and
  implement it.

## 4. What this task does NOT do
- Does **not** change `RateLimiter.allow`'s core semantics (fixed window,
  the `increment` parameter) — only adds eviction/bounding on top of the
  existing table.
- Does **not** change `EvaluateConnectionRequestUseCase`'s or
  `DeviceIdentityRepository.createDeviceIdentity`'s own gate logic —
  `E13-T02` already built and tested both; this task only supplies real
  arguments at construction time.
- Does **not** re-litigate `RelationshipState.blocked`'s reuse as the
  rate-limit-denial outcome — that decision stands; this task only fixes
  what the UI does with it, if a fix is chosen.
- Does **not** touch any other E13 task's files (T01/T03/T04/T05/T06 are
  all independently scoped and unaffected).

## 5. Contract

### Functions
```yaml
- signature: "(construction-site changes only, per §2/§3 -- no new function signatures of their own beyond whatever eviction mechanism is chosen)"
```

### Data
If eviction requires a schema-visible change (e.g. an index for an
efficient "expired rows" query) — 🧍 `db_schema_migration` gate. If a
purely query-time eviction (delete-then-read, no new column/index)
suffices, no migration is needed; prefer the simpler shape unless a real
performance concern forces the migration.

## 6. Risks
- Getting the `bindings.dart` site wrong (or missing it again) makes this
  task repeat its own reason for existing — audit EVERY construction site
  of both `EvaluateConnectionRequestUseCase` and `DeviceIdentityRepository`
  yourself with a fresh grep, don't trust this task's own enumeration as
  exhaustive without checking.
- An eviction policy that runs too aggressively could evict a bucket
  mid-window and reset a legitimate limit — verify eviction only touches
  rows whose window has genuinely already elapsed.

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS id `46d8814`
- [x] grep-audit every construction site of both gated classes (don't
      trust §2's list as exhaustive) `46d8814`
- [x] wire all 4 (or however many are actually found) sites `46d8814`
- [x] resolve `OQ-E13-T01-1` (eviction/bounding) `46d8814`
- [x] resolve the UI-conflation finding (fix or documented accept) `46d8814`
- [x] resolve the partial-auth-state question `46d8814`
- [x] **Round 2 (post-review) — F1 fix**: stop rate-limiting a returning
      device on every relaunch (see `## Run log` round-2 entry) `ee87c4f`
- [x] **Round 2 (post-review) — F3 fix**: add wiring coverage for
      `bindings.dart`'s `DeviceIdentityRepository` singleton `ee87c4f`
- [x] **Round 3 (post-review) — F6 fix**: move `existingDeviceId()` inside
      `_signIn`'s try block so a Drift/db-read failure is caught instead
      of escaping as an unhandled async error `4707953`

## 8. Acceptance criteria (EARS) + test plan
- **EARS-ABUSE-4/5, now truly closed**: with real production wiring, a
  device id or account over its rate limit is denied through the ACTUAL
  app entry points (`MessagingStack`, `DevicesController`,
  `SignInUseCase`), not just the use-case classes in isolation.
- **EARS-ABUSE-17** (new): `RateLimitCounters` row count SHALL NOT grow
  unbounded under a sustained flood of distinct bucket keys. (FR-ABUSE-001)

### Tests
- `test_EARS_ABUSE_4_wired_messaging_stack_denies_over_limit_peer`
- `test_EARS_ABUSE_5_wired_sign_in_flow_denies_over_limit_registration`
- `test_EARS_ABUSE_17_stale_counter_rows_are_evicted`
- Round 2 (F1 fix, added post-review):
  `test_EARS_ABUSE_5_returning_device_reuses_identity_and_is_never_rate_limited`
  (`sign_in_use_case_test.dart`),
  `test_EARS_ABUSE_5_returning_device_reaches_dashboard_across_N_launches`
  (`login_controller_test.dart`), and the new widget test in
  `widget_test.dart` proving the same through the real
  welcome->login->dashboard navigation.
- Round 2 (F3 fix, added post-review):
  `test_EARS_ABUSE_5_app_binding_wires_a_real_rate_limiter_into_device_identity_repository`
  (new file `test/app/bindings_test.dart`).

### Manual
1. Flood connection requests from one device id in a real running app →
   confirm denial after the configured limit, and confirm the Devices
   screen shows whatever distinguishable outcome was chosen (or confirm
   the accepted-conflation decision holds).

## 9. Self-review + Definition of Done

### Self-review
- [x] Every §7 item done, with commit hashes
- [x] `flutter test && flutter analyze` green — round 2: 1186/1186 tests
      pass (1182 + 4 new); analyze: 0 issues introduced — same 1
      pre-existing unrelated info in
      `test/core/calls/call_migration_controller_test.dart:177`. Round 3:
      1187/1187 (1186 + 1 new); analyze unchanged.
- [x] Diff confined to `files:`; §4 respected — the 5 `lib/` files touched
      in round 1 are exactly the original `files: update` list; round 2
      additionally touches `lib/features/login/presentation/
      login_controller.dart`, now added to `files: update` and disclosed
      as a fence widening directed by the review itself (see §Deviations
      below), not a silent scope change. The 4 test files touched in round
      1 were pre-existing (`files: create: []` respected); round 2 adds
      one new test file, `test/app/bindings_test.dart` — test files are
      not enumerated in this task's `files:` fence at all (only `lib/`
      paths are), and F3 explicitly asked for coverage no existing test
      file's own scope could naturally hold (`bindings.dart`'s composition
      root has no test file of its own anywhere in this codebase).
- [x] Every construction site of both gated classes re-audited via fresh
      grep, not assumed from this task's own list — confirmed exactly 4
      real (non-test) construction/call sites exist in `lib/`, matching
      §2's own enumeration exactly; no 5th site found.
- [x] Round 2: existing `test_EARS_ABUSE_5_wired_sign_in_flow_denies_over
      _limit_registration` (a genuinely new device hitting the cap) still
      passes unmodified, alongside the new returning-device tests — see
      Run log round-2 entry for why the fix's design guarantees this.

**Deviations from spec/design**:
- `devices_view.dart` (the UI-conflation finding's natural fix location)
  is NOT in this task's `files:` fence, and has its own `design_contract:`
  (this task's is `n/a`) — touching it would also need a design-verify
  pass this task isn't scoped for. The fix instead lives entirely in
  `devices_controller.dart` (which IS in the fence): a `blocked` result
  reaching `_onDeviceDiscovered` can only be the rate-limiter's own denial
  (a genuinely-blocked device is already filtered out earlier by the
  `alreadyShown` check against `load()`'s persisted rows), so that result
  is now simply not added to the displayed list at all, rather than shown
  as if the device were blocked. See `devices_controller.dart`'s own
  comment at the fix site for the full reasoning, and
  `test_EARS_ABUSE_4_rate_limited_evaluation_is_not_shown_as_blocked`.
- The partial-auth-state question (task §3) is resolved by documentation
  only in `sign_in_use_case.dart` (its own header + call-site comment),
  not by new code: `LoginController` (outside this task's `files:` fence)
  already has a generic `catch (e)` that logs, resets `signingIn`, and
  does not navigate on any thrown `AppFailure` — the exact behavior needed
  once `device.registration_rate_limited` can be thrown here. No local
  partial state is ever persisted either way (the repository's own
  rate-limit check runs before its write, and a denied `allow()` never
  increments its counter), so there was nothing left to fix in code.

**Files actually touched:**
- `lib/core/messaging/messaging_stack.dart` — wires `RateLimiter(db)` into
  the real `EvaluateConnectionRequestUseCase` construction site.
- `lib/features/devices/presentation/devices_controller.dart` — wires a
  `RateLimiter` (via `Get.find<AppDatabase>()`, `null` if unavailable,
  e.g. in a test with no GetX container) into the default-fallback
  `EvaluateConnectionRequestUseCase`; also fixes the UI-conflation finding.
- `lib/features/login/domain/sign_in_use_case.dart` — passes `accountUid`
  into `createDeviceIdentity`; documents the partial-auth-state decision.
- `lib/app/bindings.dart` — wires `RateLimiter` into the real
  `DeviceIdentityRepository` singleton (`OQ-E13-T02-1`'s missed site).
- `lib/core/abuse/rate_limiter.dart` — resolves `OQ-E13-T01-1`: an
  opportunistic, query-time eviction sweep inside `allow()`'s own
  transaction, no schema change.
- `test/core/abuse/rate_limiter_test.dart`,
  `test/core/messaging/messaging_stack_test.dart`,
  `test/features/devices/presentation/devices_controller_test.dart`,
  `test/features/login/domain/sign_in_use_case_test.dart` — new tests
  added to existing files (`files: create: []` respected).

**Round 2 (post-review) — F1 + F3 fixes:**
- `lib/features/login/domain/sign_in_use_case.dart` — adds
  `existingDeviceId()` (thin passthrough to
  `DeviceIdentityRepository.latestDeviceIdentity()`); `call` now
  recognizes a `deviceId` matching this device's own current latest
  identity as a returning device and skips `createDeviceIdentity`/the
  rate limiter entirely for it.
- `lib/features/login/presentation/login_controller.dart` — `_signIn`
  reads `existingDeviceId()` first and reuses it instead of always
  minting fresh via `generateSecureDeviceId()`.
- `test/features/login/domain/sign_in_use_case_test.dart` — new test
  proving the reuse mechanism directly (10 consecutive calls with the
  same reused id, never denied; existing 6-distinct-ids denial test
  untouched).
- `test/features/login/presentation/login_controller_test.dart` — new
  test driving 6 fresh `LoginController` instances (simulating 6
  relaunches) through a `_TrackingSignInUseCase` spy, proving all 6
  reach the point that navigates to `/dashboard`.
- `test/widget_test.dart` — new widget test proving a returning device
  (5 other devices already at the cap) still reaches `/dashboard` through
  the real welcome->login->dashboard navigation.
- `test/app/bindings_test.dart` (new file, F3) — resolves the real
  `DeviceIdentityRepository` singleton through `AppBinding` + `Get.find`
  and proves its `RateLimiter` is actually wired.

**Round 3 (post-review) — F6 fix:**
- `lib/features/login/presentation/login_controller.dart` — moved the
  `existingDeviceId()` read (and the `deviceId` computation depending on
  it) inside `_signIn`'s existing `try` block, so a thrown error from that
  read (e.g. a Drift/SQLite read failure) is caught by the same `catch (e)`
  that already handles every other failure on this path: `signingIn` is
  set back to `false` and the failure is logged via
  `ObservabilityService.instance.logError` (mapped to
  `'auth.google_sign_in_failed'` for a non-`AppFailure` error, same as any
  other unmapped exception already hit this catch), rather than escaping
  as an unhandled async error.
- `test/features/login/presentation/login_controller_test.dart` — new
  `_ThrowingExistingDeviceIdUseCase` fake plus
  `test_EARS_AUTH_3_existing_device_id_read_failure_completes_signin_instead_of_hanging`,
  proving `_signIn()` now completes (`signingIn` flips back to `false`
  within a 2s timeout instead of hanging forever) and no device identity
  row is written, when `existingDeviceId()` throws.

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id
- [x] Review APPROVE from a **different model** than `executed_by` (rule 5)
      — opus reviewed across 3 rounds (initial, F1/F3 fix, F6 fix), sonnet
      built. Final verdict: APPROVE.
- [x] 🧍 `auth_or_payment_code` gate (this touches `sign_in_use_case.dart`,
      the sign-in path) — human approved 2026-09-06, after 3 review rounds
      and the F1 (lockout) / F6 (unhandled-async) fixes
- [ ] Squash-merged to `epic_13`; tracker + metrics stamped
- [ ] 🧍 Human `verified` (epic-level batches are fine)

## Open Questions
None new — this task exists specifically to close `OQ-E13-T02-1` and
`OQ-E13-T01-1`.

## Feedback log
- 2026-09-05 — Filed as a P1 obligation directly out of `E13-T02`'s
  cross-model review (Opus), which found the built-and-tested rate
  limiter mechanism has zero live production call site and both EARS
  criteria it was meant to satisfy are currently false in the running
  app. Priority **P1**, not P2/P3: `FR-ABUSE-001`'s two vectors are
  fully unprotected until this lands, and the epic must not be
  considered complete with this open (per the reviewer's own explicit
  recommendation to schedule it now rather than let it become a silent
  carry-forward, citing E06's own eight-task precedent for exactly this
  failure mode).

## Run log
- 2026-09-05 — Implemented in worktree `wt-E13-T07` on branch
  `epic_13_task_07`, off `origin/epic_13` (which already has T01-T06
  merged). Commit `46d8814` (`feat(E13-T07): ...`).
- **⚠️ Reviewer note — `auth_or_payment_code` gate applies.** This task
  touches `lib/features/login/domain/sign_in_use_case.dart` (the sign-in
  path): apply `agent/skills/review/references/security.md`'s security
  lens in full and route for 🧍 human sign-off per `harness.yaml`, not
  just a normal code review.
- Fresh grep-audit (task §6's own instruction to distrust its own
  enumeration) confirmed exactly 4 real, non-test construction/call sites
  of `EvaluateConnectionRequestUseCase`/`DeviceIdentityRepository` exist in
  `lib/`, matching §2's own list 1:1 — no 5th site found.
- `OQ-E13-T01-1` resolved with a query-time-only eviction (no schema
  change, no `db_schema_migration` gate needed): every `RateLimiter.allow`
  call now first deletes, inside its own transaction, any
  `RateLimitCounters` row whose window elapsed at least 2 days ago (a
  deliberately generous bound — comfortably larger than the longest
  window any caller in this codebase uses today, the 24h device-
  registration window) — bounding row growth under a flood of distinct,
  rotated bucket keys without ever touching a row that could still matter
  to a live limit. Falsified in both directions:
  `test_EARS_ABUSE_17_stale_counter_rows_are_evicted` (50 stale rows
  reduced to 1 by a single unrelated `allow()` call) and
  `test_EARS_ABUSE_17_eviction_never_touches_a_row_still_within_its_window`
  (a 1h-old row inside a 24h window survives untouched).
- UI-conflation finding resolved inside `devices_controller.dart` alone
  (not `devices_view.dart`, which is outside this task's `files:` fence
  and carries its own design contract) — see §Self-review Deviations for
  the full reasoning and `test_EARS_ABUSE_4_rate_limited_evaluation_is_not_shown_as_blocked`
  for the falsifying test.
- Partial-auth-state question resolved by documentation in
  `sign_in_use_case.dart` — `LoginController`'s existing generic
  `catch (e)` (outside this task's fence) already handles the new
  `AppFailure('device.registration_rate_limited')` safely (no navigation,
  logged, no partial local state persisted either way). See that file's
  own header comment for the full reasoning.
- **Round 2** (F1 fix): `lib/features/login/presentation/login_controller.dart`
  is not in this task's original `files:` fence. The cross-model reviewer
  named it directly as the site of the S1/S2 finding (F1) and the human
  decided the fix direction requires editing it — the reviewer's own
  authorization to widen the fence, not an improvised scope change. Added
  to `files: update` above rather than left silent.
- `flutter test`: 1182/1182 pass. `flutter analyze`: 0 new issues (1
  pre-existing, unrelated info-level finding in
  `test/core/calls/call_migration_controller_test.dart:177`, not touched
  by this task).

### Round 2 — post-review fixes (F1, F3)
- 2026-09-06 — A cross-model review came back CHANGES REQUESTED on
  `46d8814`/`6f66b4b`. Fixed in a fresh worktree off
  `origin/epic_13_task_07`, same branch (no rename), commit `ee87c4f`.
- **F1 (BLOCKING, S1/S2)**: `LoginController._signIn` called
  `generateSecureDeviceId()` unconditionally on every launch and always
  ran through `SignInUseCase`'s registration path — gated, since this
  same task wired the rate limiter in, by `DeviceIdentityRepository`'s
  5-per-24h per-account limit. A normal user opening the app a 6th time
  in a day got silently denied: `LoginController`'s existing generic
  `catch (e)` set `signingIn` back to `false` and never navigated, with
  no error surfaced — stuck on a static "Signing in..." screen.
  - **Human-decided fix direction**: reuse the existing local device
    identity when one exists; only go through registration for genuinely
    new devices. `SignInUseCase.existingDeviceId()` (new) is a thin
    passthrough to `DeviceIdentityRepository.latestDeviceIdentity()` —
    the exact same signal `lib/app/main.dart` already reads to seed
    `selfDeviceId` — so `LoginController` never needs to depend on the
    repository directly. `LoginController._signIn` now calls this first
    and reuses the id it returns instead of minting fresh.
  - `SignInUseCase.call(deviceId)` itself decides whether `deviceId`
    is a returning device: it compares `deviceId` against
    `_repository.latestDeviceIdentity()` (re-read inside `call`, after
    Google sign-in). A match means this is the SAME device's own
    already-registered identity — skip `createDeviceIdentity` (and the
    rate limiter) entirely, `markSignedIn` on the existing row instead.
    A non-match (a freshly-minted id, or a distinct id under test) goes
    through the full registration path unchanged.
  - Deliberately an *equality* check against the CURRENT latest identity,
    not "does any identity exist" — this is what keeps
    `test_EARS_ABUSE_5_wired_sign_in_flow_denies_over_limit_registration`
    (which legitimately registers 6 *distinct* device ids under one
    account, simulating several real devices or an attacker rotating
    ids) passing completely unmodified: none of those calls' device ids
    ever match the immediately-prior call's own id, so every one of them
    still goes through the real registration/rate-limit path and the 6th
    is still denied — confirmed by running that exact test unchanged
    after the fix.
  - New regression coverage, at three levels: `sign_in_use_case_test
    .dart`'s `test_EARS_ABUSE_5_returning_device_reuses_identity_and_is
    _never_rate_limited` (10 consecutive reused-id calls, mechanism
    level); `login_controller_test.dart`'s
    `test_EARS_ABUSE_5_returning_device_reaches_dashboard_across_N
    _launches` (6 fresh `LoginController` instances driven through a
    `_TrackingSignInUseCase` spy that records success/denial from outside
    `LoginController`'s own swallowing catch, proving all 6 reach the
    point that navigates to `/dashboard`); and a new `widget_test.dart`
    test driving the real `welcome -> login -> dashboard` navigation for
    a device with 5 OTHER already-registered devices on the same account
    (the cap already exhausted), proving it still reaches
    `/dashboard` end-to-end.
- **F3 (non-blocking, folded into this round)**: `bindings.dart`'s
  `DeviceIdentityRepository` singleton construction — the exact site
  `OQ-E13-T02-1` originally missed — had zero coverage of the wiring
  itself; reverting `rateLimiter: RateLimiter(...)` back to a bare
  `DeviceIdentityRepository(Get.find<AppDatabase>())` would have passed
  the whole suite before this. New file `test/app/bindings_test.dart`
  builds a real `MessagingStack`, runs `AppBinding(...).dependencies()`,
  resolves `DeviceIdentityRepository` via `Get.find`, and proves 5
  registrations under one account succeed while the 6th, through that
  SAME resolved singleton, is denied.
- F2 (human sign-off gate) and F4/F5 (documented non-blocking
  limitations) intentionally NOT touched this round, per the fix
  instructions — F2 is a human gate, not an implementer's to clear;
  F4/F5 stand as previously documented.
- `flutter test`: 1186/1186 pass (1182 + 4 new: 1 in
  `sign_in_use_case_test.dart`, 1 in `login_controller_test.dart`, 1 in
  `widget_test.dart`, 1 in the new `bindings_test.dart`). `flutter
  analyze`: 0 new issues — same 1 pre-existing, unrelated info-level
  finding in `test/core/calls/call_migration_controller_test.dart:177`.

### Round 3 — post-review fix (F6)
- 2026-09-06 — Round-2 review found one remaining blocking finding, F6:
  `existingDeviceId()` (and the `deviceId` computation depending on it)
  sat OUTSIDE `_signIn`'s `try` block. `onInit()` calls `_signIn()`
  unawaited, so a throw from that read (a real Drift/SQLite read error —
  corrupt or locked db) escaped as an unhandled async error: `signingIn`
  stayed `true` forever (no navigation), and — unlike every other failure
  on this path — nothing was ever logged via `ObservabilityService`. This
  reintroduced F1's exact original symptom (a permanently frozen
  "Signing in..." screen), but as a regression introduced by round 2's
  own F1 fix, and now with no logging at all.
  - Fixed in a fresh worktree (`../wt-E13-T07-f6`) off
    `origin/epic_13_task_07`, same branch (no rename).
  - Fix: moved the `existingDeviceId()`/`deviceId` computation inside the
    existing `try` block. The existing `catch (e)` (unmodified) now
    handles this failure exactly like any other: `signingIn.value = false`
    then `ObservabilityService.instance.logError(e is AppFailure ? e.code
    : 'auth.google_sign_in_failed', cause: e)`. A Drift/db error is not an
    `AppFailure`, so it logs under the generic
    `'auth.google_sign_in_failed'` code — the same fallback code every
    other unmapped exception on this path already used before this fix;
    no new error-code taxonomy was introduced.
  - New regression test in `login_controller_test.dart`:
    `test_EARS_AUTH_3_existing_device_id_read_failure_completes_signin_instead_of_hanging`,
    using a new `_ThrowingExistingDeviceIdUseCase` fake that throws
    directly from `existingDeviceId()`. Proves `_signIn()` completes
    (`signingIn` flips back to `false`, observed via awaiting the
    `signingIn` stream with a 2s timeout so a regression fails the test
    instead of hanging the suite) and that no `DeviceIdentities` row is
    ever written — consistent with every other failure on this path
    (no partial state, no navigation).
  - `flutter test`: 1187/1187 pass (1186 + 1 new). `flutter analyze`: 0
    new issues — same 1 pre-existing, unrelated info-level finding in
    `test/core/calls/call_migration_controller_test.dart:177`.
