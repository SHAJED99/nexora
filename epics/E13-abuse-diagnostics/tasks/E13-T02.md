---
id: E13-T02
epic: E13
type: feature
title: Rate-limit connection-request spam and device-registration abuse
layer: backend
size: S
status: review-requested
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: S, range: "15k-30k" }
priority: { moscow: should, p: P2 }
depends_on: [E13-T01]
blocks: []
traces_to: [FR-ABUSE-001]
required_context:
  - epics/E13-abuse-diagnostics/tasks/E13-T01.md
  - lib/features/trust/domain/evaluate_connection_request_use_case.dart
  - lib/features/login/data/device_identity_repository.dart
design_contract: n/a
files:
  create:
    - test/features/trust/domain/evaluate_connection_request_rate_limit_test.dart
    - test/features/login/data/device_identity_rate_limit_test.dart
  update:
    - lib/features/trust/domain/evaluate_connection_request_use_case.dart
    - lib/features/login/data/device_identity_repository.dart
  delete: []
started_at: 2026-09-05
completed_at: 2026-09-05
executed_by: sonnet (claude code, builder role)
reviewed_at:
reviewed_by:
review_outcome:
---
# E13-T02 · Rate-limit connection-request spam and device-registration abuse

## 1. Goal
Close two of `FR-ABUSE-001`'s eight named vectors: a device id that spams
connection requests, and an account that spams new-device registrations.

## 2. Business logic
`FR-ABUSE-001`. Today, `EvaluateConnectionRequestUseCase.call` (per
`E02-T0x`'s own contract, unchanged by this task) is a pure lookup: it reads
whatever `RelationshipState` is already stored for a `deviceId` and defaults
to `unknown` if none exists — nothing throttles how OFTEN it may be called
for the same or many device ids. `DeviceIdentityRepository.createDeviceIdentity`
is likewise an unthrottled write.

This task adds an admission check BEFORE each of those two existing
operations, using `E13-T01`'s `RateLimiter`, and denies (rather than
silently dropping) when a caller is over its limit — the caller (whichever
UI/controller invokes these) decides what "denied" looks like to a user;
this task's own contract is the boolean gate only.

Bucket keys (this task's own decision, per `E13-T01` §4): 
`"connection_request:<deviceId>"` for connection requests,
`"device_registration:<accountUid>"` for device registration — keyed by the
REMOTE device id in the first case (limits how often any single peer can be
evaluated) and by the LOCAL account uid in the second (limits how many
devices one account can register in a window), matching each vector's own
actual abuse shape.

## 3. What this task does
- `EvaluateConnectionRequestUseCase.call` gains a `RateLimiter` dependency
  (constructor-injected, matching this codebase's existing DI pattern for
  every other use case) and calls `allow('connection_request:$deviceId',
  maxCount: <chosen>, window: <chosen>)` before doing its existing lookup;
  returns a deny-shaped result (mirroring the existing `blocked`/`unknown`
  result shape, not a thrown exception — a rate-limited caller is not a
  programming error) when denied.
- `DeviceIdentityRepository.createDeviceIdentity` gains the same dependency
  and the same admission check keyed by `accountUid`, called before its
  existing `_db.createDeviceIdentity` write; throws (or returns a mapped
  failure, matching `sign_in_use_case.dart`'s existing error-mapping
  convention — follow whatever that file already does, do not invent a new
  error shape) when denied.
- Concrete limit numbers and window durations: this task's own decision at
  implementation time, informed by realistic legitimate use (a user
  reasonably re-requesting a connection a few times after a mesh dropout;
  a user reasonably registering 2-3 devices in a sitting) — document the
  chosen numbers and the reasoning in the Run log, not silently guessed.

## 4. What this task does NOT do
- Does **not** change `RelationshipRepository`, `RelationshipState`, or any
  trust/block semantics — this task only gates HOW OFTEN the existing
  lookup runs, never WHAT it returns when it does run.
- Does **not** change what a UI/controller does when denied — that is
  whichever screen calls these (out of this task's `files:` fence); this
  task only makes "denied" a reachable outcome these callers must now
  handle (flag as a Deviation if a caller's existing exhaustive
  switch/error-mapping breaks because of the new outcome — do not silently
  swallow it into an existing case).
- Does **not** implement device-registration abuse via Firebase (i.e. does
  not touch `FirebaseMetadataService`/`database.rules.json`) — this is a
  purely LOCAL admission check before the local Drift write; a
  Firebase-side registration-abuse control (if ever wanted) is a distinct,
  separate task and not silently folded in here.
- Does **not** add any UI copy, error screen, or design contract — no
  `layer: frontend` surface is touched.

## 5. Contract

### Functions
```yaml
- signature: "EvaluateConnectionRequestUseCase.call(deviceId: String) -> Future<ConnectionRequestResult>"
  params: { deviceId: "the remote peer's device id being evaluated" }
  returns: "unchanged result type; gains a rate-limited/denied outcome as a new reachable case, exact shape matching this task's existing result type's convention"
  purpose: "adds a RateLimiter.allow('connection_request:$deviceId', ...) gate before the existing lookup logic"
- signature: "DeviceIdentityRepository.createDeviceIdentity(deviceId: String, accountUid: String) -> Future<int>"
  params: { deviceId: "the new device id being registered", accountUid: "the authenticated account creating it" }
  returns: "unchanged on success; throws/maps to a failure (match sign_in_use_case.dart's existing convention) when the accountUid is over its registration-rate limit"
  purpose: "adds a RateLimiter.allow('device_registration:$accountUid', ...) gate before the existing write"
```
Note: `createDeviceIdentity`'s current signature (per `lib/features/login/data/device_identity_repository.dart`) takes only `deviceId` — verify at execution time whether `accountUid` is already available at this call site (via `sign_in_use_case.dart`) or needs threading through; do not invent a global/singleton account-uid lookup if it isn't already passed down this call chain.

## 6. Risks
- `EvaluateConnectionRequestUseCase`'s current callers (find them via a
  repo-wide reference search before starting) may assume this call never
  fails/never returns anything but the existing states — a new
  rate-limited outcome is a breaking change to every exhaustive `switch` on
  its result type. Audit every call site, don't just add the case and hope.
- Fixed-window counting means a legitimate burst right at a window boundary
  (e.g. 2 requests just before rollover + 2 just after) can total more than
  `maxCount` within a short real-time span even though each window
  individually stayed under it — acceptable per `E13-T01`'s own scope (task
  §2, "simplest correct primitive"), not a defect to fix here.

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS id `257ea8d` — both new
      test files reference the new `rateLimiter`/`accountUid` constructor
      parameters that did not exist on `main` before this task, so they
      failed to compile against pre-task code (same "code under test
      doesn't exist yet" red `E13-T01` already documented for this
      primitive), then passed once the two source files were updated in
      the same commit
- [x] audit `EvaluateConnectionRequestUseCase`'s call sites for exhaustive
      result-type handling `257ea8d` — real (non-test, non-doc) call sites:
      `lib/core/messaging/messaging_stack.dart` (constructs the use case at
      line 275, and only ever checks `== RelationshipState.blocked` to
      refuse — not an exhaustive switch, so a `blocked` rate-limit outcome
      is handled identically to a real block, no change needed) and
      `lib/features/devices/presentation/devices_controller.dart`
      (constructs a default instance at line 48, stores whatever state
      comes back into its display list with no switch at all). Neither
      file is in this task's `files:` fence, so neither was edited.
- [x] wire `RateLimiter` into `EvaluateConnectionRequestUseCase.call`
      `257ea8d`
- [x] wire `RateLimiter` into `DeviceIdentityRepository.createDeviceIdentity`
      `257ea8d`
- [x] choose and document concrete `maxCount`/`window` values (Run log)
      `257ea8d`

## 8. Acceptance criteria (EARS) + test plan
- **EARS-ABUSE-4**: WHEN a device id has exceeded its configured
  connection-request rate limit, THE system SHALL deny evaluation without
  consulting `RelationshipRepository`. (FR-ABUSE-001)
- **EARS-ABUSE-5**: WHEN an account has exceeded its configured
  device-registration rate limit, THE system SHALL deny the registration
  write. (FR-ABUSE-001)

### Tests
- `test_EARS_ABUSE_4_connection_request_over_limit_is_denied`
- `test_EARS_ABUSE_4_connection_request_under_limit_still_evaluates_normally`
- `test_EARS_ABUSE_5_device_registration_over_limit_is_denied`

### Manual
None — no UI surface.

## 9. Self-review + Definition of Done

### Self-review
- [x] Every §7 item done, with commit hashes
- [x] `flutter test && flutter analyze` green (1165 tests passed, 0 failed;
      `flutter analyze` reports only one pre-existing, unrelated info-level
      finding in `test/core/calls/call_migration_controller_test.dart:177`,
      not touched by this task — same finding `E13-T01`'s own self-review
      already noted)
- [x] Diff confined to `files:`; §4 respected
- [x] Every call site of both changed functions still compiles and handles
      the new outcome — see Deviations below: both new dependencies are
      OPTIONAL, so the three existing production call sites
      (`messaging_stack.dart`, `devices_controller.dart`,
      `sign_in_use_case.dart`) compile completely unchanged and their
      behavior is byte-for-byte identical to before this task; only
      callers that explicitly opt in (this task's own new tests) exercise
      the new gate

**Deviations from spec/design:**
- **Both new dependencies (`RateLimiter` on `EvaluateConnectionRequestUseCase`,
  `RateLimiter` + `accountUid` on `DeviceIdentityRepository.createDeviceIdentity`)
  are OPTIONAL, not required, and this task's real production call sites are
  NOT wired to pass them.** Reason: completing that wiring would require
  editing `lib/core/messaging/messaging_stack.dart` (constructs
  `EvaluateConnectionRequestUseCase(RelationshipRepository(db))`),
  `lib/features/devices/presentation/devices_controller.dart` (constructs a
  default instance with no `db` in scope to build a `RateLimiter` from),
  and `lib/features/login/domain/sign_in_use_case.dart` (has `accountUid` in
  hand at its `createDeviceIdentity(deviceId)` call site but does not pass
  it) — none of which are in this task's `files:` list, and the builder
  role's own constitution (rule 6, `agent/agents/builder.md` "You never:
  Touch files outside the task's `files:` list") forbids editing them
  regardless. So: the admission-check MECHANISM is fully implemented,
  tested, and wired into both named functions exactly as §3 specifies, but
  the three real call sites that would need a one-line edit each to
  actually invoke it in production are left unchanged — this task closes
  the mechanism, not yet the production wiring. Flagging as a follow-up
  task (see Open Questions below) rather than silently declaring the
  vectors closed.
- **`RelationshipState.blocked` reused for the rate-limited outcome**
  (§3's "mirroring the existing blocked/unknown result shape"): chosen over
  `unknown` because `MessagingStack` (the one real caller with
  block-sensitive behavior) only refuses on `== RelationshipState.blocked`
  — reusing `unknown` would have let a rate-limited caller's connection
  proceed as if merely unseen. This never writes to the `relationships`
  table, so no persisted state is affected; a UI happening to display a
  transient rate-limited evaluation as "Blocked" is the kind of caller-side
  display decision task §4 explicitly leaves to whichever screen calls
  this (out of fence).
- **`prefer_initializing_formals` lint**: both new optional named
  parameters are declared as `{this._rateLimiter}` / already-final field
  initializing formals directly, per `flutter analyze`'s own guidance —
  no separate deviation, just documenting the small mechanical fix.

**Files actually touched:**
- `lib/features/trust/domain/evaluate_connection_request_use_case.dart`
  (updated)
- `lib/features/login/data/device_identity_repository.dart` (updated)
- `test/features/trust/domain/evaluate_connection_request_rate_limit_test.dart`
  (new)
- `test/features/login/data/device_identity_rate_limit_test.dart` (new)

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id
- [ ] Review APPROVE from a **different model** than `executed_by` (rule 5)
- [ ] Squash-merged to `epic_13`; tracker + metrics stamped
- [ ] 🧍 Human `verified` (epic-level batches are fine)

## Open Questions
None new — concrete limit numbers are this task's own implementation
decision (§3), not a spec gap.

- **OQ-E13-T02-1 — production call sites still need a one-line wire-up.**
  This task implemented and tested the admission-check mechanism on both
  named functions per §3/§7, but left the three real production call
  sites unwired (see §9 Deviations for the full reasoning): `messaging_stack.dart`
  needs `EvaluateConnectionRequestUseCase(RelationshipRepository(db),
  rateLimiter: RateLimiter(db))` at its construction site;
  `devices_controller.dart`'s default-fallback path needs a `db` (or a
  pre-built `RateLimiter`) threaded into `DevicesController`'s own
  constructor to build a real default; `sign_in_use_case.dart` needs its
  existing `createDeviceIdentity(deviceId)` call changed to
  `createDeviceIdentity(deviceId, accountUid: accountUid)` (the value is
  already in scope there, one line earlier). None of those three files are
  in this task's `files:` fence.
  - **Status:** 🟡 open
  - **Answer:** _<empty — fill here>_
  - **Answered by:** _<empty>_
  - **Date:** _<empty>_

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- 2026-09-05, builder (sonnet): read `E13-T01.md`, `rate_limiter.dart`,
  `evaluate_connection_request_use_case.dart`,
  `device_identity_repository.dart`, and `sign_in_use_case.dart` in full
  before starting, per this task's own `required_context` and §5 note.
  Grepped every real (non-test, non-doc) reference to
  `EvaluateConnectionRequestUseCase` and `createDeviceIdentity` —
  confirmed exactly two production callers of the use case
  (`lib/core/messaging/messaging_stack.dart:275`,
  `lib/features/devices/presentation/devices_controller.dart:48`) and one
  production caller of `createDeviceIdentity`
  (`lib/features/login/domain/sign_in_use_case.dart:42`), none of which are
  in this task's `files:` list.
  - **Chosen limits** (§3, this task's own decision):
    - `connection_request:<deviceId>` — `maxCount: 10`, `window: 1 minute`.
      Reasoning: a legitimate mesh-dropout retry is a handful of
      re-evaluations in quick succession, not sustained polling; 10/minute
      gives generous headroom above that while still bounding a device
      that spams evaluation requests for the same peer.
    - `device_registration:<accountUid>` — `maxCount: 5`, `window: 24
      hours`. Reasoning: task §3's own example of legitimate use is "2-3
      devices in a sitting"; 5/day leaves clear room above that (e.g. a
      phone + a tablet + a reinstall the same day) while still bounding an
      account that scripts repeated device registration.
  - **Constructor-signature constraint discovered mid-task**: making
    `RateLimiter`/`accountUid` REQUIRED parameters would have broken
    compilation at all three production call sites above, none of which
    are in this task's `files:` fence and so cannot be edited (rule 6 /
    `agent/agents/builder.md`'s "You never: Touch files outside the
    task's `files:` list"). Resolved by making both new parameters
    OPTIONAL (named, nullable): when omitted, the gate is skipped and
    behavior is byte-for-byte identical to pre-task code — verified by a
    dedicated test in each new test file
    (`test_evaluate_connection_request_without_rate_limiter_is_unaffected`,
    `test_device_registration_without_account_uid_is_unaffected`) that
    exercises the exact call shape those three production sites use. This
    means the admission-check MECHANISM is fully implemented and tested on
    both named functions per §3/§7's checklist, but the three real call
    sites still need a one-line edit each (outside this task's fence) to
    actually invoke it — logged as OQ-E13-T02-1 above, not silently
    treated as done.
  - Wrote both new test files FIRST, referencing the not-yet-existing
    `rateLimiter`/`accountUid` constructor parameters (compile-red, same
    "code under test doesn't exist yet" shape `E13-T01`'s own Run log
    documents), then implemented the gate in both files in the same
    commit. Reused `RelationshipState.blocked` for the rate-limited
    connection-request outcome (reasoning in §9 Deviations) rather than
    adding a new enum value (§4 forbids changing `RelationshipState`).
    Reused the existing `AppFailure(code, {cause})` envelope
    (`core/auth/google_auth_service.dart`) for the device-registration
    denial, matching `sign_in_use_case.dart`'s existing convention of
    letting `AppFailure`s propagate uncaught.
  - `flutter test`: **1165 passed, 0 failed** (full suite). `flutter
    analyze`: 1 pre-existing, unrelated info-level finding only
    (`test/core/calls/call_migration_controller_test.dart:177`, not
    touched by this task — same finding noted in `E13-T01`'s own
    self-review). Fixed two new `prefer_initializing_formals` info-level
    findings this task introduced by switching both new optional params to
    initializing-formal syntax (`{this._rateLimiter}`).
  - Commit `257ea8d` (implementation + both new test files). This tracker
    update is a separate, immediately-following commit on the same branch.
    Pushed `epic_13_task_02` to `origin`. Left `status:
    review-requested`; `## Open Questions` OQ-E13-T02-1 (production
    call-site wiring) left open, flagged as this task's own scope boundary
    per the `files:` fence, not a spec gap requiring a blocked status.
