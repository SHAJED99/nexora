---
id: E13-T06
epic: E13
type: feature
title: Real observability client (Sentry-or-equivalent) replacing the console-log stub
layer: backend
size: M
status: review-requested
owner_agent: builder
model: sonnet
preferred_agent: any
token_estimate: { tier: M, range: "50k-150k" }
priority: { moscow: should, p: P2 }
depends_on: []
blocks: []
traces_to: [FR-DIAG-001, FR-DIAG-002]
required_context:
  - agent/memory/decisions/ADR-0006-observability-services.md
  - lib/core/observability/observability_service.dart
  - docs/conventions.md
design_contract: n/a
files:
  create:
    - test/core/observability/observability_service_test.dart
  update:
    - lib/core/observability/observability_service.dart
    - pubspec.yaml
    - docs/conventions.md
  delete: []
started_at: 2026-09-05
completed_at:
executed_by: claude-sonnet-5 (builder)
reviewed_at:
reviewed_by:
review_outcome:
---
# E13-T06 · Real observability client (Sentry-or-equivalent) replacing the console-log stub

## 1. Goal
Give the team actual field visibility into crashes/errors (`ADR-0006`,
`NFR-REL-001`), replacing the genesis stub that only `print()`s in debug
builds and reports nothing in production.

## 2. Business logic
`FR-DIAG-001`/`FR-DIAG-002`, `ADR-0006` (accepted: a dedicated,
privacy-conscious crash/error tool, e.g. Sentry; no analytics/usage
tracking for v1). `ObservabilityService` (`lib/core/observability/
observability_service.dart`, 23 lines) is the ONE existing call surface
every other service in this codebase already calls (`logError`,
`log(LogLevel, code, {cause})`) — this task swaps what happens INSIDE
`init()`/`log()`, it does not change either method's signature, so no
other file's call site changes.

`ADR-0006` explicitly leaves the specific vendor (Sentry vs. an
equivalent) as an implementation detail for this task, not a new
foundational choice — but adding ANY new SDK dependency is still a 🧍
rule-3 `new_dependency` gate at `pubspec.yaml` (`docs/conventions.md`'s own
"requires a human-approved addition" rule). This task's own `files:` list
already includes `pubspec.yaml` — the gate is a real precondition on
merging this task, not a blocker on sharding it.

## 3. What this task does
- Pick a concrete vendor (Sentry is the ADR's own named example; `sentry_flutter`
  is the standard Flutter package) — this task's own decision, subject to
  the pubspec 🧍 gate at merge time, not before.
- `ObservabilityService.init()` initializes the real client (DSN from a
  build-time config, never hardcoded — follow whatever config-injection
  convention this codebase already uses for other secrets/keys, verify at
  execution time before inventing one).
- `ObservabilityService.log(LogLevel, code, {cause})` forwards to the real
  client's error/breadcrumb API instead of (or in addition to, for debug
  builds) the existing `print()`.
- Record the vendor pick in `docs/conventions.md`, per `ADR-0006`'s own
  closing line ("`docs/conventions.md` (T02) can record the pick").
- Every existing call site (`logError` used throughout `lib/core/services/`,
  `lib/features/`, etc. — dozens of them, unchanged) keeps working
  unmodified, since neither public method's signature changes.

## 4. What this task does NOT do
- Does **not** add usage/behavior analytics — `ADR-0006`'s decision is
  crash/error reporting only, explicitly not analytics for v1.
- Does **not** change `LogLevel`'s enum values or `logError`'s signature —
  every existing call site across the codebase must compile unmodified.
- Does **not** audit every existing `logError`/`log` call site's `cause`
  argument for FR-DIAG-002 compliance (no plaintext/keys/voice/location) —
  that is a separate, cross-cutting audit this task does not scope-creep
  into; flag as a follow-up bug if a violation is noticed in passing, don't
  silently fix call sites outside this task's `files:` fence.
- Does **not** decide self-hosted vs. managed hosting for the chosen
  vendor — `ADR-0006`'s own closing line leaves this as a `docs/
  conventions.md` implementation note, this task's own call, not a new
  rule-3 decision, but still document the choice made.
- Does **not** wire crash reporting into native (Kotlin) code — Dart-side
  `ObservabilityService` only; a native crash reporter (if ever wanted) is
  a distinct, separate task.

## 5. Contract

### External services & flags
Service: the chosen crash/error vendor (Sentry or equivalent) — 🧍 new
dependency, human gate at `pubspec.yaml`. Env var/config: DSN, injected at
build time, never committed as a literal string in source.

### Functions
```yaml
- signature: "ObservabilityService.init() -> Future<void>"
  params: {}
  returns: "unchanged signature; internally initializes the real client instead of a no-op"
  purpose: "existing method, new implementation"
- signature: "ObservabilityService.log(level: LogLevel, code: String, {Object? cause}) -> void"
  params: { level: "unchanged", code: "unchanged", cause: "unchanged" }
  returns: "unchanged signature; internally forwards to the real client's error/breadcrumb API"
  purpose: "existing method, new implementation -- every existing call site keeps compiling"
```

## 6. Risks
- A crash/error SDK's own instrumentation (network requests, native
  hooks) must itself never transmit anything FR-DIAG-002 forbids — verify
  the chosen SDK's default breadcrumb/context collection doesn't
  auto-capture something like device location or clipboard content by
  default; disable any such default collection explicitly rather than
  assuming it's off.
- Every existing `logError`/`log` call site across the codebase already
  assumes calls are fire-and-forget and never throw — the new
  implementation must preserve this (never let a network failure from the
  observability SDK itself propagate as an exception to a caller that
  didn't expect one).

## 7. Implementation checklist
- [x] tests written FIRST and failing, named by EARS id `33799b6`
- [x] pick vendor, add to `pubspec.yaml` (🧍 gate) — human approved
      `sentry_flutter` 2026-09-05; added `sentry_flutter: 8.14.2` (pinned
      exact, per the `geolocator` convention) to `pubspec.yaml`, ran
      `flutter pub get` (`pubspec.lock` updated) `<pending-commit>`
- [x] `ObservabilityService.init()`: internal client-injection seam built
      (`ObservabilityClient`) `33799b6`; real vendor client
      (`SentryObservabilityClient`) now wired in as the default, calling
      `SentryFlutter.init` with the privacy-locked options `<pending-commit>`
- [x] `ObservabilityService.log()`: real forwarding through the
      `ObservabilityClient` seam, preserving fire-and-forget semantics
      (EARS-DIAG-3, tested) `33799b6`; now forwards to
      `SentryObservabilityClient.capture` (mirrors to console in debug
      builds, "in addition to" per §3) `<pending-commit>`
- [x] record the vendor pick in `docs/conventions.md` (PROPOSED) `33799b6`;
      updated to human-approved + hosting choice (managed Sentry SaaS) +
      DSN/build-time-config convention + disabled-option list
      `<pending-commit>`
- [x] audit the chosen SDK's default data collection against FR-DIAG-002 —
      `sendDefaultPii`, `attachScreenshot`, `attachViewHierarchy`,
      `enableUserInteractionBreadcrumbs`, `enableAutoNativeBreadcrumbs` all
      explicitly disabled in `SentryObservabilityClient.configurePrivacyOptions`
      (reasoning inline per setting); `test_EARS_DIAG_1_*` un-skipped and
      asserts against the real `SentryFlutterOptions` object `<pending-commit>`

## 8. Acceptance criteria (EARS) + test plan
- **EARS-DIAG-1**: THE system SHALL never log message plaintext, private/
  session keys, voice content, or sensitive personal/location data, via
  `ObservabilityService` or the chosen vendor's own default instrumentation.
  (FR-DIAG-002 — the epic-level criterion this task is the one place it
  becomes mechanically checkable: every OTHER file's own `logError`/`log`
  call sites already existed before this task and are each individually
  responsible for what they pass as `cause` — task §4 explicitly does not
  audit them — but §6 Risks' vendor-default-collection check IS this
  task's own new surface area, and is where this criterion is tested here.)
- **EARS-DIAG-2**: WHEN `ObservabilityService.log` is called with
  `LogLevel.error`, THE system SHALL forward the event to the configured
  crash/error client. (FR-DIAG-001)
- **EARS-DIAG-3**: IF the observability client itself fails (e.g. network
  error sending the event), THEN `log`/`logError` SHALL NOT throw to the
  caller. (FR-DIAG-001, matches every existing best-effort pattern in this
  codebase)

### Tests
- `test_EARS_DIAG_1_chosen_vendor_default_instrumentation_disables_pii_collection`
  (asserts the chosen SDK's own default breadcrumb/context collection
  settings match whatever this task explicitly disabled per §6 Risks —
  exact assertion shape depends on which vendor is picked, confirm at
  implementation time)
- `test_EARS_DIAG_2_error_level_log_forwards_to_client`
- `test_EARS_DIAG_3_client_failure_does_not_propagate`
- `test_existing_logError_call_sites_still_compile_unmodified` (a
  compile-time proof, not a runtime assertion — the existing signature is
  unchanged)

### Manual
1. Trigger a real error in a debug build with the DSN configured → confirm
   it appears in the vendor's dashboard.

## 9. Self-review + Definition of Done

### Self-review
- [x] Every §7 item done, with commit hashes — all five items complete,
      the two previously-blocked items unblocked by the human-approved
      `sentry_flutter` dependency
- [x] `flutter test && flutter analyze` green — full suite:
      `+1157: All tests passed!` (was `+1156 ~1`; `test_EARS_DIAG_1_*` is
      now un-skipped and passing, not just present); `flutter analyze`:
      1 pre-existing info (`test/core/calls/call_migration_controller_test.dart:177`,
      unrelated to this task, not in `files:`) — identical to the prior run,
      no new issues (an `experimental_member_use` warning on
      `attachViewHierarchy` was suppressed with an inline `ignore:` since
      the field itself, not just the warning, is deliberately used)
- [x] Diff confined to `files:` (+ `pubspec.lock`, implied by the
      `pubspec.yaml` change); §4 respected — no analytics/usage tracking
      added; `LogLevel`/`logError`/`log`/`init` signatures unchanged
- [x] No secrets (DSN) committed as a literal string — DSN is read via
      `String.fromEnvironment('SENTRY_DSN')` (`--dart-define` build-time
      config), never hardcoded; grep-verified no literal DSN string exists
      anywhere in the diff
- [x] Every existing `logError`/`log` call site across `lib/` still
      compiles unmodified — grep-verified (8 files outside
      `observability_service.dart` call `ObservabilityService`/`logError`),
      `flutter analyze` clean, plus
      `test_existing_logError_call_sites_still_compile_unmodified` exercises
      the real singleton (now defaulting to `SentryObservabilityClient`)
      against the same call shapes, and the full suite (`+1157`) exercises
      every one of those call sites in its own tests
- [x] §6 Risks addressed: default SDK instrumentation that could leak PII
      is explicitly disabled (`sendDefaultPii`, `attachScreenshot`,
      `attachViewHierarchy`, `enableUserInteractionBreadcrumbs`,
      `enableAutoNativeBreadcrumbs`), asserted by `test_EARS_DIAG_1_*`
      against the real `SentryFlutterOptions` object the client configures;
      client failures still never propagate (EARS-DIAG-3, unchanged
      try/catch in `ObservabilityService`)

**Deviations from spec/design:** none remaining — the prior deviation
(`pubspec.yaml` not modified pending the human gate) is resolved now that
the gate has cleared.

**Files actually touched (this completion):**
- `pubspec.yaml` — added `sentry_flutter: 8.14.2` (pinned exact)
- `pubspec.lock` — updated by `flutter pub get` (implied by the manifest
  change, per rule 3)
- `lib/core/observability/observability_service.dart` — added
  `SentryObservabilityClient implements ObservabilityClient`, wired in as
  `ObservabilityService`'s default; removed the now-superseded
  `_ConsoleObservabilityClient` (its debug-console behaviour is preserved,
  folded into `SentryObservabilityClient.capture`)
- `test/core/observability/observability_service_test.dart` — un-skipped
  and implemented `test_EARS_DIAG_1_*` against the real `SentryFlutterOptions`
- `docs/conventions.md` — vendor pick updated from PROPOSED to
  human-approved, with hosting choice, DSN convention, and disabled-option
  list recorded; dependency logged in "Third-party dependency additions"
- `epics/E13-abuse-diagnostics/tasks/E13-T06.md` (this file)

### Definition of Done
- [x] Every §8 criterion passes via a test named by its EARS id —
      EARS-DIAG-1/2/3 all pass (`test_EARS_DIAG_1_*`,
      `test_EARS_DIAG_2_*`, `test_EARS_DIAG_3_*`)
- [ ] Review APPROVE from a **different model** than `executed_by` (rule 5)
- [ ] Squash-merged to `epic_13`; tracker + metrics stamped
- [ ] 🧍 Human `verified` (epic-level batches are fine)
- [x] 🧍 Human approval of the new `pubspec.yaml` dependency (rule 3) —
      approved 2026-09-05, `sentry_flutter` 8.14.2

## Feedback log
- (human feedback on this deliverable lands here)

## Run log
- 2026-09-05 (claude-sonnet-5, builder, worktree `wt-E13-T06`, branch
  `epic_13_task_06` based on `epic_13` with E13-T01 merged): wrote
  `test/core/observability/observability_service_test.dart` first, confirmed
  red (compile error — `ObservabilityClient`/`ObservabilityService.withClient`
  did not exist yet). Implemented the `ObservabilityClient` injection seam in
  `lib/core/observability/observability_service.dart` — `init()`/`log()`/
  `logError()` signatures unchanged. Confirmed green: `flutter test`
  `+1156 ~1: All tests passed!` (project-wide), `flutter analyze` clean
  (1 pre-existing unrelated info). Verified 16 existing `logError`/`log`/
  `ObservabilityService.` call sites outside this file still compile
  unmodified (grep + analyze + the compile-proof test). Recorded the
  `sentry_flutter` vendor pick as PROPOSED (not approved) in
  `docs/conventions.md`. Did NOT touch `pubspec.yaml` — the
  `new_dependency` 🧍 gate — per rule 3; status set to `blocked`, not
  `review-requested`. Pushed `epic_13_task_06` to origin.
- 2026-09-05 (claude-sonnet-5, builder, same worktree/branch, resumed after
  human approval of `sentry_flutter`): added `sentry_flutter: 8.14.2`
  (pinned exact) to `pubspec.yaml`, ran `flutter pub get`
  (`pubspec.lock` updated). Wrote `SentryObservabilityClient implements
  ObservabilityClient` in `lib/core/observability/observability_service.dart`:
  `init()` calls `SentryFlutter.init` with a DSN read from
  `String.fromEnvironment('SENTRY_DSN')` (build-time `--dart-define`, no
  existing config-injection convention found elsewhere in the codebase to
  reuse); `capture()` maps `LogLevel` to `SentryLevel` and calls
  `Sentry.captureException`/`captureMessage`, mirroring to console in debug
  builds ("in addition to", per §3). Extracted the options-configuration
  logic into `@visibleForTesting static configurePrivacyOptions` so
  `test_EARS_DIAG_1_*` can assert on the real `SentryFlutterOptions` object
  without going through `SentryFlutter.init`'s native platform-channel
  bootstrap (which the `flutter_test` VM environment can't exercise).
  Explicitly disabled `sendDefaultPii`, `attachScreenshot`,
  `attachViewHierarchy`, `enableUserInteractionBreadcrumbs`,
  `enableAutoNativeBreadcrumbs` (each commented with why, per §6 Risks/
  FR-DIAG-002); left `tracesSampleRate` at 0.0 (no analytics/performance
  tracing, per ADR-0006). Removed the now-superseded
  `_ConsoleObservabilityClient` class (its debug-only console print is
  folded into `SentryObservabilityClient.capture` instead, so no behaviour
  is lost). Un-skipped `test_EARS_DIAG_1_*` with a real assertion against
  `configurePrivacyOptions`'s output. Updated `docs/conventions.md`:
  vendor pick promoted from PROPOSED to human-approved, hosting choice
  (managed Sentry SaaS) recorded, DSN/build-time-config convention
  documented, dependency logged in "Third-party dependency additions".
  Grep-verified 8 files outside `observability_service.dart` call
  `ObservabilityService`/`logError`/`log` — all unchanged call shapes.
  `flutter analyze`: initially surfaced 2 new `experimental_member_use`
  warnings on `attachViewHierarchy` (marked experimental in
  `sentry_flutter` 8.14.2); suppressed with inline `// ignore:` comments in
  both the source and the test since the field itself is deliberately used,
  not a misuse — back to the same single pre-existing unrelated info as
  before. `flutter test`: `+1157: All tests passed!` (full suite; was
  `+1156 ~1`, the `~1` skip is now a real pass). Status →
  `review-requested`. Open Questions/Handoff sections removed — no longer
  blocking. Pushed `epic_13_task_06` to origin.
