// features/welcome/data — no repository yet: the welcome screen holds no
// state of its own. Kept here, empty, so the folder convention
// (docs/conventions.md) holds for the next feature that needs one.
