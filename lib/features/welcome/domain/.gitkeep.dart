// features/welcome/domain — no use case yet: the welcome screen only
// navigates to /login on tap (the real work happens there — see
// features/login/domain/sign_in_use_case.dart). Kept here, empty, so the
// folder convention (docs/conventions.md) holds for the next feature that
// needs one.
