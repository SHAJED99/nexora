// Autogenerated from Pigeon (v28.0.0), do not edit directly.
// See also: https://pub.dev/packages/pigeon
// ignore_for_file: unused_import, unused_shown_name
// ignore_for_file: type=lint

import 'dart:async';
import 'dart:typed_data' show Float64List, Int32List, Int64List;

import 'package:flutter/services.dart';
import 'package:meta/meta.dart' show immutable, protected, visibleForTesting;

Object? _extractReplyValueOrThrow(
    List<Object?>? replyList,
    String channelName, {
    required bool isNullValid,
}) {
  if (replyList == null) {
    throw PlatformException(
      code: 'channel-error',
      message: 'Unable to establish connection on channel: "$channelName".',
    );
  } else if (replyList.length > 1) {
    throw PlatformException(
      code: replyList[0]! as String,
      message: replyList[1] as String?,
      details: replyList[2],
    );
  } else if (!isNullValid && (replyList.isNotEmpty && replyList[0] == null)) {
    throw PlatformException(
      code: 'null-error',
      message: 'Host platform returned null value for non-null return value.',
    );
  }
  return replyList.firstOrNull;
}


List<Object?> wrapResponse({Object? result, PlatformException? error, bool empty = false}) {
  if (empty) {
    return <Object?>[];
  }
  if (error == null) {
    return <Object?>[result];
  }
  return <Object?>[error.code, error.message, error.details];
}
bool _deepEquals(Object? a, Object? b) {
  if (identical(a, b)) {
    return true;
  }
  if (a is double && b is double) {
    if (a.isNaN && b.isNaN) {
      return true;
    }
    return a == b;
  }
  if (a is List && b is List) {
    return a.length == b.length &&
        a.indexed
            .every(((int, dynamic) item) => _deepEquals(item.$2, b[item.$1]));
  }
  if (a is Map && b is Map) {
    if (a.length != b.length) {
      return false;
    }
    for (final MapEntry<Object?, Object?> entryA in a.entries) {
      bool found = false;
      for (final MapEntry<Object?, Object?> entryB in b.entries) {
        if (_deepEquals(entryA.key, entryB.key)) {
          if (_deepEquals(entryA.value, entryB.value)) {
            found = true;
            break;
          } else {
            return false;
          }
        }
      }
      if (!found) {
        return false;
      }
    }
    return true;
  }
  return a == b;
}

int _deepHash(Object? value) {
  if (value is List) {
    return Object.hashAll(value.map(_deepHash));
  }
  if (value is Map) {
    int result = 0;
    for (final MapEntry<Object?, Object?> entry in value.entries) {
      result += (_deepHash(entry.key) * 31) ^ _deepHash(entry.value);
    }
    return result;
  }
  if (value is double && value.isNaN) {
    // Normalize NaN to a consistent hash.
    return 0x7FF8000000000000.hashCode;
  }
  if (value is double && value == 0.0) {
    // Normalize -0.0 to 0.0 so they have the same hash code.
    return 0.0.hashCode;
  }
  return value.hashCode;
}


/// The physical/logical carrier a `TransportDevice` was discovered or
/// connected over. Bluetooth is the only carrier a real implementation
/// exists for (T03b/T03c); `wifiDirect`/`internet` are named here because
/// they are part of the shape every future transport must fit (ADR-0004),
/// not because either is implemented.
enum TransportType {
  bluetooth,
  wifiDirect,
  internet,
}

/// Lifecycle state of a connection to a given device id.
enum ConnectionState {
  connecting,
  connected,
  disconnected,
  failed,
}

/// A device/peer as seen by the native transport layer, before any app-level
/// identity/session concept is layered on top (that's `core/auth`,
/// ADR-0005 — out of scope here).
class TransportDevice {
  TransportDevice({
    required this.id,
    required this.displayName,
    required this.type,
    this.rssi,
  });

  String id;

  String displayName;

  TransportType type;

  /// Signal strength in dBm, when the native layer can supply it (e.g.
  /// Android's Bluetooth scan result RSSI). Null when unavailable.
  ///
  /// Contract-only as of E04-B03: no native implementation populates this
  /// field yet — wiring real RSSI is E05's job (FR-ROUTE-001, FR-ROUTE-002).
  int? rssi;

  List<Object?> _toList() {
    return <Object?>[
      id,
      displayName,
      type,
      rssi,
    ];
  }

  Object encode() {
    return _toList();  }

  static TransportDevice decode(Object result) {
    result as List<Object?>;
    return TransportDevice(
      id: result[0]! as String,
      displayName: result[1]! as String,
      type: result[2]! as TransportType,
      rssi: result[3] as int?,
    );
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  bool operator ==(Object other) {
    if (other is! TransportDevice || other.runtimeType != runtimeType) {
      return false;
    }
    if (identical(this, other)) {
      return true;
    }
    return _deepEquals(id, other.id) && _deepEquals(displayName, other.displayName) && _deepEquals(type, other.type) && _deepEquals(rssi, other.rssi);
  }

  @override
  // ignore: avoid_equals_and_hash_code_on_mutable_classes
  int get hashCode => _deepHash(<Object?>[runtimeType, ..._toList()]);

  @override
  String toString() {
    return 'TransportDevice(id: $id, displayName: $displayName, type: $type, rssi: $rssi)';
  }
}


class _PigeonCodec extends StandardMessageCodec {
  const _PigeonCodec();
  @override
  void writeValue(WriteBuffer buffer, Object? value) {
    if (value is int) {
      buffer.putUint8(4);
      buffer.putInt64(value);
    }    else if (value is TransportType) {
      buffer.putUint8(129);
      writeValue(buffer, value.index);
    }    else if (value is ConnectionState) {
      buffer.putUint8(130);
      writeValue(buffer, value.index);
    }    else if (value is TransportDevice) {
      buffer.putUint8(131);
      writeValue(buffer, value.encode());
    } else {
      super.writeValue(buffer, value);
    }
  }

  @override
  Object? readValueOfType(int type, ReadBuffer buffer) {
    switch (type) {
      case 129:
        final value = readValue(buffer) as int?;
        return value == null ? null : TransportType.values[value];
      case 130:
        final value = readValue(buffer) as int?;
        return value == null ? null : ConnectionState.values[value];
      case 131:
        return TransportDevice.decode(readValue(buffer)!);
      default:
        return super.readValueOfType(type, buffer);
    }
  }
}

/// Host-side API: Dart calls into native Kotlin.
class TransportApi {
  /// Constructor for [TransportApi]. The [binaryMessenger] named argument is
  /// available for dependency injection. If it is left null, the default
  /// BinaryMessenger will be used which routes to the host platform.
  TransportApi({BinaryMessenger? binaryMessenger, String messageChannelSuffix = ''})
      : pigeonVar_binaryMessenger = binaryMessenger,
        pigeonVar_messageChannelSuffix = messageChannelSuffix.isNotEmpty ? '.$messageChannelSuffix' : '';
  final BinaryMessenger? pigeonVar_binaryMessenger;

  static const MessageCodec<Object?> pigeonChannelCodec = _PigeonCodec();

  final String pigeonVar_messageChannelSuffix;

  Future<void> startDiscovery() async {
    final pigeonVar_channelName = 'dev.flutter.pigeon.nexora.TransportApi.startDiscovery$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(null);
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    _extractReplyValueOrThrow(
        pigeonVar_replyList,
        pigeonVar_channelName,
        isNullValid: true,
    )
    ;
  }

  Future<void> stopDiscovery() async {
    final pigeonVar_channelName = 'dev.flutter.pigeon.nexora.TransportApi.stopDiscovery$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(null);
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    _extractReplyValueOrThrow(
        pigeonVar_replyList,
        pigeonVar_channelName,
        isNullValid: true,
    )
    ;
  }

  Future<bool> connect(String deviceId) async {
    final pigeonVar_channelName = 'dev.flutter.pigeon.nexora.TransportApi.connect$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(<Object?>[deviceId]);
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    final Object? pigeonVar_replyValue = _extractReplyValueOrThrow(
        pigeonVar_replyList,
        pigeonVar_channelName,
        isNullValid: false,
    )
    ;
    return pigeonVar_replyValue! as bool;
  }

  Future<void> disconnect(String deviceId) async {
    final pigeonVar_channelName = 'dev.flutter.pigeon.nexora.TransportApi.disconnect$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(<Object?>[deviceId]);
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    _extractReplyValueOrThrow(
        pigeonVar_replyList,
        pigeonVar_channelName,
        isNullValid: true,
    )
    ;
  }

  /// Runs on a dedicated background `TaskQueue` (not the platform/UI thread)
  /// — E04-B11: this method's native implementation
  /// (`BluetoothTransport.send`) does a genuine bounded blocking wait
  /// (`SEND_TIMEOUT_MS = 3_000L`) for the write to settle, and without this
  /// annotation Pigeon dispatches a plain `@HostApi()` method on the
  /// platform thread by default — a designed multi-second wait there is a
  /// real defect regardless of its exact user-visible symptom. Confirmed
  /// live: `send()` now genuinely runs on a background worker thread
  /// (`flutter-worker-2`, not the platform thread), and the write itself
  /// completes near-instantly with the connection staying up afterward.
  /// (An earlier version of this comment additionally cited a repeating
  /// `InputDispatcher "not responsive"` warning as ANR evidence for this
  /// bug specifically — retracted, see `E04-B11.md`'s own on-device
  /// verification: that warning also reproduces with zero Bluetooth
  /// activity at all, so it was never valid evidence for this claim.) This
  /// does not change `send()`'s contract (same timeout, same
  /// disconnect-on-timeout behavior) — only which thread blocks.
  Future<bool> send(String deviceId, Uint8List bytes) async {
    final pigeonVar_channelName = 'dev.flutter.pigeon.nexora.TransportApi.send$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(<Object?>[deviceId, bytes]);
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    final Object? pigeonVar_replyValue = _extractReplyValueOrThrow(
        pigeonVar_replyList,
        pigeonVar_channelName,
        isNullValid: false,
    )
    ;
    return pigeonVar_replyValue! as bool;
  }

  /// Requests this device become discoverable to nearby peers for a fixed,
  /// time-boxed window (120s), via Android's own
  /// `ACTION_REQUEST_DISCOVERABLE` system dialog (E04-B09,
  /// `FR-DISC-001`) — the human-decided discoverability mechanism (not a
  /// continuous listen-only scan mode). Fire-and-forget from the Dart side:
  /// the OS system dialog handles user confirmation, and there is no
  /// return value or settled-state event to await.
  Future<void> requestDiscoverable() async {
    final pigeonVar_channelName = 'dev.flutter.pigeon.nexora.TransportApi.requestDiscoverable$pigeonVar_messageChannelSuffix';
    final pigeonVar_channel = BasicMessageChannel<Object?>(
      pigeonVar_channelName,
      pigeonChannelCodec,
      binaryMessenger: pigeonVar_binaryMessenger,
    );
    final Future<Object?> pigeonVar_sendFuture = pigeonVar_channel.send(null);
    final pigeonVar_replyList = await pigeonVar_sendFuture as List<Object?>?;

    _extractReplyValueOrThrow(
        pigeonVar_replyList,
        pigeonVar_channelName,
        isNullValid: true,
    )
    ;
  }
}

/// Flutter-side API: native Kotlin calls into Dart.
abstract class TransportEventsApi {
  static const MessageCodec<Object?> pigeonChannelCodec = _PigeonCodec();

  void onDeviceDiscovered(TransportDevice device);

  void onDeviceLost(String deviceId);

  void onConnectionStateChanged(String deviceId, ConnectionState state);

  void onDataReceived(String deviceId, Uint8List bytes);

  /// Link-quality signal for a given neighbor, when the native layer can
  /// measure it (round-trip latency, observed packet loss).
  ///
  /// Contract-only as of E04-B03: declared here so `RoutingEngine` has a
  /// stable production event to consume, but nothing in this repo calls
  /// this method yet — no native implementation emits it. Wiring real
  /// Bluetooth latency/loss measurements into this event and calling
  /// `RoutingEngine.recordLinkMeasurement` from it is E05's job
  /// (FR-ROUTE-001, FR-ROUTE-002). Do not synthesize values here.
  void onLinkQuality(String deviceId, int latencyMs, double lossRate);

  static void setUp(TransportEventsApi? api, {BinaryMessenger? binaryMessenger, String messageChannelSuffix = '',}) {
    messageChannelSuffix = messageChannelSuffix.isNotEmpty ? '.$messageChannelSuffix' : '';
    {
      final pigeonVar_channel = BasicMessageChannel<Object?>(
          'dev.flutter.pigeon.nexora.TransportEventsApi.onDeviceDiscovered$messageChannelSuffix', pigeonChannelCodec,
          binaryMessenger: binaryMessenger);
      if (api == null) {
        pigeonVar_channel.setMessageHandler(null);
      } else {
        pigeonVar_channel.setMessageHandler((Object? message) async {
          final List<Object?> args = message! as List<Object?>;
          final TransportDevice arg_device = args[0]! as TransportDevice;
          try {
            api.onDeviceDiscovered(arg_device);
            return wrapResponse(empty: true);
          } on PlatformException catch (e) {
            return wrapResponse(error: e);
          }          catch (e) {
            return wrapResponse(error: PlatformException(code: 'error', message: e.toString()));
          }
        });
      }
    }
    {
      final pigeonVar_channel = BasicMessageChannel<Object?>(
          'dev.flutter.pigeon.nexora.TransportEventsApi.onDeviceLost$messageChannelSuffix', pigeonChannelCodec,
          binaryMessenger: binaryMessenger);
      if (api == null) {
        pigeonVar_channel.setMessageHandler(null);
      } else {
        pigeonVar_channel.setMessageHandler((Object? message) async {
          final List<Object?> args = message! as List<Object?>;
          final String arg_deviceId = args[0]! as String;
          try {
            api.onDeviceLost(arg_deviceId);
            return wrapResponse(empty: true);
          } on PlatformException catch (e) {
            return wrapResponse(error: e);
          }          catch (e) {
            return wrapResponse(error: PlatformException(code: 'error', message: e.toString()));
          }
        });
      }
    }
    {
      final pigeonVar_channel = BasicMessageChannel<Object?>(
          'dev.flutter.pigeon.nexora.TransportEventsApi.onConnectionStateChanged$messageChannelSuffix', pigeonChannelCodec,
          binaryMessenger: binaryMessenger);
      if (api == null) {
        pigeonVar_channel.setMessageHandler(null);
      } else {
        pigeonVar_channel.setMessageHandler((Object? message) async {
          final List<Object?> args = message! as List<Object?>;
          final String arg_deviceId = args[0]! as String;
          final ConnectionState arg_state = args[1]! as ConnectionState;
          try {
            api.onConnectionStateChanged(arg_deviceId, arg_state);
            return wrapResponse(empty: true);
          } on PlatformException catch (e) {
            return wrapResponse(error: e);
          }          catch (e) {
            return wrapResponse(error: PlatformException(code: 'error', message: e.toString()));
          }
        });
      }
    }
    {
      final pigeonVar_channel = BasicMessageChannel<Object?>(
          'dev.flutter.pigeon.nexora.TransportEventsApi.onDataReceived$messageChannelSuffix', pigeonChannelCodec,
          binaryMessenger: binaryMessenger);
      if (api == null) {
        pigeonVar_channel.setMessageHandler(null);
      } else {
        pigeonVar_channel.setMessageHandler((Object? message) async {
          final List<Object?> args = message! as List<Object?>;
          final String arg_deviceId = args[0]! as String;
          final Uint8List arg_bytes = args[1]! as Uint8List;
          try {
            api.onDataReceived(arg_deviceId, arg_bytes);
            return wrapResponse(empty: true);
          } on PlatformException catch (e) {
            return wrapResponse(error: e);
          }          catch (e) {
            return wrapResponse(error: PlatformException(code: 'error', message: e.toString()));
          }
        });
      }
    }
    {
      final pigeonVar_channel = BasicMessageChannel<Object?>(
          'dev.flutter.pigeon.nexora.TransportEventsApi.onLinkQuality$messageChannelSuffix', pigeonChannelCodec,
          binaryMessenger: binaryMessenger);
      if (api == null) {
        pigeonVar_channel.setMessageHandler(null);
      } else {
        pigeonVar_channel.setMessageHandler((Object? message) async {
          final List<Object?> args = message! as List<Object?>;
          final String arg_deviceId = args[0]! as String;
          final int arg_latencyMs = args[1]! as int;
          final double arg_lossRate = args[2]! as double;
          try {
            api.onLinkQuality(arg_deviceId, arg_latencyMs, arg_lossRate);
            return wrapResponse(empty: true);
          } on PlatformException catch (e) {
            return wrapResponse(error: e);
          }          catch (e) {
            return wrapResponse(error: PlatformException(code: 'error', message: e.toString()));
          }
        });
      }
    }
  }
}
