// core/routing_engine — placeholder only; not genesis scope.
//
// docs/conventions.md lists this folder as "cost-function + migration
// logic (feature epic, not genesis)". Nothing to build here yet — this
// file exists so the folder is not silently empty/missing from the tree.
